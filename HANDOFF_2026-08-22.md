# 《最强水果》交接文档 — 2026-08-22

> 本文用于把当前聊天窗口的上下文交接给新窗口。新窗口请先读 `D:\0818new\最强水果\.workbuddy\memory\MEMORY.md`（项目长期基线）与本文件，再读 `.workbuddy\memory\2026-08-22.md`（当日流水）即可无断点续作。
> 若 MEMORY.md 与本文冲突，以 MEMORY.md 为准（本文是其 2026-08-22 快照）。

---

## 0. 一行恢复指令（新窗口先跑）

```
先 Read D:\0818new\最强水果\.workbuddy\memory\MEMORY.md 和 D:\0818new\最强水果\HANDOFF_2026-08-22.md 和 D:\0818new\最强水果\.workbuddy\memory\2026-08-22.md，
然后 cd /d/0818new/最强水果 跑：git status / git log --oneline -3 / git branch --show-current / netstat -ano | grep :5173
确认 HEAD=433f182、分支=foundation-02-canonical、Dev Server 在 5173 监听，再等我下一条任务。
```

---

## 1. 项目核心定位（一句话）

极简策略 + 事前组装 + 自动物理战斗。玩家循环：「我觉得这样能赢 → 看它自己打 → 原来问题在这 → 改一下再试」。判断机制是否保留的唯一标准：**它是否让玩家做出不同的下一步动作？**

## 2. 指令优先级（3 级）

1. **最新明确的单次 WorkBuddy 执行指令**（最高）
2. **项目稳定边界（MEMORY.md / 本文）**
3. 历史设计草稿 / 旧方案

冲突处理硬规则：最新任务与核心定位冲突时，**不自行默默实现冲突方案**，必须先指出「冲突点 + 体验/架构后果 + 最小建议修正」。

---

## 3. 当前状态（2026-08-22）

| 项 | 状态 |
|---|---|
| 分支 | `foundation-02-canonical` |
| 本地 HEAD | `433f182` |
| 远端 `refs/heads/foundation-02-canonical` | `433f182`（已 `ls-remote` 核验） |
| Dev Server | PID 7156 监听 127.0.0.1:5173（Badge 显示 `433f182`，VITE v8.2.1） |
| 全量测试 | 536/536 通过（84 文件） |
| tsc --noEmit | 0 errors |

### 已完成队列
- **F-PROJ-HF1**（commit `f5065cd`）：机枪/喷火器小弹体动量收敛（`projectileMass` 1→0.1 / 1→0.03，仅调质量；speed/damage/射程/视觉/Physics 不变）。
- **Q14-A/B-R2-FINAL**（`1643c25`）：机枪短弹迹 + 喷火器连续 Fire Jet。
- **F-PRESENT-1**（`fc30682`）：高频伤害数字聚合。
- **Q15**（commit `433f182`）：正常玩家游戏主循环 V1 —— 见 §4。

### 挂起项
- **Q14-C（HOLD）**：等你做重设计方案后再处理。本轮未动，不在 Q15 范围内。属方案漂移红线里的「复杂机关成胜负主体 / Body 免费攻击」一类，需你明确 redesign 后才开工。

---

## 4. Q15 交付明细（已完成、已推送、已核验）

冻结范围：新 Content / Physics / 正式参数全部冻结；仅做玩家流程编排层。

交付文件（单 commit `433f182`，4 个文件，434 增 / 31 删）：
- `src/player/opponentPool.ts`（新增）：6 套手动对手 Build，仅西瓜/香蕉 + 正式 PART_OPTIONS，无 wedge/ramHead/lifter/旋锤 HOLD 内容；`cloneBuildDraft` 深拷贝 + `nextOpponentIndex`（含 size 0 安全）。
- `src/core/buildPersistence.ts`（新增）：localStorage key `strongfruit.playerBuild.v1`；校验 body/part 已知 + `validateSnapshot` 合法才接受，非法旧档返回 null → 默认 `silDraft('watermelonBody')`。
- `src/main.ts`（改）：`playerPhase('garage'|'matchPreview')` + `matchedIndex` 状态机；Garage CTA「开始战斗」→「寻找对手」；MatchPreview 新 `matchBar`（调整配置 / 开始战斗）；Result「下一场」(`nextMatch` 换对手) / 「调整配置」(`adjustConfig` 回 Garage)；`updateStartButton` 重写；B 面板 PROD 只读（`import.meta.env.DEV`）；`refreshFromEdit` 末位 `savePlayerBuild`；Dev Tools 经 `TOOLS_DEV_VISIBLE`（PROD 隐藏 / DEV 可见）保留且隔离。
- `tests/q15PlayerLoop.test.ts`（新增，16 用例）：对手池 / 持久化全部边界。

验收对应（7 项全过）：刷新即进 Garage（无开发工具依赖）｜整圈 Garage→Match→Battle→Result→NextMatch｜连续 3 场不 Reset｜每场对手变化｜调整 Build 重入 Battle｜刷新保留 Build｜Scenario/Dev 仍可用无回归。

---

## 5. 关键边界速查（勿破）

- **物理**：2D 侧视（X/Y 平移 + Z 轴旋转），禁 3D Roll/Yaw。数字定能力，Physics 定能力能否表达。同 Build 同条件结果大体稳定，禁随机命中/伤害/散布。
- **四类结构**：Body（碰撞几何/质量分布，无主动攻击）、Movement（V1 只 Wheel，真实 Ground Contact 驱动）、Weapon（直接 HP 伤害）、Gadget（改碰撞/距离/姿态，不带大额 Direct Damage）。
- **Slot/Energy/Mass 分工**：Slot 限空间、Energy 限装配预算（超载直接拒绝穿戴）、Mass 制造 Runtime 物理后果（禁 MaxWeight 硬门槛）。
- **Auto Battle**：玩家不操作，AI 不替玩家聪明赢。V1 持续朝唯一敌人驱动，不后退/拉距/自动瞄准。攻击方向来自 Body 姿态 + Hardpoint 世界方向。允许挥空/射空/互扰。
- **Damage/Force 独立**：Damage 不自动映射 Knockback。Physics Contact 统一进 Contact Router，Damage 统一进 Damage Resolver，Renderer 不参与判定。
- **Arena**：V1 平地 + 左右普通墙。Convergence = Active→Warning→Closing→End，刺墙真实 Collider + Hazard。结果只有 Win/Lose，不建 Draw（同帧双死随机兜底）。
- **Quality**：白绿蓝紫橙红粉彩。跨品质核心 Behavior/物理身份稳定，尺寸/Collider/Mass/COM 基本不变，成长走 HP/Energy/Functional Hardpoint。
- **Physics Lab**：必须直接调正式 Battle Runtime，禁测试场专用物理。第三层体验验收必须 1x + Debug 关闭。

## 6. 三层验收

1. 技术正确（tests/build/Runtime）
2. 方案落地（设计进正式 Runtime）
3. 实际体验成立（正常速度 + Debug 关 + 不听解释，玩家能感知/理解/归因/决策）

只有第三层通过才算设计完成。**开发完成 ≠ 体验通过 ≠ 进入正式主线。**

## 7. 方案漂移红线（出现即主动提醒，不默认实现）

复杂职业 AI / 自动瞄准 / 自动拉距 / 大量隐藏 Buff / Body 免费攻击 / Weapon-Gadget 独立槽 / 高品质全维度碾压 / 复杂机关成胜负主体 / MaxWeight 硬限制 / 动画保证命中 / Gadget 偷偷大额伤害 / 大量例外规则 / 稳定最优 Build 通吃但只加 Content / Debug 成立但玩家感知不到。

---

## 8. Canonical Physics Foundation（2026-08-17，feature/foundation-02-canonical）

统一后的物理基线，后续所有 Queue 必须基于此（勿再用旧 gravity 0.01）：

- **gravity.scale = 0.0001**（唯一值）。根因：Matter Verlet 积分 `velocity += (force/mass)*deltaTime²`，FIXED_DT²≈277.8 放大 gravity.scale；0.01 会让 body.velocity.y 单调累积到 ~278（碰撞求解器只改 positionPrev 不回写 velocity）。
- **固定步进**：Drive/Behavior 必须在每个 FIXED_DT 的 Engine.update 之前执行（PhysWorld.step 的 onBeforeStep 回调）；战斗时间按 steps*FIXED_DT 推进，不按渲染帧累计。
- **setMeta 已传播到 compound sub-part**：Matter 碰撞事件 pair.bodyA/bodyB 是 sub-part（parts[0]=parent 被 Detector 跳过），不传 meta 则 Contact Router 读不到 Owner。
- **【F-02A 已解决】接触相对速度**：dispatch 改用 `collision.parentA/parentB`（父刚体 COM）+ 接触点公式 `vPoint = vCOM + ω×r`（r = supports[0] − COM），bodyA/bodyB 仍传 sub-part。纯平移 3.6910=COM 投影（旧值恒 0）；旋转 ω=0.373 时报 26.39 vs 仅 COM 0.81（差 32 倍）。
- **Matter 速度读取时机铁律**：collisionStart/collisionActive 事件回调内 velocity 才是真实值（积分后、求解前）；Resolver.solveVelocity 在事件之后回写 velocity（实测 1.845 → 0.00014），事件外读到的不是真实接近速度。beforeSolve 事件参数不含 collision pairs（在 Detector 之前触发）。
- **git 身份**：本仓库 `xiaoyue <xiaoyue@local>`（新会话需 `git config user.name/email` 重新设置）。push 走 SSH（`git@github.com:HubinAI/fruits.git`）。
- **git ref 写入不可靠（重要）**：此环境 `git commit` 后分支 ref 常不落盘；`git update-ref` / `git branch -f` 也静默失败（exit 0 但不写文件，还会清掉已建 refs/heads 子目录）。**可靠恢复路径（大海已授权，项目惯例）**：`mkdir -p .git/refs/heads/<branch> && printf '%s\n' <full-sha> > .git/refs/heads/<branch>`，commit 后必须立刻 `git log` 验证 ref 落盘，未落盘则用 fsck/cat-file 确认 commit 对象完整后手动写 ref，再 push。

---

## 9. 环境坑（实测，必读）

- **vitest 运行环境问题（2026-08-18 发现）**：`node .../vitest run <file>` 默认 forks/threads 池所有测试报 `TypeError: Cannot read properties of undefined (reading 'config')`，0 test 全失败；与项目代码无关。根因：@vitest/runner 的 `runner` 全局为 undefined → describe 链抛 reading 'config'，属 vitest 4.1.10 在 Node 24.18 / Windows 的双实例故障。**规避（已实测 10/10 通过）**：所有 vitest 命令必须带 `--pool=vmThreads`（或 `--pool=vmForks`）+ `--maxWorkers=1`。后续所有 vitest 命令务必带 `--pool=vmThreads`，否则会误报"测试失败"。
- **Git push / 凭据经验（2026-08-19）**：remote 是 SSH，走 SSH key，无 credential dialog；`git config --global credential.helper manager`（单键覆盖，必须整行覆盖旧的 `!` 前缀 exe 路径行）。`git push --dry-run` 退出码：124=超时 / 128=fatal / 0=通过。
- **Dev Server Badge（virtual:runtime-info）**：显示 branch @ sha，vite 端口 5173 + `strictPort`。**进程命名空间坑**：Bash `kill` 够不到 PowerShell 启动的 vite。重启方法：Bash `netstat -ano | grep :5173` 拿 PID → PowerShell `Stop-Process -Id <PID> -Force` 终止 → 再启动 `./node_modules/.bin/vite --port 5173 --strictPort`（Bash `run_in_background`）。Badge 在 vite 启动瞬间从新 HEAD 重算。
- **工具层坑（中文路径 + plank/planck 显示伪影，2026-08-19）**：
  - `git status` 把 `planck` 显示成 `plank`（无 c）= 中文路径显示伪影，真实文件名始终是 `planck`（有 c）。用伪影名去 ls/grep/cat/Read 会全失败。正确做法：用 `ls <dir>/` 拿真实拼写，或用 Grep/Read 直接按真实路径读。
  - Git Bash 对中文目录相对/绝对路径偶发不稳；git 命令用裸相对路径（如 `git diff HEAD -- <file>`）几乎都稳。
  - Read 工具对绝对中文路径偶发解析失败（"Not found"）；Bash 用相对 `cat`/`ls` 立即兜底。

---

## 10. 关键文件地图（近期）

| 文件 | 作用 |
|---|---|
| `src/main.ts` | 玩家流程编排（uiMode / battleState / playerPhase / matchedIndex）、Build 编辑器接线、Preview/HUD/Result |
| `src/player/opponentPool.ts` | Q15 对手池（6 套）+ `cloneBuildDraft` / `nextOpponentIndex` |
| `src/core/buildPersistence.ts` | Q15 localStorage Build 持久化（含 validateSnapshot 校验） |
| `src/lab/buildEditorModel.ts` | `BuildDraft` 形状、`EMPTY_SLOT`、`buildSnapshotFromDraft`、`EDITABLE_FUNCTIONAL_DEF_IDS` |
| `src/core/buildValidator.ts` | `validateSnapshot(snap, registry)`（≥1 Weapon / Energy≤容量 / hardpoint 合法） |
| `src/core/content.ts` | 正式 PART_OPTIONS / bodies（西瓜 cap110/hp1100、香蕉 cap90/hp900）/ 能量档 |
| `tests/q15PlayerLoop.test.ts` | Q15 对手池 + 持久化 16 用例 |
| `src/core/partOptions.ts` | 正式 part 选项枚举 |

---

## 11. 续作建议（下一步）

1. **短期**：无待办 bug；Q15 已交付。若要做真人第三层验收，请在浏览器开 `http://127.0.0.1:5173/`，刷新后进 Garage，点「寻找对手」走完整圈，确认 3 连胜对手变化、刷新保留 Build。
2. **中期（等你决策）**：**Q14-C 重设计**——当前 HOLD。需你给出 redesign 方案（避开方案漂移红线：不把复杂机关做成胜负主体、不 Body 免费攻击）后再开工。
3. **常规**：新 Queue 接续时，先确认是否触碰 §5 冻结边界；若触碰，按 §2 冲突处理先提醒。所有 vitest 带 `--pool=vmThreads --maxWorkers=1`；commit 后 `git log` 验 ref；push 走 SSH；改完重启 Dev Server 刷新 Badge。

---

## 12. 验证命令速查

```bash
# 测试（务必带 --pool=vmThreads）
node node_modules/vitest/vitest.mjs run tests/q15PlayerLoop.test.ts --pool=vmThreads --maxWorkers=1
node node_modules/vitest/vitest.mjs run --pool=vmThreads --maxWorkers=1

# 类型
node node_modules/typescript/bin/tsc --noEmit

# 状态 / 推送核验
git status
git log --oneline -3
git branch --show-current
git ls-remote origin foundation-02-canonical

# Dev Server 重启
netstat -ano | grep :5173          # Bash 拿 PID
# Stop-Process -Id <PID> -Force    # PowerShell 终止
./node_modules/.bin/vite --port 5173 --strictPort   # Bash run_in_background 重启
```
