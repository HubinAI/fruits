# 交接文档 2026-09-12 —— PBL-B1 触发停止条件，**未实现 Arena B**（如实报告）

## 一、结论（一句话）

**按你 Queue 的「停止条件」停止：Arena B 无法在不改变共享车辆尺寸 / 不缩放镜头 / 不加地形的前提下成立。**
390 宽竖屏下，两辆共享测试车的真实 collider 外接框合计 **410–498 px**（1v1）/ **828–872 px**（1v3），
而可活动区只有 **366 px** —— 两车**必然且持续互嵌**（静止解算后残余 43–124 px，真实战斗 5s 后仍 46–125 px），
这不是「贴脸」，是**车体几何放不下**。零行生产代码改动，未 commit 功能。

`PBL-A1`（`2ee47bb`）仍为当前 HEAD，三路 SHA 一致，工作区干净。

---

## 二、Git / 状态（已核实）

| 项 | 值 |
|---|---|
| HEAD | `2ee47bb3f4ab7556d3feac4e5c0f1c9cee2c1197` |
| LOCAL = REF = REMOTE | `2ee47bb`（`git fetch` 后核对） |
| 分支 | `prototype-portrait-battle-lab` |
| 工作区 | 仅历史未跟踪交接文档（与 A1 交付后一致） |
| 生产代码改动 | **0 行**（未新建 `arenaB.ts`，未改 `arenaA.ts` / `lab.ts` / `scene*` / E2E / 任何正式模块） |
| 临时探针 | 3 个 `_scratch_b1*.test.ts` 已全部删除（`scratch 剩余: 0`） |

---

## 三、实测证据（全部可复现，非估算）

### 3.1 几何：所需横向宽度 vs 竖屏可活动区

口径：
- 竖屏舞台 390×844（`PORTRAIT_LOGICAL_W/H`）；Arena B 左右实体边界厚度 12 → 可活动区 = `390 − 2×12` = **366 px**；
- 车辆宽度 = **整车真实 collider 外接框**（车身 + 轮 + 全部功能件，经 `settlePlanckVehicleToRestPose` 贴地后的真实几何），
  不是车身 collider 宽度。

| 组合 | 整车外接框总宽 | 缺口（总宽 − 366） | 含 4px 净空 + 双墙所需宽度 | 相当于舞台宽 |
|---|---|---|---|---|
| WatermelonHeavyCannon / Chaser | 454.0 | **+88.0** | 486.0 | **×1.25** |
| WatermelonHeavyCannon / RangedTurret | 410.0 | **+44.0** | 442.0 | **×1.13** |
| BananaChargeHammer / Chaser | 498.0 | **+132.0** | 530.0 | **×1.36** |
| BananaChargeHammer / RangedTurret | 454.0 | **+88.0** | 486.0 | **×1.25** |
| WatermelonHeavyCannon / LightSwarm3 | 827.7 | **+461.7** | 859.7 | **×2.20** |
| BananaChargeHammer / LightSwarm3 | 871.7 | **+505.7** | 903.7 | **×2.32** |

单车真实宽度：`WatermelonHeavyCannon` 205.0 / `BananaChargeHammer` 249.0 / `Chaser`(OPP-16) 249.0 /
`RangedTurret`(OPP-03) 205.0 / `LightSwarm3` 单车(OPP-14) 207.6。

**补充口径（重要）**：若只看车身 collider（不含前伸武器），1v1 总宽 344–384 px ——
`Watermelon/RangedTurret` 344（−22，勉强放得下）、`Watermelon/Chaser` 364（−2，恰好不剩净空）、
`Banana/Chaser` 384（**+18，仍放不下**）。也就是说：**即使忽略武器，香蕉车组合的车身本身就已经超过 366**；
而武器前伸（炮向敌侧 +35、锤向敌侧 +59）必然插入对方车体，使 1v1 全部组合都放不下。

### 3.2 静止解算（无任何驱动 240 步 ≈ 4s）

出生即按**最大分离布局**（玩家贴左墙内表面、敌人贴右墙内表面，几何最优解）摆放，之后不做任何驱动：

| 组合 | 出生瞬间互嵌 | 静止解算后残余互嵌 | 解算期间峰值速度 |
|---|---|---|---|
| WatermelonHeavyCannon / RangedTurret | 52×71 | **43×56** | 0.218 |
| BananaChargeHammer / Chaser | 140×71 | **124×61** | 0.296 |
| BananaChargeHammer / RangedTurret | 96×71 | **90×62** | 0.230 |
| LightSwarm3 | 多向 55×71 / 208×82 | 多向 41–165 px | 0.258–0.293 |

残余互嵌 ≈ **总宽 − 可活动区**（44↔43、88↔90、132↔124），说明这是**几何硬约束**，
不是解算器失败：两车被两侧实体边界夹住，物理上无法再分开。

### 3.3 真实战斗 5s（正式 `drivePlanckVehicle` + 正式 Behavior 全接入）

| 组合 | 5s 后残余互嵌 | 说明 |
|---|---|---|
| WatermelonHeavyCannon / RangedTurret | **46×51** | 全程互嵌 |
| BananaChargeHammer / Chaser | **125×60** | 全程互嵌 |
| BananaChargeHammer / RangedTurret | **90×63** | 全程互嵌 |
| WatermelonHeavyCannon / LightSwarm3 | 多向 6–113 px | 四车堆叠 |
| BananaChargeHammer / LightSwarm3 | 多向 12–206 px | 四车堆叠 |

即：整个战斗过程中车辆**始终嵌在对方体内**，不构成「侧视物理决斗」。

---

## 四、为什么这构成「停止条件」而不是「B 的真实缺陷」

你的 Queue 停止条件原文：

> 如果 B 只有通过改变共享车辆尺寸、射程、伤害或镜头缩放才能成立，停止并如实报告。

实测把「出路」逐一排除：

| 出路 | 是否可用 | 依据 |
|---|---|---|
| 缩小车辆 | 你明文**禁止**（「禁止因为横向空间不足而缩小车辆」） | — |
| 缩放 / 拉远镜头 | 你明文**禁止**（「不用 Camera zoom 掩盖问题」） | — |
| 加地形 / 台阶 / 平台错位摆放 | 你明文**禁止**（「不加坡道、平台」「不加地形来补救空间不足」） | — |
| 把 Arena 做到比舞台更宽 | 等价于「镜头看不到左右实体边界」→ 属镜头处理，且出生点会跑出固定竖屏摄像机 | 固定摄像机约束 |
| 出生时把两车在纵向错开（叠放） | 与「底部平地」「侧视」语义冲突，且落地后仍回到 3.2 的硬约束 | — |
| 只调低敌人速度 / 提高射程 / 改伤害 | 你明文**禁止**，且对**几何放不下**完全无效 | 3.1 是纯几何 |

关键区别：**「几乎开局即贴脸」我可以原样保留为实验结论；但实测结果是「开局即互嵌 88–140 px，且全程互嵌」**——
前者是紧凑，后者是车体放不下。二者不是同一件事，后者正好落在停止条件上。

---

## 五、我**没有**做的事（严格遵守边界）

- 未新建 `src/lab/portraitBattleLab/arenaB.ts`，未改 `arenaA.ts` / `lab.ts` / `arenaScene.ts` / `scene.ts` / `state.ts`
  / `constants.ts` / `entities.ts` / `testData.ts`；
- 未改 `tests/portraitBattleLab*.test.ts`、`tests/_e2e_portrait_battle_lab.cjs`（Arena A 账本仍是 `25200`，未被污染）；
- 未改任何正式 Battle / Physics / Content / 平衡数值（HP / 伤害 / CD / 射程 / 弹丸 / 质量）；
- 未改 `.workbuddy` skill 或其它开发工具说明（本 Queue 明文禁止）；
- 未调参救场、未加规则补救、未做视觉 polish；
- 未合并回 `foundation-02-wechat`，未部署 Pages。

---

## 六、已复用的正式能力（供你判断后续选项时参考，均已验证可用）

以下正式侧视能力**工程上可直接复用**，本次已实测跑通（探针阶段），
即「Arena B 的物理部分没有技术障碍，**唯一障碍是宽度**」：

- `PlanckWorld({x:0, y:10})` 正式重力；`createStaticBox` 静态地面（friction=1）+ 左右低反弹边界（restitution=0.05）；
- `createPlanckVehicle(..., { policy: 'instance-exclusive', instanceIndex })`（PBL-F2）→ 多实体真实碰撞；
- `settlePlanckVehicleToRestPose`（正式无冲量贴地静置）；
- `drivePlanckVehicle` 正式轮地驱动 + `ContactRouter.handlePlanckContact` 维护的真实 `wheel.grounded`
  → **侧视 motor 门控链完整闭合**（这一点与 Arena A 的关键差异：A 必须自建 topdown 适配器，B 不需要）；
- 正式 `BehaviorRegistry`（cannon / machineGun / hammer / saw / thruster …）与 `DamageResolver` / `CombatEventBus`。

⚠️ 唯一需要 Lab-local 处理的是：**不得复用 `PlanckArenaRuntime`**（它无条件创建左右 Closing 刺墙 + hazard，
无开关）；Arena B 需自建 ground + 左右墙两组静态体（约 30 行）。

---

## 七、需要你裁决的三个选项（我未开工任何一个）

| 选项 | 内容 | 代价 / 风险 |
|---|---|---|
| **B1-A｜确认停止（我的建议）** | 接受「竖屏窄宽度下侧视物理无法承载正式车辆尺寸」为实验结论，B1 关闭 | 0 风险；代价是 390 宽竖屏侧视方案被否 |
| **B1-B｜授权放宽「镜头」约束** | 新增 Queue：允许 Arena B 的竞技场宽于舞台（例如 900 px），固定摄像机改为「固定但可显示更宽逻辑场」 | 需要你明确改写「固定竖屏摄像机」与「不用 Camera zoom」的边界；会改变 Lab 的坐标契约 |
| **B1-C｜授权 Lab-only 缩小规则** | 新增 Queue：允许在 **Lab 内**对 Arena B 使用缩小后的「实验车」（不改正式库） | 与你「与 Arena A 完全相同的 Test Loadout / 基础数值」目标冲突，A/B 对照会失去同源性 |

若你选 B1-B 或 B1-C，请在新 Queue 里写明新的边界，我会按「先查后改」重新调查；
在此之前**不自动推进 PBL-G1**，也不自动实现 B。

---

## 八、Next Action

1. 等你对上面三个选项的回执（默认：确认停止 B1）。
2. 回执后若确认停止：下一条串行 Queue 是 `PBL-G1`（是否开工由你决定，我不自动开始）。
3. 低优先级既有项不变：`KNOWN-WX-COLD-BOOT-PREVIEW-SCALE-01`、mobile drive slot、strip-scroll no clamp、O1/O2 非阻塞优化项。
