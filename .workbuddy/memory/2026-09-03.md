# 2026-09-03 工作日志

## F-GARAGE-INVENTORY-FUSION-P0｜核验 + 补齐交付（非重做）

### 关键前置冲突
- Queue 硬性前置要求 HEAD = `2fade0f`，但当前 HEAD 实为 `f5b283c`，且远端 `origin/foundation-02-wechat` 也指向 `f5b283c` → **local = remote = f5b283c**。
- `f5b283c` 提交标题正是本 Queue，22 文件改动，含 `tests/garageInventoryFusionP0.test.ts`(T1-T24)，声明「全量 vitest 失败回到基线 1」「移除旧金币跨 defId 融合」。
- 结论：本 Queue 已在上一会话实现并推送。经用户确认（AskUserQuestion）采用「核验 + 补齐交付」路线，**不重置、不重做**。

### 验收门结果（以 f5b283c 为交付物）
- targeted：`tests/garageInventoryFusionP0.test.ts` 24/24 全绿（数据模型 A / canFuse B / fuseSameStar C / §4 边界 D / grant 联动 E）。
- `tsc --noEmit`：0 错误。
- 全量 vitest（`--pool=vmForks --maxWorkers=1`）：**1649 passed / 1650，仅 1 失败 = `garageBuildBoardP0.test.ts > T6`**（Queue 预标的已知脆性）；无新增失败。
- 四路构建：`build` / `build:pages` / `build:e2e` / `build:wechat` 全 0。
- repo-health：9/9 OK（脚本 scripts/repo-health.js 只读门禁）。
- RC 干净构建：`npm run build:wechat:rc`（待 RC_EXIT 回填；内嵌 bundle-clean + 四方 SHA 校验）。
- 受控工作树清理：将未跟踪 stray 草稿 `tests/_e2e_backpack_fusion.cjs` 移出仓库至 `/d/0818new/_quarantine/`（非 git stash、可逆），使 RC dirty-gate 通过；其余仅 `.workbuddy/memory` 与根级文档（均被 IGNORED_PREFIXES 忽略）。

### 实际数据模型（partInventory.ts）
- `PartInventory = { [defId]: { one: number; two: number } }`，按 (defId, star) 计副本数，**已支持多星级同存**；未引入实例 UID / 第二套库存。
- 存档：`strongfruit.ownedParts.v2`（JSON + saveVersion 信封），v1 owned-id 数组 → v2 迁移；损坏 → 回退默认 starter 副本。
- `MAX_STAR = 2`（数据模型钉死，仅 1★→2★ 单步跃迁）；Movement 复用 PartInventory（one 计数），`wheelStd` 恒拥有不进库存；Body 走独立 `bodyOwnership`（拥有即解锁，无数量/星级）。

### 合成规则（最小、确定、无经济）
- 5 个「同 defId、同星级」未装备副本 → 1 个同 defId 下一星级；Functional + Movement 可合，Body 不合成。
- `available = owned(star) - equippedCount(defId,star,build)`；`<5` → 禁合并显示 `还差 N 个`；`star>=MAX_STAR` → `已满星` 禁；已装备副本保护、不跨 defId/分类/降星/负库存。
- `fuseSameStar` 原子（consume 5 + add 1 下一星 + 一次 saveInventory）；`canFuse` 预检驱动禁用态；UI 连续点击靠 available 下降二次预检失败天然只执行一次。

### defId 清单
- Functional（OFFICIAL_PARTS, PART_OPTIONS 去 EMPTY）：cannon, hammer, pushRod, spear, laser, rammer, saw, shotgun, thruster, machineGun, flamethrower（11）。STARTER：cannon/hammer/pushRod/spear。
- Movement（OFFICIAL_MOVEMENTS）：smallWheel, largeWheel, heavyWheel（3）。wheelStd 恒拥有。
- Body（OFFICIAL_BODIES）：watermelonBody, bananaBody, pineappleBody, coconutBody（恒默认）+ durianBody, pearBody, mangoBody, orangeBody（新增，默认未获得）（8）。

### 入口与页面
- Garage 顶栏「背包」按钮：`canvasPlayerUIHost.ts:2124-2125` `this.button(BASE_W - 120, dockY + 12, 96, 30, 'nav:backpack', '背包')`（hitArea 与可见同源）；正常 Garage 与 Result-adjust Garage 均可进入（metaPage='garage' 顶栏同源），不进首页。
- 二级页 `garage-inventory`：`drawBackpackPage`（:3109+），分类 combat/movement/body；卡片显示 名称/星级/总数量/已装备数量/可合数量/未获得/已装备（互斥）；底部合成面板 当前★1 / 消耗5 / 产出★2 / 可用数量 / 暖金「合成」；`backpack-fuse` 调用 `onFuse`；关闭重进库存/星级/Build 保持（共用 PartInventory + saveVersion）。

### 与「全部件×1」联动
- `debugGrants.grantAllPartsOnce`（×1 幂等：缺失才补到 1，不重置/不累加/不升星）；`hasAllOfficialDebugContent` 同源读真实库存/拥有，背包卡片拥有态一致；普通微信包（`build:wechat`，无 WECHAT_BADGE/WECHAT_DEBUG_GRANT）无 grant 入口、功能仍正常。

### 已知限制 / 后续
- 浏览器 E2E（§7：Garage→背包→合成→返回→重进，420/844/1280 × DPR1/DPR3）**未提交进 f5b283c**；仅有 draft 已 quarantine 至 `/d/0818new/_quarantine/_e2e_backpack_fusion.cjs`。提交测试为模块层 24 项，Queue 的浏览器级 T1-T3/T18-T20（入口可见性、尺寸几何）由既有 garage shell/integration 套件部分覆盖。
- 拖动优化后续 Queue：**F-GARAGE-TAP-EQUIP-R2**（点卡片→点高亮挂点完成安装；拖动保留为可选高级操作）。本 Queue 未实现（冻结项）。
- 全量 vitest 既有 1 失败 `garageBuildBoardP0.test.ts T6` 非本 Queue 引入，维持已知脆性基线。

## 收口（浏览器 E2E 补交，commit `64de5c2`）
- 经用户「continue」指示，将 quarantine 草稿 `tests/_e2e_backpack_fusion.cjs` 还原进仓库并提交为 `64de5c2`（1 file, +161），已 push → **local = remote = 64de5c2**。
- 内容：真实指针管线+真实可见像素点击；首页→车库→`nav:backpack`→背包页（三类 tab + 部件卡）→选卡→页内 `backpack-fuse` 门控（默认库存<5 不出现）→`nav:garage` 返回车库（不经过 Home）；视口 420×210 / 844×390 / 1280×592 × DPR1/DPR3（6 组合）。
- 沙箱无 Chromium/Edge，未在本环境执行（脚本自身诚实披露）；5→1 消耗与重进保持由 `garageInventoryFusionP0.test.ts` 等价覆盖。playwright-core 已装、`node --check` 通过、引用 hitArea id 与实现一致。
- 重新门禁（新 HEAD 64de5c2）：repo-health 9/9；build/build:pages/build:e2e/build:wechat 全过；RC 干净构建 + bundle-clean PASS + 四方 SHA 一致（64de5c2）。本 Queue 验收全部闭环。

## 收口（沙箱实跑 E2E + 取位崩溃修复，commit `5fdd06a`）
- 用户再「continue」→ 在沙箱实跑浏览器 E2E。沙箱无 Edge/Chromium，先 `npx playwright-core install chromium`（chromium-1234 + headless-shell）。
- 首跑 CRASH：`tapVisibleById` 取 `document.querySelectorAll('canvas')[1]` 在单 canvas 构建下为 undefined → `Cannot read properties of undefined (getBoundingClientRect)`。
- **修复（真实 bug，影响所有环境）**：`tests/_e2e_backpack_fusion.cjs` L48 改 `canvas[1] || querySelector('canvas')`（与 `_e2e_gate.cjs` 兜底一致）；保留 `channel:'msedge'`（真实 Edge 所需）。提交为 `5fdd06a`（1 file, +1/-1），已 push。
- **实跑证据**：`npm run build:e2e` → `tests/_serve_pages.cjs`(E2E_DIR=e2e, 127.0.0.1:8138) + `outputs/_e2e_backpack_fusion_chromium.cjs`（仅把 `channel:'msedge'` 改 bundled chromium 适配沙箱，逻辑同正式脚本）。`E2E_EXIT=0`，末行 `BACKPACK FUSION E2E GATE: 60/60 PASS`。6 视口×DPR 下每组合 10 断言（A 首页 home-garage / B 进装配台 3 分类 / C 真实点击背包+卡片 n=6+三 tab / D 门控默认<5 无合成按钮 / E 返回车库非首页+未穿透首页 / 全程无 pageerror·console.error）全绿。
- 重新 RC 门禁（新 HEAD 5fdd06a）：`node scripts/wechat-rc.js` → `RC_EXIT=0`；bundle-clean PASS；badge `#5fdd06a`、`dirty=false`；四方 SHA 一致（HEAD=rc-build.fullSha=bundle runtimeInfo sha=`5fdd06add3b625ce8ed75b2afb5b5ae3b1ce5c61`）。**local = remote = 5fdd06a**。
- 诚实边界：沙箱默认账号每卡仅 1×1★，覆盖「背包可达+卡片渲染+合成门控+返回上下文」，但浏览器内 5→1 实际消耗未覆盖（需种子库存）；该路径仍由 `garageInventoryFusionP0.test.ts` T10-T16 等价覆盖。
- **F-GARAGE-INVENTORY-FUSION-P0 至此完整闭环**（实现+24单测+浏览器E2E实跑+RC+四方SHA+local=remote）。后续 Queue：F-GARAGE-TAP-EQUIP-R2（点卡→点高亮挂点安装）。

## 调查｜F-WX-RESUME-RENDER-STATE-P0（investigation-only，未改码）
- 接续被暂停的「WeChat resume 跨页 render-state leak」调查。先读 Playbook（前后台/resume 关键词触发）；基线 HEAD `5fdd06a`。
- 完整事件链 trace（DOM→canvas→WebInput→handlePointer→hitArea→uiMode/playerPhase）已逐文件取证：
  game.ts:340 onVisibilityChange → L348 syncViewport('show') → viewportSync.ts:119 syncWechatViewport
  → L148-149 resetBacking(清屏) → L155 runtime.doResize → L158 forceRedraw(dirty=true) → L165-166 loop.start+request
  → 帧 runtime.tick→host.render(L595)→renderBattleFrame(L661)→draw(L1660: ensureSize+hitAreas=[]+按metaPage/lastState绘制)。
- **首个 leak 点（结构位置）**：`viewportSync.ts:145-166` 破坏性/关键动作（resetBacking+doResize）排在恢复动作（forceRedraw+loop.start+request）之前，无 try/finally。
  若 `runtime.doResize`（playerGameRuntime.ts:602→arenaDims+battle.resize+reframePlayerCamera→battle.reframe+getPreviewFramingRect）抛错，
  则 forceRedraw/loop.start 跳过 → loop 在 hide 已 loop.stop(单例 running=false, SingleLoop:42) 后永不重启 → 画面空白/冻结在切后台前最后一帧（可能不同页）= 跨页 leak；连带 hitAreas 不重建→输入链死亡。
  注意 SingleLoop.step 的 try/catch(SingleLoop:69) 只保护 onFrame 内部，不覆盖 syncWechatViewport 同步调用。
- **测试零覆盖**：wechatResumeRenderStateP0.test.ts(C1-C6) 仅走 host.cancelInteraction+runtime.tick，未接入 game.ts onVisibilityChange；
  wechatResumeViewportP0.test.ts(T1-T13) 直调 syncWechatViewport 但**未断言** show 后 loop.isRunning/dirty、且未覆盖 doResize 抛错。真实 resume 入口恢复保证+失败容错=0 覆盖。
- 根因**未锁定为已触发生产 bug**（结构性风险+未测路径）；按纪律**未改码**。报告：`outputs/F-WX-RESUME-RENDER-STATE-P0-调查报告.md`。
- 建议 fix（待批准独立 Queue）：syncWechatViewport 内将 forceRedraw+loop.start+request 置于 try/finally（或 loop.start 前移），使 resume 恢复不被 doResize 成败影响；
  落 T-VP-1(恢复保证)/T-VP-2(doResize抛错仍恢复)/T-VP-3(hitAreas重建)；升级 C1-C6 接入真实 resume 入口；真机复验（Playbook 阶段E）。

## F-GARAGE-INVENTORY-FUSION-R1｜背包信息层级 + 真实 5合1 闭环（commit `9c76c9b`）

### 根因（本 Queue 最有价值的一条经验）
- **`globalThis.__fx` 命名空间被两处独立赋值抢占** → 库存种子句柄被静默抹掉：
  `main.ts:452` 挂表现层特效探针 `{spawnDamage, spawnSpark, spawnLaserBeam, debug}`；
  `canvasPlayerUIHost.ts` 两处（mount L542 / mountScreen L580）挂 `{seedInventory}`。
  两者都是 `= { … }` **整体赋值**（非属性合并），main.ts 后落笔 → 宿主实现被覆盖。
- 症状极具误导性：bundle 里 `grep seedInventory` **有**（1 次，源码确实进包），
  但浏览器里 `Object.keys(window.__fx)` = `["spawnDamage","spawnSpark","spawnLaserBeam","debug"]`。
  → **「bundle 含字符串」≠「运行时句柄可用」**。取证只能靠 `page.evaluate(() => Object.keys(window.__fx))`。
- 修复：库存种子句柄改独立命名空间 **`globalThis.__inv`**，并**同步**把
  `globalThis.__inv = ` / `window.__inv = ` 加入 `scripts/check-wechat-bundle-clean.js`
  FORBIDDEN + E2E allowlist（新句柄必须同时进门禁，否则等于开了未受管的泄漏通道）。
- 单测加了防回归断言：宿主源码**不得再出现** `.__fx = {`（T38）。

### 其他被此 Queue 澄清的事实
- `metaPage` 是 `CanvasPlayerUIHost` **私有字段**（L386），**不在** `PlayerUIState` 里 →
  测试里 `render({ metaPage:'backpack' })` 是**静默 no-op**（原 10/15 失败的根因）。
  唯一正确进入方式 = 真实点击链 `home-garage` → `nav:backpack` → `bfilter:*`。
- `button()` disabled → **不注册 hitArea**（drawn but no `this.hit()`）→
  「可用<5 无假可合」可用 `getHitAreasForTest()` 里缺 `backpack-fuse` 来硬验证。
- `makeStarterDraft()` 默认把 `cannon` 装在 `frontMass` → 想造「5 件全未装备」场景必须用
  全 `EMPTY_SLOT` 的 bareDraft，否则 available=4 按钮恒 disabled。
- E2E 读面板真实文案：`page.addInitScript` 劫持 `CanvasRenderingContext2D.prototype.fillText`
  记录 `window.__txt` —— 纯测试侧 instrumentation，app 源码零改动、不新增诊断接口。
- **后台服务器坑**：`(node server &)` 挂在 Bash 单次调用里会随调用结束被回收 →
  E2E 首跑 `ERR_CONNECTION_REFUSED`。必须用 `run_in_background=true` 常驻。

### 门禁结果
- 浏览器真实 E2E：**136/136 PASS**（6 视口）。844×390 DPR1+DPR3 跑完整 5合1：
  装备保护（拥有5/装备1→可用4→「还差 1 个」且按钮无命中区）→ 补足可用5 →「合成」→
  1★ 6→1 / 2★ 0→1 / 已装备 1 件未被消耗 / 连点不重复 / Build 持久化不变 / reload 后 2★ 保持。
- 定向：R1 T25-T39 15/15、rcBundleCleanP0 9/9；tsc 0。
- 全量 vitest（`--pool=vmForks --maxWorkers=1`）：**1664 passed / 1 failed（1665）**。
  基线 2 failed/1648（1650）→ 新增 15 全绿、失败 2→1（偶发 `platformCore` WebLifecycle rAF
  timeout 本轮未复现），唯一失败 `garageBuildBoardP0.test.ts T6` 已取证为**既有脆性基线**
  （固定 2600 字符源码切片窗口，`git show 5fdd06a:` 原文件同样不含 `equipped` → 与本 Queue 无关）。
- 四路构建全过；bundle-clean wechat/rc/e2e/web 四路 PASS。
- RC clean-HEAD：`RC_EXIT=0`、badge `#9c76c9b`、`dirty=false`。
- **四路 SHA 一致**：LOCAL = REMOTE = rc-build.fullSha = bundle runtimeInfo =
  `9c76c9b79b4a561f9054bd7c9c4f667b764ef934`；repo-health 9/9。
- diff：4 files, +890/-119（仅源码+测试+门禁脚本；dist* 全在 .gitignore）。

### 诚实边界
- ①背包页可达 ②数据逻辑 ③**浏览器真实 5合1** 三层均通过；
  ④**iOS 真机真人体验未验证**（浏览器通过 ≠ 微信模拟器通过 ≠ iOS 真机通过）。
- 未部署 Pages（Queue 明确不要求）。

### 沉淀：项目级技能 `queue-delivery-gate`
- 落 `.workbuddy/skills/queue-delivery-gate/SKILL.md`（**不是**用户级）——流水线绑定本仓库脚本名/分支/路径，跨项目无意义。
- 已实测该路径**不破 RC dirty-gate**：`isIgnoredPath('.workbuddy/…')=true`、`filterControlledDirty` 不收录；新文件在位时 `npm run build:wechat:rc` 仍 `RC_EXIT=0` / `dirty=false` / badge `#9c76c9b`。
- 覆盖内容：基线核验（repo-health 无 CLI main → 必须 `node -e` 调 `checkRepoHealth`）、vitest `--pool` 铁律与「基线失败 vs 新增回归」的 base-commit 对照判定法、四路构建 + bundle-clean（**须从 index.html 解析当前 bundle 名**，dist 关了 emptyOutDir 堆着旧产物）、E2E 句柄命名空间铁律、后台服务器必须常驻、ref 落盘不可靠的手动 `printf` 兜底、四路 SHA 一行核对、收尾 6 步与四层结论表述。
