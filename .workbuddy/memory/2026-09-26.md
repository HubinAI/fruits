# 2026-09-26｜PRODUCT-LOOP-R5-BASIC-CONTENT-POOL-R1 交付

**commit `5baff53`**（base `06e44de` / R3）｜分支 `prototype-portrait-battle-lab`
16 files changed, +1524 / −27。四路 SHA 一致（HEAD = 本地 ref = `ls-remote` = FETCH_HEAD）。

## 做了什么
新增一次性「正式内容池」种子，**不设计任何新内容**，只把仓库里已存在、Runtime 已跑得通的
正式内容整理成验证账号可用的内容池：

| 维度 | 集合 | 结果 |
|---|---|---|
| Body | `NEW_OFFICIAL_BODIES`（4 台） | 全部解锁 ⇒ 8 台正式车身全可用 |
| Functional | `OFFICIAL_PARTS`（11 件） | 每件补到 ★1 ≥1 ⇒ 武器槽 3 张 → **9 张**、车身槽 8 张 |
| Movement | 不动 | `OFFICIAL_MOVEMENTS` 三档原样，不新增、不改数值 |

- 新模块 `src/product/r5ContentPoolSeed.ts`（422 行）；key `strongfruit.r5ContentPoolSeed.v1`。
- 接线 `playerGrowth.openGrowthSession`（R4 Body 种子之后、`saveInventory` 之前）；
  `inventoryChanged`（**不是** `applied`）驱动 `changed`。
- `homePage` 探针 +5 字段。
- ⚠️ **不放宽** `runCompatibility.FULL_RUN_SUPPORTED_WEAPON_IDS = ['cannon']`。

## 实测数字（门禁）
- `tsc` **零错**。R5 targeted **40 files / 671 tests** 全绿。
- product E2E：home **98/98** · loop **53/53** · reward **57/57** · reseed **21/21** ·
  fail **34/34** · star-power **22/22** · legacy-profile **18/18**。
- 边界：`src/{core,battle,physics,render,player,platform,game,presentation,ui}` **零改动**；
  `core/content.ts` / `battle/contactRouter.ts` 逐字节不变。

## 沿 Runtime 查到的 canonical 事实（下一步都靠这几条）
- `contactRouter.ts`：弹丸读 `projectileDamage`(:996)、直击读 `baseDamage`(:688)；
  **grep `'cannon'` 为空** ⇒ 伤害链共用，**无 cannon 专属 modifier**。
- `behaviorRegistry.FACTORIES` 11 项已注册；**`'ram'` 未注册** ⇒ `spear` 靠 collider 直击、
  无 behavior runtime。`ramHead`/`lifter`/`testMass`/`wedgeShovel` **不在** `OFFICIAL_PARTS`。
- `buildSnapshotFromDraft` 无 behavior/category 过滤 ⇒ 11 件**全部可进 Snapshot**。

## ⚠️ 环境陷阱（本窗口新发现，务必记住）
1. **`child_process.spawnSync` 在本宿主被无条件拒绝**，返回
   `status=null` + `error: spawnSync … EBUSY`。实测：spawn `node.exe` / `cmd.exe` 全 EBUSY；
   从 vitest worker 内、从普通 node 进程、**沙箱外**都一致；**异步 `spawn` 正常**。
   ⇒ 受影响只有 2 个文件：`tests/rcBundleCleanP0.test.ts`（6/9 红）、
   `tests/rcFusionTestEntryP0.test.ts`（T16 红）。**二者在干净 HEAD 上同样红** ⇒ 非回归，
   是环境假失败。判定这类红**不要**去改被测逻辑。
2. **`e2e:product-reward` 单跑 ~2m09s**，超过 Bash 工具默认 120s ⇒ 会被 SIGTERM 掐掉且
   **不打印 `=== 结果`**（日志到 `PASS J1` 就断）。**必须 `run_in_background` 跑**再取日志。
3. 曾被判为「批次污染」的 `garageCombatTabR1.test.ts`：**单跑绿、HEAD 大批次也红另一组**
   （MV-10 / MC-07b / T16）⇒ 是**批次/负载抖动**，不是交叉污染。判据 = 「同一批次在干净
   HEAD 上跑」若也红 ⇒ 非回归。

## 因本轮失效、已连同修掉的既有假设（均非放宽）
- `playerLoadout.ts` 关于 `saw`「**结构上不可达**」的注释**已作废**（内容池发放后 `saw`
  就在库存里，武器槽按 id 序第 7 张走的就是那条「不画伤害行」的分支）。
  ⚠️ **行为与数值一字未改**，只把过期判断改成事实。
- 6 份既有测试的 `isolate*()` 预置 R5 为隔离变量；`PL-26` import 白名单**登记**两条。
- E2E 基线校准（内容基线变了，不是放宽）：
  - `product-home`：**E5** key 闭集 七→八；**GS6** 的「天然未拥有样本」前提被内容池作废
    ⇒ 改为**受控样本**（同会话内临时写正式 `ownedBodies.v1`、取证后恢复），新增 **GS6b** 证明复原。
  - `product-loop` **A1b** / `product-reward` **A3、G1**：「3 张武器卡」改为 **9 件逐 id 相等**。
  - `product-reward` `clickSelector` 补 `scrollIntoViewIfNeeded()` —— 9 张卡后
    `[data-ph-weapon="spear"]` 落到滚动区外，`boundingBox` 仍给矩形但**真实鼠标点空**
    （实测 `lastEquip=null`、`equipped` 不变）。
  - `product-reseed` **R2g** key 闭集 九→十。

## 未决 / 待裁决
- **`e2e:product-reward` 的 C9 是抖动**：一轮 56/57 红在 C9（CDP
  `Cannot find context with specified id`，CTA 按下即导航导致上下文失效），复跑 57/57。
  ⇒ 未处置，仅记录（属既有断言/探针时序脆弱性，与本轮无关）。
- **本轮未跑全量 Gate**（Queue 明令）。记录基线：`220 files / 2329 tests` →
  本轮后应为 **233 files / 2334+ tests**（新增 1 文件 / 5 用例，具体以实跑为准）。
- 「Q2」文本未随本轮 Queue 到达 ⇒ 未开工。

---

# PRODUCT-LOOP-R5-SPEAR-FULL-RUN-R1｜判定 **STOP**（只调查、零改码、无 commit）

**HEAD 保持 `a06f2a4`；工作树零改动；临时诊断文件已删。**

## 五条前提（沿真实 Runtime 查，非据文档猜）
| 项 | 结论 | 证据 |
|---|---|---|
| canonical Def | `content.ts:950-968`：`category:'weapon'` / 多边形 collider（尖端 x=90） / `behavior:'ram'` / `behaviorParams.baseDamage:60` | — |
| 正式攻击逻辑 | ⚠️ **无 behavior runtime** —— `behaviorRegistry.ts:25-38` 的 `FACTORIES` **没有 `'ram'`**；`planckBattleOrchestrator.ts:295-296` `if(!factory) continue` **只跳过 runtime，part 与 collider 照常创建** | 与 `ramHead`（`content.ts:481-489`）同链路 |
| Collision / Damage | **真实且完整**：`contactRouter.ts:684-716` `handleWeaponContact` → 直击伤害读 `baseDamage`（需 `relativeVelocity ≥ WEAPON_CONTACT_THRESHOLD`）→ `damageResolver.applyDamage` | 无 cannon 特判 |
| Snapshot / entity chain | 通：`buildSnapshotFromDraft` 无 behavior/类别过滤；星级走**既有**通用链（`isDamageKey=/damage/i` ⇒ `baseDamage` ∈ 目标）★1=60 | `buildSnapshot.ts:81-83/119-131` |
| **当前 Product Run 阻塞点** | 见下（两层资格 + **结构性接缝**） | — |

## 真实阻塞点（决定性）
1. **两层资格**：产品 `runCompatibility.ts:58` `FULL_RUN_SUPPORTED_WEAPON_IDS=['cannon']`；
   局内 `runLoadoutCompat.ts:66-78`。实测（临时诊断）：`fullRunCompat(spearDraft)`
   = `{ok:false, reason:'unsupported-weapon'}`；`resolveRunPlayerLoadout` = `{blocked:true, unsupported-loadout}`。
   ⇒ **这两层是允许改的（合法性判断），但它们不是真阻塞点。**
2. **真阻塞点 = Run 的强化注入接缝只认基准武器 cannon，而完整 Run 在唯一路径上有 3 个必选 CHOICE**：
   `runScript.ts` `d2-choice1`(layer1) → `d4-lateral`(lateral) → `d5-choice2`(layer2)，三池的
   `affectsWeapon:true` 项（heavyShell/twinCannon/fastReload/tripleLoad）**全是 cannon 派生 overlay**。
   - `applyRunModifiersToSnapshot`（`runModifiers.ts:590-609`）判据 = `snapshotHasRunBaseWeapon`（存在性）
     ⇒ spear 装载不满足 ⇒ DAY3 第二场创建时 **throw**（实测复现：`RunModifier: 本局装载里没有 "cannon"…`）。
   - battle1(DAY2) 不 throw（`build` 为空 + 纯基线 ⇒ 非错误分支原样返回）—— **正是 P0 缺陷形状**。
3. **为什么不能「把 cannon overlay 套到 spear」**（Queue 明令禁止的「临时套用 Cannon modifier」）：
   `composeRunWeaponDef`（`runModifiers.ts:481-493`）会把 `behavior` **一并改成 overlay 的 behavior**。
   实测 `composeRunWeaponDef(正式spear, ['heavyShell'])` = `{behavior: 'ram' → 'cannon',
   params: {baseDamage:60} → {baseDamage:60, projectileRadius:16, projectileMass:4, recoilImpulse:90}}`
   ⇒ **等于把「刺」变成「炮」**，凭空发明一件新武器，且是 silent-failure 形态（玩家选了强化却只有一半生效）。
4. **战斗层独立证据**（无头真实物理，carriedHp 链按 `runCarriedPlayerHp` 复刻，`build:[]`）：
   - spear 装载：第 1 场 `A/hp` 但**玩家 hpA=0** → 第 3 场 `B` 落败 ⇒ 打不完。
   - **对照 cannon（同样无强化）：第 1-3 场胜、第 4 场 `B` 落败** ⇒ **强化不是可选装饰，是通关必需**。
   ⇒ 「让 spear 完成 Run」**必然**要给它等效强化 ⇒ 即「新设计成长/攻击规则」（Queue 明令禁止）。
   （附带修正：R1-C 交接记的「spear 第 2 场僵持」在当前代码上**不复现** —— 四场都能出结果。）

## 结论
**STOP**。三条禁止项同时命中：① 临时套用 Cannon modifier ② 新设计攻击/成长规则
③ 新增特殊资格状态（让 spear 跳过强化 = 第二套 Run 流程）。
⇒ 不写任何代码、不 commit、不 push（Queue：成功才 commit）。工作树保持零改动。

## 可选的后续（未做，等裁决）
- 建议补一条**结构性守卫**（仓内已有同型先例 `LC-11`，`tests/productRunBuildLoadoutCompat.test.ts:273-291`）：
  钉死「`composeRunWeaponDef` 对非 cannon 基准武器会把 behavior 改成 cannon」⇒ 防止将来有人
  用「套 cannon modifier」的方式悄悄支持 spear。属独立小 Queue，本轮未擅自开工。
- 「Q3」文本未随本轮 Queue 到达 ⇒ 未开工。

---

# PRODUCT-LOOP-R5-HAMMER-FULL-RUN-R1（本会话第 5 条消息）｜判定 **STOP**（只调查、零改码、零源码 commit）

链路：HEAD `cf16cb9`（= spear STOP 的 chore(memory)）→ 本轮**零源码改动**，工作树始终干净。

## 一、Hammer 的攻击 Runtime **完整**（与 spear 不同，**不是**「缺真正攻击实现」）
- canonical def：`core/content.ts:512-531` `category:'weapon'` / `mass:40` / box 60×14（offset x=40）/
  `behavior:'hammer'` / `behaviorParams:{baseDamage:90}`。
- behavior **已注册**：`battle/behaviorRegistry.ts:28` `hammer: createHammerRuntime`；
  专属实现 `battle/hammerBehavior.ts`（真实 Revolute motor + limit 的真实物理弧，
  windup→windup-pause→swing→recover 相位机；**无 setAngle / 无 teleport / 无直接扣血**）。
  另有装配侧特判 `planckVehicleAssembly.ts:413`（hammer/lifter/saw 走同一类挂载）。
- 伤害链：`contactRouter.ts:688` 直击读 `baseDamage`（**无任何 cannon 特判**）。
- 实测：装备 hammer 后 `validateSnapshot` **valid=true**（functionals = `frontMass:hammer` + `top:hammer`）；
  单场真实战斗能打完并出结果。

## 二、真阻塞点 = Run 的强化注入接缝**只认 cannon**（与 spear **同一**结构性阻塞）
- 唯一接缝 `runModifiers.ts`：`RUN_BASE_WEAPON_DEF_ID = 'cannon'`（:110）；`RUN_MODIFIER_OVERLAY`
  里 4 项「改武器」的强化（heavyShell/twinCannon/fastReload/tripleLoad）**全是 cannon 派生**。
- 真实顺序实测（`applyRunModifiersToSnapshot`）：battle1（DAY2，`build:[]`）**不 throw**；
  `d2-choice1`(layer1) 是唯一线性路径上的**必选** CHOICE（池 = 3 个 cannon 强化）⇒
  battle2（DAY3）创建时 **throw**：`RunModifier: 本局装载里没有 "cannon"，无法注入强化 [heavyShell]`。
- 「套用 cannon modifier」实测会把武器改种：`composeRunWeaponDef(正式hammer, ['heavyShell'])` =
  `{behavior: 'hammer' → 'cannon', params: {baseDamage:90} → {…, projectileRadius:16, projectileMass:4, recoilImpulse:90}}`
  ⇒ 把「锤」变成「炮」（silent-failure 形态）。
- 因此放宽两层资格 = **重新打开 P0 的「DAY3 throw → Run 卡死」真实崩溃路径**，
  而 `LC-11`（`tests/productRunBuildLoadoutCompat.test.ts:273`）已把它钉成**必须响亮失败**的强 invariant。

## 三、产品侧已经**明文裁决**过这件事（不是本轮发明的结论）
- `product/runCompatibility.ts:20-23` 逐字：「在 Spear / Hammer 有正式 Run Build 内容之前：**它们不得进入完整 Run**。
  ⚠️ 因此本模块**不为** Spear / Hammer 临时设计任何 Buff」；`FULL_RUN_SUPPORTED_WEAPON_IDS = ['cannon']`（:58）。
- 机器钉死的三条冻结守卫：`LC-02`（非 cannon 一律不支持）/ `LC-11`（heavyShell + no cannon 必须 throw）/
  `LC-23`（没有新 Spear / Hammer Buff，既有 Run 强化一字未改）。

## 四、第二条独立证据：**内容/平衡缺口**（实测表）
无头真实物理 + 真实注入链（`playerBaseline:true`），四场按 `runCarriedPlayerHp` 口径继承耐久：

| 装载 | 第1场 | 第2场 | 第3场 | 第4场 | 完成 |
|---|---|---|---|---|---|
| **V1 双锤**（**产品可达**：主武器槽换锤 ⇒ starter 固定在 `top` 的那把锤仍在） | **B 败**（hpA 0 / hpB 450） | — | — | — | **0/4** |
| V2 单锤（`top` 留空） | A（hpA 373） | A（hpA 302） | **B 败** | — | 2/4 |
| V3 锤 + 炮（`hammer@frontMass` + `cannon@top`） | A（1007） | A（1002） | A（754） | **A（270）** | **4/4** |
| V4 产品默认（炮 + 锤） | A（916） | A（873） | A（452） | **B 败** | 3/4 |
| V5 单炮 | A（1008） | — | — | — | — |

- ⇒ **hammer 本身能打**（V2/V3 都赢），但**产品可达形状是「双锤」⇒ 第一场就落败（0/4）**。
- ⇒ 即使放宽接缝，也拿不到「完整一局」的可用体验 ⇒ 需要「新设计」（强化 / 平衡 / 遭遇）＝禁止项。
- 验收 1 对 hammer **已经成立**（无需改动）：`isWeaponDefId('hammer')=true`、
  starter 库存 `getCount(hammer,★1)=1`、卡片读数 `攻击 90 → 113`、`equipWeapon('hammer')` → `{ok:true}`；
  装备后 `functionalSelections = {front:'none', frontMass:'hammer', top:'hammer', rear:'none'}`（**双锤**，见上）。

## 结论
**STOP**，记录为「**内容实现缺口**」：**Hammer 的攻击实现不缺，缺的是它的正式 Run Build 内容（强化）+ 与之匹配的可行性**。
三条出口全部命中禁止项：① 新增 Hammer 专属强化（新设计，且冲 `LC-23`）② 让 hammer 装载跳过强化（第二套 Run 流程 /
新增特殊资格状态）③ 套用 cannon modifier（`behavior` 会被改成 cannon）。
⇒ 不写任何代码、不 commit 源码（Queue：成功才 commit）。仅 `chore(memory)` 记录判定。

## 门禁（零改动下的零回归取证）
`npx tsc --noEmit` **EXIT=0** · Q4 targeted **12 files / 207 tests 全绿**
（含 `productRunBuildLoadoutCompat` / `portraitRunBattle` / `hammerBehavior` / `hammerAssembly` /
`hammerScenarios` / `behaviorRegistry` / `productRunBodyCanonical` / `productRunMovementCanonical` /
`productLoopHomeGarage` / `productContentPoolSeed` / `buildEditorModel` / `productLoopEndToEndPlayerLoop`）·
`e2e:product-home`（`_e2e_product_home.cjs`）**98/98 PASS, 0 FAIL**。边界：源码 diff 为空。

## 待裁决（未做）
- 「Spear（若 Q2 成功）不退化」一项 **N/A**：Q3 已判 STOP，spear 从未获得 Run 支持，无退化面。
- 若仍要让 hammer 进完整 Run，最小正路 = **一份正式 Hammer Run Build 内容（新强化）+ 与四场阶梯的可行性**，
  这是新设计 Queue（且须先解冻 `LC-23` 与 `FULL_RUN_SUPPORTED_WEAPON_IDS` 的前置条件）。
- 「Q4」正文未随本轮 Queue 到达 ⇒ 未开工。

---

# PRODUCT-LOOP-R5-MULTI-WEAPON-PRODUCT-COMPAT-R1（Q5）｜**统一检查**轮，源码零改动

**性质**：这是验收 / 统一检查 Queue —— **不新增武器、不改任何源码**。`git diff --stat -- src` 全程为空。
交付物 = 两条把「多武器兼容性」变成机器判据的守卫。

## 关键事实修正（本轮最重要的一条）
**registry 里的武器是 10 件，不是 9 件**：`weaponDefs()` 含 **`ramHead`（冲撞头）** —— 它有
`category:'weapon'` 定义（energy 20 / behavior `ram`）但**不在 `OFFICIAL_PARTS`** ⇒ `isOfficialPart('ramHead') === false`
⇒ **玩家永远拿不到、不进任何库存**。
⇒ 「本批次可接入的武器」= `OFFICIAL_PARTS` 武器子集 = **9 件**；差集恰为 `['ramHead']`。
⚠️ 写任何「武器全表」断言时**不要**用 `weaponDefs().length`，要用 `OFFICIAL_PARTS.filter(isWeaponDefId)`。

## 交付 1｜`tests/productMultiWeaponCompatR5.test.ts`（新增，11 条，真源驱动）
集合一律**现读真源**（`weaponDefs()` / `OFFICIAL_PARTS`）⇒ 任何新增武器**自动**进入矩阵。
MW-01 集合与差额 / MW-02 支持清单唯一真源（全 `src/` 只声明 1 处）+ 奖励池 ⊆ 支持清单 /
MW-03 **逐件矩阵**（10 件）`ok ⟺ 在支持清单里` + **恰好一件放行**（这一条让白名单被放宽时
**矩阵本身**红，否则 `ok` 与 `supportsFullRun` 会一起翻、矩阵自洽变绿）/ MW-04 逐件 Run 创建资格
（支持 ⇒ `source='profile'` 交回玩家那份；不支持 ⇒ `blocked` + `unsupported-loadout` + **演示占位**）/
MW-05 **读取零副作用**（不写盘、不改 draft、装备仍是它）/ MW-06 判据 = **存在性**不是槽位
（不支持武器占主武器槽 + cannon 在别的挂点 ⇒ **仍放行**且装载逐字节不改写）/ MW-07 「没有武器」在
产品侧**发不出来**（正式校验要求「至少 1 件 Weapon」）/ MW-08 + MW-08b **多武器库存 × Fusion** /
MW-09 源码守卫（`equipWeapon(` 调用点闭环）/ MW-10 页面不自算。

## 交付 2｜`tests/_e2e_product_fail.cjs`（+124/−1，既有断言一字未改）
- 新增 **A4b 逐件守门矩阵**：按 `BLOCKED_WEAPON_MATRIX`（= canonical − 支持，真源驱动）把库存里
  **每一件**非支持武器过同一组判据（真实点击 → **独立读 localStorage** 取主武器槽 → 车库 unsupported
  → 首页 `startRunHref === null` → `equippedWeaponId` **仍是它** = 不自动换炮）+ A4b-0 / A4b-9 汇总。
- `clickSelector` 补 `scrollIntoViewIfNeeded()`（与 loop / reward 同一处置；9 张卡后下面的卡在滚动区外）。

## 门禁
`npx tsc --noEmit` **EXIT=0** · targeted **41 files / 682 tests 全绿** ·
product E2E：**fail 45/45**（含新增 A4b 11 条）· **reward 57/57** · **home 98/98** · **loop 53/53**。
边界：`src/**` 零改动，`content.ts` / `contactRouter.ts` 逐字节不变。commit **`9dcbf7b`**，四路 SHA 一致。

## 负控制（三注入，全部生效并干净回退）
① 白名单放宽 `['cannon','spear']` ⇒ MW-01/02/03/04 **4 红**；② 读路径注入写盘 ⇒ MW-05/09 **2 红**；
③ `FUSE_STACK = 1` ⇒ MW-08/MW-08b **2 红**。回退后 `git diff --exit-code -- src` = 0、关键词零残留。

## 待裁决 / 未做
- 「Q6」正文未随本轮 Queue 到达 ⇒ 未开工。
- `tests/rcBundleCleanP0.test.ts` / `tests/rcFusionTestEntryP0.test.ts` 在本会话结构性红
  （宿主 `child_process.spawnSync` 无条件 EBUSY，干净 HEAD 上同样红）⇒ 本文件未纳入绿批次。
