# 2026-09-18

## PRP-RUN-02-R2-MAKE-DURABILITY-CHOICE-REAL ｜交付（RUN-02 最后收口）

### 缺陷（真人验收）

RUN-02-R1 修正后两条分支**唯一**的差别只剩那 275 点耐久 ⇒
「维修**严格支配**继续改装」（维修 = +275 耐久 + DAY5 第二层；继续改装 = 不回血 + 同一个 DAY5 第二层）。
根因**不是数值**，是 upgrade 分支在结构上**没有换到任何东西**。

### 改了什么

**设计定稿：候选池的「种类」由 CHOICE 节点声明**，不再按 `buffs.length` 数数 ——
否则「横向改装」这一选（发生在 `buffs.length === 1`）会被误判成第二层。

| 文件 | 改动 |
|---|---|
| `src/lab/portraitBattleLab/runScript.ts` | 新增 `RunChoicePoolKind` + `RunScriptNode.choicePool?`；`d4-durability.branch.upgrade → 'd4-lateral'`；新增 `d4-lateral` 节点（CHOICE · DAY 4）；`d2-choice1='layer1'` / `d5-choice2='layer2'` |
| `src/lab/portraitBattleLab/runModifiers.ts` | 新增 `isLayer1Modifier` / `runLayer1PoolDefs` / `runLateralPoolDefs(owned)`；`RUN_LAYER2_POOLS` **一字未动** |
| `src/lab/portraitBattleLab/runPageState.ts` | `runMainRouteId` / `dedupeOwned` / `runChoicePoolKind` / `runChoicePoolLayer`；`runChoicePool` 改为节点驱动；推进逻辑**零分支特判** |
| `src/lab/portraitBattleLab/runPage.ts` | 探针新增 `choicePoolKind`；`choicePoolLayer` 由 `buffs.length + 1` 改为读 `runChoicePoolLayer` |
| `src/lab/portraitBattleLab/nextRunValidation.ts` | M2 脚手架 `PRIOR_RUN_DURABILITY` `upgrade → repair`（见下） |
| `tests/portraitRunPage.test.ts` | 冻结表重测；RP-F2-01/07/08/09/11/12 按三层 Build 改写；RP-22b 与 RP-RUN-02-01 **收紧**；新增 RP-RUN-02-R2-01 / RP-F2-11b / RP-F2-12b |
| `tests/_e2e_run_page.cjs` | 11c 段拆成「横向 → 第二层」两步（R52 家族重编号 R52…R52k）；下游按三层 Build 改写；新增 R58h；13 段改走维修分支 |

E2E 的 R52 家族现在覆盖：`R52` 横向节点 / `R52b` 横向池 = 另外两个一层（不含 `emergencyRepair`）/
`R52c` 不回耐久 / `R52d`+`R52e` 横向浮层 **2 张卡**（复用同一套几何 = 必改 4）/
`R52f`+`R52g` 照样进 DAY5 第二层 / `R52h`+`R52i` 条件池三项且不含已拥有 / `R52j`+`R52k` 第二层 3 张卡。

### 实测（口径 = 显式实测更新，不是就地重算）

维修分支 `FROZEN_REPAIR`（**逐值不变** = 必改 3 的机器证据）：
`919/688/678/472` · `919/907/891/602` · `919/908/1023/778`（三条全 COMPLETE）

改装分支 `FROZEN_UPGRADE`（三项全变，横向取「横向池第一项」）：
`919/688/489/167`(COMPLETE) · `919/907/907/0`(FAILED) · `919/908/170/0`(FAILED)

E2E 主走查组合 `FROZEN_UPGRADE_E2E` = `[919, 907, 839, 357]`（双联 + 横向**快装** + 三连），
整局日志 **40** 行；浏览器实测 `357.43`（32%）⇒ 与 Node 端**逐值相等**（新增 R58h 钉死这条）。

### 三条已接受副作用（必须上报）

1. **第二层池会被去重砍成 2 项**（横向先拿的那项若落在条件池里）；本轮 E2E 刻意横拿 `fastReload`
   （不在双联的二层池里）⇒ 保留 3 张卡，去重本身由 `RP-F2-07` + `R52i` 显式断言。
2. **M2 脚手架改走 `repair`**：否则快进会多拿一项 ⇒ `priorRunSummary.build` 由 2 项变 3 项 ⇒
   M2 已验收输出变化。代价 = `runCarriedPlayerHp(prior)` 从此含 275 补偿（416 = 141 + 275）；
   `priorRunSummary.durabilityPercent` 读 `battle.playerHp` **不受影响** ⇒ 结论不变。
3. **`RUN_MAX_CHOICES` 2 → 3**（维修分支只拿 2 项、改装分支 3 项 = 「构筑数量优势」的口径）。

### 门禁与途中发现

- `tsc --noEmit` 0 错；`portraitRunPage` **66/66**；`portraitNextRunValidation` **16/16（零改动）**；
  全量 vitest **206 文件 / 2017 用例 PASS**。
- E2E：**run-page 503/503** · next-run 47/47 · portrait-lab 148/148 · encounter-lab 47/47 ·
  validation-hub 42/42 · default-entry 79/79 —— 全绿（首个 run-page 档期的产物核对：
  `grep -rl "d4-lateral" dist-portrait-lab/` 命中 + 十个节点 id 齐全）。
- ⚠️ **首轮 run-page E2E 暴露一处漏改**：13 段（动能爆发感知链）原本用
  `chooseById('upgrade')` → `chooseById('kineticBurst')`，R2 之后 `upgrade` 先落到 `d4-lateral`，
  `kineticBurst` 不在横向池里 ⇒ R63/R64/R65/R66 四条 FAIL（实测 0 次命中 / 0 个环）。
  修法：该段改走**维修**分支（同样到达 `d5-choice2`，第二层池一致）⇒ Build 仍是
  `[heavyShell, kineticBurst]`，即感知链阈值原本标定的那条 `FROZEN_REPAIR` 路线，**口径一字不改**。
  修后实测：11 次真实命中 · 有环帧 115 · 位置不符 0 · 年龄越界 0 · 环峰值 2 · 位移峰值 66.02px。
  ⇒ 教训：**改分支图之后，凡是用 `chooseById(id)` 按 id 选卡的段落都要重新核对
  「那一步现在落在哪个节点」**（`pointer` 落在 CHOICE 之外的节点时会静默返回 null）。
- Memory 维护：`MEMORY.md` 超出注入上限被截断 ⇒ 完整版归档到
  `.workbuddy/memory/archive/MEMORY_FULL_2026-09-18.md`，并按「只留不变量 + 陷阱 + 下一步」重写。

### 下一步

按指令**停止**，不继续其它 Run 内容。真人复验 = 录一段
「DAY 4 选继续改装 → 横向二选一 → DAY 5 第二层 → DAY 6 三层 Build」。
⚠️ 待裁决（新假设，须单开 Queue）：改装分支在装配上占优，但终局耐久常更低（三条路线里两条直接 FAILED）
⇒ 是否给改装分支补偿生存，属**新设计假设**，不在本 Queue 内动。
