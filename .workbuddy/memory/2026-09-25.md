# 2026-09-25｜PRODUCT-LOOP-R3-MOVEMENT-GARAGE-EQUIP-RECOVERY

**基线**：branch `prototype-portrait-battle-lab`；HEAD = remote = `6b6aa9e`（= Queue 给定基线）。
**恢复检查结论**：tracked workspace clean，`src/` `tests/` 无未提交修改 ⇒ 上一会话
**未留下任何未提交的有效修改**（实现尚未开始就已中止）。未跟踪件仅历轮既有
`交接文档_*.md` / `新窗口交接指令_*.md`。**未触发停止条件。**

## 1. 交付内容（Garage 里真实装备 Movement）

| 文件 | 改动 | 作用 |
|---|---|---|
| `src/product/playerLoadout.ts` | +255 | `movementReading()` 只读读数 + **`equipMovement()` 唯一写入口** |
| `src/product/homePage.ts` | +303 | `renderMovementSection()` + 探针四字段 |
| `home.html` | +19 | Movement 区的结构类样式（`.ph-mv-row` / `.ph-card-mv` 等） |
| `tests/productMovementGarageEquip.test.ts` | 新增 28 用例 | A~H 八段 |
| `tests/_e2e_product_home.cjs` | +217 | M1~M9 + M2b + G3/G4 |
| `tests/productRunMovementCanonical.test.ts` | +149 | MC-07b 改结构判据 + 新增 MC-07c/07d |
| `tests/productLoopHomeGarage.test.ts` | +21 | PL-26 白名单 +2 |

## 2. 关键设计（**改前必读**）

- **rear / front 本来就是两个独立正式字段** ⇒ `equipMovement` **按挂点分别写**
  `rearWheelDefId` / `frontWheelDefId`，**不引入统一 Movement 槽模型**（Queue 明令禁止）。
- **三态逐字节可区分**（`MOVEMENT_DRAFT_KEY` 表查键，写入恒为间接的 `next[key] = …`）：
  - `'none'` → 写 `EMPTY_SLOT`（明确卸下）
  - 缺省轮（`defaultDefId`）→ **`delete next[key]`**（回到「存档没这个键」＝旧存档兼容口径）
  - 其它 defId → 写 defId **并同步该槽 radius = 该轮组默认半径**
    （⚠️ 必须一起改：`radius` 会作为 `overrides.radius` 被 Snapshot 带下去，
     只改 defId 会让新轮组带着旧半径跑）
- **`not-owned` 判据与 `movementInventory.owned` 同源**（`needsInventory` + `getCount`），
  **不新造**；缺省轮恒豁免。不消耗库存（装备是引用）。
- **Run 侧一致性是既有链路，零改动**：`buildAdventureHref` → `encodeRunLoadout`
  （`runReward.ts:155`，**只在字段 `!== undefined` 时才写** ⇒ 线路上也保持「缺省 = 无键」）
  → `equipped=` 查询参数 → `parseRunPlayerLoadout` → `validateSnapshot` → Runtime
  （`entities.ts:221` 的 `movements` 真实参与实体数值 + 内容指纹 `entityBaseKey`）。

## 3. ⚠️ 本轮真实缺陷（已修，但要知道成因）

**`renderMovementSection()` 的插入位置引发可用性回归**：
初版把它插在「武器卡阵」与「武器 装备/返回 按钮」**之间**，实测后果 ——
武器「装备」按钮被推到 `y=1034`，而视口只有 720 高 ⇒ **主操作落到屏幕外**
（`elementFromPoint(center)` 返回 `null`，真实鼠标点不到）。这**不只是 E2E 取不到坐标**，
玩家同样点不到。E2E 里表现为 B6/B7/C1/C3/D1/E1/E2/E3 连锁变红（`lastEquip=null`）。

- **修法**：把 `renderMovementSection()` 移到武器 `actions` **之后**
  ⇒ 每组配置 = 卡阵 + 它自己的动作条，中间不夹另一组内容。
  （不做 Garage 大改，只调一行调用位置。）
- **教训**：`boundingBox()` 给的是**滚动后视口坐标**，`mouse.click` **不会替你滚动**。
  页面比一屏高之后，`clickSelector` 必须先 `scrollIntoViewIfNeeded()`（只滚动，不触发事件，
  仍是真实鼠标点击）。已在 `_e2e_product_home.cjs` 的 helper 里补上。
- **教训**：新加的样式类必须**在 `home.html` 里真的定义**。`ph-mv-row` 等结构类起初没有 CSS，
  会退化成无间距裸块 —— 功能在但玩家看不出「哪行是 rear / 哪行是 front」。

## 4. 门禁实测

| 门禁 | 结果 |
|---|---|
| `tsc --noEmit` | 0 错 |
| targeted vitest 7 文件 | **173/173** |
| `_e2e_product_home` | **46/46**（含新增 M1~M9 + M2b + G3/G4） |
| `_e2e_product_reward` | **57/57**（= 冻结基线，一字未降） |
| `_e2e_product_fail` | **34/34** |
| `_e2e_product_star_power` | **22/22** |
| `_e2e_product_reseed` | **21/21** |
| `_e2e_product_legacy_profile` | **18/18** |
| `_e2e_product_loop` | **53/53** |

**E5 四 key 闭集不变**：`ownedParts.v2` / `playerBuild.v1` / `r2Onboarding.v1` / `r2Reseed.v1`。
**边界取证**：`src/{core,battle,physics,render,player,platform,game,presentation}` diff **全空**；
`src/lab` diff **全空**（**刻意不动 Lab 冻结面** —— Run Snapshot 一致性改用 URL 载荷取证，
不为测试给产品代码加探针）。

## 5. 负控制（按项目铁律做了）

往 `equipMovement` 里注入一处「字面量当左值」的散落赋值 `next['rearWheelDefId'] = defId`：
- 预期：MC-07b 变红；实际：**MC-07b 报「实测 5 行 @ 470,471,472,515,518」**（期望 4 行），
  同时 **MC-07c 也红**（`['largeWheel','largeWheel']` ≠ `['smallWheel','largeWheel']`，真实行为损坏）。
- 回退后 `grep` 全仓确认零残留（`next['rearWheelDefId']` 计数 = 0）。
- ⚠️ 第一次注入写坏了 `if/else` 语法（`else` 被孤立）⇒ 文件直接 parse 失败。
  注入点要选在**语句边界**（如 `next[key] = defId;` 之后），不要插在 `if ... ; else ...` 中间。

## 6. MC-07b 为什么改成结构判据

原断言「`playerLoadout.ts` 里根本不许出现 Movement 字段名」与本 Queue 目标（补写入口）
**直接冲突** ⇒ 改写为**结构判据**（不是放宽）：
- 出现位置按**行**去重必须恰好 **4 行**、按 **token** 恰好 **5 个**
  （⚠️ 470 行类型注解一行含 **2 个 token**，按行数会误记成 1 —— 这是第一次算错的根因）；
- 4 处必须彼此邻近（跨度 ≤ 50 行）；
- **最强的一条**：字面量**绝不允许**出现在赋值左值
  （`/['"]?(?:rearWheelDefId|frontWheelDefId)['"]?\s*=[^=]/` 必须不匹配）。

---

# PRODUCT-LOOP-R3-MOVEMENT-CARD-READABILITY（轮组卡片刻度量）

**commit `28c3cf9`** · branch `prototype-portrait-battle-lab` · 6 files, +666 / −0

## 1. 做了什么

玩家在 Movement Choice Seed 之后**已拥有四档轮组**，但卡片只能识别「名称 + 拥有状态」
⇒ 没有比较依据。本 Queue 让卡片显示 canonical 的三个属性：**轮径 / 质量 / 能耗**。

数据链（**不复制第二套常量**）：
```
core/content.ts registry.movements（唯一真源）
  → runMovementCanonical.canonicalMovements()（既有，已带 radius/mass/energy）
  → movementInventory.movementEntries()   ← 本轮新增透传三字段
  → playerLoadout.movementReading().cards[].statsText ← 本轮新增成品文案
  → homePage.renderMovementSection() 卡片上那一行
```

实测四档显示（E2E MC2/MC4 取证，与 canonical 逐件相等）：
| 卡片 | 刻度 |
|---|---|
| 标准轮 | 轮径 20 · 质量 10 · 能耗 0 |
| 小轮组 | 轮径 12 · 质量 6 · 能耗 5 |
| 大轮组 | 轮径 26 · 质量 14 · 能耗 12 |
| 重型轮组 | 轮径 20 · 质量 20 · 能耗 15 |

## 2. ⚠️ 本轮最有价值的发现（下次先读这条）

**「读数正确」≠「玩家看得见」—— 负控制实测暴露的真实守卫缺口。**

把 `card.append(el('span','ph-card-stats', c.statsText))` **整行删掉**之后：
- `productMovementCardReadability.test.ts` 的 MVR-01..MVR-14 **全绿**
  （因为前 14 条测的是**读数层** `movementReading()` 里有 `statsText`）；
- 只有**浏览器 E2E**能发现屏幕上一个字都没有。

⇒ 补上 **MVR-15 接线守卫**（源码层钉住：渲染调用必须存在、位置必须落在
`for (const c of m.cards)` loop 内、必须用读数提供的文案不自拼、必须挂 `data-ph-movement-*`）。

**推广规则**：凡是「新增一个可见元素」，除了读数层断言，必须**另有一条**
①源码接线守卫（函数被调用 / 位置在 loop 内）+ ②浏览器 DOM 可见文本断言。
两者缺一，删掉渲染行都不会红。

## 3. 负控制（两条，均干净回退 + grep 零残留）

| # | 注入 | 命中 |
|---|---|---|
| ① | `statsText` 改成自造文案 `速度 N` | MVR-04 / 06b / 10 / 12 红 |
| ② | 删掉 `card.append(...ph-card-stats...)` | **vitest 全绿**（见 §2）⇒ 补 MVR-15 |

## 4. 门禁

`tsc` 零错 · targeted **285/285**（12 files）· build:portrait-lab 成功
· product-home **65/65**（基线 57/57 ⇒ 新增 8 条 MC 全绿）
· reward 57/57 · loop 53/53 · star-power 22/22 · fail 34/34 · reseed 21/21 · legacy 18/18
· default-entry 84/86（A3/A4 与上轮在 `4f0be1a` 同源复现的既有夹具抖动一致）
· 全量 vitest **2460 passed / 7 failed**（`rcBundleCleanP0` ×6 + `rcFusionTestEntryP0` ×1）

## 5. `EBUSY` 环境限制——已拿到**硬签名**（此前只是「签名一致」推断）

在**真实 shell**（非 vitest worker）里直接 `spawnSync(process.execPath, [...])`：
```
status = null
error  = EBUSY      ← 硬签名，不再是「expected -1 to be N」的间接推断
```
⇒ `rcBundleCleanP0` / `rcFusionTestEntryP0` 的 7 fail **确证**为 Windows 本机
`spawnSync` 无法启动子进程（`node scripts/check-wechat-bundle-clean.js`），
与产品代码无关（两测试文件 + 该脚本本轮 **0 改动**）。
**下次遇到这 7 条，直接引用此签名，不必重新定性。**

## 6. 边界取证

`src/{core,battle,physics,render,player,platform,game,presentation,ui,lab}` diff **全空**；
`content.ts` / `contactRouter.ts` / `buildSnapshot.ts` / `partInventory.ts` **逐字节 0 改动**。
改动只在：`src/product/{movementInventory,playerLoadout,homePage}.ts` + `home.html` + 2 个测试。

## 7. 表达纪律（禁止清单的落地形式）

卡片文案里不出现 `%` / `★` / `+N%` / 速度 / 稳定 / 稀有 / 史诗 / 传说 / 品质；
也不引入第四个字段（抓地 / 转速 / 扭矩）。这些在 MVR-10/11 与 E2E MC6 上
**双重**钉住（源码层 + 真实 DOM 文本层）。

---

# 追加｜Queue 3 + Queue 4 收口（12:04–12:45）

## 8. Queue 3 `PRODUCT-LOOP-R3-MOVEMENT-EQUIP-PREVIEW` → commit `76a6f98`

**缺陷根因（两级传播，本轮首次实证）**：`BuildDraft` 的
`rearWheelDefId`/`frontWheelDefId`（**装了什么**）与 `rearRadius`/`frontRadius`（**数值**）
是**两个可各自漂移的字段**。
1. `equipMovement()` 的缺省轮分支**只 `delete next[key]`、保留 radius**（`playerLoadout.ts`）
   ⇒ 装 smallWheel 后切回缺省轮 ⇒ `rearWheelDefId` 已删、`rearRadius` 仍 = 12
   ⇒ 预览把标准轮画成 **24×24**（应为 40×40）。
2. `buildSnapshotFromDraft` 把 `rearRadius` 作为 **`overrides: { radius }`** 写进 movements
   （`buildEditorModel.ts`），`applyMovementOverrides` 做 `{ ...def, ...overrides }`
   （`core/buildSnapshot.ts`）⇒ **陈旧值覆盖 def 自身 radius**。
   ⚠️ 故 **`runtimeNumbers.radius` 也不是真源**（第一次修错就栽在这 —— MV 用例仍 24×24）。

**修复口径**：新增 `runMovementCanonical.effectiveMovementRadius(draft, hardpointId)`
（`+42 行`）—— 走 `movementMapping(draft).slots[].effectiveDefId` 取生效 def，
半径取 **def 自身 `radius`**（真源 `registry.movements`），**刻意不读** `draft.*Radius`
与 `runtimeNumbers.radius`。`vehiclePreview` 轮子循环改读它（`+59/-10`）；
`effectiveDefId === null` ⇒ **不画轮子**（三态之 `none`）。`homePage` 预览盒新增只读
`data-ph-scale`（本次布局换算率，供 E2E 真实像素对账）。

**新测试** `tests/productMovementEquipPreview.test.ts`（MV-01..MV-10，14 例）；
E2E `_e2e_product_home` 新增 **MV-1..MV-8**（真实 `style.width` 对账，四组合
标准/标准·small/large·large/small·heavy/heavy + 方向 + 切回缺省轮 + reload + `none`）。
E2E 由 65 → **73/73**。

**负控制（做了，干净回退）**：轮径退回 `draft.*Radius` ⇒ **MV-1 / MV-3 / MV-4 三条红**，
MV-4 精确复现 `backToDefault.rear=wheelStd:40.00`（期望 66.67）。

**既有守卫按契约变更适配（非放宽）**：MC-10 反向对照改用恒定存在的 `'wheel'`
（产品侧 `strip()` 后**已无任何真实 defId 字面量**，`'wheelStd'` 在 `playerLoadout.ts`
只剩注释）；MC-10b 收窄为「fallback 字面量必须**不存在**」；`vehiclePreview.ts`
import 白名单显式登记 `'./runMovementCanonical'`。

⚠️ **教训**：`strip()` 必须**逐文件**调用 —— 探查脚本若整仓一起剥，会把某个文件的
注释剥离边界算错，误判「还有字面量」。

## 9. Queue 4 `MOVEMENT-CONTENT-BATCH-GATE` → **零代码改动，不制造空 commit**

必验 A/B/C/D 全通过：ChoiceSeed 18 · CardReadability 16 · EquipPreview 14 ·
GarageEquip 28 · Ownership 17 · ProductLoopSurface 12 · RunMovementCanonical 47 ·
LoopHomeGarage 33 · LoopRunReward 32 · GrowthR2A 22 · MovementPackR1 ·
ReseedR2 20 · FusionR2B 20 · StarPowerR2C（含 SP-11a/b/c/d/e = 80/120/150 基线开关）
· planckMovement —— targeted **335/335**。

**统一 Gate**：tsc 零错 · 全量 vitest **2474 passed / 7 failed（2 files，均 EBUSY）** ·
产品 E2E home 73 · loop 53 · reward 57 · fail 34 · reseed 21 · star-power 22 · legacy 18 ·
默认入口 portrait-lab 176 · run-page 503 · encounter-lab 54 · validation-hub 53 ·
**default-entry 86/86** · 四路 builds 成功 · bundle-clean wechat/rc 双 PASS。

⚠️ **`e2e:default-entry` 的 A3/A4 是时序抖动，不是确定性失败**：
- 现象：两次跑得 **84/86**（A3「Local=(未捕获)」/ A4「未捕获」），第三次 **86/86**。
- 根因：`startDevServer()` 用 `shell: true` 起 `npm run dev`，其 stdout pipe
  在本机 Windows Git Bash 下**不落地** ⇒ `state.out` 常为空；A2（服务就绪）与
  A5（HTTP 内容）恒 PASS ⇒ **功能完全正常**，只是日志抓不到。
- 定性：在 Q1 前基线 `cb3275d` 的临时 worktree 上复跑 —— **逐字同签名 84/86**；
  该文件自 `4f0be1a` 起未改、对本轮改动面 grep **0 匹配** ⇒ **基线既有环境问题**。
- **下次遇到这 2 条，直接引用此结论，不必重新定性。**

⚠️ **`npm run repo-health` 在本仓库不存在**（38 个脚本中零匹配）⇒ 记为**不可用**，非门禁项。

**未决 / 未做**：Garage 观感、奖励动画、内容量、Buff 平衡、挂点几何（本轮范围外）；
E2E 里的 A3/A4 与 7 条 EBUSY 未修（前者属测试卫生、后者属环境，Queue 4 明令不处理）。

## PRODUCT-LOOP-P0-MOVEMENT-RADIUS-SOURCE-OF-TRUTH（commit `38fee05`，branch `prototype-portrait-battle-lab`）
- **根因**：`BuildDraft` 的 `rearWheelDefId/frontWheelDefId` 与 `rearRadius/frontRadius` 是两条可各自漂移的字段。
  切回缺省轮时 `equipMovement` 只 `delete` defId 键、不碰 radius ⇒ 上一件轮组半径残留；
  `buildSnapshotFromDraft` 把它当 `overrides.radius` 写进 movements，`applyMovementOverrides` 做
  `{...def, ...overrides}` ⇒ **陈旧值覆盖 def 自身半径**。上一 Queue 只修了 Preview 读 def（显示对），
  但 Run Snapshot/Runtime 仍用残留值 ⇒ 屏幕是标准轮、跑起来却是小轮（显示与行为分叉）。
- **修复**：`equipMovement` 的缺省轮分支 + explicit 分支都把 radius 与 defId **同步写回 canonical 值**
  （缺省轮经新增 `defaultMovementRadius()`，explicit 经 `canonical.radius`）。`defaultMovementRadius()` 在
  `runMovementCanonical.ts`，与 `defaultMovementDefId()` 同一推导路径、刻意不读 overrides 里的陈旧值。
- **守卫适配**：MC-07b 魔法计数→**结构守卫**（defId 只许在表声明/hardpoint 判等；radius 只经 equip 写、用真源、
  不带裸数字、rear↔rearWheelDefId、front 为 rear 判等的 else 穷尽分支）。MV-08 作废「依赖缺陷残留」旧断言→
  重写为三方同值契约；新增 MV-08c（rear/front 独立不污染）。新增 `productMovementRadiusSourceOfTruth`（RD-01..RD-09）。
- **负控制**：A 恢复旧逻辑（只 delete）⇒ MV-08/MV-08c/RD-01/01b/03/03b/04a 必红；B 裸数字 20 ⇒ MC-07b/RD-07/RD-08 必红；均干净回退。
- **门禁**：tsc 零错；Movement targeted 201/201；product-home E2E 73/73（MV-1..MV-8 浏览器端取证，MV-4 确认
  `backToDefault.rear=wheelStd:66.67`）；边界取证冻结目录零改动。未跑全量 vitest/全套 E2E/build 全家桶（最小门禁）。
- **铁律沿用**：四路 SHA 核对 `38fee05aa05a08e08082cbd6181e5b1c2a256eec`；`.workbuddy/memory/2026-09-25.md` 不入库。

## PRODUCT-LOOP-R3-MOVEMENT-DIRECT-EQUIP（commit `448e929`，branch `prototype-portrait-battle-lab`）
- **目标**：删 Movement 二次确认。原链「选 Movement→找底部装备按钮→生效」过重；改为点 rear 卡即装 rear、
  点 front 卡即装 front、点未装载卡即卸下，**同一次交互**完成 persisted BuildDraft + 当前装备态 + Preview 三处更新。
- **改动**（仅 `src/product/homePage.ts`）：删 `selectedMovement` 选中态 / `movementEquipEnabled` 探针字段 /
  独立 `data-ph-action="equip-movement"` 按钮及其无效分支；新增 `equipMovementAndRender(hp, defId)`
  点卡直接走唯一写入口 `equipMovement()`（过 validateSnapshot、只经唯一 persistPlayerBuild 落盘）。
  **Weapon 区「先选后装备」流程原样保留**（未碰 renderWeaponSection）。
- **守卫适配**：MVR-14 改为断言源码**不再含** `selectedMovement`/`equip-movement` 且写入口仍唯一 `equipMovement(`；
  E2E M3/M8/equipWheel/MV-7 全部改为单次点击即装备（去掉二次按钮点击）。
- **负控制（结构性内建）**：E2E M3 单次点卡后即断言 `lastMovementEquip.ok===true`（退回只选中必红）；
  MVR-14 源码守卫（重新引入按钮/选中态必红）。
- **门禁（最小集）**：tsc 零错；Movement targeted 9 文件 201/201；product-home E2E **72/72**（M3 单点即装备、
  M5/M6/M8/M9/M10/M11 双挂点独立+reload+三方一致、MV-1..MV-7 预览直径按 def 真值、G3 Run Snapshot 与 Garage 一致、G4 武器槽不被顶掉）。
  边界取证冻结目录零改动；未跑全量 Gate/全套 E2E/build 全家桶（按 Queue 要求）。
- **铁律沿用**：四路 SHA `448e92955a5084e6acc784f027f20cc01d4663d6`；memory 不入库。

## R4 Body（Body Canonical + Garage MVP）—— 交接指针

- **现状**：Body 代码已写完并通过 `tsc --noEmit`（EXIT=0），但 **targeted 测试未写、未 commit/push**。
  - 新建（untracked）：`src/product/bodyInventory.ts`(129) / `runBodyCanonical.ts`(123) / `r4BodyChoiceSeed.ts`(249)。
  - 修改（modified）：`homePage.ts`(+252) / `playerLoadout.ts`(+185) / `playerGrowth.ts`(+39)。
- **HEAD 仍为 `448e929`**（R3-MOVEMENT-DIRECT-EQUIP）；所有 Body 改动在工作区未提交。
- **下一步**：① 写 Body targeted 测试 BG-/BV-/BM-（镜像 MVR + CS 模板）② 跑统一 Gate（tsc + 全量 vitest + product-home/loop/reward + Movement/Body E2E + builds + bundle-clean）③ 无修复不空 commit，有回归才最小修复+commit+push ④ 产出 8 项最终报告，**不进入下一批**。
- **完整交接**：`交接文档_2026-09-25_PRODUCT-LOOP-R4-BODY-CANONICAL-AND-GARAGE-MVP.md`（本地件，不入库）。

## R4 Body 产品化收口（MVP 收窄 + 直接装备）—— commit 3e48b16

- **两条补充约束已落地**：
  1. **MVP 最小验证集合**（不铺全部 4 台新车身）：`MVP_BODY_CHOICE_IDS = NEW_OFFICIAL_BODIES.filter(durianBody|mangoBody)`
     = 默认 watermelonBody（恒拥有，无需 seed）+ durianBody（重 135/1100/105）+ mangoBody（轻 48/950/95）。
     选型依据 = 真实 hp/baseMass/energyCapacity 三维差异最大，**不造「坦克/轻量」策划标签**。
  2. **直接装备**：点已拥有 Body 卡即 equip，无二次确认（`equipBodyAndRender` → `equipBody` 唯一写入口）。
- **r4BodyChoiceSeed 改动**：`grantAllNewBodies()` → 逐台 `grantBody(defId)`（MVP 2 台）；`raised`/`entries` 遍历 MVP。
- **测试**：BG-01..15（16 tests）+ BM-01..16（17 tests）+ BV-01..15（19 tests）三套全绿（52 tests）。
- **E2E 白名单同步**：`_e2e_product_home.cjs`（E5）+ `_e2e_product_reseed.cjs`（R2g）key 闭集各 +2
  （`strongfruit.r4BodyChoiceSeed.v1` + `strongfruit.ownedBodies.v1`）。
- **门禁（本 Queue 范围，非全量 Gate）**：tsc 零错；Body 三套 52/52；product-home E2E **72/72**；reseed E2E **21/21**；
  `build:portrait-lab` 成功。边界取证 `src/{core,battle,physics,render,game,presentation,ui}` 零改动。
- **提交**：`3e48b16` 单功能 commit + push，四路 SHA 一致（HEAD=local-ref=ls-remote=FETCH_HEAD）。
- **未做**（诚实披露）：未跑全量 vitest / 全量 E2E / 全套 builds（按 Queue「禁止全量 Gate」要求）。

## R4-CONFIGURATION-BATCH-GATE 统一收口 —— commit 8887ad6（真实回归修复）

**Gate 抓出 3 处真实回归（全部由 R4 Batch 引入），已最小修复：**

① **产品缺陷（必修）**：R4 在首页 `renderHome` 新增的「当前车身」独立区块（h2 + ul）
   把 actions 行**下推 91px**。在真实高内容态（URL 带 run=/reward= ⇒ 头部领奖提示；
   再叠加「不可冒险」兼容提示）下，「调整战车」/「开始冒险」被推到舞台 844 之下，而
   `documentElement.scrollHeight` 仍 = 844（舞台 overflow:hidden 不可滚）⇒ **按钮既看不见也点不到**。
   - 取证（真实鼠标 + elementFromPoint）：基线 448e929 `open-garage` box.y=**760**（命中生效）；
     R4 box.y=**851**，elementFromPoint=**null**（点击落空）。
   - 命中：`product_loop` **E2b** / `product_fail` **A9**（基线分别 53/53、34/34 全绿）。
   - 修法：**删掉该冗余摘要**（首页 `.ph-spec` 行本就展示车身名 + 耐久 + 能量）→ 首页高度
     **逐像素回到 R4 之前**（复测 y=707/760 与基线一致）；Garage 的 Body 配置区原样保留。
   - ⚠️ **教训：给 390×844 舞台加首页内容 = 可能把主 CTA 推出折叠线且不可滚。以后在首页
     增删区块必须复测 actions 行 y 坐标。**
② `productMovementOwnership` MO-01 夹具计数 3 → 4（未随第 4 份迁移标记同步）。
③ `productReseedR2` 的 `seedConsumedProfile()` 未预置 `markR4BodySeed()` ⇒ VR-03 键集**闭集**被破。

**另补齐**：`3e48b16` 遗漏提交的 5 个 R4 测试白名单适配（key 闭集 +2 / import 白名单 /
isolateMigrations 预置 R4 标记）+ 必验 C 取证 **BV-16/BV-17**（真实三维组合：
mangoBody + cannon + rear largeWheel + front smallWheel → 落盘 → reload → Run 链路；
只换 Body / 只换 rear 时其余维度逐字节不变）。

**Gate 结果**：tsc 0 错 · vitest **2550/2551**（唯一失败 = 既有负载抖动 LSE-09，三步已证非回归）
· home 72/72 · loop 53/53 · reward 57/57 · reseed 21/21 · star-power 22/22 · fail 34/34
· legacy 18/18 · default-entry 86/86 · build / build:pages / build:wechat / build:portrait-lab
· bundle-clean rc+wechat PASS · repo-health 无独立 CLI（无 npm script）⇒ 契约由 repoHealth.test.ts 覆盖。
边界：src/{core,battle,physics,render,game,presentation,ui,lab,player,platform} 零改动。

**方法论（可复用）**：判定「既有缺陷 vs 本轮回归」用 **git worktree add <dir> <baseSHA> +
junction 链接 node_modules + 在该基线 build/E2E 跑同一断言** —— 不动当前工作区、不用 git stash。
本轮即以此证明 R4 引入回归（基线 53/53 绿、当前红）。
⚠️ 清理 worktree 前**必须**先用 Windows 原生 rmdir 解联接（rmdir <worktree>\node_modules），
否则可能误删主体 node_modules。

---

## PRODUCT-LOOP-P0-GARAGE-MOBILE-INTERACTION-R1（`f357383`）

**动机（真人手机录屏）**：Garage 四维纵向铺成超长页 ⇒ 操作 Body/Movement 时 Preview 已滚出屏幕；
Weapon 两步（选中→装备）与其余一步不一致；未拥有与可用内容混排。

**改动面（7 files, +1289/−180）**：`home.html`(+78) · `src/product/homePage.ts`(437) ·
4 套 product E2E · 新增 `tests/productGarageMobileInteraction.test.ts`(577)。
**`src/core` 及 battle/physics/render/player/platform/game/presentation/ui 零改动**（diff 空）+ 冻结真源
`content.ts`/`contactRouter.ts` exit 0。

**新结构（三段式兄弟节点，结构性保证）**：
`.ph-main.ph-main-garage`（整页 `overflow:hidden`）内含 `.ph-garage-top`（固定：Preview + 4 Tab）/
`.ph-garage-body`（**唯一** `overflow-y:auto`）/ `.ph-actions`（固定：返回首页）。
Tab 键名与 `MOVEMENT_HARDPOINT_LABELS` **刻意同名** ⇒ `renderMovementSection(body, garageTab)` 无第二张映射表。
`render()` 里 `stage.classList.toggle('ph-main-garage', view === 'garage')` 是唯一开关。

**删掉的旧交互**：`selected` 中间态、`[data-ph-action="equip"]` 二次按钮、探针
`selectedWeaponId`/`selectedWeaponStar`/`equipEnabled`、5 个状态标签常量
（EQUIP/PICK/PICKED/EQUIPPED/DEFAULT）+ 2 个 SECTION_LABEL + LOCKED_SECTION_LABEL。
新探针：`garageTab` + `garageEquipButtonCount`（运行时 `querySelectorAll` ⇒ 恒 0，非「源码里没写」）。

**390×844 实测**：docScrollH=844=winH · car=[67..228] tabs=[236..272] body=[278..778] actions=[778..844]
screen.bottom=844 · body=auto / main=hidden / screen=hidden · bodyScrollH=bodyClientH=500 ·
4 Tab + 0 二次按钮 · body 分类 locked=2/inDetails=2/usable=6（`<details>` 默认闭合）。

**Gate**：tsc 0 错 · targeted vitest **22 files / 439 tests** ·
home E2E **80/80**（含新增 GM1..GM8）· loop 53/53 · fail 34/34 · reward 57/57 ·
**未跑全量 Gate**（Queue 明令）。四路 SHA `f3573834acb80521b1e1775b172c3d909c2e787a` 一致。

**可复用方法论**：
① E2E 改「单分类页」时**不要删断言**，把「一次 `querySelectorAll` 抓 8 张」改成
   **两个分类各采一遍再合并**（`gotoGarageTab` 幂等 helper：不在 garage 先进页，不在该 tab 再点）——
   MC1..MC6 一字未改。
② `garageTab` 是**模块级 `let`**，`page.reload()` 会重置成 `weapon` 但**视图切换不会** ⇒
   写 E2E 时必须显式切 tab，不能依赖「上次留下的分类」。
③ 负控制两轮（`renderMovementSection(stage,…)` / `phAction='equip'`）各只让**目标断言**变红，
   回退后 grep 确认零残留。
④ 源码守卫查英文禁用词时会误伤（`store` 命中 `storedDefId`）⇒ 英文侧只查 `ph-*` 类名形态，
   中文侧查词本身；且一律先 `strip()` 剥注释（注释里大量出现「默认」「已装备」）。


---

## PRODUCT-LOOP-P0-GARAGE-SLOT-INTERACTION-R2（`a51de9b`）

**动机（真人手机录屏，三个 P0）**：① 不知道装备该「拖动」还是「点击」；② 战车 Preview 朝向与正式战斗
**相反** ⇒ 前轮 / 后轮认知反转；③ 不知道从哪里进合成。诊断 = **装备操作模型本身不自然**，不是字号 /
gap / 卡片尺寸问题（Queue 明令不许用缩字号 / 压 gap 维持旧结构）。

**改动面（6 files, +1296/−351）**：`home.html` · `src/product/homePage.ts` · `src/product/vehiclePreview.ts` ·
`tests/{_e2e_product_home.cjs, productGarageMobileInteraction.test.ts, productLoopHomeGarage.test.ts}`。
`src/{core,battle,physics,render,player,platform,game,presentation,ui,lab}` **diff 空**；
`content.ts`/`contactRouter.ts` `--exit-code` 通过。**无新增 storage key / 迁移模块 / 商城 / 教程 / 评分。**

**新结构（五段兄弟节点）**：`.ph-main.ph-main-garage`（整页 `overflow:hidden`）内含
`.ph-header`（标题 + `.ph-back` 返回，`back-home`）/ `.ph-garage-stage`（**固定**：`.ph-car-wrap` + 4 个
`.ph-slotnode`）/ `.ph-my-parts`（「我的装备 · <当前槽>」）/ `.ph-garage-body`（**唯一** `overflow-y:auto`）/
`.ph-garage-foot`（合成入口）。
⚠️ **槽位节点挂在 `.ph-car` 内部**（不是 stage）：`left` 与 `top: calc(100% + 6px)` 都以**车体**为基准；
挂到 stage 会让 `100%` 变整段高度 ⇒ 槽位落到「我的装备」上（实测踩过：slot top:322 vs myParts top:316）。

**四槽 ↔ 配置字段**：`weapon→functionalSelections[WEAPON_SLOT]` / `body→bodyDefId` /
`front→frontWheelDefId` / `rear→rearWheelDefId`。常量 `GARAGE_SLOT_ORDER` / `GARAGE_SLOT_LABELS`
（旧的 `GARAGE_TAB_ORDER` / `GARAGE_TAB_LABELS` / `GarageTab` 全部删除；探针 `garageTab`→`garageSlot`）。
槽位横向 = 该件**正式挂点** `localPosition.x`（`previewOffset` 换算 + 夹取 ±`GARAGE_SLOT_MAX_DX`=130）；
武器挂车体**上方**、其余挂**下方**（纵向刻意不用挂点 dy，否则标签压住它指向的部件）。

**必改 3 根因（本轮最有价值的一条）**：R1 期 `vehiclePreview.ts` 文件头自述「y 向上，与物理一致」+
`renderPreview` 用 `-(it.cy - by) * scale` ⇒ 比正式链路**多取一次反** ⇒ 整车**上下镜像**。
正式链路三处同口径 = **y 向下**：`ARENA_A_GRAVITY.y=+10`（`groundY=700`/height 900）·
`battleContract.visualWorldTransform`（`position.y = physPos.y + anchor.y`，不取反）·
`runPageLayout.placeSideViewVisuals`（`y0 = s(b.cy - b.h/2)` 直接当屏幕 y）。
修法 = 统一 y 向下、视图层不翻转；抽出**唯一**换算函数 `previewOffset(layout, cx, cy)`
（`{dx:(cx-bx)*scale, dy:(cy-by)*scale}`，`bx/by`=包围盒中心），`renderPreview` 与槽位定位**共用**
⇒ 两者坐标不可能漂移。新增 `vehiclePreview.vehicleSlotAnchors(draft)`（锚点真源 = 正式 `BodyDef`
挂点）—— 放 `vehiclePreview` 而**不是** `homePage`，因为 `PL-26` 的 import 白名单是**闭集**，
`homePage` 加 `../core/content` 会直接红。

**必改 2 硬证据（运行时数 DOM，不是「源码里没写」）**：探针
`garageSlotCount`（`[data-ph-slot]` 恒 4）· `garageEquipButtonCount`（`[data-ph-action="equip"]` 恒 0）·
`garageDraggableCount`（`[draggable="true"]` 恒 0）。源码侧另有 `not.toMatch(/drag(start|end|over|enter|leave)\b/)`
+ `not.toMatch(/\.draggable\s*=/)`。

**必改 4 合成入口**：`.ph-fuse-entry` **无条件**渲染（`stage.append(garageFuseEntry(r))` 是唯一调用点）；
有可合成组 ⇒ `可合成 N` + `.ph-fuse-entry-ready`（红点 `::before`）+ 可用；无 ⇒ `暂无可合成` + `disabled`。
点击**只带路**：`fuseFocusPending = true; garageSlot = 'weapon'; render();`，渲染末尾对第一张
`[data-ph-fuse-focus="1"]` 卡 `scrollIntoView({block:'center'})` + `focus()`。
⚠️ `home.split('fuseStack(').length - 1 === 1` —— 全页仍只有正式武器卡上那一个调用点，入口**不自己合成**。

**390×844 实测（DPR 2，scale 1）**：`docScrollH=844=winH`；
stage=[59..332] · car=[65..328] · myParts=[332..357] · body=[357..777]（`clientH=scrollH=420`）·
foot=[777..844] · fuse rect=12,786 366×44；`body=auto / main=hidden / screen=hidden`；
slots=4 · equipButtons=0 · draggables=0 · fuse ready=false 但仍可见。
**朝向实测**：wheelF cx=300.67 / wheelR cx=82.49 / weapon cy=165.75 < 两轮 cy=206.66 ·
slotF cx=300.67（贴前轮）/ slotR cx=82.5。

**Gate**：tsc 0 错 · targeted **28 files / 558 tests** ·
product-home E2E **89/89**（GS1..GS13 全绿）· product-reward 57/57 · star-power 22/22 · loop 53/53 ·
`build:portrait-lab` EXIT=0 · **未跑全量 Gate**（Queue 明令）。四路 SHA
`a51de9b5411cf89f3eb2a075258e19f2cb943402` 一致。

**可复用方法论（新）**：
① **验收逐字项必须在 E2E 里 1:1 落地**：验收 2「点 Front 槽 → 点 smallWheel → 前轮立即变化」与
   验收 3「点 Body 槽 → 点 mangoBody」都做成了独立断言（含**基线前置断言**「当前不是它」，否则
   「点了就变」无从观察）。前置拥有关系走**既有正式种子**（`smallWheel`∈`OFFICIAL_MOVEMENTS`；
   `mangoBody`∈`r4BodyChoiceSeed.MVP_BODY_CHOICE_IDS`）⇒ **不新增解锁入口、不放宽断言**。
② **新流程不要插进既有长 E2E 的中间**：把「装备 front」这类动作插在桌面主流程里会撞
   `M5`（断言 `front.storedDefId === null`）与 `M6`（断言存档里没有 `frontWheelDefId`）⇒
   正确做法是放到**独立 390×844 context**（全新 localStorage）里做，主流程一字不动。
③ **快照要重取**：`measure()` 的结果在换过车身 / 换过前轮 / reload 之后就是**旧 DOM 的快照**，
   拿它断言等于没量 ⇒ 重新 `measure()` 成 `L2` 再用。
④ 探针字段**嵌套**要看清：车身是 `p.body.bodyDefId`（不是 `p.bodyDefId`）；Movement 是
   `p.movement.slots[].effectiveDefId`。
⑤ `!!L.fuseReadyCount` 会把合法的 0 判成假 ⇒ 数值断言写 `>= 0`，语义断言单独查 `fuseEntryReady === 'false'`。
⑥ 负控制两轮：`dy: -(cy - by) * layout.scale`（GS-10 变红）与 `btn.draggable = true`（GS-07b 变红），
   回退后 `grep -n "draggable = true\|dy: -(cy\|-(it.cy"` 确认 ZERO RESIDUE。
⑦ 测试文件从「R1 分类 Tab 版」升级时，**用例 ID 换前缀**（`GM-*` → `GS-*`）并同步文件头映射表，
   避免同名 ID 在两个轮次里语义不同（本仓库 `GM-*` 只在 E2E 段名里还有历史含义）。
