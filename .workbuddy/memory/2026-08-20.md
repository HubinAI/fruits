# 2026-08-20 工作日志

## Q06-UX-R1｜Build 测试交互重构（P0，commit e051e83，已 push）

- **问题（真人录屏）**：Build Editor 与 Scenario 同时显示/占中央画面、改 Build 中央不变化、
  默认跑 Scenario、无 Weapon 时 Start 被阻止但反馈不明显。
- **改动（4 文件，+384/−87）**：
  - physicsLab.ts：新增 `loadCustomPreview(buildA, buildB)`（engine:'planck' + autoDrive:false +
    `step()` 跳过推进，只显示真实 Planck 组装；同一 getRenderSnapshot/Renderer；非法 Build 也创建
    裸车 Preview）；`get previewMode()`；loadScenario/loadCustom/clear 同步 isPreviewMode；
    reset() 按 currentCustom.config 判定重建 preview 或 battle。
  - buildEditorModel.ts：`SLOT_LABELS` + `slotLabel()`（front→前端 / frontMass→前部 / top→顶部 /
    rear→后端，未知回退原 id）。
  - main.ts 全量重构：顶层模式【装配测试】(默认)/【机制场景】互斥切换（控件严格互斥，
    切回恢复 Draft Preview）；三态 Editing→Fighting→Ended（Fighting 锁定 A/B 全部 Build 控件 +
    Preset 按钮 + 装载按钮，不允许战斗中编辑下一局）；编辑后非战斗态立即 showPreview()；
    默认 Draft A=boxBody+cannon / B=heavyBox+cannon（首屏合法+Start 可点）；Start 阻断原因直显
    （「A：… / B：…」+ disabled 样式）；槽位主标签位置语义；Result hp 取整（Math.round）。
- **关键设计**：preview 用「PhysicsLab.step 跳过」而非 autoDrive:false 单独——因为 CannonBehavior
  会自发射，只有 step 不跑才能真正「不推进战斗」。
- **验证**：targeted 17 文件 100/100（新增 buildUx 7：preview→Planck+previewMode / step 不推进对照
  loadCustom 推进会动 / 默认 Draft valid / 删 Weapon→阻断原因含 Weapon / 槽位自然名称 /
  cannon→hammer preview 立即重建且仍 previewMode / 非法 Build 裸车 preview（parts 0 + wheels 2））；
  Q06 build 全家 + Cannon/Hammer/Push 全量回归通过；scoped tsc EXIT=0；git diff --check 干净；
  vite build 36 modules transform 全过（产物清理沙箱限制同前）。
- HEAD=e051e83056955ca2482c2da5aeeca01bf7e7f181（parent 454d7c6=Q06-B1），远端已同步，
  tracking ref 已补。待验收：真人打开页面确认首屏 Preview / 改下拉立即变外形 / 战斗锁控件 /
  结束后改配置再战 / 模式切换互斥。

## Q06-HUD-F1｜双方实时战斗状态合同（commit d3c6642，已 push）

- **问题**：BattleResult 只在结束时被 UI 消费，战斗过程无引擎中立实时 HP。
- **改动（4 文件，+216）**：
  - battleContract.ts：`BattleSideStatus{team,hp,maxHp}` + `BattleStatusSnapshot{sideA,sideB,phase}`；
    BattleOrchestratorApi 新增 `getBattleStatusSnapshot()`。
  - battleOrchestrator.ts（Matter）与 planckBattleOrchestrator.ts（Planck）统一实现：
    hp 直读 vehicle.hp、maxHp 直读 vehicle.maxHp（=Resolved 初始 body.hp）、phase 复用 this.phase。
    纯读取无副作用。
- **验证**：battleStatus 6/6（初始 hp=maxHp（Matter+Planck）/ 真实 ramHead 直击 80 后 B hp 实时下降
  且 A 独立 / End 后 status 与 result.hpA/hpB 一致（Matter+Planck，B.hp=0 → detectEnd 模式）/
  进行中连续读取无副作用）；相关回归 51/51；scoped tsc EXIT=0；diff-check 干净。
- HEAD=d3c6642f1a24fbb1714374bbc7f6c5a6123ab81c（parent e051e83=Q06-UX-R1），远端已同步。
  下一步：HUD UI 队列（消费 getBattleStatusSnapshot 实时显示双方 HP）。

## Q06-HUD-U1｜战斗 HUD + 结算卡（commit 17a51a3，已 push）

- **改动（2 文件，+315/−37，仅 main.ts + battleHudUx.test.ts）**：
  - Fighting 专注模式：Start 后 A/B Build 面板收起、Canvas 自动扩展 + reframe 一次；
    顶部固定 HUD（A 蓝 / B 橙 HP Bar + 整数 HP 文本 + 中间「战斗中」），每帧读
    getBattleStatusSnapshot()，bar=clamp(hp/max)，不用 Debug overlay。
  - Ended：中央半透明遮罩 + 大结算卡（A 视角 胜利/失败/平局 + 双方整数剩余 HP）；
    删除 toolbar 小字 resultLabel；Build 控件结算卡背后保持锁定，先点「调整配置」；
    【调整配置】→ Editing + 面板恢复 + Draft Preview；【原配置再战】→ 同 Build
    直接重建 battle → Fighting + HUD 回满。
  - Editing：HUD 隐藏、面板正常、实时 Preview、Start 按 Validator。
- **验证**：battleHudUx 5/5（HUD 数据源每帧可读且 HP 实时下降（ramHead 80→920/1000
  bar 0.92）/ 结算 winner+整数 HP / 原配置再战 HP 回满 / 调整配置回 previewMode /
  clamp+整数规则含超界与除零保护）；Q06 build/UX + Cannon/Hammer/Push 全量
  19 文件 111/111；scoped tsc EXIT=0；diff-check 干净；vite build 36 modules transform 全过。
- HEAD=17a51a3382422ebba0f86f766105c3c6d00c308f（parent d3c6642=Q06-HUD-F1），远端已同步。
  至此 Build Loop 完整闭环：配置→实时预览→开战（专注模式+HUD）→结算卡→调整/原配置再战。

## W1-R0｜Battle Foundation Closure 技术定位（只读，0 改动）

- **1. Battle End/Closing**：ArenaPhaseClock（Active 10s→Warning 3s→Closing 5s→End，>=判定
  单次跨一阶段）双引擎同构；detectEnd 用 resolveBattleResult(phase,hpA,hpB)；**平局路径**：
  ①End 且 HP 相等→draw ②非 End 同帧双死→draw。**关键缺口**：刺墙 hazard 只 tag 无伤害路径
  （DamageSource 'hazard' 无生产消费；Closing 只物理挤压）；正式「无平局」需 tie-break 规则。
- **2. Generic Behavior**：PlanckBattleOrchestrator 对 cannon/hammer/pushRod 硬编码三组
  专属数组+构造循环+onBeforeStep 驱动；新增部件要改 4 处。缺口=无 Behavior 注册表/工厂；
  Matter 侧 L121 仅注释。迁移=orchestrator 内部重构（行为类不动）。
- **3. Hit Policy/Events**：ContactRouter 引擎无关路由完整——Impact（batch team 对去重、
  阈值 0.75/0.5 每速/max120、双方各半、one-shot start 才结算）、Direct Weapon（weapon part
  去重、阈值 0.5、baseDamage 一次）、Projectile（实例级 BodyHandle 去重、drain→Behavior 销毁）。
  缺口：无 continuous tick/cooldown 接口；CombatEvent 无 eventType（hit/fire/death/impact），
  death 靠 hpAfter===0 推断、fire 无事件。
- **4. Visual≠Collider**：Renderer 只消费 RenderShape（不直接读 ColliderDef ✓）；但 Planck
  snapshot 直接用 ColliderDef→RenderShape、Matter 用物理 vertices（也源于 ColliderDef）
  →渲染几何≡碰撞几何，无 VisualDef。
- **5. Async**：全 src grep Math.random/crypto=0 → **当前 Runtime 无非确定随机源**（planck
  固定步进确定性引擎）；BattleConfig/BuildSnapshot 无 battleId/seed/版本；BattleResult 无
  endReason/battleId。缺口=BattleRequest + 工厂 + 结果稳定字段。
- **建议队列**（全部可并行，独立模块）：W1-END-1 tie-break / W1-END-2 刺墙伤害；
  W1-BH-1 BehaviorRegistry；W1-EV-1 事件结构化 / W1-EV-2 HitPolicy 接口；
  W1-VIS-1 VisualDef；W1-ASYNC-1 BattleRequest 工厂。
- **冻结清单**：BattleResult 规则、hit policy 默认参数、阶段时钟/arena 几何、Behavior 注册表
  契约、CombatEvent 结构、VisualDef 契约、BattleRequest/Result 合同、Cannon/Hammer/Push 参数、
  物理 Foundation。HEAD 仍 17a51a3。

## W1-ASYNC-1｜Async Battle Contract + Deterministic Seed（commit d3e1648，已 push）

- **改动（3 文件，+275/−1）**：
  - battleContract.ts：BattleRequest{battleId, buildA, buildB, config, randomSeed(uint32),
    rulesVersion, contentVersion}；mulberry32 确定性 PRNG（禁 Math.random/crypto）；
    deterministicTieBreak(seed)→'A'|'B'（未来无平局兜底）；BattleResult 可选 metadata
    （battleId/rulesVersion/contentVersion/durationMs，只携带不改胜负规则）。
  - battleRequestAdapter.ts（新）：createPlanckBattle(request, registry) + battleResultWithMeta
    （winner/hp/phase 原样透传，durationMs=orchestrator.timeMs）；旧入口全保留。
- **验证**：battleRequest 6/6（同 request 重建输入一致=同 settle 初始位置 / tie-break 同 seed
  同结果 + 两个 seed 产生 A/B / mulberry32 同序列 [0,1) / metadata 完整 + 胜负不变 / 旧入口
  Matter+preview+旧 resolveBattleResult 兼容）；回归 10 文件 62/62；scoped tsc EXIT=0；
  diff-check 干净；全 src+新测试无非确定随机源（仅注释）。
- HEAD=d3e164817eb4c9f2c28165c211852d16a761f388（parent 17a51a3=Q06-HUD-U1），远端已同步。
  下一候选：W1-END-1（tie-break 正式接入无平局）/ W1-BH-1（BehaviorRegistry）。

## W1-EV-1｜Battle Event 正式结构（commit 376f74a，已 push）

- **改动（20 文件，+369/−77）**：
  - combatEvents.ts：BattleEvent = DamageEvent|WeaponFireEvent|DeathEvent；DamageEvent 保留
    CombatEvent 全字段 + type:'damage'；WeaponFireEvent{type,team,partId,behavior,
    worldPosition,worldDirection,timestamp}；DeathEvent{type,team,sourceTeam?,damageSource?,
    timestamp}；CombatEventBus 支持 BattleEvent；三个类型谓词。
  - damageResolver.ts：damage 事件带 type；HP 首次 >0→<=0 追加 death（只一次）。
  - cannonBehavior.ts：onFire 回调仅真正创建 projectile 后触发（fire 成功）；Hammer/Push 不设置；
    orchestrator 接线补 timestamp emit。
  - 兼容：onCombatEvent 签名 BattleEvent；renderer 只消费 damage（不猜 death/fire）；
    13 个测试文件 listener 加 isDamageEvent 判别（字段/数值断言不变）。
- **验证**：battleEvent 4/4；全量回归 25 文件 131/131；scoped tsc EXIT=0；diff-check 干净。
- **要点**：death 判定在 DamageResolver（唯一扣血入口）——「只发一次」由 hpBefore>0 && hpAfter<=0
  天然保证；fire 事件位置在 cannon fire() 的 createDynamicCircle 之后（真实发射语义）。
- HEAD=376f74a010437afa71b18d4daceefa29ceecc708（parent d3e1648=W1-ASYNC-1），远端已同步。
  下一候选：W1-BH-1（BehaviorRegistry）/ W1-END-1（无平局 tie-break 接入）。

## W1-BH-1｜Generic Behavior Runtime Registry（commit 493fa76，已 push）

- **问题**：orchestrator 硬编码 cannons/hammers/pushRods 三套数组/循环，Content 膨胀会持续改中央。
- **改动（5 文件，+313/−91）**：
  - behaviorRuntime.ts（新）：PartBehaviorRuntime 统一生命周期（beforePhysicsStep /
    afterPhysicsStep? / destroyOutOfBoundsProjectiles? / getRenderProjectiles?）+ 三 wrapper
    （Cannon 含 projectile 消费/越界/渲染/fire 事件 timestamp=step timeMs；Hammer/PushRod
    仅 beforePhysicsStep）——内部复用原 Behavior 类，玩法/参数/timing 零改动。
  - behaviorRegistry.ts（新）：behavior id → factory；新机制只注册不再改 Orchestrator。
  - planckBattleOrchestrator.ts：三组字段/构造循环/before-step 循环/after-step/越界/渲染
    全删 → 统一 behaviors[]（registry 创建；onBeforeStep 统一 beforePhysicsStep；after-step
    统一 afterPhysicsStep+越界；snapshot 统一 getRenderProjectiles）。grep 确认无专属数组残留。
  - buildFlow.test.ts：接线断言改 behaviors[] 按 behavior 分组统计。
- **验证**：behaviorRegistry 3/3（注册表含三件+未知 undefined / cannon runtime 真实发射+渲染
  贡献 1 / 生命周期差异 cannon 有 projectile 能力 hammer/pushRod 无）；全量回归 28 文件 144/144；
  scoped tsc EXIT=0；diff-check 干净。
- HEAD=493fa76c805fcee26a5eb3a91d073829bacbd260（parent 376f74a=W1-EV-1），远端已同步。
  剩余 W1-R0 候选：W1-END-1（无平局 tie-break）/ W1-END-2（刺墙伤害）/ W1-VIS-1（VisualDef）。

## W1-VIS-1｜Visual 与 Physics Collider 正式解耦契约（commit c72cb91，已 push）

- **改动（5 文件，+343/−34）**：
  - types.ts：VisualDef{visualId,size,anchor,rotation,layer,mirrorWithFacing}；Body/Wheel/
    FunctionalPart 加 optional visual（不参与物理）。
  - battleContract.ts：RenderVisual{visualId,position,rotation,size,layer}；RenderVehicle+
    bodyVisual?/wheelVisuals?、RenderFunctionalPart+visual?；共享 visualWorldTransform
    （anchor 随真实 rotation 旋转、facing=-1+mirrorWithFacing 镜像 anchor.x 与 rotation、
    rotation 叠加、size/layer 透传）。
  - 双 orchestrator snapshot：有 VisualDef → RenderVisual；无 → Collider Shape fallback 原样。
- **验证**：visualDef 4/4（无 visual 时 snapshot 不变 / transform 精确 / facing=-1 镜像 /
  collider 不受影响）；renderer+三机制+battle 回归 17 文件 86/86；tsc EXIT=0；diff 干净。
- HEAD=c72cb91a1dab91b1ba9bd2d404e06f4875a813cc（parent 493fa76=W1-BH-1），远端已同步。
  剩余 W1-R0 候选：W1-END-1（无平局 tie-break）/ W1-END-2（刺墙伤害）。

## W1-HIT-1｜Generic Contact Hit Policy（commit c9648ec，已 push）

- **改动（4 文件，+397）**：
  - contactRouter.ts：ContactHitPolicy = contactOnce | contactTick{intervalMs, damage,
    minRelativeVelocity?}；readHitPolicy（缺省/非法 → contactOnce，Hammer 零变化）；
    contactTick 生命周期：start→registerActiveTick（key=attacker team+part instance+
    target team；minRel 缺省=WEAPON_CONTACT_THRESHOLD；登记不立即伤害）、
    advanceContactTicks(nowMs) 按固定物理时间 interval 结算（无 setInterval/墙钟/渲染帧）、
    end→removeActiveTick 立即停止；handleWeaponContact 分派（contactOnce baseDamage 原路径）。
  - 双 orchestrator：每物理步后 advanceContactTicks(this.time)。
  - 坑：TS class 体内不能声明 interface（解析错位报错在别行）→ ActiveTickContact 移到模块顶层。
- **验证**：contactTick 6/6（解析/回退 / contactOnce 一致 / 1s 可预测 tick（0→999 无、1000 首、
  2000 次）/ end 停止+重接触重计时 / 同批多 pair 单 key / 两独立 weapon 独立 tick+单方 end）；
  全量回归 22 文件 113/113；tsc EXIT=0；diff 干净。
- HEAD=c9648ec92591c78bd0fef38c47632abd02d8047b（parent c72cb91=W1-VIS-1），远端已同步。
  剩余 W1-R0 候选：W1-END-1（无平局 tie-break）/ W1-END-2（刺墙伤害）。

## W1-END-1｜正式无平局 Battle Result（commit 72d964c + e9710c4，已 push）

- **规则**：正式战斗不允许平局。①只有 A 死→B胜、只有 B 死→A胜（endReason='hp'）；
  ②同一 fixed-step 双方死亡→deterministicTieBreak(randomSeed)；③Arena End→HP 高者胜
  （endReason='arenaEnd'）；④End 同 HP→同 seed tie-break。普通 HP 胜负不读 seed。
- **改动（10 文件，+152/−64 主 + 16/−7 卫生）**：
  - battleContract.ts：BattleResult.winner 正式类型 'A'|'B'（去 draw/null）+ endReason:
    'hp'|'arenaEnd'；BattleConfig.randomSeed?（缺省 0，禁 Math.random）；
    resolveBattleResult(phase,hpA,hpB,randomSeed)。
  - battleOrchestrator/planckBattleOrchestrator detectEnd 传 config.randomSeed ?? 0；
    battleRequestAdapter.createPlanckBattle 注入 request.randomSeed。
  - main.ts 结算卡删「平局」分支（胜利/失败双态）。
  - tests：battleContract 重写 10 用例（同 seed 一致/不同 seed A/B/普通胜负不受 seed）；
    battleRequest/buildLoop/planckBattleOrchestrator 迁移。
  - test-hygiene（独立 commit）：修 battleEvent.test.ts 既有编译错误（maxHp stub /
    never 类型断言 / 未用 import），运行语义零变化（4/4）。
- **验证**：相关回归 30 文件 160/160；全量 73 文件 336 通过、仅 2 个既有失败
  （impactProtection「低速持续挤压不掉血」999.9375 vs 1000、weaponThreshold「只减 80」
  919.9 vs 920——**已在 HEAD c9648ec 原版 checkout 复现相同失败，与本次无关**，
  疑似既有伤害阈值抖动，建议后续 W1-END-2/独立队列排查）；scoped tsc EXIT=0；
  diff-check 干净；UI 无 draw 残留。
- **【严重环境事故+恢复路径（重要）】**：git stash 失败后检测到 `.git/objects/pack/*.pack`
  文件丢失（仅剩 .idx + multi-pack-index）、全部 refs 丢失 → 本地历史不可读
  （git cat-file/rev-parse 全失败）。**恢复**：`git fetch origin`（SSH 可达，远端完整）
  恢复对象 → 手动重建 ref（mkdir -p .git/refs/heads && printf sha > ref，SHA 来自
  `git ls-remote origin foundation-02-canonical`）→ `git reset -q` 重建 index →
  恢复完好。教训：本地 pack 可能被环境清理删除；**commit 后立即 push 是唯一安全策略**；
  恢复路径已验证可用（远端始终是完整事实源）。
- HEAD=e9710c4285cd5658caf869cd4ac7d7322ce8403f（parent 72d964c=W1-END-1 →
  parent c9648ec=W1-HIT-1），远端已同步，tracking ref 已补。
- 剩余 W1-R0 候选：W1-END-2（刺墙 hazard 伤害路径）。另发现全量 tsc 既有债：
  impactThreshold / planckContactKinematics / planckDriveCalibration /
  planckRevoluteSpike / wheelHardAxleDiagnostic / wheelStiffnessDiagnostic（均未触碰）。

## W1-END-2｜Closing Spike Wall Hazard Damage（commit 14946a7，已 push）

- **问题**：Closing Wall 真实推进但 hazard tag 无正式伤害路径（W1-R0 发现）。
- **改动（5 文件，+484/−33）**：
  - arenaConfig.ts：ArenaConfig 增加 hazardTickMs / hazardDamagePerTick（DEFAULT 500ms/40
    骨架占位，规则冻结、数值不冻结）。
  - contactRouter.ts：ActiveTickContact 泛化 weapon|hazard 联合；handleHazardContact
    （Active/Warning 0 伤害不登记；Closing start 登记 `hazard|<team>` 去重；end 移除；
    hazardConfig 缺省未配置 → 无伤害）；route() 在 batch 收集前处理 hazard；
    advanceContactTicks(nowMs, arenaPhase?)——hazard 仅 Closing 结算、非 Closing 移除；
    damage 走 DamageResolver、damageSource:'hazard'（source/target=被压车辆）、固定物理时间。
  - battleOrchestrator（Matter）/ planckBattleOrchestrator（Planck）：router 注入 arena
    hazard 参数；接触监听/advance 传当前 arena.phase。普通墙（'arena'）/Ground 无 hazard 伤害。
- **验证**：hazardDamage 7/7（Active/Warning 0 / Closing 周期下降首 tick 从接触计时 / 离开即停 /
  CombatEvent 'hazard' / 普通墙+Ground 无 / 真实 battle 压死唯一赢家+墙推进不变 / 双死同一步
  →END-1 seed 唯一赢家）；回归 28 文件 139/139；全量 74 文件 343 通过、仅 2 个既有失败
  （impactProtection/weaponThreshold）；scoped tsc EXIT=0；diff-check 干净。
- **物理坑（重要）**：kinematic 刺墙推轮式车辆时保持 ~1px skin 缝隙（轮子滚动滑动），
  begin 接触不稳定 → 「双方同时被压死」不能靠自由物理保证；orchestrator 级只断言「至少一方
  被压死 + 唯一赢家 + 墙推进」，「双死同一步 → seed tie-break」用 router 级确定性测试
  （单次 advanceContactTicks 齐发两 tick = 同一 fixed-step 双死）覆盖。测试碰撞世界必须建
  Ground（无 tag 仅物理阻挡），否则车辆下落穿过刺墙、接触 end 移除 tick、0 伤害。
- HEAD=14946a7236d57ac6d0ab9f1721e2cab89ff332ef（parent e9710c4=W1-END-1 卫生 commit →
  72d964c=W1-END-1），远端已同步，tracking ref 已补。**W1-R0 全部候选完成**：
  ASYNC-1/BH-1/EV-1/VIS-1/HIT-1/END-1/END-2 已闭环；剩余待办：2 个既有失败测试 + 6 个
  既有 tsc 债文件（建议独立卫生队列）。

## W1-INTEG｜Battle Foundation Closure Integration（commit 3ed2c63，已 push）

- **原则**：只集成、补测试、修真实集成 Bug（0 production change）。
- **新增 tests/battleIntegration.test.ts（7 用例，一次通过）**：BattleRequest →
  createPlanckBattle → 正式 Planck Runtime 完整战斗。
  1. 同 request 两次：winner/hpA/hpB/duration 位级一致 + 无 draw（确定性成立）；
  2. Closing hazard 真实扣血（'hazard' 事件 + hp 下降）；
  3. 压死场景两次一致 + 双死条件断言 tieBreak(seed)（同一步双死精确覆盖在 W1-END-2）；
  4. Cannon×2/Hammer×1/PushRod×1 统一 behaviors[] lifecycle（weaponFire 真实产生）；
  5. contactOnce weapon 直伤事件发生（contactTick 由 contactTick.test 覆盖）；
  6. 无 VisualDef fallback（polygons + visual undefined）；
  7. battleResultWithMeta 保留 battleId/rulesVersion/contentVersion/durationMs。
- **无触发停止条件**：无 Physics 参数重调、无机制体验变化、无非确定性（两次运行位级
  一致，无需补测试阈值绕过）。
- **验证**：全量 75 文件 350 通过、仅 2 个既有失败（impactProtection/weaponThreshold）；
  集成文件 scoped strict tsc EXIT=0（全量仅剩 6 个既有测试债文件）；vite build 38
  modules transform 全过（产物清理沙箱限制）；diff-check 干净。
- **W1 Foundation Closure 正式完成**。HEAD=3ed2c63b28ddb70f50ce394ac72c492cb8fb7274
  （parent 14946a7=W1-END-2），远端已同步，tracking ref 已补。
- **待办（不阻塞 W2）**：2 个既有失败测试（impactProtection/weaponThreshold）+ 6 个
  既有 tsc 债文件；建议独立卫生队列后进入 W2。

## W1-P0-CLOSE-R1｜Closing 车辆/部件消失 —— 停止分支（根因已定位，0 改动，无 commit/push）

- **现象（真人 1x 录像）**：Closing 后 ~19.4s 异常超长部件连接线 → 车辆离开可视区域 → 只剩
  Hazard Damage 数字悬空。
- **诊断**：正式 Planck Runtime + 含 Push Rod 的 Custom Build + 短 phase（closingSpeed=40
  正式默认），逐 fixed-step 记录 chassis/part/translation/connector/墙/arena/hp/phase。
- **判定：A 类（Physics）主根因 + C 类（Camera）次根因；B 类排除。**
  - **A 类（主根因，证据）**：closingSpeed=40px/step 的 kinematic 刺墙每步移动 40px（车半宽 60
    的 2/3），两车对撞顶住后墙持续推进 → **墙体穿透车辆** → 巨大速度脉冲 → Push Rod Prismatic
    joint 约束崩溃：step84 trans 瞬间 **1026**（limit 90）、稳定后 96~99（>90 越界）；rod part
    与 chassis 真实分离（connector 1570px 是真实物理分离，非 snapshot 错误——B 类排除）；
    墙穿透后继续相向飞（wall0→2280 穿出 arena 右界 1780）。**排除 solver 迭代因素**：stepFixed
    临时改 8/8 position iterations 数据完全一致（已回滚）。
  - **对照实验（根因确认）**：同诊断仅 closingSpeed=4（测试内覆盖）→ **firstAnomaly=null**：
    trans 全程 ≤90.5、connector ≤90、车始终在 arena 内、墙正常推进。40→4 现象完全消失。
  - **C 类（次根因）**：renderer.reframe 'vehicles' fit **只含两车当前世界位置**（不含 arena
    边界/closing 墙）→ 车被挤到 x≈21/1579（40 速场景）时超出初始构图 [≈320,1280] → 出画
    （"车辆离开可视区域"）。任务要求 C 类修复 = 初始构图覆盖 Closing 可达区域。
- **为什么停止**：A 类根因修复必须重调 Physics 参数（closingSpeed=40→物理安全值，如 4；或改
  墙推进为不穿透语义）——命中任务停止条件「Physics 参数需要重新调 → 停止并报告根因」。
  且 closingSpeed=40 与 arenaConfig 注释「骨架阶段默认较慢」矛盾（40px/step=2400px/s 极快），
  疑似历史遗留未落地单位（px/s 语义 40 ≈ 0.67px/step 才是「慢」）；W1-END-2 测试用 4 才通，
  40 的破坏性此前从未被任何验收捕获（W1-INTEG test1 用默认 40 但只断言确定性不断言 connector）。
- **处置**：0 production change、0 commit、0 push（HEAD 仍 3ed2c63）；临时诊断测试与 solver
  实验全部回滚。未调任何参数、未修 renderer（避免在 A 未修时用 C 掩盖）。
- **最小修复建议（待用户决策）**：① closingSpeed 调至物理安全值（≤ 车半宽/步，建议 4，
  对照已证明 joint 稳定 + 车可见）；② 可选 C 类：Custom 正式 Battle 初始构图包含 arena
  内区域（'vehicles' fit 扩展含 closing 墙初始位置），保持 fixed camera 无 follow。

## W1-HYGIENE-R1｜Foundation Freeze 测试债清理（commit 0cb13bf，已 push）—— 全量绿灯

- **2 个既有失败根因（均为旧阈值 6 预期过严，Runtime 语义正确，修测试）**：
  - impactProtection 999.9375：relV=1 > 正式阈值 0.75（F-02I 标定后）→ 正式 Impact
    min(120,0.25*0.5)=0.125 双方各半 → B 扣 0.0625 ✓。修：relV 1→0.5 + 注释。
  - weaponThreshold 919.9：正常 ram 攻击 relVel~1.114 同时触发 Weapon(80)+Impact
    （0.182 双方各半 → 0.091）——「Impact 与 Weapon 可同时成立」是设计语义。修：断言
    用正式公式精确推导 Impact 验证 hpA/hpB。
- **6 个 tsc 债**：删 4 个纯旧诊断（untracked 从未提交：planckDriveCalibration /
  planckRevoluteSpike / wheelHardAxleDiagnostic / wheelStiffnessDiagnostic）；
  修 2 个有价值测试类型（impactThreshold capture 容器恒非 null 避免闭包窄化 never、
  planckContactKinematics 移除未用变量）。
- **验证**：全量 vitest 71 文件 346/346 **0 failure**；全量 scoped strict tsc **0 error**
  （src+tests 全部文件）；Cannon/Hammer/Push + W1 Integration 不变；diff-check 干净；
  0 production change（纯测试修/删）。
- HEAD=0cb13bfec1b1bd5e32b9ea89a6362397ec60541d（parent 3ed2c63=W1-INTEG），远端已同步。
- **Foundation Freeze 测试基线正式全绿**。剩余唯一阻塞：W1-P0-CLOSE-R1 的 closingSpeed=40
  根因（已定位 A 类，待用户决策是否批准调参）。

## W1-P0-CLOSE-FIX｜Closing 正式收束修复 + 固定战场构图（commit 8ec22f1，已 push）

- **closingSpeed 40→3**（几何推导：1600 宽/60 墙厚、Closing wall 初始 ±120/1720、Closing
  5s≈300 步每侧 890~900px → 3px/step）。40 穿透崩溃根因（R1 实证）已消除；3 下
  firstAnomaly=null。Scenario 显式 override 不强制覆盖。
- **hazardTickMs 500→100**（W1-END-2 数值不冻结；诊断依据）：kinematic 墙推车时车/墙
  「短暂反复接触」（周期 ~百 ms 级），500ms 间隔 tick 从未结算（0 伤害——autoDrive/
  对顶/站桩全场景实测）；100ms 下全部正常扣血。damage 40 不变，非掩盖。
- **CameraFit 'battle'**：正式战斗固定战场构图（x∈[0,width]、y=Closing 墙顶..地面），
  Start/Reset 一次、无 follow/zoom/projectile 扩；Editing Preview 仍 vehicles。
- **closingStability.test.ts（新 2 用例）**：完整默认流程（trans≤110/conn≤210/位置有限/
  墙 777/823 收束不穿出/唯一 winner）+ hazard 收束（hazardEvents 6、hpA 640、maxTrans 99.9）。
- **测试适配**：planckArenaRuntime/arenaRuntimePhase 断言 40→closingSpeed 常量引用；
  battleIntegration it5 用默认 phases（speed 3 下短 phase 对撞不足）。
- **实测回传**：maxTrans=99.5（limit 90 + ~10px solver 收敛余量；崩溃对照 1026）、
  maxConn=99.9、墙最终 777/823（中央闭合不穿出）。
- **验证**：全量 vitest 72 文件 348/348 0 failure；全量 tsc 0 error；vite build 38
  modules transform 全过；diff-check 干净。未改 gravity/solver iterations/Prismatic
  limit；无 teleport/setPosition 补偿/Damage 降低。
- HEAD=8ec22f1459f1fd916cb6df076589ba0d5628f2ae（parent 0cb13bf=W1-HYGIENE-R1），
  远端已同步，tracking ref 已补。**W1-P0 阻塞项正式关闭**。

## W2-VIS-1｜正式 Sprite Visual Asset Runtime（commit 6c45daf，已 push）

- **新增 src/render/visualRegistry.ts**：VisualRegistry——visualId → 资源元数据 + 加载后
  image（最小 VisualImageLike 接口）；register/has/getMeta/setImage（外部加载注入）/
  isReady（缺资源/未加载 → Renderer 安全回退 Collider graybox）；未注册注入抛错。
- **renderer.ts**：构造注入 registry（缺省空 → 全灰盒兼容）；drawVisual：translate·
  scale(-1,1)[mirror]·rotate——先旋转再水平镜像、anchor 已烘焙进 position、size 随
  镜头缩放、缺资源 false → 灰盒；drawVehicle：body→wheel（wheelVisuals 对应）→part
  （组内 visual.layer 稳定升序）；分组顺序 background→ground→walls→vehicle→projectile→FX。
- **battleContract.ts**：RenderVisual 加 mirror?: boolean（2D canvas rotate 无法表达图片
  左右翻转，需 scale(-1,1)；visualWorldTransform 输出；旧 snapshot 兼容不翻转）。
- **main.ts**：创建 registry 注入 Renderer（首版无正式 Content 资源 → 全灰盒；后续
  register + 加载注入即可，Preview/Fighting 共用同一 runtime）。
- **验证**：visualRuntime 6/6（registry / body sprite translate+rotate / wheel+part /
  mirror scale(-1,1) / 缺资源 drawImage=0 灰盒照画 / layer 升序 / Preview+Battle 共用）；
  renderer 相关回归 9 文件 49/49；全量 vitest 73 文件 354/354 0 failure；全量 tsc
  0 error；vite build 39 modules transform 全过；diff-check 干净。
- **禁止项确认**：未改 Collider 适配图片、无 image 入 Content Def、Behavior 无 drawImage、
  灰盒 fallback 未破坏。Physics 完全不知图片（visual 只出现在 snapshot 渲染层）。
- HEAD=6c45dafb558d08308849d59052ca18c5d3ef1057（parent 8ec22f1=W1-P0-CLOSE-FIX），
  远端已同步，tracking ref 已补。下一候选：W2 Content 正式 sprite 接入 / W2-BUILD 等。

## W2-SIL-1｜正式 Content 视觉轮廓样板（commit TBD，已 push 待确认）

- **新增 2 个 Body + 3 个 Functional VisualDef**（src/core/content.ts）：
  - watermelonBody（180×60 sprite，Collider 170×50 box，baseMass 120，hp 1100，energy 110，
    4 槽 front/frontMass/top/rear；visual: body_watermelon, layer 1, mirrorWithFacing）。
  - bananaBody（200×56 sprite，Collider 两段 box 近似弧形，baseMass 45，hp 900，energy 90，
    同 4 槽；visual: body_banana, layer 1, mirrorWithFacing）。
  - cannon visual: 48×22, anchor {16,0} → 本地跨距 [-8,40]，炮口右端 == 真实 muzzle (40)；
  - hammer visual: 74×20, anchor {34,0} → 本地跨距 [-3,71]，柄根 local 0 = Revolute pivot；
  - pushRod visual: 98×18, anchor {43,0} → 本地跨距 [-6,92]，推板覆盖 collider 前端（80）。
- **5 张程序化轮廓 PNG**（assets/visuals/，2x 超采样+AA 降采样 + zlib PNG 自校验）：
  body_watermelon / body_banana / part_cannon / part_hammer / part_pushRod。
  删除临时生成脚本 _gen_silhouettes.cjs（仅一次性）。
- **main.ts**：
  - 导入 5 PNG（vite 资源 import 返回 URL；vite/client 提供 *.png 声明）；
  - visualRegistry register + new Image() onload 注入 setImage（onerror 灰盒 fallback）；
  - BODY_OPTIONS 新增 watermelonBody / bananaBody；
  - 默认 Draft 改为 W2-SIL-1 样板（front=pushRod, frontMass=cannon, top=hammer）——
    双车并排 Preview 一次展示 5 个正式 Content（watermelon 宽厚低矮重 +
    banana 长条轻弧 + 3 个功能性部件），首屏合法 valid。
- **测试**：
  - 新增 contentSilhouette.test.ts（7 用例）：5 件 VisualDef 齐全 / cannon 炮口 ==
    collider muzzle / hammer 柄根 ≈ Revolute pivot / pushRod 推板覆盖 collider 前端 /
    watermelon+banana 4 槽 editableSlots / 样板 Draft 双方 valid / facing=-1 mirror 路径。
  - 修 visualDef.test.ts case1（cannon→ramHead，保持旧 Content fallback 断言）。
  - 修 battleIntegration.test.ts it6（triBuild→ramHead，保持旧 Content fallback 断言）。
- **验证**：全量 vitest 74 文件 361/361 0 failure；全量 scoped strict tsc 0 error；
  vite build 44 modules transform 全过（产物清理沙箱 delete 保护中止，环境限制）；
  git diff --check 干净。Physics 参数未改（Collider/Behavior 全冻结）。
- 远端 SHA 待 push 后 ls-remote 确认。
- HEAD=c9c04ec4e541c519adf71a2fadf9512130e8e52e（parent 6c45daf=W2-VIS-1），远端已同步
  `6c45daf..c9c04ec foundation-02-canonical`，tracking ref 已补。

## W2-FX-1｜Battle VFX / SFX Runtime（commit 8c80523，已 push）

- **问题**：Runtime 已产出 damage/weaponFire/death（W1-EV-1 冻结），但正式表现无统一消费层。
- HEAD=8c805231edc768aa7a5a492a4b5af7ac3e85acb8（parent 4328a04=W2-SIL-1 docs），远端已同步
  `4328a04..8c80523 foundation-02-canonical`，tracking ref 已补。
- **新增**：
  - src/presentation/battlePresentationController.ts：BattleEvent→Presentation 唯一消费层——
    8 个可选 hook（weaponFire→muzzleFlash+fireSound；damage→hitFlash+spark+sound+number
    （damage=0 只 flash）；death→deathFx+sound）；bind() 幂等（重复 bind 先解绑旧订阅，
    同事件不重复播放）；stop() 解绑；全部 hook 缺省 no-op（无资源安全 skip）。
  - src/presentation/audioService.ts：统一 SfxService（play/setMuted/isMuted；临时 Web Audio
    oscillator beep 占位：fire 880→1400Hz / hit 320→160 / death 220→60；无 AudioContext /
    muted / 异常 → 安全 no-op 不抛错）。
- **renderer.ts**：删 bind()（事件订阅），改 5 个纯表现方法（spawnDamageNumber 统一数字入口 /
  spawnHitFlash / spawnSpark / spawnMuzzleFlash / spawnDeathFx）+ 对应绘制（火花小圆、炮口
  亮圆、死亡扩散环，vehicleCenter 兼容 polygons/circle 判别联合）。
- **physicsLab.ts**：constructor 注入 presentation?；createBattle 内 preview→stop()、
  正式战斗→bind(orch.onCombatEvent)（Preview 不消费战斗 FX / Fighting 才消费）；clear() 停。
- **battleContract.ts**：onCombatEvent 返回类型 void → () => void（实现本就返回解除订阅）。
- **main.ts**：创建 SfxAudioService + controller（hooks→renderer 表现方法 + sfx.play），注入 lab。
- **测试**：新增 battlePresentation.test.ts（7 用例：fire→1 次 / damage→对应 hit+number /
  damage=0 只 flash / death→1 次 / 重复 bind 解绑旧订阅 + stop 不消费 / preview 不 bound +
  custom battle bound + clear 解绑 / sfx node 无 ctx+muted 安全 / 真实 Planck 首步 cannon
  发射→presentation 恰 1 次（A 装炮 B 裸车））；修 rendererProjectile.test（bind→spawnDamageNumber）。
- **验证**：全量 vitest 75 文件 368/368 0 failure；全量 scoped strict tsc 0 error；vite build
  46 modules transform 全过；git diff --check 干净。Cannon/Hammer/Push gameplay 完全不变
  （Behavior 未改；presentation 不决定伤害）。

## W2-CONTENT-R0｜V1 首发 Content 生产规划（只读/设计，0 改动，0 commit）

- **V1 目标**：Body 4 / Movement 3 / Weapon 6 / Gadget 3。
- **Foundation 能力盘点（全部已实现）**：
  - Fixed Mount（Weld）：ramHead、cannon（装配默认分支）；
  - Revolute：hammer（motor+limit 状态机）；装配分支 `behavior==='hammer'`；
  - Prismatic：pushRod（motor+limit 伸缩）；装配分支 `behavior==='pushRod'`；
  - Projectile：cannon（bullet CCD、真实 muzzle、OwnerTag、越界销毁、recoil；
    createDynamicCircle **已支持 restitution** 0..1）；
  - contactOnce：ram/hammer（WEAPON_CONTACT_THRESHOLD 0.5，baseDamage 一次）；
  - contactTick：W1-HIT-1 已实现（intervalMs/damage/minRelativeVelocity）但
    **尚无任何正式 Content 使用**——首吃是 drill；
  - recoil：cannon applyLinearImpulse 于真实 muzzle 点；
  - mass/posture/grounded：质量分布（testMass）、wheel drive grounded gate、
    Impact 双方各半。
- **首发组合**：
  - Body 4（GREEN 已存在 0 开发）：watermelonBody / bananaBody / boxBody / heavyBox
    （wedge 低鼻 / tall 高重心作后补）。
  - Movement 3（1 已有 + 2 GREEN）：wheelStd / wheelBig(r26) / wheelMini(r12)。
  - Weapon 6 = cannon + hammer + **ramHead（GREEN，def 已存在只正式化）** +
    **drill（GREEN：Weld + contactTick，纯参数）** + **sawBlade（YELLOW：Revolute
    连续旋转小行为 + contactTick）** + **shotgun（YELLOW：Cannon 派生多弹散射）**。
  - Gadget 3 = pushRod + **plow（GREEN：Weld 宽推板无伤害）** +
    **ballast（GREEN：Weld 高 mass 压舱改 posture）**（scoop 斜面铲 GREEN 备选）。
- **标记**：GREEN=纯 Content（ramHead/drill/plow/ballast/scoop/wheelBig/wheelMini/
  Body4）；YELLOW=小 Behavior（sawBlade 连续旋转、shotgun 多弹）；RED 记录不开发
  （flail 链式摆锤=Revolute 子装配需新规则；rocket 尾迹=新推进行为）。
- **开发顺序**：C1 GREEN 批量（Content 队列）→ C2 sawBlade（contactTick 真实
  Content 验证）→ C3 shotgun（多弹）→ 每步 Physics Lab 1x+Debug 关集中验收 →
  W2-BALANCE 平衡队列（数值不冻结）。
- **冻结**：不批量开发；不改 Frozen Foundation（hit policy 默认参数/BattleResult/
  阶段时钟/Behavior Registry 契约/BattleEvent/VisualDef/BattleRequest 合同）。

## W2-FX-2｜阶段级战斗表现（commit 7754521，已 push）

- HEAD=77545210af850a692157a3bafd26ca5668cf5dd8（parent 5a5372d=W2-FX-1 docs），远端已同步
  `5a5372d..7754521 foundation-02-canonical`，tracking ref 已补。

- **改动（4 文件 +2 新）**：
  - src/presentation/battlePhaseFx.ts（新）：phaseRemainingMs / warningCountdown（3→2→1，
    ≤0 空）/ DeathPauseScheduler（80~120ms 定格调度，注入 now 可测）/ damageFeedbackColors
    （hazard 刺伤专属红）/ vehicleDeathAlpha（未死亡 1 / 淡出 0..1 / 超 ttl null 跳过绘制）。
  - src/render/renderer.ts：closingWalls 阶段视觉（Warning 预高亮描边+闪烁、Closing 正式
    刺墙锯齿 drawSpikes+脉动描边；读 orchestrator.phase）；车辆死亡淡出 alpha→消失跳过
    绘制（手动 globalAlpha 管理，不用 ctx.save/restore 保持与既有 canvas stub 兼容）；
    spawnSpark 支持 damageSource 配色；hitFlash 跳过已消失车。
  - src/main.ts：Warning HUD 倒计时（hudPhase 旁 phaseCountdown 元素 + CSS）+ 场边红脉冲
    （canvasWrap.phase-warning class + keyframes）；pollArenaPhase（orchestrator 实例变化
    重置、非 preview 才显示、phase 变化记 startMs、remaining=warningMs-(timeMs-start)）；
    Death 定格（onDeathFx → deathPause.trigger(100) + lab.timeScale=0 记 prevTimeScale，
    loop 里 shouldResume → 恢复 prevTimeScale）；hazard/weapon/impact 反馈配色。
  - tests/battlePhaseFx.test.ts（新 9 用例）：阶段剩余/倒计时/定格调度/配色/死亡 alpha
    + Renderer smoke（Active 无高亮 / Warning 多一次描边 stroke / Closing 锯齿 fill 更多 /
    spawnDeathFx 后 render 正常）。
- **关键设计**：timeScale=0 定格安全（world.step acc+=realDt*0 → 0 步不回调）；恢复原
  timeScale（玩家 debug override 不被覆盖）；倒计时纯表现层轮询（0 改 battleContract）；
  死亡淡出只影响绘制（物理 body 不销毁）。
- **坑**：① ctx.save/restore 不在既有测试 stub → 改手动 globalAlpha 复位；② vehicleDeathAlpha
  初版未死亡返回 null 被当作跳过绘制 → 改返回 1（未死亡正常绘制 / 已消失才 null）。
- **验证**：全量 vitest 76 文件 377/377 0 failure；全量 tsc 0 error；vite build 47 modules
  transform 全过（产物清理沙箱限制）；diff-check 干净。Gameplay/Physics 时间语义未改。

## Q06-P0-RUNTIME｜浏览器实际运行版本追查（0 代码改动，0 commit）

- **现象**：真人录屏显示 UX-R1 前旧版（Scenario 下拉 / Build+Debug 同屏 / 无模式切换 /
  boxBody+heavyBox / Energy 0/100 / 旧 Preset），与 aad9102 源码不符。
- **证据链（只读诊断）**：
  1. Git：branch=foundation-02-canonical，HEAD=aad9102，工作区干净（仅 memory/交接 MD
     未跟踪），git worktree list 仅 1 个（D:/0818new/最强水果），main.ts 标记全在
     （装配测试 8 / 机制场景 6 / watermelonBody 2 / showPreview 5 / loadCustomPreview 3）。
  2. 运行进程：netstat 无 5173/4173/3000/8080/8000 监听；查 node/vite 进程为空——
     诊断时本机没有任何 dev/preview server。
  3. HTTP：候选端口 curl 全 000（无服务）。
  4. dist：**残缺**——index.html 引用 /assets/index-A-228l8r.js 但 dist/assets 不存在
     （历次 vite build 在 prepare-out-dir 被沙箱 safe-delete 拦截，产物从未完整写出）。
- **根因**：真人浏览器在本机**没有正在运行的旧版服务**；录屏旧版页面 = 历史旧录像
  （录制于 Q06-U1/B1 时代旧实例运行时，现已停止）或旧实例在别处。aad9102 此前从未被
  启动进浏览器（本机一直无 dev server 跑新版）。
- **修复（仅运行来源）**：从正确目录 D:\0818new\最强水果（git HEAD=aad9102）启动
  `node node_modules/vite/bin/vite.js --host --port 5173`（PID 8272，0.0.0.0:5173，
  strictPort）。HTTP 验证：首页 200、入口 /src/main.ts transform 含 装配测试 6 /
  机制场景 6 / watermelonBody 2 / showPreview 5 / loadCustomPreview 3；旧标记
  「A · Light vs Heavy」0、「额外部件」0。
- **结论**：0 代码改动、0 commit（正确结果——只启动服务）。真人访问
  `http://localhost:5173/` 即 aad9102 新版（首屏装配测试 + W2-SIL 西瓜/香蕉样板）。
  坑：本机「无 dev server」≠「版本对」——之前从未在浏览器跑过新版；dist 残缺是
  vite build 沙箱拦截的已知环境限制（与代码无关）。

## Q06-UX-R2｜装配测试交互减负（commit e14a5ba，已 push）

- HEAD=e14a5ba7656c72e411e2634508a6ece4e03fc7b8（parent aad9102=Q06-P0-RUNTIME 诊断 0 改动），
  远端已同步 `aad9102..e14a5ba foundation-02-canonical`，tracking ref 已补。

- **改动（5 文件）**：
  - main.ts：① 顶部减负——Pause/Reset/Clear/速度/装载→A/Preset 全部移出 toolbar 一级，
    收进「测试工具」折叠区（toolsToggle + toolsHost，main 布局中 toolbar 下方）；一级只剩
    模式×2 + 开始战斗 + scenario 下拉（机制场景）。② B 改「测试对手」：默认折叠（panel-collapse
    header + 「编辑对手 ▸」toggle），展开状态 bEditorOpen；B Draft 能力不删，锁定逻辑
    setBuildControlsLocked 同步锁 toggle。③ reframeCamera：build+previewMode → 'preview'
    fit（近距放大）；build 战斗 → 'battle' 不变；scenario 原语义。④ updateHud：Ended 时
    hudPhase 显示「战斗结束」（不再「战斗中」与结算卡矛盾）。
  - renderer.ts：CameraFit 新增 'preview'——A+B 进画面 + 小边距（PREVIEW_MARGIN 18）+
    大 zoom（PREVIEW_ZOOM 1.9）；transformScale 只读 getter（测试断言构图差异用）。
  - buildEditorModel.ts：SLOT_LABELS 改「前端挂点/前上挂点/顶部挂点/后部挂点」
    （front/frontMass 不再作主视觉文字；内部 id 仍作次级）。
  - tests：buildUx test5 更新新标签；battlePhaseFx 新增 6e（preview fit scale >
    vehicles fit scale 且 >1，近距放大成立）。
- **验证**：全量 vitest 77 文件 378/378 0 failure；全量 tsc 0 error；vite build 47
  modules transform 全过（产物清理沙箱限制）；diff-check 干净。未改行为/Energy/Physics/
  状态机；正式 Battle 构图不受 Preview 放大影响（battle fit 分支未动）。

## Q06-UX-R2-FIX｜Preview 裁切回归修复（commit 5b9d4ae，push 验证 = 5b9d4ae）

- **问题**：真人录屏确认 Editing Preview 中 A/B 完整车辆被裁出画面（只剩左右边缘 Functional Part），改 Cannon/Hammer/Push 后无法判断装配结果。
- **根因**：reframe('preview') 先按 A+B bounds fit，又乘 PREVIEW_ZOOM=1.9 → 必然超过 safeW×safeH（×CONTENT_ZOOM=1.05 同理也会超）；且 Preview 复用正式 spawn（400/1200）间距过大。
- **修复**：
  1. renderer.ts：删 PREVIEW_ZOOM=1.9；preview fit 直接取 fitLimit（不再乘任何 >1 zoom），A/B 完整入画成为数学硬约束；仅保留 PREVIEW_MARGIN_WORLD=18。vehicles/primary-fire/battle 的 ×1.05 语义不变。新增只读 worldRectToScreen（测试断言 screen bounds）。
  2. physicsLab.ts：loadCustomPreview cfg 加专属 spawnA={620,640,facing:1} / spawnB={980,640,facing:-1}；正式 Battle 默认 400/1200 未动。
  3. 测试：battlePhaseFx 6e 改 screen-bounds 入画断言（不再以 scale>vehicles 为主验收，已证明假绿）；buildUx 新增「Q06-UX-R2-FIX 装配 Preview 完整入画」describe（真实 Planck + renderer reframe：默认 W2-SIL 双车 body/wheels/parts 全入画；front pushRod→cannon 变化 Build 重建仍完整；正式 spawn 仍 400/1200）。
- **关键教训**：任何 >1 的 fit 后 zoom 都会让「含 margin 的内容」超出 safe viewport 被裁；「scale 数字变大」测试会假绿（内容已出框但 scale 更大），必须断言最终 screen bounds。
- **验证**：全量 vitest 76 文件 381/381（+3 新测试）+ 全量 tsc 0 error；未改 Toolbar/B 折叠/挂点文案/HUD/Cannon/Hammer/Push/Energy/正式 spawn；无第二套 Preview Renderer。

## Q07-A｜Game Flow 页面层级重排（commit 2a88d52，push 验证 = 2a88d52）

- **问题**：功能已能 Preview→Battle→Result，但页面是开发工具感——「装配测试/机制场景」双主按钮抢焦点、Start 与调试按钮同级、B 仍是测试口吻。
- **改动（纯 UI 结构/视觉层级，0 Runtime 改动）**：
  1. 装配 = 默认主页：首屏「我的车辆 A → 配置 → 中央 Preview → 当前对手 B → 开始战斗」。
  2. toolbar 只留弱化「开发工具 ▸」（.dev-toggle opacity .72 + margin-left:auto）+ scenario 模式「返回装配」；机制场景 select / Pause / Reset / Clear / 速度 / Preset 全部收进 toolsHost 折叠区（scenarioSelect 移入 toolsHost，选中即 setMode('scenario')；scenarioSelect.display 不再由 setMode 单独控制——由 toolsHost 折叠控制）。
  3. B 改「当前对手 B」：collapsed 默认只显示概要（.opponent-summary：Body 名 + 部件列表 + 能量）+「编辑对手」入口；完整表单展开逻辑保留。
  4. Start = 唯一主 CTA：移到 canvasWrap 底部 start-bar（.btn-start-cta 17px 大按钮 + 蓝 + 阴影，z-index 6）；合法可点/非法禁用 + 就近 start-hint；Fighting/Ended 时 start-bar 整体隐藏。
- **坑**：editableSlots(body) 返回 string[]（hp id）不是 hardpoint 对象——概要里误用 hp.id 报 TS2339；场景 select 移入折叠区后 setMode 里旧的 scenarioSelect.style.display 控制已无意义（由 toolsHost 控制），删掉避免 build 模式展开时看不到场景入口。
- **验证**：vitest 76/381 全绿 + tsc 0 error + build transform 47 modules 全过；diff 仅 main.ts（+84/−31）。

## Q07-B｜Functional 装配交互卡片化（commit d5dc610，push 验证 = d5dc610）

- **问题**：Functional 用多个原生 select，认知仍是「填表」而非「装东西」。
- **改动（仅 main.ts UI，0 Runtime/规则改动）**：
  1. Functional select → 挂点卡片（.part-slot-card：挂点名 + 当前装备 [Push Rod]/[空]，金色值/灰空态）；按 editableSlots(body) 真实 hardpoints 生成。
  2. 点击卡片 → selectedSlotA/B（per-panel 状态）→ .active 高亮 + 展开部件选择区（.part-picker：空/Cannon/Hammer/Push Rod + 标题「正在改『顶部挂点』」）；点选项立即 d.functionalSelections 更新 → refreshFromEdit（Energy/Validator/真实 Planck Preview 全刷新），无 Apply；选择区保持展开支持连续切换。
  3. 当前装备在选项里 .active 高亮；再点卡片取消选中。
  4. 交互分层：卡片点击只走 renderPanelsOnly()（重渲染两面板，不重建 Preview，选中态切换不闪画面）；选项点击才 refreshFromEdit()（含 showPreview 重建）。
- **设计点**：selectedSlot 按 A/B 分开（同挂点 id 不互相污染）；body 切换后 `editableSlots(body).includes(selSlot)` 守卫使失效挂点自动隐藏；Body/轮保留 select；migrateDraftBody 真实 hardpoint 逻辑未动。
- **验证**：vitest 76/381 + tsc 0 error；diff 仅 main.ts（+81/−7）。

## Q07-C｜Battle / Result 游戏流程收口（commit 41adc1a，push 验证 = 41adc1a）

- **问题**：Editing/Fighting/Ended 技术正确但视觉像同一 Lab 页面切状态。
- **改动（仅 main.ts UI/表现，0 规则改动）**：
  1. Fighting 观看态：startOrRematch 额外隐藏 toolsToggle + toolsHost（开发工具入口）；panelA/B/startBar/startHint 已隐藏；只留战场 + HUD + 阶段提示。
  2. READY/开战 overlay：btnStart.onclick 改 wrapper——startTransitioning 防重入 → setBuildControlsLocked(true)（防 0.8s 内改配置竞态）→ .ready-overlay（z-index 8，READY 小字 + 「开战！」46px 金字）→ 0.8s setTimeout → startOrRematch；防御分支（battleState !== fighting → 解锁回编辑）。纯 Presentation 延迟（build/engine/seed 不变，晚 0.8s 创建实例），rematch 不经过 overlay。
  3. Result 结算卡已满足（胜利/失败 + 双方 HP + 调整配置 primary / 原配置再战）；adjustConfig 用当前 draftA/draftB 重建 Preview（Build/对手不重置）并恢复 toolsToggle；rematch 直接 startOrRematch 不经编辑态。
- **设计点**：toolsToggle 显隐由 startOrRematch 隐藏 / adjustConfig / clear / reset(preview 分支) 恢复，Ended 期间保持隐藏（结算卡焦点）；Fighting 中若开发工具已展开（toolsOpen=true）也强制隐藏 toolsHost。
- **验证**：vitest 76/381 + tsc 0 error；diff 仅 main.ts（+52/−3）。

## Q08-A｜Battle Camera 正常观看构图（commit fbfbaa7，push 验证 = fbfbaa7）

- **问题**：battle fit 为始终含完整 Arena/Closing wall，正常阶段也是超远景（scale≈0.54），车辆过小、上半屏无效空区。
- **改动**：
  1. renderer.ts：reframe opts 加 `phase?: string`；battle fit 按 phase 分支——Active（A+B + 每侧 BATTLE_ACTIVE_EXTENT=100 运动空间，不含墙，scale≈0.78 ≈1.4×全景）/ Warning（BATTLE_WARNING_EXTENT=240，≈0.61 适度拉远）/ 其他 Closing+End（保留 W1-P0-CLOSE-FIX 全景：0..width + closingWalls，End 不二次 zoom）。Preview/vehicles/primary-fire 语义不变；projectile 不参与 bounds。
  2. main.ts：reframeCamera 传 `phase: fit==='battle' ? orch.phase : undefined`；pollArenaPhase 在 `o.phase !== lastPhase` 时调 reframeCamera（Active→Warning→Closing 稳定切换一次，非每帧重算）。
  3. battlePhaseFx +4 测试：Active A/B body+wheel+parts screen bounds 完整入画 + scale/屏幕高度明显大于 Closing 全景；Warning 介于中间且完整；Closing 墙+A/B 完整；含/不含 projectile 构图一致。
- **设计点**：镜头只在明确事件重构图（start/phase 切换/resize/reset）；不做 follow/呼吸 zoom/插值（第一版直接稳定切换）；数值粗调一次（extent 100/240），禁止连续微调。
- **验证**：vitest 76/385（+4）+ tsc 0 error；diff 3 文件（renderer/main/test）。

## Q08-B｜Start / Result Presentation 收口（commit e9b81c4，push 验证 = e9b81c4）

- **问题**：READY 时背后仍是完整编辑器；Result 卡片过小结算力度不足。
- **改动（仅 main.ts UI）**：
  1. Start 点击瞬间退编辑：btnStart.onclick 在显示 READY overlay 前立即隐藏 panelA/B/startBar/startHint/toolsToggle/toolsHost + setBuildControlsLocked(true)（Build 从点击那一刻冻结，Runtime 输入语义不变）；readyOverlay 背景减淡 0.55→0.35（中央两车 Preview 成为 READY 背景）；0.6s（原 0.8s）后 startOrRematch；防御分支完整恢复编辑视觉。
  2. Result 结束层：modal 背景 0.55→0.78 压暗；card min-width 300→420、padding 34×58；title 30→44px；HP 降次级（14px 灰 #8a93a5）；primary CTA 强化（16px 加粗+阴影+padding 12×34）；btnRematch 加 .secondary（opacity .85）。
- **状态清理确认**：READY 无编辑控件（隐藏+锁定+pointer-events none）；Ended hudPhase「战斗结束」；Adjust 恢复上局 Draft；Rematch 直接重建不闪回。
- **验证**：vitest 76/385 + tsc 0 error；diff 仅 main.ts（+30/−15）。

## Q08-A-FIX｜Battle Camera 出框根因修复（commit d903fe4，push 验证 = d903fe4）

- **问题**：Active 后 B 很快右侧出框（9~13s 大量车体离画面）；Warning 不稳定；Closing 车辆挤边缘。非 extent 参数问题。
- **三根因 + 修复（renderer.ts + 测试）**：
  1. battle 用 fitLimit×1.05（fit 后再放大破坏完整入画）→ battle 一律 fitLimit（同 preview 原则）；vehicles/primary-fire/scenario 保留 ×1.05。
  2. includeVehicle 只含 collider 不含真实 Visual → 新增 includeVisual（position+size+rotation 算视觉 AABB，mirror 不影响）；bodyVisual/wheelVisuals/part.visual 全纳入；「完整入画」= Visual 完整。
  3. Active 用初始 bounds+100 extent → 固定 Battle Corridor：横向 arena.width×0.25/0.75（对齐默认 spawn 400/1200）± CORRIDOR_ACTIVE_EXTENT（MOVE 160 + VISUAL_HALF 110），不读瞬时位置；纵向 groundY−190..groundY（不含墙顶）→ 车辆大、上方无空区；Warning 外扩 100；Closing/End 保留全景。
- **测试 +6**（battlePhaseFx 20）：Active 默认双车 Visual 全入画；左右偏移 150 仍完整；Warning 完整+介于中间；Closing Visual+墙完整；banana visual 240>collider 190 抓旧 bug（旧实现 sx≈983>944 出框）；projectile 变化不影响 camera。
- **验证**：vitest 76/391 + tsc 0 error；diff 2 文件（renderer +108/test +183）；main.ts 未动（Q08-A 的 phase 事件重构图已够）。

## Q08-C｜Combat Feedback 去遮挡（commit 41f5ab0，push 验证 = 41f5ab0）

- **问题**：连续受击后水果车整块白色遮身份；Damage Number 重叠 + 无意义 -0。
- **改动（仅表现层）**：
  1. hitFlash 整块白 fill（drawShape #ffffff）→ 白描边轮廓（strokeShape body+parts，alpha (1-age)×0.85）；sprite 可见，受击可感知由描边+spark 承担。
  2. main.ts onDamageNumber：`const dmg = Math.round(ev.damage); if (dmg<=0) return;`——Gameplay 照常结算，只去无意义 Presentation -0。
  3. spawnHitFlash 去重：find(team)+刷新 bornAt，不 push 叠加（同 team 最多一个表现状态）。
- **测试 +1**（battlePhaseFx 21）：同 team 连续命中两次 render 的 fill 计数一致（旧 3→5）+ fillStyle 非 #ffffff + stroke 存在。坑：ctx.calls 累积，两次 render 间需 `ctx.calls.length = 0`。
- **验证**：vitest 76/392 + tsc 0 error；diff 3 文件（renderer/main/test）。

## Q08-CAM-D1｜Battle Camera 真实出框链路诊断 + Atomic Fix（commit 478ef10）

- **诊断方法**：临时 vitest 诊断（真实 Runtime：loadCustom 正式默认 Build + step 循环 + 模拟 reframeCamera 触发（start/phase 变化）→ visualAABB → worldRectToScreen 每帧出框检测）。
- **结论**：默认 Build 12.25s 内 HP 归零（Warning 中途 End）无出框；延长 phase 变体抓到 firstOOB t=11767ms **Active** B visual 右缘 1534.7 > corridor 1470（screen 944 贴界）。根因 = **A 类（唯一）**：A（120+）持续驱动 +X 顶推 B（45），交战团整体右移，最终可达 arena 右墙内侧；spawn+对称预算 corridor 未覆盖。B/C 类排除。
- **Atomic Fix（renderer.ts 1 module）**：Active/Warning corridor 右界 `spawnB+EXTENT` → **`arena.width − CORRIDOR_EDGE_PAD(60)`**（锚定 arena 右缘，对齐墙厚）；删 CORRIDOR_SPAWN_B_RATIO。修复后诊断 firstOOB 全 null（B 1534.7 ≤ 1540+margin）；scale 0.577 > 全景 0.514（不回退超远景）。
- **测试 +1**（battlePhaseFx 22→变体 2b：B 右缘 1534 实测位置 Active 完整 + 大于全景）。
- **坑**：诊断测试需 `expect` import；临时诊断文件跑完即删（不留 0 价值产物）。
- **验证**：vitest 76/393 + tsc 0 error；diff 2 文件。

## Q08-CAM-A1｜Battle 切换 Canvas backing 同步修复（commit 21b07f5）

- **问题**：Battle 刚进入 ~1.7s B 右侧被裁（早于运动，排除 D1 后期右移根因）。
- **根因成立**：renderer.resize() 才同步 backing（canvas.width = clientWidth×DPR），但只有 window resize 触发；Start 隐藏面板 → clientWidth 变宽、backing 旧 → reframe 按新 clientWidth 取景、绘制旧 backing → 右裁。
- **修复（仅 main.ts 布局切换链补 doResize()，doResize = renderer.resize + reframeCamera）**：btnStart.onclick / startOrRematch / adjustConfig / reset preview 分支 / clear / setMode 共 6 处面板显隐后先同步。
- **测试 +1**（battlePhaseFx 24：resize 按 clientWidth/Height×DPR 重设 backing 契约）。
- **验证**：vitest 76/394 + tsc 0 error；diff 2 文件。

## Q09-A｜Body/Wheel 装配去表单化（commit bb30766）

- **问题**：Functional 已卡片化但 Body/轮还是 select。
- **改动（main.ts UI）**：mkSelect → mkOptGroup（.opt-group/.opt-card/.opt-cards 样式，当前选中 .active）；Body 6 卡片（复用 migrateDraftBody）+ 前后轮 12/20/26 卡片；点击即 refreshFromEdit（Preview/Validator 无 Apply）；删 mkSelect 与死参数 onChanged（renderPanel 签名收窄）。
- **测试 +1**（buildUx 11：6 个 Body 全遍历迁移后真实挂点一致/无虚假槽/Validator 不崩）。
- **验证**：vitest 76/395 + tsc 0 error；dev server 转换后 mkSelect 0 处。

## Q09-B｜部件选择信息可读性（commit b19a7c0）

- **问题**：部件选择区只有名字，缺「正在选什么」基本信息。
- **改动（main.ts UI）**：part-picker 选项两行（.pp-name 名称 + .pp-meta「Weapon/Gadget · N 能量」，空=「空 · 0 能量」，读 registry 真实 category/energy）；当前装备 .active 不变；Energy 纯文本 → .energy-row 条形（.energy-bar/.energy-fill 比例 + 数字，超载沿用 Validator 仅表现层红色）。
- **测试 +1**（buildUx 12：Cannon/Hammer=weapon、PushRod=gadget、energy 30/25/20 与 UI 数据一致）。
- **验证**：vitest 76/396 + tsc 0 error；diff 2 文件。

## Q10-A｜正式装配内容与测试内容分离（commit 7980e37）

- **问题**：装配页暴露 wedge/box/tall/heavy 工程测试 Body。
- **改动**：BODY_OPTIONS 只留 watermelonBody/bananaBody（装配卡片唯一数据源）；测试 Body 保留 registry，Scenario/Preset 不经 BODY_OPTIONS 不受影响；migrateDraftBody 仍支持任意 registry Body。
- **测试 +1**（buildUx 13：4 测试 Body 仍完整 registry + 迁移/快照/Validator 链路可用）。
- **验证**：vitest 76/397 + tsc 0 error。

## Q10-B｜玩家侧命名与装配层级清理（commit 0f8efd4）

- **改动**：PART_OPTIONS t → 炮/锤/推杆；pp-meta Weapon→武器/Gadget→辅助（defId/category 不变）；WHEEL_OPTIONS → '12 小'/'20 标准'/'26 大' + .opt-cards.wheel（≤3 项等权一行，Body 两列不变）。
- **测试 +1**（buildUx 14：中文显示名 + defId 英文不变 + category 数据源不变）。
- **验证**：vitest 76/398 + tsc 0 error；dev 转换后玩家侧英文工程名 0 处。

## Q11-A｜楔铲 Gadget（commit ffd2933）

- **新增**：content.ts wedgeShovel（gadget，polygon 低矮楔形 collider——ColliderShape 已有 polygon，复用 colliderToVertices/planckVehicleAssembly，0 Foundation 改动；无 baseDamage/behavior none/无隐藏力）；scenarios.ts Q11 专用场景（A 西瓜+前楔铲 vs B 普通车）；不进 PART_OPTIONS。
- **物理验证**（确定性 seed）：B 俯仰峰值 90.1°、离地 368px；A 反作用 102.7°；未接触期 B 静止（失败自然）。
- **测试 +2**（buildUx 16：定义契约 + 场景物理效果断言）。
- **验证**：vitest 76/400 + tsc 0 error。

## Q11-B｜固定刺 Weapon（commit 0895d96）

- **新增**：content.ts spear（weapon，细长 polygon 长 96 高 6 前端尖，挂 top 高处，baseDamage 60，behavior ram 复用 ramHead 链路）；scenarios.ts Q11-B（A 刺车 vs B tallBody 高车）；不进 PART_OPTIONS。
- **验证**（确定性 seed）：刺 vs 高车 weapon damage > 0（命中）；刺 vs boxBody+12 矮车 weapon damage = 0（擦空 Miss）——同一几何靠高度差不同结果。
- **测试 +3**（buildUx 19：契约 + 命中 + 擦空）。
- **验证**：vitest 76/403 + tsc 0 error。

## Q11-C｜蓄能镭射 Weapon（commit fe0c973）

- **新增**：content.ts laser（weapon，chargeMs 1500/cooldown 1800/speed 16/damage 160/recoil 60 ≈Cannon 2×）；laserBehavior.ts（独立，冷却→蓄能→发射状态机，weaponCharge 事件 progress 0→1，复用真实 Projectile/CCD 链路，无自动瞄准/无第二套系统）；combatEvents +weaponCharge；Presentation +onWeaponCharge/onWeaponChargeEnd；Renderer 蓄能光点（partId upsert，发射清除）；scenarios Q11-C；不进 PART_OPTIONS。
- **坑**：chargeProgress 递减后读取 → 末步返 0（改递减前取 progress）；完整 battle 中 weld 约束使 part.body 读不到 -3（后坐断言改 chassis vx 骤降 Δ≈-0.32）。
- **验证**（确定性 seed）：蓄能差 ≈1500ms/progress≈0.99；命中 ≥150；打空 0；后坐骤降 0.32。
- **测试 +5**（buildUx 24）+ behaviorRegistry 断言更新。
- **验证**：vitest 76/408 + tsc 0 error。

## Q11-R1｜Q11 三部件正式接入 Build（commit 14af9de）

- **问题**：Q11-A/B/C 全被限制「只进测试场景不进正常 Build」，正常装配页看不到楔铲/刺/镭射，不算实装。
- **改动**：PART_OPTIONS 正式加入 wedgeShovel（楔铲/gadget 15）/spear（刺/weapon 25）/laser（镭射/weapon 45）——与炮/锤/推杆同一套挂点选择/Energy/Validator/真实 Planck Preview（无专属槽无 per-slot 过滤）；选择写现有 defId，Preview 显示真实部件，Start 走既有 Runtime（楔铲碰撞/刺 ram Contact/镭射蓄能发射后坐）；Q11 专用 Scenario 保留。
- **测试 +1**（buildUx 25：三部件契约 + 真实 Preview 含 defId + Energy + Validator——只装楔铲被「至少1件Weapon」阻断、楔铲+炮通过、刺/镭射单独可 Start）。
- **验证**：vitest 76/409 + tsc 0 error；diff 2 文件。

## Q11-A-R1｜楔铲实际抬升效果修复（commit 47b0d50）

- **问题**：真人录像楔铲正面接触是普通挤压，「钻底→抬起」因果不清。
- **诊断**（真实 Runtime 全链路）：旧 offset.y 22 楔尖 y≈674.3 高于自车底盘下缘 679.3——楔铲藏在自车底盘内；第一次接触撞对手左壁（底盘下缘 680）贴壁挤压 14s（B 俯仰 0.7°），17s 撞墙才掀翻。autoDrive 相向 A/B 接触锁定（同速无相对滑动）→ 楔形不前进；只有深插对手底盘空隙（20px）才第一次接触即可见姿态改变。
- **修复**（仅 content.ts wedgeShovel 几何）：offset.y 22→38（楔尖世界 y≈690.3 深插对手底盘空隙，0.7s 内抬 10px + 抬头 10°）+ 楔尖 x 38→78（加长 40px 够到对手前轮下方，旧 64px 停在轮子左缘外 0.2px）。数值/质量/坡面未动；无隐藏力。
- **测试**：Q11-A test2 改真实语义（前 5s 内 B 明显抬升——旧 5s 时 B 静止；旧 30°/100px/20° 断言是撞墙伪验收）+ test3 新增（B 在 A 后方 → 无自动翻车，允许失败）。buildUx 26。
- **坑**：调参 6 轮收敛（offset 22/30/34/38 × vy -8/-18/-24/-28/-35）——「顶轮 10px」与「舀底盘掀翻」在接触锁定下互斥；最终选 offset 38 + vy -18（第一次接触即有效 + 最终真实反作用量级有限）。
- **验证**：vitest 76/410 + tsc 0 error；diff 2 文件。

## Q11-C-R1｜镭射射击身份与后坐（commit de2d282）

- **问题**：发射后弹体与普通炮太像；强后坐（recoil 60）正常速度不可感知。
- **诊断**（真实 Runtime 沿一次 fire）：recoil 60 → FIRE 帧 chassis vx 1.5→1.171（Δ0.33px/step≈20px/s），autoDrive 150ms 拉回，angle 0.085°——recoil 本身弱（非 drive 抵消）。
- **修复**：① content.ts recoilImpulse 60→240（Cannon 8×，延续「初版故意做大」）：FIRE 帧 vx→0.111（Δ1.39px/step≈83px/s 明显后顿），恢复 ~700ms；蓄能/弹速/伤害/冷却未动。② projectile 视觉区分：RenderProjectile + visual?:'laser'（只读渲染标记）；LaserRuntime 输出 visual:'laser'；renderer 镭射弹亮白青+光晕+绘制半径×1.6（真实碰撞半径未改，hit/miss 仍真实 Projectile/CCD）。Cannon 弹不带标记。
- **测试**：Q11-C test1 recoil 断言 >4×、test5 骤降断言 0.8（Δ1.39）、test6 新增（镭射弹带 visual/Cannon 不带）。buildUx 27。
- **验证**：vitest 76/411 + tsc 0 error；diff 5 文件。

## Q11-A-R2｜楔铲几何重新设计（commit bdc0daf）

- **问题**：楔铲已进香蕉前轮下方但香蕉几乎不抬头（两车水平顶住）。根因 = 旧楔面过长过平（104/16 ≈ 9°），且上轮测试针对 boxBody 不代表 banana。
- **新几何**（按 watermelon(A)+banana(B) 世界坐标）：楔尖 (A+138, groundY−10) 贴地+低于 banana 底盘下缘；坡顶 (A+90, groundY−32) 侵入 banana 底盘 12px；坡长 48/高差 22 → atan(22/48)≈24.6°（20-30° 短陡坡，矫枉过正）；灰盒=Collider 自动一致。
- **Q11 场景**：B 改 bananaBody+标准轮+无功能件（bananaTargetBuild）；A 已是 watermelon+楔铲无干扰件；正常 Build 接入不变。
- **实测**（确定性 seed）：firstEnter 2.9s → 4s 时 banana 前轮离地 21px（轮完全离地）+ 抬头 9.7°，全在碰墙/Closing（10s）前；final 掀翻 98.9° + A 反作用 92.4°。
- **测试**：Q11-A test2 改 banana 语义（碰墙前前轮离地>15px + 抬头>7° + A 反作用；不依赖墙）；test3 自动随 buildB 用 banana。buildUx 27。
- **验证**：vitest 76/411 + tsc 0 error；diff 3 文件。

## Q11-C-F2｜能量弹无重力 Projectile（commit 4bf8b14）

- **能力**：PlanckBodyOptions + gravityScale?:number（默认 1，允许 0）；assertBodyOptions 校验 >=0；private createBody 加参数 → native.setGravityScale；createDynamicCircle/Polygon/Box 三处透传 options?.gravityScale ?? 1；createDynamicCompound 支持（不传保持 1）。不改世界重力/不每帧强制 y。
- **Laser**：createDynamicCircle 加 gravityScale:0 → 水平炮口直线飞行（无抛物线）；Cannon 未传 → 默认 1 抛物线不变。
- **测试 +1**（buildUx 28）：镭射弹 y 范围 <1px（直线）+ x 前进 >100px；Cannon 弹 y 下降 >2px（抛物线保持）。
- **验证**：vitest 76/412 + tsc 0 error；diff 3 文件。

## Q11-A-CLOSE｜楔铲退出正式 Build（commit 1f32dc5）

- **结论**：固定被动楔铲连续多轮真人验收失败（对方前轮/前置部件优先阻挡楔面，「钻底盘→明显抬升」不稳定成立）；不再调 geometry。
- **改动**：main.ts PART_OPTIONS 移除 wedgeShovel（装配页不再出现）；content.ts wedgeShovel 定义 + scenarios.ts Q11 场景标记 ARCHIVED PROTOTYPE（保留可加载）；polygon/collision 底层能力未删。
- **测试 +1**（buildUx 29：registry 保留 + Scenario 可加载 + 刺/镭射不受影响）；既有 Q11-A 物理测试保留。
- **验证**：vitest 76/413 + tsc 0 error；dev 转换后装配选项 wedgeShovel 0 处。

## Q11-C-R2-RECOVER｜镭射 R2 未落地追查并恢复（commit 3adfd2f，remote=local=3adfd2f ✓）

- **追查**：git log 证实无 Q11-C-R2 提交（仅 Q11-C/R1/F2）；无未提交改动/其他分支/worktree；dev 服务即 HEAD → **A 类（R2 从未实现）**。
- **恢复四项**：① muzzleSpeed 16→56（48~64，保留 gravityScale 0 直线）；② recoil 改作用于 vehicle.body（chassis，非 weld 子体，muzzlePoint 力矩）；③ Renderer 圆球→长条能量束（RenderProjectile +velocity 只读标记，LaserRuntime 读真实速度，150px 世界长 + 白青核心 + glow + 尾迹，碰撞半径不扩大）；④ 音效闭环（audioService startLaserCharge 升调 180→600Hz + stopLaserCharge 爆鸣 1200→2400 + 低频冲击 120→40；Start resume AudioContext；无环境/muted no-op 降级）。
- **测试**：test1 muzzleSpeed 48~64 区间；test7 采样放宽（弹速 56 命中前 ~3 步）；新增 Q11-C-R2（velocity 字段 + 音效降级不抛错）。buildUx 30。
- **验证**：vitest 76/414 + tsc 0 error；dev 四标记 grep 全在；remote=local SHA 已对比确认。

## F-DEV-1｜Runtime 版本可追溯 + 防旧 Dev Server（commit 046b02e）

- **问题**：真人录像多次录到旧 Runtime（远端已删楔铲但录像还能装）→ 旧代码被当新版本验收。
- **改动**：① Runtime Badge（DEV only，右下角 branch @ shortSHA）——vite 虚拟模块 virtual:runtime-info 启动时 git 读取注入（非手写常量，import.meta.env.DEV 门控）；② vite.config strictPort=true（端口占用直接失败实测 'Port 5173 is already in use'，不自动切端口/不 kill）；③ listening 钩子打印 cwd/branch/HEAD/port；④ PART_OPTIONS 移入 src/core/partOptions.ts（UI 与测试共用数据源）+ smoke 测试（不含 wedgeShovel 含 spear/laser）。
- **坑**：vite 8 define 不替换 __RUNTIME_INFO__（改用虚拟模块 resolveId/load 才生效）；TS 解析虚拟模块需独立 .d.ts（src/virtual-runtime-info.d.ts）；tsconfig include 移除 vite.config.ts（node API 类型）；dev server 需重启加载新 config（HMR 不覆盖 config 变更的完整语义）。
- **验证**：vitest 76/415 + tsc 0 error；badge SHA=c36929b=git rev-parse HEAD；strictPort 失败实测。

## Q12-A｜冲撞头 Weapon（commit fd70f20）

- **实现**：复用既有 ramHead（behavior ram → ContactRouter 直击），玩家名「冲撞头」入 PART_OPTIONS；collider 20×30 → 44×26 offset 22（短粗前置，vs 刺 96×6）；Q12 隔离场景（watermelon+ramHead vs bananaTarget）。
- **适配（ramHead collider 变更副作用）**：planckVehicleAssembly COM 85→97（offset 10→22）；projectileRenderSnapshot spawnB 640→700（B=lightVehicle 也装 ramHead，变宽后 B 左缘前突 44px，弹第一步即碰 B ramHead 被销毁——弹道测试距离需适配）。
- **测试 +1**（buildUx 33：定义契约/装配链路/正面 damage≥75/擦空 0 伤害）。
- **验证**：vitest 76/416 + tsc 0 error。
- **坑**：git stash 失败清掉了 .git/refs/heads 与 pack 数据（对象库损坏）→ 按项目惯例 mv .git 备份 + git init + SSH fetch 远端 + reset 恢复（mixed 保工作区改动）；期间定位 projectileRenderSnapshot 失败根因 = lightVehicle 装 ramHead 的 collider 变宽导致弹出生接触。

## Q12-B｜主动举升臂 Gadget（commit 0a9cef9）

- **实现**：lifter（举升臂）Gadget——front Revolute（assembly 与 Hammer 同走 createRevoluteJoint）+ LifterBehavior 状态机（rest→lift→hold→lower 循环，复用 setRevoluteLimit/Motor/getRevoluteAngle）；上翻 70°（upperRad 1.22，60~80° 目标）、lift 0.015 rad/step ~1.35s；臂 100×14 offset {50,0}；无 baseDamage → 无 Direct Weapon Damage（实测 sources 仅 impact/hazard）。PART_OPTIONS「举升臂」+ Q12-B 场景（watermelon vs banana 空车）。
- **调参收敛（几何探索）**：offset.y 30（贴地）→ banana 滚过不接触；10（底盘下）→ 臂被推开；0（pivot 高度侵入底盘）→ banana 被顶（低位 24.7px + 11.3°）✓。臂长 60→80→100（翻起水平投影缩短，够到 banana 停靠位）。
- **测试 +1**（buildUx 34：定义契约/Revolute/Energy 15/gadget 单独 Validator 拦截、+cannon 通过/前 15s 翻动 55~85°/banana 抬起 >15px/无 weapon/A 反作用）；behaviorRegistry +lifter。
- **验证**：vitest 76/417 + tsc 0 error。
- **坑**：测试跨 world 用旧 part joint 报错（场景要重新 find part）；maxAngle 180° 仅 A 翻车后异常（测试限定前 15s 正常翻动）。

## Q12-C｜伸缩冲锤 Weapon（commit 63ef7ae）

- **实现**：rammer（冲锤）Weapon——Prismatic（assembly 与 pushRod 同走 createPrismaticJoint）+ RammerBehavior 状态机（rest 前摇 → strike 快伸 → hold → retract 循环）；baseDamage 70 → ContactRouter weapon 直击（真实 Contact 才伤害，撞空 Miss）；strike 8 vs 推杆 2 px/step、行程 160 vs 90、maxForce 500 vs 30；PART_OPTIONS「冲锤」+ Q12-C 场景。
- **调参**：maxForce 200→500（200 时锤头顶住 B（被 A 车身推着）推不动 → trans 卡住不循环；500 有力推出 + 回收）。
- **测试 +1**（buildUx 35：契约/Prismatic/Energy 25/weapon 可 Start/伸收循环 行程≥140+回收<10/weapon damage≥60/撞空 Miss 0）；behaviorRegistry +rammer。
- **验证**：vitest 76/418 + tsc 0 error。

## 会话交接（2026-08-20 22:55）｜生成交接包

- 生成 `最强水果_WorkBuddy会话交接_2026-08-20_晚.md`（含本会话 5 队列汇总 / git 状态 / 环境惯例 / 已知坑 / 下一步建议）。
- 新窗口交接对话指令已随交接文档一并给出（复制即用）。

## 接手验证（22:55→23:00，新窗口 take-over）

- **Git 核对**：branch=foundation-02-canonical；local HEAD=efad32c（docs 提交，父 63ef7ae=Q12-C impl）== 远端 refs/heads/foundation-02-canonical（ls-remote 确认一致）；Q12-A/B/C 三 impl 提交 fd70f20/0a9cef9/63ef7ae 均存在且在分支上；旧 feature/ 分支仍 d0411fb（弃用未删）。交接文档 §1(HEAD=efad32c) 与 §3(Q12-C impl=63ef7ae) 不矛盾（docs 包装 vs impl）。
- **Dev server 关键隐患（已修复）**：接手时 5173 在跑的 dev server（PID 14580）Badge SHA=`bb8953f`（=b593bf1，Q12-B docs 时代），**落后当前 HEAD=efad32c 整整一个 Q12-C 实现提交**——即页面跑的是不含冲锤的旧 Runtime。根因：vite 虚拟模块 `virtual:runtime-info` 的 load 结果被 Vite 模块图缓存，首次读取后不随 git 更新（vite.config 注释写「每次请求读取最新」但实际不生效）。按 §6 重启：`taskkill` 无效（MSYS `//PID` 被原样传）→ 改用 PowerShell `Stop-Process -Id 14580 -Force`；重启后 Badge=`efad32c`（**=HEAD，一致**），新 PID 17684。
  - **【新坑·缓存误导】**：F-DEV-1 Badge 在 dev server 不重启时仍会 stale —— 未来任何 commit/push 后若未重启 dev server，Badge 显示旧 SHA，防护形同虚设。如需真·实时，应改 vite 插件（transformIndexHtml 注入 / 每次请求 invalidate 虚拟模块），而非仅靠重启约定。待用户决策是否修。
- **测试基线**：vitest 76 文件 418/418 + scoped tsc 0 error（与环境标注一致，绿）。
- **结论**：接手环境健康；旧 Runtime 风险已消除；下一步按 §7 做 Q12-A/B/C Physics Lab 集中验收（正式速度+Debug 关，录像）。

## Queue Q12-A-HOLD｜冲撞头暂退正式 Build（prototype/hold，23:00→23:07）

- **判断**：冲撞头（ramHead）只是固定短方块 + Contact Damage，技术成立但真人录像无独立动作/明显物理结果，与普通车头碰撞差异太弱；不补特效/参数。
- **必改**：从玩家 PART_OPTIONS 移除 ramHead / 冲撞头（src/core/partOptions.ts）。registry（content.ts def + functionals Map）、Q12-A Scenario（scenarios.ts，改名 `Ram Head (prototype/hold)`，仍可加载）、Contact Runtime（contactRouter/combatEvents，底层 ram Contact 不修改）、测试（buildUx Q12-A-HOLD 用例）全部保留并标记 prototype/hold。buildEditorModel.ts 过时注释同步更新。
- **关键决策·presets.ts 不动**：`ram()` 辅助被 lightVehicle/heavyVehicle/frontHeavy/rearHeavy 四个玩家可见 Preset 引用；但接手文档 §6 已知坑点名"ramHead collider 变更会波及 lightVehicle/heavyVehicle preset（B 也装 ramHead）"——作者已知并保留为 Scenario/测试脚手架；本指令"必改"只点名 PART_OPTIONS、保留清单未含 presets，按"不顺带扩大模块"纪律不动 presets，避免牵连依赖它的物理测试回归。验收"正常 Build 不再显示冲撞头"按装配页下拉解读。
- **测试改写**：F-DEV-1 smoke 补 `expect(ids).not.toContain('ramHead')`；Q12-A 用例改名 Q12-A-HOLD，section 2 由"PART_OPTIONS 含 ramHead"改为"不在 PART_OPTIONS（已 hold）但仍在 registry"，其余 Preview/Energy/Validator/Q12 正撞≥75/擦空0 保留（验证 Contact Runtime 仍成立）。
- **验证**：buildUx 34/34 + 全量 vitest 76 文件 418/418 + scoped tsc 0 error。
- **提交**：`6d3755f` Q12-A-HOLD｜冲撞头暂退正式 Build（prototype/hold）（5 文件 +20/−9）；未跟踪的交接 MD / 旧 memory 文件按"勿动"未提交。待补 docs 日志提交。

## Q12-B-R1｜举升臂真实向上修正（facing 感知，commit 78a35e5，已 push）

- **问题（真人录像）**：当前所谓 lift 实际从水平位置向地面扫下，不是向上举升。
- **根因**：本世界 Y-down（y 越小越靠屏幕上方），Planck joint angle 增大 = 顺时针 = 臂尖向地面扫。旧代码不分 facing 地把 angle 推到 +upperRad → facing=+1 向下扫（tip.worldY 增大 ~40px，甚至撞地）；facing=-1 恰好正确。
- **修复（src/battle/lifterBehavior.ts）**：举升方向随 facing 翻转 `liftSign = -facing`，liftTarget = liftSign·upperRad；Revolute limit 设为 [min(rest,target), max(...)] 结构禁止臂低于水平 → 正常直立车辆上绝不主动扫地；lift/lower 的 motor speed 符号随 liftSign。不改对手 angle/velocity、不加向上 impulse、不改全局 Physics、不调 torque 掩盖。
- **数值（src/core/content.ts）**：liftSpeed 0.015→0.045（完整上翻 ~0.45s，原 1.35s 太软）；lowerSpeed 0.01→0.025（回落稍慢）；保留 upperRad=1.22（70°，60~80°内）、lowerRad=0（近水平）、maxTorqueNm=400。
- **调查方法（关键，可复用）**：先写临时诊断测试跑真实 Runtime，记录 lifter 远端 tip 的 worldY 轨迹，确认 facing=+1 向下（bug）、facing=-1 向上；修复后重跑确认两朝向都上移 ~95px（worldY 654→559）、downOvershoot 仅 ~3px。再删诊断、写正式测试。
- **测试（tests/buildUx.test.ts）**：Q12-B 场景角度幅度断言改 `Math.abs(ang)`（facing=+1 现为负角）；新增 `Q12-B-R1` 双 facing tip worldY 方向验证——记录真实 Runtime 下臂尖 worldY 断言 upReach>60，且相对 joint angle 绝不朝下（结构上限）。注意：世界 tipY 受底盘俯仰污染，故"绝不扫地"改用相对 joint angle 极值判定，更精确。
- **验证**：buildUx 35/35 + 全量 vitest 76 文件 419/419（+1）+ scoped tsc 0 error。
- **提交**：`78a35e5` Q12-B-R1｜举升臂真实向上修正（facing 感知）（3 文件 +121/−26）；未跟踪交接 MD / 旧 memory 按"勿动"未提交。

## Q12-C-R1｜冲锤机械结构与冲击感修正（commit 0e181ba，已 push）

- **问题（真人录像）**：冲锤锤头伸出后像悬空方块，看不出与车身机械连接；RenderSnapshot 仅 pushRod 输出 connector，rammer 没有。
- **修复（src/battle/planckBattleOrchestrator.ts）**：rammer 复用 Push Rod 同一真实 Prismatic Joint connector——`pushRodAnchorWorld`/`PUSH_ROD_CONNECTOR_WIDTH` 改名 `prismaticAnchorWorld`/`PRISMATIC_CONNECTOR_WIDTH` 共用；connector 三元判断加入 `rammer`：from = chassis hardpoint 真实世界位置（镜像 facing 随车身姿态旋转）、to = rammer part 原点真实位置、轴长 = 真实 Prismatic translation。不复制第二套 Connector；轴随真实伸缩伸长/缩短，无假动画。锤头（30×22 短粗）从车身经轴连续连到锤头，全程无漂浮方块。
- **关键坑（必记）**：冲锤打击节奏原"仅提 strikeSpeed 无效"——实测 motor 力瓶颈 ~7.6px/step（maxForceN=500 力受限）。必须同步提 maxForceN 才提速。最终 strikeSpeed 8→20、maxForceN 500→1200（~2.4×），实测 160px ≈ 12 步 ≈ 0.20s（目标 0.18~0.25s）；restSteps 40→24（回收位短暂停顿更利落）。只调 rammer，不改 Push Rod。
- **测试（tests/buildUx.test.ts）**：新增 `Q12-C-R1` 真实 Runtime 验证——rammer connector 存在、轴长随真实 translation 连续（rest≈0、mid/full≈translation，证无假动画）、打击 0.18~0.25s、Miss 仍完整伸出+自然回收+无 weapon damage。注意：snapshot 的 RenderFunctionalPart 不含 `def`，按 `vehicleA.parts` 索引定位 rammer 再取同序 snapshot part。
- **验证**：buildUx 36/36 + 全量 vitest 76 文件 420/420（+1，419→420）+ scoped tsc 0 error；Push Rod 原表现不变（Q04-R1B 测试无回归）。
- **提交**：`0e181ba` Q12-C-R1｜冲锤机械结构与冲击感修正（3 文件 +85/−16）；未跟踪交接 MD / 旧 memory 按"勿动"未提交。

## Q11-C-R3-FINAL｜镭射爆发感最终结构修正（commit c74f33f，已 push）

- **问题（真人录像仍软）**：能量束仅 150px 且跟随 56px/步高速弹（30fps 下每帧飞 ~500px 出屏，只一闪）；蓄能最大半径 21px、炮口黄色闪、laser 还混播 Cannon 的 fire 音。这是 Laser 当前最后一次结构性返修（若真人仍无爆发感则转正式美术/VFX）。
- **巨炮束（必改 1）**：renderer 新增 `laserBeam` VFX——发射后沿真实 fire 方向固定驻留（不跟弹），长 520 / 核心 15 / glow 38（世界 px，达标 450~600/12~18/30~45），TTL 130ms 衰减（≈3.9 帧可读）；真实 Collider/伤害范围绝不扩大。替换原 150px 跟随束为小亮头。新增 `spawnLaserBeam(x,y,dirX,dirY)` + `activeLaserBeams` getter（测试用）。
- **蓄能末段升级（必改 3）**：progress>0.7 外圈明显扩大（extra +22）+ 脉冲加速（period 随 p→1 缩短）+ 亮度提升；fire 瞬间白青大闪（`spawnMuzzleFlash` 支持 color/radius，laser 用 #eafdff r14）。不建复杂粒子系统。
- **强后坐（必改 4，关键坑）**：实测 recoil 240 时车**从不反向位移**（autoDrive 当帧拉回 vx，只剩数字变化，不满足「明显反向运动」）。只调 Laser 自身 `recoilImpulse` 240→560（Δv≈3.3px/step），开炮后前 ~2 帧 vx 转负、车实际顿退 ~22px，正常速度肉眼可见后震；不改全局物理/对手。
- **音效（必改 5）**：main.ts 让 laser 不再播 Cannon 的 'fire'（仅用 stopLaserCharge 爆鸣+低频，明显区别 Cannon）；audioService 降 laser burst 增益（high 0.16→0.13 / low 0.22→0.16）防 clipping。不改伤害/Projectile Foundation/Cannon。
- **测试（tests/rendererLaserBeam.test.ts + buildUx.test.ts）**：renderer 级 3 项（束几何/三层绘制宽度/蓄能末段外圈扩大）；buildUx 新增 3 项 Q11-C-R3-FINAL（事件→hook→renderer 巨炮束几何达标、真人可见后坐 maxBack>1px/帧且 vx<0、Cannon 不受影响不生成束）。
- **验证**：buildUx 39/39 + 全量 vitest 77 文件 426/426（+6）+ scoped tsc 0 error。
- **提交**：`c74f33f` Q11-C-R3-FINAL（6 文件 +388/−62，含新 tests/rendererLaserBeam.test.ts）；未跟踪交接 MD / 旧 memory 按"勿动"未提交。
