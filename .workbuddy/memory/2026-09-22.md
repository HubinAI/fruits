# 2026-09-22

## Queue：`PRODUCT-LOOP-R2-VALIDATION-STATE-RESEED-R1`（R2 验证起点的版本化一次性 reseed）

**问题**：上一轮真人已用 `r2Onboarding` 完整验过一轮 ⇒ 存档落在「`cannon ★2` 已合成且装备着、
新领的 ★1 只剩 1 件、旧 onboarding 标记已消费」的形态，
`4/5 → 领奖 → 5/5 → Fusion → ★2 → 下一局更强` **第二次不可达**。
Queue 明令不许让玩家自己清 localStorage。

**根因**：旧 `r2Onboarding` 结构上救不了 —— 判据 ② 是「存在 ★≥2 就一个字节都不动」
（那是它当初的**保护逻辑**）。「把旧标记 +1」是死路。

**交付**（7 个文件）：
- **新增** `src/product/r2Reseed.ts`（新 key `strongfruit.r2Reseed.v1` / `R2_RESEED_VERSION = 1`）
- **新增** `tests/productReseedR2.test.ts`（VR-01..VR-16，19 条）
- **新增** `tests/_e2e_product_reseed.cjs`（R0..R9，18 条；端口 8172）
- 改 `src/product/playerGrowth.ts`（接进唯一读入口）· `src/product/homePage.ts`（消费 `growth.draft`）·
  `tests/productLoopHomeGarage.test.ts`（`PL-26` 导入白名单闭集 +1）· `package.json`（+`e2e:product-reseed`）

**五条判据**（同时成立才执行）：① 本模块标记不在盘 ② 旧 onboarding 已执行（打开过产品首页）
③ 领奖账本非空 ④ `cannon` 有 ★≥2 ⑤ `cannon ★1 < 4`。
⚠️ ②③ 都要：库存 key 是**旧横屏与竖屏产品共用**的，旧横屏 `fuseSameStar` 同样造得出 `cannon ★2`；
③ 才是那个**只可能由产品链路产生**的独立证据。

**执行顺序**：`plan`（纯读）→ `addPart` 补到 4（内存）→ `equipWeapon(…,1)` → `consume` ★2..★5
→ `saveInventory`（唯一落盘）→ 最后 mark。装备被拒 ⇒ **零副作用**（回滚内存改动、零落盘、可重试）。

**踩到并修正的坑（★1 = 缺省）**：`equipWeapon(star = 1)` 会把 `functionalStars[WEAPON_SLOT]`
**整个键删掉** ⇒ 断言里读它直接抛 `Cannot read properties of undefined (reading 'frontMass')`。
要断的是 `functionalStars === undefined`（★1 的正确形态就是没有这个键）。

**负控制 A（`NEGCTL`）**：把 `planR2Reseed` 末行改成 `return keep('not-consumed')`（迁移永不执行）
⇒ 单测 **10 failed / 9 passed**、E2E **6/16**（R2/R2b/R2c/R2d/R2g/R3/R4/R4d/R4e/R9 全红）。
干净回退 + grep 零残留。证明守卫不是空转。

**同时收口**：`MEMORY.md` 去重压缩并把「现状 / 下一步」块**移到最前**（以后截断先截尾部）；
旧版全文存 `.workbuddy/memory/archive/MEMORY_FULL_2026-09-22.md`；
`REF_GUARDS_TRAPS_CONTRACTS.md` 追加 **§14（a–f）**；写本地交接文档
`交接文档_2026-09-22_PRODUCT-LOOP-R2-VALIDATION-STATE-RESEED-R1.md`（不入库）。

**边界取证**：`src/{core,battle,physics,render,player,platform,ui,game,presentation}` diff 全空；
`git diff --exit-code -- src/core/content.ts src/battle/contactRouter.ts` = 0。

⚠️ **本轮首绿（19/19 + 18/18）不是终点**：全量门禁随后被 `e2e:product-reward` 拦下
⇒ 见 `2026-09-24.md`（那里才是这个 Queue 真正的收口）。
