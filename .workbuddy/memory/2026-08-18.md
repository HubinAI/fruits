# 2026-08-18 工作日志

## Queue F-02M-A8｜Planck Revolute motor ON/OFF 驱动验证（未提交）

- planckWorld.ts：新增 `setRevoluteMotor(joint, { enabled, speedRadPerStep, maxTorqueNm })`——speed 用 radPerStepToRadPerSec 换算、maxTorqueNm 原值传 Planck（N·m）、校验有限/torque>=0/enabled boolean；新增 `revoluteJoints` Map 保持真实 RevoluteJoint 类型引用（无 as any/escape hatch/不暴露 Planck 类型）。
- 新测试 tests/planckRevoluteMotor.test.ts（复用 A6 车架、双轮 friction=1、motor 0.02 rad/step / 5 N·m）：
  - 测试1 OFF：停稳 180+300 步，位移 0.0000px、vx=0、不自行行驶；
  - 测试2 ON→OFF：ON 300 步行驶 114.28px（>50）、wheelOmega 精确 0.02、vx 0.395（=ω×r 纯滚动）；OFF 后 vx 保持 0.395（不刹停/不反向）、Δvx(瞬时)=0.0000、总行驶 232.78px（>100）、最终 vx 0.395（>0.05）。
  - maxChassisAngle 0.00027、maxErr 0.00212（<2）、maxSpeed 0.4103（<3）、穿透 0、NaN=false。
- 6/6 通过（motor 2 + friction 1 + A6 3 回归）。HEAD=1f7cd97，未执行 Git 写入，测试保留未跟踪。

## Queue F-02M-A8S｜Revolute motor 驱动接口提交（commit 6ec0a39）

- 提交（parent=1f7cd97，diff 严格 2 文件）：planckWorld.ts（setRevoluteMotor + revoluteJoints 真实类型引用）+ tests/planckRevoluteMotor.test.ts（ON/OFF 驱动，6/6 已验证）。
- 前置：A8 定向 6/6 通过 + scoped strict tsc（--ignoreConfig）0 错误，均在本轮完成。
- cached 清单严格 2 文件 + check 通过；commit-tree parent 严格匹配；`--force-with-lease=...:1f7cd97` 推送成功（1f7cd97..6ec0a39）；loose ref 同步。
- 本地=远程=6ec0a39。分支历史：6ec0a39→1f7cd97→ff609fe→3c286b2→f764ce1→08434ca→4be95e6。
- 未提交：.workbuddy/memory、3 个 Matter 诊断测试。
- 注：定向 tsc 需带 `--ignoreConfig`（新版 tsc TS5112 守护），已记入记忆。

## Queue F-02M-B1｜Planck Body Owner Meta（未提交）

- planckWorld.ts：type-import OwnerTag（src/core/types.ts，kind/vehicleId/partId/team）；新增 ownerTags Map（按 handle 私有）+ setOwnerTag/getOwnerTag（输入/返回均浅拷贝防御复制；未设置 null；跨 World 抛错走 bodyOf 校验；无 as any）。
- 新测试 tests/planckOwnerMeta.test.ts（3/3）：标签往返（chassis/wheel/ground）；防御复制（输入改 vehicleId/kind、返回值改 team 均不污染内部）；未设置 null + 跨 World set/get 抛错。
- 3/3 通过（单 worker）。HEAD=6ec0a39，未执行 Git 写入，测试保留未跟踪。

## Queue F-02M-B1S｜Owner Meta 提交（commit 407e9bf）

- 提交（parent=6ec0a39，diff 严格 2 文件）：planckWorld.ts（OwnerTag 存取，B1 已审）+ tests/planckOwnerMeta.test.ts（3/3，B1T 已审）。
- cached 清单严格 2 文件 + check 通过；commit-tree parent 严格匹配；`--force-with-lease=...:6ec0a39` 推送成功（6ec0a39..407e9bf）；loose ref 同步；未重跑测试。
- 本地=远程=407e9bf。分支历史：407e9bf→6ec0a39→1f7cd97→ff609fe→3c286b2→f764ce1→08434ca→4be95e6。
- 未提交：.workbuddy/memory、3 个 Matter 诊断测试。

## Queue F-02M-B2｜Planck Collision Filter（未提交）

- planckWorld.ts：导出 PlanckCollisionFilter（categoryBits/maskBits/groupIndex?）；Box/Circle/Ground 创建均加可选 collisionFilter（Circle friction 兼容）；createBody 加 filter 参数；filterFixtureDef 辅助（未传 filter 时不附加字段保持默认）；校验 category 1..0xffff、mask 0..0xffff、group -32768..32767 均整数。
- 新测试 tests/planckCollisionFilter.test.ts（5/5）：无 filter 默认碰撞 begin=1；互相允许 mask begin=1；相同负 group begin=0；mask=0 排除 begin=0；非法输入 4 种抛错。
- HEAD=407e9bf，未执行 Git 写入，测试保留未跟踪。

## Queue F-02M-B2S｜Collision Filter 提交（commit 7814c03）

- 提交（parent=407e9bf，diff 严格 2 文件）：planckWorld.ts（PlanckCollisionFilter + 三创建方法扩展 + filterFixtureDef）+ tests/planckCollisionFilter.test.ts（5/5，B2T 11/11 已审）。
- cached 清单严格 2 文件 + check 通过；commit-tree parent 严格匹配；`--force-with-lease=...:407e9bf` 推送成功（407e9bf..7814c03）；loose ref 同步；未重跑测试。
- 本地=远程=7814c03。分支历史：7814c03→407e9bf→6ec0a39→1f7cd97→ff609fe→3c286b2→f764ce1→08434ca→4be95e6。
- 未提交：.workbuddy/memory、3 个 Matter 诊断测试。

## Queue F-02M-B3｜Planck Contact Kinematics（未提交）

- planckWorld.ts：ContactBridgeEvent 扩展 contactPoint(px)/normal(从最终 bodyA→bodyB 单位向量)/relativeVelocity(px/step 正值靠近)；emitContact 重构——begin 用原生 getWorldManifold(null) 取首个真实世界点+法线，规范化 swap 时反转法线+对换点速度；点速度必须 getLinearVelocityFromWorldPoint（含 ω×r）；relVel=(vA−vB)·normal 经 mpsToPxPerStep（不取绝对值/不截零）；begin 快照按 Contact 缓存，end 复用并清理；无 manifold point 抛错不伪造。
- 新测试 tests/planckContactKinematics.test.ts（3/3）：纯平移 relVel=1.0000、normal=(1,0) 单位、end 复用同一快照；反向创建顺序 bodyA=先创建、normal 反转、relVel 仍 1.0；旋转 ω=-0.5 relVel=-0.094 vs comOnly=-2.025 diff=1.93>1（ω×r 计入）。
- 关键：A5 原 contact 测试（begin=1/end=1）需回归确认——接口扩展兼容。
- HEAD=7814c03，未执行 Git 写入，测试保留未跟踪。

## Queue F-02M-B3S｜Contact Kinematics 提交（commit cb8ec49）

- 提交（parent=7814c03，diff 严格 2 文件）：planckWorld.ts（ContactBridgeEvent 运动学扩展：contactPoint/normal/relativeVelocity + getWorldManifold + begin 快照缓存）+ tests/planckContactKinematics.test.ts（3/3，B3T 13/13 已审）。
- cached 清单严格 2 文件 + check 通过；commit-tree parent 严格匹配；`--force-with-lease=...:7814c03` 推送成功（7814c03..cb8ec49）；loose ref 同步；未重跑测试。
- 本地=远程=cb8ec49。分支历史：cb8ec49→7814c03→407e9bf→6ec0a39→1f7cd97→ff609fe→3c286b2→f764ce1→08434ca→4be95e6。
- 未提交：.workbuddy/memory、3 个 Matter 诊断测试。
- 至此 Planck 基础层完整：单位→World/Body/Revolute→重力/地面→接触桥接(含运动学)→friction→motor→Owner Meta→Collision Filter→Contact Kinematics。

## Queue F-02M-B3R｜旋转接触同刻采样修正（未提交）

- 仅改 tests/planckContactKinematics.test.ts 测试3：begin 事件回调内同刻采样（按事件 bodyA/bodyB+normal 读双方 COM 线速度/角速度），计算同刻 COM-only 投影并与事件 relVel 比较；删除旧"240 步后读 COM"逻辑。
- 实测（3/3）：relVel=2.3407 vs 同刻 comOnly=0.5000，diff=1.8407>1，maxSpin=0.5（角速度非零），allFinite=true；前两测试不变（relVel=1.0000 / normal 反转 / end 快照复用）。
- HEAD=cb8ec49，src 零改动，未执行 Git 写入。

## Queue F-02M-B3RS｜同刻采样测试修正提交（commit 19d58b0）

- 提交（parent=cb8ec49，diff 严格 1 文件）：tests/planckContactKinematics.test.ts（B3R 同刻采样修正，B3RT 10/10 已审）。
- cached 清单严格 1 文件 + check 通过；commit-tree parent 严格匹配；`--force-with-lease=...:cb8ec49` 推送成功（cb8ec49..19d58b0）；loose ref 同步；未重跑测试。
- 本地=远程=19d58b0。分支历史：19d58b0→cb8ec49→7814c03→407e9bf→6ec0a39→1f7cd97→ff609fe→3c286b2→f764ce1→08434ca→4be95e6。
- 未提交：.workbuddy/memory、3 个 Matter 诊断测试。

## Queue F-02M-B4A｜Planck 接触批次监听（未提交）

- planckWorld.ts：ContactBridgeEvent 加可选 batch{timestamp,index,size}；新增 setBatchedContactListener（每原生 step 完成后统一派发，stepFixed 每步 flush，不跨步合并）；timestamp=physicsStep×SECONDS_PER_STEP×1000（单调，禁 Date.now）；同 phase 共享 timestamp/size、index 连续；begin/end 分别计数；即时监听（setContactListener）保持同步派发、batch 恒 undefined（绝不延迟，保 B3 同刻采样）；两者并存不重复不改快照。
- 新测试 tests/planckContactBatch.test.ts（4/4）：同一步双 begin ts 相同 size=2 index=[0,1]；两批 ts 633.3→2633.3 严格增加、第二批 index 重 0；即时无 batch/批次有 batch/relVel 一致；仅批次监听可用。
- HEAD=19d58b0，未执行 Git 写入，测试保留未跟踪。

## Queue F-02M-B4S｜Contact Batch Listener 提交（commit b5c1281）

- 提交（parent=19d58b0，diff 严格 2 文件）：planckWorld.ts（batch 可选字段 + setBatchedContactListener + 每步 flush）+ tests/planckContactBatch.test.ts（4/4，B4T 9/9 已审）。
- cached 清单严格 2 文件 + check 通过；commit-tree parent 严格匹配；`--force-with-lease=...:19d58b0` 推送成功（19d58b0..b5c1281）；loose ref 同步；未重跑测试。
- 本地=远程=b5c1281。分支历史：b5c1281→19d58b0→cb8ec49→7814c03→407e9bf→6ec0a39→1f7cd97→ff609fe→3c286b2→f764ce1→08434ca→4be95e6。
- 未提交：.workbuddy/memory、3 个 Matter 诊断测试。
- 至此 Planck 接触层完整：运动学（B3）+ 批次（B4A）——Contact Router 所需的全部原始数据就绪。

## Queue F-02M-B5A｜ContactRouter 接入 Planck Contact（未提交）

- contactRouter.ts：抽取引擎无关 RouterContactEvent（无 body）+ 共享 route()；新增 handlePlanckContact(world, ev)——getOwnerTag 读 Owner（无 Owner 安全忽略）、begin→start/end→end、运动学+batch 原值传递；Matter handleContact 与 Planck 入口共用 Grounded/批次去重/Impact/Weapon；Planck Runtime 接入只用 setBatchedContactListener（单投递）。
- 新测试 tests/planckContactRouterBridge.test.ts（2/2）：双 ram（part:front）对称撞 B 同一步 2 个 begin（size=2）→ weapon 只结算一次 80（hpB 1000→920）、Impact 999 隔离 lastImpact null、Debug lastContact/lastDamage 有值；wheel→ground begin/end 翻转 grounded（正式 vehicleA.wheels）。
- 关键经验：同一步多部位接触需对称撞（B4A 已验证模式）或同平面几何；单 ram 撞车会因撞停/移动不同步。
- HEAD=b5c1281，未执行 Git 写入，测试保留未跟踪。

## Queue F-02M-B5S｜ContactRouter Planck 桥提交（commit 7156248）

- 提交（parent=b5c1281，diff 严格 2 文件）：contactRouter.ts（RouterContactEvent 抽取 + handlePlanckContact + 共享 route）+ tests/planckContactRouterBridge.test.ts（2/2，B5T 13/13 已审）。
- cached 清单严格 2 文件 + check 通过；commit-tree parent 严格匹配；`--force-with-lease=...:b5c1281` 推送成功（b5c1281..7156248）；loose ref 同步；未重跑测试。
- 本地=远程=7156248。分支历史：7156248→b5c1281→19d58b0→cb8ec49→7814c03→407e9bf→6ec0a39→1f7cd97→ff609fe→3c286b2→f764ce1→08434ca→4be95e6。
- 未提交：.workbuddy/memory、3 个 Matter 诊断测试。
- 里程碑：Planck 物理层（World/Body/Revolute/重力/接触运动学/批次/filter/Owner/Motor/friction）→ Battle 层（ContactRouter 双入口共用）已打通。下一步可做 Planck BattleOrchestrator 或驱动验证。

## Queue F-02M-B6A｜Planck Weld Joint 最小实现（未提交）

- planckWorld.ts：新增 setAngle(body, angleRad)（有限校验、保留位置、不清零速度）+ createWeldJoint（referenceAngle 保存当前相对角度不归零、锚点走 units.ts、createJoint null 抛错、opaque handle 无 as any）。
- 新测试 tests/planckWeldJoint.test.ts（3/3）：非零相对角 0.5 Weld 后贴地滑动 400 步——锚点 maxErr=0.000000px（<1）、相对角漂移 0.000000rad（<0.01）、水平运动 410px；setAngle NaN/Infinity 抛错+位置保留速度不清零；跨 World 抛错。
- 经验：Weld 落地测试需贴地 spawn（高空砸地冲击会让 WeldJoint 锚点误差到 4.4px——与 A4/Planck POC 的落地瞬态一致）。
- HEAD=7156248，未执行 Git 写入，测试保留未跟踪。

## Queue F-02M-B6S｜Planck Weld Joint 提交（commit c8c907b）

- 提交（parent=7156248，diff 严格 2 文件）：planckWorld.ts（setAngle + createWeldJoint referenceAngle）+ tests/planckWeldJoint.test.ts（3/3，B6T 7/7 已审）。
- cached 清单严格 2 文件 + check 通过；commit-tree parent 严格匹配；`--force-with-lease=...:7156248` 推送成功（7156248..c8c907b）；loose ref 同步；未重跑测试。
- 本地=远程=c8c907b。分支历史：c8c907b→7156248→b5c1281→19d58b0→cb8ec49→7814c03→407e9bf→6ec0a39→1f7cd97→ff609fe→3c286b2→f764ce1→08434ca→4be95e6。
- 未提交：.workbuddy/memory、3 个 Matter 诊断测试。

## Queue F-02M-B7A1｜Planck Polygon Body（未提交）

- planckWorld.ts：导出 PlanckBodyOptions（friction?/restitution?/collisionFilter?）；Box/Circle 改用（默认行为不变）；新增 createDynamicPolygon + normalizePolygonVertices（shoelace 面积/凸性/相邻叉积校验，CCW 规范化，px→m）；createBody 加 restitution；filterFixtureDef 扩展；assertBodyOptions 校验（friction>=0、restitution∈[0,1]）。
- 新测试 tests/planckColliderBody.test.ts（4/4）：wedge 轮廓（content.ts 原顶点）mass=50.0000 精确；CW 反转规范化后一致；6 类非法 polygon（非有限/2/9 顶点/零面积/凹/共线重复边）全抛错；非法 material 抛错+合法 friction0.8/restitution0.4 正常。
- HEAD=c8c907b，未执行 Git 写入，测试保留未跟踪。

## Queue F-02M-B7A2｜Planck Compound Collider（未提交）

- planckWorld.ts：createDynamicCompound（ColliderDef[] → 单 native body 多 fixtures；box 转 polygon 支持旋转；polygon 顶点先按 angle 旋转加 offset；统一 density=mass/总面积 → mass 精确；同 filter/material；禁止拆 body/Weld）；colliderToShape/colliderAreaPx2 辅助；getCenterOfMass（getWorldCenter → px）。
- 测试追加（5/5 含 B7A1 回归）：Box+Circle+Polygon(angle 0.3) compound——mass=100.0000 精确、pos=(50,100) 原点保持、COM=(35.52,104.87) 偏移(-14.48,4.87)（与 shoelace 手算一致）、30 步无 NaN、OwnerTag 单一 handle 往返。
- HEAD=c8c907b，未执行 Git 写入，测试保留未跟踪。

## Queue F-02M-B7S｜Planck Collider Body 提交（commit d4be059）

- 提交（parent=c8c907b，diff 严格 2 文件）：planckWorld.ts（PlanckBodyOptions + createDynamicPolygon + createDynamicCompound + getCenterOfMass）+ tests/planckColliderBody.test.ts（5/5，B7T 13/13 已审）。
- cached 清单严格 2 文件 + check 通过；commit-tree parent 严格匹配；`--force-with-lease=...:c8c907b` 推送成功（c8c907b..d4be059）；loose ref 同步；未重跑测试。
- 本地=远程=d4be059。分支历史：d4be059→c8c907b→7156248→b5c1281→19d58b0→cb8ec49→7814c03→407e9bf→6ec0a39→1f7cd97→ff609fe→3c286b2→f764ce1→08434ca→4be95e6。
- 未提交：.workbuddy/memory、3 个 Matter 诊断测试。

## Queue F-02M-B7R｜Compound 输入边界 + 真实 Contact 返修（未提交）

- planckWorld.ts：新增 validateCollider（统一校验：Box width/height/offset/angle 有限且尺寸正；Circle radius/offset/angle 有限且 radius 正；Polygon offset/angle 有限，顶点走现有严格校验），createDynamicCompound 面积/shape 创建前先校验——杜绝 NaN/Infinity 进 density/Planck。
- 测试追加（7/7 含回归）：真实 Contact——多 fixture Compound 落地 begin=1，事件 handle 恒为同一 BodyHandle、getOwnerTag 一致；8 类非法 collider 输入（circle NaN/Infinity radius/offset/angle、box 0 尺寸/Infinity offset/NaN angle、polygon NaN offset）全拒绝；合法 compound 正常。
- 未改质量/COM/几何/旧 Box/Circle 默认语义。HEAD=d4be059，未执行 Git 写入，测试保留未跟踪。

## Queue F-02M-B7RS｜Compound 边界返修提交（commit b90ec16）

- 提交（parent=d4be059，diff 严格 2 文件）：planckWorld.ts（validateCollider 统一校验）+ tests/planckColliderBody.test.ts（7/7，B7RT 12/12 已审）。
- cached 清单严格 2 文件 + check 通过；commit-tree parent 严格匹配；`--force-with-lease=...:d4be059` 推送成功（d4be059..b90ec16）；loose ref 同步；未重跑测试。
- 本地=远程=b90ec16。分支历史：b90ec16→d4be059→c8c907b→7156248→b5c1281→19d58b0→cb8ec49→7814c03→407e9bf→6ec0a39→1f7cd97→ff609fe→3c286b2→f764ce1→08434ca→4be95e6。
- 未提交：.workbuddy/memory、3 个 Matter 诊断测试。

## Queue F-02M-B8A｜Planck Vehicle Chassis 装配（未提交）

- 新增 src/battle/planckVehicleAssembly.ts：PlanckCategory（本地复制 adapter Category 数值，禁 import adapter）、PlanckWheelRuntime/PlanckPartRuntime/PlanckVehicle 类型、createPlanckVehicle(world, resolved, team, initialPos, facing)——createDynamicCompound(body.baseMass) + OwnerTag(kind=vehicle/vehicleId=buildId/partId=body/team) + 正式碰撞规则（cat A=0x4/B=0x8、group A=-1/B=-2、mask 不含自身、friction 0.5/restitution 0.05）+ mirrorCollider（offset.x/angle/polygon 顶点 x 镜像）；返回 hp/maxHp/totalMass/COM；wheels/parts 空。
- 新测试 tests/planckVehicleAssembly.test.ts（2/2）：A(facing=1) mass=50 精确、OwnerTag、COM=(93.277,200.527) 局部(-6.72,+0.53)；B(facing=-1) COM=(-93.277,207.973) 局部(+6.72,+7.97)——x 镜像对称 ✓、y 因 wedge 非上下对称而改变（正确物理，非 bug）。
- 关键坑（已记）：① facing 是第 5 参数，不能塞进 initialPos 对象；② wedge 重心偏后（尾部面积大）非前鼻方向，COM.x 方向断言须以实测为准；③ 镜像形状 y 质心会变（wedge 非上下对称），断言不能假设 y 不变。
- scoped tsc（6 文件含 buildSnapshot/types）0 错误；Planck 文件无 adapter/matter import（仅注释约束）。
- HEAD=b90ec16，未执行 Git 写入，测试保留未跟踪。

## Queue F-02M-B8B｜Planck Vehicle Wheels 装配（未提交）

- planckVehicleAssembly.ts：PlanckWheelRuntime 扩展（id/def/hardpoint/body/joint/grounded）；createPlanckVehicle 装配 wheels——镜像硬点 X（facing*hp.x，Y 不变）→ wheelPos=initialPos+hpWorld；createDynamicCircle(真实半径/质量/{friction:grip, restitution:0.05, 同车 filter})；OwnerTag partId=`wheel:<hardpointId>`；chassis 本地硬点与 wheel 原点精确重合后 createRevoluteJoint(body, hpWorld, wheel, {0,0})；grounded 初始 false；totalMass=baseMass+Σwheel.mass；质量加权真实 COM（getCenterOfMass×各质量）。未启用 motor/drive。
- 测试追加（4/4 含 B8A 回归）：A facing=1 wheels rear(-55,327)/front(55,327) vs B facing=-1 rear(55,327)/front(-55,327)（镜像对调 ✓）；初始 anchor err <1e-6；OwnerTag wheel:rear/wheel:front team 独立；总质量 70（50+10+10）精确；com=(0,307.71)。
- 重力/地面 600 步（贴地 spawn wheel y=640）：maxAnchorErr=0.01431px（<2 ✓）、同车 selfContacts=0（负 group 无自碰撞 ✓）、chassis 稳定 612.58、无逐步纠偏。
- scoped tsc（6 文件）0 错误。HEAD=b90ec16，未执行 Git 写入，测试保留未跟踪。

## Queue F-02M-B8C｜Planck Fixed Functional Parts 装配（未提交）

- planckVehicleAssembly.ts：PlanckPartRuntime 扩展（id/def/hardpoint/body/joint）；createPlanckVehicle 装配 parts——collider facing=-1 完整 mirrorCollider；part body 原点=镜像后硬点世界位置（hpWorld=facing*hp.x）；单 collider createDynamicCompound（collider offset/angle 由 compound 保留 → shape 中心=硬点+offset，与 Matter 语义等价）；Hardpoint localRotation 真实生效（setAngle(facing*rotation)，Weld 锁参考角）；OwnerTag partId=`part:<hardpointId>`；Weld(body, hpWorld, partBody, {0,0})——part body 原点即硬点，创建瞬间锚点精确重合；totalMass/COM 加入 parts。
- 测试追加（6/6 含 B8A/B 回归）：Ram（box 20×30 offset(10,0)）A 车 pos=(75,300)/COM=(85,300) vs B 车 pos=(-75,300)/COM=(-85,300)（offset 镜像 ✓）；OwnerTag part:front；总质量 100；Weld 初始锚点 <1e-6、重力 300 步后 maxErr=0.094px（<1 ✓）；非零 localRotation 0.3：A angle=+0.3 / B=-0.3（符号镜像 ✓）、300 步后 Weld 0.00024px 相对角保持 0.3（✓）。
- 装配语义要点：part body 原点=硬点、collider offset 由 compound 保留（非 partPos 加 offset）、Weld 锚点=part 本地原点——三者配合锚点才精确重合（若 partPos 加 offset 会双重偏移或锚点错位）。
- 补上了 B6 测试未覆盖的「带 offset 挂点 + 非零 localRotation」缺口。
- scoped tsc（6 文件）0 错误。HEAD=b90ec16，未执行 Git 写入，测试保留未跟踪。

## Queue F-02M-B8T｜Planck Vehicle Assembly 定向回归（只读验收）

- vitest 5 文件 21/21 通过：planckVehicleAssembly 6 + planckColliderBody 7 + planckWeldJoint 3 + planckVehicleRevolute 3 + planckContactRouterBridge 2（B8 装配与 B5~B7 全链路无回归）。
- scoped strict tsc（units+planckWorld+planckVehicleAssembly+测试 4 文件，--ignoreConfig）0 错误。
- 边界：planckVehicleAssembly.ts 无实际 adapter/matter import（仅注释约束）；git diff --check OK；无 NaN（B8 数据全有限）；无逐步纠偏（纯 stepFixed）。
- 非 .workbuddy 改动仅 src/battle/planckVehicleAssembly.ts（新增）+ tests/planckVehicleAssembly.test.ts（新增）。HEAD=b90ec16，未执行 Git 写入。

## Queue F-02M-B8S｜Planck Vehicle Assembly 提交（commit 0873fb9）

- 提交（parent=b90ec16，diff 严格 2 文件）：src/battle/planckVehicleAssembly.ts（B8A chassis + B8B wheels + B8C parts 完整装配）+ tests/planckVehicleAssembly.test.ts（6/6，B8T 21/21 已审）。
- cached 清单严格 2 文件 + check 通过；commit-tree parent 严格匹配；`--force-with-lease=...:b90ec16` 推送成功（b90ec16..0873fb9）；loose ref 同步；未重跑测试。
- 本地=远程=0873fb9。分支历史：0873fb9→b90ec16→d4be059→c8c907b→7156248→b5c1281→19d58b0→cb8ec49→7814c03→407e9bf→6ec0a39→1f7cd97→ff609fe→3c286b2→f764ce1→08434ca→4be95e6。
- 未提交：.workbuddy/memory、3 个 Matter 诊断测试。
- 里程碑：Planck 车辆装配（Body compound + Revolute wheels + Weld parts，含镜像/offset/localRotation/质量加权 COM）已进入主线。下一步：Planck BattleOrchestrator 或驱动。

## GitHub Pages 部署（2026-08-18，仓库 HubinAI/fruits）

- **体验地址：https://hubinai.github.io/fruits/**（HTTP 200，标题「最强水果 — Physics Lab」，已生效）。
- 流程：`vite build --base=./`（相对路径适配子路径，未改 vite.config.ts）→ worktree 建 gh-pages 分支（git worktree add -b gh-pages C:/tmp/fruits-ghpages）→ 清空+copy dist → commit → `git push origin <sha>:refs/heads/gh-pages`（直接 SHA refspec 规避本地 ref 环境问题）→ worktree remove + 本地 branch -D 清理。
- **环境坑（重要）**：① vite build 的 prepare-out-dir 清空 dist 会被 WorkBuddy safe-delete shim 拦截（trash 失败）——先手动 `rm -rf dist` 再 build；② **git 的 /tmp 与 bash 的 /tmp 映射不同**（git=/C:/tmp，bash=别处）——用 git worktree list 查实际路径，或全程用 C:/ 绝对路径；③ 误在错误 cwd 执行 `git rm -rf .` + `cp dist/* .` 会污染主仓库（本次仅 index.html 被覆盖，git checkout -- index.html + rm -rf assets 已恢复，src 零损伤）；④ dist/ 被 .gitignore 忽略，build 产物不进 git。
- 主仓库当前状态：HEAD=0873fb9 未变，仅 .workbuddy 记忆 + 3 个 Matter 诊断测试未跟踪，与部署前完全一致。

## Queue F-02M-B8R1｜镜像 COM 测试修正（失败停止 + 关键发现）

- **重要发现：planck@1.4.2 的 PolygonShape.computeMass 质心与标准数学质心有 y 偏差**。同一 wedge 顶点集：标准 shoelace 质心 y=+3.143，planck 报 y=+0.527（偏差 ~2.6px）；镜像形状 shoelace y=+3.143（数学上镜像严格保持 y），planck 报 y=+7.973。x 方向偏差对称（镜像后 x 正确取反），y 方向每 shape 偏差不同。
- 证据链：hull 重排后内部顶点（读 m_vertices）跑标准 shoelace 仍是 3.143 → 偏差在 planck computeMass 实现（s=平均顶点 fan 三角化 + 凸包重排的顶点顺序）。
- B8R1 按任务改断言 localB.x≈-localA.x / localB.y≈localA.y：**localB.x 通过（+6.723），localB.y 失败（7.973 vs 0.527）**——失败根源是 planck 质心偏差而非镜像逻辑。按任务规则停止，未改生产代码、未提交。
- 测试文件改动保留（planckVehicleAssembly.test.ts 的 B8A 测试 2 已改为 localA/localB 版本）。
- 影响提示：getCenterOfMass 返回的是 planck 质心（含偏差）；若后续需要精确几何质心（如 COM 物理验证），须在测试侧用 shoelace 或接受 planck 偏差。生产代码 planckWorld.ts 未动。

## Queue F-02M-B8R2｜planck 1.4.3 质心修复复验（完成，未提交）

- **planck 1.4.2 → 1.4.3（npm install --save-exact）**：npm 收尾超 3 分钟被终止，但 node_modules 已实际升级（与 A1 同模式）；package.json 已写入 1.4.3，package-lock 用 `npm install --package-lock-only --offline`（19s）同步。
- **lock diff 严格仅 planck**：version/resolved/integrity 1.4.2→1.4.3 + planck 条目内 engines node>=14.0→>=24.0（1.4.3 自身声明）；stage-js/matter-js/根包版本零变更，无无关 churn。
- **质心修复确认（B8R1 失败根因消除）**：1.4.3 下 PolygonShape.computeMass 质心与标准数学质心一致——A/B 车 com.y 均 =203.143（局部 +3.143，= shoelace 数学值），localA=(-6.723,+3.143)、localB=(+6.723,+3.143)，**localB.y≈localA.y 差 0**（<1e-3 ✓）、localB.x≈-localA.x 差 0 ✓。
- 6/6 全通过（含 B8R1 正确断言 localA/localB 版）；无 NaN；未跑全量/build/tsc；未执行 Git 写入。
- 改动范围：package.json、package-lock.json、tests/planckVehicleAssembly.test.ts（+ .workbuddy 记忆）。HEAD=0873fb9 未变。

## Queue F-02M-B8R3｜Node 24 兼容性只读核验（阻塞判定）

- node=v22.22.2（PATH 默认，主版本 22）、npm=10.9.7、engine-strict=false、根包无 engines 声明。
- planck 1.4.3 engines=node>=24.0（**当前 22 < 24 → 按规则标记「阻塞，禁止提交 planck 1.4.3」**）；vite 8.2.1 engines=^20.19.0||>=22.12.0（22.22.2 满足）；typescript 7.0.2 engines=>=16.20.0（满足）。
- 补充事实：系统另有 node 24.18.0（C:/Program Files\nodejs\node.exe，显式路径可用）；engine-strict=false 不强制 engines，故 B8R2 在 22.22.2 下测试仍 6/6 通过——但按任务判定规则仍标记阻塞。
- git status 执行前后完全一致（未改任何文件、未 Git 写入）。

## Queue F-02M-B8R4｜Node 24 项目基线确立（完成，未提交）

- package.json 新增 engines {"node": ">=24.0.0"}；planck 保持精确 1.4.3。
- **临时 PATH 前置 C:/Program Files\nodejs（Node 24.18.0，npm 11.16.0），仅当前队列进程，未改系统环境/全局 npm 配置**。
- Node 24 执行 lock-only offline（19s，无 EBADENGINE 警告）：lock 根包新增 engines.node=>=24.0.0 + planck 1.4.3（version/resolved/integrity）+ planck 条目 engines >=24.0；无任何其他依赖变化/无关 churn。
- Node 24.18.0 单 worker 测试 **6/6 通过**：localA=(-6.723,+3.143)、localB=(+6.723,+3.143)，X/Y 差值均 0（<1e-3 ✓）。
- git diff 严格仅 3 文件（package.json/package-lock.json/planckVehicleAssembly.test.ts）；未暂存/提交/推送/写 ref；HEAD=0873fb9 未变。
- 基线结论：项目 Node 引擎基线 = 24（planck 1.4.3 满足）；此前 B8R3 的「阻塞」判定已解除。

## Queue F-02M-B8RS｜Planck 质心修复 + Node 24 基线提交（commit 51dbbfa）

- 提交（parent=0873fb9，diff 严格 3 文件）：package.json（planck 1.4.3 + engines node>=24.0.0）+ package-lock.json（planck 1.4.3 条目 + 根包 engines）+ tests/planckVehicleAssembly.test.ts（B8R1 localA/localB 镜像断言，B8R4 已 6/6 审）。
- cached 清单严格 3 文件 + check 通过；commit-tree parent 严格匹配；`--force-with-lease=...:0873fb9` 推送成功（0873fb9..51dbbfa）；loose ref 同步；未重跑测试、未创建第二个提交。
- 本地=远程=51dbbfa。分支历史：51dbbfa→0873fb9→b90ec16→d4be059→c8c907b→7156248→b5c1281→19d58b0→cb8ec49→7814c03→407e9bf→6ec0a39→1f7cd97→ff609fe→3c286b2→f764ce1→08434ca→4be95e6。
- 未提交：.workbuddy/memory、3 个 Matter 诊断测试。
- 里程碑：**Node 24 项目基线 + planck 1.4.3（质心修复）正式进入主线**——镜像 COM 断言（localB.y≈localA.y）在 1.4.3 下精确成立。

## Queue F-02M-B9A｜引擎中立战斗车辆契约（未提交）

- 新增 src/battle/combatVehicle.ts：CombatWheelState（id/可写 grounded）、CombatPartState（id/def: FunctionalPartDef）、CombatVehicleState（id/team/可写 hp/maxHp/只读 wheels/parts 数组）；仅 type-import TeamId/FunctionalPartDef；零物理引擎依赖（无 Matter/Planck/adapter/Body/Joint）。
- 新增 tests/combatVehicleContract.test.ts（2/2，Node 24.18.0 单 worker）：类型级赋值证明 Matter Vehicle 与 PlanckVehicle 均兼容 CombatVehicleState（无 as any/as unknown）；运行时 hp/wheel.grounded 可写、maxHp=1000、parts def 可读（ramHead/category weapon）。
- scoped strict tsc（--ignoreConfig，combatVehicle + 测试，依赖链自动包含）0 错误。
- HEAD=51dbbfa，未执行 Git 写入，两文件保留未跟踪。

## Queue F-02M-B9B｜DamageResolver 接入引擎中立契约（未提交）

- damageResolver.ts：删除 `import type { Vehicle } from './vehicleAssembly'`，改 type-import CombatVehicleState；applyDamage target 类型 Vehicle→CombatVehicleState。扣血公式/事件字段/timestamp/事件总线/computeHpAfter 零改动。
- 测试扩展（4/4，Node 24.18.0 单 worker）：同一 DamageResolver 分别对 Matter Vehicle 与 PlanckVehicle 扣血——HP 均 1000→920；事件 hpBefore/hpAfter/timestamp（1234/5678）与请求一致；事件总线收到 [1234,5678]；过量伤害 5000 两者均截断为 0（hpBefore=1000/hpAfter=0）。无 as any/as unknown。
- scoped strict tsc（combatVehicle+damageResolver+测试）0 错误。
- 改动仅 src/battle/damageResolver.ts（M）+ tests/combatVehicleContract.test.ts（M）。HEAD=51dbbfa，未 Git 写入。

## Queue F-02M-B9C｜ContactRouter 接入引擎中立契约（未提交）

- contactRouter.ts：删除 vehicleAssembly 的 Vehicle/WheelRuntime/PartRuntime type-import，改用 CombatVehicleState/CombatWheelState/CombatPartState；constructor vehicles、findVehicleByTeam/findWheel/findPart、processBatch attacker/defender、applyImpact、handleWeaponContact 全部换契约类型；无 Matter Vehicle 运行时类型残留（grep 验证空）；接触入口/OwnerTag/batch 去重/Grounded/阈值/伤害公式/Debug/物理参数零改动。
- planckContactRouterBridge.test.ts 重构：删除 BattleOrchestrator/Vehicle/Matter 状态；makePlanckState() 用独立 PlanckWorld + createPlanckVehicle(resolveSnapshot(lightSnapshot)) 创建真实 A/B PlanckVehicle；**状态世界（只装配不推进）与碰撞世界（只制造事件）分离**。验证：同批 ram size=2 只扣一次 80（b.hp 1000→920）、Impact 999 隔离 lastImpact null、wheel-ground begin/end 翻转 grounded、Debug lastContact/lastDamage 完整。无 as any/as unknown。
- 三文件测试 10/10 通过（combatVehicleContract 4 + contactBatchDamage 4[Matter 旧入口回归] + planckContactRouterBridge 2）；scoped strict tsc（6 文件含全部依赖链）0 错误。
- 改动仅 src/battle/contactRouter.ts（M）+ tests/planckContactRouterBridge.test.ts（M）。HEAD=51dbbfa，未 Git 写入。

## Queue F-02M-B9S｜引擎中立战斗车辆契约提交（commit 6c1acfb）

- 提交（parent=51dbbfa，diff 严格 5 文件）：combatVehicle.ts（契约，B9A）+ damageResolver.ts（B9B）+ contactRouter.ts（B9C）+ combatVehicleContract.test.ts（4/4）+ planckContactRouterBridge.test.ts（B9C 重构 2/2）。
- cached 清单严格 5 文件 + check 通过；commit-tree parent 严格匹配；`--force-with-lease=...:51dbbfa` 推送成功（51dbbfa..6c1acfb）；loose ref 同步；未重跑测试、未创建第二个提交。
- 本地=远程=6c1acfb。分支历史：6c1acfb→51dbbfa→0873fb9→b90ec16→d4be059→c8c907b→7156248→b5c1281→19d58b0→cb8ec49→7814c03→407e9bf→6ec0a39→1f7cd97→ff609fe→3c286b2→f764ce1→08434ca→4be95e6。
- 未提交：.workbuddy/memory、3 个 Matter 诊断测试。
- 里程碑：**DamageResolver + ContactRouter 已引擎中立化**（CombatVehicleState 契约），Matter/Planck 车辆共用同一战斗结算路径，正式进入主线。

## Queue F-02M-B10D1｜Planck 双向驱动动力标定（诊断通过，测试保留未提交）

- 候选参数固定推导（wheelStd r20/m10/driveTorque100/maxRPM300）：speed=min(1.5/20, 300×2π/60/60)=0.075 rad/step、RPM 上限=0.5236、torque=100×0.5×10×(0.2)²=20 N·m。未扫描参数。
- 场景：boxBody+wheelStd×2 真实 createPlanckVehicle；A(facing=+1) speed +0.075、B(facing=-1) speed -0.075；独立 PlanckWorld；重力 {0,10} 地面顶 660；chassis y=613（wheel 最低点=660 贴地）；disabled 停稳 180 步 → enabled 600 步。
- 实测（1/1 通过）：A dist=+868.05px、vx=+1.4812、ω=+0.07500、slip=0.0188；B dist=-868.05px、vx=-1.4812（**A/B 完美镜像，|vx| 差=0.0000**）；maxAngle=0.00209/final=0.00061；maxAnchorErr=0.02345；maxPen=0；maxSpeed=1.5211；NaN=false。
- 结论：候选动力参数达标——vx 1.4812≈目标 1.5（98.7%），近纯滚动（slip 0.0188），双向镜像对称，姿态/锚点/穿透全绿。
- scoped strict tsc（--ignoreConfig）0 错误。HEAD=6c1acfb，未 Git 写入，测试保留未跟踪。

## Queue F-02M-B10A1｜轮驱动力单位契约（未提交）

- units.ts 新增 4 导出：rpmToRadPerStep(rpm×2π/60/HZ)、radPerStepToRpm（精确逆）、solidDiskInertiaKgM2(0.5·m·pxToM(r)²)、angularAccelerationToTorqueNm(τ=I·α)；正负号/0 原样透传，无 clamp/abs/经验倍率。契约头更新：仅「圆盘惯量+τ=Iα」扭矩路径已由 B10D1 验证，通用 force/impulse 换算仍禁止猜测。
- 测试追加 4 项（14/14，Node 24 单 worker）：300RPM=0.5235987756 rad/step；RPM↔rad/step 正/负/0 往返 <1e-12；m=10/r=20px 惯量=0.2 kg·m²；α=100/I=0.2→20 N·m。
- scoped strict tsc（units+测试）0 错误。改动仅 src/physics/units.ts + tests/physicsUnits.test.ts；B10D1 未动。HEAD=6c1acfb，未 Git 写入。

## Queue F-02M-B10A2｜正式 Planck Wheel Movement（未提交）

- 新增 src/battle/planckMovement.ts：PlanckDriveCommand（enabled/worldDirection/targetSpeedPxPerStep）+ drivePlanckVehicle——每 wheel：targetAngularSpeed=target/radius、rpmLimit=rpmToRadPerStep(maxRPM)、speed=worldDirection×min(…)、inertia=solidDiskInertiaKgM2(mass,radius)、maxTorque=angularAccelerationToTorqueNm(driveTorque,inertia)、motor.enabled=command.enabled&&grounded；targetSpeed 非有限或<0 抛错；disabled 只关 motor 不刹停/不归零；禁 force/impulse/setVelocity/setPosition/姿态锁/经验倍率/grip 二次乘扭矩；无 Matter/adapter import、无 Planck native、无 as any。
- 新增 tests/planckMovement.test.ts（3/3，真实 createPlanckVehicle）：①grounded=false 位移 0.0142px / enabled=false 0.0142px（不驱动 ✓）；②target=1.5 grounded=true A(+1)/B(-1) 600 步——A dist=867.90 vx=+1.4812 ω=+0.07500 slip=0.0188、B dist=-867.90 vx=-1.4812（|vx|差=0.0000 完美镜像）、angle 0.00261/0.00054、anchor 0.01282、pen 0.1158、maxSpeed 1.5147、无 NaN；③ON→OFF 关闭瞬间 |Δvx|=0.0001（<0.1）、OFF 后 300 步位移 445.86px 同向滑行、最终 vx=1.4813 不制动不反向。
- 推导复验：ω=0.075 rad/step 精确达成（1.5/20 < rpmLimit 0.5236）、torque=20 N·m（driveTorque 100 × 惯量 0.2）。
- 三文件 19/19（physicsUnits 14 + planckRevoluteMotor 2 + planckMovement 3）；scoped strict tsc 0 错误。
- 改动：units.ts/physicsUnits.test.ts（B10A1，未提交）+ planckMovement.ts/planckMovement.test.ts（本队列）；B10D1 未动。HEAD=6c1acfb，未 Git 写入。

## Queue F-02M-B10S｜Planck 正式轮驱动提交（commit 1cb0864）

- 提交（parent=6c1acfb，diff 严格 4 文件）：units.ts（B10A1 轮驱动换算）+ physicsUnits.test.ts（14/14）+ planckMovement.ts（drivePlanckVehicle）+ planckMovement.test.ts（3/3，B10A2 19/19 已审）。
- cached 清单严格 4 文件 + check 通过；commit-tree parent 严格匹配；`--force-with-lease=...:6c1acfb` 推送成功（6c1acfb..1cb0864）；loose ref 同步；未重跑测试/tsc、未创建第二个提交。
- 本地=远程=1cb0864。分支历史：1cb0864→6c1acfb→51dbbfa→0873fb9→b90ec16→d4be059→c8c907b→7156248→b5c1281→19d58b0→cb8ec49→7814c03→407e9bf→6ec0a39→1f7cd97→ff609fe→3c286b2→f764ce1→08434ca→4be95e6。
- 未提交：.workbuddy/memory、B10D1 诊断 + 3 个 Matter 诊断。
- 里程碑：**Planck 正式轮驱动进入主线**——wheelStd 1.5px/step 目标下驱动稳定（vx 1.4812、双向镜像、无制动语义），动力标定参数（speed 0.075 / torque 20 N·m）落地为正式代码。

## Queue F-02M-B11A｜引擎中立 Arena 阶段时钟（未提交）

- 新增 src/battle/arenaPhase.ts：ArenaPhaseDurations（activeMs/warningMs/closingMs，默认 10000/3000/5000 与 ArenaRuntime 一致）、ArenaPhaseUpdate（previous/current/changed）、ArenaPhaseClock——初始 Active、只读 phase/elapsedMs；update(dtMs) 严格复现 ArenaRuntime 语义（Active→Warning→Closing→End、>= 判定、单次最多跨一阶段、转换后 elapsed 清零不继承溢出、End 后不变）；setPhase 只设阶段+清零无物理副作用。仅依赖 BattlePhase 类型，无 Matter/Planck/adapter/ArenaRuntime import。
- 新增 tests/arenaPhase.test.ts（6/6，Node 24 单 worker）：初始态/默认时长；边界 >= 判定（9999 不转、+1 转且清零）；完整顺序；单次不跨多阶段（100000ms 一次只到 Warning）；End 稳定；setPhase 清零（Closing→End→Active 往返）。
- scoped strict tsc（types+arenaPhase+测试）0 错误。未改 arenaRuntime.ts，未接入引擎。HEAD=1cb0864，未 Git 写入，两文件保留未跟踪。

## Queue F-02M-B11B｜Matter Arena 接入共享阶段时钟（未提交）

- arenaRuntime.ts：改用 ArenaPhaseClock——删除 _phase/phaseElapsed 两套重复状态；phase getter 与 setPhase() 委托时钟；构造用 config.phases 时长初始化时钟；update(dtMs) 用 ArenaPhaseUpdate 驱动副作用：Warning→Closing 才激活墙（setStatic false、当步不推进）；previous==='Closing' 才设左右相反速度（含 Closing→End 当步最后一次推进）；End 后开头 return 不再更新；setPhase 只切阶段清零、不激活墙。ArenaConfig/默认时长/墙体/速度/类别/Projectile Bounds 零改动。
- 新增 tests/arenaRuntimePhase.test.ts（4/4，Node 24 单 worker）：阶段顺序与边界（9999/1、2999/1、4999/1）；进入 Closing 墙 static→dynamic、当步速度 0、下一步 left +40/right -40；Closing→End 当步最后一次推进 + End 后不再变；setPhase('Closing') 不激活墙、计时清零重计 5000ms 才 End、可回 Active。
- 10/10 通过（arenaPhase 6 + arenaRuntimePhase 4，B11A 回归 ✓）；scoped strict tsc 0 错误。
- 改动：arenaRuntime.ts（M）+ arenaRuntimePhase.test.ts（新增）；B11A 两文件保留。HEAD=1cb0864，未 Git 写入。

## Queue F-02M-B11S｜共享 Arena 阶段时钟提交（commit d03dd5c）

- 提交（parent=1cb0864，diff 严格 4 文件）：src/battle/arenaPhase.ts（B11A 引擎中立时钟）+ src/battle/arenaRuntime.ts（B11B 接入）+ tests/arenaPhase.test.ts（6/6）+ tests/arenaRuntimePhase.test.ts（4/4）。
- cached 清单严格 4 文件 + check 通过；commit-tree parent 严格匹配；`--force-with-lease=...:1cb0864` 推送成功（1cb0864..d03dd5c）；loose ref 同步；未重跑测试、未创建第二个提交。
- 本地=远程=d03dd5c。分支历史：d03dd5c→1cb0864→6c1acfb→51dbbfa→0873fb9→b90ec16→d4be059→c8c907b→7156248→b5c1281→19d58b0→cb8ec49→7814c03→407e9bf→6ec0a39→1f7cd97→ff609fe→3c286b2→f764ce1→08434ca→4be95e6。
- 未提交：.workbuddy/memory、B10D1 诊断 + 3 个 Matter 诊断。
- 里程碑：**Arena 阶段时钟已引擎中立化**（ArenaPhaseClock 单一事实源），Matter ArenaRuntime 委托共享时钟驱动阶段副作用，为后续 Planck Arena 复用同一时钟铺路。

## Queue F-02M-B12A｜Planck 静态/运动学矩形原语（完成，未提交）

- planckWorld.ts：新增 `createStaticBox` / `createKinematicBox`（xPx,yPx,widthPx,heightPx, options?: PlanckBodyOptions）——真实 Planck static/kinematic body；共用 px↔m 换算、assertFinite/assertPositive/assertBodyOptions 校验、材质与 collisionFilter；density 传 0（static/kinematic 不参与质量计算）；私有 createBody 加第 8 参 bodyType（默认 'dynamic'，现有调用零影响）；`createStaticGround` 改为委托 createStaticBox（{friction:1, collisionFilter}，保持原 friction=1/过滤/既有行为）；无 as any/native 暴露/额外 Arena 代码。
- 测试追加 tests/planckWorldCore.test.ts（10/10，含原有 7 项回归）：static 有重力 120 步 p0=p1=(100,600) 位置不变、速度恒 0；kinematic 0.5px/step 120 步 **dx=60.000000、dy=0.000000、vx=0.500000**（精确达成，y 零漂移）；12 类非法输入（NaN/负/零尺寸、friction<0、restitution 越界、categoryBits 非法）全抛错。
- **【重要环境坑】vitest 默认池（forks/threads）在此环境全量报 `reading 'config'`（0 test，最小复现）**：@vitest/runner 的 runner 全局 undefined → describe→initSuite→validateTags(runner.config) 抛错，属 vitest 4.1.10 双实例/初始化类故障，与项目代码无关。**规避：`--pool=vmThreads`（或 vmForks）恢复正常**，单 worker 语义不变。已记入 MEMORY.md，后续 vitest 命令必须带 `--pool=vmThreads`。
- scoped strict tsc（--ignoreConfig，planckWorld+units+types+测试）0 错误。HEAD=d03dd5c，未 Git 写入，2 文件改动保留。

## Queue F-02M-B12B｜ArenaConfig 引擎中立化（完成，未提交）

- 新增 src/battle/arenaConfig.ts：ArenaConfig 接口 + DEFAULT_ARENA_CONFIG（原 DEFAULT_ARENA 改名导出）——纯引擎中立（零 import，连类型依赖都没有）；closingSpeed 注释从「px/s」修正为「px/step」（仅文档契约，字段名/数值 40 不变）；默认值全部不变：1600×900、groundY 700、wallThickness 60、阶段 10000/3000/5000ms、closingSpeed 40、projectileTopY -50。
- arenaRuntime.ts：删除本地 ArenaConfig/DEFAULT_ARENA（-34/+4）；`import { DEFAULT_ARENA_CONFIG, type ArenaConfig } from './arenaConfig'` + `export type { ArenaConfig } from './arenaConfig'`（保持 battleOrchestrator 等既有调用方兼容）；构造器 `{ ...DEFAULT_ARENA_CONFIG, ...config }` 合并规则不变。未改 ArenaPhaseClock/墙体/碰撞类别/阈值/测试。
- 验证：arenaPhase 6 + arenaRuntimePhase 4 = **10/10 通过**（--pool=vmThreads --maxWorkers=1）；scoped strict tsc（--ignoreConfig，6 文件含依赖链）0 错误。
- **CLI 备注**：vitest 4 CLI 无 `--minWorkers` 参数（Unknown option）；minWorkers 属配置池选项但本队列禁改配置文件 → 用 `--maxWorkers=1` 单 worker 语义覆盖，行为等价。
- HEAD=d03dd5c，未 Git 写入。工作区累计：arenaConfig.ts（新增）+ arenaRuntime.ts（M）+ B12A 两文件（M），未提交。

## Queue F-02M-B12S｜Planck Arena 前置基础提交（commit 1b5c97f）

- 提交（parent=d03dd5c，diff 严格 4 文件）：src/physics/planckWorld.ts + tests/planckWorldCore.test.ts（B12A 静态/运动学矩形，10/10 已审）+ src/battle/arenaConfig.ts + src/battle/arenaRuntime.ts（B12B ArenaConfig 引擎中立化，10/10 已审）。
- cached 清单严格 4 文件 + check 通过；commit-tree parent 严格匹配；`--force-with-lease=...:d03dd5c` 推送成功（d03dd5c..1b5c97f）；loose ref 同步；未重跑测试、未创建第二个提交。
- 本地=远程=1b5c97f。分支历史：1b5c97f→d03dd5c→1cb0864→6c1acfb→51dbbfa→0873fb9→b90ec16→d4be059→c8c907b→7156248→b5c1281→19d58b0→cb8ec49→7814c03→407e9bf→6ec0a39→1f7cd97→ff609fe→3c286b2→f764ce1→08434ca→4be95e6。
- 未提交：.workbuddy/memory、B10D1 诊断 + 3 个 Matter 诊断。
- 里程碑：**Planck Arena 前置基础进入主线**——静态/运动学原语（static/kinematic box）+ 引擎中立 ArenaConfig（DEFAULT_ARENA_CONFIG），为 Planck Arena 装配与运行时铺路。

## Queue F-02M-B13A｜PlanckArenaRuntime 最小实现（完成，未提交）

- 新增 src/battle/planckArenaRuntime.ts：PlanckArenaRuntime（config/world/phaseClock 私有；ground=createStaticBox(cx, groundY+h/2, w+2t, h, friction 1, GROUND mask VEHICLE_A|B|PROJECTILE, OwnerTag ground)；左右墙=createStaticBox(-t/2, groundY, t, h, friction 0.2/restitution 0.05, ARENA mask 同上, OwnerTag arena)；Closing Wall×2=createKinematicBox(∓2t, groundY-h/4, t, h/2, friction 0.2/restitution 0, HAZARD mask VEHICLE_A|B, OwnerTag hazard, 初始速度 0)）。update 语义与 Matter 完全一致（Warning→Closing 当步 return 不驱动；previous==='Closing' 才设 ±closingSpeed；Closing→End 当步保留；End 后开头 return）；setPhase 只切阶段；isOutOfProjectileBounds 同构。复用 PlanckCategory（planckVehicleAssembly），无 adapter/Matter import，不接车辆/Damage/ContactRouter/Orchestrator。
- **OwnerTag 无 side 字段**（core/types.ts 限制，禁止改其他文件）→ setOwnerTag 只用 {kind:'arena'}/{kind:'hazard'}，左右区分由 runtime 字段（leftWall/rightWall/closingWalls[].side）承担；Matter 侧 setMeta 的 side 属附加字段，Planck 侧不支持。
- 新增 tests/planckArenaRuntime.test.ts（5/5）：①默认几何/OwnerTag/初始速度（ground=(800,1150)、左墙(-30,700)、右墙(1630,700)、closing left(-120,475)/right(1720,475)、速度 0）；②真实重力 VEHICLE_A 动态体落 ground 停稳 **y=678.500（底部 698.5，悬浮 1.5px 为 Planck slop 正常行为）**，30 步收敛 <0.05；③阶段边界 9999/1、2999/1，进入 Closing 当步速度 0，下一步 left +40/right -40，10 步位移 **±400.000000 精确**；④Closing→End 当步保留 ±40、End 后 update 不再改速度；⑤setPhase 只切阶段不驱动、Projectile Bounds（y<-50 越界）。
- 三文件 21/21 通过（planckArenaRuntime 5 + arenaPhase 6 + planckWorldCore 10，--pool=vmThreads --maxWorkers=1）；scoped strict tsc（--ignoreConfig，10 文件）0 错误（修过 TS6133 unused arena）。
- HEAD=1b5c97f，未 Git 写入，2 文件新增保留。

## Queue F-02M-B13S｜PlanckArenaRuntime 提交（commit 0c798b3）

- 提交（parent=1b5c97f，diff 严格 2 文件）：src/battle/planckArenaRuntime.ts + tests/planckArenaRuntime.test.ts（B13A 5/5 已审）。
- cached 清单严格 2 文件 + check 通过；commit-tree parent 严格匹配；`--force-with-lease=...:1b5c97f` 推送成功（1b5c97f..0c798b3）；loose ref 同步；未重跑测试、未创建第二个提交。
- 本地=远程=0c798b3。分支历史：0c798b3→1b5c97f→d03dd5c→1cb0864→6c1acfb→51dbbfa→0873fb9→b90ec16→d4be059→c8c907b→7156248→b5c1281→19d58b0→cb8ec49→7814c03→407e9bf→6ec0a39→1f7cd97→ff609fe→3c286b2→f764ce1→08434ca→4be95e6。
- 未提交：.workbuddy/memory、B10D1 诊断 + 3 个 Matter 诊断。
- 里程碑：**Planck Arena Runtime 进入主线**——Planck 侧 Arena（static ground/墙 + kinematic Closing 刺墙 + 共享阶段时钟）已就绪，与引擎中立 ArenaConfig 配套。

## Queue F-02M-B14A｜引擎中立 Battle 合同与结果解析（完成，未提交）

- 新增 src/battle/battleContract.ts：BattleConfig（impact?: Partial<ImpactConfig>、arena?: Partial<ArenaConfig>、autoDrive、settleToGround、spawnA/spawnB——字段与 battleOrchestrator 完全一致）+ BattleResult（winner: TeamId|'draw'|null、hpA、hpB、phase: string）+ resolveBattleResult(phase, hpA, hpB)——**严格复现 Matter detectEnd()**：非 End 且双 HP>0 → null；phase=End → 按剩余 HP 比较 A/B/draw（原值不归零）；非 End 双死 → draw（双 0）；A 死 → B 胜（hpA=0 原值 hpB）；B 死 → A 胜（原值 hpA、hpB=0）；结果 phase 恒 'End'。纯 type import（BattlePhase/TeamId、ArenaConfig、ImpactConfig），无物理引擎对象。
- 新增 tests/battleContract.test.ts（7/7，--pool=vmThreads --maxWorkers=1）：①Active/Warning/Closing 双活均 null（含 HP=1 边界）；②End 下 A 胜 500>300 / B 胜 100<200 / 平局 400=400（HP 原值保留）；③Active 下 A 死 0→B 胜、负数 -5 同样死亡处理、B 死 900→A 胜、双死 0/0→draw（负数 -10/-10 同）。数值与 Matter detectEnd 逐字段一致。
- scoped strict tsc（--ignoreConfig，battleContract+arenaConfig+contactRouter+types+测试）0 错误。HEAD=0c798b3，未 Git 写入，2 文件新增保留。

## Queue F-02M-B14B｜Matter Orchestrator 接入共享 Battle 合同（完成，未提交）

- battleOrchestrator.ts（-45/+8）：删除本地 BattleConfig/BattleResult 接口；`import { resolveBattleResult, type BattleConfig, type BattleResult } from './battleContract'` + `export type { BattleConfig, BattleResult }`（保持既有导入路径兼容）；detectEnd() 改为 `this._result = resolveBattleResult(this.arena.phase, this.vehicleA.hp, this.vehicleB.hp)`（_result 只接收函数结果，null 语义与原来「非 End 双活保持 null」一致）；清理 unused（ArenaConfig/TeamId/ImpactConfig 随定义删除失去使用点）。固定步/Drive/Arena/ContactRouter/DamageResolver/事件总线/spawn/settle/阈值零改动，无 Planck 代码。
- 验证 16/16（--pool=vmThreads --maxWorkers=1）：battleContract 7 + scenario 4（Scenario A/B/C/E 数值语义不变）+ foundationCanonical 5 全通过；scoped strict tsc（--ignoreConfig，battleOrchestrator+battleContract+3 测试）0 错误。
- HEAD=0c798b3，未 Git 写入。工作区累计：battleOrchestrator.ts（M）+ battleContract.ts/battleContract.test.ts（B14A 未跟踪），未提交。

## Queue F-02M-B14S｜共享 Battle 合同提交（commit eed81db）

- 提交（parent=0c798b3，diff 严格 3 文件）：src/battle/battleContract.ts + src/battle/battleOrchestrator.ts + tests/battleContract.test.ts（B14A 7/7 + B14B 16/16 已审）。
- cached 清单严格 3 文件 + check 通过；commit-tree parent 严格匹配；`--force-with-lease=...:0c798b3` 推送成功（0c798b3..eed81db）；loose ref 同步；未重跑测试、未创建第二个提交。
- 本地=远程=eed81db。分支历史：eed81db→0c798b3→1b5c97f→d03dd5c→1cb0864→6c1acfb→51dbbfa→0873fb9→b90ec16→d4be059→c8c907b→7156248→b5c1281→19d58b0→cb8ec49→7814c03→407e9bf→6ec0a39→1f7cd97→ff609fe→3c286b2→f764ce1→08434ca→4be95e6。
- 未提交：.workbuddy/memory、B10D1 诊断 + 3 个 Matter 诊断。
- 里程碑：**共享 Battle 合同进入主线**——BattleConfig/BattleResult/resolveBattleResult 引擎中立化，Matter Orchestrator 委托同一判定路径，为 Planck Orchestrator 复用铺路。

## Queue F-02M-B15A｜Planck 固定步累加接口（完成，未提交）

- planckWorld.ts（+29）：模块级 `const FIXED_STEP_MS = 1000 / PHYSICS_HZ`（与 Matter FIXED_DT=1000/60 数值一致）；类字段 `private fixedStepAccMs = 0`；新增 `step(realDtMs, timeScale=1, onBeforeStep?): number`——行为严格对齐 PhysWorld.step：`fixedStepAccMs += realDtMs*timeScale`，while >= FIXED_STEP_MS 先 onBeforeStep 再 `world.step(SECONDS_PER_STEP)+flushBatched()`，减固定步、steps++，`if (steps>8) break` 防 spiral-of-death（上限 9 步），返回实际步数、保留余量。stepFixed(steps) 原样保留。
- 新增 tests/planckFixedStep.test.ts（4/4，--pool=vmThreads --maxWorkers=1）：①两次半帧累计一步、回调 1 次且回调内位置=推进前；②30fps=60/60fps=60/jitter=59 与 Matter 逐序列一致（**浮点分帧边界发现：Matter 自身 jitter [34,33,33]×10 也是 59 步，非 60**——数学 1000ms=60 步，但分帧浮点残差累积导致 59，Planck 与 Matter 逐帧行为完全一致）；③timeScale 0→0 步/0.5→10 步/2→20 步；④5000ms 长帧 Matter=9 步（steps>8 break）Planck=9 一致、回调 9 次。
- 回归 planckWorldCore 10/10；scoped strict tsc（--ignoreConfig，planckWorld+units+adapter+2 测试）0 错误。
- HEAD=eed81db，未 Git 写入。工作区：planckWorld.ts（M）+ planckFixedStep.test.ts（新增），未提交。

## Queue F-02M-B15A-R1｜固定步 jitter 边界修正（完成，未提交）

- **问题**：B15A 发现 [34,33,33]×10 = 1000ms 时 Matter/Planck 均只 59 步（浮点向下舍入：33.33333333333333−16.666666666666668=16.666666666666664 < 门槛 → 边界帧少一步）。
- **修正（三文件）**：adapter.ts 新增导出 `FIXED_DT_EPS = FIXED_DT * 1e-9`；planckWorld.ts 新增 `FIXED_STEP_EPS = FIXED_STEP_MS * 1e-9`。两套 step 循环统一改「累加值 + 容差 >= 固定步」判定（`acc + EPS >= FIXED_DT`），扣除后仅把 (−EPS, 0) 的微小负余量归零（`if (acc < 0 && acc >= -EPS) acc = 0`）；禁 Math.round；catch-up `steps > 8` 原样（5000ms 仍 9）；回调顺序/stepFixed/重力/单位/碰撞零改动。
- 测试更新（planckFixedStep.test.ts 测试 2）：jitter 断言 **Matter=60、Planck=60、位置=60.000000**；30fps/60fps 与 Matter 逐序列一致。
- 验证 19/19（--pool=vmThreads --maxWorkers=1）：planckFixedStep 4（jitter p=60/m=60、catch-up 9/9、半帧/timeScale 原样）+ planckWorldCore 10 + foundationCanonical 5 全通过；scoped strict tsc（--ignoreConfig，6 文件）0 错误。
- HEAD=eed81db，未 Git 写入。工作区：adapter.ts（M）+ planckWorld.ts（M）+ planckFixedStep.test.ts（新增），未提交。

## Queue F-02M-B15S｜固定步累加修正提交（commit dbcfd47）

- 提交（parent=eed81db，diff 严格 3 文件）：src/physics/adapter.ts + src/physics/planckWorld.ts + tests/planckFixedStep.test.ts（B15A+B15A-R1 已审，19/19）。
- cached 清单严格 3 文件 + check 通过；commit-tree parent 严格匹配；`--force-with-lease=...:eed81db` 推送成功（eed81db..dbcfd47）；loose ref 同步；未重跑测试、未创建第二个提交。
- 本地=远程=dbcfd47。分支历史：dbcfd47→eed81db→0c798b3→1b5c97f→d03dd5c→1cb0864→6c1acfb→51dbbfa→0873fb9→b90ec16→d4be059→c8c907b→7156248→b5c1281→19d58b0→cb8ec49→7814c03→407e9bf→6ec0a39→1f7cd97→ff609fe→3c286b2→f764ce1→08434ca→4be95e6。
- 未提交：.workbuddy/memory、B10D1 诊断 + 3 个 Matter 诊断。
- 里程碑：**固定步累加容差修正进入主线**——Matter 与 Planck 双引擎统一「累加值+容差>=固定步」语义，jitter 1000ms 精确 60 步，为帧率无关战斗推进定基。

## Queue F-02M-B16A｜Planck Body 位置与边界契约（完成，未提交）

- planckWorld.ts（+53）：新增 `setPosition(body, xPx, yPx)`——游戏层 px 经 units 换算、assertFinite + bodyOf 跨 World 校验、只改位置不动角度/速度、不注入伪速度；新增 `getBounds(body): {minX,minY,maxX,maxY}`——遍历 getFixtureList() 逐个 shape.computeAABB（out 参数 `new planck.AABB()`，用当前 getTransform() 实时计算，不依赖旧 broadphase AABB），px 输出、无 fixture 抛错、无 native 暴露。
- **关键坑（planck polygon skin）**：PolygonShape.computeAABB 含 m_radius（= 2×linearSlop ≈ 0.01m = 1px），box 40×20 @(100,200) 原生返回 79/189/121/211 而非验收的 80/190/120/210 → getBounds 对 `shape instanceof planck.PolygonShape` 用 `shape.getRadius()` 补偿（lower+skin、upper−skin）；circle 无 skin 不补偿。
- 新增 tests/planckBodyTransform.test.ts（8/8，--pool=vmThreads --maxWorkers=1）：box 40×20@(100,200)→**80/190/120/210 精确**；circle r15@(0,0)→-15/-15/15/15；compound（box 40×40 + circle r15 offset(60,0)）合并→-20/-20/75/20；setAngle(π/2) 后旋转 AABB→90/180/110/220（随 transform 更新）；setPosition(300,400) 全边界 +200 平移；setPosition 前后 angle/linearVelocity/angularVelocity 不变；NaN/Infinity 与跨 World 抛错。
- 回归 planckWorldCore 10/10 + planckFixedStep 4/4（合计 22/22）；scoped strict tsc（--ignoreConfig，5 文件）0 错误（修过 ColliderDef.offset 必填）。HEAD=dbcfd47，未 Git 写入，2 文件保留。

## Queue F-02M-B16A-R1｜区分几何边界与碰撞边界（完成，未提交）

- planckWorld.ts：getBounds 语义明确为「几何边界」（polygon/box 排除 collision skin、circle 半径属真实几何不扣）；新增 `getCollisionBounds(body)`——直接合并各 fixture computeAABB 原始结果（含 polygon skin、circle 不虚构），当前 transform、校验 handle、无 native 暴露。共用私有 `computeWorldAABB(body, subtractPolygonSkin)`（true=几何/补偿 skin，false=碰撞/原样）。
- 测试追加（planckBodyTransform.test.ts 13/13，--pool=vmThreads --maxWorkers=1）：box 40×20 **几何 80/190/120/210 vs 碰撞 79/189/121/211**（容差 1e-6）；circle r15 两类完全一致；compound（box 40×40 + circle r15 off(60,0)）**几何 -20/-20/75/20 vs 碰撞 -21/-21/75/21**；setAngle(π/2) 后几何 90/180/110/220、碰撞 89/179/111/221（实时更新）；setPosition(300,400) 两类均 +200 平移；getCollisionBounds 跨 World 抛错。
- 回归 planckWorldCore 10/10 + planckFixedStep 4/4（合计 27/27）；scoped strict tsc（--ignoreConfig，5 文件）0 错误。HEAD=dbcfd47，未 Git 写入，2 文件保留。

## Queue F-02M-B16AS｜Planck Body 变换与边界契约提交（commit 8a705e7）

- 提交（parent=dbcfd47，diff 严格 2 文件）：src/physics/planckWorld.ts + tests/planckBodyTransform.test.ts（B16A 22/22 + B16A-R1 27/27 已审）。
- cached 清单严格 2 文件 + check 通过；commit-tree parent 严格匹配；`--force-with-lease=...:dbcfd47` 推送成功（dbcfd47..8a705e7）；loose ref 同步；未重跑测试、未创建第二个提交。
- 本地=远程=8a705e7。分支历史：8a705e7→dbcfd47→eed81db→0c798b3→1b5c97f→d03dd5c→1cb0864→6c1acfb→51dbbfa→0873fb9→b90ec16→d4be059→c8c907b→7156248→b5c1281→19d58b0→cb8ec49→7814c03→407e9bf→6ec0a39→1f7cd97→ff609fe→3c286b2→f764ce1→08434ca→4be95e6。
- 未提交：.workbuddy/memory、B10D1 诊断 + 3 个 Matter 诊断。
- 里程碑：**Planck Body 变换与边界契约进入主线**——setPosition + getBounds（几何 AABB，排除 polygon skin）/ getCollisionBounds（碰撞 AABB，含 skin），为 Planck 场景摆放与检测铺路。
