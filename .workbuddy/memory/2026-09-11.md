# 2026-09-11

- 生成交接文档 `交接文档_2026-09-11_1bb35d7.md`（R3 交付后停等回执状态交接）。HEAD=远端=RC=1bb35d7，工作树仅 M .workbuddy/memory/MEMORY.md（有意未入库）。

## 会话接续基线复核（新会话，小月）
- repo-health 9/9 PASS（显式调用 `checkRepoHealth`，脚本本身无 CLI 输出）。
- 三路 SHA 一致：HEAD = refs/heads/foundation-02-wechat = `git ls-remote origin` = RC fullSha = `1bb35d781a31d18c915a52bcfde9fab7bc15496c`；RC dirty=false。
- 工作树：M .workbuddy/memory/MEMORY.md（有意未入库，随下条功能 commit 并入）。
- 额外未跟踪文件（非阻塞，均为文档/日志，未 ignore）：`交接文档_2026-09-05_8fbac75.md`、`交接文档_2026-09-11_1bb35d7.md`、`.workbuddy/memory/2026-09-11.md`。
- 状态：停等用户 R3 电脑录屏体验验收回执；未开新 Queue、未部署 Pages、未重跑全量 vitest（交接文档门禁已绿）。

## PBL-F0-PORTRAIT-BATTLE-LAB-FOUNDATION（竖屏战场实验台）
- 先查：HEAD=remote=`1bb35d7`；repo-health 9/9；工作区仅 M MEMORY.md（无 R4 代码改动）→ 判定可开工。
- 架构结论：正式横屏契约 `PLAYER_LOGICAL_W/H=844×390`；`PlayerViewportTransform` 构造器本就支持自定义逻辑尺寸 → 竖屏 `390×844` 直接复用，**零修改共享代码**；既有 Lab 先例 `src/lab/physicsLab.ts`、`src/dev/reviewMode.ts`（REVIEW_PRESETS 仅横屏，未改动）。
- 隔离方式（最小）：新增独立入口 `portrait-lab.html` + `src/lab/portraitBattleLab/`（constants/state/layout/lab/main）+ `vite.portrait-lab.config.ts`（outDir `dist-portrait-lab`）；Lab 自绘占位舞台，不接正式 Renderer/Runtime/Physics；`npm run build` 默认只构建 index.html → 正式产物零污染。
- 正式文件改动面：仅 `.gitignore`(+1) 与 `package.json`(+2 script)，正式源码 0 修改。
- 门禁：targeted 25/25；E2E 35/35（真实浏览器 1280×720@1 + 700×900@1.5，真实点击 + 真实 getImageData，dpr=1 面积精确）；全量 vitest 1766/1766（194 files，+25）；tsc 0；Web/Pages/WeChat 构建 EXIT 0；bundle-clean rc/wechat/e2e PASS；repo-health 9/9。
- 布局教训：首版占位矩形与 Arena 标记重叠 → 像素面积断言不可精确；改为「Arena 外侧 14px 竖柱 + 中央横条」与玩家/敌人区互不重叠后，R25 不重叠不变量 + 精确面积断言成立。

## PBL-F1 序列开工前「先查」→ 触发停止（未改码、未提交）
- 序列 `PBL-F1 → PBL-A1 → PBL-B1 → PBL-G1` 第 1 条即触发用户设定的停止条件「发现设计假设无法成立」→ **0/4 完成**，A1/B1/G1 未开工。
- 基线：branch=`prototype-portrait-battle-lab`，HEAD=ref=remote=`27df945`，repo-health 9/9，工作区仅 3 个未跟踪历史交接文档。
- 阻塞根因（file:line 证据）：
  - PBL-F1 点名 8 件部件，其中 **3 件在正式内容库无对应定义**：`履带`（movements 仅 `wheelStd/smallWheel/largeWheel/heavyWheel`，`src/core/content.ts:390/411/433/456`）、`磁铁`、`护盾`（全仓 `magnet|shield|磁铁|护盾` 0 命中，`OFFICIAL_PARTS` 白名单 11 件亦无，`src/core/partInventory.ts:30`）。
  - `LightSwarm3`（3 个敌人）：正式 Runtime 严格 1v1（`src/battle/battleOrchestrator.ts:70-71` 仅 vehicleA/vehicleB），对手池 49 套均为单对手 BuildDraft（`src/player/opponentPool.ts:192`），不存在多实体同场数据模型。
  - 补齐 磁铁/护盾 需改 `src/core/content.ts`（冻结共享内容库）或新增行为 → 同时命中「需要修改冻结模块」与「自行扩大范围」两条禁止项，故按规则停整条序列。
- 已确认可复用映射（若要继续）：西瓜车身 `watermelonBody`(mass120/hp1100/4挂点) / 香蕉车身 `bananaBody`(mass45/hp900/4挂点) / `cannon` / `hammer` / `thruster` 均存在且可直接引用。
- 状态：等待用户对 履带 / 磁铁 / 护盾 / LightSwarm3 映射口径的裁决；未写任何代码、未 commit。

## PBL-F1-SHARED-LOADOUT-ENCOUNTER（A/B 共用测试数据 + Spawn 流程）
- 开工基线：branch=`prototype-portrait-battle-lab`，HEAD=ref=remote=`27df945`，repo-health 9/9，工作区无代码改动。
- 用户裁决 **①**：Loadout 只引用正式库真实存在的件。落地口径：`履带`→`heavyWheel`（adapted）、`LightSwarm3`→正式模板 `OPP-14` ×3、`磁铁`/`护盾` 正式库无 → unavailable（不引入、不造数值；`testData.ts` 加载时动态断言，将来正式库补上会立刻抛错）。
- 正式映射（零手写数值）：Loadout = 正式 `BuildDraft`（`watermelonBody`+`heavyWheel`×2+`cannon` / `bananaBody`+`wheelStd`×2+`hammer`+`thruster`）；敌人 = 正式 `OPPONENT_TEMPLATES`（追猎者 `OPP-16` / 远程炮台 `OPP-03` / 三轻敌人 `OPP-14`）；全部经 `buildSnapshotFromDraft`→`validateSnapshot`→`resolveSnapshot`。
- 新增模块：`testData.ts`（共享数据 + 槽位解析）、`entities.ts`（SpawnPlan + 实体/弹丸容器 + clear/spawnSerial）、`scene.ts`（由正式 collider 组装分层场景）；改 `layout.ts`（分层绘制 + `paintedAreas` 精确面积账本）、`state.ts`（state 持有 run）、`lab.ts`（HUD 带 + 分层绘制 + 缺口披露）、`constants.ts`（id 契约）。
- 关键不变量：`buildSpawnPlan(loadout, encounter)` 无 arena 入参 → A/B 必然同一套基础数据；`plan.baseKey` 指纹不含 arena/team/序号；Reset / 任一选择项变更 → 实体+弹丸彻底清空（`isRunClean`）。
- 隔离守卫收紧（F0→F1）：由「禁止一切正式 import」改为**单向** —— 反向硬约束（正式 src 全树 + 入口 + 4 构建配置 0 引用 Lab）+ 正向白名单（只读纯模块 7 个）+ 显式禁止 Runtime/DOM/平台/物理模块。
- ⚠️ 实测缺陷修复：HUD「缺口」提示原用 `#ffd35a`（与 Arena 层同色）→ 文字抗锯齿像素被 E2E 分类器误计 → arena 虚增 ~310px 且抖动；修法 = 提示色改 `#ff5ee0`（与所有分层色互斥）+ E2E 统计区排除 HUD 带，并加 F1-R25 守卫（HUD 带内不得出现分层几何）。
- 门禁：targeted 51/51（F0 26 + F1 25）；E2E 64/64（真实点击 + 真实 getImageData；精确账本 初始 A/西瓜/追猎者 = arena 4200 / playerBody 8360 / playerPart 800 / enemyBody 7618 / enemyPart 1560；dpr=1.5 比例校验）；全量 vitest 1792/1792（195 files）；tsc 0；Web/Pages/WeChat 构建 EXIT 0；bundle-clean rc/wechat/e2e PASS；repo-health 9/9；正式产物 0 处 Lab 引用。
- 改动面：全部在 `src/lab/portraitBattleLab/` + 2 个 Lab 测试文件 + memory；**正式源码 0 修改**。
- 交付：commit `6aa8238`（已 push）；targeted 51/51、E2E 64/64、全量 vitest 1792/1792、tsc 0、三端构建 EXIT 0、bundle-clean PASS、repo-health 9/9、三路 SHA 一致。

## PBL-A1 先查 → 再次触发停止（未改码、未提交）
- 基线：branch=`prototype-portrait-battle-lab`，HEAD=ref=origin=`6aa8238`，repo-health 9/9，工作区干净（仅 3 个未跟踪历史交接文档）。
- 探查动作：读 `contactRouter.ts`(918 行)、`planckBattleOrchestrator.ts`(587 行)、`planckVehicleAssembly.ts`、`planckMovement.ts`(70 行)、`planckWorld.ts`(1265 行)、`damageResolver.ts`、`arenaConfig.ts`、`buildSnapshot.ts`；并跑一次性实证脚本（已在判定后删除 `tests/_probe_multi_entity.test.ts`）。
- **阻塞根因 1｜正式战斗层是「每队至多一辆车」，3 敌同场无法复用**：
  - `ContactRouter.findVehicleByTeam(team)`（`src/battle/contactRouter.ts:236-239`，注释明文「一个 battle 内每个 team 至多一辆车」）是 `private`，Impact/Weapon/Projectile/Grounded 四条路径**全部按 team 反查车辆**（:442-443、:471-472、:547、:710、:891、:899）→ LightSwarm3 的 3 个敌人同时在场时，命中/撞击/弹丸一律落到「第一个 B 车」上，**静默错记伤害**。
  - `PlanckBattleOrchestrator` 构造即 `new ContactRouter([this.vehicleA, this.vehicleB], …)`（`planckBattleOrchestrator.ts:301-309`）且只暴露 `vehicleA`/`vehicleB` 两个 `readonly` 字段（:216-218）→ 无法扩成多敌。
  - 修法只有两条：改 `src/battle/contactRouter.ts`（命中「不改正式 Battle Runtime」禁止项），或在 Lab 内自建**按实例分派**的 contact 层（百行级新层，且 Lab 的战斗结算语义不再等于正式 Runtime 原样）。两条都命中用户设定的停止条件「需要修改冻结模块」/「发现设计假设无法成立」。
- **阻塞根因 2｜敌我以外的「敌人↔敌人」物理碰撞被冻结代码双重锁死**：
  - `createPlanckVehicle` 把 `vehicleMask(team)`/`vehicleGroup(team)` 烘进该车**所有** fixture（`planckVehicleAssembly.ts:226-230`、:84-103）：team B 的 `maskBits` 显式**排除 VEHICLE_B**（:92-102），且 `groupIndex = -2`（:88-90）→ B↔B 既不匹配 mask 也被负 group 关闭。
  - `PlanckWorld` **没有**运行期 fixture 过滤器变更 API（全文件仅 :121-132 `filterFixtureDef` 在 fixture 创建时应用；grep `setFilterData|Fixture` 无命中）→ 无法在 Lab 内后置开启敌人互撞。
  - 实证：同队两车置于完全相同坐标，1 秒步进后位移 0px、仍完全重叠（`b1DisplacementPx: 0`，`sameTeamVehiclesStillOverlapping: true`）。
  - 后果：PBL-A1 验收第 3 条「三目标同时存在仍可辨认车辆/弹丸/**碰撞关系**」中，蜂群内部的碰撞关系物理上不存在。
- **阻塞根因 3｜`PlanckBattleOrchestrator` 无法用于 Arena A**：构造硬编码重力 `{x:0,y:10}`（`planckBattleOrchestrator.ts:244-246`，注释「禁止 0 重力假悬浮」）且**无条件**构造 `PlanckArenaRuntime`（:249，含 active/warning/closing + 刺墙 + hazard tick，见 `arenaConfig.ts:17-45`）→ 与 Arena A 要求的「俯视 / 无缩圈 / 无边界伤害」直接冲突，且无 config 开关可关。故 Arena A 必须自建 Lab-local 运行时（4 边静态边界 + 出生位 + step 顺序）。
- **另需注意（非阻塞但属设计外使用）**：`drivePlanckVehicle` 的 motor 开关含 `w.grounded`（`planckMovement.ts:65`），而 `grounded` 只由 `ContactRouter.handleGrounded` 在 wheel↔ground 接触时维护；无重力俯视场里永远为 false → 电机恒不使能。Arena A 若沿用正式 Movement，需 Lab 自供 `grounded`，或**自建 topdown 驱动适配**（PBL-G1 明文允许「Movement/Physics Adapter 因空间模型不同而存在明确差异」）。
- **已确认可复用的中立资产**：`PlanckWorld`（`createDynamicBox`/`createStaticBox`/`createStaticGround`/`createDynamicCircle`/`step`/`stepFixed`/`getBounds`/`applyLinearImpulse`/`setRevoluteMotor`/`setBatchedContactListener`/`getOwnerTag`/`destroyBody`）、`createPlanckVehicle`、`settlePlanckVehicleToRestPose`、`DamageResolver`（仅需 `CombatEventBus`）、`CombatVehicleState` 契约、`getBehaviorFactory` + `behaviorRuntime`、`weaponProjectile`；`src/physics/*` 与 `src/battle/planck*` 全部引擎中立（0 处 platform/DOM/rAF 引用）。
- **可观测量**：Lab 侧已具备按实例区分敌人的条件 —— `entities.ts:248` 给每个敌人不同 snapshot id（`lab-encounter-<enc>-<n>`），故 `OwnerTag.vehicleId` 唯一，`buildSpawnPlan.length === 2` 不变量不受影响。
- 状态：**PBL-A1 / B1 / G1 全部未开工**，等待用户裁决（口径 A：Lab 自建多实体 contact 分派层；口径 B：LightSwarm3 降级；口径 C：授权改冻结模块；口径 D：LightSwarm3 标注「正式 Runtime 不支持」并从 A1/B1 验收中移出）。未写任何 PBL-A1 代码、未 commit、未 push。

## PBL-F2-MULTI-ENTITY-COMBAT-FOUNDATION（受控版 C：共享底层 1 player + N enemies）
- 用户裁决：**采用受控版 C** —— 允许改 Battle/Physics 底层「每队只能 1 辆车」假设，但仅在 `prototype-portrait-battle-lab` 分支；不改正式 1v1 流程、不改 Arena/刺墙/Match/Battle Flow、不合并回 `foundation-02-wechat`。
- 先查（file:line 锁定）：`ContactRouter.findVehicleByTeam`（private，:236-239）是 Impact/Weapon/Projectile/Grounded/hazard 全部路径的反查依据；碰撞过滤器在 `createPlanckVehicle` 里由 `vehicleMask/vehicleGroup(team)` **烘进全车 fixture**（`planckVehicleAssembly.ts:226-230`、:84-103），team B 的 mask 排除 VEHICLE_B 且 group=-2 → B↔B 双重禁止；`PlanckWorld` 无运行期 fixture 过滤器变更 API。
- 改动面（2 个共享文件 + 1 个新测试，**零 Lab / 零 orchestrator / 零 arena / 零 content / 零平衡改动**）：
  - `src/battle/contactRouter.ts`：新增 `instanceKeyOf` + 惰性 `vehicleIndex` + `resolveVehicle(vehicleId, team)` / `resolveMeta(meta)`。规则 ①vehicleId 唯一命中 → 该实例；②否则该 team 恰好 1 辆 → 返回它（= 旧语义，1v1 完全等价）；③否则 **undefined 安全跳过**（绝不静默落到第一个同队车）。全部 7 处反查改实例级；Impact 合并 key / Weapon 合并 key / `registerActiveTick` / `removeActiveTick` / hazard key 全部改实例粒度；`projectileHitMeta` 增 `projVehicleId`/`defVehicleId`/`defKey`，`ProjectileContactFact` 增 `projectileVehicleId`。**DamageEvent source/target 仍是 team 字符串 → 事件契约零变化。**
  - `src/battle/planckVehicleAssembly.ts`：新增 `PlanckVehicleCollisionPolicy`（`team-exclusive` 缺省 / `instance-exclusive`）+ `PlanckVehicleCollisionOptions{policy, instanceIndex}` + 导出 `resolveVehicleCollisionFilter(team, options)`；`createPlanckVehicle` 追加**可选第 6 参**，现有调用点零改动。instance-exclusive：mask 额外含己方车辆类别 + 每实例独享 group `-(100+instanceIndex)`。
  - ⚠️ **关键坑**：实例 group 必须避开正式 projectile 占用的 **-1 / -2**（`weaponProjectile.ts:93` / `cannonBehavior.ts:222` / `laserBehavior.ts:228`），否则会出现「某辆敌车对弹丸免疫」的静默过滤器 bug → 故取 `-(100+i)` 基准并在测试中断言 `!== -1 && !== -2`。
- 先红后绿（真实 Planck 碰撞 + 正式 Router，无 fake router / 无代理碰撞体 / 无排斥力）：
  - 红（改前 4 failed | 2 passed）：`[PBL-F2-PROJ] target=enemy-B1 hp B1=920 B2=1000 B3=1000`（弹丸物理命中 B2，伤害落到 B1）；`[PBL-F2-IMPACT] hp B1=999.6875 B3=1000`（撞 B3 伤 B1）；
  - 绿（改后 7/7）：`target=enemy-B2 B2=920`；`B3=999.6875`；`destroy` 场景 `target=enemy-B3 B3=920`；同队碰撞 `default separation=20`（不变）vs `instance-exclusive separation=77.15`（真实分离）+ 友伤 0。
- 门禁：targeted `tests/planckMultiEntityCombat.test.ts` 7/7；既有 Contact/Impact/Weapon/Projectile 回归 10 文件 45/45（与改前基线完全一致）；全量 vitest **196 files / 1799 passed**（基线 195/1792，+1 文件 +7 测试）；tsc 0；Web/Pages/WeChat/E2E/Portrait-Lab 五路构建 EXIT 0；bundle-clean wechat/rc/e2e/web/lab 五路 PASS；repo-health 9/9；Lab E2E 64/64（共享底层改动对 Lab 零影响）。
- ⚠️ 全量首跑出现 `garageFusionResultInteractionR22.test.ts` 9 处 5s 超时：该文件只 import `CanvasPlayerUIHost`/`makeStarterDraft`/`registry`，**与本次改动无引用路径**；单独跑 11/11 绿、全量重跑 1799/1799 绿 → 判定为 `--pool=vmForks --maxWorkers=1` 全量负载下的超时抖动（vitest 未设 testTimeout，默认 5s），**非回归**。
- 边界（诚实披露）：① Matter 路径 `vehicleAssembly.ts` 的同类 `vehicleGroup(team)` 硬编码**未改**——生产/微信路径走 Planck（`battleRequestAdapter.ts:24`、`wechatBattleHost.ts`），Matter 仅 `main.ts` 旧路径 + `lab/physicsLab.ts` + 13 个测试在用，无多实体消费者，按「最小、明确」原则不动；② `PlanckBattleOrchestrator` 未被改造（仍严格 1v1），多实体装配由 Lab/测试侧直接组合 `PlanckWorld + createPlanckVehicle + ContactRouter`；③ 未做 Arena A/B、未做磁铁/护盾/履带、未做 Roguelike/Day/Build、未改平衡数值。
