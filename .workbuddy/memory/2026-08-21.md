# 2026-08-21 工作日志

## Q12-B-CLOSE｜举升臂退出正式 Build

- 结论：Q12-B 经一次结构返修（R1 已修正向上方向），Revolute Runtime / 动作方向均正常，
  但真人正常速度下对手仅轻微短暂抬头，「机械臂→明显举起对手」战斗作用不成立 → 退出正式 Build。
- 改动（沿用 Q12-A-HOLD 模式）：
  - `src/core/partOptions.ts`：从玩家装配页移除 lifter（PART_OPTIONS 不再含 lifter）。
  - `src/core/content.ts`：保留 lifter def（registry 不删），头部注释加 Q12-B-CLOSE prototype/hold；behaviorParams 未改（禁止再调参）。
  - `src/lab/scenarios.ts`：Q12-B Scenario 改名 "Lifter Arm (prototype/hold)"，lifterCarBuild 注释标记 hold。
  - `src/battle/behaviorRegistry.ts`：保留 `lifter: createLifterRuntime`（Scenario 仍可加载），注释标记 prototype/hold。
  - `src/battle/lifterBehavior.ts` / `behaviorRuntime.ts`：LifterRuntime 源码保留，头部注释标记 hold。
  - `src/battle/planckVehicleAssembly.ts`：保留 `hammer || lifter` Revolute 装配路径（底层 Revolute Gadget 能力供后续机制复用）。
  - 测试：`tests/buildUx.test.ts` 新增 Q12-B-CLOSE 集成测试（不在 PART_OPTIONS / registry 保留 / Q12-B Scenario 仍可加载 / Revolute 仍装配）；F-DEV-1 smoke 加 `not.toContain('lifter')`；Q12-B / Q12-B-R1 测试名加 "(prototype/hold)"；`tests/behaviorRegistry.test.ts` 注释标记。
- 禁止项全部遵守：未调 lifter 参数 / 未加距离检测自动触发 / 未加隐藏向上 impulse / 未改对手 Physics / 未碰 laser·rammer。
- 验收：targeted vitest（buildUx + behaviorRegistry）43/43 通过；scoped tsc 0 error；镭射 / 冲锤无回归。
- 流程：impl + docs 两连提交；push origin foundation-02-canonical；ls-remote 校验 SHA。

## Q13-A｜高速圆锯 Weapon（已实现）

- 体验目标：不用看伤害数字，玩家也能看到「一个持续高速旋转的锯片正在切对手」。
- 改动：
  - `src/core/content.ts`：新增 `saw` def（id 'saw'，name '圆锯'，category 'weapon'，
    circle collider radius 28 / offset {0,0} → 圆心=Revolute 枢轴、原地自转，behavior 'saw'；
    behaviorParams：`spinSpeedRadPerStep:0.4`（故意明显≈24rad/s≈3.8rev/s）、`maxTorqueNm:400`、
    `hitPolicy:{mode:'contactTick', intervalMs:100, damage:8, minRelativeVelocity:0.3}`）。加入 registry map。
  - `src/core/partOptions.ts`：PART_OPTIONS 加入 `{v:'saw', t:'圆锯'}`（进入正常 Build）。
  - `src/battle/planckVehicleAssembly.ts`：Revolute 条件加 `|| f.def.behavior === 'saw'`。
  - `src/battle/sawBehavior.ts`（新）：持续单方向高速 Revolute 旋转，无状态机 / 无 limit / 不 setAngle。
  - `src/battle/behaviorRuntime.ts` + `behaviorRegistry.ts`：注册 `createSawRuntime`（behavior 'saw'）。
  - `src/battle/battleContract.ts`：RenderFunctionalPart 增加可选 `behavior?: string`。
  - `src/battle/planckBattleOrchestrator.ts`：buildVehicleSnapshot 透传 `behavior: p.def.behavior`。
  - `src/render/renderer.ts`：新增 `drawSaw`（16 齿锯齿 + 圆盘 + 轮毂 + 径向标记线，随真实
    part angle 旋转）；drawVehicle 对 `circle + behavior==='saw'` 优先画锯片（一眼可见高速旋转）。
  - `src/lab/buildEditorModel.ts`：EDITABLE_FUNCTIONAL_DEF_IDS 加 'saw'。
  - `src/lab/scenarios.ts`：新增 `sawCarBuild()` + `Q13-A` Scenario（autoDrive 靠近 banana 目标）。
  - 测试：`tests/buildUx.test.ts` 新增 Q13-A 描述块（定义契约 / 进正常 Build / 持续单方向旋转 /
    接触持续切割伤害>0 / 没接触无伤害 / 真实碰撞反作用）+ F-DEV-1 加 `toContain('saw')`；
    `tests/behaviorRegistry.test.ts` 期望列表加 'saw'、makeRuntime 加 saw 分支。
- 禁止项全部遵守：无固定击退 / 不直扣 HP（走 contactTick）/ 不新建 Contact Damage 系统 /
  不改 Hammer。伤害完全复用 W1-HIT-1 已实现 contactTick，无第二套伤害。
- 验收：targeted vitest（buildUx 46 + behaviorRegistry 3 = 49/49）通过；scoped tsc 0 error。
- 流程：实现 + 本日志；push origin foundation-02-canonical；ls-remote 校验 SHA；重启 dev server 刷新 Badge。

## Q13-B｜霰弹炮 Weapon（已实现）

- 体验目标：与普通炮（单发稳定弹道）形成完全不同的远程选择 —— 炮=单发稳定；霰弹=近距离扇形爆发。
- 核心决策：**不新建第二套 Projectile 系统**。抽 `src/battle/weaponProjectile.ts` 的
  `spawnWeaponProjectile`（从 Cannon/Laser 抽取的共享唯一真实弹生成点），Shotgun 仅负责
  「何时发射 / 朝哪些方向 / 发几发」，弹体生成 / 归属 / CCD / ContactRouter 伤害结算全部复用同一条正式链路。
- 改动：
  - `src/battle/weaponProjectile.ts`（新）：共享 `spawnWeaponProjectile(world, vehicle, part, {dir, muzzleSpeed, projectileRadius, projectileMass, gravityScale?})`；返回 `{proj, muzzlePoint}`；碰撞过滤 PROJECTILE 类别 mask GROUND|ARENA|HAZARD|敌方类别 + 同队负 group（不碰己方）；初速度 = 射手线速度 + dir×muzzleSpeed。
  - `src/battle/shotgunBehavior.ts`（新）：`ShotgunBehavior`。每次开火同时生成 5 发真实 projectile，
    固定扇形 `fanAnglesDeg:[-12,-6,0,6,12]`（相对炮口基准方向，由 part 世界姿态 + facing 推出；无随机 / 不自动瞄准 / 不 raycast）；
    一次齐射只发 1 次 `weaponFire` 事件（驱动一次爆闪）+ 只 `applyLinearImpulse` 一次 recoil（沿基准反方向作用于真实炮口点，由 Weld 传给整车；禁 setLinearVelocity）。
  - `src/battle/behaviorRuntime.ts` + `behaviorRegistry.ts`：注册 `createShotgunRuntime`（behavior 'shotgun'）。
  - `src/core/content.ts`：新增 `shotgun` def（weapon / box collider 46×22 offset{23,0} 短粗枪管 / behavior 'shotgun'；
    behaviorParams：`cooldownMs:1300, muzzleSpeed:6`（< Cannon 8，射程偏近、自然散开）, `projectileDamage:30`, `projectileRadius:7`,
    `projectileMass:1`, `recoilImpulse:45`, `fanAnglesDeg:[-12,-6,0,6,12]`）。加入 registry map。
  - `src/core/partOptions.ts`：PART_OPTIONS 加 `{v:'shotgun', t:'霰弹炮'}`（进正常 Build）。
  - `src/lab/buildEditorModel.ts`：EDITABLE_FUNCTIONAL_DEF_IDS 加 'shotgun'。
  - `src/lab/scenarios.ts`：新增 `shotgunCarBuild()`（watermelon + front shotgun）+ `Q13-B` Scenario（A 霰弹炮 / B 无攻击件目标车，spawn 450/1150）。
  - `src/main.ts`：onMuzzleFlash 加 `shotgun` 分支 → 暖橙 `#ffd35a` 半径 16 更大爆闪（Cannon 默认小闪 / Laser 白青巨束不变）。
- 禁止项全部遵守：无随机散布（首版可复现）/ 不自动瞄准 / 不做扇形 raycast / 不复制第二套 Projectile 系统 / 不改 Cannon·Laser。
- 关键物理事实（验收实测）：muzzleSpeed 6 + 世界重力 → 弹体约 step~12 触地（向下扇弹更早）；
  单发齐射伤害随距离：Bx≈520–600→210（多弹命中）、680–720→150（5 发全中）、760→60（部分 Miss，仅中央 2 弹中）、≥800→0（全部 Miss，弹触地=自然射程上限）。
  即「近距多弹命中 / 远距自然散开 Miss」完全由 muzzleSpeed+重力+扇角自然形成，无距离判定消失伤害。
- 验收：targeted vitest（buildUx 55 + behaviorRegistry 4 = 59/59）通过；scoped tsc 0 error。
- 流程：实现 + 测试 + 本日志；push origin foundation-02-canonical；ls-remote 校验 SHA；重启 dev server 刷新 Badge。

## Q13-C｜推进器 Gadget（已实现）

- 体验目标：玩家一眼看到「推进器喷火 → 整辆车突然向前冲」；改的是距离/碰撞时机，不直接造成 Weapon Damage。
- 核心决策：**推进器 = Weld part**（与 Cannon/Shotgun 同路，`planckVehicleAssembly` 的 `!== 'hammer' && !== 'lifter' && !== 'saw'`（且非 pushRod/rammer）→ `createWeldJoint`，无需改装配/关节）。推力 = 每个固定步施 1 次 `applyLinearImpulse`（≡连续力 via Δv/step，禁 setVelocity/teleport），沿真实 chassis facing 方向，只作用于自己 chassis。喷焰走 snapshot（非事件）：`battleContract` 加 `RenderFlame` + `BattleRenderSnapshot.flames`，orchestrator 聚合 `getRenderFlames?()`（镜像 `getRenderProjectiles`），renderer `drawFlames` 在真实安装位置画短喷焰；停推即空数组 → 自动立即消失。
- 改动：
  - `src/battle/thrusterBehavior.ts`（新）：`ThrusterBehavior`。固定周期状态机 `windup→thrust→cooldown`（windup≈0.4s→thrust≈0.5s→cooldown≈1.5s，按 FIXED_DT_MS 60Hz 算 step 数：`stepsFor(ms)=max(1,ceil(ms/FIXED_DT_MS-1e-9))` → 24/30/90）。`stepFixed`：thrust 期每步 `applyThrust`（沿 `rotateLocal({x:facing,y:0}, world.getAngle(body))`）；`getFlame` 仅 thrust 期返回 1 条 `RenderFlame`（root=真实安装 pos，dir=反向前向，纯视觉）。`readThrusterParams` 校验有限正数 + flameColor 字符串。
  - `src/battle/behaviorRuntime.ts` + `behaviorRegistry.ts`：注册 `createThrusterRuntime`（behavior 'thruster'）。
  - `src/battle/battleContract.ts`：`RenderFlame{x,y,dirX,dirY,length,width,color,team}`，`BattleRenderSnapshot.flames?:RenderFlame[]`（仅推进器贡献，停推即空）。
  - `src/battle/planckBattleOrchestrator.ts`：snapshot 返回 `flames:buildFlamesSnapshot()`（聚合 `b.getRenderFlames?.(world) ?? []`）。
  - `src/render/renderer.ts`：新增 `drawFlames`（外层 glow alpha 0.3 / 内层亮 alpha 0.9 / 根白橙核 #fff3d0 三角喷焰，root=真实安装 pos，tip=pos+dir*length，半宽用垂直向量）。
  - `src/core/content.ts`：新增 `thruster` def（gadget / box 30×24 offset{15,0} / behavior 'thruster'；behaviorParams：`windupMs:400,thrustMs:500,cooldownMs:1500,thrustImpulse:8,flameColor:'#ffb24a',flameLength:26,flameWidth:16`）。无 baseDamage → 无 Direct Weapon Damage。加入 registry map。
  - `src/core/partOptions.ts`：PART_OPTIONS 加 `{v:'thruster', t:'推进器'}`（进正常 Build）。
  - `src/lab/buildEditorModel.ts`：EDITABLE_FUNCTIONAL_DEF_IDS 加 'thruster'。
  - `src/lab/scenarios.ts`：新增 `thrusterCarBuild()`（watermelon + 前置 thruster）+ `Q13-C` Scenario（A 推进器 / B plainCar，spawn 450/1150，camera 'vehicles'）。
- 禁止项全部遵守：不 setVelocity / 不 teleport / 不给对手固定冲击力 / 不造成 Direct Weapon Damage（无 baseDamage）/ 不改轮子 Movement 参数。每辆合法战斗车必须 ≥1 Weapon（validateSnapshot 强制），Gadget 单独非法。
- 验收实测（调参 debug 测试，已删）：windup=23 step、thrust=30 step；thrust 期 vx 0.049→1.454 px/step（明显「突然加速」）；喷焰仅 thrust 窗口出现；windup/cooldown 无偷偷加速。
- 测试：`tests/behaviorRegistry.test.ts` 期望列表加 'thruster' + makeRuntime 加 thruster 分支 + 2c 测试（固定周期 windup→thrust→cooldown；thrust 期喷焰出现且 chassis 受真实冲量）；`tests/buildUx.test.ts` 新增 Q13-C 块（定义契约 / 进正常 Build / 固定周期可复现 / 仅自己 chassis 加速 / 无对手力·无冷却偷加速 / 喷焰仅在 thrust 且位置·team·color·方向正确 / 碰撞用真实质量 / 不改轮子 Movement / F-DEV-1+Q13-C 选项）+ F-DEV-1 smoke 加 `toContain('thruster')`。
- 验收：targeted vitest（buildUx 64 + behaviorRegistry 5 = 69/69）通过；scoped tsc 0 error。
- 流程：实现 + 测试 + 本日志；push origin foundation-02-canonical；ls-remote 校验 SHA；重启 dev server 刷新 Badge。

## Q13-A-R1｜圆锯旋转与切割感（已实现）

- 问题：真人 30fps 录像中圆锯 Runtime 正常，但看起来接近静止齿轮。根因：16 个完全对称齿 + spinSpeed 0.4 rad/step，60Hz→30fps 即每帧 ≈45.8° ≈ 跨 2 个 22.5° 齿距（接近整数齿）→ 明显视觉 alias（频闪）。
- 目标：不看伤害数字也能直接看到「高速旋转圆锯正在持续磨切对手」。
- 改动（保留真实 Revolute / Collider r=28 / contactTick，不重做伤害系统）：
  - `src/core/content.ts`：saw `spinSpeedRadPerStep` 0.4 → **0.27**（≈16.2 rad/s ≈ 2.58 rev/s；0.27×2帧≈30.9°/帧≈1.37 齿/帧，避开整数齿频闪区）；JSDoc 注释同步。
  - `src/battle/sawBehavior.ts`：`SAW_DEFAULT_PARAMS.spinSpeedRadPerStep` 0.4 → 0.27 + 注释说明；class JSDoc 解释 0.27 动机。
  - `src/battle/battleContract.ts`：新增 `RenderSpark{x,y,nx,ny,intensity,team}` + `BattleRenderSnapshot.sparks?:RenderSpark[]`（仅 saw 有效 contactTick 贡献，离开接触即空数组）。
  - `src/battle/contactRouter.ts`：新增 `getActiveSparks(): RenderSpark[]`（遍历 activeTicks，仅 `kind==='weapon'` 取出 contactPoint/contactNormal/relativeVelocity/attackerTeam）。
  - `src/battle/planckBattleOrchestrator.ts`：`getRenderSnapshot()` 返回 `sparks:buildSparksSnapshot()`（聚合 `router.getActiveSparks()`）。
  - `src/render/renderer.ts`：
    - `drawSaw` 改造：**保留** 16 齿锯齿 + 圆盘 + 轮毂；**新增** 3 个非对称辐条（120° 均布暗色楔形 `rgba(18,20,26,0.55)`，内 r*0.24→外 r*0.82）+ 1 个高对比旋转标记（红橙 `#ff5a3c` 粗线 `r*0.1` 宽，从圆心到 rOuter，`lineCap='round'`）；全部跟随真实 part angle `a`。
    - `drawSparks`（新增，drawFlames 之后调用）：每个 spark `translate(sx,sy)`+`rotate(atan2(ny,nx))`；画白橙核 `arc r=ss(4)` + 切向亮弧 `arc r=ss(8) from -0.9..0.9` + 5 条确定性火花射线（k=-2..2，`k*0.35` rad，len `ss(13)`，无随机）。纯视觉、确定性。
  - 测试：
    - `tests/buildUx.test.ts`：saw 定义断言 `radius` 收紧为 exact `28`（禁止扩大 Collider）；`spinSpeedRadPerStep` 改 `>=0.25 && <=0.3`；旋转总量 `>25`（≈0.27×120=32.4 rad）。新增 `describe('Q13-A-R1 圆锯旋转与切割感')`：(1) 0.25–0.30 + 保留真实 Revolute/Collider r=28/contactTick；(2) 接触前 sparks 空、saw 切到 B 后 `>=1` 且 x/y 有限且 team 'A'；(3) Hammer 不变（behavior 'hammer'/box collider/不泄漏 spinSpeed 参数）。
    - `tests/contactTick.test.ts`：新增 test 6（Q13-A-R1）：makeStart(saw,bBody,3.0)+advanceContactTicks(0) → `getActiveSparks().length===1`，x===500,y===400,team==='A',法线有限；makeEnd → `length===0`。
- 禁止项全部遵守：不加固定击退 / 不扩大 Collider / 不改 Hammer / 不做假角度动画（标记全部跟随真实 part angle）/ 不靠伤害数字证明「正在切」（火花走真实 contact 事件，伤害仍只走现有 contactTick）。
- 验收：targeted vitest（buildUx 67 + contactTick 7 = 74/74）通过；scoped tsc 0 error。
- 流程：实现 + 测试 + 本日志；push origin foundation-02-canonical；ls-remote 校验 SHA。

## Q13-B-R1｜霰弹炮从"慢珠子"改成瞬间喷射（已实现）
- 问题：真人录像中霰弹炮 muzzleSpeed=6，表现为几颗慢速小圆弹，完全没有霰弹枪的瞬间爆发。根因：把"短射程"错误实现成"子弹飞得慢"+靠重力制造近程。
- 目标：删除"霰弹近程=子弹飞得慢"的错误规则，改为"霰弹近程优势=多弹扇形扩散，距离越远命中弹数自然越少"。一次开火肉眼直接看到：砰→5 道高速弹迹扇形喷出→整车明显后顿。
- 改动（保留 5 个真实 Projectile / 固定扇形 -12/-6/0/+6/+12° / 现有 CCD / Owner Filter / Damage 链；不改 Cannon/Laser；不扩大 projectile Collider；不新增第二套 projectile 系统）：
  - `src/core/content.ts`：shotgun `muzzleSpeed` 6 → **13**（12~14 目标区间）；`recoilImpulse` 45 → **160**（第一版允许明显过强，直接作用于本车 chassis 真实炮口世界点）；JSDoc 注释同步。
  - `src/battle/battleContract.ts`：扩展 `RenderProjectile.visual` 支持 `'tracer'`（原仅 `'laser'`）。
  - `src/battle/behaviorRuntime.ts`：`ShotgunRuntime.getRenderProjectiles` 给每个 pellet 加 `visual:'tracer'` + `velocity`（复用 Laser 既有字段，renderer 据此画弹迹尾巴；不扩大真实命中范围）。
  - `src/battle/shotgunBehavior.ts`：后坐从 `part.body`（weld 子体）改为 `vehicle.body`（chassis），沿基准炮口反方向 applyLinearImpulse，作用点仍是真实 muzzlePoint → 产生力矩使整车明显后顿（与 Laser 模式一致）。
  - `src/render/renderer.ts`：
    - 新增 `ShotgunFan` VFX（TTL 100ms≈3 帧 @30fps）+ `spawnShotgunFan()`/`activeShotgunFans` getter + `drawShotgunFans()`：扇形 ±14° 半角、R=ss(34)、暖橙填充、5 条确定性亮射线 -12/-6/0/+6/+12°（呼应 5 弹）+ 亮炮口核。
    - `drawProjectiles` 新增 `tracer` 分支：沿真实 `velocity` 在弹头后方画短高速弹迹 TRACER=ss(42)（35~50px 目标），弹头用真实 `radius`（不扩大命中范围）。render 循环在 `drawLaserBeams()` 后调用 `drawShotgunFans()`。
  - `src/main.ts`：`onMuzzleFlash` 中 shotgun 从普通圆形 flash 改为 `renderer.spawnShotgunFan(...)` 有方向短促扇形炮口爆闪。
  - `src/lab/scenarios.ts`：Q13-B 隔离 Scenario `spawnB` x 1150 → **560**（真正近距离验证，首发即可看清多弹命中；不改变正常 Build 接入）。
  - 测试 `tests/buildUx.test.ts`：
    - Q13-B 旧块：muzzleSpeed 断言收紧为 12~14 + 断言 Cannon 仍为 8；fan 散布测量改用 `loadCustom(shotgunSnapshot('A'),plainSnapshot('B'),{spawnB:{x:2000,...}})`（远近两场景）；远距 partial-Miss 最终 B=900（高 muzzleSpeed 后重力不再兜底，需近距才能命中）。
    - 新增 `describe('Q13-B-R1 霰弹炮瞬间喷射')` 5 项：(1) muzzleSpeed 12~14 + 5 pellet + Cannon 仍 8；(2) tracer visual + 真实 `velocity` + `radius`≈7（不扩大）+ 速度 8~22 + 朝 +x；(3) `spawnShotgunFan` → 1 个 fan 沿开火方向；(4) 近距 B=560 → ≥2 pellet 命中（weaponDmg≥60）；(5) chassis 后坐 → 可见向后位移 >0.3px/帧 + vx 负。
- 禁止项全部遵守：不做 raycast / 不随机散布 / 不自动瞄准 / 不扩大 projectile Collider / 不改 Cannon/Laser / 不新增第二套 projectile 系统。
- 验收：targeted vitest（Q13-B+R1 子集 14/14；全量 buildUx+scenario+behaviorRegistry = 81/81）通过；scoped tsc 0 error。
- 流程：实现 + 测试 + 本日志；push origin foundation-02-canonical；ls-remote 校验 SHA。

## Q13-C-R1｜推进器安装方向 + 爆发推进（已实现）

- 问题（真人录像）：车有位移但几乎看不出「推进器造成位移」。两直接原因：(1) Q13-C Scenario 把 thruster 装 front、flame 朝车身后方 → 喷焰穿入车身内部；(2) flameLength=26/flameWidth=16 甚至小于标准轮视觉尺度。
- 目标：车尾点火 → 大喷焰 → 整车突然窜出去。
- 改动（沿用既有 ThrusterBehavior 状态机；不改 Wheel Movement / 不造成 Weapon Damage / 不 setVelocity / 不 teleport / 不给对手力）：
  - `src/battle/thrusterBehavior.ts`：
    - 新增 `computeDirs(world,vehicle,part)`：`exhaustDir = chassis 中心 → thruster 安装点（part 世界坐标）的 outward`；`thrustDir = -exhaustDir`。装在后部→向后喷火车向前冲；装在前部→向前喷火车向后推。不再无条件用 `vehicle.facing`；位置决定物理结果（不增专属槽/隐藏资格）；安装点≈中心时退路用 facing。
    - `applyThrust`：冲量沿 `thrustDir` 施加到 `vehicle.body`，作用点 = 真实安装点世界位置（自然姿态反作用）；`advancePhase`/`stepFixed` 透传 `part`。
    - `getFlame`：`dirX/dirY = exhaustDir`（沿 outward 喷出，绝不穿车）；根部仍是真实安装点。
  - `src/core/content.ts`：thruster `windupMs` 400→**250**、`thrustMs` 500→**300**、`cooldownMs` 1500 不变；`thrustImpulse` 8→**40**（矫枉过正，0.3s burst 明显窜出）；`flameLength` 26→**105**、`flameWidth` 16→**35**（大喷焰，90~120 / 30~40 区间）。
  - `src/lab/scenarios.ts`：Q13-C 隔离 Scenario `thrusterCarBuild` 改 `functionals` 由 `front` → **`rear`**（西瓜 rear hardpoint）；description 同步。
  - `src/render/renderer.ts`：`drawFlames` 加确定性轻微长度抖动（`1 + 0.05*sin(performance.now()/35 + (x+y)*0.03)`，非随机）让大喷焰有喷发感；仍保留亮核 + 外焰，根部=真实安装点。
  - 测试 `tests/buildUx.test.ts` + `tests/behaviorRegistry.test.ts`：
    - Q13-C 旧块：test1 windup/thrust 断言改 0.2~0.3s/≈0.3s + 新增 flameLength 90~120/flameWidth 30~40 + impulse>20；test3 时序阈值改（首焰步 11~19、推进 14~22）；test4 步进改 14 步 windup→18 步 thrust（rear→+x）；test5 cooldown 断言改 `末速度 ≤ 初速度+0.3`（允许自然摩擦）；`thrusterSnapshot` helper 改 rear 安装。
    - `behaviorRegistry.test.ts` 2c：时序改 14 步 windup + 进 thrust 当步喷焰（dirX>0，front 安装 outward）；方向断言改 `vAfterThrust < 0`（front→向 -X 推），验证安装位置推导。
    - 新增 `describe('Q13-C-R1 推进器安装方向 + 爆发推进')` 5 项：(1) 官方 Q13-C rear 安装 → 喷焰根部=真实安装点、方向 outward 朝车外、tip 在车身外（half-width 85）、绝不穿车；(2) 点火与加速同步（windup vx≈0、进 thrust 当步即喷焰+加速）；(3) 0.3s burst 速度明显增长、等长 cooldown 段不再增长（无隐藏推力）+ 爆发段位移>15；(4) 前置安装方向自然反转（vx<0、flame dirX>0、不穿车）；(5) B 不受推进器直接推力（隔离首轮采样，排除碰撞位移）。
- 禁止项全部遵守：不 setVelocity / 不 teleport / 不给对手力 / 不造成 Weapon Damage / 不改 Wheel Movement / 不做假位移动画。
- 验收：targeted vitest（Q13-C+R1 子集 14/14；全量 buildUx+scenario+behaviorRegistry = 86/86）通过；全量 `tsc -p tsconfig.json` 0 error。
- 流程：实现 + 测试 + 本日志；push origin foundation-02-canonical；ls-remote 校验 SHA。

## Q13-C-R2｜推进器前进方向修正（删除「安装位置决定前进方向」）

- 触发：真人录像确认 rear 装 thruster 时喷焰朝右（敌方）、车反而向左远离敌人。先按 R2 要求做运行期几何确认（临时诊断 test，跑完即删）：Q13-C 首 thrust 记录 `facing=1 / chassisAngle≈0 / mountRelCenter.x=-78`（rear hardpoint 在车身**后方**，非前方）→ 无 hardpoint/assembly 异常，按规约继续，不停止。
- 根因澄清：当前 rear 装 + 旧「安装位置推导」规则实际给出的方向与「车头=推进」**重合**（都 flame 左/thrust 右）。用户看到的「flame 右/车左」对应**旧 front 装**场景（front 装时旧规则确实反转成 flame 右/thrust 左）——正是 R2 要修的 case。
- 改动：
  - `src/battle/thrusterBehavior.ts`：删除 `computeDirs` 的 mount 推导；`chassisForward = rotateLocal({x:facing,y:0}, chassisAngle)`；`thrustDir=chassisForward`；`exhaustDir=-chassisForward`；`computeDirs` 不再收 `part`；`applyThrust` 冲量仍作用于真实安装点（自然力矩）；`getFlame` dir = exhaustDir。
  - `src/battle/battleContract.ts`：`spawnA/spawnB` 增加可选 `angle?: number`（初始 chassis 倾角，仅供倾斜验收）。
  - `src/battle/planckVehicleAssembly.ts`：新增 `rotatePlanckVehicle(world, vehicle, angleRad)`，绕 chassis 中心刚性旋转整车所有 body（保持关节约束），用于倾斜验收。
  - `src/battle/planckBattleOrchestrator.ts`：settle 之后若 `config.spawnA?.angle / spawnB?.angle != null` 调用 `rotatePlanckVehicle`（正常战斗不传→不旋转）。
  - `src/lab/scenarios.ts`：Q13-C description 改述为「推进方向=车辆真实前向；喷焰与推进方向严格相反；安装位置只影响冲量作用点/力矩」。
  - 测试：
    - `behaviorRegistry.test.ts` 2c：方向断言翻转——front 装 facing=+1 → `flame.dirX<0`（喷焰朝车尾反方向）、`vAfterThrust>0`（车头方向=推进，不再反转）。
    - `tests/buildUx.test.ts`：Q13-C test1 描述改「推进方向=车辆真实前向」；R1 test4「前置方向反转」改为「前置**不再**反转（front 装 facing+1 → vx>0、flame dirX<0）」；新增 `describe('Q13-C-R2 推进器前进方向修正')` 5 项：(1) A facing+1 → thrustDir.x>0 & exhaustDir.x<0；(2) facing-1 → thrustDir.x<0 & exhaustDir.x>0；(3) 首点火 A-B 水平距离明显缩短（>10px）；(4) 首点火当步 vx>0 且 flame dirX<0；(5) 倾斜 angle=0.5 空中出生 → 喷焰 dir ≈ -chassisForward 且 |dirY|>0.1（随真实角度旋转、非固定 world X）。
- 禁止项全部遵守：不按 chassisCenter→mount 推导方向 / 不动态追踪敌人 / 不自动瞄准 / 不 setVelocity / 不 teleport / 不改 Wheel / 不单独翻转 flame 伪装推进。
- 验收：targeted vitest（Q13-C+R1+R2 子集 + 全量 buildUx 82 + scenario 4 + behaviorRegistry 5 = 91/91）通过；全量 `tsc -p tsconfig.json` 0 error。
- 流程：实现 + 测试 + 本日志；push origin foundation-02-canonical；ls-remote 校验 SHA。

## Q13-C-R3-PRESENT｜推进器表现重做（仅表现层，不改物理/方向/节奏）
- 问题：R2 方向已对，但表现层仍是「巨大扁平三角 + 轮子大小白圆 + 生硬 On/Off」，缺点火节奏、火焰覆盖车身。
- 必改（全为 Presentation，禁改 thrustImpulse/windup/thrustMs/cooldown/Physics/方向）：
  1. flameWidth 语义修正为「总宽」：renderer 所有横向计算改 `halfW = ss(width)/2`；外层最大约 `width×1.3/2`（不再 width×1.6 巨三角）。
  2. 喷焰几何重做 3 层（平滑叶 nozzle 窄→mid 宽→tip=0，非三角）：C 外层红橙 glow（低 alpha）、B 主橙焰（flameColor）、A 白黄火芯（≈36% 长、窄、高亮、与 B 不同步抖动）；删 width×0.55 巨白圆，喷口只留 ≈5~8px 小白黄亮核。
  3. 渲染层级：plume/glow 在 Vehicle **之前**绘制（车身自然遮住重叠 → 火从车尾后方出），喷口亮核在 Vehicle **之后**绘制；root 仍=真实 nozzle/mount world position（不假移坐标）。
  4. 最小点火节奏（无粒子系统）：`RenderFlame` 增加 `phase:'windup'|'thrust'|'cooldown'` + `tMs`；thrusterBehavior.getFlame 在 windup 返回小橙光(windup)、thrust 返回爆发焰(thrust)、cooldown 爆发后约 70ms 内返回收缩焰(cooldown) 之后 null。renderer 据此：windup 仅喷口小橙光脉冲；thrust 前 80ms 长度 60%→110% 冲开、稳定 ±6% 长/±5% 宽抖动；cooldown 70ms 内快速收掉。
- 文件：`src/battle/battleContract.ts`（RenderFlame +phase/tMs）、`src/battle/thrusterBehavior.ts`（getFlame 三态 + tMs，物理/方向零改动）、`src/render/renderer.ts`（drawFlames→drawFlamePlumes/drawFlameNozzles/drawFlameShape + render 顺序：plume 在车前后、亮核在车后前；Cannon/Laser/Saw/Shotgun 完全未动）。
- 测试：registry 2c 前摇断言改「windup flame 存在(phase=windup)」；buildUx Q13-C test3 改相位轨迹（windup/thrust 可复现）、test6 改三态齐全(cooldown 收掉为 null)、R1 test2 前摇改「windup flame 存在」；新增 `describe('Q13-C-R3-PRESENT 喷焰表现重做')` 4 项（windup glow/tMs、thrust 携带 exhaust 方向+真实 width 总宽 30~40 不翻倍+length 90~120、cooldown 70ms 收缩后 null、三态齐全）。
- 验收：全量 vitest 476/476 通过（含 Q13-C/R1/R2/R3 子集）；全量 `tsc -p tsconfig.json` 0 error。
- 禁止项全遵守：不改 R2 方向 / 不改 thrustImpulse / 不改车速 / 不 setVelocity / 不加屏震 / 不建通用粒子系统 / 不引入图片 / 不顺带重构 Renderer / 不用大白圆做爆发感。
- 流程：实现 + 测试 + 本日志；push origin foundation-02-canonical；ls-remote 校验 SHA；完成后重启 Dev Server 刷新 Badge。
