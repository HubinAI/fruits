# 2026-09-05 工作日志

## F-GARAGE-FUSION-UX-R2 交付（重构背包合成交互）

- **commit**：`398f10a854a2631e7fa2eaec9af9fd03452e4332`（15 files，+1949/−1192；foundation-02-wechat）。local=ref=remote=398f10a（三路一致，push 后验证）。
- **正式规则落地**：`fuseCategoryMaterials(inv, materials, cat, build, star, rng)` —— 5×同分类同星未装备（可混 defId，body 不参与，MAX_STAR=2 阻挡，产物仅来自官方 Registry）→ 1×同分类下一星；rng 可注入；原子落盘（校验全过后一次 saveInventory）。旧 `fuseSameStar` 标 LEGACY。
- **主流程**：自动放入 → 合成 = 2 次核心点击；`autoPickFusionMaterials` 优先级=未装备→当前星→副本多→defId 字典序（稳定可复现）；双击防重。
- **Host UI**：fusionSlots[5]/fusionResult/fusionGlow/fusionToken；`bfilter/backpack-select/fusion-slot:N/fusion-auto/backpack-fuse/backpack-test-material/fusion-result-dismiss` 全套 dispatch；切分类/离开/backpack 返回清槽；结果卡 0.8s 可跳过 + 金色高亮 + reload 保持。交互 id 命名铁律（`__fx`/`__inv` 新句柄须入 bundle-clean allowlist）继续生效。
- **测试**：`garageFusionUX_R2.test.ts` 28/28（T1–T28：core 规则/装备保护/自动放满/原子/rng 精确输出/MAX_STAR；host 状态机/还差提示/满5enable/双击防重/reload 持久/Build 不变/body 隐藏/切分类清槽/Result-adjust 保留；11/3/8 分类可达性+底栏不重叠+3 viewport+DPR1/3 几何一致；v1 迁移/旧 2★ cannon 保留）。`rcFusionTestEntryP0` 重写 17/17（R2 门控字符串守卫：`tmShow=(rcGrantTM||e2eProbeTM||devResetTM)&&backpackFilter!=='body'`）。删 `garageInventoryFusionR1.test.ts`、`_e2e_backpack_fusion.cjs`。
- **E2E**：`_e2e_fusion_ux_r2.cjs` §十 真实浏览器闭环 **160/160 PASS**（4 视口 844×390@dpr1/844×390@dpr3/420×210@dpr1/1280×592@dpr1.5；seed cannon2+hammer2+saw1 + only-spear Build；自动放入确定性 `cannon,cannon,hammer,hammer,saw`→移除/补回→合成→1★−5/2★+1→结果卡→reload 保持→Build 未变）。
- **门禁**：全量 vitest **190 files / 1698 tests 0 failed**（vmForks --maxWorkers=1）；tsc 0；四端构建 EXIT=0（build/build:wechat/build:pages/build:e2e）；bundle-clean（wechat/e2e/rc）三路 PASS；repo-health 9/9；RC 重建 **#398f10a dirty=false**，四方 SHA 一致。
- **范围**：未执行 F-WX-6 / resume / 其它 Queue；未部署 Pages。玩家体验（真机体感/结果卡节奏）留待人工点检。

## 🔴 环境坑：vitest 全量红先查 cwd 盘符大小写（双实例）

- **症状**：本 Queue 收尾时全量回归偶发/持续红，失败面不稳定（1→2→3 文件递增）：`demoPlayerRuntimeP0`（Error: PlatformCore 未绑定）、`q23Economy`（coin +0，持久化结算全挂）、`q31Stability`（loadInventoryRaw null / countAfter 不累加）。三者共同点=依赖 setup.ts 的 `bindPlatformCore(createWebCore())`。
- **根因**：Windows 下 Bash cwd 盘符大小写（`d:\0818new\最强水果` vs `D:\0818new\最强水果`）使 vite-node 把 `src/platform/context` 实例化两次 → setup 绑定落在实例 A、测试文件 import 读到实例 B → `bound=false`。setup 探针证实同 pid 执行但模块状态不可见。与代码/pool/测试无关。
- **判定/修复**：`cd /D/0818new/最强水果`（大写）后全量 190/190 0 failed。**未来 vitest 出现这类「PlatformCore 未绑定/库存读写 null」红测，先 `node -e "console.log(process.cwd())"` 看盘符大小写，统一大写再跑**。切勿为此改 src/setup 或加 try/catch 掩盖。

## F-GARAGE-FUSION-FEEDBACK-LAYOUT-R2.1 交付（合成页布局/反馈重构）

- **commit**：`dcd199b7afcc15d01501b66ca211e8bc6ac8e47b`（5 files，+1979/−263；foundation-02-wechat）。local=ref=remote=repo-health 9/9 全 PASS；RC 重建 **#dcd199b dirty=false**，badge/rc-build.json/runtimeInfo/HEAD 四方一致。
- **统一布局单源**：`computeFusionLayout` 一个函数供给 draw+hitArea 的七区（header/页签/状态行/卡网格/分页/材料栏/动作区）；`drawBackpackPage` 每帧一次调用分发。禁重复手算坐标。
- **顶部**：RC badge 移页头右缘独立位（`fusionBadgeSpot` 由 drawBackpackPage 每帧算，drawBuildBadge 在 metaPage==='backpack' 时消费；默认全局 areas.badge 锚点落在 tab 行会 E1 overlap——4 视口全消除）；测试材料×5 语义锚 `right ≤ menuButton.left − 6` + `!isBody`。
- **short(420×210) 卡**：修复为三行 .20/.52/.82（L1 名/L2 星/L3 状态），主 15fs/次 13fs × fontScale 0.8 → 可见 12.0/10.4（满足主≥12/次≥10）；normal 分支不变。
- **材料栏/反馈**：五槽真实微图标（`drawPartIcon(micro=true)` → `fusionIconMicro(defId,…)` 12 部件 defId 专属）+ 短名 + ×N（同 defId 仅首槽右上）；点槽移除；加入/移除/自动放入行内 toast（`fusionToastFor(defId,'add'|'remove'|'auto')`）+ `showFusionFlash` 卡/槽闪 150–250ms；N/5 实时。
- **状态机/结果层**：主按钮 还差N件(disabled)→合成→合成中…(fusionPending 260ms)→结果卡；结果卡=真实产出图标+「消耗：5件战斗1★」+「获得：名2★」+「点击任意处继续」(≥0.8s)；dismiss（`fusion-result-dismiss`）→ `fusionJumpTo=product` → draw 时非 dispatch 分页聚合并消费 → 产物卡「新获得」金呼吸 2s + 星级数量立即可见；token 防 late-callback。
- **测试**：新增 `garageFusionFeedbackLayoutR21.test.ts` T1–T22 正式套件 28/28（几何/drawOps/像素断言：badge×tab no-overlap、七区两两相交=0、DPR1/1.5/3 几何恒等、卡内文字半高边界、单主状态、槽图标签名可区分、N/5 全程同步、还差N件、结果卡同屏、dismiss 跳页、双击防重、reload 持久、普通构建零调试入口、卡片全集==Registry）；旧 `garageFusionUX_R2` 适配（parseBarCount `/^([0-5])\/5$/`、T24 pending sleep 340）；`rcFusionTestEntryP0` source-guard 适配（isBody 语义 + 方法边界截取）。
- **E2E**：`_e2e_fusion_feedback_r21.cjs` 16 步 ×4 视口 **184/184 PASS**（844@dpr1/844@dpr3/420@dpr1/1280@dpr1.5）。**关键修正**：geometryGate 越界判定原拿 CSS viewport（420×210）比较 hitArea **logical 舞台坐标**（cssW/cssH 恒 844×390，CSS 缩放只在外层）→ 420×210 全误报越界；改为以 `getTransformInfo().cssW/cssH` 为越界边界后 4 视口全过。**教训：E2E hitArea 几何门禁边界=host logical 舞台（844×390），不是 CSS viewport**。
- **门禁**：全量 vitest **192 files / 1726 tests 0 failed**（vmForks --maxWorkers=1）；tsc 0；四端构建 PASS；bundle-clean rc+e2e 全 chunk PASS；repo-health 9/9。
- **范围**：临时取证 `_r21_evidence.test.ts` 与 `_dbg*.cjs` 已删（断言已并/覆盖于正式套件）；未部署 Pages；未动合成规则/库存/物理/渲染/生命周期。**电脑端真实体验录屏验收未执行（留待用户）**；iOS 未验证。停止待回执。

## F-MEMORY-RUNTIME-COMPACT-P0 交付（主 MEMORY.md 压缩 + 全文归档）

- **前置检查**：repo-health 9/9 PASS；local=remote=`8fbac75`（HEAD 未变）；工作树干净；**`git ls-files` 确认 `.workbuddy/memory/MEMORY.md` 受 Git 跟踪**（连同每日日志/PLAYBOOK 入库）→ 走独立维护 commit 路径。
- **归档**：`.workbuddy/memory/archive/MEMORY_FULL_2026-09-05.md`（cp 原文不改写；93 行 / 8052 字符 / 13092 字节，非空核对）。此前无同名文件，无覆盖。
- **压缩后主 MEMORY.md**：51 行 / 2429 字符 / **2467 bytes ≤ 2600**；六部分齐（Identity / Collaboration rules / Git-build safety / Stable technical contracts / Current truth / Next action）；含全 SHA `8fbac75dc5…`、`F-GARAGE-FUSION-RESULT-INTERACTION-R2.2`（pending）、"technical gates PASS / **computer experience FAIL**"、"do NOT auto-start"；无陈旧 Queue 状态/测试流水账。历史细节索引到 archive + 每日日志 + 交接文档。
- **commit**：`ff6a20d7d7554aaa13903335bf4530f6f336a44b` `chore(memory): compact runtime memory and archive history`（2 files +144/−93，含新 archive 文件）。local=ref=remote=ff6a20d（push 后实测）。repo-health 9/9 PASS。**未重建 RC**；旧 RC `#8fbac75` 仍是上一功能版本验收产物；下一条功能 Queue 以 ff6a20d 为代码基线（memory 维护 commit，不作新功能版本描述）。
- **范围**：仅动 `.workbuddy/memory/MEMORY.md` + archive 新增 + 本日志；未改 src/tests/scripts/package/dist/outputs/RC 产物/游戏文档。**R2.2 尚未启动**，等用户正式指令。停止待指令。

## F-GARAGE-FUSION-RESULT-INTERACTION-R2.2 交付（合成结果交互闭环）

- **commit**：本功能 commit（SHA 见交付报告；memory/daily 已并入同一 commit，功能 commit 后不再改 tracked memory）。
- **红测先行**：新增 `tests/garageFusionResultInteractionR22.test.ts`（TA1–TA11）→ 基线 9df1eab 首跑 **9 failed / 2 passed**，稳定复现 7 类问题（自动关 950ms / 点卡即关 / 误导文案 / 结果帧无底层 / 遮罩 0.78 / 顶层单层 / glow 1999ms / 两处静默）。改码后同文件 11/11 绿。
- **必改 1–5 落地（canvasPlayerUIHost.ts，+93/−47 净）**：
  1. 关闭规则：`showFusionResult` 删 950ms 自动关 timer；`fusionResult` 类型去 `until`（`{product,token}`），展示=持卡态直到空白点击；新增 `fusion-result-card` 命中=点卡阅读 no-op；dismiss 仅空白遮罩区触发；文案「点击任意处继续」→「点击空白处继续」。
  2. 输入隔离：结果层画在整页**之上**（drawBackpackPage 尾调 `drawFusionResultCard`，删除原中段早退）；z 序=页控件 < `fusion-result-dismiss`(整页) < `fusion-result-card`(卡区)，后注册先命中 → 底层不可达。
  3. 视觉：底层页面保留可读 + 遮罩 `rgba(6,9,14,0.62)`（0.78→0.62，∈[0.55,0.70]）；veil 改 `this.rect`+`this.hit`（不再用 button 全屏实底）。
  4. 关闭后反馈：dismiss 重武装 glow/new `nowMs+2500`，清理 timer 2100→2600；showFusionResult 内 glow/new 同步 2500/2600。
  5. 异常反馈：`tryAddMaterial` 失败三分支（avail≤0 / uses≥avail / 槽满）各给行内原因 toast（暂无可放入的1★材料 / 该1★材料已全部放入 / 材料槽已满，先移除一件）；`autoFillFusion`、`runFusion` 防御路径补 toast。
- **测试**：R2.1 套件适配 T15（文案）/T17（改点空白右下角关闭，harness 增 `tapAt`）；R2.2 新套件 TA1–TA11 覆盖 7 类问题映射（见文件头注释）。单/双套件 34/34 绿。
- **门禁**：全量 vitest **192 files / 1732 tests 0 failed**（vmForks --maxWorkers=1；platformCore rAF 全量下偶发 5s 超时，单跑 23/23 绿，判定环境抖动与本次改动无关，复跑全量稳定）；tsc 0；四端构建 EXIT=0；bundle-clean wechat+e2e 全 chunk PASS。
- **E2E**：`_e2e_fusion_feedback_r21.cjs` **196/196 PASS**（4 视口；新增 S11e 文案/S11f 底层保留/S11g 不自动关(静置1.2s)/S11h 点卡不关 + S12 改点空白角 `tapCornerById`）；`_e2e_fusion_ux_r2.cjs` **172/172 PASS**（含 G9–G11 同款 R2.2 断言 + H 改角点关闭）。**修正**：R2 driver G1–G7 基线竞态（tapVisibleById 200ms < pending 280ms，结果卡未现即读）→ 点击后补 `waitForTimeout(340)`（与 feedback driver 420ms 同法），非 R2.2 行为回归。
- **repo-health 9/9**：功能 commit 后跑（见交付报告）。
- **范围**：未动 5合1规则/库存/经济/Garage/Home/Matching/Battle/Result/Renderer/DPR/Physics/Pages；E2E 测试句柄未新增（沿用 __h/__txt，无新命名空间）；未部署 Pages；未重建 RC（RC 于 commit 后重建四方一致——待交付报告）。停止等用户电脑录屏体验回执。

## F-GARAGE-FUSION-STAR-SELECTION-R3（合成页星级选择 + 单星选材）
- **基线**：HEAD `019daa4`（R2.2 交付版）；8 点调查锁定 UI 层钉死 1★ 为唯一遗漏层（core/runtime 已支持 star：fuseCategoryMaterials/autoPickFusionMaterials 带 star 参数）。
- **实现**（canvasPlayerUIHost.ts）：星级行 starChips（动态 MAX_STAR，chip `1★ → 2★｜N件` / `2★｜满星`）；默认星级规则（①最低可合成≥5 → ②最低有库存 → ③1★）+ per-category 会话记忆 fusionStarByCat；卡片单星视图（L2 `{star}★`、L3 `可用 N/装备占用N/可用 0 弱态`）；fusionSlots 对象化 `{defId,star}`；有材料切分类/星级 → 页内轻确认 fusionSwitchAsk（取消=不变/确认=清空切换）；满星查看态纯查看（无槽/自动/合成）；toast/自动放入/结果卡全带星级文案；dismiss 后 fusionStar=productStar 定位产物。
- **红→绿**：新套件 garageFusionStarSelectionR3 RB1–RB9 首跑 9/9 failed 锁定缺陷，改码后 9/9 绿。
- **R2.1 套件 7 断言适配**（T4 `1★×N`→`1★`、T5 `已装备`→`装备占用N`+未拥有隐藏、T6 chip 52 几何+自身文本排除、T12 新文案、T17/T22 满星视图、全集改全库存种子）；UX_R2 套件 3 处适配（T18 轻确认流、T21/T22 全库存种子）。
- **⚠️ 布局密度回归修复**：星级行 +23px 使 844×390 两行卡网格（pageSize 8）塌缩单行（4），metaShell 验收4 暴露 → 正常屏压缩（hh 36→30/th 30→26/starH 20→18/sH 18→16/间隙-1px）恢复 2 行（实测 twoRowCap 55≥54）。
- **⚠️ infra 重大发现（cwd 盘符大小写）**：bash cwd `/d/…`（小写）下 vitest 模块图把 `src/platform/context` 实例化两次——setup.ts 绑定与测试图分离 → loadInventoryRaw/saveInventory 整体 try/catch 静默回退 defaultInventory → UX_R2 T1 与 P0 T12/T22-24 假失败（纯 core 不涉 R3 文件）。**大写 `/D/…` 下全绿**；memory 既有「vitest cwd 大写」纪律升级为「必须验证 setup 绑定可达测试图」哨兵法（setup.ts 临时 console.log + isPlatformCoreBound 对照）。
- **E2E**：新建 `_e2e_fusion_star_selection_r3.cjs` **128/128 PASS**（20 步闭环 × 4 视口 844×390 DPR1/DPR3/420×210/1280×592；S1–S20 全程真实点击）；适配 `_e2e_fusion_ux_r2.cjs`（C 段全库存种子 + slots 对象化 E3/E4/F/G5 + I4 改 R3 满星重进语义）**160/160 PASS**；`_e2e_fusion_feedback_r21.cjs`（S3b/S5a/S7a/S8/S13d/S15d 同款）**184/184 PASS**。
- **门禁**：全量 vitest **193 files / 1741 tests 0 failed**（`/D/` cwd）；tsc 0；Web/Pages/WeChat 构建 EXIT=0；bundle-clean wechat+e2e PASS；repo-health 9/9。
- **范围**：未动 core/partInventory、5合1规则、库存经济、Garage 装配、Home/Matching/Battle/Result、Renderer、DPR、Physics、Pages；未部署 Pages；未重建 RC；未拥有隐藏/弱态语义 = RB7/RB5 已锁定的 R3 有意变更。
- 教训记录：同文件多 Edit 并行发送会互相覆盖（本 Queue 三次踩中）→ 必须串行编辑。
