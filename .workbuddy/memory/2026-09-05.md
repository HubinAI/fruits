# 2026-09-05 工作日志

## F-GARAGE-FUSION-UX-R2 交付（重构背包合成交互）

- **commit**：`398f10a854a2631e7fa2eaec9af9fd03452e4332`（15 files，+1949/−1192；foundation-02-wechat）。local=ref=remote=398f10a（三路一致，push 后验证）。
- **正式规则落地**：`fuseCategoryMaterials(inv, materials, cat, build, star, rng)` —— 5×同分类同星未装备（可混 defId，body 不参与，MAX_STAR=2 阻挡，产物仅来自官方 Registry）→ 1×同分类下一星；rng 可注入；原子落盘（校验全过后一次 saveInventory）。旧 `fuseSameStar` 标 LEGACY。
- **主流程**：自动放入 → 合成 = 2 次核心点击；`autoPickFusionMaterials` 优先级=未装备→当前星→副本多→defId 字典序（稳定可复现）；双击防重。
- **Host UI**：fusionSlots[5]/fusionResult/fusionGlow/fusionToken；`bfilter/backpack-select/fusion-slot:N/fusion-auto/backpack-fuse/backpack-test-material/fusion-result-dismiss` 全套 dispatch；切分类/离开/backpack 返回清槽；结果卡 0.8s 可跳过 + 金色高亮 + reload 保持。交互 id 命名铁律（`__fx`/`__inv` 新句柄须入 bundle-clean allowlist）继续生效。
- **测试**：`garageFusionUX_R2.test.ts` 28/28（T1–T28：core 规则/装备保护/自动放满/原子/rng 精确输出/MAX_STAR；host 状态机/还差提示/满5enable/双击防重/reload 持久/Build 不变/body 隐藏/切分类清槽/Result-adjust 保留；11/3/8 分类可达性+底栏不重叠+3 viewport+DPR1/3 几何一致；v1 迁移/旧 2★ cannon 保留）。`rcFusionTestEntryP0` 重写 17/17（R2 门控字符串守卫：`tmShow=(rcGrantTM||e2eProbeTM||devResetTM)&&backpackFilter!=='body'`）。删 `garageInventoryFusionR1.test.ts`、`_e2e_backpack_fusion.cjs`。
- **E2E**：`_e2e_fusion_ux_r2.cjs` §十 真实浏览器闭环 **160/160 PASS**（4 视口 844×390@dpr1/844×390@dpr3/420×210@dpr1/1280×592@dpr1.5；seed cannon2+hammer2+saw1 + only-spear Build；自动放入确定性 `cannon,cannon,hammer,hammer,saw`→移除/补回→合成→1★−5/2★+1→结果卡→reload 保持→Build 未变）。
- **门禁**：全量 vitest **190 files / 1698 tests 0 failed**（vmForks --maxWorkers=1）；tsc 0；四端构建 EXIT=0（build/build:wechat/build:pages/build:e2e）；bundle-clean（wechat/e2e/rc）三路 PASS；repo-health 9/9；RC 重建 **#398f10a dirty=false**，四方 SHA 一致。
- **范围**：未执行 F-WX-6 / resume / 其它 Queue；未部署 Pages。玩家体验（真机体感/结果卡节奏）留待人工点检。

## 🔴 环境坑：vitest 全量红先查 cwd 盘符大小写（双实例）

- **症状**：本 Queue 收尾时全量回归偶发/持续红，失败面不稳定（1→2→3 文件递增）：`demoPlayerRuntimeP0`（Error: PlatformCore 未绑定）、`q23Economy`（coin +0，持久化结算全挂）、`q31Stability`（loadInventoryRaw null / countAfter 不累加）。三者共同点=依赖 setup.ts 的 `bindPlatformCore(createWebCore())`。
- **根因**：Windows 下 Bash cwd 盘符大小写（`d:\0818new\最强水果` vs `D:\0818new\最强水果`）使 vite-node 把 `src/platform/context` 实例化两次 → setup 绑定落在实例 A、测试文件 import 读到实例 B → `bound=false`。setup 探针证实同 pid 执行但模块状态不可见。与代码/pool/测试无关。
- **判定/修复**：`cd /D/0818new/最强水果`（大写）后全量 190/190 0 failed。**未来 vitest 出现这类「PlatformCore 未绑定/库存读写 null」红测，先 `node -e "console.log(process.cwd())"` 看盘符大小写，统一大写再跑**。切勿为此改 src/setup 或加 try/catch 掩盖。

## F-GARAGE-FUSION-FEEDBACK-LAYOUT-R2.1 交付（合成页布局/反馈重构）

- **commit**：`dcd199b7afcc15d01501b66ca211e8bc6ac8e47b`（5 files，+1979/−263；foundation-02-wechat）。local=ref=remote=repo-health 9/9 全 PASS；RC 重建 **#dcd199b dirty=false**，badge/rc-build.json/runtimeInfo/HEAD 四方一致。
- **统一布局单源**：`computeFusionLayout` 一个函数供给 draw+hitArea 的七区（header/页签/状态行/卡网格/分页/材料栏/动作区）；`drawBackpackPage` 每帧一次调用分发。禁重复手算坐标。
- **顶部**：RC badge 移页头右缘独立位（`fusionBadgeSpot` 由 drawBackpackPage 每帧算，drawBuildBadge 在 metaPage==='backpack' 时消费；默认全局 areas.badge 锚点落在 tab 行会 E1 overlap——4 视口全消除）；测试材料×5 语义锚 `right ≤ menuButton.left − 6` + `!isBody`。
- **short(420×210) 卡**：修复为三行 .20/.52/.82（L1 名/L2 星/L3 状态），主 15fs/次 13fs × fontScale 0.8 → 可见 12.0/10.4（满足主≥12/次≥10）；normal 分支不变。
- **材料栏/反馈**：五槽真实微图标（`drawPartIcon(micro=true)` → `fusionIconMicro(defId,…)` 12 部件 defId 专属）+ 短名 + ×N（同 defId 仅首槽右上）；点槽移除；加入/移除/自动放入行内 toast（`fusionToastFor(defId,'add'|'remove'|'auto')`）+ `showFusionFlash` 卡/槽闪 150–250ms；N/5 实时。
- **状态机/结果层**：主按钮 还差N件(disabled)→合成→合成中…(fusionPending 260ms)→结果卡；结果卡=真实产出图标+「消耗：5件战斗1★」+「获得：名2★」+「点击任意处继续」(≥0.8s)；dismiss（`fusion-result-dismiss`）→ `fusionJumpTo=product` → draw 时非 dispatch 分页聚合并消费 → 产物卡「新获得」金呼吸 2s + 星级数量立即可见；token 防 late-callback。
- **测试**：新增 `garageFusionFeedbackLayoutR21.test.ts` T1–T22 正式套件 28/28（几何/drawOps/像素断言：badge×tab no-overlap、七区两两相交=0、DPR1/1.5/3 几何恒等、卡内文字半高边界、单主状态、槽图标签名可区分、N/5 全程同步、还差N件、结果卡同屏、dismiss 跳页、双击防重、reload 持久、普通构建零调试入口、卡片全集==Registry）；旧 `garageFusionUX_R2` 适配（parseBarCount `/^([0-5])\/5$/`、T24 pending sleep 340）；`rcFusionTestEntryP0` source-guard 适配（isBody 语义 + 方法边界截取）。
- **E2E**：`_e2e_fusion_feedback_r21.cjs` 16 步 ×4 视口 **184/184 PASS**（844@dpr1/844@dpr3/420@dpr1/1280@dpr1.5）。**关键修正**：geometryGate 越界判定原拿 CSS viewport（420×210）比较 hitArea **logical 舞台坐标**（cssW/cssH 恒 844×390，CSS 缩放只在外层）→ 420×210 全误报越界；改为以 `getTransformInfo().cssW/cssH` 为越界边界后 4 视口全过。**教训：E2E hitArea 几何门禁边界=host logical 舞台（844×390），不是 CSS viewport**。
- **门禁**：全量 vitest **192 files / 1726 tests 0 failed**（vmForks --maxWorkers=1）；tsc 0；四端构建 PASS；bundle-clean rc+e2e 全 chunk PASS；repo-health 9/9。
- **范围**：临时取证 `_r21_evidence.test.ts` 与 `_dbg*.cjs` 已删（断言已并/覆盖于正式套件）；未部署 Pages；未动合成规则/库存/物理/渲染/生命周期。**电脑端真实体验录屏验收未执行（留待用户）**；iOS 未验证。停止待回执。
