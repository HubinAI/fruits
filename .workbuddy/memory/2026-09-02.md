# 2026-09-02

## F-DEBUG-GRANT-COVERAGE-P0｜修复 debug 授予 ×1 语义与「已领取」真实判定（commit 2fade0f，已 push，local=remote）

- **基线**：local=remote=`4e74bad`（foundation-02-wechat）。用户真实录屏实证：RC 包「测试：全部件×1」按钮已出现；点击显示"已领取"，但战斗分类仍大量武器「未获得」。
- **Must#1 改码前覆盖实证（关键结论）**：临时 harness 证明 `grantAllPartsOnce` 一次点击后**数据层全覆盖、无遗漏**（11 功能件 + 3 轮组 + 4 新车身均补齐，cannon 1→2 / laser 0→1 等，N=11）。**首次遗漏不在授予数据层，而在 UI 层两处**：①×1 语义实为无脑 +1 累加（违反四.1/T6/T7）；②`dev-grant-all` handler 把 `claimed` 无条件置 `devGrantClaimed=true`（五节 bug）→ 按钮"已领取"与真实库存脱钩。授予遍历本身已 Registry 驱动（满足三节单一来源），无需改遍历。
- **Fix #1（src/core/debugGrants.ts）**：
  - `grantAllPartsOnce` 改 ×1 语义：`getCount(store,defId,1)<1` 才 `addPart(store,defId,1,1)`（缺失补到 1，已拥有 ≥1 不再增）；`grantAllNewBodies()`+`grantAllNewMovements(store)` 保留；返回 `target.length`。
  - 新增 `hasAllOfficialDebugContent(inv?)`：遍历 `OFFICIAL_BODIES`（8）/ `OFFICIAL_MOVEMENTS`（3）/ `OFFICIAL_PARTS`（11），任一 `getCount<1`/`!isBodyOwned` 即 false；单一来源 = 正式 Registry，后续新增正式件自动纳入。
- **Fix #2（src/ui/canvasPlayerUIHost.ts）**：
  - import 增补 `getInventory`（partInventory）+ `hasAllOfficialDebugContent`（debugGrants）。
  - `dev-grant-all` dispatch 分支：删除无条件置真，改为仅 `this.actions?.onGrantAllParts?.()`（先授予，按钮态每次重算）。
  - `drawDevGrantEntry`：`claimed` 改 `hasAllOfficialDebugContent(getInventory())` 实时计算 → id/label 由真实库存决定。
  - 删除 `private devGrantClaimed=false` 字段（改注释说明运行时计算），同步顶部 stale doc comment。
- **修复后覆盖（Registry 单一来源，后续自动覆盖）**：Body 8（默认4恒拥有 + 新4点击后拥有）/ Movement 3（全部 ≥1）/ Functional 11（OFFICIAL_PARTS 全部 ≥1）。内部/测试 Body（boxBody/tallBody/heavyBox）与对手池专用/内部占位**不授予**（T5）。
- **冻结范围（未触碰）**：背包/合成/拖动/数值/正式奖励/经济/Garage 布局/Physics/viewport/iOS 冷启动/武器辅助顶层分类/确认按钮/Web 冒充微信。
- **验证矩阵**：tsc EXIT=0；targeted `tests/debugGrantAllPartsP0.test.ts` **T1-T16 全过（17 it）**；build:wechat 1,357.63 kB；**全量 vitest 1633 passed / 1 failed**——唯一失败 `tests/garageBuildBoardP0.test.ts:244` 源码守卫测试，经 `git show 4e74bad` 对照证实为既有基线失败（`drawGarageStripCards` 拉长导致窗口脆性，BASELINE 与 CURRENT windowHas 同为 false），**八.5 纪律不修、只登记**。
- **四构建**：web `dist/` 627.45 kB / pages `dist-pages/` 581.66 kB / wechat `dist-wechat/` 1,357.63 kB / wechat:rc 1,357.63 kB 全过。
- **RC 门禁（build:wechat:rc）**：repo-health 通过 → 受控树 clean → bundle-clean PASS（无内部句柄泄漏）→ badge `#2fade0f`（dirty=false）→ rc-build.json fullSha=`2fade0f0d9c4fd6137fcad7758a56b778bf22250` → 三方 SHA 校验通过；bundle 含 `dev-grant-all`(2)/`grantAllPartsOnce`(2)/`hasAllOfficialDebugContent`(4)。
- **三路 SHA**：local HEAD = remote `ls-remote` = server badge(rc-build.json fullSha) = `2fade0f0d9c4fd6137fcad7758a56b778bf22250` ✓。
- **单 commit**：`2fade0f`（3 files, +226/-84：debugGrants.ts + canvasPlayerUIHost.ts + debugGrantAllPartsP0.test.ts）+ push（4e74bad..2fade0f）。
- **纪律**：不部署 Pages、不需录屏（Queue 明确）；stop-and-wait。
- **下一步**：等用户真实微信预览包录屏回执（按钮"已领取"由真实库存计算、点击后所有正式武器不再「未获得」）确认，再开下一条 Queue。下一 Queue 基线 = `2fade0f`。
