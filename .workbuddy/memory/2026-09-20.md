# 2026-09-20

## Queue｜`PRODUCT-LOOP-P0-LEGACY-PROFILE-MIGRATION-AND-RUN-REACHABILITY`（调查完成，必改 1 判定为「不可可靠区分」⇒ 按 Queue 明写规则停止并报告）

### 1. 真实链（persisted profile → … → BuildDraft → Home / Run）
`localStorage['strongfruit.playerBuild.v1']`（带 `__v` 信封）
→ `readJsonWithVersion()`（`core/saveVersion.ts:51`）
→ `migrateLegacy('build', obj, v)`（`:74`）—— **对 build 是 no-op**（`upgrade_v0_to_v1` 的 build 分支
   `:98-102` 只注释「数据形状不变，仅补 `__v`」）；`CURRENT_SAVE_VERSION = 1` ⇒ **今天没有任何 build 迁移步骤**
→ `isBuildDraftShape()`（`core/buildPersistence.ts:63`）+ `draft.drive` 归一（`:44`）+ `validateSnapshot`（`:47`）
→ `loadEquippedDraft()` = `loadPlayerBuild() ?? defaultPlayerDraft()`（`product/playerLoadout.ts:289`）
→ 首页 `loadoutReading()` / Run 读同一份。

### 2. 旧 starter `pushRod` 的写入真源
`makeStarterDraft(bodyDefId, registry)`（`lab/buildEditorModel.ts:182-203`，Q26）
⇒ `functionalSelections = { front:'pushRod', frontMass:'cannon', top:'hammer', rear:'none' }`。
- 横屏正式玩法**无存档兜底**就是它：`playerGameRuntime.ts:442 / :464`
  `loadPlayerBuild() ?? silDraft('watermelonBody')`，`silDraft` → `makeStarterDraft`（`:1125`）。
- 产品侧 `defaultPlayerDraft()`（`playerLoadout.ts:272-282`）= 同一份 + 清空 `front`（R1-C `DEFAULT_CLEARED_SLOT`）。
- ⚠️ `makeStarterDraft` **至今仍写着 pushRod@front**（产品侧只在包装函数里清），Lab 函数未动。

### 3. ⚠️ 关键判定：`front` 槽**在旧横屏正式产品里是可编辑的** ⇒ 无法用结构本身区分「旧默认」与「玩家主动装备」
1. `editableSlots(body)`（`lab/buildEditorModel.ts:91-93`）返回 **全部** functional hardpoint（含 `front`）。
2. **面向正常玩家的车库**就用它：`ui/webDomPlayerUIHost.ts:429`
   `for (const hpId of editableSlots(body))`；`main.ts:4` 明写「正常玩家 UI 已抽到唯一 PlayerUIHost 边界
   （WebDomPlayerUIHost / CanvasPlayerUIHost）」⇒ 不是 debug 面板。`canvasPlayerUIHost` 同构
   （`garageChipDefs` / `garageOptions(state, garageSelected)`）。
3. 旧横屏 Built Editor 的槽位卡片逐个渲染 `editableSlots`（`main.ts:678`），应用时写
   `d.functionalSelections[selSlot] = defId`（`:756`）。
4. `playerGameRuntime.applyBuildEdit` 对**任意** `slotKey`（含 `front`）写
   `functionalSelections[slotKey]` + `functionalStars[slotKey]`（`:299-305`），并经
   `savePlayerBuild(this.draftA)`（`:651`）**落到与当前产品同一个 key**。
5. `pushRod` 在候选池里：`core/partOptions.ts:24` `{v:'pushRod',t:'推杆'}` ⇒ ∈ `OFFICIAL_PARTS`；
   且 ∈ `partInventory.ts:36 STARTER_PARTS` ⇒ `canEquipPart('pushRod',1)` 恒真 ⇒ 玩家能主动装上/卸下。
6. 入口身份：`build/branchDevEntry.ts:6` 「`/` 解析为 `index.html`（**正式横屏游戏**）」、
   `:30` 「`/index.html` —— **旧横屏正式游戏**（`npm run dev:legacy`）」；且 **R1-C 之前根路径 `/` 指向它**
   ⇒ 真人 profile 极可能出自该时代。

**⇒ 按必改 1 的明写规则**（「如果不能可靠区分用户主动装备与旧默认：停止这一部分并报告，不允许粗暴删所有
pushRod」）**停止迁移实现，上报待裁决**。未做任何存档改动。

### 4. 手头唯一候选判别式（已评估，未采用）
所有**玩家侧槽位写入路径**都同时盖星级印记（`main.ts:757-758`、`playerGameRuntime.ts:303-304`、
`canvasPlayerUIHost.ts:1291-1292`），而 `makeStarterDraft` **完全不写 `functionalStars`**。
⇒ 候选条件：`functionalSelections.front === 'pushRod'` **且** `functionalStars?.front === undefined`。
**已知缺口**：Q22（星级概念引入）之前的旧 profile 无法区分；且这是 Queue 未授权的**新判别机制**。

### 5. 其它必改的现状核对（未改码）
- **必改 3（Reachability Gate）**：fresh + cannon 基线**已达标** —— 既有 `e2e:product-loop`
  驱动真实鼠标打完一局并抓到「第一场真实战斗」+ 首次 3选1（`B5` / `E*` 段），R1-C 实测 `front` 留空
  → COMPLETE 4/4。**唯一缺口 = 未迁移的 legacy profile**（front 挂推杆 ⇒ R1-C 实测第一场 FAILED）。
- **必改 4**：未触发（fresh 基线第一场不失败，不需要换低压 Encounter）。
- **必改 5**：本 Queue 未触碰该链路，`productRunBuildLoadoutCompat` 套件重跑确认仍全绿。
- **必改 2**：`fresh` starter（front 空）**已经就是**当前规则真源 `defaultPlayerDraft()`；迁移一旦获批，
  其产物必须与该函数同源（不得复制一份平行实现）。

### 6. 用户裁决 ⇒ 收紧判别式迁移（已实现）

上报后用户选择「**收紧判别式迁移**」。判别式 = **两个条件同时成立**：

| # | 条件 | 可靠性来源 |
|---|---|---|
| ① | `sel.front === makeStarterDraft(body, registry).functionalSelections.front` 且 `sel.frontMass` 同 | **签名直接取自真源函数**（不抄常量表 ⇒ 不产生第二份真源） |
| ② | `draft.functionalStars?.front === undefined` | **所有**玩家侧槽位写入路径都同时盖星级印记（`main.ts:757-758` · `playerGameRuntime.ts:303-304` · `canvasPlayerUIHost.ts:1291-1292`），而 `makeStarterDraft` **完全不写** `functionalStars` |

任一不成立 ⇒ 原样返回、**一个字节不改**。**已知缺口**：Q22 之前的旧存档无法被 ② 区分（已上报）。

### 7. 实现（`src/product/playerLoadout.ts`）

- `export interface LegacyProfileMigration { migrated; reason: 'legacy-starter' | 'not-legacy-shape' | 'player-chosen' | 'invalid' | 'no-profile'; draft; selections }`
- `export function migrateLegacyStarterProfile(draft)` —— **纯判定 + 纯变换，不落盘**（`:350`）
- `loadEquippedDraft()`（`:383`）：有存档 ⇒ 迁移 + 命中时**落盘一次**；无存档 ⇒ 仍 `return defaultPlayerDraft()` **不写盘**
  （保住 `core/onboarding.ts` 的「全新账号」判定语义）
- ⚠️ **落盘点收口**：新增私有 `persistPlayerBuild(draft)`，把「① 玩家动作 `equipWeapon` ② 加载期归一化」两条语义
  收进**同一处** ⇒ `PL-03` 的 `savePlayerBuild(` 计数回到 **1**，**守卫原样通过、未放宽**
- `homePage.ts` 仅文档同步（零逻辑改动）

### 8. ⚠️ 本轮实测新发现（必须披露）

**`drive` / 轮径 / 轮组真的会改变战斗结果。** 第一版 E2E 把 `drive:'stationary'` + 非默认轮径塞进**战斗夹具**
⇒ `L6` 直接 `phase=FAILED pool=null battles=1/4`。拆成「战斗夹具」与「保留性夹具」两份后恢复。
**含义**：迁移**不碰**这些字段（必改 1 明令保留）⇒ 一个 `drive` 被设成 `stationary` 的旧存档，
即便清空 `front`，第一场**仍可能失败**。Reachability Gate 覆盖的是「标准 Cannon 产品基线」，
不是「任意用户自改配置」⇒ 后者属独立议题（已列入待裁决）。

### 9. 门禁（全绿）

`tsc` 零错误 · 新单测 **15/15** · 受影响 10 套件 **280/280** · R22b+产品守卫 **62/62** ·
新 E2E `e2e:product-legacy` **17/17** · 回归 home **30/30** / loop **51/51** / fail **34/34** /
star-power **20/20** / reward **48/48** / default-entry **86/86** · **全量 vitest 218 files / 2287 tests**
（基线 217/2272 ⇒ +1 文件 +15 用例）· 四构建全绿（wechat `game.js` **1,415.83 kB 与 P0 持平**）·
冻结目录 `git diff` **空** · `content.ts` / `contactRouter.ts` **零改动**。

**必改 3 达标 / 必改 4 未触发**：标准 cannon 基线**真的**打到 `d2-choice1` / `layer1`
⇒ 不换 Encounter、不做任何数值扫描。

### 10. 提交

- 功能 **`9078cc5`**（5 files, +1034/−5）· memory **`3637a2b`** · 四路 SHA
  `3637a2bc2f0c7e1f2a5ccb5c511885ddec81356a` · 交接文档
  `交接文档_2026-09-20_PRODUCT-LOOP-P0-LEGACY-PROFILE-MIGRATION-AND-RUN-REACHABILITY.md`（本地件，不入库）

---

## Queue｜`PRODUCT-LOOP-R2-A-REWARD-STACK-INVENTORY`（2026-09-20 复核，**未新开实现**）

### 1. 结论：本 Queue **早在 `c40977a` 已交付**（16 files, +2991/−852）

用户本轮把它作为「Queue A」重发（前置写「Queue 0 已通过」= 刚交付的 PLP0-LEGACY）。按纪律**先核对
当前 HEAD（`9078cc5`→`3637a2b`）下的真实状态**，而不是盲目重做 —— 因为后续 `R2-B`/`R2-C`/`P0`/`PLP0-LEGACY`
四轮都叠在它之上，且**有意**改过它的两处边界。

### 2. 九条验收逐条（当前 HEAD 机跑证据，**全部成立**）

| # | 验收 | 当前证据 |
|---|---|---|
| 1 | fresh profile `cannon ★1 ×4` | `FRESH_STACK_SEED`（`playerGrowth.ts:149`）· reward E2E `A1/A2/A3` |
| 2 | COMPLETE 奖励 3 选 1 | reward E2E `C3`（逐条 defId/name/★/预览）· `C4`（三条各自 href）· `C6`（真实像素） |
| 3 | 选 cannon → 4→5 | reward E2E `D2/D3`（localStorage 独立取证）/`D4`（5/5） |
| 4 | 选其它 Weapon 只增对应 stack | reward E2E `H3/H4` · loop `F8`（`cannon=4 spear=2 hammer=1`） |
| 5 | 重复领奖无效 | reward E2E `E1/E2/E3` · loop `G1`（`already-claimed`）/`G2`/`G3`（换一件也领不到） |
| 6 | FAILED 完全不增长 | reward E2E `I1/I2/I4`（FAILED 专属图形 0 像素）/`I5` |
| 7 | old Profile migration 不丢数据 | 单测 `PG-07..PG-10`（含 `ownedParts.v1` 数组 / 无 `__v` 信封 / 脏数据夹紧） |
| 8 | Equipped 引用始终有效 | 单测 `PG-11..PG-14`（`repairEquippedStack()` 只增不减）· loop `D4/D4b/E1` |
| 9 | Reload 后 count 一致 | reward E2E `K8/K9`（★1=0 / ★2=1 / 装的还是 ★2，全部真实持久化） |

**门禁实跑**：`tsc --noEmit` 零错 · 定向 5 套件 **184/184**（`playerGrowthR2A` 17 · `productLoopRunReward` 31 ·
`productLoopEndToEndPlayerLoop` 29 · `productLoopHomeGarage` 33 · `portraitRunPage` 74）·
`npm run build` ✅ · `build:pages` ✅ · `e2e:product-reward` **48/48 PASS** · `e2e:product-loop` **51/51 PASS**。

### 3. ⚠️ 用户新 Queue 与已交付版的**三处差异**（都是「后续轮次有意越过 R2-A 边界」）

| 新 Queue 说 | 当前实际 | 性质 |
|---|---|---|
| 必改 7「此 Queue **只显示**，不提供合成按钮」 | Garage **已有**合成按钮（`fusable`/`maxStar`） | **R2-B（Fusion Queue）有意取代**，非回退 |
| 禁止「Fusion」 | 已做（5 合 1，R2-B） | 同上 |
| 禁止「Damage star bonus」 | 已做（`starDamageMultiplier`，R2-C） | 同上 |

⇒ 这三条**不能**当作「R2-A 未完成」，否则会把 R2-B / R2-C 整轮回退。

### 4. ⚠️ 唯一的**真实字面缺口**：必改 1「**必须增加 schema version**」

- 现状：`CURRENT_SAVE_VERSION = 1`（`core/saveVersion.ts:21`，**从未提升**）；但**机制齐全** ——
  `__v` 信封（`STAMP_KEY`）+ `migrateLegacy(kind, obj, fromVersion)`（`:74`）+ 真实 key 级迁移先例
  （`STORAGE_KEY_V1 → V2`，`partInventory.ts:113-114/164-170`）。
- R2-A 交付时的**刻意决策** =「沿用 `__v` 信封，`CURRENT_SAVE_VERSION` **不动**」（因形状未变：
  `PartInventory` 本来就是 `(defId,star)→count`）。R2-B 加 `three/four/five` 时也选了
  「可选字段 + 缺省 0 ⇒ 旧档零迁移成本」，同样没提版本。
- **两种解释**：(a)「存档带版本 + 有迁移入口」已满足（key 名 `.v2` + `__v`）；
  (b) 字面要求把版本号**从 1 提到 2**。
- 实现 (b) 的形状（已评估、**未执行**）：`CURRENT_SAVE_VERSION = 2` + 新增 no-op
  `upgrade_v1_to_v2(kind, obj) => obj`。**安全性已核**：`migrateLegacy` 对
  `fromVersion >= CURRENT_SAVE_VERSION` 直接早退（`:75`）⇒ 已发布的旧客户端读 `__v:2` 也不会坏。
  ⚠️ 但它是**全局**版本号，同时覆盖 `build` / `inventory` / `progress` / `onboarding` 四个 kind
  ⇒ 属「改动前必须确认」。
- ⚠️ **本轮找到的硬冲突（这是上报的核心依据）**：`core/saveVersion.ts:20` 的**契约注释**明写
  「当前存档格式版本（单调递增；**下次破坏性变更 +1** 并在 `migrateLegacy` 增加 v(N-1)→vN 步骤）」
  —— 而 R2-A（建立 stack 表达）与 R2-B（加 `three/four/five` 可选档）**都是非破坏性扩展**
  （旧档零迁移成本），按该契约**恰恰不该 +1**。
  ⇒ 用户 Queue 的「**必须**增加 schema version」与代码库既定契约**直接矛盾** ⇒ 必须由用户裁决，
  **未擅自改**（存档版本号影响所有玩家存档 + 旧横屏共用同一 key）。
- 另一侧证据（支持「已满足」）：`readJsonWithVersion()` 把「无 `__v`」读作 **0**（`:51`），
  `migrateLegacy` 是**统一迁移入口**（`:74`），且有真实 key 级升级先例
  （`ownedParts.v1` 数组 → `ownedParts.v2` 映射，`partInventory.ts:164-170`）；
  测试 `tests/q27SaveVersion.test.ts` 全部跟随 `CURRENT_SAVE_VERSION` 常量（不硬编码 1）⇒ 提版本号不会让它们红。

---

## Queue｜`PRODUCT-LOOP-R2-B-FUSION-STAR-PROGRESSION`（2026-09-20 复核，**未新开实现**）

### 1. 结论：本 Queue **早在 `16a221f` 已交付**；11 条验收 + 7 条必改在当前 HEAD 下成立

### 2. 验收 11 条 → 机器判据

| # | 验收 | 判据 |
|---|---|---|
| 1 | 4/5 不可合成 | `FB-01`（预检不过 + 零副作用）· reward E2E `K2`（按钮在 DOM 里**根本不存在**） |
| 2 | 5/5 可合成 | `FB-02` · reward `K1`（「可合成」徽标 + 真实按钮） |
| 3 | ★1×5 → ★2×1 | `FB-03` · reward `K3`/`K4`（★1 卡消失、★2 是新卡） |
| 4 | source 正确扣 5 | `FB-03`（★1 归零）· `K3`（`countAfter=0`） |
| 5 | 不同 part 不可混 | `FB-05`（cannon 5 件不影响 hammer 的 4 件） |
| 6 | 不同 star 不可混 | `FB-06`（合 ★1 不消耗 ★2） |
| 7 | equipped 被消耗时自动升级引用 | `FB-08`（**含 Build 真的落盘**）· `FB-09`（没被合空 ⇒ 一字不动）· `FB-11`（写不进 Build ⇒ **整体回滚**，结构上不可能出现无效引用） |
| 8 | 5★ 不可继续 | `FB-12`（零副作用）· `FB-16`（`fusable=false` / `maxStar=true`）· `GARAGE_MAX_STAR_LABEL='已满星'`（`homePage.ts:108/737`） |
| 9 | ×10 一次只消耗 5 | `FB-07`（★1 = 10 → ★1 = 5 + ★2 = 1，绝不连锁）· reward `K7` |
| 10 | Reload 一致 | `FB-14`（star/count/equipped 三样）· reward `K8`/`K9` |
| 11 | old legacy Fusion 不得影响正式产品页面 | 产品侧 import 表只有 `../core/*` + `../lab/buildEditorModel` + `../platform` + 产品兄弟模块 + 图片资源 —— **无** `../ui/*` / 旧 Garage 组件 ⇒ 结构上接不到 |

**必改 7 条**：① 统一 Fusion Domain → `canFuseStack()`（`playerGrowth.ts:343`）+ `fuseStack()`（`:417`），`FB-04` 钉「所有 Weapon 一视同仁」；源码**无** `if (partId === 'cannon')` ✅ ② 不混合 → `FB-05`/`FB-06` ✅ ③ 装备中可参与 → `FB-08` ✅ ④ Garage 最小交互 → reward `K1`/`K2` ✅ ⑤ 不连锁 → `FB-07` ✅ ⑥ 5★ 终态 → `FB-16` + 「已满星」badge ✅ ⑦ 持久化 → `FB-14`/`K8`/`K9`，UI **不自己维护** Fusion state ✅

### 3. 门禁实跑

`tsc --noEmit` 零错 · targeted 6 套件 **204/204**（`productFusionR2B` **20** · `playerGrowthR2A` 17 ·
`productLoopHomeGarage` 33 · `productLoopRunReward` 31 · `productLoopEndToEndPlayerLoop` 29 · `portraitRunPage` 74）·
garage smoke `e2e:product-reward` **48/48** · `e2e:product-home` **30/30**。

### 4. ⚠️ 唯一冲突：本 Queue 的「**禁止再次出现 `MAX_STAR = 2`**」

- 现状：`MAX_STAR = 2` **仍在** `core/partInventory.ts:426`，是**旧横屏 Garage 融合规则**的策略上限
  （Q22 起**冻结**；`:417-424` 注释明写「旧横屏测试都把 `MAX_STAR === 2` 当域不变量断言」）。
- 产品侧**完全不依赖它的值**：`GROWTH_MAX_STAR = INVENTORY_MAX_STAR = 5`（`playerGrowth.ts:319`）；
  唯一的间接接触是 `stackThreshold()` 读 `canFuse().need`（无论走不走 `maxStar` 分支都返回 5）。
- ⚠️ **本 Queue 自己另有一条与之相反**：「不恢复旧横屏 Fusion UI…**可以复用纯逻辑/数据能力**；
  **禁止把旧横屏合成页面整体接回来**」⇒ 该条**承认**旧横屏代码/测试继续存在。
- ⇒ 最合理读法：用户要的是「**产品侧**不得再写死 1/2 星上限、必须数据驱动」——
  这一点已由 `FB-13`（★1..★4 都能合、★5 是上限，**不是只支持 1★→2★**）钉死。
- ⚠️ 若按字面**删除/改写 core 的 `MAX_STAR`**：会改旧横屏融合行为、并让 Q22 系列把
  `MAX_STAR === 2` 当域不变量的测试全部变红 ⇒ 与「冻结」及本轮「不触碰旧横屏」冲突。
- ⇒ **已上报待裁决，未擅自改**（连同 Queue A 的 schema version 一条）。

---

## Queue｜`PRODUCT-LOOP-R2-C-STAR-POWER-END-TO-END`（2026-09-20 复核，**一处判据强化**）

### 1. 结论：本 Queue **早在 `9e4e88c` 已交付**（12 files, +1522/−12）

7 条必改 + 12 条验收在当前 HEAD（`5d47d98`）上**全部成立**。之后 `P0` / `PLP0-LEGACY` 两轮叠在它之上，
本 Queue 的核心链路（星级 → 真实伤害）未被破坏。

### 2. 逐条机器证据（摘要）

| 必改 | 判据 |
|---|---|
| ① 星级**只**加 Weapon Damage，`1 + 0.25×(star−1)` | `starDamageMultiplier`（`buildSnapshot.ts:68`）· 单测 SP-01/SP-02/SP-03（正式 10 件武器**逐项**非伤害参数逐字不变）· E2E `E6`（`变了 projectileDamage: 80 → 100 · 其余 5 项逐字相同`） |
| ② 正式 Weapon Def 冻结，星级走 Profile Overlay | `core/content.ts` 零改动；无 `cannon_star2`；E2E `E3` 读到 `cannon@frontMass ★2 damage=100` |
| ③ Run-local Buff 与永久星级分离 | SP-06（overlay **只重映射 defId、保留 star**）；`playerSnapshot` = Runtime Weapon Star 唯一真源（`runBattleRuntime.ts:376-380`） |
| ④ Garage 提前告诉玩家收益 | `weaponEntries` 的 `damage/damageNext/damageText`（`playerLoadout.ts:451-473`）· E2E `A2`/`D1`/`D3`（真实 DOM 文本 `攻击 80 → 100`） |
| ⑤ 产品正式入口完整闭环 E2E | `tests/_e2e_product_star_power.cjs`，A1→F1，**21/21** |
| ⑥ P0 兼容性不回归 | 守门读 `install.defId` + `category==='weapon'`（`runCompatibility.ts:fullRunCompat`）——**星级是独立字段 `install.star`，不改 `defId`** ⇒ 「partId+star 被当成新武器 ID」**结构上不可能**；`e2e:product-fail` 34/34 |
| ⑦ 失败链回归 | `e2e:product-fail` `E2`：`BUILD_KEY`/`INV_KEY`/`CLAIMS_KEY` **三件套逐字节不变** |

### 3. ⚠️ 本轮唯一动作：E1b（DAY = 1 从「代理判据」升为「字面判据」）

- **缺口**：必改 5 STEP 6 逐字要求「必须确认 DAY = 1」，但原 `E2` 只断「两局同 `day` / 同 `nodeId`」——
  这只是**代理判据**（脚本起点若漂到第 3 天，同 day 仍成立）。
- **根因（实测）**：第一场战斗节点是 `d2-battle1`，其 `day` 字段 = **2**
  （E2 detail 打印 `Run1 起点 day=2 node=d2-battle1`）⇒ 战斗采样**读不到**「新局起点 = DAY 1」。
- **修法**：`enterRunAndDrive()` **本来就采了** `runStart`（进入 Run 的瞬间探针），但从未被断言 ⇒
  新增 `E1b` 用它字面断言 `DAY=1 / battles=0 / build=[] / modifier=null`。**零新增采集成本**。
  实测输出：`DAY=1 battles=0 build=[] modifier=null`。E2E **20/20 → 21/21**。
- 同时改正了 E2 上方那段与实测不符的注释（原文「脚本的起始节点不等于第 1 天」）。
- 边界：`RUN_FIRST_DAY = RUN_SCRIPT[0].day = 1`（`runScript.ts:168`）；单测 `portraitRunPage` RP-06
  已有 `expect(s.day).toBe(RUN_FIRST_DAY)` + `.toBe(1)`。E2E 那份字面量**刻意独立声明**
  （黑盒不引用被测方常量）。

### 4. 门禁实跑

`tsc --noEmit` 零错 · targeted growth **9 套件 239/239** · `e2e:product-star-power` **21/21** ·
`e2e:product-reward` **48/48** · `e2e:product-loop` **51/51** · `e2e:product-fail` **34/34** ·
全量 vitest **218 files / 2287 tests** · `build` / `build:pages` / `build:wechat`（`game.js` 1,415.83 kB）。

⚠️ **全量 vitest 首跑 9 个偶发失败**（1 file / 9 tests，旧横屏 `garageFusion*` 系列），发生在
**紧接前一个 msedge headless E2E 之后** ⇒ 空闲复跑 **218/2287 全绿**（exit 0，97.48s）+
该 4 文件单独复跑 **71/71 全绿** ⇒ **资源竞争型偶发**，非遗留真红、非本轮引入
（本轮只改 1 个 `.cjs` + memory，未触碰任何 `.ts`）。**印证 user memory 既有记录**。

### 5. 沿用的待裁决 4 条（未变，见 §9）

① `validateSnapshot` 星级能量盲（`:114` 用 `def.energy` vs `:48` 的 `computeEnergy` 用 `starTierEnergy`，
Q22 漏改）② ★2 炮 33 能量超 `bananaBody` 90 容量是否要「容量提示」③ `saw` 边界（当前结构上不可达）
④ 下一条 Queue 是否开 Body/Gadget 成长。

---

## 会话交接｜本窗口收口（2026-09-20 21:5x）

**交付两份跨窗口交接件**（**本地件，不入库**，与 `交接文档_*.md` 同级纪律）：

- `交接文档_2026-09-20_SESSION-HANDOFF.md`（~17.3 KB）—— 跨窗口续接件：仓库快照 / 本窗口 5 个 commit
  逐条 / 门禁基线对照表 / 结构性不变量 / **Top 10 陷阱** / 15 条待裁决全表 / 3 条已知未修 / 开工建议 /
  关键文件速查表。
- `新窗口交接指令_2026-09-20.md`（~5.4 KB）—— **可直接整段粘贴到新窗口**的第一条消息
  （自包含：项目身份 → 必读文件 → SHA 核对 → 当前状态 → 纪律与陷阱 → 「回报三项 + 停下等指令」）。

**本窗口 commit 全表**（截至收口，HEAD = `115fe52`）：

| commit | 类型 | 内容 |
|---|---|---|
| `9078cc5` | feat | PLP0-LEGACY：旧 starter profile 迁移 + Run Reachability Gate（5 files / +1034 −5） |
| `3637a2b` | docs(memory) | PLP0-LEGACY 收口：契约固化到 REF §10a–§10i（3 files / +250 −4） |
| `1a9f4a0` | docs(memory) | R2-A 复核（未新开实现）+ schema version 冲突上报（+66） |
| `5d47d98` | docs(memory) | R2-B 复核（未新开实现）+ `MAX_STAR = 2` 冲突上报（+44） |
| `115fe52` | test+docs | R2-C 复核：`E1b` 判据强化 + REF §8g 修订 + MEMORY 压缩（4 files / +190 −131） |
| （本次） | docs(memory) | `MEMORY.md` 二次压缩（17.8 → 12.5 KB）+ 本交接记录 |

**⚠️ 本窗口最重要的三条经验（已固化）**：

1. **用户连贴的 Queue A/B/C 都是「历史 Queue 重号」**（`PRODUCT-LOOP-R2-A/B/C`，分别早已交付于
   `c40977a` / `16a221f` / `9e4e88c`）⇒ 正确处置是**先核对当前 HEAD 的落地状态**，而不是盲目重做。
   实测三条的验收 + 必改在当前 HEAD 下**全部成立**，仅 R2-C 有一处**代理判据可升为字面判据**。
2. **用户新 Queue 里出现过与项目冻结决策直接冲突的条目**（R2-B「禁止再次出现 `MAX_STAR = 2`」、
   R2-A「必须增加 schema version」）⇒ 按「先调查后修改 + 禁止臆测」**上报待裁决，未擅自动 core**。
3. **`MEMORY.md` 有注入体积上限**（超 ~12–13 KB 会在会话注入时被截断，本窗口收到过系统告警）⇒
   已做两轮压缩，体例固化为「**只放不变量 + 陷阱 + 下一步；实测数字指向当日 log，细节指向 REF 与交接文档**」。
   压缩过程中**新增**了「会话交接」导航行与 `§2b` 的**定位锚点行**（避免锚点被压掉后无法 grep 定位）。
