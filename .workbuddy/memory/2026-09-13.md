# 2026-09-13 工作日志

## PRP-F2-R1-ISOLATED-UPGRADE-VALIDATION（单变量收紧 + Cannon 真实 Burst）

### 0. 为什么做这一轮（真人录像的两个结构问题）

上一轮 PRP-F2（`ca3fb43`）技术闭环成立，但**真人验收未通过**：

1. **流程不是单变量实验**：第二场结束后仍能进 CHOICE、继续 +Day → 出现 `DAY 8/7`，
   同一强化可反复叠加。三个强化无法在**相同基础条件**下独立验证。
2. **双联炮是 shotgun workaround**：借用 `shotgun.fanAnglesDeg: [-4,4]` **同时**齐射两发，
   正常速度下两发轨迹高度重叠，玩家无法自然理解为「连续两发」。

真人裁决：重型弹头**方向通过**（冻结）；快速装填有趋势（待独立复验）；双联炮**不通过**（换表现机制）。

---

### 1. 必改 1｜流程收紧为严格单变量（`runPageState.ts`）

新增状态字段与常量：

- `RunPageState.battlesCompleted: number`（0 = 未打；1 = 只打完基础；2 = 验证结束）
- `RUN_MAX_BATTLES = 2`、`RUN_RESTART_LABEL = '重新开始验证'`
- `runVerificationComplete(s) => s.battlesCompleted >= RUN_MAX_BATTLES`

`pressRunAction` 的 RESULT 分支改为**分岔**：

| 场次 | 行为 |
|---|---|
| 第一场（`battlesCompleted < 2`） | `→ CHOICE`（保留**唯一一次**改装机会） |
| 第二场（`>= 2`） | **`return createRunPageState(ctx)`** = 全新 Run（DAY 3 / `buffs: []` / `modifier: null` / `battle: null`） |

- `runActionLabel`：终局 RESULT → `'重新开始验证'`；其余阶段文案不变。
- `chooseRunBuff` 加守卫：`if (s.modifier !== null || s.buffs.length > 0) return s;`
- `finishRunBattle` 第三行叙事按场次分岔：第一场「你发现了一次改装机会……」/ 终局「本次改装的验证到此结束。」
- `finishRunBattle` 每次 `battlesCompleted + 1`。

**结构后果**：`DAY 8/7`、多 Buff 累计、同一 Buff 重复堆叠在**结构上不可能**（不靠运行期检查）。

宿主侧（`runPage.ts`）无需新增清理逻辑：`apply()` 既有的
`if (next.phase === 'IDLE' && this.battle) this.endBattle();` 会在重置时 `dispose()` 旧运行时
→ Projectile / Contact / Battle Runtime 全清。

---

### 2. 必改 3｜Cannon 最小可复用 Burst（本 Queue **唯一**触碰正式 gameplay 的改动）

`src/battle/cannonBehavior.ts`：

- `CannonParams` 新增两个**可选**字段：`burstRounds`（默认 `1`）/ `burstIntervalMs`（默认 `0`）。
- `readCannonParams` 用新增的 `optNum()` 读取这两个字段：**缺失/非法 → 取默认值，不抛错**。
  ⚠️ 这是刻意的：6 个基准参数保持「缺失即抛错」，只有 burst 这两个可选 →
  **正式 `content.ts` 不用加任何字段**，默认行为逐帧不变。
- `stepFixed` 拆为三段：`连发间隔 → 主冷却 → emitRound()`
  - 连发期间**不并行递减冷却**（冷却只在最后一发之后开始计时 → 「一次攻击 = 一个整体」）；
  - `emitRound()`：打满 `burstRounds` 才进 `cooldownMs`，否则进 `burstIntervalMs` 发间隔。
- 新增 getter：`burstRounds` / `burstIntervalMs` / `roundsFiredSoFar` / `burstGapRemaining`。
- 连发的**每一发**都走同一条 `fire()` → 真实 projectile / 碰撞 / 伤害 / recoil / 生命周期。

**实测时序**（`FIXED_DT_MS = 1000/60`，口径 = 「新弹丸出现的固定步号差」）：

| 场景 | 第 1→2 发步差 | 换算 |
|---|---|---|
| 正式 `cannon`（`burstRounds=1`） | **60 步** | = 冷却 1000ms（回归零变化） |
| 双联炮（2 发 / `burstIntervalMs=100`） | **6 步** | = 100ms 连发间隔 |

**双联炮 overlay 改写**（`runModifiers.ts`）：

```
旧：behavior 'shotgun' + { fanAnglesDeg: [-4, 4] }   ← 同步齐射、轨迹重叠（真人判不通过）
新：behavior 'cannon'  + { burstRounds: 2, burstIntervalMs: 100 }
```

- 弹道**同向**（实测两发出生 y 差 < 3px），不再靠散射把两发分开；
- 渲染标记回到正式 cannon 弹丸（不再是 shotgun runtime 固定写死的 `'tracer'`）。

✅ 回归：`cannonScenarios / contentCannon / cannonBehavior / contactRouterProjectile / battleIntegration` = **27/27** 全绿。

---

### 3. 冻结项确认（本 Queue 未动）

- 重型弹头 `projectileRadius 16 / projectileMass 4 / recoilImpulse 90` —— **一个数未改**（本 Queue 只保证它在独立单 Run 里仍生效）。
- 快速装填 `cooldownMs 400` —— **未调整**（真人复验后再判断）。
- PRP-R3 UI / 日志布局 / CHOICE 布局 / PRP-R5 Camera / Arena / world / spawn / enemy / Base Cannon /
  Movement / mass / 基础 Damage·HP —— 全部未动。
- 唯一新增的日志文案分岔 = 终局 RESULT 第三行（否则会说错「你发现了一次改装机会」）。

---

### 4. 门禁实测

| 门禁 | 结果 |
|---|---|
| `npm run typecheck` | **0 错** |
| `tests/portraitRunModifier.test.ts` | **16/16**（新增：正式 cannon 无 burst 字段冻结 / 真实时间差 60 vs 6 / 出生点同源 < 3px） |
| `tests/portraitRunPage.test.ts` | **46/46**（新增 RP-F2-08 终局停止 / RP-F2-09 重新开始验证=全新 Run / RP-F2-10 单变量守卫） |
| Cannon / Battle 回归（5 文件） | **27/27** |
| `e2e:run-page`（4 视口） | **315/315**（新增 R50–R54） |
| `e2e:default-entry` | 见下 |
| 全量 vitest | 见下 |
| 五路构建 + repo-health | 见下 |

E2E 关键实测（1280×720@1.5）：`R49` 窗口内最多 **2 发在飞**（第一场同窗口最多 1 发）·
`R50` 终局 `battlesCompleted=2` · `R51` `day=4/7`、`buffs=双联炮`（1 个）、主动作 `"重新开始验证"` ·
`R52` 新 Run `day=3 / buffs=0 / modifier=null / battles=0` · `R53` `phaseTrail` 从 `IDLE` 重开。

---

### 5. 交付回填

- **commit SHA = `1a3b080fa6b239a78e37df6b1dfb0ca755c06a17`**（`1a3b080`）
- 9 文件 / **+648 −83**；三路 SHA **四方一致**（`HEAD` = 本地 ref = `ls-remote` = `FETCH_HEAD`）。
- **正式 gameplay diff = 仅 `src/battle/cannonBehavior.ts`（+117 −9）**，即 Queue 授权的最小 burst Foundation；
  `src/core | physics | render | player | platform` diff = **空**。
- 门禁尾数：全量 vitest **202 文件 / 1927 passed / 0 failed**（上一轮 1923 → 本轮 +4）；
  五路构建 + repo-health 全 **EXIT 0**（`ok:true`）。

---

## PRP-F2-R2-FAST-RELOAD-PARAMETER-RECOVERY（快速装填 400 → 650）

### 0. 真人三段录像结论
- 重型弹头 ✅ 通过（冻结）；双联炮 ✅ 通过（冻结）；
- 快速装填 ✅ 机制可感知，但**数值明显过强**：`1000ms → 400ms` ≈ 2.5× 基础频率，
  把「远程开火 → 后坐 → 敌方接近 → 碰撞」整段正式物理节奏压缩掉，有形成稳定最优解的风险。

### 1. 改动（只有一处数值 + 断言同步）
- `src/lab/portraitBattleLab/runModifiers.ts`：`fastReload.behaviorParams.cooldownMs: 400 → 650`（≈1.54× 基础）。
  **未做多档扫描**（Queue 明令禁止 600/625/650/675/700 微调），就是一次大幅回收。
- 其余 overlay 一个数未动：重型弹头 `16/4/90`、双联炮 `burstRounds 2 / burstIntervalMs 100`。

### 2. 实测数据（一次性探针，跑完即删）
只有玩家（team `A`）开火 —— **敌方无炮**（`byTeam` 仅 `A`），所以开火间隔可干净归因。

| 口径 | 基础炮 | 快速装填 |
|---|---|---|
| `cooldownMs`（配置） | 1000 | **650** |
| 真实开火间隔（`weaponFire.timestamp` 差值，每个间隔都精确相等） | 1000.000000ms | **650.000000ms** |
| 600 帧（10s）内开火次数 | **10** | **16** |
| 整场（1600 帧上限）结束 | `A/hp`，剩余 **269.78/1100** | `A/hp`，剩余 **280.55/1100** |
| 频率比 | 1× | **1.538×**（回收前 2.5×） |

⚠️ **弹丸数增量会漏计开火**：`birthGaps` 实测出现 120/240 步（因为「弹丸命中销毁的那一步」常常
正好等于「下一发的开火步」，计数净变化为 0）→ 所以单测断言必须用 **`weaponFire` 事件时间差**，
E2E 断言必须用 **最小观测间隔**（漏计只会让间隔变大 → 最小值是稳健下界）。

### 3. 门禁实测
| 项 | 结果 |
|---|---|
| `tsc` | **0 错** |
| `portraitRunModifier` + `portraitRunPage` | **62/62**（16 + 46） |
| Cannon / Battle 回归（5 文件） | **27/27** |
| `e2e:run-page`（新增 R55–R57） | **318/318 PASS**（上一轮 315 → +3，只在首个视口跑满时序观测） |
| `e2e:default-entry` | **77/77 PASS** |
| 全量 vitest | **202 文件 / 1927 passed / 0 failed** |
| 五路构建 + repo-health | 全 **EXIT 0** / `ok:true` |

E2E `R55–R57` 关键实测（390×844@1，浏览器端真实开火间隔）：
`R55` 第一场基础炮 `modifier=null`、满耐久开局 · 观测 5 次开火 / 4 个间隔 / **最小间隔 985ms**；
`R56` 第二场 `modifier=fastReload` · `day=4` · `buffs=快速装填`（单变量隔离）；
`R57` **基础 985ms → 快速装填 647ms · 频率比 1.52×**（下界 480ms 用来证明「不再是 400ms 那一版」）。
（985 而非 1000 是 25ms 采样粒度的正常误差。）

### 4. 交付回填
- **commit SHA = `896086b3dccb00e4fa58db994fc7a39c4a1e0718`**（`896086b`）
- 6 文件 / **+301 −131**；三路 SHA **四方一致**（`HEAD` = 本地 ref = `ls-remote` = `FETCH_HEAD`）。
- **正式 gameplay diff = 空**（本 Queue 唯一源码改动 = `src/lab/portraitBattleLab/runModifiers.ts` +7 −2）。
- 顺带：MEMORY.md 压缩 **20204 → 17378 字节**（去重 + 交叉引用校正，分节 §1–§6 / §5.1–§5.5 完整）。

---

## PRP-BUILD-01-TWO-STEP-CANNON-BUILD（两层 Cannon Build：第一次选择决定第二次条件池）

### 0. 验证的核心假设
> **第一次强化会改变第二次强化的价值与期待 → 形成 Build，而不是连续拿两个互不相关的 Buff。**

流程升级为 `基础战斗 → 三选一① → 强化后战斗 → 条件池三选一② → 最终战斗 → 结束`，全程同一 Run Page。

### 1. 三条 Build 路线（第二层条件池 = 固定，不随机）

| 第一层 | 强联动 synergy | 安全通用 safe | 轻度转向 pivot |
|---|---|---|---|
| 重型弹头 | **动能爆发** kineticBurst | 紧急维修 emergencyRepair | 快速装填 fastReload |
| 双联炮 | **三连装填** tripleLoad | 紧急维修 emergencyRepair | 重型弹头 heavyShell |
| 快速装填 | **反冲蓄能** recoilCharge | 紧急维修 emergencyRepair | 双联炮 twinCannon |

⚠️ 第三格复用**第一层**的某一项 → 它的 `role` 仍是 `'base'`（`role` 描述「定义在总池里的身份」，
转向语义由**槽位**表达）。第一版不要用 `pool[2].role === 'pivot'` 做断言。

### 2. 必改 4 的接缝判定（本 Queue 最重要的调查结论）
反冲蓄能**不需要**修改任何正式模块 → **未触发停止条件**。三处干净接缝：
1. `PlanckBattleOrchestrator.onCombatEvent`（`planckBattleOrchestrator.ts:413`）是公开订阅口；
2. `WeaponFireEvent`（`cannonBehavior.ts:352`，**每次真实开火恰好一次**）与 `DamageEvent`
   （`combatEvents.ts:36`，含 `source`/`target`/`damageSource`/`behavior`/`relativeVelocity`）是正式既有事件；
3. `world.applyLinearImpulse` 是公开 API，且**正式 Cannon 自己就用它做 recoil**（`cannonBehavior.ts:363`）。

### 3. 架构：能力模块只拿**端口**，不拿编排器
两条 Lab 硬守卫逼出了更好的设计（都是真守卫，不是要绕过去）：
- **R22a-4（`tests/portraitBattleLab.test.ts`）**：正式编排器**只允许 `runBattleRuntime.ts` import**。
  新文件 `runBuildAbilities.ts` 因此**不 import 编排器**，改为声明极窄的 `RunAbilityPorts`
  （`subscribe` / `isFinished` / `facingOf` / `projectileMass` / `applyImpulse`），
  由 `RunBattleRuntime` 适配实现 → 「PRP ↔ 正式战斗栈只有 runBattleRuntime 相连」在**运行期**也成立。
  连带把 `readRunProjectileMass(orchestrator)` 放进 `runBattleRuntime.ts`。
- **G1-08（`tests/portraitBattleLabG1.test.ts`）**：`arenaConditionLines()` 只豁免
  `team|winner|loser|vehicle|snapshot|projectile|side|driver` 前缀的比较行，因此
  **`ev.source !== 'A'` 会被误判成 arena 分叉**；`ev.team !== 'A'` 不受影响。
  改法：用**命名队伍常量** `PLAYER_TEAM` / `ENEMY_TEAM`（`const PLAYER_TEAM: RunTeamId = 'A'`）。
  ⚠️ 不是放宽守卫，而是「队伍 id ≠ arena id」这件事本来就该用命名说清楚。

### 4. 关键设计
- **两层组合**：改武器的项按选择顺序**浅合并** `behaviorParams` → 一个确定性 overlay 部件
  `run.mod.<a>+<b>`（无改武器项 → `null`，不重映射）。`tripleLoad` 只抬 `burstRounds` 2→3，
  其余（含 `burstIntervalMs: 100`）全部沿用 → 「复用现有 burst Foundation」，无需新机制。
- **动能爆发**：`magnitude = KINETIC_BURST_GAIN(12) × 当前 projectileMass × relativeVelocity`。
  `projectileMass` 读自本局**真实 resolved 武器 def**（基础 1 / 重型弹头 4）→ 重弹天然更狠，
  **没有「重型弹头专属伤害」这种写死值**。方向 = 该次真实开火的炮口方向，作用点 = 敌车质心（整台撞开）。
- **反冲蓄能**：每 `RECOIL_CHARGE_THRESHOLD(3)` 次**真实开火**蓄一次 → 玩家车前向冲量 `45`。
  不变量可断言：`charge + chargesSpent × 3 === 真实开火次数`（**结构上排除定时器**）。
- **⚠️ 冲量在步边界施加**：`damage` 事件发在 `world.step` 的接触回调链里 → 回调内只**入队**，
  `RunBattleRuntime.step()` 在 `orchestrator.step` **之前** `abilities.flush()`（延迟 ≤1 步 ≈16.7ms）；
  `result` 非空时直接丢弃 → **不破坏「RESULT = 战场冻结」**。
- **紧急维修**：写独立字段 `repairBonus`（`+= round(playerHpMax × 0.25)`），**不改写**
  `battle.playerHp`（那是上一场的真实结果）→ `runCarriedPlayerHp = min(max, 剩余 + 补偿)`。

### 5. ⚠️ 本 Queue 暴露的既存体验问题（**只记录，不调参**）
Queue 冻结敌方 HP / 禁止数值平衡扫描，因此**未做任何调参**，但必须如实披露：
- 第一场（无强化）**确定性**收在 `269.78/1100` ≈ 24.5%（物理确定性 → 每次一样）；
- 第二场于是以 ≈24.5% 开局 → **三条路线都必然被打退**（浏览器 E2E 实测 `R48 开局 228.09/1100`，
  `R53d 上一场结束 0 → 终局按规则满耐久 1100`）；
- 后果：终局第三场按 `runCarriedPlayerHp` 的「<=0 → null」分支**满耐久开幕** ——
  好处是**两层 Build 能在满耐久下完整展示方向**（更好的验收条件），
  代价是「第一层强化后那场战斗」玩家看不到赢。
- 这是 **F2-R2 就存在**的余量问题（同一条 carry 规则），非本 Queue 引入。**留待真人裁决**。

### 6. 流程与账本的连锁变化（预期后果，非放开断言）
- `RUN_MAX_BATTLES 2 → 3` + 新增 `RUN_MAX_CHOICES = 2`；`RunPageState.modifier` 字段**删除**，
  改为查询函数 `runBuildIds(s)`；`buffs` 升格为「本局 Build 唯一来源」。
- 顶部进度节点随 day 前移：**DAY3 384/512 → DAY4 512/384 → DAY5 640/256**（总量恒 896）；
  强化图标行 **0 → 1 → 2** 个（每个 30×30：底色 756 + 高光块 144）。
- 日志累积：`IDLE 2 → RESULT 7 → IDLE 9 → RESULT 14 → IDLE 16 → RESULT 21`
  （每场 RESULT +5 = 2 敌情 + 3 结果；每次选择 +2 = 强化结果 + DAY 行）。
- 模型层账本新增 **RP-22b**（九个关键帧 + 两选三场的节点/图标字面量）；
  E2E 的 `LEDGER` 静态表改为 `ledgerExpect({day, stage, buffs, action, masked})`
  **按同源规则生成**（day 会变、图标数会变，静态表会退化成手抄）。

### 7. 门禁实测
| 项 | 结果 |
|---|---|
| `tsc` | **0 错** |
| Lab 守卫 + PRP 定向（6 文件） | **170/170** |
| 全量 vitest | **202 文件 / 1939 passed / 0 failed** |
| `build:portrait-lab` + `e2e:run-page` | **398/398 PASS**（F2-R2 为 318 → +80，见下） |

E2E 关键新增/变更断言（390×844@1 与桌面视口均通过）：
- `R32` 第一次选择 `choicePoolLayer=1` + 正是三项固定选项；
- `R50` 第二场结束 `battlesCompleted=2` **但 `verificationComplete=false`**（还不是终局）→ `R51/R51b`
  第二次选择 `choicePoolLayer=2`、池 = `tripleLoad / emergencyRepair / heavyShell`、**与第一次池不同**；
- `R52` Build = `[twinCannon, tripleLoad]`、DAY 5、顶部 2 图标；`R53b` 最终战斗
  `build=[twinCannon,tripleLoad]` 且能力不串味（kinetic/charge 均 false）；
- `R54` **三连装填窗口内最多 3 发在飞**（第二场双联炮同口径最多 2 发）→ 真实 projectile 而非视觉假弹；
- `R55e` 完整闭环轨迹 `IDLE→EVENT→BATTLE→RESULT→CHOICE→IDLE→EVENT→BATTLE→RESULT→CHOICE→IDLE→EVENT→BATTLE→RESULT`；
- `R56` 重新开始验证 → `build=[]`（Reset 后 Build 完全清空）。

### 8. 交付回填
- **commit SHA = `39f6af9b9e0bdfb3ca591c98e8d336192f39a645`**（`39f6af9`），基线 `65809c0`。
- 11 文件 / **+2216 −541**（含 1 个新文件 `runBuildAbilities.ts`）。
- 三路 SHA **四方一致**：`HEAD` = 本地 ref = `ls-remote` = `FETCH_HEAD` = `39f6af9…`。
- **正式目录零改动（硬证据）**：`git diff --stat 65809c0 39f6af9 -- src/core src/physics src/render
  src/player src/platform src/battle` → **输出为空**。本 Queue 全部改动落在
  `src/lab/portraitBattleLab/`（6 文件）+ `tests/`（3 文件）+ memory（2 文件）。
- 其它门禁：`tsc` 0 错 · Lab 守卫 + PRP 定向 6 文件 **170/170** · 五路构建 **EXIT 0** ·
  `repo-health ok:true` · `e2e:default-entry` **77/77 PASS**。
- `交接文档_2026-09-13_PRP-BUILD-01.md`（本地件，不入库）。
