# 2026-09-13 工作日志

## PRP-F2-R1-ISOLATED-UPGRADE-VALIDATION（单变量收紧 + Cannon 真实 Burst）

### 0. 为什么做这一轮（真人录像的两个结构问题）

上一轮 PRP-F2（`ca3fb43`）技术闭环成立，但**真人验收未通过**：

1. **流程不是单变量实验**：第二场结束后仍能进 CHOICE、继续 +Day → 出现 `DAY 8/7`，
   同一强化可反复叠加。三个强化无法在**相同基础条件**下独立验证。
2. **双联炮是 shotgun workaround**：借用 `shotgun.fanAnglesDeg: [-4,4]` **同时**齐射两发，
   正常速度下两发轨迹高度重叠，玩家无法自然理解为「连续两发」。

真人裁决：重型弹头**方向通过**（冻结）；快速装填有趋势（待独立复验）；双联炮**不通过**（换表现机制）。

---

### 1. 必改 1｜流程收紧为严格单变量（`runPageState.ts`）

新增状态字段与常量：

- `RunPageState.battlesCompleted: number`（0 = 未打；1 = 只打完基础；2 = 验证结束）
- `RUN_MAX_BATTLES = 2`、`RUN_RESTART_LABEL = '重新开始验证'`
- `runVerificationComplete(s) => s.battlesCompleted >= RUN_MAX_BATTLES`

`pressRunAction` 的 RESULT 分支改为**分岔**：

| 场次 | 行为 |
|---|---|
| 第一场（`battlesCompleted < 2`） | `→ CHOICE`（保留**唯一一次**改装机会） |
| 第二场（`>= 2`） | **`return createRunPageState(ctx)`** = 全新 Run（DAY 3 / `buffs: []` / `modifier: null` / `battle: null`） |

- `runActionLabel`：终局 RESULT → `'重新开始验证'`；其余阶段文案不变。
- `chooseRunBuff` 加守卫：`if (s.modifier !== null || s.buffs.length > 0) return s;`
- `finishRunBattle` 第三行叙事按场次分岔：第一场「你发现了一次改装机会……」/ 终局「本次改装的验证到此结束。」
- `finishRunBattle` 每次 `battlesCompleted + 1`。

**结构后果**：`DAY 8/7`、多 Buff 累计、同一 Buff 重复堆叠在**结构上不可能**（不靠运行期检查）。

宿主侧（`runPage.ts`）无需新增清理逻辑：`apply()` 既有的
`if (next.phase === 'IDLE' && this.battle) this.endBattle();` 会在重置时 `dispose()` 旧运行时
→ Projectile / Contact / Battle Runtime 全清。

---

### 2. 必改 3｜Cannon 最小可复用 Burst（本 Queue **唯一**触碰正式 gameplay 的改动）

`src/battle/cannonBehavior.ts`：

- `CannonParams` 新增两个**可选**字段：`burstRounds`（默认 `1`）/ `burstIntervalMs`（默认 `0`）。
- `readCannonParams` 用新增的 `optNum()` 读取这两个字段：**缺失/非法 → 取默认值，不抛错**。
  ⚠️ 这是刻意的：6 个基准参数保持「缺失即抛错」，只有 burst 这两个可选 →
  **正式 `content.ts` 不用加任何字段**，默认行为逐帧不变。
- `stepFixed` 拆为三段：`连发间隔 → 主冷却 → emitRound()`
  - 连发期间**不并行递减冷却**（冷却只在最后一发之后开始计时 → 「一次攻击 = 一个整体」）；
  - `emitRound()`：打满 `burstRounds` 才进 `cooldownMs`，否则进 `burstIntervalMs` 发间隔。
- 新增 getter：`burstRounds` / `burstIntervalMs` / `roundsFiredSoFar` / `burstGapRemaining`。
- 连发的**每一发**都走同一条 `fire()` → 真实 projectile / 碰撞 / 伤害 / recoil / 生命周期。

**实测时序**（`FIXED_DT_MS = 1000/60`，口径 = 「新弹丸出现的固定步号差」）：

| 场景 | 第 1→2 发步差 | 换算 |
|---|---|---|
| 正式 `cannon`（`burstRounds=1`） | **60 步** | = 冷却 1000ms（回归零变化） |
| 双联炮（2 发 / `burstIntervalMs=100`） | **6 步** | = 100ms 连发间隔 |

**双联炮 overlay 改写**（`runModifiers.ts`）：

```
旧：behavior 'shotgun' + { fanAnglesDeg: [-4, 4] }   ← 同步齐射、轨迹重叠（真人判不通过）
新：behavior 'cannon'  + { burstRounds: 2, burstIntervalMs: 100 }
```

- 弹道**同向**（实测两发出生 y 差 < 3px），不再靠散射把两发分开；
- 渲染标记回到正式 cannon 弹丸（不再是 shotgun runtime 固定写死的 `'tracer'`）。

✅ 回归：`cannonScenarios / contentCannon / cannonBehavior / contactRouterProjectile / battleIntegration` = **27/27** 全绿。

---

### 3. 冻结项确认（本 Queue 未动）

- 重型弹头 `projectileRadius 16 / projectileMass 4 / recoilImpulse 90` —— **一个数未改**（本 Queue 只保证它在独立单 Run 里仍生效）。
- 快速装填 `cooldownMs 400` —— **未调整**（真人复验后再判断）。
- PRP-R3 UI / 日志布局 / CHOICE 布局 / PRP-R5 Camera / Arena / world / spawn / enemy / Base Cannon /
  Movement / mass / 基础 Damage·HP —— 全部未动。
- 唯一新增的日志文案分岔 = 终局 RESULT 第三行（否则会说错「你发现了一次改装机会」）。

---

### 4. 门禁实测

| 门禁 | 结果 |
|---|---|
| `npm run typecheck` | **0 错** |
| `tests/portraitRunModifier.test.ts` | **16/16**（新增：正式 cannon 无 burst 字段冻结 / 真实时间差 60 vs 6 / 出生点同源 < 3px） |
| `tests/portraitRunPage.test.ts` | **46/46**（新增 RP-F2-08 终局停止 / RP-F2-09 重新开始验证=全新 Run / RP-F2-10 单变量守卫） |
| Cannon / Battle 回归（5 文件） | **27/27** |
| `e2e:run-page`（4 视口） | **315/315**（新增 R50–R54） |
| `e2e:default-entry` | 见下 |
| 全量 vitest | 见下 |
| 五路构建 + repo-health | 见下 |

E2E 关键实测（1280×720@1.5）：`R49` 窗口内最多 **2 发在飞**（第一场同窗口最多 1 发）·
`R50` 终局 `battlesCompleted=2` · `R51` `day=4/7`、`buffs=双联炮`（1 个）、主动作 `"重新开始验证"` ·
`R52` 新 Run `day=3 / buffs=0 / modifier=null / battles=0` · `R53` `phaseTrail` 从 `IDLE` 重开。

---

### 5. 交付回填

- **commit SHA = `1a3b080fa6b239a78e37df6b1dfb0ca755c06a17`**（`1a3b080`）
- 9 文件 / **+648 −83**；三路 SHA **四方一致**（`HEAD` = 本地 ref = `ls-remote` = `FETCH_HEAD`）。
- **正式 gameplay diff = 仅 `src/battle/cannonBehavior.ts`（+117 −9）**，即 Queue 授权的最小 burst Foundation；
  `src/core | physics | render | player | platform` diff = **空**。
- 门禁尾数：全量 vitest **202 文件 / 1927 passed / 0 failed**（上一轮 1923 → 本轮 +4）；
  五路构建 + repo-health 全 **EXIT 0**（`ok:true`）。

---

## PRP-F2-R2-FAST-RELOAD-PARAMETER-RECOVERY（快速装填 400 → 650）

### 0. 真人三段录像结论
- 重型弹头 ✅ 通过（冻结）；双联炮 ✅ 通过（冻结）；
- 快速装填 ✅ 机制可感知，但**数值明显过强**：`1000ms → 400ms` ≈ 2.5× 基础频率，
  把「远程开火 → 后坐 → 敌方接近 → 碰撞」整段正式物理节奏压缩掉，有形成稳定最优解的风险。

### 1. 改动（只有一处数值 + 断言同步）
- `src/lab/portraitBattleLab/runModifiers.ts`：`fastReload.behaviorParams.cooldownMs: 400 → 650`（≈1.54× 基础）。
  **未做多档扫描**（Queue 明令禁止 600/625/650/675/700 微调），就是一次大幅回收。
- 其余 overlay 一个数未动：重型弹头 `16/4/90`、双联炮 `burstRounds 2 / burstIntervalMs 100`。

### 2. 实测数据（一次性探针，跑完即删）
只有玩家（team `A`）开火 —— **敌方无炮**（`byTeam` 仅 `A`），所以开火间隔可干净归因。

| 口径 | 基础炮 | 快速装填 |
|---|---|---|
| `cooldownMs`（配置） | 1000 | **650** |
| 真实开火间隔（`weaponFire.timestamp` 差值，每个间隔都精确相等） | 1000.000000ms | **650.000000ms** |
| 600 帧（10s）内开火次数 | **10** | **16** |
| 整场（1600 帧上限）结束 | `A/hp`，剩余 **269.78/1100** | `A/hp`，剩余 **280.55/1100** |
| 频率比 | 1× | **1.538×**（回收前 2.5×） |

⚠️ **弹丸数增量会漏计开火**：`birthGaps` 实测出现 120/240 步（因为「弹丸命中销毁的那一步」常常
正好等于「下一发的开火步」，计数净变化为 0）→ 所以单测断言必须用 **`weaponFire` 事件时间差**，
E2E 断言必须用 **最小观测间隔**（漏计只会让间隔变大 → 最小值是稳健下界）。

### 3. 门禁实测
| 项 | 结果 |
|---|---|
| `tsc` | **0 错** |
| `portraitRunModifier` + `portraitRunPage` | **62/62**（16 + 46） |
| Cannon / Battle 回归（5 文件） | **27/27** |
| `e2e:run-page`（新增 R55–R57） | **318/318 PASS**（上一轮 315 → +3，只在首个视口跑满时序观测） |
| `e2e:default-entry` | **77/77 PASS** |
| 全量 vitest | **202 文件 / 1927 passed / 0 failed** |
| 五路构建 + repo-health | 全 **EXIT 0** / `ok:true` |

E2E `R55–R57` 关键实测（390×844@1，浏览器端真实开火间隔）：
`R55` 第一场基础炮 `modifier=null`、满耐久开局 · 观测 5 次开火 / 4 个间隔 / **最小间隔 985ms**；
`R56` 第二场 `modifier=fastReload` · `day=4` · `buffs=快速装填`（单变量隔离）；
`R57` **基础 985ms → 快速装填 647ms · 频率比 1.52×**（下界 480ms 用来证明「不再是 400ms 那一版」）。
（985 而非 1000 是 25ms 采样粒度的正常误差。）

### 4. 交付回填
- **commit SHA = `<待填>`**；文件数/增减行见提交后回填。
