# 2026-08-29

## F-WX-IOS-CANVAS-CRASH-P0｜修复微信UI帧累积与渲染卡死（已完成，待真机验收）

- **Commit**：`46d6ea13d245b8c9a17a997d1c4edc8246b18354`（独立 commit + push 到 origin/foundation-02-wechat；三路 SHA 一致 local=remote=46d6ea1）。
- **根因修复**：
  - MUST#2/#3：Renderer `clearScreen()` 改为显式 identity + 完整 backing 尺寸清屏（不用 save/restore，兼容 mock ctx）；`compositeOverlay()`(Web) / `compositeUi()`(微信) 显式 identity 1:1 drawImage，重置 globalAlpha=1 / globalCompositeOperation='source-over'，消除 UI 逐帧叠加（真机主因：renderer FX 残留 `lighter` 让 UI 每帧 ADD 到 screen）。
  - MUST#5：`SingleLoop.step()` 捕获 onFrame 异常 → 上报一次后继续调度，修复 ~6s 主循环静默卡死。
  - MUST#6：彻底移除 `__E2E_PROBE__`；探针统一迁移到 `__WX_DEBUG__`（仅 vite.e2e.config.ts 注入 true）。微信构建 define `__WX_DEBUG__` 恒 false → 编译期折叠为零开销。
- **构建隔离验证（grep dist-wechat/game.js）**：
  - 普通 `build:wechat`：`__E2E_PROBE__`=0 / `__h`=0 / `__probe`=0 / `__WX_DEBUG_GRANT__`=0 / `__WX_BUILD_BADGE__`=0。
  - RC `build:wechat:rc`（C3）：`__E2E_PROBE__`=0 / `window.__h`=0 / `__probe`=0；仅保留 `globalThis.__h`(调试入口，来自 `__WX_DEBUG_GRANT__`=true) + `setBuildBadge(#<sha>)`。
- **验收门全部通过**：scoped tsc 通过；全量 vitest **1350/1350**（163 文件）；新增 4 测试文件（wechatCanvasStability / wechatRendererClear / singleLoopException / wechatCanvasFrameHost 无 DOM 像素门禁）覆盖 MUST#1/#2/#3/#4/#5/#7。
- **C3 RC 包**：`dist-wechat/`（game.js + game.json + project.config.json），badge `#46d6ea1`。
- **关键 gotcha（重要）**：`runtimeInfoPlugin` 在**构建时**读 `git rev-parse HEAD` 注入 SHA。先 build 后 commit 会让 badge 显示旧 SHA。本次先 commit（46d6ea1）再 `build:wechat:rc`，确保 C3 badge 与 commit 一致。若将来改源码后只重跑 rc 脚本而不重新 commit，badge 仍对。
- **下一步**：停止，等待**一次**真机验证（C3）。**不得宣布真人体验通过**——自动化门禁通过 ≠ 真机体验确认。真机若仍有异常，按 Must#1-#7 已建门禁反查，不臆测。
- **未提交项（按 Cleanup 排除）**：`outputs/`(A/B/C1/C2 iso 包)、`tests/_e2e_garage_live.cjs`、handoff/接续包 .md、`.workbuddy/memory/*.md`（含本文件）均不进本 fix commit。`dist-wechat/` 被 gitignore（RC 产物本地部署，不入库）。

---

## F-WX-VIEWPORT-SURFACE-P0｜统一微信画布与逻辑舞台变换（commit c3ed90d）

- **真根因（比上一条更彻底）**：微信首画布默认 `canvas.width = windowWidth`（逻辑），而链路按「canvas.width=物理 backing」假设；且 WechatViewport 把 backing 当 `surface.width`（renderer 的 fit 域），绘制时又 `setTransform(dpr)` → **fit 域与绘制域双重缩放**（真机/模拟器全局放大+裁切；C3 的 backing 定版只是必要不充分）。
- **修复**：① game.ts 在 surface/UI/Input 创建前按 `windowWidth×pixelRatio` 定版 screen+ui 两块 Canvas backing；② `WechatViewport.surface.width/height = canvas.backing ÷ dpr`（=逻辑窗口，契约见 canvasSurface.ts）；③ renderer 新增 `viewToLogical`（恒 1）统一所有 vk=`surface?viewDpr:1` 与 `÷viewDpr` 域对齐点（isCompact/getVehicleScreenRects/groundScreenY/lastGroundScreenYLogical/fit insets/framing）——DPR 只在最终绘制一次；④ `wx.onWindowResize`（如有）同步两画布 + `runtime.doResize()`；⑤ 新增 `[WX-SURF]` boot+首帧全链诊断（__WX_DEBUG__ 门控）。
- **测试基建升级**：FakeCanvas 支持运行时 resize（ink 随 backing 重分配）；FakeCtx2D 新增 drawOps（device bbox 几何验收）、affine（translate/rotate/scale/transform）、path 跟踪（arc 4 轴向点防 45° 退化）、textAlign/textBaseline 对齐、fastRaster（面积记账，120 帧×4 环境控时）、resetInk。**fontSize() 必须匹配 `px` 后缀**（否则抓到字重 700 使 text bbox 虚大）。
- **新门禁**：`wechatSurfaceTransform.test.ts`（4 环境 backing 定版，先红后绿——禁用定版后红：`expected 1024 to be 2048`）；`wechatSurfaceGeometry.test.ts`（4 环境×10 项最终像素：SHA 左上/CTA 50%/车辆居中完整/UI 全在舞台/Garage 装配带底部/Matching 双车/Battle 双车+HUD/点击一致/120 帧无累积；**fast 面积记账不可作累积指标**——重叠绘制重复计数，须在 frame11/120 用慢速光栅+resetInk 采唯一像素）。
- **域契约修正波及既有测试**：crossLayerRectDprP0 T1-T4、rendererScaleTrace、platformCore WechatViewport 断言——surface 一律改传**逻辑尺寸**（backing÷dpr）。
- **验收**：scoped tsc ✓；全量 vitest **1360/1360**（167 文件）；build:wechat/rc ✓；普通包零 badge/debug/probe/`__h`，RC 仅 `#c3ed90d` badge+debugGrant。push 后 local=remote=`c3ed90d`。
- **C4 RC 包**：`dist-wechat/`（badge `#c3ed90d`）。下一步：等待一次 C4 验证（DevTools 模拟器 + 真机）。**未宣布真人体验通过**。

---

## 任务：同步微信 iOS 真机调试长期记忆（纯记忆任务，零代码改动）

- 完整读取根目录《最强水果-微信iOS真机调试踩坑复盘与固定流程.md》（484 行全文）。
- 原文逐字节落盘：`.workbuddy/memory/F-WX-IOS-REALDEVICE-DEBUG-PLAYBOOK.md`（cmp 一致）。
- MEMORY.md 新增「微信 iOS 真机调试必读规则」：触发关键词清单 + 必读该文档 + 核心纪律（坐标域先标注 / DPR 单次应用 / 内部 rect≠最终像素 / Web≠微信≠真机 / 三层状态分开表述 / 先红后绿+模拟器+真机录屏 / 禁补偿掩盖）+ 基线 c3ed90d 及其范围限定（仅 Canvas/DPR/主流程真机通过；Garage/UI/安全区/生命周期长测未通过）。

---

## 任务：生成会话交接文档（纯文档任务，零代码改动）

- 产出：`交接文档_2026-08-29_c3ed90d.md`（仓库根目录，惯例不进 git）。
- 实测确认三路 SHA：local HEAD = `c3ed90d44f2712dc3bc1ad28ed10039c903f8356`；`git ls-remote origin foundation-02-wechat` 实测一致（SSH 通道免密可用）；remote URL = `git@github.com:HubinAI/fruits.git`。
- 交接文档含 8 节：交接快照 / 项目定位 / 微信渲染管线现状（双重缩放根因+viewToLogical 契约+测试基建）/ 架构地图 / 项目纪律与工具坑 / 待办与下一步 / 构建命令速查 / 新窗口交接对话指令（可整段复制）。
- 待办清单：C4 真机验收（最高优先，未宣布通过）、C3 回执未收、F-WX-6 横屏 UX 适配标为「待确认事项」（工作区 MEMORY 无记录，需新会话与用户确认）。

---

## F-WX-SAFE-AREA-P0｜统一微信横屏胶囊与安全区布局（已完成，独立 commit + push，待真机验收）

- **Commit**：`357dba25688c2475c15e4e56b2659d5398486c20`（独立 commit + push 到 origin/foundation-02-wechat；三路 SHA 一致 local=remote=357dba2）。
- **根因**：WechatViewport.safeInsets() 只读 safeArea，从不读 wx.getMenuButtonBoundingClientRect() → 顶部右侧 UI（首页宝箱 / Matching 名牌 / Battle 右 HUD / SHA）被微信胶囊遮挡。微信真实路径走 mountCanvas，this.W/this.H = 实际 window 逻辑尺寸，safeArea 与胶囊同属 window 逻辑域，无需 stage→window 缩放。
- **修复（单一契约）**：
  - `readWechatWindowInfo()` 新增读取 `getMenuButtonBoundingClientRect()`，产出 `menuButton`（window 逻辑 px；API 缺失则 null）。
  - `WechatViewport.safeInsets()` 折叠胶囊进 `top/right` 内缩（right = windowWidth minus capsule.left + 6 gap；top = capsule.top + 6 gap；与 safeArea 取 max）。UI 仅消费 `this.insets` 即同时避让刘海与圆角与胶囊，禁止各页面独立硬编码偏移。
  - `canvasPlayerUIHost`：SHA 水印从裸坐标改为 `insL+6, insT+6`（避开左上状态栏/刘海）；Matching 名牌（居中 0.7W，长名右缘可能顶到胶囊）整体左移使右缘 ≤ W−insR−4；DevGrant 入口钳制右缘 ≤ W−insR−4。
  - 所有布局（homeLayout / mobileGarageLayout / Battle H,UD）已消费 insets，胶囊折叠自动传播（宝箱、车库顶栏、Battle 右 HUD 全部避让）。
- **测试**：`platformCore.test.ts` 新增 readWechatWindowInfo + safeInsets 胶囊折叠 4 单测；`wechatSurfaceGeometry.test.ts` 新增「注入胶囊，宝箱/右 HUD 右缘 ≤ capsule.left」集成断言（device px 矩形相交判定，排除 clear/image）。
- **验收**：scoped tsc 通过；全量 vitest 1363/1363（165 文件）；build:wechat 与 build 均通过。push 后 local=remote=357dba2。
- **冻结**：未触碰 C3/C4 的 Canvas backing / Surface logical / DPR 单次绘制 / Input 坐标链。
- **下一步**：停止，等待一次真机验证（微信胶囊 + 安全区避让）。未宣布真人体验通过。

---

## F-GARAGE-TOUCH-ASSEMBLY-R2｜优化微信真机拖装连续性与挂点反馈（已完成，独立 commit + push，待真机验收）

- **前置**：三路 SHA 基线 357dba2（local=remote，已 ls-remote 实测一致）；git identity xiaoyue@local 已就位。
- **调查结论（Must#1 逐帧追状态链）**：多数 Must 项已被前序 Queue（DRAG-ASSEMBLY-P0 / CONTINUITY-R1）满足——方向锁(Must#2)、ghost 连续跟随(Must#3)、吸附+松手必成(Must#5)、占用挂点替换(Must#6)、空白松手返回(Must#7)、微信 changedTouches(Must#9)、已装备灰态+标签(Must#10)、横滑/单击备用路径(Must#11)、边界不破坏(Must#12)。真实缺口仅 2 处：
  - **Must#4 反馈噪声**：`drawVehicleHardpoints` 旧码在**每个兼容挂点**都画粗蓝环 + 每个已占兼容挂点画白色虚线环 → 圆圈/虚线/红圈同时出现。改为：仅悬停的「最近有效挂点」金色呼吸环（overload 时转红）+ 白色虚线「替换」环；其余兼容挂点仅弱提示细环；不兼容默认不画红圈。
  - **Must#8 切后台清理**：`cancelInteraction()` 已定义但从未接线；`installDragSafetyNet()` 在 `typeof window==='undefined'`（微信）时提前 return → 微信 safety net 永不开火。改为订阅 `platform.lifecycle.onVisibilityChange(hidden=>hidden&&cancelInteraction())`（Web/微信统一后台入口），再保留 Web `window` 事件兜底。
- **Commit**：`5ee15693951fc5ace3bb2836dfbf2440e94de2b4`（独立 commit + push 到 origin/foundation-02-wechat；三路 SHA 一致 local=remote=5ee1569）。仅 3 文件：src/ui/canvasPlayerUIHost.ts（Must#4+#8）、tests/garageDragAssemblyR2.test.ts（新增 4 R2 测试）、tests/wechatSurfaceGeometry.test.ts（删除一个 TS6133 未用局部，与 R2 同批门禁）。
- **验收门**：scoped tsc --noEmit 通过（TSC_EXIT=0）；vitest 30/30（R2 4 + P0 回归 14 + R1 回归 12）。R2 测试覆盖：hoverHp 单目标切换、去噪渲染（远帧 0 金/0 粗蓝/2 弱提示；近挂点恰 1 金环+1 弱提示+0 粗蓝）、切后台 hidden→garageDrag 清空、离开 Garage→清空。
- **冻结**：未改库存/能量/挂点规则/Physics/部件数值/Canvas 底层/Input 坐标链。
- **下一步**：停止，等待一次 iOS 真机验收（连续 10 次真实拖装、无冻结/误滚/误装备、松手必成、空白返回配置不变、每次结束回 idle/null；DPR1/2/3 × 420×210/621×351/844×390/1280×592）。**未宣布真人体验通过**。C4（前序 SAFE-AREA-P0）回执仍待收，本 Queue 亦不扩展范围。

---

## F-GARAGE-VISUAL-DENSITY-R2｜强化中央车辆并精简底部装配带（已完成，独立 commit + push，待真机验收）

- **前置**：三路 SHA 基线 5ee1569（local=remote，R2-TOUCH 已 push）。本 Queue 为 playbook §10 问题 #1/#2/#4，推荐顺序 #3。
- **真实问题**：真机录屏底部装配带接近半屏、车辆偏小、卡片信息堆叠、左右箭头压住边缘卡片。
- **调查**：多数 Must 已被前序 Queue 满足（strip 32%∈30~34、三分类保留、卡片内容已极简、无新页签/按钮、车辆已居中）。真实缺口 3 处 → 全部已修：
  - Must#2 车辆宽下限：renderer `HOME_VEHICLE_WIDTH_MIN_PCT` 0.38→0.40（garage/home previewSolo clamp 同源；高度完整入画上限仍优先）。
  - Must#3 装配带占比：mobileGarageLayout `STRIP_HEIGHT_RATIO` 0.32→0.30；`Math.round`→`Math.ceil` 防 621×351 小数舍入跌到 29.9%（保证 ≥30%）。
  - Must#8/#9 箭头压卡 + 边缘卡误点：canvasPlayerUIHost 卡片行两侧加 gutter（short16/normal20），左右翻页箭头移入 gutter 槽位内，不覆盖卡片命中区；边缘露卡仍只作滚动暗示、不注册点击。
  - Must#6 已装备/可装备同亮度：visualTokens 新增 `availableFill`(#33507a 明显更亮蓝底) + `equippedFill` 降为 #1e242c 中性暗灰；卡片四态 fill/stroke：可装备=availableFill+更亮描边 / 已装备=equippedFill+暗描边 / armed=金 / 未获得=最暗。relLum 差 0.061≥0.04。
- **Commit**：`07df5c2b96649a8f77784161e8c6422b09e69491`（独立 commit + push 到 origin/foundation-02-wechat；三路 SHA 一致 local=remote=07df5c2）。仅 6 文件：renderer.ts / canvasPlayerUIHost.ts / mobileGarageLayout.ts / visualTokens.ts / garageCenterStageP0.test.ts（回归界 38→40/48）/ garageVisualDensityR2.test.ts（新增 4 测试：T1 strip30~34%、T2 stage≥40%且车辆中心≈舞台中心、T3 箭头在 gutter 不覆盖命中区+gutter 区 garageCardAt 返回 null、T4 可用/已装备 relLum 差≥0.04）。
- **验收门**：scoped tsc --noEmit 通过（TSC_EXIT=0）；targeted+回归守卫 31/31（R2 4 + garageCenterStageP0 5 + garageDragAssemblyR2 4 + mobileGarageLayout 7 + garageMobileShellR1 11）；**全量 vitest 167 文件 1371 测试全绿**（无回归）。不部署。
- **冻结**：未碰 Canvas backing / Surface logical / DPR 单次绘制底层、Garage 数据 / 装备规则 / 上一 Queue 拖装状态机。
- **下一步**：停止，等待一次 iOS 真机验收（420×210/621×351/844×390/1280×592：第一眼先见车辆、装配带≤34%、车辆不因底部 UI 永久缩小、卡片文字可读、当前装备一眼可辨、横滑/拖装/边缘卡不互相误触）。**未宣布真人体验通过**。C4 / R2-TOUCH 回执仍待收，本 Queue 不扩展范围。

---

## F-BATTLE-VISUAL-CLEANUP-R3｜清理战斗调试痕迹与接触点信息重叠（已完成，独立 commit + push，待真机验收）

- **前置**：三路 SHA 基线 07df5c2（local=remote，GARAGE-VISUAL-DENSITY-R2 已 push）。本 Queue 为 playbook §10 问题「部件矩形外框 / 接触点伤害数字重叠 / HUD 接近胶囊 / 调试感」。
- **调查（Must#1 先查来源）**：确认矩形外框 = `drawShape` 正式 graybox 的 `#0d0f14`/lw1.5 硬描边（非碰撞调试/挂点/E2E）；`debugDraw` 仅 DEV `physicsLab` 传入，正式路径不传；挂点/ghost 只在 `playerPhase==='garage'` 绘制。故重做表现轮廓而非删真实部件。
- **修复（只改最终表现，零删部件/几何）**：
  - Must#3 表现轮廓：`drawShape`(circle+polygon) / `drawSaw`(盘体+轮毂) / projectile 旧近黑硬描边 `#0d0f14` 全部改为 `partOutlineColor(fill)` = `mixHexColor(fill,'#1b2130',0.5)`（同色系柔和派生），线宽 `ss(0.9)` clamp 到 0.6~1.2、`lineJoin='round'`。新增纯函数 `parseHexColor` / `mixHexColor`(export) / `partOutlineColor`(export)。
  - Must#4 伤害数字屏幕空间避让：`damageLateralOffsets(snap)` 按 `vehicleCenter` 屏幕 x 差决定方向（不写死左右），受伤方数字朝受伤方侧偏移 `ss(26)` clamp 14~34px；render FX 循环 `tx = sx(f.x) + lateral`；保持同车≤2组、slot 纵向错层、总伤害守恒（仅挪像素不污染战斗数据）。
  - Must#5/#6/#7：HUD 安全区（前序 SAFE-AREA-P0 已折叠胶囊进 insR）、白描边 hit-flash（前序已就绪）、Active/Warning/Closing/End 四阶段均经 R3 测试断言无 `#0d0f14` 描边。
- **验收门（新增 guard）**：`tests/battleVisualCleanupR3.test.ts`（9 例，真实链路 `PlanckBattleOrchestrator`+真实 `Renderer`+真实 `reframe`；surface.now 受控 fakeNow 驱动 TTL/窗口）：T1 四分辨率×四阶段无 `#0d0f14` stroke + 部件轮廓 0.6≤w≤1.2 且 join=round；T2 车身填充仍在 + 轮廓亮度显著高于旧近黑；T3 双车完整入画、A 在 B 左不重叠；T4 同 contactPoint 双方数字 |Δx|≥28 且受伤方偏受伤侧；T5 TTL 内总量守恒 + 同车≤2组；T6 TTL(900ms)后清零不绘制；T7 HUD 不越胶囊安全区(capsuleLeft/左安全区)；T8 hit-flash 有白描边无整块白填充；T9 mixHexColor/partOutlineColor 纯函数（含 clamp/非法输入不抛）。修正 `tests/battlePhaseFx.test.ts` T6a（原 `expect(strokeStyle).toBe('#0d0f14')` 随轮廓重做改为「Active 阶段任何 stroke 都不等于 #0d0f14」+ 末笔非近黑；CtxStub 新增 strokeStyles 历史记录）。
- **验收结果**：scoped tsc --noEmit 干净（TSC_EXIT=0，仅余既有 `__WX_DEBUG__`/`__WX_RCA__` 全局声明噪声非回归）；**全量 vitest 168 文件 1380 测试全绿**；`build`(web) + `build:wechat` 均通过（exit 0；>500kB chunk 警告为既有非回归）。
- **Commit**：`606cab7a2b69923a94a36713e5312aabfac0dd82`（独立 commit + push 到 origin/foundation-02-wechat；三路 SHA 一致 local=remote=606cab7）。仅 3 文件：src/render/renderer.ts（M）、tests/battlePhaseFx.test.ts（M）、tests/battleVisualCleanupR3.test.ts（?? 新增）。memory/HANDOFF/doc 等未跟踪文件不进本 commit。
- **冻结**：未碰 Canvas backing / Surface logical / DPR 单次绘制底层、Physics、战斗规则、速度/质量/碰撞/伤害/胜负/时长/Result。
- **下一步**：停止，不部署，等待一次 iOS 真机验收（420×210/621×351/844×390/1280×592：双车无矩形外框/调试感、接触点双方伤害数字可区分不重叠、HUD 不顶胶囊、无持续噪音、Result 路径正常）。**未宣布真人体验通过**；自动化门禁通过 ≠ 真机体验确认。成功后继续最终 Queue。
