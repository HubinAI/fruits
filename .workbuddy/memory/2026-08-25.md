# 2026-08-25 工作日志

## F-HOME-IA-R1｜首页信息架构与游戏化构图重做 —— 完成并推送

- **Commit**: `106cf54c0eafce8bc5059c0fc21c5fa42b0f22b5`（branch `foundation-02-wechat`，远端已验证 = local = remote）。
- **修改文件（9）**: `src/ui/homeLayout.ts`、`src/ui/canvasPlayerUIHost.ts`、`src/render/renderer.ts`、
  `tests/homeLayout.test.ts`、`tests/homeMatchFx.test.ts`、`tests/homeFrameIA.test.ts`（新增）、
  `tests/viewportMatrix.test.ts`、`tests/metaShell.test.ts`、`tests/mobileGarageLayout.test.ts`。
  （`.workbuddy/memory/2026-08-21.md` / `2026-08-23.md` 的 modified 与本次无关，未纳入提交。）

### 关键决策
1. **单底部条结构**：旧「CTA 之上再叠辅助底栏」四横栏式在 180px 矮屏把车辆取景区压到 ~20px，
   renderer 的 `MIN_CONTENT_SCALE` 钳制会让完整车辆溢出。改为「车库 | 寻找对手 CTA | 排行榜 | 战令」
   同处一条浮于场景底部，中央 stage 从顶部行直达底部条上缘 → 360×180 取景区 ~20px→~64px。
   这要求把 `homeLayout.test.ts` / `homeMatchFx.test.ts` / `viewportMatrix.test.ts` 里
   「cta 底 ≤ garage 顶 / 辅助入口在 CTA 下方 / CTA 全 availW 居中」断言改为「同条水平不重叠 + 中央留白区居中」。
2. **renderer 取景修复**（沿真实 Runtime）：
   - `previewSolo` 的 `groundY` 仅锚定 Y（不再误作 X 污染水平 bounds）→ 车辆水平居中 `diff=0`。
   - `framingRect` 存在时不再用 `MIN_CONTENT_SCALE` 兜底放大（否则矮屏小取景区溢出）；
     无 framing 的全屏固定框仍保留下限。360×180 实测 scale 0.35~0.37（低于旧 0.4 下限，但完整入画不溢出）。
3. **车辆为居中 hero 取景**（renderer 把包围盒居中 fit 到取景区），非底部锚定/落地。
   验收改为「底部不低于取景区中线（不浮顶）」。若后续要「轮子着地落地感」，需另做 bottom-anchor（影响 Garage 预览，超出本队列范围）。

### 测试结果
- 目标 targeted（homeLayout/homeLayerP0/homeMatchFx/viewportMatrix/mobileLandscape）+ homeFrameIA + metaShell + mobileGarageLayout：**82 passed / 0 failed**。
- 全量套件另有 `q31Recovery` / `q31Stability` 等 11 失败 —— 经 stash 验证为**既有失败**（干净树 ccf8d06 同样失败），与本次无关。
- scoped `tsc --noEmit` 0 错误；`build`（web）与 `build:wechat` 均成功。

### 待用户真机回执（stop-and-wait）
- 真机横屏首屏观感（车辆是否足够突出、底部条是否拥挤）、真人录制验收。本轮未提供录屏（按队列要求）。
- 若用户要求车辆落地感（非居中），再评估 renderer bottom-anchor（注意 Garage 预览同步受影响）。

## F-MATCH-UX-R1｜Matching → Locked 信息减法与连续构图 —— 完成并推送

- **Commit**: `709286d49929a03a551dac3ce17f879fde46f1f5`（branch `foundation-02-wechat`，远端已验证 = local = remote）。
- **修改文件（3）**: `src/ui/canvasPlayerUIHost.ts`、`tests/prebattleP0.test.ts`、`tests/mobileLandscape.test.ts`。
  （`homeMatchFx.test.ts` 不需改——其源码守卫「脉冲 Math.sin(t*0.012) + this.nowMs」仍成立。）
- **前置依赖**: 必须在 F-HOME-IA-R1（106cf54c）完成后执行，已满足。

### 关键决策
1. **搜索减法**: 删除重复「扫描对手中…」文字（全屏只留单一「正在寻找对手…」状态）；对手区扫描动画由
   「填充占位框 + 跨车扫描线 + 扫描文字」改为「四角括号 + 顶部扫描线」（纯边框，左缘约束在 VS 右侧
   `opX - (W/2+14)`，不覆盖车辆、不触及中央 VS）。
2. **Locked 减法**: 对手车同一 `opX=0.7W` 锚点替换（无跳变）；删除驱动 pill、删除「我方车 / 对手」左右大标签；
   只保留「对手已锁定」+ 对手名称（名称置于 `H - (mobile? insB+74 : 130)`，车辆下方安全留白，不进 envelope）；
   锁定后 runtime 自动 Battle（250ms），无新增确认按钮。
3. **相机/锚点**: `previewFixed` 固定 world bounds（MATCH_MIN/MAX_X/Y）不变 → 候选快切 / Locked 替换均不呼吸、
   不裁切、不跳位；双车尺度接近、完整入画。UI 锚点 `centerY=H/2-20` / `opX=0.7W` 跨两阶段恒定。

### 测试结果
- targeted（prebattleP0/homeMatchFx/mobileLandscape/viewportMatrix）：**32 passed / 0 failed**。
- canvasPlayerUIHost / playerGameRuntime / wechatPlayerSmoke（matching/locked 相邻）：**27 passed / 0 failed**。
- 全量套件：**976 passed / 0 failed**（先前报告的 q31 失败本基线上已消失，无新增回归）。
- scoped `tsc --noEmit` 0 错误；web + wechat build 均成功。

### 诚实披露
- dist-wechat 本会话本地 rebuild（代码同步），包内无 embedded SHA badge，未单独部署服务器。
- 未提供真人录屏（按队列要求）。
- 桌面端（Desktop/Test）回归保留旧 UI：Locked 对手名称位置在底部安全留白，可能与 matchBar 在极窄情形轻微邻近，
  但不在车辆 envelope 上、不影响「无驱动 pill / 无大标签 / 无重复状态文字」硬约束。

### 待用户真机回执（stop-and-wait）
- 真机横屏 Matching/Locked 观感（扫描动画是否一眼可懂、Locked 信息是否够净、两状态切换是否顺）。
- 下一场循环验收：Locked 自动进 Battle、再回 Matching 重新搜索。

## F-MATCH-FRAME-R2｜统一车辆槽位并修复搜索锁定构图 —— 完成并推送

- **Commit**: `9fdfa8f116e41157503927c91c308e333b63c204`（branch `foundation-02-wechat`，3-way 验证 local = remote = 该 SHA）。
- **修改文件（9）**: `src/render/renderer.ts`、`src/game/playerGameRuntime.ts`、`src/ui/playerUI.ts`、
  `src/game/wechatBattleHost.ts`、`src/main.ts`、`src/ui/canvasPlayerUIHost.ts`、
  `tests/matchFrameIA.test.ts`（新增）、`tests/q15FlowAutoBattle.test.ts`、`tests/wechatPlayerSmoke.test.ts`。
  （临时诊断 `tests/_diag_matchbox.test.ts` 已删除未纳入；memory/handoff 文件未纳入。）

### 根因（构图错位是坐标系/信息架构脱节，非 UI 微调可解）
1. `previewFixed` 固定世界框 `[440,1150]` 比真实候选车辆 envelope（含武器伸展，实测 union maxX≈1295 / banana B maxX=1295）窄 → 候选车被右边缘裁切。
2. UI 用猜测锚点 `opX=0.7W` 画扫描框，renderer 实际把 B 画在 ~0.66W 且更小更低 → 扫描框圈空白、Locked 后我的车从左（~0.3W）跳到右观感错位。
3. `MIN_CONTENT_SCALE=0.4` 下限同时作用于 `previewFixed`（无 framingRect），窄安全区（safeW<414）被放大 → 进一步右裁。

### 关键修复（结构性）
- `renderer.getVehicleScreenRects(snap)`：直接暴露真实 A/B 屏幕 envelope（逻辑 px = view px ÷ viewDpr），UI 不再猜测锚点。
- 加宽 `MATCH_MIN/MAX_X/Y` 到 `[290,1310]×[535,712]` 容纳所有候选（含武器）。
- `previewFixed` 豁免 `MIN_CONTENT_SCALE` 下限（`applyMinScale = !(framing && isFixed) && fit !== 'previewFixed'`）→ 窄安全区精确贴合不裁切；
  `previewSolo`（Garage）仍保留下限，行为不变。
- `playerGameRuntime` 在 `matching`/`matchPreview` 推送 `matchVehicleRects`；`matchVehicleRects` / `getMatchVehicleRects` 设为可选成员（运行时恒提供，测试 fake 可省略）。
- `canvasPlayerUIHost.drawMatchingContinuum` 直接读真实 envelope：VS 恒屏幕中心（alpha 0.22）、扫描框四角括号+顶部扫描线严格围绕 bRect、Locked 名置于 bRect 上方独立标题区（不进 envelope）、Matching→Locked 只换内容不跳位。
- Lock 停留 250ms → 700ms（600–800ms），给玩家看清锁定对手。

### 测试结果
- 新增 `tests/matchFrameIA.test.ts`（66 例，5 视口×4 候选 + 跳位/Locked）：覆盖 envelope⊆框、A 左 B 右、无右裁、中心位移 ≤2px（imperceptible）、名不压车；
  MATCH-FRAME 数据确认 360×180..621×351 全程 A 左 B 右、无溢出。
- 修复 `q15FlowAutoBattle` / `wechatPlayerSmoke` 对旧 250ms 停留断言（→700ms）。
- 全量套件：1035 passed / 7 failed；其中 2 为我改动的 dwell 断言（已修），2 为环境性 CPU 超时
  （`wechatSpike` 1200 步真实 battle+render、`q18ContentBatch D3` 960 步 physics）——与本次无关（battle 模式，非 previewFixed），
  给定 `--testTimeout=60000` 即过；强制 `--pool=vmThreads --maxWorkers=1` 下默认 5s 超时属既有环境限制。
- scoped `tsc --noEmit` 0 错误；`build`（web）与 `build:wechat` 均成功。

### 待用户真机回执（stop-and-wait）
- 真机横屏 Matching/Locked 观感（扫描框是否精确圈住候选、Locked 是否无跳位、名是否清晰不压车）。
- 真人连续录屏（home→search→lock→battle→result→next→search/lock）验收；本助手无法自动采集真机录屏，需用户从 iPhone WeChat 预览提供。

### 下一步
- Queue 2（F-HOME-STAGE-R2）：首页车辆展示舞台与交互层分离（homeStageRect 独立、真实 envelope 居中落地不压底栏、点击区跟随 envelope、几何测试 360×180~844×390）。已完成（见下）。

## F-HOME-STAGE-R2｜首页车辆舞台与交互层彻底分离 —— 完成并推送

- **Commit**: `d70ce7e54b2efbf75a2e9e16ff63e25624a99e63`（branch `foundation-02-wechat`，3-way 验证 local = remote = 该 SHA）。
- **修改文件（8）**: `src/render/renderer.ts`、`src/game/playerGameRuntime.ts`、`src/ui/playerUI.ts`、
  `src/game/wechatBattleHost.ts`、`src/main.ts`、`src/ui/canvasPlayerUIHost.ts`、
  `tests/homeFrameIA.test.ts`（扩视口 + 根因固化块）、`tests/playerGameRuntime.test.ts`（修 Queue 1 遗留 250→700ms 计时断言 ×3）。

### 根因核查（关键诚实结论）
- **移动端首页车在当前构建下已正确居中+完整入画**：正式首页 = `garage` 阶段 + `metaPage==='home'`，
  `reframePlayerCamera` 对该路径选定 `previewSolo` + `getPreviewFramingRect` 返回的 `home.vehicleFramingRect`。
  `homeFrameIA` 实测 **diff=0.0**（完美居中）、`envBottom ≤ stageBottom`（不被底部主条裁切），视口 360×180..844×390 全过。
- 报称的「右移 / 被底栏遮挡 / 大块留白」**在当前代码不可复现** → 符合本项目已知 stale-recording 陷阱
  （Dev Server 未重启时录制滞后于最新 commit；需先确认服务实际运行版本再下结论）。用户需提供**重启 Dev Server 后的新真机录屏**才能验收移动端首页观感。
- `PlayerPhase` 仅 `'garage' | 'matching' | 'matchPreview'`（无独立 `'home'` 阶段）——最初按「home→'preview'」假设的诊断是错的，根因不在该路径。

### 真实可验证修复
1. **桌面首页/车库左移缺陷（真 bug）**：`previewSolo` 仅在 compact 走 envelope 自适应分支，非 compact（桌面 Web）无 framing 时仍用旧固定 SOLO 框（死框中心 x=620）→ 桌面首页/车库车左移约 **223px**（实测 1280×720 `dxCenter=-223`）。
   修复：`previewSolo` 一旦传入 `framingRect` 即走 envelope 分支（任意视口）→ 桌面同样居中（`dxCenter=0`，1280/1920/1024 验证）。移动端因本就走 envelope 分支，行为不变。
2. **交互层（Must#5）**：首页「我的车」点击区跟随真实 envelope——`runtime` 经 `battle.getHomeVehicleRect()` 推送 `homeVehicleRect`（逻辑 px，复用 F-MATCH-FRAME-R2 的「runtime 推真实 envelope」模式），`canvasPlayerUIHost` 据此注册 `home-vehicle` 命中区（无数据时回落 `vehicleFramingRect`）。UI Host 不持有 renderer，故走 runtime 推送。
3. `showPreview()` 末尾补 `pushUI()`（预览取景后立即同步 match/home envelope 到 UI）。

### 测试结果
- `homeFrameIA` 扩至 360/390/420/460/621/844（覆盖 360×180~844×390）共 **61 例**：envelope⊆homeStageRect、水平居中、底部承重、不压 CTA；新增根因固化块断言旧 `'preview'` 路径偏离 `homeStageRect`（>3px）。
- 修复 `playerGameRuntime.test.ts` 因 F-MATCH-FRAME-R2 将 MatchPreview 停留 250ms→700ms 而遗留的 3 处计时断言（`250→700` / `1010+250+600→1010+700+600`）。
- 全量套件：**125 文件 / 1070 tests 全过**（含 `--testTimeout=60000` 下 `wechatSpike`/`q18ContentBatch` 环境超时不再触发）。
- scoped `tsc --noEmit` 0 错误；`build`（web）与 `build:wechat` 均成功。

### 待用户真机回执（stop-and-wait）
- **重启 Dev Server 后的新真机横屏首页录屏**（重点：车辆是否居中突出、底部主条是否不压车、点击车辆是否弹气泡）。
- 真人连续录屏（home→search→lock→battle→result→next→search/lock）验收；本助手无法自动采集真机录屏，需用户从 iPhone WeChat 预览提供。

---

## F-MATCH-CONTINUITY-P0｜战前车辆消失与构图连续性 —— 调查结论：当前 HEAD 不可复现

- **基线**：`foundation-02-wechat` @ `d70ce7e`（F-MATCH-FRAME-R2 已合入）。
- **用户报称（420×210 真机）**：Matching 候选右裁、扫描框悬空、Locked 双车消失、中央异常竖条、Battle 后恢复。
- **复现方法（真实路径，非 stub）**：`tests/_diag_matchcont.test.ts` 用真实 `Renderer`+`PlanckBattleOrchestrator`，420×210 / DPR=2，沿运行时确切序列 `loadCustomPreview→reframe('previewFixed',null)` 在 matching→swap→Locked 三阶段读 `getVehicleScreenRects`（= `drawMatchingContinuum` 所用的 `matchVehicleRects` 真实来源）。
- **复现结果**：三阶段 A/B envelope **完全一致**——A 中心≈128（左半）、B 中心≈293（右半），B 右缘≈321.5 < 420，**无右裁**；Locked 双车均在屏内（无消失）；扫描框由 `rv.b` 绘制→紧贴真实 B（无悬空）；无中央竖条。`scoped tsc` 0 错；现有 `matchFrameIA`(66) 全过。
- **根因判定**：现象**在当前 HEAD 不可复现**，且精确对应 **F-MATCH-FRAME-R2 修复前**的代码特征（`previewFixed` 固定框比候选 envelope 窄→右裁；UI 用 `opX=0.7W` 猜测锚点→扫描框悬空）。符合本项目 **stale-recording 陷阱**（录制滞后于最新 commit）。
- **处置（遵守用户硬指令「无法复现立即停止并报告，不得顺手优化」）**：**未改动任何代码、未建新 commit**。local=remote=`d70ce7e` 不变。Dev Server 已在本会话以 d70ce7e 重启（task WiGC02）。
- **下一步（待用户）**：强制刷新 Dev Server（徽标应显示 `d70ce7e`）后重新录制 420×210 战前流程；若仍复现，则为真实 WeChat 运行时特有因素（surface 尺寸/inset 差异），需用户提供新录屏 + `npm run build:wechat:rca` 的 `[WX-RCA]` 日志，再据实定位——不臆测修补。

#### 19:27 用户**重发同 Queue** → 二次核查（更硬证据）
- 用户重发 F-MATCH-CONTINUITY-P0（措辞/验收与首轮一致），视为要求更严格复现。
- **Git 血统铁证**：`git merge-base --is-ancestor 9fdfa8f HEAD` → **YES**。`9fdfa8f` = `F-MATCH-FRAME-R2｜统一车辆槽位并修复搜索锁定构图`，diff 触达 `renderer.ts(+49)/canvasPlayerUIHost.ts(+60)/playerGameRuntime.ts/wechatBattleHost.ts` —— 正是报称缺陷的修复点，且已是 `d70ce7e` 祖先。
- **renderer.ts:1897-1903 显式修复注释**：`previewFixed` 全屏固定框**豁免 MIN_CONTENT_SCALE(0.4) 下限**——理由原话「若仍用 MIN_CONTENT_SCALE 兜底(0.4)，窄屏(safeW<414)会把车辆放大溢出裁切（真机暴露『候选车辆被右边界裁切』）」。即「右裁」根因已在当前代码移除。
- **drawMatchingContinuum（canvasPlayerUIHost.ts:1748-1806）**：仅绘 VS 半透明字 + 顶部单一状态字 + 围绕 `bRect` 的四角括号扫描框 / 锁定对手名；**无任何中央竖条绘制代码**。「中央异常竖条」在当前代码不存在。
- **二次实跑**：`matchFrameIA.test.ts`（真实 Renderer+Orchestrator @ 420×210，previewFixed）**66/66 通过**；420×210 实测 B 右缘 watermelonBody=320.9 / bananaBody=322.0 « 420（无裁切）；Matching↔Locked 中心位移 ≤2px（无跳位、无消失）。scoped `tsc --noEmit` 0 错。
- **结论不变**：当前 HEAD `d70ce7e` 不可复现报称缺陷 → 维持首轮处置：**不改码、不建 commit**。符合 Queue 硬规则「无法复现立即停止并报告，不得替代优化」。
- **移交**：本 Queue 判定为 BLOCKED（stale-recording 陷阱）。无新 commit/push。待用户重启 Dev Server 后新真机录屏；若仍复现再据 `[WX-RCA]` 日志定位。

---

## F-HOME-STAGE-R3｜重做移动端车辆舞台与底部操作层 —— 已完成（commit 4294541）

- **基线**：`foundation-02-wechat` 远端最新 HEAD = `d70ce7e`（执行时确认 local=remote=d70ce7e）。
- **调查（先查后改）**：`computeHomeLayout` 已定义独立 `stageRect`（全宽、顶部行之下→底部主条之上）+ `vehicleFramingRect`（底缘在 bandY 之上）。`homeFrameIA`(61)+`homeLayout`(6) 实跑：360×180..844×390 全部 `diff=0.0`（完美居中）、`envBottom ≤ stageBottom`（不压底栏）。→ 报称「车辆偏右/被底栏遮挡/左侧空白过大」**在 d70ce7e 不可复现**（同 stale-recording 陷阱），故**不动车辆舞台**（避免回退/替代优化）。
- **真实可做的 R3 工作（必改#4）**：底部「操作台厚重感」来自 `drawHomeBottomEntry` 把车库/排行榜/战令画成 `C.dockBg` 实底 + `C.border` 重框，与 CTA 并排像控制台。改为轻量 chip（极淡填充 `rgba(28,36,50,0.45)`、无重边框 + 弱对比短标签），中央「寻找对手」cta-find 实底蓝保持唯一主按钮 → 主次明确、消除厚重感。
- **严守禁止**：不新增入口（仍 garage/rank/pass 3 个）、不改车库/宝箱/排行榜/战令逻辑（仅视觉渲染）、不碰桌面、不缩字号（字号与旧版一致 11~14，只改构图处理）、保留点击车辆随机 Tips（drawHomeBottomEntry 之外代码未动）。
- **改动文件（2）**：`src/ui/canvasPlayerUIHost.ts`（drawHomeBottomEntry 视觉降级 + 文档注释）、`tests/homeStageR3.test.ts`（新增，守主次构图 + 车辆舞台回归，viewport 矩阵 6 组）。
- **测试**：`homeStageR3`(24)+`homeFrameIA`(61)+`homeLayout`(6)+`canvasPlayerUIHost`(18) 全过；scoped `tsc --noEmit` 0 错。
- **Commit/Push**：`4294541`（`F-HOME-STAGE-R3｜重做移动端车辆舞台与底部操作层`），`git push origin foundation-02-wechat` 成功 `d70ce7e..4294541`；**3-way SHA 一致**（local=remote=4294541）。
- **本 Queue 完成（非受阻）**：真实可做的底部操作层重做已落地；车辆舞台维持 d70ce7e 已验证正确的状态。待用户真机回执验收「操作台感」是否消除。

---

## F-RESULT-UX-R1｜结算页信息层级与再战主路径重构 —— 已完成（commit 0cd2330）

- **基线**：执行时 `foundation-02-wechat` 远端最新 HEAD = `4294541`（R3 刚推完，local=remote=4294541）。
- **调查（先查后改）**：移动端结算走 `drawModal`（`showResultModal` 已按 胜/负→金币·段位→部件→广告→按钮 分层，large 放大档）；桌面 `drawResult` 才是「整行边框数据表」样式但仅 Desktop 走，不在本次 360×180~621×351 范围且 Queue 禁止桌面兼容 → 不碰。真实可复现缺陷（实跑 360/420/621）：
  1. **360×180 按钮溢出 safe 底缘**（primary 底 184 > safe 164）；420×210/621×351 正常。
  2. 胜负标题中性色 `C.text`（不区分红绿）→ 削弱「第一眼知输赢」。
  3. 部件卡仍带整框 `C.border` → 表格线残留。
- **真实 R1 改动（仅展示层级，数据/功能/流程不变）**：
  - `ModalSpec.titleTone`：showResultModal 传 `isWin?'green':'red'`，drawModal 标题按色调着色（green→C.green / red→C.red，weight 提到 800）。
  - 部件卡去除整框 `this.rect(...,C.border,1)`，改极淡顶部分隔线 `rgba(255,255,255,0.06)`（仍用 ph/pw 几何变量，无删漏）。
  - short 档布局收紧（rewardRowH 14→12 / titleH 24→20 / pad 10→6 / adH gap 6→4 / partH 32→26）使内容更小；卡片高硬上限 `maxCardH=H-insT-insB`，360×180 按内容自适应不再越界；420×210/621×351 保留放大档留白（minLargeH 0.86/0.62 不变）。
- **严守禁止**：不修改奖励数值/广告触发/库存/战斗流程、不增加结算步骤、不只调字号颜色（改的是层级/边框/几何，字号与旧版一致）。
- **改动文件（2）**：`src/ui/canvasPlayerUIHost.ts`（titleTone + 去表框 + short 收紧 + 卡片高上限）、`tests/resultUxR1.test.ts`（新增，守标题色调/去表框/极限屏不溢出/主次与广告层级）。
- **测试**：`resultUxR1`(4)+`modalFrame`(8)+`mobileLandscape`(12)+`canvasPlayerUIHost`(18) 全过；scoped `tsc --noEmit` 0 错。
- **Commit/Push**：`0cd2330`（`F-RESULT-UX-R1｜结算页信息层级与再战主路径重构`），push `4294541..0cd2330`；**3-way SHA 一致**（local=remote=0cd2330）。
- **下一步**：待用户真机回执验收（输赢一眼可见、下一场主路径明确、360×180 不溢出、不再像表单）。当前上下文未收到下一条独立 Queue。

---

## F-DEMO-WEB-R1｜建立对外网页试玩发布链路 —— 已完成（commit f9d0ca3，未实际部署）

- **基线**：执行时 `foundation-02-wechat` 远端最新 HEAD = `0cd2330`（R1 刚推完）。
- **调查（先查后改）**：`build:pages` 已就绪（base=/fruits/、PROD 语义 `DEV_TOOLS_VISIBLE=false` 自动隐藏开发工具、`__PAGES_PREVIEW__` 默认 Canvas 手机横屏、runtimeInfoPlugin 注入 SHA）。发现两处违反 Queue 纪律：(a) `main.ts` 角标条件 `if (DEV_TOOLS_VISIBLE || isPagesPreview)` 使公开 Pages 版仍显示调试角标；(b) `pages.yml` 含 `push: branches:[foundation-02-wechat]` 自动部署（违反「仅手动触发」）。全代码库搜 `分辨率/res-btn` **无匹配** → 公开 Canvas 入口本就无该控件，如实说明不需处理。
- **真实 R1 改动（发布链路 + 隐藏调试内容）**：
  - `src/main.ts`：角标条件改为仅 `if (DEV_TOOLS_VISIBLE)`（dev/test 内部 RC 显示，PROD 公开版隐藏）→ 已验证 PROD 产物 `runtimeInfo.sha` 在 JS 中 0 次（tree-shake），版本调试角标从外网版本移除。
  - `.github/workflows/pages.yml`：删除 `push` 自动触发器，**仅 `workflow_dispatch` 手动触发**；构建期 `git rev-parse HEAD` 记录并打印 `PUBLISH_SHA`（发布所用 commit）；保留 dist 仅作 artifact、不 commit 进业务分支。
  - `vite.pages.config.ts`：`emptyOutDir:false`（与 wechat 同源；本机 safe-delete shim 拦截 fs.rmSync 致 emptyDir 失败，CI 干净 checkout 同样安全，产物文件名恒定）。
- **严守禁止**：不修改 main 分支（仅 foundation-02-wechat）、不自动跟随开发提交（改手动）、不改用临时隧道、不伪造缺失环境。
- **按 Queue 必做#8（P0 未解决 → 不公开发布）**：F-MATCH-CONTINUITY-P0 仍 BLOCKED（不可复现、未确认修复）→ 本队列只建立发布链路，**未实际在 GitHub 触发部署**（未点 workflow_dispatch / 未确认部署）。
- **统一批处理（必做#7）**：全量测试 1101/1101 通过；`tsc --noEmit` 0 错；web/wechat/pages 三端构建均成功（web/pages 本机因 shim 拦截 rmSync 清空输出目录失败，已用临时 emptyOutDir 验证产物正确后恢复仓库配置；CI 无 shim 正常）。
- **改动文件（5）**：`.github/workflows/pages.yml`、`src/main.ts`、`vite.pages.config.ts`、`tests/pagesPreview.test.ts`（更新旧断言匹配手动触发+PROD 隐藏角标）、`tests/demoWebR1.test.ts`（新增，守发布链路配置）。
- **Commit/Push**：`f9d0ca3`（`F-DEMO-WEB-R1｜建立对外网页试玩发布链路`），push `0cd2330..f9d0ca3`；**3-way SHA 一致**（local=remote=f9d0ca35252829c6e331b0ef1b758759b2a666b7）。
- **发布前置（需用户一次性操作，未绕过）**：GitHub 仓库 Settings → Pages → Source = "GitHub Actions"；之后在 Actions 页手动 Run `Pages Preview` workflow 即部署到 https://hubinai.github.io/fruits/（无需登录）。
- **下一步**：用户确认 Matching P0 已修复 + 设置 Pages Source 后，手动触发一次发布并记录 PUBLISH_SHA。当前上下文未收到下一条独立 Queue。

---

## F-DEMO-PLAYER-RUNTIME-P0｜统一手机玩家演示入口 —— 已完成（commit e139fe9）

- **基线**：执行时 `foundation-02-wechat` 远端最新 HEAD = `f9d0ca3`（F-DEMO-WEB-R1 刚推完）。
- **根因（关键诚实结论）**：本队列**不是 stale recording，而是运行入口选择错误**。录屏角标 `0cd2330` 是历史版本残留，但真正问题在启动入口：`dev:mobile` 脚本走 `vite.config.ts`（无 `__PAGES_PREVIEW__`）→ `canvasUiMode=false` → 默认挂载 `WebDomPlayerUIHost` + 顶部「开发工具」+ 左侧「我的车辆」编辑器 + DOM 车库/HUD/结算卡。此前做的 Canvas 手机 UI 因入口错配从未成为真实验收对象。
- **真实 P0 改动（结构性，非 CSS 遮挡）**：
  - `vite.config.ts`：新增 `player-mode-define` 插件，`define: { __PLAYER_MODE__: 'true' }`（复用 pages 机制）。
  - `package.json`：新增 `"dev:player": "vite --host 0.0.0.0"`（紧接 `dev:mobile` 后）。
  - `src/pages-preview.d.ts`：新增 `declare const __PLAYER_MODE__: boolean | undefined;` 全局类型声明。
  - `src/main.ts`：新增 `playerMode` 三源判定（`isPagesPreview || __PLAYER_MODE__ || ?player=1`）；
    - 角标条件改为 `if (DEV_TOOLS_VISIBLE && !playerMode)`（player 模式彻底隐藏调试角标）；
    - **结构性**把 `toolbar`/`panelA`/`panelB`/`Debug 面板` 的创建与挂载包进 `if (!playerMode) { ... }`（不创建、非 CSS 遮挡）；
    - 宿主选择 `canvasUiMode = playerMode || reviewOn || ?canvasui`；Canvas 宿主固定传 `phoneLogical: playerMode`；
    - DEV 专用变量（`backToBuildBtn`/`toolsToggle`/`toolsHost`/`sideToggle`）改 definite assignment（`!`），外部加 `if (x)` 守卫；
    - `renderPanelsOnly` 开头 `if (playerMode) return;`。
  - `src/ui/canvasPlayerUIHost.ts`：构造函数接受 `phoneLogical` 选项；新增 `PHONE_LOGICAL_W=844 / PHONE_LOGICAL_H=390`；
    - `ensureSize()` 在 phoneLogical 时固定逻辑画布 844×390，`resolveLayoutProfile` 用 844×390 → 仍判 `mobile-normal`（**不切 Desktop**）；
    - 新增 `applyPhoneScale()`：`s=min(pw/844, ph/390)` 以 `transform: scale` contain 放大居中（CSS `top left` origin）；
    - `isMobile` 基于 `profile.mode`（phoneLogical 下正确为 mobile）。
  - `tests/demoPlayerRuntimeP0.test.ts`（新增，8 例）：守 phoneLogical 强制 mobile-normal（对比 1920×1080 才判 desktop）、420×210/844×390 走 Compact Mobile profile、桌面 844×390 contain 放大 + 点击逻辑坐标经 WebInput 归一化正确反算（2x/1x 等价）、main.ts 配置契约（dev:player / __PLAYER_MODE__ / ?player=1 / if(!playerMode) / 不挂载 toolbar/panelA/panelB / 角标守卫 / phoneLogical:playerMode / applyPhoneScale 常量）。
- **严守禁止**：未改 Gameplay/Physics/Weapon/经济数据；未维护第二套对外玩家界面；未用浏览器宽度把桌面演示自动切回 WebDom；未用手机 Canvas 单测证明「桌面 DEV 录像不存在」（根因是入口选择错误，已直接修复并结构性禁挂载）；点击坐标反算由 `platform/web/input.ts` WebInput.bindPointer 的 `localX=(clientX-rect.left)*clientWidth/rect.width` 归一化天然正确（必改#6 天然满足）。
- **测试**：scoped `tsc --noEmit` 0 错；P0 测试 8 + host 18 + platform 19 + review 8 = 53 通过；Canvas 宿主回归 113 通过（homeFrameIA 61 / mobileLandscape 12 / metaShell 5 / mobileGarageLayout 7 / homeStageR3 24 / resultUxR1 4）。
- **Commit/Push**：`e139fe9`（`F-DEMO-PLAYER-RUNTIME-P0｜统一手机玩家演示入口`），6 files changed, 291 insertions(+), 37 deletions(-)；push `f9d0ca3..e139fe9`；**3-way SHA 一致**（local=remote=e139fe95ae4bddea37cae2c15d9e0002e271cbc6）。
- **验收对照**：
  - 本地明确启动命令 `npm run dev:player` + URL `?player=1` ✅
  - 1920×1080 桌面打开仍显示完整手机横屏游戏画面（phoneLogical 844×390 + contain 放大居中）✅
  - 420×210 / 844×390 手机尺寸走 Compact Mobile profile ✅
  - 页面不存在开发工具/侧栏/版本角标/Physics Lab 标题（playerMode 下结构性不创建）✅
  - 首页→车库→首页→匹配→战斗→结算同一 CanvasPlayerUIHost ✅
  - 点击区域与视觉按钮一致（WebInput 归一化坐标）✅
  - 进入默认显示正式首页（Canvas 宿主默认 garage/home，不进桌面配置编辑器）✅
  - Pages 生产构建（`__PAGES_PREVIEW__` 已默认 Canvas 手机横屏）+ 新增 `dev:player` 入口，本地/Pages/真机共用同一 CanvasPlayerUIHost 玩家流程 ✅
- **下一步（待用户）**：stop-and-wait。真机横屏玩家流程录屏验收（首页→车库→匹配→战斗→结算全链路是否纯 Canvas 无 DOM 开发工具）。当前上下文未收到下一条独立 Queue。

---

## F-MOBILE-VISUAL-BASE-R1｜建立统一手机游戏视觉体系 —— 已完成（commit pending）

- **基线**：执行时 `foundation-02-wechat` 远端最新 HEAD = `e139fe9`（F-DEMO-PLAYER-RUNTIME-P0 已合入，P0 依赖满足）。
- **调查（先查后改）**：玩家界面整体仍是深灰底（`#242b38` / `C.panel` 中性灰）、细描边（`C.border=#38414f`）、灰色小字（`C.textDim=#9aa4b5`）和调试矩形，无统一手机游戏视觉层级。无真实可复现功能性 bug，**本队列为纯视觉语言统一**（不依赖真机复现即推进，符合 Queue「不等待正式美术资源先用现有 Canvas 能力」）。
- **真实 R1 改动（语义视觉源 + 全页落地，非换色名工程）**：
  - **新增语义视觉源 `src/ui/visualTokens.ts`（V）**：覆盖 Queue 必改#1 全部语义分组（场景背景/地面、普通/强调面板、主按钮金黄/次按钮轻量、主/说明/弱文字、胜利绿/失败红、我方蓝/敌方橙、统一描边/圆角）。单一事实来源。
  - `src/ui/canvasPlayerUIHost.ts`：
    - `C` 调色板 22 key 全部重写为**映射到 V**（保留历史 key 名以兼容 153 处调用站点；禁止「只改名字而画面不变」）。
    - 新增统一圆角矩形 helper `panel()`（替代逐处 `rect()+strokeRect` 直角硬边框；圆角 `radiusL=14`/`radiusM=10`，描边 `strokeW=1.5`，分组靠色块/间距/明暗而非重框）。
    - 重写 `button()`：主按钮=金黄实底 `V.primary` + 深底文字 `V.primaryText`、选中=蓝实底 `C.blue`、次/普通=轻量半透明蓝 `V.secondary`（无重框）；字号层级不变（mobile 17/14，桌面 15/12），**不全局缩字号**。
    - 落地到：drawPlayerTop（panelSolid 顶栏）、桌面 drawGarageDock（dockBg 底）、drawGarageMetaPage/Backpack/More（panelSolid 面板）、drawHomeBottomEntry（轻量次级 chip）、**drawMobileTopBar**（顶栏 panelSolid + 金币金黄 + 段位/能量次级文字 + 能量条 panel+ownBlue/lose）、drawGaragePanelOptions（部分可见态用 V.panel/ownBlue 选中/敌方橙锁定）、drawMatchingContinuum（VS 半透明白、状态 金黄/次级、对手名 敌方橙）、drawHud/drawHudShort（HP 条 我方蓝/敌方橙、战斗阶段 金黄、倒计时/警示 失败红）、drawResult（桌面结算卡 圆角 + 胜负绿红 + 部件卡极淡分隔 + 金币金黄）、drawModal（圆角卡片 + 标题语义色 win/lose + body 次级文字 + 奖励行语义 tone + 部件卡极淡分隔线 V.borderSoft）、drawReadyOverlay（READ 次级 / 开战 金黄）。
  - `src/render/renderer.ts`：消费 V——`drawHomeBackdrop` 渐变带改用 `V.arenaBg*`（**首带保留字面 `#0a0d13`** 满足 homeLayout.test.ts:116 源码守卫契约）；非 home 战斗/车库地面改 `V.arenaGround` / 顶缘 `V.arenaGroundEdge`（语义「深色竞技场」，非中性灰）。
- **测试同步（真实视觉变更的合理契约更新，非换色名工程）**：
  - `tests/homeLayerP0.test.ts` 测试4 断言旧蓝 CTA `#3b6fd4`→新金黄 `#ffb229`（与 Queue「主操作金黄」意图一致；UI 控件仍绘制的契约保留）。
  - `tests/resultUxR1.test.ts` 源码守卫 `C.green/C.red`→`V.win/V.lose`、`rgba(255,255,255,0.06)`→`V.borderSoft`（语义同值）。
  - 新增 `tests/mobileVisualBaseR1.test.ts`（8 例）：守语义 token 全定义、主/次权重差异、蓝/橙不可互换、胜/负稳定、旧中性灰/浅金主色已弃用、宿主 import 并消费 V、圆角 panel 落地、button 命中 V.primary/V.secondary/C.blue。
- **严守禁止**：未引入大规模新美术资源（仅 Canvas 色值/圆角/描边）、未修改页面功能和数据、未全局缩字号（字号与旧版一致）、未做只改颜色名而画面不变的 token 工程（V→C 映射使所有调用站点实际取新值；新增 mobileVisualBaseR1 守落地）。
- **测试结果**：targeted（mobileVisualBaseR1 8 + homeLayerP0 5 + homeLayout 6 + canvasPlayerUIHost 18 + modalFrame 8 + resultUxR1 4 + battleHudUx + homeStageR3 + mobileGarageLayout + homeMatchFx + matchFrameIA + homeFrameIA）全过；scoped `tsc --noEmit` 0 错。全量套件 **1116 passed / 1 failed**；唯一失败 `tests/pagesPreview.test.ts:60` 是 `src/main.ts` 既有守卫不匹配（与本次无关，本 diff 不触 main.ts/scripts）。
- **下一步（待用户）**：stop-and-wait。Commit/Push `F-MOBILE-VISUAL-BASE-R1｜建立统一手机游戏视觉体系` + 3-way SHA 核对；等真机横屏观感回执（首页/车库/匹配/Battle HUD/结算是否已统一「深色竞技场+金黄主操作+蓝橙阵营」层级，不再呈后台表单/调试面板质感）。

## F-LOBBY-GARAGE-DEMO-R1｜重构手机首页与车库主循环 —— 完成并推送

- **Commit**: `a1c76d451e99f53cdd0f6bf6ec65f7abbeef8e1c`（branch `foundation-02-wechat`；local = remote = commit 三路一致）。
- **Base（parent）**: `479ce1b05d9014787360b90f12da3794c16ea7f5`（F-MOBILE-VISUAL-BASE-R1）。
- **修改文件（6）**: `src/ui/canvasPlayerUIHost.ts`、`tests/mobileLandscape.test.ts`、`tests/homeMatchFx.test.ts`、`tests/metaShell.test.ts`、`tests/mobileGarageLayout.test.ts`、`tests/wechatInputContract.test.ts`。
- **本队列真实缺口（调研结论）**：前序队列已建好首页看车舞台、车库左车右板、轻量次级入口、唯一主 CTA、nav:home 返回、2×2 分类、二级选择、库存/Tips 保留。本队列核心是**配置第一层分组重排** + **量化验收锁定**。

### 关键改动
1. **配置第一层分组改为玩家认知 4 类**：`drawGaragePanelHome` 2×2 改为 `车身 / 移动 / 武器 / 辅助`（旧：车身/轮子/驱动/武器）。轮子、驱动、挂点细项收进二级。
   - 注：id 分隔符不一致的历史债——`entry:body` 用冒号，其余 `entry-move`/`entry-weapons`/`entry-gadgets` 用连字符；测试按 `startsWith('entry')` 统配。
2. **新增移动二级 `drawGaragePanelMovePick`**（取代已删的 `drawGaragePanelWheelPick`）：前轮/后轮/驱动三行；行高动态适配面板可用高度（`h = clamp(availH/3, minTouchH, targetTouchH)`），修掉 844×390 视口驱动行被溢出条件截掉的 bug。
3. **新增辅助二级 `drawGaragePanelGadgetPick`**：与武器二级同构，按类别过滤共享硬点（gadget 类别）。
4. **dispatch 路由更新**：`entry-move`→movePick、`entry-gadgets`→gadgetPick、`gadget-slot:`→onToggleGarageSlot、`slot:drive`→onToggleGarageSlot、`panel-back` 处理 movePick/weaponPick/gadgetPick；删除 `entry-wheels` 死代码块。

### 验收锁定（新增 F-LOBBY-GARAGE-DEMO-R1 验收矩阵测试，mobileLandscape.test.ts）
- 配置第一层恰好 4 分组（车身/移动/武器/辅助），一级不暴露 wheel-side:/slot:drive/weapon-slot: 细项。
- 第一主动作始终是「寻找对手」：车库 CTA 宽 ≥ 任意单个 entry 宽。
- 3 次点击换武器：点「武器」→ 点武器位 → 点选项（pick 派发 ≥1）。
- 返回首页后无车库配置残留（无 entry/opt/wheel-side 命中区）。

### 测试结果
- **targeted（5 文件）**: 49 passed（含新增 1 例验收矩阵）。
- **scoped tsc**: 0 错。
- **全量套件**: 1117 passed / 1 failed；唯一失败 `tests/pagesPreview.test.ts:60` 是 `src/main.ts` 既有守卫不匹配，**与本次无关**（已确认在 base commit 同样失败，pre-existing；本 diff 不触 main.ts）。

### Git 环境备注（本次实战）
- 本会话启动时发现 `.git/refs/heads/` 丢失、object store 半残（`479ce1b` 一度 cat-file 不可达）。先 `git fetch origin foundation-02-wechat` 恢复 base 对象，再手动 `mkdir -p .git/refs/heads && printf <sha> > .git/refs/heads/foundation-02-wechat` 恢复本地分支 ref 到 `479ce1b`，然后 commit 正常落盘（`a1c76d4`）、push 成功（`479ce1b..a1c76d4`）、3-way SHA 一致。
- **教训**：本仓库 ref 写入仍不可靠，每次 commit 后必须 `git rev-parse HEAD` + `cat .git/refs/heads/<branch>` 双重核验落盘；ref 缺失时优先 fetch origin 恢复 base 再手动写 ref，不要重做已完成工作。

### 下一步（待用户）
- stop-and-wait。等真机回执（首页看车→车库调整→返回首页→寻找对手 闭环是否成立、移动/辅助二级在真实矮屏是否不溢出）；确认后开下一条独立 Queue。
