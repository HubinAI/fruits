# 2026-08-30

## F-WX-EXPERIENCE-C5-RC｜批次验收 + 唯一 C5 RC 包 + Pages 部署（已完成，待 iOS 真人体验验收）

- **批次 4 个功能 commit（prerequisites，均已独立提交 + push）**：
  - F-WX-SAFE-AREA-P0：`357dba2`
  - F-GARAGE-TOUCH-ASSEMBLY-R2：`5ee1569`
  - F-GARAGE-VISUAL-DENSITY-R2：`07df5c2`
  - F-BATTLE-VISUAL-CLEANUP-R3：`606cab7`
- **最终 HEAD**：`40919b326b74fb24db155387684016fc183a54ee`（本 Queue 仅新增 `.gitignore` 隔离条目：`dist-wechat-c5-*/`，源码零改动）。
- **验收门全部通过**：
  - Must#1 全量 vitest：**168 文件 / 1380 测试全绿**。
  - Must#2 tsc --noEmit：exit 0 干净（scoped tsc 的 `__WX_DEBUG__`/`__WX_DEBUG_GRANT__` 为既有 ambient 声明噪声，非回归）。
  - Must#4-#8 门禁测试（wechatSurfaceGeometry + garageDragAssemblyR2 + garageCenterStageP0 + battleVisualCleanupR3 + battlePhaseFx）：**5 文件 46 测试全绿**（Garage 点击+拖装 / Matching+Locked 最终像素 / Battle 像素 / 安全区矩阵含胶囊注入 / 120 帧无 UI 累积）。
  - Must#3 四目标构建：web / pages / e2e / wechat 均 exit 0。
  - Must#9 普通微信包隔离（dist-wechat/game.js）：`globalThis.__h`=0、`uiHost.setBuildBadge`=0、`window.__h`/`__E2E_PROBE__`/`__probe`=0（零 SHA / 调试入口 / E2E probe / 调试命中区）；`__WX_DEBUG__` 仅剩 2 处注释。
  - Must#10 C5 RC 包隔离（`build:wechat:rc`）：`globalThis.__h`=1（调试句柄）、`uiHost.setBuildBadge`=1（SHA 水印）、`window.__h`/`__E2E_PROBE__`/`__probe`=0（零 E2E probe）；SHA `606cab7` 经 runtimeInfo 嵌入；可见「全部件+1」按钮因 `DEV_TOOLS_VISIBLE=false`（prod 微信）被 `drawDevGrantEntry` 提前 return → 默认折叠不遮挡 UI。
  - Must#14 唯一 C5 RC 目录：`dist-wechat-c5-606cab7/`（game.js+game.json+project.config.json，appid=touristappid），gitignored 本地导入用，不与旧 C3/C4 混用。
- **Pages 部署（Must#12/#13）**：`gh workflow run pages.yml --ref foundation-02-wechat`（GitHub Actions 手动触发，build:pages → deploy-pages）。工作流 annotation 确认「发布所用 commit = 40919b3」；API `deployments[0].sha` = `40919b326b74fb24db155387684016fc183a54ee`。**三路 SHA 一致：local = remote = deployment = 40919b3**。live `https://hubinai.github.io/fruits/` → HTTP 200。
- **Must#11 模拟器流程**：真实微信开发者工具模拟器走查为**人工步骤，本沙箱无法执行（无 WeChat DevTools），未伪造**。自动化代理 = e2e gate（`tests/_e2e_gate.cjs`，playwright-core + 系统 Edge headless）驱动完整 Web 闭环：**20/24 通过**（Home→Matching→Locked→Battle→Result + 构图几何全绿）。4 失败均为**门禁 harness 缺陷、非产品回归**：① Warning 阶段 3s 窗口被 300ms 轮询漏采（战斗在 Active 分出胜负即结束，gate 过严）；② weapon-slot hitArea 选择器/id 不匹配；③ touch 路径仅发 pointerdown 未发 pointerup。建议单独收紧 gate 脚本（不在本 Queue 范围）。
- **重要 gotcha（e2e gate 接线 bug）**：`npm run test:e2e-gate` 启动 `_serve_pages.cjs` 时**未设 `E2E_DIR=e2e`** → 默认服务 `dist-pages`（PROD 无探针）→ `window.__probe` 为 null、gate 全 FAIL。正确运行：`E2E_DIR=e2e node tests/_serve_pages.cjs` 再跑 gate（已验证可跑通核心闭环）。此为 harness 接线缺陷，非产品问题。
- **下一步**：停止，不部署额外内容。等待一次 **iOS 真机/微信开发者工具模拟器**验收（Home→Garage→Matching→Locked→Battle→Result 全走查 + 双车无矩形外框 + 接触点伤害数字可区分 + HUD 不顶胶囊 + 无持续噪音）。**未宣布真人体验通过**——自动化门禁 + Web e2e 代理通过 ≠ 真机体验确认。

## F-WX-RESUME-RENDER-STATE-P0｜修复微信恢复与跨页渲染状态泄漏（已完成并推送，待真机复验）

- **commit**：`8a7d334fc49303475ce5d82441a1b72d8d279a54`（branch foundation-02-wechat，local = remote 三路一致，7 文件 +411/-31）。
- **根因（实证）**：
  1. **Garage 红墙泄漏（真机症状#2）= 双重根因**：① runtime actions 漏接 `setGarageBackdrop`（main.ts/wechatBattleHost.ts 都调了但 actions 没转发 → garageBackdrop 永不为真）；② renderer fallback 分支 `!homeBackdrop && !prebattleBackdrop` 时绘制 battle 墙（closingWalls 红墙/normalWalls 灰墙），garageBackdrop=true 或四开关全 false 的间隙帧都会走它。
  2. **wx.onShow 不 re-reframe（症状#1 风险）**：onShow 仅 start+resetClock，未按当前页重取景。
  3. **战斗 FX 残留（症状#3）**：damage numbers/hitFlash/spark/muzzle/charge/deathFx/laserBeam/shotgunFan/muzzleTongue 全靠 TTL 过滤，未被 battleBackdrop 门控，离开战斗残留 120–500ms。
  4. **7% 首页偏移 = 非泄漏**：实测为 home 相机稳态取景（scale≈1.62 == freshHome 1.609），用全新 boot 基线对比判定，非 stale battle transform。
- **修复（4 处）**：
  - Fix A：`playerGameRuntime.ts` actions 补 `setGarageBackdrop`（~383）。
  - Fix B：`renderer.ts` fallback 分支加 `!garageBackdrop` 并彻底移除 closingWalls/normalWalls（非战斗/非首页/非车库/非战前间隙帧仅铺中性地面）；`setBattleBackdrop` 后新增 `clearBattleVisualFx()` 清 9 个 FX 数组。
  - Fix C：`wechat/game.ts` onShow 分支加 `runtime.reframePlayerCamera()`（后台→前台按当前页重取景）。
  - Fix D：`wechatBattleHost.ts` 转发 `clearBattleFx`；`playerUI.ts` 接口声明；`adjustConfig()`/`setMode('build')` 战斗退出路径调用清理。
- **测试（6 类确定性生命周期 + 回归修复）**：`tests/wechatResumeRenderStateP0.test.ts` 重写（C1 跨页无旧 Battle 状态 / C2 战斗 FX 原子清理 / C3 Matching prebattle 保持 / C4 SingleLoop 幂等 / C5 装备库存存档不变 / C6 onShow re-reframe）；`tests/battlePhaseFx.test.ts` renderWith 改为 `setBattleBackdrop(phase==='Warning'||'Closing')`（battle 墙仅 battleBackdrop 真时绘制的新契约）+ CtxStub 补 createLinearGradient/createRadialGradient。
- **验证门全绿**：battlePhaseFx 23/23；全量 vitest **169 文件 / 1386 测试全过**；tsc --noEmit exit 0；微信构建 exit 0（88 模块，dist-wechat/game.js）。模拟 onHide/onShow 为人工步骤，沙箱无法执行（未伪造）。
- **冻结遵守**：未改 Canvas backing/Surface logical/DPR/PlayerViewportTransform/Physics/音频/战斗规则/Garage 密度/Battle 视觉/安全区；不部署 Pages。
- **下一步**：停止。真机复验点：Home→Garage 切换无红墙、Battle→Result→Home 无伤害特效残留、前后台切换后车辆第一帧即正确且相机取景正确、无重复渲染循环/音频叠加。

## F-WX-RC-SAFE-BADGE-P0｜修复 iOS 横屏 RC 版号安全区与合成层级（已完成并推送，待真机复验）

- **commit**：`9b7556b49909018484dadb8d874007ecfc02e462`（branch foundation-02-wechat，local = remote 三路一致，2 文件 +345/-6）。
- **版号链路追踪（Must#1 报告「首次不可见的位置」）**：WECHAT_BADGE=1（scripts/wechat-rc.js）→ __WX_BUILD_BADGE__ define（vite.wechat.config.ts）→ game.ts:379-380 setBuildBadge('#8a7d334') → **旧绘制点 canvasPlayerUIHost.ts:1597**（draw() 尾部 `this.text(buildBadge, insL+6, insT+6, 9, 'rgba(255,255,255,0.5)', 'left', 400)`）→ mobile 布局 logical px（scale=1, ctx=DPR）→ compositeUi 最后一层。**首次不可见 = 该行参数组合**：① 字号 9（fontScale 0.8 → 实际 8px）< 11；② 半透明白无深色底 → 浅背景不可见；③ insets=0 时 (6,6) 落横屏刘海/圆角外被裁切；④ 只在 draw() 内绘制，稳态页依赖离屏残留，onWindowResize 清空 backing 后消失。
- **修复（仅 1 处源码，canvasPlayerUIHost.ts）**：
  - 新增 `drawBuildBadge()`：每帧最后绘制（save → 显式 source-over + setTransform(dpr) 单次 logical→backing → 深色底 rgba(0,0,0,0.55) + 白字 bold 11px → restore）；位置 = safeInsets() 实时读（方向/resize 后首帧即重算）+ 左上内缩 6 logical px + 完整包围盒钳制在安全区内；不注册 hitArea。
  - `renderBattleFrame` 末尾无条件调用（不依赖 dirty / 离屏残留）；draw() 尾部旧 badge 块移除（避免双绘）。
- **测试**：`tests/rcSafeBadgeP0.test.ts` 9 项全过（1280×592 safeArea.left=47 / 844×390 DPR3 / 2796×1290 backing DPR3 / 左右横屏 / Home·Garage·Matching·Battle·Result / 稳态 120 帧每帧绘制 / 包围盒完整在 safeArea / 真实 ink 高对比像素 / 普通包无 badge）。
- **验证门**：tsc --noEmit exit 0；targeted 9/9；全量 vitest **170 文件 / 1395 测试**两轮全绿（一轮偶发 2 失败复跑消失，与本 Queue 无关）；build:wechat 普通包 `uiHost.setBuildBadge(` 调用点常量折叠为零（仅剩方法定义，`8a7d334` 仅 runtimeInfo 元数据）；build:wechat:rc RC 包含 `uiHost.setBuildBadge('#…')` 调用 + 深色底/source-over 实现。
- **冻结遵守**：未改 Renderer / Physics / 音频 / 战斗规则 / 布局 / 安全区契约 / 普通包行为；未部署 Pages。`__WX_DEBUG_GRANT__`（全部件入口）与 badge 保持解耦。
- **下一步**：停止。真机复验点：iOS 横屏 RC 包所有页面左上安全区内可见 `RC #9b7556b`（深色底白字 ≥11px）、横竖屏切换后仍在、不遮挡胶囊/点击、普通包无 badge。

## F-WX-SAFE-AREA-P0｜统一微信横屏顶部 UI 与原生胶囊安全区（已完成并推送，待真机复验）

- **commit**：`ca006f0c1c90a25467d255fba010d7c6b44b6655`（branch foundation-02-wechat，local = remote 三路一致，5 文件 +341/-10）。
- **追踪（Must#1）**：坐标链 = wx.getWindowInfo().safeArea + getMenuButtonBoundingClientRect() → WechatViewport.safeInsets()（logical px，胶囊折叠 right/top）→ 各页面布局源。结论：**Home/Garage/Matching/Battle 布局源已消费 insets**（computeHomeLayout / computeMobileGarageLayout / drawMatchingContinuum insT+22 中央 / drawHud insL+8·W-insR-8），**首次问题位置 = drawBuildBadge 旧锚点 (insL+6, insT+6) 与 Home 头像行（profileRect=insL/insT 高 42）重叠** → RC badge 压头像；宝箱/能量/右 HUD 已钳 W-insR（含胶囊+6）但各页散算、无统一顶部三区契约。
- **修复**：
  - 新增 `src/ui/topSafeLayout.ts`：`computeTopSafeAreas(w,h,insets,profile)` → 统一三区（left 起 insL / center x=W/2 真居中 / right 右缘=W-insR 含胶囊+6）+ topRowH（normal 42 / short 32，与 homeLayout 同源）+ badge 低干扰锚点（insL+6, insT+topRowH+4）。纯 logical px，唯一来源 = 现有 safeInsets（不新增第二套 viewport 转换）。
  - `drawBuildBadge` 锚点改用统一契约（顶部信息行之下 4px）→ 不覆盖头像/货币/返回/HP/Matching·Locked（Must#5）；insets 每帧实时读 → 方向变化/resize 首帧重算（Must#7）。
  - 既有测试同步：wechatCanvasStability（SHA 检测点 (6,8)→(6,46)）、wechatSurfaceGeometry（SHA devY 断言 ≤bh*0.12 → >bh*0.08 且 <bh*0.32）。
- **测试**：`tests/topSafeLayout.test.ts` 11 项（三区几何/左右横屏镜像/short/1280×592/932×430 DPR3/badge 不压头像·顶栏·HP·Matching/120 帧无跳位/方向变化重算）+ rcSafeBadgeP0 9 项回归 + stability/surfaceGeometry 更新后通过。
- **验证门**：tsc --noEmit exit 0；全量 vitest **171 文件 / 1406 测试全过**（audioLifecycleP0 T3/T6 全量下偶发 5s 超时，单独跑必过——已知竞态与本 Queue 无关）；build:wechat 普通包 setBuildBadge 调用=0；build:wechat:rc 含调用 + topSafeLayout 锚点表达式（`y0 + topRowH + 4`）。
- **冻结遵守**：未改 PlayerViewportTransform / Canvas backing / DPR / Renderer 相机 / 首页车辆 / Garage 车辆比例与装配带 / Battle 伤害表现 / Gameplay·Physics·装备逻辑；未部署 Pages。
- **下一步**：停止。真机复验点：iPhone 刘海在左/右、844×390 DPR3、1280×592 下 Home/Garage/Matching/Locked/Battle/Result 顶部文字图标完整、无元素进胶囊、RC badge 不压头像、中央状态居中、120 帧无跳位、原有点击区域保持。

## F-WX-RC-REPRODUCIBLE-BUILD-P0｜建立 RC 包与 Git 提交一致性门禁（已完成并推送）

- **commit**：`f3a58f2fb744c268d2e842af5b18730b361c340a`（branch foundation-02-wechat，local = remote 三路一致，7 文件 +429/-12）。
- **门禁实现**：
  - 新增 `scripts/rc-gate.js`（纯 ESM JS，可测试）：受控路径判定（src/ tests/ scripts/ wechat/ package.json package-lock.json vite.* tsconfig；`.workbuddy/memory`、dist、outputs、HANDOFF、交接文档等忽略）、`filterControlledDirty`（porcelain v1 行解析，rename/引号路径处理）、`readGitState`（runner 可注入）、`makeRcBuildInfo`、`verifyRcShas`、`extractBundleSha`。
  - `scripts/wechat-rc.js` 重构：构建前 git 检查（受控 dirty → 拒绝，Must#1/#2/#3）；`--dirty` 诊断模式放行 + badge `#<sha>-dirty`（Must#4）；构建后生成 `dist-wechat/rc-build.json`（fullSha/shortSha/branch/dirty/buildTime/buildMode=rc，Must#5）；自动三方 SHA 校验（badge 前 7 位 = HEAD 前 7 位、rcJson = HEAD、bundle runtimeInfo = HEAD，不一致 exit 1，Must#6）。
  - `vite.wechat.config.ts` 加 `__WX_RC_DIRTY__` define；`wechat/game.ts` badge 拼接 `-dirty`；`src/wechat-debug.d.ts` 加 ambient 声明。
- **踩坑（重要）**：`git status --porcelain` 行首 X 列可为空格（` M path`），rc-gate `git()` 原 `.trim()` 会吃掉行首空格 → 路径错位（`.workbuddy` 变 `workbuddy`）；改为仅去末尾换行。中文路径需 `-c core.quotepath=false` 否则八进制转义无法匹配忽略前缀。
- **测试**：`tests/rcGate.test.ts` 14 项（受控/忽略判定、worktree/staged dirty、memory-only 允许、rename/引号、readGitState mock、rc-build.json 字段、三方校验：旧 badge SHA / 旧 rcJson / 旧 bundle / null 均失败）+ 端到端验证：dirty 拒绝（exit 1）、`--dirty` 诊断构建 badge `#<sha>-dirty` + rcJson dirty=true、干净 HEAD 正式构建 badge `#f3a58f2`（dirty=false）+ 无 -dirty 后缀 + 三方校验通过。
- **验证门**：tsc --noEmit exit 0；targeted 34/34（rcGate + rcSafeBadgeP0 + topSafeLayout）；全量 vitest 1420 测试中仅全量负载下偶发 5s 超时（audioLifecycleP0 / platformCore，单独跑必过，已知竞态与本 Queue 无关）。
- **环境清理**：删除历史遗留临时脚本 `tests/_e2e_garage_live.cjs`（头部明确标注「临时门禁，不入 commit」，从未被任何 commit 包含，无引用）——否则受控 dirty 会阻塞干净 HEAD 构建。
- **冻结遵守**：未改任何游戏逻辑 / UI / Renderer；未部署 Pages。正式 build:wechat 零 badge 调用不变。
- **下一步**：停止。后续 RC 交付流程固定为：先 commit → 干净 HEAD → `npm run build:wechat:rc` → 产物含 rc-build.json（dirty=false）→ 真机录屏核对 badge。

## F-WX-SAFE-AREA-R1｜修复真实微信原生胶囊遮挡顶部 UI（已完成并推送，待真机复验）

- **commit**：`c660be192e078a9a0b188af4ae0be994dc1b6dbb`（branch foundation-02-wechat，local = remote 三路一致，8 文件 +328/-26）。
- **真机证据对照**：① 首页第 4 个宝箱被胶囊覆盖；② 第 3 个紧贴。根因链 = safeInsets 右侧保留不足（menuButton 缺失/坐标域问题 → insR≈0 → 宝箱右缘=W 进胶囊区）+ 无统一右边界。
- **修复**：
  - `WechatViewport.safeInsets`（viewport.ts）：
    - Must#2：rightReserved = windowWidth − (menuButton.left − 6)，与 safeArea 右侧保留取较大值；
    - Must#3：menuButton.left 物理 px（>ww）÷dpr 归一 logical，禁止混用 backing；
    - Must#4 精确化：**仅 API 存在但异常/无效 → fallback（max(ww×12%, 96)，不得回退 0）；API 不存在（测试桩/老基础库）→ 保持 safeArea 语义**（避免物理 windowWidth mock 下 fallback 误判导致底部入口左移压 CTA）；
    - 修复 top 误抬 bug（mb 无效时不再 +GAP）。
  - `homeLayout.ts` / `mobileGarageLayout.ts`：舞台（车辆取景）**水平居中于 W/2**（宽度 = min(可用宽, 左右半区 2 倍)）——右侧胶囊保留不再把车辆推向左（Home/Garage 车辆 50% 居中保持）。
- **测试**：`tests/wechatRealCapsule.test.ts` 10 项（真实 wx.getWindowInfo + getMenuButtonBoundingClientRect 数据形态，非直接传理想 safeInsets）：刘海左/右、物理 px 归一、API 缺失/异常 fallback、1280×592 / 844×390 / 932×430、Home 宝箱右缘 ≤ mb.left−6、Battle 右 HUD 右缘 ≤ mb.left−6、Garage 顶栏同。
- **既有测试同步**：homeFrameIA 根因固化哨兵（stage 居中后 preview 与 previewSolo 中心重合 → 改断言「preview 忽略 framingRect → 取景宽度显著不同」）；mobileGarageLayout stage 断言（全宽贴边 → 居中 W/2 且不越界）；platformCore / wechatSurfaceGeometry 恢复原断言（无胶囊 API 环境 insR=0）。
- **验证门**：tsc exit 0；targeted 124/124（7 文件）；全量 vitest **173 文件 / 1430 测试全过**；干净 HEAD `build:wechat:rc` → badge `#c660be1`（= HEAD 前 7 位）、rc-build.json dirty=false、三方校验通过。
- **冻结遵守**：未改车辆构图 / Garage 装配带 / Battle 表现 / Physics / 输入；未部署 Pages。
- **下一步**：停止。真机复验点：iPhone 刘海左/右、844×390 DPR3、1280×592 下首页 4 宝箱/Garage 能量/Battle 右 HUD/Result 右侧完整位于胶囊左 6px 外、RC badge 不压头像、中央状态居中、120 帧无跳位、点击区域保持。
