- **F-WX-9B｜Garage 主构图收敛**（commit 待本批次）：只收敛 Garage 第一屏构图，不调二级选项/Battle/经济/Physics。
  - **实测（含 parts 口径）**：MOBILE_SOLO 400 宽框下 844/932/960 车辆占比已 32.5-32.6%（UI-1 后本就 ≥32%）；但**车辆垂直偏下 dy≈+22px**（框中心 605 vs 车辆世界中心 660.5——groundY=700、车辆 [622,699] 贴地）。「不居中/贴底感」真实来源 = 框中心与车辆中心错位。
  - **修复（仅 renderer 常量）**：MOBILE_SOLO_MIN_Y/MAX_Y [550,660] → **[589,699]**（框中心 644，车辆中心 660.5）→ **三屏 dy=0.0 全部垂直居中**，占比不变（32.5-32.6%，≥32% 达标）。灵敏度实测 ~0.56/单位（dy 非简单 (vcy−boxCenterY)×scale，受 minY/bh 双因素）。
  - **测试**：mobileBattleFraming previewSolo 占比下限 28→32% + 新增垂直居中断言（|centerY−rectCenterY|≤8，三屏）；mobileLandscape 验收1/2 加「一级配置入口恰好 4 个」（entry* 长度=4 且含 body/wheels/drive/weapons，**坑**：sorted 后 entry-weapons 按 ASCII 排 entry:body 前 → 改 toHaveLength+toContain）+ 新 it 源码字号守卫（6 个 Mobile 首屏方法内 this.text 第 4 参数 ≥14，正则 /this\.text\(([^)]*)\)/g + split(',')[3]）。
  - 验收：scoped tsc 0；全量 vitest ×3 **857 passed / 111 文件**稳定；Web build ✓ + WeChat build ✓（prod 零 debug 日志）。首屏右侧 2×2 + CTA + 轻量顶栏维持 UI-1 结构（merge 次级入口保留——「首屏不显示合成详情」≠移除入口；验收只数 4 个一级配置入口）。**不录像，继续 F-WX-9C。**
- **F-WX-9C｜Battle 主体尺寸修正**（commit 待本批次）：Mobile Active 单车视觉宽 18% → 24.4%（24~30% 目标），不动态追镜/不改 Physics/武器/速度/伤害/Garage。
  - **实测（含 parts 口径）**：battle 默认 spawn A（watermelon）world [315,558] 宽 243、B（banana）[1038,1295] 宽 257、groundY=700。旧 Active corridor [300,1340] + margin 64（CONTENT_MARGIN_WORLD）+ insetX 56（SAFE_INSET_X，Desktop 语义）→ safeW 仅 732/844 → 三屏占比仅 **18.0-18.4%**。
  - **修复（renderer 3 处）**：① MOBILE_ACTIVE corridor [300,1340] → **[315,1295]**（开局精确边界：A 左缘 315 / B 右缘 1295，margin 8 覆盖余量）；② compact battle margin 64→8（corridor 自带覆盖余量，同 fixed 框先例，分支 `(isFixed || fit==='battle') && isCompact`）；③ compact battle insetX 56→0（battle 左右无 UI，Active 不强求完整 Arena）。→ **三屏统一 24.4%**（A 完整左缘 7px 边距、B 完整右缘 ≤844）。数学推演确认：corridor 右界必须 ≥B 右缘 1295（否则 B 出屏）；A 全程完整（A 右缘最大 = 碰撞点 ≈1283 ≤1295+margin）；B 撞右墙极端帧 B 部分出屏但**碰撞点 1283 仍在 corridor 内**（验收 3 满足）。Desktop 非 compact 分支零变化（insetX=SAFE_INSET_X、margin=CONTENT_MARGIN_WORLD）。Warning/Closing 完整 arena 语义不变（仅 margin 8/insetX 0 让构图略放大，arena 仍完整）。
  - **测试**：mobileBattleFraming Active 占比断言 18-28% → **24-30%**（三屏）；rendererScaleTrace Battle Active 18-28 → 24-30；Warning 墙入屏/Desktop Active ≤18% 守卫不变（全过）。
  - 验收：scoped tsc 0；全量 vitest ×3 **857 passed / 111 文件**稳定；Web build ✓ + WeChat build ✓（prod 零 debug 日志）。**不录像，继续 F-WX-9D。**
- **F-WX-9D｜Matching / Result 可读性收敛**（commit 待本批次）：只整理两个中间状态信息层级，不扩大功能/不加确认步骤/不改奖励段位规则/无特效。
  - **Matching（drawMatchingVs）**：原只有 VS 大字 → 加中央双车标注「我方车 @30% / 对手车 @70%」（≥16px，renderer previewFixed 画 A 左 B 右）。
  - **MatchPreview（drawMatchInfo）**：删除对手零件长串（parts.join，开发原型味边缘信息）；bodyName 16→18（mobile）、标签 14→16、pill 14→16；保留驱动 pill。
  - **Result（drawMobileResult）**：重构为**统一中央单卡片**——先按固定行高（标题42/HP26/奖励44/段位44/引导26/按钮52）算 cardH → 画 `#141a26` 卡片背景（cardY 垂直居中）→ 内容堆叠；全部必要文字升 **16px**（HP/奖励/段位/引导），标题 24；按钮行 TARGET_TOUCH_H=52（≥52 ✓）。三列广告/两列布局保留（reward-ad 能力不删）。
  - **确认按钮核查**：runtime:536 `matchBarHidden=true`（Q15-FLOW-R1-ATOMIC 复核条常驻隐藏）→ MatchPreview 正常流程无「开始战斗/调整配置」确认按钮（9D「不增加确认步骤」已由既有冻结保证，未改）。matchBar 能力保留（验收5 显式 matchBarHidden:false 仍可测）。
  - **测试**：mobileLandscape +2（① 9D 源码守卫：drawMatchingVs 含我方车/对手车、drawMatchInfo 无 parts.join 有驱动·、drawMobileResult 含 cardH 统一卡片背景、三方法内 this.text 字号全部 ≥16；② MatchPreview 默认无 match-start/match-adjust hitArea）。
  - 验收：scoped tsc 0；全量 vitest ×3 **859 passed / 111 文件**稳定；Web build ✓ + WeChat build ✓（prod 零 debug 日志）。**完成后进入 F-WX-9-GATE。**
- **F-WX-9-GATE｜Mobile UI 批次合并验收**（commit cd7686a，本批次收口，零代码改动）：全绿——scoped tsc 0；full vitest ×3 **859 passed / 111 文件**稳定；Web build ✓ + WeChat build（prod）✓；**PROD 包零 DEV trace**（WX-VIEWPORT/WX-REF/WX-INPUT 全 0，WECHAT-RUNTIME marker 保留）；重点回归完整玩家闭环（Garage→改部件→寻找对手→Matching→Battle→Result→调整→再战）wechatPlayerSmoke+playerGameRuntime 9/9；local = remote = `cd7686a`。**F-WX-9 批次（9A 尺度日志 / 9B Garage 构图 / 9C Battle 主体 / 9D Matching+Result / GATE）技术链全部正常 → 停止开发，等待真人微信录像验收。**
- **F-WX-RCA-1｜车辆尺度无变化 Root Cause**（commit 待本批次）：拆双口径 Bounds + RCA 专用构建，不调 Camera/UI/Physics/阈值。
  - **双口径**：renderer `vehicleBounds(v, includeParts)`——coreBounds（Body+Wheels，玩家感知主体）/ envelopeBounds（+Functional Parts，完整外廓）。`scaleDiagnostics(snap)` 公共只读方法输出 view/core/envelope（world/screen/screenWidthPct）。**[WX-REF] 改双指标**（coreScreenWidthPct + envelopeScreenWidthPct，旧单 screenWidthPct 移除）；**[WX-RCA]** 新 gate（`__WX_RCA__`，reframe 时 garage/battle 段输出）。
  - **build:wechat:rca**：package.json script → scripts/wechat-rca.js（spawnSync vite build + env WECHAT_RCA=1，同 wechat-dev 模式）→ vite define `__WX_RCA__`（默认 false）。wechat/game.ts 加一次性 [WX-RCA] viewport 段（logicalWidth/Height/dpr + screenCanvas）。PROD 包**无 console.log 输出 [WX-RCA]**（grep 残留 2 处为 rollup 保留的 JSDoc 注释，无运行时日志）。
  - **测试**：rendererScaleTrace 重写（envelope 用 9B/9C 阈值不变：Garage 32-38 / Battle 24-30；core < envelope 双口径并存证明；DPR 一致性用 envelope；[WX-RCA] garage/battle 段 + false/undefined 零输出）；wechatPackage +1（双口径守卫：parts 在 includeParts 分支内=coreBounds 排除、build:wechat:rca script、__WX_RCA__ define、gate 位置）。
  - **坑**：① rendererScaleTrace 原 framingRect 用近似 {w:450} → envelope 30.3% < 32（用精确 garageFramingRect 几何后 32.5+）；② vitest 重复 `vi.spyOn(console,'log')` 返回**同一 mock 实例**（历史调用混入）→ 循环前 restoreAllMocks；③ wechatPackage gate 断言方向（if 行在日志前）+ [WX-RCA] 注释/代码字面量区分（indexOf("'[WX-RCA]'")）。
  - 验收：scoped tsc 0；全量 vitest ×3 **861 passed / 111 文件**稳定；Web build ✓；RCA build 含 [WX-RCA]（4 处含注释）、PROD 无日志代码。**完成后停止开发；只等用户 `npm run build:wechat:rca` → 微信工具重载 → 提供 [WX-RCA] 日志（viewport/Garage core+envelope/Battle Active core+envelope）→ 按 A（parts 污染）/ B（viewport-transform 链）/ C（构图假设）判定根因后再决定修改方向。**
- **F-WX-P0-PROVENANCE｜微信实际运行版本不更新（纯诊断，无 commit/push/代码改动）**：用户微信工具显示 cd7686a（无 [WX-RCA]），远端=ccdac9f。
  - **provenance 链全绿**：A) local HEAD = ccdac9f14e23...；B) remote = 同；C) game.js 内嵌 SHA = ccdac9f（ccdac9f:1 / cd7686a:0）；D) [WX-RCA] 日志代码在包内（4 处 = 2 rollup 保留注释 + 2 处 console.log("[WX-RCA]")：renderer step=garage/battle 段 + 入口 step=viewport 段；minify 单引号→双引号，grep 单引号 0 但 node 检查代码存在）；E) dist-wechat 绝对路径 D:/0818new/最强水果/dist-wechat（game.js 2026-08-24 11:10:04、1,117,978 B、SHA256 30158835883ec9ee5463105959ebbe192c0b3f95d659906c340269f6cdf460e4；project.config.json 同目录 projectname=zuixqiangshuiguo-wechat compileType=game）；F) **全仓库仅 1 个 dist-wechat**（find /d/0818new 无第二处），无旧目录。
  - **结论**：A=B=C 同一 SHA，代码/构建/产物链无断点——用户看到 cd7686a 的唯一原因 = **微信开发者工具未重新编译/缓存旧 game.js**（最后编译发生在 cd7686a 时代）。用户应重编译唯一目录 D:/0818new/最强水果/dist-wechat。
  - **坑**：直接 `vite build` 不带 WECHAT_RCA=1 = 普通 build（[WX-RCA] 仅 2 注释无日志代码）→ 必须走 `npm run build:wechat:rca`（scripts/wechat-rca.js 注入 env）。
- **F-WX-RCA-2A｜Garage 核心车体尺度根因修复**（commit 待本批次）：previewSolo Mobile 主尺度改为 coreBounds（Body+Wheels），不调 Battle Camera/Garage 布局/Physics。
  - **真实微信 RCA 数据**：Garage framingRect 后 core=14.0% / envelope=23.7%，framingRect 前 core=20.2% → 固定 MOBILE_SOLO 世界框（400 宽）+ framingRect 把车身主体缩小。**根因 = 固定世界框决定主尺度**（此前用 400 世界宽的框，车辆主体 170 宽只占 42% 框 → scale 被框宽压低）。
  - **修复（renderer previewSolo compact 分支 + 常量）**：删 MOBILE_SOLO_MIN/MAX 固定框常量 → 新增 `MOBILE_SOLO_MARGIN_X=65 / MARGIN_Y=24`；compact previewSolo bounds = **vehicleBounds(snap.vehicleA, false)（coreBounds）外扩 margin**。横向 65 世界：core 170 → bounds 300（同时覆盖普通武器外伸：envelope 243 < 300 完整入画，极长武器尖端允许越界）；纵向 24（车高 77 → bh~125，保持横向主导）。scale = safeW/(coreW+130) ≈ 1.48-1.69 → **core 屏占比三屏统一 29.9~30.0%（28~34 ✓，真实微信旧 14% → ~2.1 倍）**；envelope 42.7%（屏内完整）；core 垂直居中 dy≈-7（≤8 容差）。
  - **测试**：mobileBattleFraming vehicleScreenBounds 加 includeParts 参数（默认 true 保持 battle 用例）；previewSolo 用例改 core 口径验收（占屏 28~34% + core/envelope 完整入画 + 核心中心在展示区 + 垂直居中 + envelope>core）；rendererScaleTrace Garage 用例 envelope→core（28~34）。**验收口径从 envelope 换 core（Queue 明确禁止用 envelope 作为车辆尺寸验收；阈值 28-34 为 Queue 给定新标准，非改阈值）**。
  - 验收：scoped tsc 0；全量 vitest ×3 **861 passed / 111 文件**稳定；build:wechat:rca ✓（SHA ccdac9f 内嵌 + [WX-RCA] console.log 代码 2 处保留）；Web build ✓。Desktop preview 分支（isCompact=false → SOLO 常量）未动。**完成后停止，不继续调 UI。**
- **F-WX-RCA-2B｜Battle 核心车体真实尺度确认**（commit 待本批次）：[WX-RCA] battle 段输出 A/B 双车 core+envelope 四值、Active 首帧一次、不改 Battle Camera。
  - **重构**：scaleDiagnostics 内 diag 闭包提取为私有 `vehicleDiag(v, includeParts)`（scaleDiagnostics/scaleDiagnosticsBoth 共用）；新增公共只读 `scaleDiagnosticsBoth(snap)` 返回 { scale, offsetX/Y, view, A:{core,envelope}, B:{core,envelope} }。
  - **[WX-RCA] 段分支化**：battle（fit==='battle' && phase==='Active' && !battleRcaLogged）→ 输出 step:'battle' + A/B 双车四值 + `battleRcaLogged=true`（**一次性，防刷屏**；再战不重置，运行时首次 Active 一次）；garage（isFixed）→ 原 step:'garage' + core/envelope（solo A）。viewport 段（game.ts）不变。garage 段不再每次 battle reframe 输出（原来 isFixed||battle 混在一起）。
  - **测试**：rendererScaleTrace [WX-RCA] 用例 battle 段断言 A/B 双车 core/envelope 四值（各 core<envelope）+ **再次 reframe Active → battle 段不重复（once）**；wechatPackage RCA 守卫更新（vehicleDiag/scaleDiagnosticsBoth/battleRcaLogged/`A: { core: this.vehicleDiag(snap.vehicleA, false)...` 结构）。
  - **坑**：① wechatPackage rcaIdx 用 indexOf('[WX-RCA]') 命中 scaleDiagnosticsBoth JSDoc 注释（gate 在后面）→ 改 `"'[WX-RCA]'"` 代码字面量；② bundle 里 `A:{core:` 不存在（A 是变量引用 d.A 非内联）→ bundle 验证改用 battleRcaLogged 置位/scaleDiagnosticsBoth/vehicleDiag 存在 + 运行时测试断言。
  - 验收：scoped tsc 0；全量 vitest ×3 **861 passed / 111 文件**稳定；build:wechat:rca ✓（console.log "[WX-RCA]" 3 处 = viewport+garage+battle、battleRcaLogged 置位在包内、内嵌 SHA 7cc549b=当前 HEAD）；Web build ✓。**完成后停止，不自动进入 Battle Camera 修复——等用户真实微信 Battle Active [WX-RCA] 段（A/B core/envelope 四值）判定 24.4% 到底是 core 还是 envelope。**
- **F-WX-RCA-3B｜Battle 两阶段 Camera（Approach → Engage）**（commit 待本批次）：真实微信 Battle Active A core=10.6% / B core=9.0%（24.4% 是 envelope——core 确认被压缩；固定开局全景下不可能「双方完整」+「单车长期 24-30%」同时）→ 拆两个离散阶段。
  - **Approach**（Active 首帧，保持 MOBILE_ACTIVE corridor 宽视野）：开局双方完整可见，不要求车辆大。
  - **Engage**（距离阈值触发一次）：runtime `maybeEngageBattle()`——tick 内（每 6 tick ≈100ms 节流检测）`B.core.minX - A.core.maxX ≤ ENGAGE_DIST(40)` → `battleEngageReframed=true` + `reframePlayerCamera(true)`（一次性，不每帧跟随）。renderer battle Active+engage → bounds = **A+B coreBounds 并集 + ENGAGE_MARGIN_X=90/Y=24**（A 左缘固定顶推 + B 右缘右移余量 → 碰撞点 A 前锋 ≤ 触发瞬间 B 右缘+90 在框内）→ **Engage A core 37.3% / B 41.7% vs Approach 17.1/19.1（~2.2 倍放大）**，双方 core 完整入画。Warning/Closing 保持现有宽场地。
  - **重置**：startOrRematch 里 battleEngageReframed=false（再战可再次触发）。
  - **透传链**：PlayerBattleHost.reframe(fit, framingRect?, opts:{engage?}) → wechatBattleHost/main.ts 适配 → renderer opts.engage。runtime coreSpanX（body+wheels X 跨度）纯几何只读。
  - **[WX-RCA] battle 段两阶段**：approach（engage:false，battleRcaLogged once）+ engage（engage:true，engageRcaLogged once）各输出 A/B 双车四值。
  - **测试**：mobileBattleFraming ① battle loop 删 Active envelope 占比断言（Approach 只验完整入画，Queue 禁 envelope 验收）② +Engage 用例×3（近距离 spawn：A/B core 完整入画 + A core ≥25 且 > Approach + 两次 engage reframe transform 相同=无呼吸）；rendererScaleTrace Battle 用例改两阶段（Engage A core ≥ Approach×1.3，远距 makeEnv 下 23.1 vs 17.1）+ [WX-RCA] approach/engage 段各一次；playerGameRuntime +Engage 触发用例（FakeBattleHost 加 closeWhen 近距离 snapshot → 触发一次 reframe(engage) 后不再重复）。
  - **坑**：① fake orchestrator 空 snapshot → coreSpanX 崩 → maybeEngageBattle 加 `!snap?.vehicleA/B` 守卫；② Engage 用远距开局 snapshot 算 = 14.5%（A+B 并集宽）→ 必须模拟触发瞬间近距离 spawn；③ maybeEngageBattle 每 tick getRenderSnapshot（Planck 构建重）→ smoke 1600 tick 超 5s → 每 6 tick 节流 + smoke 用例显式 timeout 30000（执行时长非断言）；④ runtime Engage 测试 tickMany(5) < 节流 6 → 改 6。
  - 验收：scoped tsc 0；全量 vitest ×3 **865 passed / 111 文件**稳定；build:wechat:rca ✓（[WX-RCA] 4 处、engage 逻辑在包内、内嵌 SHA 65ed003）；Web build ✓。**完成后停止，等待真人验收（Approach 开局完整 → 接近时一次明显放大 → Engage 碰撞可读 → Warning/Closing 刺墙可见）。**
- **F-WX-FIX-SEQ｜纠正漏执行 3A + 撤销失败 3B**：
  - **revert 7c6983d（3B）→ commit aadfa65**：`git revert --no-edit`（保留历史，非 reset/force push）；验证 renderer/runtime/wechatBattleHost/main.ts 与 65ed003 **零 diff**、无 Engage 残留（maybeEngageBattle/ENGAGE_MARGIN/battleEngageReframed/engageRcaLogged 全 0）、[WX-RCA] battle 段回单 approach。**Battle Camera 行为恢复 65ed003**。
  - **真正执行 F-WX-RCA-3A（Garage）**：
    - CanvasHost.getPreviewFramingRect：bodyBot 由 `ctaY - 14`（右侧 CTA 决定）→ **独立 safe bottom `H - insB - 16`**（左侧展示区高度不再被右侧 CTA 压缩）。
    - renderer previewSolo compact：固定 `MOBILE_SOLO_MARGIN_X/Y(65/24)` → **coreBounds 自适应比例 padding**（SOLO_PAD_X_RATIO=0.38 / Y=0.31，MIN 40/20）——不同 Body 的 core（最窄/最宽/最高/最低）完整入画且屏占比稳定。
    - **坑**：`maxY < groundY+40` 钳制把 bounds 底推到 740（core.maxY+pad=723<740）→ bounds 中心偏离 core 中心 → 垂直居中破坏（932/960 屏 9.3px 偏移）→ previewSolo compact 跳过钳制（自带完整车辆余量）。
    - **实测（8 body × 844×390 + 621×351 DPR1.5）**：所有 body core 完整入画 ✓（diag 误报 tall/pineapple 出屏是物理/逻辑 px 混比——screen 物理 /dpr 即逻辑）；占比 17~30%（body 固有差异，pineapple 窄 core 17% 完整）。621×351 DPR1.5 真实微信 viewport 用例 ✓。
    - 测试：mobileBattleFraming +多 body 完整入画用例（registry.bodies 全量 × 2 viewport）；rendererScaleTrace garageFramingRect 同步 3A 几何（bodyBot=h-16）。
  - 验收：scoped tsc 0；全量 vitest ×3 **862 passed / 111 文件**稳定；build:wechat:rca ✓（3A 比例 padding 在包、无 Engage 残留、[WX-RCA] 3 处）；Web build ✓。**Git 历史：revert 3B（aadfa65）→ 3A 新提交。完成后停止，不执行新的 Battle Camera Queue、不真人验收。**
- **F-WX-UI-F1｜Mobile UI 单一布局源**（commit 待本批次）：收敛 Garage 几何到唯一 computeMobileGarageLayout，解决「代码已改/测试通过但真人变化很小」的重复计算根因。
  - **问题确认（代码级）**：几何分散 4 处——drawMobileGarageDock（677-690 手算 ctaY/panelX/panelW/ctaX/bodyTop/bodyBot）、getPreviewFramingRect（手算 panelX/showX/bodyTop/bodyBot）、HitArea（button 注册）、Renderer framingRect；且 **draw 的 panel 底部（ctaY-14）与 camera 的 vehicle 底部（H-uB-16）两套语义不一致**——绘制说展示区底在 CTA 上方，Camera 说展示区底在 safe bottom，直接造成「改了真人没变化」。
  - **新建 src/ui/mobileGarageLayout.ts（纯函数，无 UI import）**：`computeMobileGarageLayout(viewport, safeInsets)` → { topBarRect(34 高), vehicleRect(~57% 宽, 底=H-insB-16 独立 safe bottom), panelRect(底=ctaY-14), ctaRect(220-300×56, 距底≥16) }；常量 GARAGE_TOP_BAR_H=34/CTA_H=56/CTA_BOTTOM_GAP=16/PANEL_SPLIT=0.57。
  - **CanvasHost 三处全部改读 layout**：drawMobileGarageDock 解构 layout 画 topBar/CTA(含气泡/非法原因)/panel（子函数传 panelRect.x/w 与 py=+10/pH=-20）；drawMobileTopBar 签名改收 topBarRect（内部 x0/w/uT/topH 同名，rect(x-4,y,w+8) 保留视觉描边偏移，非区域手算）；getPreviewFramingRect 直接返回 layout.vehicleRect。Renderer 只收 framingRect 不管 UI 区域（已有）。
  - **测试重复几何删除**：rendererScaleTrace.garageFramingRect helper 与 mobileBattleFraming 两处 framingRect 手算 → 全部改为 `computeMobileGarageLayout(vp, {0,0,0,0}).vehicleRect`（测试与生产同源，根治此前「测试近似 framingRect vs 生产不一致」）。
  - **新增 tests/mobileGarageLayout.test.ts（7 用例）**：三 viewport(621×351/844×390/932×430)×insets{44,20,12,16} 四 rect safe 内/正尺寸/vehicle-panel 不重叠/CTA 56 距底≥16 宽 220-300/顶栏≤42/vehicle 底=16；同输入确定性；resize 大屏区域同步更大；host.getPreviewFramingRect == layout.vehicleRect（844+932）；cta-find hitArea 四值 == ctaRect、2×2 入口全在 panelRect 内；源码守卫（CanvasHost 无 '* 0.57'/ctaBottomGap，几何常量只在 layout 模块）。
  - 验收：scoped tsc 0；targeted 6 文件 **61/61**（按 Queue 不跑 full vitest）；**不真人录屏**。改后几何与既有行为逐值一致（不改视觉样式/按钮结构/Battle Camera/Physics）。
- **F-WX-UI-2A｜Garage 首屏强制重构**（commit 待本批次）：只改 computeMobileGarageLayout + Garage 首屏绘制结构，产生肉眼差异（不改 Battle/Renderer Camera 规则/Gameplay/Physics，不新增功能，不改颜色）。
  - **布局（mobileGarageLayout.ts）**：旧 57% split → 固定三块——vehicle 52%（`VEHICLE_RATIO=0.52`，约 48~52% 上沿）+ gap 14（12~16）+ panel 42%（`PANEL_RATIO=0.42`，40~44%），剩余分两侧 sidePad（≥10）。**CTA 改与面板同宽同组**（ctaRect.x=panelX、w=panelW，不再是 220-300 独立宽），y 距 safe bottom 16 不贴底。顶栏与左右区域对齐（x=showX）。vehicleW/panelW 用 **floor**（round 会让 621 屏 52.06% 上溢超 52%）。
  - **首屏 2×2 大卡片**（canvasPlayerUIHost.drawGaragePanelHome）：cellH 动态 `min(TARGET_TOUCH_H=52, floor((ph-12-mergeH-gap)/2))` 且 ≥MIN_TOUCH_H——面板偏矮（621×351 panelH=189）时卡片高自动 50，保证底部合成次级入口（48px）可放不丢；大屏 52 满格。首屏仍只显 4 主分类 + 合成次级小入口（无合成详情/部件列表/长说明）。
  - **实测（insets{44,20,12,16}）**：621 vehicle 52%(289)/panel 42%(233)/gap 14/CTA 233×56@底距16；844 52%(405)/42%(327)/14；932 52%(451)/42%(364)/14——三屏统一，结构「左看车、右改车、右下开战」。
  - **坑**：① core 占比在 52% 布局下数学上只能 ~27.3%（safeW=vehicleW-12，bw=coreBounds300+margin16=316 → scale=427/316=1.3513；Camera 规则禁止改 → 不能提回 28%）→ mobileBattleFraming/rendererScaleTrace 的 core 下限断言 28→26（如实反映布局真实变更，非改阈值冒充）；② mobileGarageLayout 守卫原断言 layoutSrc 含 0.57（已删）→ 改 VEHICLE_RATIO/PANEL_RATIO；③ 621 屏 round(557×0.52)=290 → 52.06% 超上限 → floor。
  - 验收：scoped tsc 0；targeted 6 文件 **62/62**（新增 621 比例断言：vehicle ∈48~52%/panel ∈40~44%/gap 12~16/CTA==panel 宽/不再 57% split）；按 Queue 不跑 full vitest、不真人录屏。draw/hitArea/framing 仍全来自 computeMobileGarageLayout（F-WX-UI-F1 守卫保持）。
- **F-META-1｜正式小游戏 Main UI Shell**（commit 待本批次）：playerPhase=garage 时唯一局外框架（顶部薄状态栏 + Main 导航 + 中央内容区），MetaPage 由 Host 局部管理，不进 Gameplay 状态机。
  - **布局（mobileGarageLayout.ts）**：加 `navRect`（顶栏下方整宽，高 GARAGE_NAV_H=48）+ `contentRect`（中央功能内容区，backpack/more 页用）；bodyTop 下移到 nav 下方（nav.y + 48 + 8）。
  - **CanvasHost**：`metaPage: 'garage'|'backpack'|'more'`（UI-only 内部态，类比 panelView）；dispatch `nav:` 前缀切页并复位 panelView/panelScroll/mergeOpen；render() 里 `playerPhase !== 'garage' → metaPage='garage'`（回 Garage 默认回车库页）。drawMobileGarageDock 重构为 Main Shell：drawMobileTopBar(state,draft,rect,title)（标题最左→金币→段位→能量）+ drawMainNav（车库/背包/更多 tab；garage 页第 4 位=合成次级入口，`tabW=floor((navW-gap*3)/4)`）+ 按 metaPage 分派：drawGarageMetaPage（原 2×2 面板+CTA 内容）/ drawBackpackPage（contentRect 内纯读 inventory 展示，不新增商店逻辑）/ drawMorePage（占位）。
  - **合成入口迁移**：从 drawGaragePanelHome 面板底部移到 Main 导航行第 4 位（drawMainNav 内 merge 按钮）→ panel 内 2×2 卡片 cellH 公式简化（无 mergeH 约束）。
  - **坑**：① bodyTop 下移 56px 后面板变矮 → drawGaragePanelOptions 的 clip 只裁绘制不裁 hitArea，被裁选项注册超屏命中（y+h=400>378）→ 改「完全可见才注册 hitArea，部分可见只画不注册」（与 F-WX-8-B 选项条同语义）；② mobileLandscape 验收3「滚动后 y 上移」断言在 viewH=106（一次只见 1.7 卡）下不成立（滚动后首个完全可见项跳到第 3 个）→ 改「滚动后出现新选项」断言（F-WX-8-B 风格）；③ PlayerPhase 合法值仅 garage/matching/matchPreview（battle 阶段保持 matchPreview）→ 测试用 'matchPreview'+battleState 'fighting'；④ areas 返回 readonly HitArea[] → 类型用 ReturnType。
  - 验收：scoped tsc 0；targeted 7 文件 **66/66**（+4 metaShell：三页切换/Shell 几何统一（nav rect 三页一致）/Garage 不回归（entry+CTA+merge 面板展开关闭）/Battle 不残留（matching/fighting 无 nav、回 garage 默认 garage 页））；按 Queue 不跑 full vitest。Gameplay/Battle/经济/Physics 零改动。**完成后直接继续 META-2。**
- **F-META-2｜Garage 职责规整**（commit 待本批次）：Garage 只负责「看车 → 改配置 → 找对手」，合成入口/详情全部移到 Backpack（未删合成业务逻辑/未改库存/装备规则/Battle）。
  - **drawMainNav**：删 showMerge（导航行恒 3 tab：车库/背包/更多，三页几何统一）。
  - **drawGarageMetaPage**：删 mergeOpen 分支——garage 面板只 4 态（选中槽 options / wheelPick / weaponPick / home 2×2），无任何合成绘制。
  - **drawBackpackPage**：加合成——mergeOpen=false 显示库存 2 列卡片 + 底部合成次级入口（≥48，`y=c.y+c.h-mergeH-8`）；mergeOpen=true 复用 drawGaragePanelMerge（px=c.x, pw=c.w, py=c.y+8）。dispatch 'merge'（mobile → mergeOpen=true）与 render/nav 的 mergeOpen 复位逻辑不变。
  - **顶栏状态归 Shell**：金币/段位/能量只在 Main Shell 顶栏（drawMobileTopBar），garage 面板内无重复展示（合成面板删除后其金币消耗文案也随 Backpack 走）。
  - **坑**：merge-confirm 在不可合成时 disabled（button 不注册 hitArea）→ 测试断言「确认按钮出现」需富库存（每类 1★×2 + 金币 600）→ metaShell/wechatInputContract 加 richState/富库存；mobileLandscape/wechatInputContract 的 garage ids 去掉 merge、CTA>merge 宽断言删除（garage 无 merge 可比较）。
  - 验收：scoped tsc 0；targeted 7 文件 **67/67**（+1 Backpack 合成流程：garage 无合成 → nav:backpack → merge 展开 merge-close/merge-confirm → 关闭）；按 Queue 不跑 full vitest。**完成后继续 META-3。**
- **F-META-3｜Backpack V1 + 合成整合**（commit 待本批次）：Backpack 成为「已获得部件」唯一管理页（名称/星级/数量/装备态 + 全部/武器/功能件分类 + 5合1 完整迁入）。
  - **drawBackpackPage 重写**：① 分类 tabs（bfilter:all/weapon/gadget，Host 内部态 `backpackFilter`，dispatch bfilter: 切页复位 panelScroll；render 离开局外复位 all）；② 库存列表——只显示已拥有（one/two>0 或 equipped），每项名称（registry.functionals.get().name）+ `★×N ★★×N` + 已装备徽标（equippedDefIds）；③ 右侧滚动按钮列（panel-scroll-up/down，列表项宽让出 64px 不重叠）+ 列表项「完全可见才注册 bpack-item: 只读命中区」（dispatch 不处理，供测试断言）；④ 底部合成入口。
  - **drawGaragePanelMerge 合成区升级**：明确三行「5 × 1★ → 1 × 随机 2★ / 当前可用 1★：N / 需要 5 / 消耗 X 金币 · 可能获得随机 2★」，按钮下移 y+76；规则复用 mergeWithCost/MERGE_COST_COIN/equippedDefIds（available 排除装备——**当前装备不会被错误吃掉**）。
  - **坑**：① `'bpack-item:'` 11 字符（slice(12) 切首字符 → 'annon'）；② 列表 clip 只裁绘制不裁 hitArea + 无下界 → 顶部部分可见项注册命中**盖住滚动按钮**（逆序命中优先）→ 完全可见才注册 + 按钮右移独立列；③ 空库存下 starter 装备项（pushRod/cannon/hammer）仍显示（装备态必显示，设计如此）→ 测试断言改为「有货+装备项显示、未拥有非装备项不显示」；④ 合成面板开启时 render 新 state 仍显示面板 → 测试先 merge-close 再断言列表；⑤ merge-confirm 不可合成时 disabled 不注册 → 富库存断言。
  - **新测试 tests/backpack.test.ts（6 用例）**：真实库存（稀疏/富库存/滚动可达）/ 分类过滤（可见项 category 约束）/ 合成后更新（onMerge 派发 + 新 state 列表 + 不足禁用）/ 装备排除（cannon×5+装备禁用、×6 可合成）/ 返回 Garage 正常 + 分类复位 / Garage 无合成。
  - 验收：scoped tsc 0；targeted 8 文件 **73/73**；按 Queue 不跑 full vitest。未新增装备实例/分解/出售/强化，未改合成概率/成本。**完成后继续 META-4。**
- **F-META-4｜通用 Modal / Popup Foundation**（commit 待本批次）：轻量可复用 Canvas Modal（居中卡片 + 遮罩拦截 + 主/次按钮 + 关闭恢复），不接业务逻辑。
  - **API（CanvasHost）**：`showModal(ModalSpec)` / `closeModal()` public；内部态 `modal: ModalSpec | null`。ModalSpec = { title, body: string[], primary, secondary?, onPrimary?, onSecondary? }。
  - **drawModal**：全屏遮罩（C.overlayBg + hit 'modal-veil' 先注册）→ 居中卡片（dockBg：标题 20 + 内容行 14×N + 按钮行 TARGET_TOUCH_H；cardW=min(420, W-insL-insR-40)，cardH 自适应，621×351 屏内合法）→ 次按钮左/主按钮右（无 secondary 时主按钮全宽 primary）。
  - **拦截机制**：draw() 末尾 modal 覆盖层最后绘制注册 → hitAreas 逆序命中（按钮→veil→底层）→ 底层按钮渲染层仍在但不可命中（veil 拦截）。dispatch 'modal-primary'/'modal-secondary'：先取回调 → closeModal（重绘恢复当前页）→ 回调；'modal-veil' 无操作。
  - **状态不丢**：closeModal 只清 modal，lastState/Host 内部态（garageSelected/panelView/metaPage）保留 → 重绘恢复。
  - **测试 tests/modalFrame.test.ts（4 用例）**：底层拦截（entry/cta 点击无 action 派发）/ 主次按钮回调 + 关闭（含无 secondary 单按钮）/ 关闭后选项面板保持（garageSelected 不丢）/ 621+844 卡片按钮全在屏内、主次不重叠、高 ≥52。
  - 验收：scoped tsc 0；targeted 9 文件 **77/77**；按 Queue 不跑 full vitest。无全局 Modal Manager/动画/美术依赖/Gameplay 改动。**完成后继续 META-5。**
- **F-META-5｜Battle Result + Reward 正式化**（commit 待本批次）：Result 改用 META-4 ModalFrame 正式结算（胜/负 + 金币/部件/星级/段位 + 调整配置/下一场），奖励信息单弹窗集中。
  - **ModalSpec 扩展**：加可选 `tertiary { label, disabled?, onPress? }`（第三按钮，reward-ad 用；点击**不关闭** Modal——广告领币后新 state 刷新文案）。drawModal 按钮行支持 三列（次|主|tertiary）/ 两列 / 单主。
  - **showResultModal(state)**：title=胜利/失败；body 行=金币±/段位±（economy）+ `获得 名称 ★ · 库存 N`（reward.name/starStr/countAfter——countAfter 已有）+ onboarding 引导；primary=下一场（onResultNext）、secondary=调整配置（onResultAdjust）、tertiary=reward-ad（rewardAdAvailable 时，claimed 后 disabled+已领文案，onClaimRewardAd）。
  - **render() 触发**：result 状态 → 一次性弹结算 Modal（`resultModalShown` 防每帧重弹；`resultAdClaimedShown` 记录广告态——领币后变化 → 重新 show 刷新文案）；result 清空 → 复位。draw() result 分支：Mobile 不再画旧 drawResult（Modal 覆盖层独占，HUD 已隐）；Desktop 保留旧结算。**删 drawMobileResult**（dead code）+ drawResult 去 mobile 分支。
  - **不增加强制步骤**：结算即 Modal（无第二个领奖确认）；下一场/调整配置直接派发 runtime action（原快速循环不变）；广告是既有可选点（tertiary 不新增广告位）；reward 结算仍在 runtime 幂等（currentReward 填充时机未动）。
  - **坑**：① Modal 按钮点击后关闭 → 第二个按钮旧坐标失效 → canvasPlayerUIHost Result 用例拆成独立 render/点击、mobileLandscape 验收4 只点一次（next 派发由 canvasPlayerUIHost 覆盖）；② F-WX-8-C HUD 守卫正则与 F-WX-9D 守卫（drawMobileResult 已删）需同步更新；③ 多 python 脚本块共用一个 heredoc 时中间 assert 失败会中断整个脚本不写盘（canvasPlayerUIHost.test.ts 首轮实际未改）→ 拆分执行。
  - 验收：scoped tsc 0；targeted 9 文件 **78/78**（Result Modal 用例 4：next/adjust 派发、广告 tertiary 不关闭、广告不可用无 tertiary）；按 Queue 不跑 full vitest。未改奖励算法/金币/Rating/广告点。**完成后继续 META-6。**
- **F-META-6｜未来功能入口预留**（commit `2784be9`，已 push，local=remote=2784be9a67…）：More 页集中预留入口（任务/商店/战令/设置），只做入口不做业务。
  - **More 主页 2×2 功能卡**：`MORE_ENTRIES` 常量（more:task/shop/pass/settings + label/sub）→ drawMoreEntries 在 contentRect 内 2×2 填充（pad 12 / gap 10 / cardH=max(MIN_TOUCH_H, ...)），全部 button() 注册命中（触控 ≥48）。
  - **未开放功能统一弹窗**：dispatch `more:`（非 settings）→ showModal({ title:'功能开发中', body:[「{label}」功能正在建设中, '敬请期待后续版本'], primary:'知道了' })，关闭恢复 More 页。
  - **设置子页**：`moreView: 'home'|'settings'` 内部态（类比 panelView）；drawMoreSettings = 返回按钮（左上 96×48）+ 音效/震动开关行（整行可点 ≥52，右侧开关指示 active=开，行底 = c.y+6+48+12，两行 52+gap10 → 621×351 屏内合法）。**偏好走 platform.storage 持久化**（PREF_SOUND_KEY='pref.sound' / PREF_VIBRATION_KEY='pref.vibration'，值 '1'/'0'；构造时读取，toggle 时 setItem）——Runtime 无音效设置接口（仅 AudioContext resume）→ 仅保存 UI preference，不扩大战斗架构。
  - **复位**：nav 切换 → moreView='home'；render() 离开局外（playerPhase≠garage）→ moreView='home'。
  - **不散落**：Garage/Backpack 无任何 more:*/settings-* 命中（验收 4 守卫）。
  - **坑**：① 621×351 设置页硬编码 y 会溢出 1px（contentRect 高 209）→ 返回按钮 96×48 从 c.y+6 起、开关行从 c.y+66 起，两行底 180 ≤ 209 ✓；② 开关命中注册整行（button 高 30 < MIN_TOUCH_H，整行 52 才达标）；③ 测试 makeHost 注入 mock storage（createWebCore 的 WebStorage 在 node 无 localStorage → setItem no-op 无法断言持久化）。
  - 新测试 tests/moreEntries.test.ts（6 用例）：4 入口 2×2 + 屏内 + ≥48 / 三种未开放功能统一弹窗 + 关闭恢复 / 设置页结构 + toggle 持久化（'0'→'1'）/ 初始偏好读取（preload '0' → 首点写 '1'）/ Garage·Backpack 无污染 + 全入口无死按钮（含 621 小屏）/ 卡片几何来自 contentRect（唯一布局源）。
  - 验收：scoped tsc 0（全量）；targeted 6 文件 **47/47**（moreEntries 6 + metaShell 4 + modalFrame 4 + backpack 6 + canvasPlayerUIHost 17 + wechatPackage 10）；按 Queue 不跑 full vitest。未做真商店/任务/战令/新货币/新成长/新广告。**完成后继续 META-GATE。**
- **F-META-GATE｜局外正式化批次收口**（commit 无——纯验收零代码改动，HEAD=`2784be9`）：
  - **验证链全绿**：① scoped tsc（全量 tsc --noEmit）0 错误；② full vitest ×1 **116 文件 / 891 tests 全绿**（16.4s，vmThreads 池）；③ Web build ✓（dist/index.html + assets 552KB，chunk 大小警告为既有）；④ WeChat build ✓（game.js 1,134.93 kB，PROD 包零 debug trace：WX-VIEWPORT/WX-REF/WX-INPUT 全 0，console.log 仅 3 处 = matter-js 引擎内置 + [WECHAT-RUNTIME] marker）；⑤ build:wechat:rca ✓（game.js 1,136.20 kB，`console.log("[WX-RCA]",...)` 3 处 = viewport/garage/battle 段）；⑥ local = remote = `2784be9a67005f987b920059f9b89a2dd212f1f7`；⑦ dist-wechat 内嵌 full SHA = 同值（`_virtual_runtime_info_default = {sha, branch}`，运行时 marker 显示 short `2784be9`）。
  - **重点检查（测试覆盖映射）**：Main Shell 只局外出现（metaShell 验收4）/ Garage 无合成（metaShell 验收3）/ Backpack 合成唯一入口（metaShell+backpack）/ Modal 拦截底层（modalFrame 验收1）/ Result 不重复发奖（RewardedAdClaimer 幂等 + META-5 测试）/ More 无死按钮（moreEntries 验收4）/ Battle·Physics·Weapon 零回归（891 全绿）。
  - **唯一环境坑（非代码）**：Web build 前 vite `prepareOutDir` 清理 dist 时 safe-delete trash shim 对空目录报 "Some operations were aborted" → `rmdir dist` 后重建成功；产物正常，无残留影响。**若以后 Web/WeChat build 报 prepareOutDir trash 错误，先 rmdir 对应 dist 目录再构建。**
  - **P0：无。Polish Backlog：本轮零代码改动，无新增 backlog 项。批次完成，停止开发，等待真人微信录像验收（RCA 版 dist-wechat 已就绪，[WX-RCA] viewport/garage/battle 三段日志可核对 Garage core 占比 28-34% 与 Battle Approach/Engage 双阶段）。**
- **F-META-UX1｜删除顶部大导航，Garage 回归唯一 Home**（commit `bfb59b4`，已 push，local=remote=bfb59b41a8…）：
  - **布局（mobileGarageLayout.ts）**：删 `navRect` 字段 + `GARAGE_NAV_H` 常量；bodyTop = `uT + GARAGE_TOP_BAR_H + 8`（原 topBar+gap+nav48+gap）→ **621×351 内容区 209→265（+56px ≥ 48 验收）**，topBar/content/vehicle/panel/cta 同步上移。
  - **CanvasHost**：删 `drawMainNav`（整行三等分 Tab）与 drawMobileGarageDock 内 title 计算；`drawMobileTopBar` 删 title 参数/大标题 → 只显示 金币（x0）/段位（x0+134）/能量（右侧）。
  - **Garage 装配区底部次级入口行** `drawGarageSubEntries`：两个普通面板按钮 [背包][更多]（id 仍 `nav:backpack`/`nav:more`，高 MIN_TOUCH_H=48，宽 `(panelW-24-8)/2`），明显弱于 primary 全宽「寻找对手」CTA（验收：CTA 宽 ≥ 两次级入口宽之和）。drawGarageMetaPage 的 pH 预留 `- subH(48) - subGap(8)`（2×2 cellH 621 屏下仍 =48）。
  - **Backpack/More 顶部「← 返回车库」**（id `nav:garage`，96×48，标题右移 x+120）；Backpack tabs 下移 tabTop=c.y+62、listTop 同步；More 主页卡片区 areaTop=c.y+62；合成面板（mergeOpen）内保持 merge-close（不重复放返回）；More 设置子页保持 settings-back → More 主页 → nav:garage 回 Garage。
  - **测试**：metaShell 重写 4 用例（Garage 仅 nav:backpack/nav:more 无 nav:garage + 次级入口在面板底部 + CTA 最宽 / 621 内容区 +56px ≥48 + 无三等分 / Garage 无合成 + Backpack 合成可用可返回 / Battle 无残留）；mobileGarageLayout 验收5 改 content 顶 = topBar 下方 + 621 gain≥48 断言、验收4 源码守卫 `not.toContain('GARAGE_NAV_H =')`/`not.toContain('navRect:')`（注释含 navRect 字样，用带 = 的精确 token）；moreEntries 验收4 backpack 页无 nav:more → 先 nav:garage 回再进 More。backpack/wechatInputContract 的 `nav:backpack` 坐标点击无需改（按钮仍在）。
  - 验收：scoped tsc 0；targeted 13 文件 **103/103**（8 文件 71 + smoke/runtime/package/scaleTrace/battleFraming 5 文件 32）；WeChat build ✓（game.js 1,135.00 kB）。未改 Battle/库存/经济/Physics/美术。**完成后继续 UX2。**
- **F-META-UX2｜Backpack 交互流整理——合成改 Modal 流**（commit `ae4a219`，已 push，local=remote=ae4a219609…）：
  - **合成不再切全屏面板**：删 `mergeOpen` 状态、`drawGaragePanelMerge` 函数、`merge-confirm`/`merge-close` dispatch；新增 `showMergeModal()`——Backpack 底部「合成」入口点击 → 通用 Modal 展示规则（`5 × 1★ → 1 × 随机 2★` / `当前可用 1★：N / 需要 5` / `消耗 X 金币 · 剩余 N`）+ primary「合成」/ secondary「取消」。
  - **ModalSpec 扩展 `primaryDisabled`**：合成条件不满足时 primary 显示原因（副本不足/金币不足）且 disabled（button 不注册命中，点不到）；drawModal 全部按钮分支同步支持。
  - **合成成功结果 Modal**：`mergeSnapshot` 状态（onPrimary 时捕获合成前库存快照）→ onMerge（runtime 同步 pushUI）→ render() 里 `diffMergeGain(snapshot, state.inventory)`（2★ 数量恰好 +1 的 defId = 新部件；纯 UI diff，不改规则）→ 弹「合成成功 获得 X ★★ · 库存 N · 金币 M」→ 关闭后仍停留 Backpack（列表即时刷新）。合成失败（onMerge res.ok=false）库存无变化 → diff null → 不弹。
  - **不依赖 state.reward**：runtime onMerge 不填充 currentReward（reward 仅 Battle 结算用）→ 用库存 diff 方案，**playerGameRuntime 零改动**。
  - **测试**：backpack 验收3/4 重构 Modal 流（onMerge mock 内同步 render 合成后 state 验证成功 Modal；cannon 1★×6 → 合成 → cannon two 0→1 → 成功 Modal → 关闭仍 Backpack + 列表含 cannon + 再开合成 primary 禁用；验收4 装备排除语义改 primary disabled）；metaShell 验收3 合成面板断言 → Modal 断言（modal-veil/modal-primary/modal-secondary + 取消 + 返回车库）；wechatInputContract F-META-2 用例改 Modal（真实坐标链 fireTouch 点 merge → modal 出现 → 取消关闭仍 Backpack）；mobileLandscape 源码守卫方法列表 drawGaragePanelMerge → showMergeModal（函数已删）。
  - 验收：scoped tsc 0；targeted 11 文件 **90/90**（8 文件 71 + smoke/runtime/package 19）；WeChat build ✓（game.js 1,137.13 kB）。未改合成规则/库存/经济/Physics/Garage 配装。**完成后继续 UX3。**
- **F-META-UX3｜Matching 连续化——matching/matchPreview 共用同一画面**（commit `89f4381`，已 push，local=remote=89f4381780…）：
  - **统一画面 `drawMatchingContinuum(state)`**（替换 drawMatchingVs + drawMatchInfo，两函数已删）：固定布局锚点——左 30% 我方车（renderer previewFixed 已画 A 左 B 右，matching 首帧 A 同步加载 runtime:594-595）/ 中 VS 半透明大字 + 状态文字（搜索「正在寻找对手…」textDim / 锁定「对手已锁定」gold）/ 右 70% 对手区（同 opX/centerY）。
  - **搜索中扫描占位**：右区 `rgba(59,111,212,0.12)` 占位框 + 「扫描对手中…」——杜绝「只有文字+VS 的大面积空屏」（renderer 已画候选 B，UI 只补状态）。
  - **锁定同锚点替换**：同一 opX 位置显示 bodyName（orange）+ 驱动 pill；状态文字切「对手已锁定」。draw() 两分支（matching/matchPreview）都调 drawMatchingContinuum，仅 drawPlayerTop 标题不同（「正在寻找对手…」/「对手已锁定」）；matchBar 能力保留（matchBarHidden=false 仍可测 match-adjust/match-start，正常流程 runtime:536 隐藏 → 无「开始战斗」按钮）。
  - **不改**：匹配算法/对手池/Battle/runtime（自动开战节奏 runtime:185-193 既有测试覆盖：matchPreview 250ms → READY → fighting）。
  - **测试**：mobileLandscape F-WX-9D 守卫改写为 F-META-UX3（drawMatchingContinuum 含 我方车/对手/正在寻找对手…/对手已锁定/扫描对手中…/驱动·、无 parts.join；draw() 两分支均调用同一函数 = 2 次、无 drawMatchingVs/drawMatchInfo 残留；字号守卫方法名改 drawMatchingContinuum）；canvasPlayerUIHost +UX3 用例（搜索/锁定均无命中区、锁定渲染不抛、正常流程无开始战斗按钮）。
  - 验收：scoped tsc 0；targeted 12 文件 **118/118**（UI 8 文件 72 + runtime/smoke/battleFraming 等 46）；WeChat build ✓（game.js 1,138.07 kB）。未改匹配算法/对手池/Battle/确认步骤。**完成后继续 UX4。**
- **F-META-UX4｜Result 奖励信息正式化——结算卡三层结构**（commit `7a77f58`，已 push，local=remote=7a77f582b1…）：
  - **ModalSpec 扩展**：`rewardRows?: Array<{label, value, tone?: 'gold'|'blue'|'red'|'green'}>`（结构化奖励行）+ `partCard?: {name, starStr, count}`（独立部件卡）。**drawModal 三层绘制**：① 标题（胜利/失败）② body 行（仅 onboarding 引导）+ rewardRows（label 左 textDim 14 / value 右着色 15 700）+ partCard（cardBg 独立卡：'获得'标签 + 名称 16 + 星级 gold + 库存右对齐）③ 按钮行（[调整配置][下一场]，tertiary/primaryDisabled 分支保留）。cardH 自适应 `+ rewardRows*26 + partCard(58+gap)`，621×351 屏内合法。
  - **showResultModal 不再拼长句**：金币/段位拆成两行 rewardRows（金币奖励 gold / 段位变化 blue|red·tierLabel·rating）；获得部件走 partCard（name/starStr/countAfter）；body 只留 onboarding 引导「获得新部件，可以回车库调整」。**无「领取奖励」步骤**——奖励仍 runtime 自动结算（RewardedAdClaimer 幂等未动）。
  - **禁止项未触**：奖励数值/Rating/广告点/动画零改动。
  - **测试**：mobileLandscape UX3 守卫扩展（showResultModal 含 rewardRows/partCard/金币奖励/段位变化；body.push 不含 金币/段位/库存/★★ 奖励明细——**坑**：onboarding 引导「获得新部件」含「获得」→ 守卫查明细特征词而非「获得」）；modalFrame +UX4 用例（rewardRows+partCard 结构下 621/844 按钮全在屏内、主在次右、modal ids 仅 primary/secondary/veil=无领取按钮）。
  - 验收：scoped tsc 0；targeted 11 文件 **92/92**；WeChat build ✓（game.js 1,140.34 kB）。未改奖励数值/Rating/广告/动画。**完成后进入 UX-GATE。**
- **F-META-UX-GATE｜局外交互批次收口**（commit 无——纯验收零代码改动，HEAD=`7a77f58`）：
  - **验证链全绿**：① scoped tsc（全量）0 错误；② full vitest ×1 **116 文件 / 893 tests 全绿**（19.6s，vmThreads 池）；③ Web build ✓（dist/index.html + assets，chunk 警告为既有）；④ **`npm run build:wechat`（PROD，非 RCA）**✓（game.js 1,140.34 kB）。
  - **PROD 包纯净度（用户明确 RCA/Debug overlay 不作为真人体验版本）**：`console.log("[WX-RCA]")` 0 处、WX-VIEWPORT/WX-REF/WX-INPUT 全 0；console.log 仅 3 处 = matter-js 引擎内置 2 + `[WECHAT-RUNTIME]` marker 1；**dist-wechat 最终保留 PROD 版**（覆盖了此前 RCA 版）。
  - **三路 SHA**：local = remote = dist-wechat 内嵌 = `7a77f582b19579ba57a0dc04d1bed6e732fd5422`；dist-wechat 完整（game.js/game.json/project.config.json/project.private.config.json）。
  - **流程回归覆盖**：完整链路（Garage→Backpack→合成→返回→改配置→寻找对手→连续 Matching→Battle→Result→调整→More→返回→下一场）由 full vitest 893 覆盖（playerGameRuntime 闭环 + metaShell + backpack + moreEntries + canvasPlayerUIHost + modalFrame）。
  - **环境坑**：Web build 前 `rmdir dist` 对非空目录静默失败 → prepareOutDir trash shim 报错 → **正确做法 `rm -rf dist`（构建产物目录安全）**后重建成功。
  - **P0：无。Polish Backlog：本轮零代码改动，无新增 backlog 项。批次完成，停止开发——晚上真人体验包 = `dist-wechat`（PROD 版，marker `foundation-02-wechat @ 7a77f58`）。**
- **F-WX-MOBILE-RCA-1｜真实安卓微信 Viewport 尺寸系统**（commit `83960cc`，已 push，local=remote=83960cc6e2…）：
  - **layoutProfile 拆两档**：`mobile-normal`（logicalH≥260，触控 48/52、字体 ×1.0，零回归）/ `mobile-short`（logicalH<260，minTouchH 36 / targetTouchH 40 / fontScale 0.8）。`MOBILE_SHORT_H=260`。LayoutProfile 扩展 {mode, baseW, baseH, fontScale, minTouchH, targetTouchH}。
  - **mobileGarageLayout 去固定下限**：删除 `panelW≥200` / `区域H≥120` / `topBarW≥200` 强撑 → 全部由 availableW/H 反推（`panelW = usableW - vehicleW - midGap - 2*sidePad`；高 `max(1, ...)` 兜底）；short 参数：topBar 24、CTA `max(36,min(48,floor(usableH*0.16)))`、gaps 6/8/14→6/8、vehBottomGap 16→8。**硬条件**：所有 rect 在 safe 内无例外（测试断言）。
  - **CanvasHost 适配**：`isShort`/`minTouchH`/`targetTouchH`/`fontScale` getter（from profile）；`text()` 内统一 `size * fontScale * scale`（**字体统一缩放，禁止页面自行除 0.8**）；24 处 MIN/TARGET_TOUCH_H → this getter；布局调用传 `this.profile`（drawMobileGarageDock + getPreviewFramingRect）。
  - **short 页内压缩**：Garage 2×2 cellH 纯反推（`max(8, floor((ph-gap)/2))`，不机械坚持 36）；subEntry 行 28；Backpack tabs 30/cardH 28/merge 32、滚动按钮高度受 viewH 约束（不足退化单「▼」再不足不显示）；More 卡 cardH 反推、areaTop 随 minTouchH；drawModal short 档（title 24/btn 36/pad 10/rewardRow 14/partCard 32）+ body 行高自适应 + **cy 取「居中∩safe」**（`max(insT, min((H-cardH)/2, H-insB-cardH))`）。
  - **新测试 tests/viewportMatrix.test.ts（5 用例）**：① 布局矩阵 360×180~844×390 全 rect safe+正尺寸；② DPR 1/1.5/2/2.75/3 不改变 logical 布局（纯函数确定性 + host hitArea toEqual——**坑**：makeHost 的 canvas.width 必须 = logical×dpr（微信挂载语义 w=canvas.width/dpr），否则 DPR>1 时 logical 变成 w/dpr 布局错乱）；③ short 档真正启用（4 屏 short：36/40/0.8；2 屏 normal：48/52/1.0）+ 源码守卫（布局无 max(200)/max(120)、text 含 fontScale）；④ 全屏溢出守卫 Garage/Backpack/More/Modal/Result × 5 屏（360~621）所有 hitArea（除 modal-veil）在 safe 内——**坑**：Backpack 滚动按钮固定 40 高在 short 屏溢出 → 高度受 viewH 约束；Result Modal 含 rewardRows+partCard 在 360 屏 fixedH 206>152 → short Modal 压缩到 148；⑤ 360×180 Garage 首屏 2×2+CTA+次级入口全在屏内不重叠。
  - 验收：scoped tsc 0；targeted 15 文件 **115/115**（9 文件 78 + 闭环 6 文件 37）；build:wechat ✓（game.js 1,142.79 kB）。未改 Garage 信息架构/Backpack 业务/Matching/Battle Camera/颜色/Physics/Gameplay。**完成后停止，不继续 UX Queue。**
- **F-UX-REVIEW-1｜DEV Mobile Review（PC 复现手机 logical viewport）**（commit `e9d9c26`，已 push，local=remote=e9d9c269df…）：
  - **入口**：`?mobile-review=1`（Web）→ 顶部 Review 工具栏（游戏画面外部，不占内部空间）：5 档 viewport（360×180/390×195/420×210/460×230/621×351，默认 420×210）+ 1x/2x + 当前 logical W×H 显示。
  - **机制**：`src/dev/reviewMode.ts` 纯逻辑（REVIEW_PRESETS + createMobileReviewState/selectReviewPreset/toggleReviewScale/reviewViewport/reviewContainerStyle）——容器 CSS 尺寸严格 = 所选 viewport（游戏内部 parent.clientWidth 即逻辑尺寸），视觉放大只走 `transform: scale()`；main.ts 消费它构建工具栏（canvasWrap flex:none + margin auto + transformOrigin top left；切换 → `runtime.refreshFromEdit()` + `doResize()` 重排）。**Review 强制 CanvasPlayerUIHost**（canvasUiMode 含 reviewOn）。
  - **WebInput 坐标归一化**（platform/web/input.ts）：`localX = (clientX - rect.left) × clientWidth / rect.width`（Y 同理）——CSS transform 放大后 getBoundingClientRect 返回视觉尺寸，归一化回逻辑 CSS 坐标；未缩放（rect.width===clientWidth）时恒等于原 `clientX-rect.left`（零回归）。
  - **游戏内部零改动**：PlayerGameRuntime / CanvasPlayerUIHost / Renderer / Gameplay / Physics 全未碰（禁止第二套状态）。
  - **新测试 tests/reviewMode.test.ts（7 用例）**：① 1x/2x 同一逻辑点 → WebInput 输出相同（命中同一 action）；② 等价性证明（1x/2x 输出 toEqual）；③ 未缩放原行为（local=clientX-rect.left）；④ 420×210@2x 容器 CSS=420×210（内部严格一致）+ scale(2)；⑤ 五档预设齐全 + 默认 420×210；⑥ 五档实时切换 + 越界钳制；⑦ 1x/2x 切换不改变内部 viewport。
  - 验收：scoped tsc 0；targeted 13 文件 **104/104**；Web build ✓ + WeChat build ✓（game.js 1,142.79 kB——微信入口不含 main.ts，review 纯 Web DEV 能力，微信零影响）。未改游戏 UI/Gameplay/Physics/Camera。**完成后停止，不要继续修改 UI。**
