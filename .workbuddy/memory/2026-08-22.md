# 2026-08-22 工作记录

## F-PROJ-HF1｜高频小弹体动量收敛（已完成 + 推送 f5065cd）

根因确认（沿正式 Runtime，autoDrive=false）：
- 机枪/喷火器把香蕉推走 = 纯真实 projectile 碰撞动量传递（projectileMass × muzzleSpeed）。
- 全仓无「B 侧固定推力 / Damage→Knockback」规则：damageResolver 只扣 HP 不施力；所有
  applyLinearImpulse 均为射手自身后坐（cannon/shotgun/machineGun/laser/thruster）；喷火器零 impulse。
- 测量探针（mass=1）：机枪一个 7 发 burst 推香蕉 +24.87px / peak vx 1.20；喷火器一个 spray 推
  +143.12px（≈1.5 车身，直接推出射程/画面）/ peak vx 3.18。B 仅在 projectile 抵达后（步 7~9）
  才开始位移 → 排除隐藏推力。

改动（仅 content.ts，共享系统）：
- machineGun.projectileMass 1 → 0.1
- flamethrower.projectileMass 1 → 0.03
- 不改 muzzleSpeed / damage / fire rate / projectileRadius / CCD / 视觉 / Physics。

复测（mass 下调后）：机枪 Δx=+2.66px / peak vx 0.127；喷火器 Δx=+5.58px / peak vx 0.127。
无 Planck 不稳定（速度有限、仍为碰撞驱动）。

交付：tests/fProjHf1.test.ts（4 cases：参数档位+HP、机枪轻微位移、喷火器稳定停留、Cannon/Shotgun/
Laser 不变）。全量 520/520 通过，tsc --noEmit 0 errors。提交 f5065cd，推送
1643c25..f5065cd，ls-remote 核验落盘。Dev Server 重启（PID 20176，Badge=f5065cd）。

队列规则提示：Q14-A/B 表现修正（R2-FINAL 已合）到此动量收敛为最后一轮物理层微调，
若真人验收机枪 burst / 喷火器火流仍不达预期，按队列退出正式 Build / HOLD。

## Q15｜正常玩家游戏主循环 V1（已完成 + 推送 433f182）

冻结范围：新 Content / Physics / 正式参数全部冻结；仅做玩家流程编排层。

交付文件（4 个，单 commit 433f182）：
- src/player/opponentPool.ts（NEW）：6 套手动对手 Build，仅西瓜/香蕉 + 正式 PART_OPTIONS，
  无 wedge/ramHead/lifter/旋锤 HOLD 内容；cloneBuildDraft 深拷贝 + nextOpponentIndex（含 size 0 安全）。
- src/core/buildPersistence.ts（NEW）：localStorage key `strongfruit.playerBuild.v1`；
  校验 body/part 已知 + validateSnapshot 合法才接受，非法旧档返回 null → 默认 silDraft('watermelonBody')。
- src/main.ts（MOD）：playerPhase('garage'|'matchPreview') + matchedIndex 状态机；
  Garage CTA「开始战斗」→「寻找对手」；MatchPreview 新 matchBar（调整配置/开始战斗）；
  Result「下一场」(nextMatch 换对手) / 「调整配置」(adjustConfig 回 Garage)；
  updateStartButton 重写；B 面板 PROD 只读（import.meta.env.DEV）；refreshFromEdit 末位 savePlayerBuild；
  Dev Tools 经 TOOLS_DEV_VISIBLE（PROD 隐藏 / DEV 可见）保留且隔离。
- tests/q15PlayerLoop.test.ts（NEW，16 pass）：对手池 / 持久化全部边界。

验证：16/16 Q15 用例；全量 536/536（84 文件）；tsc --noEmit 0 errors。
提交 433f182，推送 f5065cd..433f182，ls-remote 核验 refs/heads/foundation-02-canonical = 433f182。
Dev Server 重启（旧 PID 20176 经 PowerShell Stop-Process 终止 → 新 PID 7156，Badge 显示 433f182）。

验收对应：刷新即进 Garage（无开发工具依赖）、整圈 Garage→Match→Battle→Result→NextMatch、
连续 3 场不 Reset、每场对手变化、调整 Build 重入 Battle、刷新保留 Build、Scenario/Dev 仍可用无回归。

## 交接｜HANDOFF_2026-08-22.md 已生成

应要求产出 `D:\0818new\最强水果\HANDOFF_2026-08-22.md`：含项目核心定位、3 级指令优先级、当前状态(HEAD=433f182)、
Q15 明细、冻结边界/红线、Canonical Physics Foundation、环境坑(vitest pool / git ref / Dev Server Badge / 中文路径伪影)、
文件地图、续作建议(Q14-C HOLD 等你 redesign)、验证命令速查。供新聊天窗口无断点续作。

## Q15-UX-R1｜Garage→Matching→MatchPreview 玩家流程收口（已完成 + 推送 c10eb00）

续作承接：接手 HANDOFF_2026-08-22 后按 Queue 执行，把 Q15 真人录像像 Physics Lab 的问题收口。

改动（7 文件，单 commit c10eb00）：
- src/player/opponentPool.ts：新增 pickRandomOpponent(lastIndex, poolSize, rng)（pool>1 禁止连续同对手、
  首场 last=-1 不限制）+ buildMatchingSequence(finalIdx, poolSize, rng)（长度 4：前 3 互异且≠finalIdx、
  末位=finalIdx，保证 Matching 至少 3 次可见变化）；保留 nextOpponentIndex 供测试/旧逻辑。
- src/battle/battleContract.ts：BattleRenderSnapshot 加可选 soloA。
- src/battle/planckBattleOrchestrator.ts：构造加 soloA 参数并透传进 snapshot（vehicleB 类型保持非 null，不破 tsc）。
- src/render/renderer.ts：soloA 时只画 vehicleA、相机 preview/vehicles fit 排除 vehicleB、hitFlash/deathFx 跳过 B。
- src/lab/physicsLab.ts：loadCustomPreview(buildA, buildB, soloA=false) 透传（最小正式 Player Preview 支持，
  Garage 只渲染 A，B 不绘制/不取景/不伪装）。
- src/main.ts：Garage 仅 A 编辑器（默认不展开部件全集、点挂点才展开、选完即收起 selectedSlotA=null）；
  新增 playerPhase 'matching' + matchingGeneration 防重复触发；startMatching（真随机选对手 + 约 1.1s 候选车
  真实对手可见变化 ≥3 次 + 最终锁定）+ 复用正式 Planck Preview 渲染候选车的独立 matchRenderer/matchLab；
  MatchPreview 彻底退出编辑器（左右面板隐藏，只显示 我的车 VS 对手 + 调整配置/开始战斗）；
  下一场(nextMatch)复用 startMatching 同一状态链；Reset/Clear/setMode/adjustConfig 同步收口。
- tests/q15PlayerLoop.test.ts：修 EMPTY_SLOT 导入源（partOptions→buildEditorModel，pre-existing tsc 误报）+ 新增
  pickRandomOpponent/buildMatchingSequence 确定性 rng 单测 + solo-A snapshot 测试。

验证：q15PlayerLoop 23/23；全量 543/543（84 文件）；tsc --noEmit 0 errors（修测试导入后）。
提交 c10eb00，推送 433f182..c10eb00，ls-remote 核验 refs/heads/foundation-02-canonical = c10eb00。
Dev Server 重启（旧 PID 24472 → 新 PID 27224，Badge=foundation-02-canonical @ c10eb00）。

冻结边界遵守：未改 Physics/Damage/Q02~Q14 参数、Battle HUD、Result Card、未加 Weapon/Gadget、未加 dependency、
未重写 main.ts 整体、DEV 工具保留可用且不参与正常主流程。Q14-C 仍 HOLD（等你 redesign）。

## Q15-UI-R2｜玩家 Shell 重构（Garage Dock / Matching 主画布 / MatchPreview 连续）（已完成 + 推送 43b82b9）

续作承接：接手 Q15-UX-R1 后，真人录像失败的根因是「仍用 Physics Lab 页面骨架（左长表单 + 大 canvas + 独立候选画布/第二 Renderer）」，
而非逻辑。本队列只重构玩家 UI Shell，不碰 Gameplay/Physics/Match 逻辑。

改动（2 文件，单 commit 43b82b9）：
- src/render/renderer.ts：新增 CameraFit 'previewSolo'/'previewFixed'（硬编码世界边界，防候选换车呼吸缩放）；
  B 候选淡入缩放字段 previewFxB（alpha 0.35→1 + scale 0.96→1，绕 COM 应用，仅 B、A 不动）；不破 tsc。
- src/main.ts：删除旧左右长表单 .lab-panel（build 模式）+ 独立候选画布 candidateCanvas/matchRenderer/matchLab
  （第二 Renderer 一并移除）；
  Garage 改 garage-dock（第一层槽位 chip：车身/后轮/前轮/各 functional 挂点只显当前装备；第二层单层 picker 选完即收起；
  能量合并进 Dock；「寻找对手」为主 CTA，与 MatchPreview 按钮同一视觉体系）；
  Matching 复用主画布 A左B右固定构图 + 顶部「正在寻找对手…」+ 中央 VS（候选换车仅 B 轻量淡入缩放，A 不跳变、无整屏闪/粒子）；
  MatchPreview 视觉连续（车辆位置/尺寸/相机不变，仅文字/按钮变化）+ 复核条（调整配置/开始战斗）；
  updateStartButton/setMode/Reset/Clear 统一委托 applyPlayerShell（Shell 可见性单一真相源）；
  inPlayer 仅在 battleState==='editing' 可见 → Fighting 进入即收起整个玩家 Shell（恢复战场 + Battle HUD）；
  loop 接入 applyMatchingBfx(now)（须先于 lab.render 应用）。

验证：q15PlayerLoop 23/23（--pool=vmThreads --maxWorkers=1）；scoped tsc --noEmit 0 errors（修 1 处闭包捕获
garageSelected:string|null 索引错误，改用闭包内 slotKey:string）。
提交 43b82b9，推送 c10eb00..43b82b9，ls-remote 核验 refs/heads/foundation-02-canonical = 43b82b9。
Dev Server 重启（旧 PID 27008 经 PowerShell Stop-Process 终止 → 新 PID 3896，Badge=foundation-02-canonical @ 43b82b9，HTTP 200）。

禁止项均遵守：无新 Weapon/Gadget、无 Physics/Damage/Weapon-param/Battle-Camera/Battle-HUD/Result-Card 改动、
无 opponentPool 内容改动、无 gold/rank/chest、无正式美术导入、无新依赖/UI 框架、无第二 Renderer、无 test-only 生产运行时能力、未删 Scenario/Dev Tool。

验收对应（真人可见）：刷新即 Garage（Dock 底部、无左长表单）；点击槽位展开单层选项、选完即收起、Dock 实时显示新装备+能量；
点「寻找对手」→ Dock 淡出 → 主画布 A左B右 + 顶部「正在寻找对手…」+ 中央 VS + 候选车 ≥3 次轻量淡入变化（A 不动）→ 锁定
「对手已找到」+ MatchInfo（我的战车 VS 对手+Body+≤3 部件）；点「开始战斗」→ 玩家 Shell 全隐、战场+HP+HUD 全屏；
「下一场」复用同一 Matching；「调整配置」回 Garage。

## Q15-UI-R2-RECOVER｜前提误判核查（调查结论：已落地，未重做）

用户报「真人录像 Runtime 仍 = c10eb00，Q15-UI-R2 没进远端」要求 RECOVER。先查后确认：
- LOCAL HEAD / REMOTE refs/heads/foundation-02-canonical / 运行服务器 Badge 三者一致 = 43b82b9（≠ c10eb00）。
- 远端另有 `refs/heads/feature/foundation-02-canonical` = d0411fb（无关分支，非用户所说的 c10eb00）。
- dist/ 为空目录（gitignore 不跟踪），不存在「冻构建」误导。
- 唯一在跑的 vite 即 PID 19652（重启后），badge 43b82b9；无第二实例。

结论：**Q15-UI-R2 上一轮已真实落地**，用户所见 c10eb00 是「重启前的旧录像/旧服务器实例」——典型「真人录像早于 Dev Server 重启」导致的 stale 误判。
处理：未重做第二套（禁项：不重建）；只做核验 + 重启 + 三端 SHA 一致性确认。
- scoped tsc 0 errors；q15PlayerLoop 23/23（--pool=vmThreads --maxWorkers=1）。
- 无新 commit（无代码改动，43b82b9 已满足 new HEAD != c10eb00）。
- Dev Server 重启：旧 PID 3896 → 新 PID 19652，Badge=foundation-02-canonical @ 43b82b9，HTTP 200。

复盘规律（以后复用）：本项目「真人录像显示旧 SHA」几乎总是「录像早于 Dev Server 重启」，git 实际一致；
先 `git ls-remote` + 看运行服务器启动日志 HEAD 即可判定，不必重做。

## Q15-UI-R2-RECOVER（续）｜真正根因 = display 回退 CSS none（已修复 2cc9004）

上轮「前提误判核查」后，用户二次报「玩家 Shell 完全不可见（无我的战车/Dock/寻找对手，只有车+开发工具）」。
真正根因（显示 Bug，非设计）：
- `.player-top`/`.garage-dock`/`.matching-vs`/`.match-info` CSS 默认 `display:none`；
  `applyPlayerShell()` 显示时用 `style.display = ''` → 移除内联 → **回退 CSS 的 none → 元素永远隐藏**。
- `matchBar`（className=start-bar）CSS 默认已是 `display:flex`，故 `''` 能显，但同样改成显式 'flex' 不依赖 fallback。

修复（2cc9004，3 文件）：
- 新增纯函数 `src/ui/playerShell.ts: computePlayerShellVisibility(uiMode,battleState,playerPhase)`，
  5 元素显示分支一律返回明确值（playerTop/garageDock/matchInfo/matchBar='flex'、matchingVs='block'），绝不返回 ''。
  该函数**运行时 applyPlayerShell 调用**（非测试专用 hook），同时被测试 import。
- main.ts applyPlayerShell 改用该函数赋值，移除全部 '' fallback。
- 禁止项全守：未改布局/Camera/Physics/Gameplay/opponentPool/Weapon/CSS 尺寸颜色；无新 dependency/production hook。

回归测试 `tests/q15PlayerShellVisibility.test.ts`（5 cases，纯模块层，node 环境）：
Garage/Matching/MatchPreview 三态 5 元素 display 断言 + 「visible 值不为空串」护栏 + Scenario/Fighting/Ended 全隐藏。
targeted tests 28/28（23+5）；scoped tsc 0 errors。
提交 2cc9004（`43b82b9..2cc9004`），ls-remote 核验远端=本地。Dev Server 重启 PID 19652 → 22388，Badge=2cc9004，HTTP 200。

通用教训（以后复用）：凡 CSS 默认 `display:none` 的元素，JS 显示时**禁止用 `style.display=''`**（会回退 none），
必须用明确值（flex/block 等）。这条规则适用于任何「显隐靠 JS 切换」的 UI 元素。

## 运行时验收（系统 Edge 截图核验，三态全过）

修复后用 playwright 驱动系统 Edge（channel=msedge，避免下载 Chromium）真实打开 http://localhost:5173，
读 5 元素 inline display + 顶部文案 + Dock CTA + MatchPreview 按钮 + 页面 JS 错误：

Garage：playerTop=flex, garageDock=flex, matchingVs/matchInfo/matchBar=none, ptTitle="我的战车", dockCta="寻找对手" ✓
Matching：playerTop=flex, matchingVs=block（其余 none）, ptTitle="正在寻找对手…" ✓
MatchPreview：playerTop=flex, matchInfo=flex, matchBar=flex, ptTitle="对手已找到", matchBarBtns=["调整配置","开始战斗"] ✓
PAGE_ERRORS: [] （0 运行时错误）

视觉截图（_verify_q15recover/garage.png / matching.png / matchPreview.png）确认：
- Garage 首屏「我的战车」+ 7 chip Dock + 能量 75/110 + 蓝按钮「寻找对手」+ 无左栏
- Matching A左B右同尺度同相机 + 中央 VS
- MatchPreview「我的战车 VS 对手 西瓜车身 前推撞/前上」+ 蓝按钮「调整配置」「开始战斗」
- 页面右下 Badge：foundation-02-canonical @ 2cc9004（三端一致直接证据）

工具复用：playwright 装在受管 Python venv（隔离、不污染全局/项目），用 channel=msedge 复用系统 Edge
（无需下载 Chromium）；验完可删 _verify_q15recover/ 临时目录。

## Q15-FLOW-R1-ATOMIC｜匹配完成直接开战（已完成 + 推送 ba53dd5）

用户指出上一轮 Q15-FLOW-R1 未进仓库（无该 commit）；本轮只改 goToMatchPreview() 一条正式链，不再设计。

改动（2 文件，单 commit ba53dd5，main.ts +10 行）：
- goToMatchPreview() 末尾：
  1) matchBar.style.display='none'——正常流程不再出现「调整配置/开始战斗」。要点：refreshFromEdit
     内部 applyPlayerShell 刚把 matchBar 设为 flex，必须在**同一同步任务内**立即改 none，
     浏览器不重绘中间态 → 复核条永不闪现。
  2) window.setTimeout 250ms → startBattleWithReady()（现有实现，btnFight.onclick 本就用它，
     不复制第二套 READY/Battle 逻辑）；带 guard：仅 playerPhase==='matchPreview' && battleState==='editing'
     才启动，旧 timer/已切换状态 no-op。
- 关键接线事实（排查所得）：playerPhase='matchPreview' 全仓唯一赋值点=goToMatchPreview(1089)；
  startBattleWithReady 调用点原本只有 btnFight.onclick（1046，函数引用，非调用式）；
  Result「下一场」= btnRematch.onclick=nextMatch → startMatching()（既有链，自然变成自动下一场）；
  「调整配置」= btnAdjust.onclick=adjustConfig（既有）。
- 战斗时长：DEFAULT_ARENA_CONFIG phases activeMs=10000/warningMs=3000/closingMs=5000 → 单场 ≤~18s，
  E2E 可等 Result。

验证：
- tests/q15FlowAutoBattle.test.ts（6 cases 源码级护栏，因 main.ts 不可在 node 导入）：
  matchBar 隐藏、250ms 自动调用 startBattleWithReady、guard、单一实现、下一场复用 startMatching、
  调整配置接线。targeted 34/34（23+5+6）；scoped tsc 0 errors。
- 运行时 E2E（系统 Edge）：点「寻找对手」后零二次点击；30ms 探针时间线
  lock(T+1046ms, matchInfo=flex) → ready(T+1309ms) → battle(T+1924ms)，**对手展示 gap=263ms≈250ms**；
  matchBar 全程 none；「下一场」自动锁定→自动开战（第二轮 battle 起）→ Result2「调整配置」回 Garage；
  PAGE_ERRORS []。

提交 ba53dd5（2cc9004..ba53dd5），ls-remote 核验远端=本地。Dev Server 重启 PID 22388 → 3980，
Badge=foundation-02-canonical @ ba53dd5，HTTP 200。三端 SHA 一致（local=remote=badge=ba53dd5）。
