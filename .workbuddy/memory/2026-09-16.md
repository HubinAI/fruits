# 2026-09-16｜PRP-BUILD-01-R3-REMOVE-HIDDEN-CHARGE

**Queue**：`PRP-BUILD-01-R3-REMOVE-HIDDEN-CHARGE`
**分支**：`prototype-portrait-battle-lab`
**基线**：`29045b0`
**功能 commit**：**`db8ad8c`**（11 文件 / +638 −323，含 memory）

---

## 0. 一句话

`快速装填 → 反冲蓄能` 真人第二次验收仍判失败 → 判定**根因不是幅度而是「隐藏的累计状态」**
→ 按 Queue 改判：**整条删除 charge 规则**，改成 **一炮一后坐**（`STRONG_RECOIL_IMPULSE = 110`），
第二层正式名 **强力后坐**。

---

## 1. 为什么 R2 的 450 也会失败（先调查后改码的结论）

- 物理上 450 **确实真实生效**（真人自己确认「450 impulse 真实改变双方距离」）。
- 真人读不出来的是「**这是累计 3 次开火后触发的那一次**」——
  正常速度下它和普通 Cannon recoil / Enemy 接敌 / Collision / Camera follow 混在一起。
- **根因 = 不可见的累计状态**：玩家看不到「2/3」，只能看到一串相似的开炮。
  ⇒ **自然因果优先于幅度**：一炮一后坐，因果锚点变成「炮弹离膛」这一可见事件。

## 2. 三条实测结论

1. **正式相机是「可感知性」的第一约束**：`applyBattleFollow` 对双方 envelope 求 `midX` 居中 +
   0.2/帧平滑 + 世界左界 clamp + `offsetY` 补偿 ⇒ **玩家被推退 与 敌车冲刺 互相抵消**。
   实测每炮 **6~10 世界 px → 舞台带仅 1~3 px**。⇒ 靠加大数值换屏幕可见 = 只能把车推到墙上。
2. **强度线性可换**：`Δv ≈ 冲量 / 200`（世界 px/帧）。110 = 基础后坐 30 的 **3.67×**（倍数级）。
3. **上界由「保留真实接敌」夹住**：`tmpChainProbe` 决定性实验 ——
   V=0 末场挨打 23 次 / V=110 挨打 4 次 / V=120 挨打 **0 次（零接触，已否决）**；
   V=130、150 直接 `左缘 bandX < 0` 越界 + 末段 HP 0（被自己后坐打死）。

## 3. 定值 110（粗档扫描，冻结）

| 冲量 | 每炮世界后移中位 | 玩家世界 minX | 左缘最小 bandX | 单场步数 | 单场结束 HP | 链末场 HP |
|---|---|---|---|---|---|---|
| 0 | 0.4（A 组） | 399 | 30.5 | 586 | 879 | 402 |
| 60 | — | 395 | 29.9 | 586 | 983 | 506 |
| 90 | — | 378 | 29.4 | 628 | 1007 | — |
| 100 | — | 320 | 29.2 | 629 | 1092 | 614 |
| **110** | **−6.0** | **306** | **28.9** | **626** | **1032** | **554** |
| 120 | −8.9 | 198 | 28.9 | 705 | 1100 | 622（零接触） |
| 130 | — | 86 | **−1.7 越界** | 1002 | **0** | — |
| 150 | — | 87 | **−1.1 越界** | 1004 | **0** | — |

⚠️ 链末场 HP 对冲量**强混沌非单调**（60→506 / 80→582 / 100→614 / 110→554 / 120→622）
→ **不用其它数值凑平衡**。

## 4. 落地改动（8 文件源码 / 测试，+294 −241）

- `runModifiers.ts`：id `recoilCharge → strongRecoil`、label `反冲蓄能 → 强力后坐`；
  **删导出** `RECOIL_CHARGE_THRESHOLD` / `RECOIL_CHARGE_IMPULSE`；**新增** `STRONG_RECOIL_IMPULSE = 110`。
- `runBuildAbilities.ts`：**charge 分支整条删除**（`charge` / `chargesSpent` / `chargeThreshold` 全删）；
  新增 `recoilKicks` / `lastRecoilImpulse`；`weaponFire` → 「每次都追加一次强后坐」；
  计数点 = `flush()` 的**真实施加处**（`isFinished` 丢弃的不计），与 `kineticHits` 同纪律。
- `runBattleRuntime.ts`：**仅注释同步**。
- `runPage.ts`：id / 探针同步 + 图标**最小语义同步**（`后坐箭头 + 蓄能条 + 前推箭头`
  → `炮口（右）+ 向后箭头（左）`）；盒位 / 尺寸 / 色值**零改动**。
- 测试：`'X' in module === false` 双重锁「charge 路径不存在」；Reset 无残留；
  A/B 重写为「开火对齐的一炮一后坐」；`FROZEN['fastReload+strongRecoil'] = [843, 622, 554]`；
  E2E 侧镜像字面量表 `buffIconRecoil` 同步。

## 5. 门禁（全绿）

| 门禁 | 结果 |
|---|---|
| `tsc --noEmit` | EXIT 0 |
| `portraitRunModifier` | **25 / 25** |
| `portraitRunPage` + `portraitRunKineticImpact` | **69 / 69** |
| `portrait` smoke | **211 / 211**（9 文件） |
| **全量 vitest**（`--pool=vmForks --maxWorkers=1`） | **203 文件 / 1959 测试 / 0 FAIL**（84.5s） |
| E2E `run-page` | **406 / 406 PASS** |
| E2E `default-entry` | **77 / 77 PASS** |
| E2E `portrait-lab` | **148 / 148 PASS** |
| 四路构建（build / pages / wechat / portrait-lab） | 全部 EXIT 0 |
| `node --check tests/_e2e_run_page.cjs` | OK |

零改动：`git diff --stat -- src/core src/physics src/render src/player src/platform src/battle src/ui` → 空。

## 6. 本轮踩到并确认的两个陷阱

1. ⚠️ **全量 vitest 必须独占机器**。一次并发运行里 `garageFusionResultInteractionR22.test.ts`
   出现 9 个 `Test timed out in 5000ms`（该文件只 import Garage / UI 模块，与本次改动无关）；
   **单独重跑 11/11 PASS（5.18s）**，清空并发后全量 **1959/1959 PASS**。
   ⇒ 看到「非相关文件的 5s 超时」先怀疑资源抢占，别改码。
2. ⚠️ **`kicks` 类指标必须按「开火」对齐 + 用世界 px**：按舞台带 px 会被相机追平压成噪声；
   按「N 帧净位移」会被双方接敌混淆（基线 −54.8px 来自敌人冲刺）。

## 7. 未决 / 需真人裁决

- 110 下每炮真实世界后移 6~10px，但**正式相机把双方中点居中后屏幕只动 1~3 带 px** ——
  「一炮一后坐」物理真实、屏幕幅度受限。按 Queue「若仍只能看出射得快 → 判失败，不再放大数值」，
  **本 Queue 不再动数值**，交由真人录屏裁决；若仍不可见，需**另开表现层 Queue**。
- 新 `strongRecoil` 图标字形无 node 侧几何测试（E2E 选项序列不含该选项）—— 既有覆盖缺口。

---

## 8. 交付

- **功能 commit**：`db8ad8c`（`29045b0..db8ad8c`，11 文件 / +638 −323，含 memory）
- **push**：`29045b0..db8ad8c  prototype-portrait-battle-lab -> prototype-portrait-battle-lab`
- **交接文档**：`交接文档_2026-09-16_PRP-BUILD-01-R3.md`（本地件，不入库）
- **四路 SHA**：HEAD = 本地 ref = `git ls-remote` = `.git/FETCH_HEAD` = **链尾 memory 补正 commit**（即 `git log -1`；功能 commit 为 `db8ad8c`）
- **状态**：停等，等真人录屏裁决「一炮一后坐」是否可感知。

## 9. 收尾时的一次 memory 结构整理

`MEMORY.md` 原 17.4KB，超过注入预算（注入时被截断到约 12.4KB）。处理：

- 全量原文归档 `.workbuddy/memory/archive/MEMORY_FULL_2026-09-16.md`。
- 索引层（`MEMORY.md`）只留「不变量 + 陷阱 + 下一步」→ **10.9KB / 6.5k chars**。
- 细节层拆出 `.workbuddy/memory/REF_PRP_RUNTIME.md`（8.9KB）：战斗参数 / 相机 / 接缝与能力 / RUN-R1 / R3，
  顶部索引表加指针。

---
---

# 2026-09-16（续）｜PRP-BUILD-01-R4-REPLACE-RECOIL-WITH-SUPPRESSION

**Queue**：`PRP-BUILD-01-R4-REPLACE-RECOIL-WITH-SUPPRESSION`
**分支**：`prototype-portrait-battle-lab`
**基线**：`9d6eb09`
**功能 commit**：**`6ee7fea`**（8 文件 / **+503 −293**）

## 0. 一句话

`快速装填 → 强力后坐` **真人第三次验收仍判失败** → 判定**失败点在通道本身**（整条机制都在推**重的玩家自己**，
且相机追平）→ 按 Queue 改判：**整条 recoil 二层机制删除，不再挽救**，
换成 **压制射击 suppressionShot**（冲量施加到**敌车**，只由**真实炮弹命中**触发），`SUPPRESSION_SHOT_IMPULSE = 300`。

## 1. 为什么这次是「换对象」而不是「再调数值」

| 版本 | 机制 | 失败点 |
|---|---|---|
| R2-1 | 反冲蓄能**前推** | 与「快速装填」语义矛盾 |
| R2-2 | **3 发蓄能**后强后坐 | 多了一层**玩家看不见的累计状态** |
| R3 | **每炮**强后坐（110） | 因果已最简，但对象是**重的自己**（3000 冲量仅 4.56° 转动） |
| **R4** | **压制射击（推敌车）** | ← 本轮的改判 |

R3 的 Runtime 完全真实生效，问题是：玩家被后坐推退 与 敌车冲刺 在相机 `midX` 上**互相抵消**
⇒ 正常速度永远读不出「开炮 → 自车后退」。⇒ 把冲量换到**敌车**（轻的 ProtoRusher），
R1 的动能爆发已证明**敌车侧是可感知通道**。

## 2. 关键实现口径（三条）

1. **方向必须复用 `weaponFire.worldDirection`**：`damage` 事件**不带弹道方向**，只有
   `contactPoint` / `contactNormal` / `relativeVelocity`；而 `contactNormal` 是**撞击面法线**
   （斜撞会读成横向），不是弹道。作用点则严格用本次命中**自己的** `contactPoint`。
2. **`soloA: true` 不能做「miss 不触发」判据**（实测敌人仍被命中 4 次）
   → 改用**合成事件注入**直测 `RunBuildAbilities(ports, build)`（记录型假端口，纯逻辑确定性），
   可逐条否掉四条禁止路径：开炮无命中 / 非 weapon 来源 / 反向命中 / 非 cannon。
3. **计数点 = `flush()` 的真实施加处**（`isFinished` 之后），与 `kineticHits` 同一纪律
   ⇒ 240 帧内 `suppressionHits === hits` 严格相等；两个「开炮不触发」指纹 = `hits < fires`、
   `firstHitStep > firstFireStep`。

## 3. 定值 300（粗档扫描，冻结；**未做 10% 级微调**）

| 冲击 | 每命中顶回中位 | 单发最大 | 敌车最大 x | 贴近远端墙 | 玩家挨打 | 步数 | 平均伤害 |
|---|---|---|---|---|---|---|---|
| 0（A 基线） | 0.2 | 3.5 | 1200 | 0 帧 | **23 次** | 586 | 76.9 |
| 60 | 2.4 | 7.3 | 1200 | — | 17 次 | 586 | 76.9 |
| 150 | 13.6 | 23.7 | 1200 | — | 17 次 | 586 | 76.9 |
| 200 | 22.1 | 35.8 | 1200 | — | 18 次 | 586 | 76.8 |
| 250 | 39.5 | 50.4 | 1259 | 0 帧 | 17 次 | 586 | 76.9 |
| **300** | **35.0** | **61.6** | **1336** | **0 帧** | **7 次** | **588** | **76.9** |
| 350 | 39.9 | 71.6 | 1412 | 0 帧 | 4 次 | 592 | 76.9 |
| 450 | 43.5 | 89.7 | 1512 | 0 帧 | 3 次 | 703 | 76.9 |
| **K（动能爆发）** | **76.5** | **121.3** | **1535**（右缘 1626 > 1600） | **258 帧** | 16 次 | 1021 | 79.6 |

六条验收逐条对上（Queue 必改 4）：
① 单次可感知 = A 基线的 **175×**；② 多次持续控距 = 挨打 **23 → 7 次（−70%）**；
③ 不一次飞走 = 单发最大仅为 K 的 **51%**；④ 不顶在边界 = `framesNearWall = 0`（K 组 258 帧）；
⑤ 仍可近身 = `minGap` 仍为负；⑥ 不改 Damage = 平均伤害与 A 组**完全相同**。
选 300 = 六条全满足里的**最低档**（350/450 单发最大已接近动能爆发量级，450 把步数拉高 20%）。

## 4. 落地改动（8 文件 / +503 −293）

- `runModifiers.ts`：id `strongRecoil → suppressionShot`、label `强力后坐 → 压制射击`；
  **删导出** `STRONG_RECOIL_IMPULSE`；**新增** `SUPPRESSION_SHOT_IMPULSE = 300` + 扫描表；
  `RUN_LAYER2_POOLS.fastReload[0]` 同步。
- `runBuildAbilities.ts`：**charge / recoil 分支整条删除**；`weaponFire` 改为**只记录弹道方向**（不产生冲量）；
  `damage` 分支成为唯一入口（同一入口两种语义：kinetic / suppression）；删除 `lastFireX/lastFireY`。
- `runBattleRuntime.ts`：**仅注释同步**（3 处）。
- `runPage.ts`：id / 探针 / 图标**最小语义同步**（`炮口+向后箭头` → `炮弹→敌车→向后箭头`）；
  盒位 / 尺寸 / 色值**零改动**（`iconSuppress` 沿用 `iconRecoil` 色值 `#c9a227`）。
- 测试：`'X' in module === false` + 探针六字段 `gone in raw()` 双锁「旧路径不存在」；
  合成事件注入否掉四条禁止路径；Reset 无残留；
  `FROZEN['fastReload+suppressionShot'] = [843, 622, 582]`；E2E 侧 `buffIconSuppress` / `iconSuppress` 同步。

## 5. 门禁（全绿）

| 门禁 | 结果 |
|---|---|
| `tsc --noEmit` | EXIT 0 |
| `portraitRunModifier` | **26 / 26** |
| `portrait` 全量（9 文件） | **212 / 212** |
| **全量 vitest**（`--pool=vmForks --maxWorkers=1`） | **203 文件 / 1960 测试 / 0 FAIL**（79.82s） |
| E2E `run-page` | **406 / 406 PASS** |
| E2E `default-entry` | **77 / 77 PASS** |
| E2E `portrait-lab` | **148 / 148 PASS** |
| 四路构建（build / pages / wechat / portrait-lab） | 全部 EXIT 0 |
| `node --check tests/_e2e_run_page.cjs` | OK |

零改动：`git diff --stat -- src/core src/physics src/render src/player src/platform src/battle src/ui` → 空；
`runPageLayout.ts` / `runImpactVfx.ts` / `runBattleView.ts` / `testData.ts` 同样零 diff。

## 6. 本轮踩到的陷阱（新增一条）

⚠️ **改完源码直接跑 PRP 的 E2E 会拿到旧产物的假 FAIL。**
首跑 `e2e:run-page` 得 402/406，4 个视口同报 `R53b … suppress=undefined`。
排查：`runPage.ts:1322-1324` 探针映射正确；`grep -rl "suppressionHits" dist-portrait-lab/` → **空**，
`grep -rl "strongRecoil" dist-portrait-lab/` → **命中旧 chunk** ⇒ 确认是**产物陈旧**。
`npm run build:portrait-lab` 后复跑 → **406/406 PASS**（`suppress=0`）。
根因：`vite.portrait-lab.config.ts` 的 `emptyOutDir: false` ⇒ `dist-portrait-lab/assets/` 累积历史 chunk
（本次重建后仍有 **17 个**过期 chunk，gitignored）。
**纪律：跨轮校验 PRP 前先 `build:portrait-lab`。**

## 7. 未决 / 需真人裁决

- 冲量现在作用在**敌车**上，但正式相机**同时跟随敌车** ⇒ 屏幕位移仍 ≈ **世界位移的一半 × 舞台缩放**：
  中位 **35 世界 px** ≈ **8~10 舞台 px**（敌车宽约 90px 即约 **39%**）。是否够可感知**仍交真人裁决**。
- ⚠️ 对照事实（**非本 Queue 缺陷、未处理**）：`kineticBurst` 自己会把敌车顶到远端墙
  （`enemyMaxX 1535`、贴近墙 **258 帧**）。
- 新 `suppressionShot` 图标字形仍无 node 侧几何测试（E2E 选项序列不含该选项）—— 既有覆盖缺口。

---

## 8. 交付

- **功能 commit**：`6ee7fea`（`9d6eb09..6ee7fea`，8 文件 / +503 −293，含 memory）
- **push**：`9d6eb09..6ee7fea  prototype-portrait-battle-lab -> prototype-portrait-battle-lab`
- **交接文档**：`交接文档_2026-09-16_PRP-BUILD-01-R4.md`（本地件，不入库）
- **四路 SHA**：HEAD = 本地 ref = `git ls-remote` = `.git/FETCH_HEAD` = **链尾 memory 补正 commit**
  （即 `git log -1`；功能 commit 为 **`6ee7fea`**）
- **状态**：停等，等真人录屏裁决 ——
  第一层「这辆车开炮很快。」/ 第二层「它能靠连续炮击不断把敌人顶回去，让敌人很难贴近。」
  ⚠️ 若正常速度仍需解释 ⇒ **这条第三 Build 方向判失败，停止继续开发**（Queue 原文）。
