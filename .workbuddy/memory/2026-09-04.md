# 2026-09-04 工作日志

## T6 脆性断言 Bug Queue（R1 未完成项收口）

- **背景**：R1 交付后全量 vitest 唯一 1 failed = `garageBuildBoardP0.test.ts T6`（既有脆性基线，非 R1 引入）。用户「继续未完成的任务」→ 作为独立 Bug Queue 处理，不并入 R1 scope。
- **根因**：T6 末断言用固定 `src.slice(cardsStart, cardsStart+2600)` 2600 字符窗口找 `this.drawPartCard(... c.v === curVal`；`drawGarageStripCards` 起点 L2720，目标调用在 L2778，方法体增长后 2600 窗口截不到 L2778（实证 `false`）。与 T3 同因。
- **修复**：改为方法边界截取（`src.indexOf('\n  private ', cardsStart+10)` 取下一顶层 private 方法边界），窗口随方法体伸缩（实证 3398 字符、`true`）。仅改 `tests/garageBuildBoardP0.test.ts`，不碰生产代码。
- **门禁**：garageBuildBoardP0 8/8；全量 vitest **1665/0**（vmForks --maxWorkers=1）；tsc 0；repo-health 9/9；local=remote。
- **commit**：`d41fcac25d4a65bcaf377665a9dc7dcac3993b86`（1 file +7/-1），已 push `9c76c9b..d41fcac`。
- **经验**：源码字符串守卫断言一律用「方法边界截取」而非固定字符窗口（T3/T6 已立先例）；固定窗口随方法体增长必脆性失败。

## iOS 复验就绪性收口（④层前置）

- T6 仅改测试文件，生产代码与 `9c76c9b` 字节一致。为保持四路 SHA 诚实，重建微信 RC 包：`npm run build:wechat:rc` → `dist-wechat/game.js`（1,362.64 kB）+ `rc-build.json.fullSha=d41fcac` + SHA 水印 `#d41fcac` + dirty=false + bundle-clean PASS(rc) + 三方 SHA 校验通过。
- **四路 SHA 重新对齐**：LOCAL=REMOTE=RC_JSON=BUNDLE=`d41fcac`。
- 交付 `outputs/F-GARAGE-INVENTORY-FUSION-R1-iOS-verify-checklist.md`：绑定 `#d41fcac` 的模拟器/真机复验清单（PLAYBOOK 阶段 E + §8 录屏纪律 + R1 专项 UI 检查），明确标注 WorkBuddy 本沙箱无法代执行模拟器/真机步骤。

## 当前待办（仍阻塞）

- ④ iOS 真机真人体验：需用户按上述清单走微信模拟器亲检 + 真机录屏（可见 SHA `d41fcac`）+ 前后台专项。本沙箱无法执行；用户回执后关闭 ④。

## F-RC-FUSION-TEST-ENTRY-P0（RC 包真实 5合1 验收可达性修复）

- **目标**：RC 包无法验证真实 5合1 + 恢复「全部件×1」按钮身份。仅改 RC 测试工具/UI 入口，不动正式经济与合成规则（fuseSameStar/canFuse/MAX_STAR=2 冻结）。
- **根因**：「全部件×1」(`grantAllPartsOnce`) 仅幂等补到 1，无 5 份材料入口 → RC 永远做不了 5合1；且领取后只显示「已领取」失去身份。
- **§二**：dev-grant 标签 领取前「全部件×1」/ 领取后「全部件×1 ✓」（保留可见，×1 语义不变）。
- **§三**：新增 RC 专用中性灰蓝按钮 `backpack-test-material`（标签固定「测试材料×5」带「测试」字样）；仅 `(rcGrant||e2eProbe||devReset) && 无2★副本` 时绘制+注册；点击补足当前选中 defId 1★ 可用到 5（requiredOwned=equippedCount+5；topUp=max(0,requiredOwned−owned)）；已装备保护/满5幂等/不影响其它部件·Build·能量·金币·段位·奖励池；普通微信/Pages/正式Web 恒不可见。
- **改动**：`src/ui/canvasPlayerUIHost.ts`(+37) / `tests/rcFusionTestEntryP0.test.ts`(新,17 test T1–T16) / `tests/_e2e_backpack_fusion.cjs`(重写为真实按钮闭环)。
- **门禁**：targeted 17/17；全量 vitest **1682/0**（vmForks --maxWorkers=1）；tsc 0（修复测试文件未用导入 + BuildDraft 类型补全）；四构建 EXIT=0；bundle-clean(wechat/e2e/rc) 三路 PASS；RC 重建 SHA 水印 #6ac067d dirty=false；四方 SHA 一致。
- **commit**：`31b8f22`（amend 自 `f24310e` 以保队列单 commit + 修 tsc）+ `588e615`(memory hygiene) + `6ac067d`(当日记忆日志)。local=remote=6ac067d。
- **§四 真实按钮闭环已实跑通过（2026-09-04 补）**：`tests/_e2e_backpack_fusion.cjs` 在 **headless Edge** 真实浏览器跑通 **140/140 PASS**（6 视口；844×390 DPR1/DPR3 含完整 5合1 链路 F0–F20：种子1份→「还差4个」→「测试材料×5」→1★ 1→5→「合成」→1★-5/2★+1→幂等/连点不重复→reload 2★保持）。E2E 构建(`__E2E_INTERNAL_HANDLE__`) 与 RC 包(`__WX_DEBUG_GRANT__`) 触达同一段 `showTestMaterial` 门控 + `case 'backpack-test-material'` dispatch，按钮接线逻辑 100% 同源 → RC 包可复用该结论。微信 canvas 呈现层未单独跑（无开发者工具），但不改变 UI host draw/dispatch 行为。
- **stop & wait**：交付后未继续其他 Queue；建议用户在微信开发者工具 RC 包做一次人工点检作最终体感确认。
