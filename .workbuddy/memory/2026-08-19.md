# 2026-08-19 工作日志

## 接手状态梳理（未执行代码改动，仅读档）

- 读取两份基线：`最强水果_WorkBuddy执行指令01_Physics_Foundation.md`（V1.0，Physics Foundation 任务边界）+ `最强水果_项目核心共识与开发边界_WorkBuddy_Memory.md`（V1.0，长期开发基线）。
- 当前分支 `feature/foundation-02-canonical`，HEAD=`8a705e7`（B16A Planck Body transform/bounds）。
- **Planck 基础层已全部进主线**：World/Body/Revolute/重力/接触桥接/碰撞过滤/Owner/Motor/Weld/Compound/质量加权COM/静态-运动学原语/边界契约 + 引擎中立 CombatVehicleState/DamageResolver/ContactRouter/ArenaConfig/BattleContract/PhaseClock。
- **待办缺口（关键）**：
  1. 端到端 **Planck Battle Orchestrator 尚未编写**（src/battle 仅有 Matter 版 battleOrchestrator.ts）；
  2. **Physics Lab 仍跑在 Matter 上**（physicsLab.ts → BattleOrchestrator），与规范 §10/§18.5「Lab 必须直接调正式 Canonical Runtime」还差一步；
  3. 5 个 Scenario 未切到 Planck 复验。
- **工作树未提交**：`src/battle/planckVehicleAssembly.ts` 新增 `settlePlanckVehicleToRestPose`（轮径差静置姿态，B16B，是 Orchestrator 前置 helper）；`.workbuddy/memory/*` 改动；5 个未跟踪诊断测试（wheelStiffnessDiagnostic/wheelHardAxleDiagnostic = Matter 废弃方案残留；planckRevoluteSpike/planckDriveCalibration/planckVehicleSettle = Planck POC 残留），建议后续清理。
- **已向用户确认流程**：提交前必须询问方向。已发 AskUserQuestion 让用户选「完成 Planck 运行时 / 打磨 Matter 版 / 清理工作树 / 先跑全量验证」——用户回复「等我指令」（q-0 → 等我指令），未选择具体方向。
- 后续执行前仍需用户明确方向；本会话未启动任何代码改动或 git 操作。

### 候选下一步（待用户拍板）
- 首选（分支既定轨迹）：写 PlanckBattleOrchestrator + 把 Physics Lab 切到 Planck + 5 Scenario 复验，提交 B16B settle 函数。
- 备选：先跑 tsc + vitest(--pool=vmThreads) 拿红绿基线；或先清理工作树。

## B16B 接续包执行轨迹（只读溯源 → 验收 → 本地提交）

> 来源：`最强水果_个人版WorkBuddy接续包_2026-08-19.md`（个人版只读接管）。本节追加进 B16B 全轨迹，便于后续接续不需要重读接续包。

### 队列执行清单
- **MIGRATE-00**（只读接管核验）：发现 Git 异常（工作树含未提交生产代码 `settlePlanckVehicleToRestPose`）+ 运行时不符（默认 node v22.22.2 < `engines.node>=24`）。按规则停报。
- **MIGRATE-01V**（异常工作区只读溯源）：
  - 切 Node 24.14.0 到当前进程 ✓（不动系统环境）
  - HTTPS `git ls-remote https://github.com/HubinAI/fruits.git refs/heads/feature/foundation-02-canonical` = `8a705e7959e5a41bbf50bc385277999efd3749c0`，与本地/期望 SHA 一致 ✓
  - 工作树 diff 真实存在：`src/battle/planckVehicleAssembly.ts` 的 +99 行（`+rotateLocal` + `settlePlanckVehicleToRestPose`，doc 注释标 `(B16B)`）。
  - 未跟踪测试 `tests/planckVehicleSettle.test.ts` 头注释 `Queue F-02M-B16B｜Planck 整车无冲量贴地静置测试`，6 个 `F-02M-B16B ·` describe 块。
  - 两文件均明确属 B16B；**全仓库无 `F-02M-B17A` 任何定义**，B17A 不存在。
- **F-02M-B16B-V**（候选验收）：静态范围（+99/-0，无 as any/native 暴露/跨 World）+ 语义（贴地用 collision bounds / facing 镜像成立 / 一致刚体变换 / 速度归零 / COM 一致 / 幂等 by construction / 不施力不推步）+ 测试（6+6+13=**25/25**）+ scoped tsc（exit 0）。**B16B 候选验收通过**。
  - **幂等性观察**：本轮未含独立"两次调用"用例，幂等由构造保证（非独立测试覆盖）⚠️
- **F-02M-B16B-R1**（幂等回归，只改测试）：
  - 在 `tests/planckVehicleSettle.test.ts` 末尾追加用例 #7「连续调用两次幂等」（+97 行）。
  - 实测：maxPos/Ang/Com/Bounds = **0**，maxAnchor = **8.882e-14 px**（未增加）；全部速度 <1e-12，无 NaN/Infinity。
  - **7/7 通过**，scoped tsc exit 0，仅改测试文件，未碰生产代码 ✓
- **F-02M-B16BS**（本地 commit 完成；push 因环境凭据被阻）：
  - HTTPS dry-run push 失败：首因 `gitconfig.lock` 陈旧孤儿锁（hung 进程残留），清除后新因 `helper-selector` 配在 .gitconfig 里导致 selector dialog 弹出（不是 SHA 冲突）。已 `git config --global credential.helper manager` 替换为 GCM（覆盖了原本带 `!` 前缀的 GCM 绝对路径，避免冲突）。
  - 精确暂存：`git add -- src/battle/planckVehicleAssembly.ts tests/planckVehicleSettle.test.ts` → cached 严格两个 ✓，`diff --cached --check` 无空白错误。
  - commit 完成：**SHA `16b5ca431c37ff7b5dbcda9f439fe4db5dc621b6`**（短 `16b5ca4`）、parent `8a705e7959e5a41bbf50bc385277999efd3749c0`（期望前 HEAD ✓）、parent count 1 ✓、tree 仅两个目标文件 ✓。
  - **git ref 写入不可靠再次命中**：commit 后分支 loose ref 未落盘，用 `git cat-file -p 16b5ca4` 校验 commit 完整后，按 MEMORY 里大海授权路径 `mkdir -p .git/refs/heads/feature && printf '%s\n' 16b5ca4... > .git/refs/heads/feature/foundation-02-canonical` 恢复 ref。
  - push 尝试：`GIT_TERMINAL_PROMPT=0` 立即失败（exit 128）—`fatal: could not read Username for 'https://github.com': terminal prompts disabled`。本沙箱 bash 无 TTY，`.git-credentials` / `.config/git` 凭据缓存均不存在，等 GitHub PAT 由用户提供。
  - **远程分支 SHA 未变**：`8a705e7959e5a41bbf50bc385277999efd3749c0`（push 失败，未推送）。

### 用户授权
用户截图显示 CredentialHelperSelect dialog（manager 已高亮），并声明："后续这类请求，不需要反复让我选择。我授权你直接操作就好，我相信你！"
- 已执行：`git config --global credential.helper manager`（覆盖之前 .gitconfig 中带 `!` 前缀的 GCM 绝对路径，避免冲突）。
- 未使用 AskUserQuestion；不再把决策抛回用户。
- **剩余唯一硬要求**：用户必须（一次性）在 Windows 屏幕上点 Select 按钮（OS GUI 限制，本会话无法替点），并贴一个对 HubinAI/fruits 有 write 权限的 GitHub PAT（任何时候，一个即可），之后 push 自动完成。

### 当前状态（push 前）
```
HEAD = 16b5ca4... (本地已落盘)
parent = 8a705e7...
status --short:
 M .workbuddy/memory/MEMORY.md
?? .workbuddy/memory/2026-08-17.md / 18.md / 19.md
?? tests/planckDriveCalibration.test.ts
?? tests/planckRevoluteSpike.test.ts
?? tests/wheelHardAxleDiagnostic.test.ts
?? tests/wheelStiffnessDiagnostic.test.ts
?? 最强水果_个人版WorkBuddy接续包_2026-08-19.md
```
无 index 内容；push 未执行。

## AUTH-GCM-01（GitHub 浏览器鉴权，只登录不推送）— 已停止

- **队列指令**：`git credential-manager github login --url https://github.com --web`，仅登录不推送；严禁索要/接收/显示 PAT、严禁把 token 写进 URL/命令/文件/聊天；命令不支持即停、不得改用 PAT。
- **执行与结果**：
  - `git credential-manager version` → 空输出（无版本）
  - `git credential-manager github list` → **exit 5**（GCM 通用错误）
  - `git-credential-manager.exe --version`（绝对路径 exe，132KB, Jul 10）→ **exit 139 Segmentation fault**（GCM 二进制在本环境直接崩）
  - `git credential-manager github login --web` → **exit 5**，无 stdout/stderr，**浏览器未打开**。
- **结论**：PortableGit 1.2.0 自带的 GCM 二进制在本沙箱不可用（segfault / exit 5），无法进入浏览器 OAuth 流程；用户也未提供 PAT 路径。
- **按队列硬规则已停止**，未改用 PAT、未登录、未提交/推送、未改任何文件/Git 配置/ref。
- **B16BS 推送仍阻塞**：本会话无法完成 GitHub 鉴权。后续若要 push，只能等用户在能跑 GCM 的环境（或 IDE 内凭据助手、或提供 PAT）完成鉴权；本会话内 AUTH-GCM-01 路径已证明不可行。

## F-02M-B17A-R0/R1A/R1B/R1C（Planck Battle Orchestrator 范围只读核验）— 已完成

- **R0（范围核验）**：给出 B17A 接入清单；当时错误假设「wheel.grounded 无写入来源」，建议新增 planckVehicleRuntime.ts。
- **R1A（PlanckWorld 接口）**：`step(realDtMs,timeScale=1,onBeforeStep?)` 签名与 Matter 一致；onBeforeStep 每固定步前调用一次（while 内 `onBeforeStep→world.step→flushBatched`）；返回实际步数；批次 flush 在每个 world.step 后；**Planck 无 setCollisionHandlers**，只有 `setContactListener`/`setBatchedContactListener`（单回调 + `phase:'begin'|'end'`）；ContactBridgeEvent 用 opaque BodyHandle（非 Matter.Body）、无 active 相位、relativeVelocity 公式符号与 Matter 相反。结论：不能直接替换 setCollisionHandlers。
- **R1B（ContactRouter/DamageResolver 引擎中立）**：**结论 A，两者直接复用**。ContactRouter 已有 `handlePlanckContact(world, ev)`（用 `world.getOwnerTag`）；DamageResolver 零耦合；PlanckVehicle 结构满足 CombatVehicleState。残留：`contactRouter.ts` 仍 import `getMeta`/`ContactEvent`（仅 Matter 路径用，构建期、非运行期阻塞）。relativeVelocity 符号需在 R1C 用单测对齐。
- **R1C（grounded 来源）— 关键更正**：`grounded` **已有写入来源**！`contactRouter.ts` `handlePlanckContact`→`route`→`handleGrounded`（L355-384）按 `kind==='ground'` + `wheel:` partId 在 begin(start)/end 真实置 `grounded=true/false`。wheel 有 OwnerTag(kind='vehicle',partId='wheel:')+碰撞 category；ground 有 OwnerTag(kind='ground')+PlanckCategory.GROUND。**R0 的「无写入来源」是误判**，无需新增 planckVehicleRuntime.ts。B17A 实现只需：orchestrator 用 `setBatchedContactListener(cb=>router.handlePlanckContact(world,ev))`（**只接 batched，禁止同时接 immediate**，否则双投递重复伤害）；grounded 即自动维护。relativeVelocity 符号对齐仍是单测风险项。

## F-02M-B17A-A1（Planck Battle Orchestrator 最小驱动闭环）— 已完成（未提交）

- **新增 2 文件**（严格范围内）：`src/battle/planckBattleOrchestrator.ts` + `tests/planckBattleOrchestrator.test.ts`。未改任何既有生产文件/配置/依赖/Git ref。
- **实现**：`PlanckBattleOrchestrator` 对齐 Matter `BattleOrchestrator`——构造输入/公开状态（world/arena/vehicleA/B/router/damageResolver/bus/config/result/phase/timeMs）同构；用 `new PlanckWorld({x:0,y:10})`（Canonical Planck 重力，复用既有 Planck 测试约定）、`resolveSnapshot`、`PlanckArenaRuntime`、`createPlanckVehicle`、`settlePlanckVehicleToRestPose`（config.settleToGround 默认）、`DamageResolver`、`ContactRouter`（`handlePlanckContact` 复用）、`resolveBattleResult`。**只注册一次 `setBatchedContactListener`**，未注册 `setContactListener`。`step()` 在 onBeforeStep 内 `drivePlanckVehicle`（A worldDirection=+1/B=-1，目标速度 1.5 px/step 复用 Matter TARGET_SPEED 语义），按实际步数推进 time/arena。grounded 全交给 ContactRouter，未新增检测。
- **验证**：新测试 5/5 通过（构造贴地无穿透 / 首次固定步 grounded=true / autoDrive A+163.04 B-163.04 / autoDrive=false 无推进 dx≈2.6e-5 / 200 步无 NaN）；scoped strict tsc exit 0；`git diff --check` 干净（顺手清了 MEMORY.md 一处末尾空行）。
- **留待 A2**：relativeVelocity 符号与伤害区间、重复伤害、Active→Warning→Closing→End 全阶段。
- **注意**：A1 测试 #1 初版断言写错（误用 `bb.minY` 轮子顶部判穿透），实际应判 `bb.maxY`（轮子底部）；已修正后通过。B17A 文件均**未提交**，等待 A2。

## F-02M-B17A-A2D（Planck 接触伤害与重复结算诊断）— 已完成

- 范围合规：仅改 `tests/planckBattleOrchestrator.test.ts`，新增测试 6（Weapon 隔离）/ 7（Impact 隔离），共 7/7 通过。scoped tsc 对该文件 0 错误；`git diff --check` 干净。
- 为捕获 per-pair 真实桥接细节，对 orchestrator 现有 batched listener 做**探针包装**（仅记录 + 原样转发给同一 `ContactRouter`，**单 listener / 单次路由语义不变**，无伪造事件）。
- **实测确认**：closing `relativeVelocity` 为正；Weapon 隔离首批 `B 1000→920` 恰扣 80、Damage 事件恰 1；Impact 隔离首批 3 个敌对 pair 仅结算 1 次 Impact（`lastImpact.relativeVelocity == batch.max`），双方扣血公式 `min(120,(relVel-0.75)*0.5)/2` 校验吻合；无 Weapon Damage。
- 全项目 tsc 另有 14 条错误均在**其他未跟踪诊断测试文件**（`impactThreshold` / `planckContactKinematics` / `planckDriveCalibration` / `planckRevoluteSpike` / `wheelHardAxleDiagnostic` / `wheelStiffnessDiagnostic`），属早前回合遗留、超出本队列范围且本队列禁止修改；**未引入新类型警告**。后续需单独清理队列。

## F-02M-B17A-A3（阶段时钟与 BattleResult 闭环）— 已完成

- 新增测试 8（完整阶段时钟闭环 Active→Warning→Closing→End，单次 step 最多跨一阶段，End 前 result 恒 null，首次 End 非空，双方 HP 未变 → winner=draw）/ 测试 9（KO 立即结算 winner=A，再 10 次 step 数据零漂移）。9/9 全过，scoped tsc exit 0，`git diff --check` 干净。
- **透明记录**：测试 8 在构造时仅传入 `arena.closingSpeed: 0`（与 `phases` 同为运行时 `BattleConfig`，非改生产）。原因——阶段时钟是纯时间驱动（`ArenaPhaseClock` 不依赖墙速），但默认 `closingSpeed:40` 会让刺墙在 Closing 扫过静止车造成 hazard 伤害，破坏「避免伤害干扰」意图。置 0 仅让墙不移动，阶段时序完全不变。
- **关键时序实测**：call 1→61→91→152 严格 Active→Warning→Closing→End，End 时 `result={winner:"draw", hpA:1000, hpB:1000, phase:"End"}`；End 后 10 次 step `result/hpA/hpB/posA/posB/damageCount` 全部零变化。

## F-02M-B17A-A4S（提交 B17A 两文件）— 已停止于 ref 异常

- 前置 5 项全过；提交前复核 9/9 测试 + scoped tsc exit 0 + `git diff --cached --check` 干净；精确 `git add` 两文件 + `git commit -m "feat(battle): add Plank battle orchestrator"` 成功。
- **新 commit 完整 SHA**：`665a7f3b19a96cda3cb552108e05269b431b9bb5`（parent=`16b5ca4...`，parentCount=1，diff 仅 2 文件：`src/battle/planckBattleOrchestrator.ts` + `tests/planckBattleOrchestrator.test.ts`，合计 +761）。`git fsck` 干净。
- **停止原因（本地 ref 异常）**：commit 后 `.git/refs/heads/feature/foundation-02-canonical` 文件未落盘（`HEAD` 解析 `unknown revision`，index 显示 77 条 `A` 系 unborn 显示伪影）。队列禁止 `update-ref`/`branch -f`/`commit-tree`/手写 ref，故**未恢复、未推送**，仅保留本地 commit 对象。

## F-02M-B17A-A4R（git update-ref 原子恢复尝试）— 已停止于 update-ref 静默失败

- 步骤 1 只读确认全过（cat-file -t=commit / NEW^=BASE / origin=BASE / diff 仅 2 文件）。
- **步骤 2 `git update-ref refs/heads/feature/foundation-02-canonical NEW`**：退出码 **0**，但**静默失败**——ref 文件未写入磁盘（`refs/heads/` 仅 `dev`/`master`），`packed-refs` 不存在，`rev-parse refs/heads/feature/foundation-02-canonical` 仍 `unknown revision`。
- 步骤 3 验证失败（`rev-parse HEAD=unknown revision`，ref 缺失），触发步骤 5「update-ref 异常」→ 立即停止，不补救、不改配置/凭据、未推送。

## F-02M-B17A-A4R2（手写 loose ref + 推送）— 本地已恢复，推送被环境阻断

- **手写恢复（用户授权一次性执行）**：`mkdir -p .git/refs/heads/feature && printf '%s\n' NEW > .git/refs/heads/feature/foundation-02-canonical` 成功，ref 文件落盘。
- 步骤 2 验证全过：`symbolic-ref HEAD=refs/heads/feature/foundation-02-canonical` ✓，`rev-parse HEAD=NEW` ✓，`rev-parse HEAD^=BASE` ✓，`diff --cached --stat` 空 ✓，B17A 两文件不再 `A/M/??`（已跟踪 + 干净）✓。status 只剩原有 `.workbuddy/**`（1 ` M` + 3 `??`）/ 4 诊断测试 `??` / 接续包 `??`，unborn 伪影消失。
- **推送（环境阻断）**：
    - HTTPS `git push origin ...` → exit 128，`fatal: could not read Username for 'https://github.com': terminal prompts disabled`（沙箱无 TTY）。
    - SSH 显式 URL 兜底（不改配置）`git push git@github.com:HubinAI/fruits.git ...` → exit 128，`Permission denied (publickey)`（本机无 GitHub SSH 密钥）。
    - 队列禁止改 Git 配置/凭据，**未推送**，origin 仍 `16b5ca4...`。
- **当前唯一未做**：远程推送。commit `665a7f3b...` 完整留存，本地 HEAD=NEW，index 干净，工作区健康，等待用户在有凭据/密钥的环境或授权 PAT 后推送。

## GCM CredentialHelperSelect dialog 再次出现 + Git 侧硬化

- 用户截图反馈：dialog 又弹，已自行点击通过（选 `manager` + 勾 `Always use this from now on`）。明确授权后续出现时我直接操作。
- **dialog 性质更正**：它是 **GCM 自身首次启动的后端选择 dialog**（manager/wincred/no helper），与 git 的 `[credential] helper` 是**两层独立设置**。2026-08-19 实测反例：即使 git 侧早已 `helper = manager`，该 dialog 仍会再次弹出 → 证明 dialog 触发与 git config 无关。**真正阻止 dialog 再弹的是用户 GUI 勾选 `Always use this from now on`**（GCM 把后端持久化到自己的 config）。原始 MEMORY 该条「`git config ... manager` 即可让 dialog 不再出现」不准确，已在 MEMORY.md 修正。
- **Git 侧硬化已执行**（用户授权下）：`git config --global credential.helper manager` 整行覆盖原 `!` 前缀显式 exe 路径行 → 当前 `.gitconfig` 单行 `helper = manager`（无 `!`、无双引号转义、`--get-all` 仅 1 行，干净）。
- **推送阻塞补充诊断**：本地 push 失败既非 git config 也非 ref，纯粹是沙箱**无 TTY 凭据 + 无 SSH 密钥**（GCM 二进制此前 `--version` 段错误 exit 139；即使 GUI dialog 解析完，沙箱 bash 仍无法与 GCM GUI 认证 dialog 交互）。唯一绕过路径是用户在有 TTY/密钥环境手动 push，或显式提供 PAT / 部署 SSH 密钥——这两件均非沙箱能完成。
- **下次 dialog 出现的处理流程（沙箱 bash）**：不尝试点（OS GUI 不可达）→ 仍跑一次 `git config --global credential.helper manager` 兜底清理 `.gitconfig` → 告知用户在 dialog 里勾 `Always use this from now on` 一次性根除。不再反复问。

## F-02M-B17B-V（正式双引擎接线统一验证）— PASS
- 前置：分支 `feature/foundation-02-canonical`、HEAD=`665a7f3b...`、index 无 staged。
- V1 `tests/physicsLabEngine.test.ts` 6/6；V2 B17A `tests/planckBattleOrchestrator.test.ts` 9/9（队列写的 `plank`(无c) 为 git 显示伪影，真实文件 `planck`(有c)）；V3 `battleContract` 7/7 + `foundationCanonical` 5/5 + `baselineDrive` 3/3 = 15/15；全部 exit 0，Node v24.14.0。
- scoped strict tsc（--ignoreConfig，镜像 tsconfig 全选项，仅 6 目标文件+传递 src 依赖）exit 0；`git diff --check` exit 0。结论 **PASS，可进入 B17B-S**。

## F-02M-B17B-S（精确提交并确认分支落盘）— 已停止于 ref 异常（同 A4S 模式）
- 前置 3 项全过（分支/HEAD=665a7f3b/index 空）；精确 `git add` 6 文件，`diff --cached` 严格 6 个 ✓（`planck`(有c) 真实路径）。
- `git commit -m "feat: wire Plank battle runtime into PhysicsLab"` exit 0，输出 `[feature/foundation-02-canonical 1c3c8fb]`，6 files changed, +614/-51。
- **新 commit 完整 SHA**：`1c3c8fbe60b78e8609c55520cdb7a49aafe42c3f`（parent=`665a7f3b19a96cda3cb552108e05269b431b9bb5` 精确 = 锚点 ✓；`git show 1c3c8fb` 严格 6 文件 ✓；`cat-file -t`=commit；fsck 显示 unreachable=dangling 但对象完整）。
- **停止原因（ref 未前进，同 A4S/A4R 模式）**：commit 后 `refs/heads/feature/foundation-02-canonical` 未落盘（`.git/refs/heads/` 仅 `dev`/`master`，`feature/` 子目录被清）；`rev-parse HEAD` / `rev-parse feature/foundation-02-canonical` 均 `unknown revision`（unborn 分支）；`.git/HEAD` symref 仍指向 `refs/heads/feature/foundation-02-canonical`；index 显示 78 条 `A`（unborn 显示伪影 + index 异常非空）。队列禁止 reset/update-ref/stash/commit → **未恢复、未推送**。
- **下一步（待用户授权，项目惯例恢复路径）**：`mkdir -p .git/refs/heads/feature && printf '%s\n' 1c3c8fbe60b78e8609c55520cdb7a49aafe42c3f > .git/refs/heads/feature/foundation-02-canonical` → 验证 `rev-parse HEAD`=NEW、`HEAD^`=665a7f3b、index 清理 → 再 push（需凭据/PAT 或用户 IDE 推送）。

## F-02M-B17B-SR（git update-ref 恢复尝试）— FAIL，update-ref 再次静默失败
- 第一步复核全过：commit 类型=commit、Parent=665a7f3b…（精确）、diff-tree 恰 6 文件。
- `git update-ref refs/heads/feature/foundation-02-canonical 1c3c8fb…` **exit 0 但静默失败**（第三次复现该怪癖）：`.git/refs/heads/` 仍仅 `dev`/`master`，`feature/` 目录不存在；`rev-parse HEAD`/`refs/heads/feature/foundation-02-canonical`/`HEAD^` 均 `unknown revision`；index 仍 78 项 `A`（unborn 伪影）。
- 队列禁止 mkdir/printf 手工写 ref、reset、清理 → 未补救、未 push，结论 **FAIL**。
- **结论固化**：本环境 `git update-ref` 一律静默失败（exit 0 不写盘），`git commit` 内部 ref 更新同样失败。**唯一可靠恢复路径 = 用户授权下的手工 loose ref 写入**（A4R2 已验证：`mkdir -p .git/refs/heads/feature && printf '%s\n' SHA > .git/refs/heads/feature/foundation-02-canonical`）。

## Q02-CANNON-R0（普通炮进入 Planck Runtime 只读定位）— 已完成
- 已存在：OwnerKind 含 'projectile'；PlanckCategory.PROJECTILE；arena 墙 mask 含 PROJECTILE；isOutOfProjectileBounds + projectileTopY；PlanckWorld body 创建/速度/位置/Owner/batch contact 全 API；DamageResolver 引擎中立可直接复用；orchestrator onBeforeStep 已留 Behavior 插入口（L270-284）。
- 缺失 Foundation：①PlanckWorld 无 destroyBody；②无 applyLinearImpulse/applyForce；③ContactRouter 无 projectile 路由（hostile 过滤要求双方 kind==='vehicle'）；④无 cannon/projectile 内容定义；⑤orchestrator 无 projectile 生命周期；⑥Renderer Snapshot 无 projectile 字段。
- 拆分建议：Q-F1（PlanckWorld destroy+impulse+bullet）→ Q-F2（ContactRouter projectile 路由）→ Q-C1（Cannon Behavior）→ Q-C2（Content）→ Q-C3（Renderer，延后）。

## Q02-F1（PlanckWorld Projectile Foundation）— 已完成（未提交）
- 仅改 `src/physics/planckWorld.ts` + 新增 `tests/planckWorldProjectileFoundation.test.ts`。
- **destroyBody(handle)**：真实 world.destroyBody；handle 立即失效（bodyOf 抛错）；OwnerTag/bodySeq/bodyByNative 同步清理；相连 joint（Revolute/Weld）wrapper 同步失效（先清 joints/revoluteJoints 映射）；World locked 时明确抛错（批次监听在 world.step 后派发=已解锁，可安全销毁）；跨 World/已失效明确报错。
- **applyLinearImpulse(body, impulse, worldPoint?)**：游戏层单位 mass×px/step，内部经 pxPerStepToMps（线性）转 kg·m/s，无经验倍率；worldPoint px，缺省用 getWorldCenter（真实 COM）；偏心点自然产生角速度 τ=r×J；走原生 applyLinearImpulse（wake 默认）。
- **bullet/CCD**：PlanckBodyOptions 加 `bullet?: boolean`（assertBodyOptions 校验 boolean）；createDynamicBox/Circle/Polygon/Compound 传入 → native.setBullet(true) 直接映射；默认 false 不改既有行为。
- **重要实测发现**：Planck 1.4.3（Box2D 2.4）默认 **speculative contacts** 对单静态墙场景已能阻止隧穿（探测至 102400 px/step 均命中且停靠）——非 bullet 也会被挡。故 bullet 测试断言「bullet=true 命中不穿透（验收主项）」+「缺省/显式 false 行为一致（无回归）」，不写「非 bullet 必穿透」的错误对照。
- 测试：新文件 7/7；回归（planckWorldCore/planckFixedStep/planckContact/planckContactBatch/planckBodyTransform/planckColliderBody/planckRevoluteMotor/planckWeldJoint/planckVehicleRevolute/planckContactRouterBridge）10 文件 50/50；scoped tsc exit 0；diff --check exit 0。未 commit/push（git 写全禁）。

## Q02-F2（ContactRouter Projectile Route）— 已完成（未提交）
- 仅改 `src/battle/contactRouter.ts` + 新增 `tests/contactRouterProjectile.test.ts`。
- **Projectile 伤害路由**：projectile ↔ hostile vehicle 的 begin/start → 按 OwnerTag.team + partId 反查来源 weapon part → `behaviorParams.projectileDamage` → 复用 `WEAPON_CONTACT_THRESHOLD`(0.5) → 现有 DamageResolver，damageSource='weapon'；同队 0 伤害；同一 batch 同来源武器同目标只结算一次（projByKey 合并，key=team|partId|defTeam）；projectile 不参与 Impact（Impact 仅 vehicle↔vehicle）。
- **Contact fact**：`drainProjectileContactFacts()`（读取后清空）；仅记录真实 begin；`projectileBody`（opaque BodyHandle 引用，可区分实例）+ 另一方 Owner（vehicle/arena/ground/hazard）；本模块不 destroyBody。
- **关键修复**：`processBatch` 原 hostile 空过滤提前 return 会跳过 projectile-only 批次 → 把 projectile 合并段移到 hostile 过滤之前（vehicle 路径行为不变）。
- 无 cast（TeamId 用字面量收窄）。测试 5/5：hostile 只扣一次 60 / same-team 0 damage 但 fact 记录 / fact drain 清空不重复且 body 引用一致 / arena 只记 fact 无伤害 / 同 batch 两弹只结算一次。scoped tsc exit 0；diff --check exit 0。未 commit/push。

## Q02-F2R1（Projectile 实例级伤害去重）— 已完成（未提交）
- 仅改 `src/battle/contactRouter.ts` + `tests/contactRouterProjectile.test.ts`。
- **去重单位改为「projectile 实例 + defender」**：`RouterContactEvent` 新增可选 `bodyA/bodyB`（引擎中立 opaque 引用，Planck 透传 BodyHandle）；`projectileHitMeta` 返回 `projBody`；processBatch 用嵌套 `Map<projBody, Map<defTeam, entry>>` 合并——同一实例同 batch 多 contact pair 只结算一次；不同实例即使 team/partId/target 完全相同也各自结算。无 opaque 引用（Matter 路径，当前无 projectile meta）退化为旧语义。
- 测试更新：原「同 batch 两弹只结算一次」修订为**两弹各自结算（2 次伤害，hp=880）**；新增「同一复合 projectile（双 fixture）同 batch 双 begin 只结算一次（hp=940）」。6/6 通过；scoped tsc exit 0；diff --check exit 0。未 commit/push。

## Q02-C2（Cannon Content Definition）— 已完成（未提交）
- 仅改 `src/core/content.ts` + 新增 `tests/contentCannon.test.ts`。
- 新增 weapon `cannon`：category='weapon'、behavior='cannon'、mass=20、energy=30、collider box 40×20（offset x=20 向前伸出）；**无 baseDamage**（炮身接触不直伤）。
- behaviorParams 恰六参数（明显易验证）：cooldownMs=1000 / muzzleSpeed=12 / projectileDamage=80 / projectileRadius=6 / projectileMass=1 / recoilImpulse=12（= projectileMass×muzzleSpeed，动量匹配易验证）。
- 测试 4/4：registry 可取得（createRegistry 与单例引用一致）/ category+behavior+collider+mass+energy / 六参数精确相等且无多余 key / 无 baseDamage（与 ramHead 对照）。scoped tsc exit 0；diff --check exit 0。未 commit/push。下一步 Q02-C1（Cannon Behavior + projectile 生命周期）。

## Q02-C1A（Cannon Fire + Cooldown + Recoil）— 已完成（未提交）
- 新增 `src/battle/cannonBehavior.ts` + 改 `src/battle/planckBattleOrchestrator.ts`（正式 onBeforeStep 插入口调用）+ 新增 `tests/cannonBehavior.test.ts`。
- **CannonBehavior**：初始就绪首个固定步发射；冷却 = ceil(cooldownMs/FIXED_DT_MS - 1e-9) 固定步（60 步 = 1000ms）；炮口方向 = rotateLocal({x:facing,y:0}, partAngle)（part 世界姿态 + facing 镜像）；炮口外缘 = part 本地 facing*(offset.x+w/2)；projectile = dynamic circle（真实 radius/mass + bullet=true CCD）+ OwnerTag(kind='projectile', team, partId='part:<hardpoint>')；初速度 = part 线速度 + dir×muzzleSpeed；碰撞过滤 category=PROJECTILE、mask=GROUND|ARENA|HAZARD|敌车、group=射手负 group（不碰同队）；**recoil = applyLinearImpulse(part.body, -dir×recoilImpulse, 真实炮口点)**，由 Weld 传给整车。
- **Orchestrator**：constructor 为每 cannon part 建独立 CannonBehavior；step() 的 onBeforeStep 内（drive 之后）逐个调用；不新增生命周期。
- **测试 3/3**：①首发+60 步冷却+owner/mass(1)/radius(6)/bullet/velocity(12,0)+炮口位置+recoil 立即 Δv=-0.6（J/m）+步进后整车 -X 响应；②A/B facing 相反 → +X/-X 方向正确；③端到端 200 步 = 4 发（step 1/61/121/181，命中 ~6/66/126/186，间隔 ≈60）4×80，hpB=680。
- **坑（记录）**：①wedge 车头 78 + cannon 炮管（硬点 66 + 40 → 右缘 506）会超出车头，敌方 spawn 太近会出生即重叠 → 弹道留足间隙；②未销毁弹体命中后沿车壁下滑触到敌方轮子会产生第二次 begin→第二次伤害（销毁属后续队列）——端到端测试用无轮纯车体目标隔离；③200 步/60 冷却 = 4 发，不是 2 发（预期数学错误）。scoped tsc exit 0；diff --check exit 0。未 commit/push。

## Q02-C1B（Projectile Lifecycle）— 已完成（未提交）
- 仅改 `src/battle/cannonBehavior.ts` + `src/battle/planckBattleOrchestrator.ts` + `tests/cannonBehavior.test.ts`。
- **CannonBehavior**：`projectiles: Set<BodyHandle>` 追踪本实例创建的存活 projectile；`aliveProjectiles` getter；`consumeProjectileFacts(world, facts)`（任一真实 begin fact → destroyBody，同一实例同批次引用去重只销毁一次，销毁后移除；引用比较无 cast）；`destroyProjectile(world, handle)`（越界销毁，非本实例明确报错）。
- **Orchestrator step()**：world.step 返回后（World 未锁定）①只 drain 一次 `router.drainProjectileContactFacts()` → 各 behavior.consumeProjectileFacts；②存活 projectile 逐个 `arena.isOutOfProjectileBounds(position)` → 越界 destroyProjectile；未接触未越界保持存活。
- **测试 6/6**：C1A 三例（e2e 目标恢复 lightVehicle，销毁生效后每发恰 1 次伤害 4×80=680）+ C1B 三例：①hostile 命中只 1 次伤害且销毁（handle 失效、继续步进无异常）；②撞 arena 墙销毁无伤害；③打空持续飞行 → 越界（真实 PlanckArenaRuntime.isOutOfProjectileBounds y<-50）销毁、重复销毁明确报错。
- **关键坑（记录）**：测试地面若**不带 OwnerTag**，轮子↔地面 begin 被 Router 跳过 → batch index 不连续 → collectBatch 判定异常丢弃 → projectile 伤害永不结算（但 fact 仍记录可销毁）。正式 Arena 地面带 tag 所以 e2e 正常；单元测试 makeVehicle 地面已补 `setOwnerTag(ground,{kind:'ground'})`。scoped tsc exit 0；diff --check exit 0。未 commit/push。

## Q02-C3A（Projectile Render Snapshot）— 已完成（未提交）
- 仅改 `src/battle/battleContract.ts` + `src/battle/planckBattleOrchestrator.ts` + 新增 `tests/projectileRenderSnapshot.test.ts`。
- **合同**：新增 `RenderProjectile{center:RenderVec2, radius, team:TeamId}`；`BattleRenderSnapshot.projectiles?: RenderProjectile[]`（optional，Matter 不提供）。
- **Orchestrator**：`getRenderSnapshot()` 增加 `projectiles: buildProjectilesSnapshot()`——聚合所有 CannonBehavior.aliveProjectiles；center=真实 body 世界位置；radius=真实碰撞几何（getBounds 对 circle 不扣 skin → AABB 半宽=半径 6）；team=OwnerTag（无归属跳过）；已销毁的不在 aliveProjectiles → 自动不进入。
- 未改 CannonBehavior / Renderer / Matter Orchestrator；无 FX/UI/轨迹预测。
- **测试 3/3**：①发射后 1 个 projectile，team=A、radius≈6（真实几何）、center=炮口+12px 真实一步飞行（含重力 ~0.28 下落）；②命中销毁当步及之后 20 步 Snapshot 为空；③Matter Snapshot 无 projectiles（optional 语义）。scoped tsc exit 0；diff --check exit 0。未 commit/push。

## Q02-C3B（Renderer Projectile）— 已完成（未提交）
- 仅改 `src/render/renderer.ts` + 新增 `tests/rendererProjectile.test.ts`。
- Renderer 新增 `drawProjectiles(snap.projectiles ?? [])`（车辆绘制之后、FX 之前）；按真实 center/radius 画 circle；A/B 颜色 `PROJECTILE_COLOR_A='#7de8ff'` / `PROJECTILE_COLOR_B='#ffd05a'`（导出常量）；只消费 Snapshot，不读 CannonBehavior/PlanckWorld/BodyHandle；undefined/空数组零绘制，Matter 画面不变。
- 未改 Battle Contract / Orchestrator / Cannon；无拖尾/爆炸/发光/准星/轨迹预测；不改 camera/zoom。
- **测试 3/3**（canvas/ctx stub + 镜头变换 0.9 验证）：①真实坐标×镜头（500,600,r6 → 230,340,r5.4）双弹 A/B 双色、车辆 fill < projectile arc < FX fillText；②undefined 零弧、车辆仍绘制；③空数组零弧。scoped tsc exit 0；diff --check exit 0。未 commit/push。

## Q02-C4（Cannon Visual Scenarios）— 已完成（未提交）
- 仅改 `src/lab/scenarios.ts` + 新增 `tests/cannonScenarios.test.ts`。
- 新增 3 个场景（全部 `engine:'planck'`、`autoDrive:false`）：
  - **Cannon-Hit**：A=boxBody+wheelStd×2+front cannon（右缘 565），B=无攻击件 plainCar（左缘 625），60px 弹道 ~5 步命中；
  - **Cannon-Recoil**：B 远置（x=1500）不参与前几秒；A 连续开炮观察整车后座位移；
  - **Cannon-Angle**：前小(12)/后大(26)轮径 → 车头下倾 ~7.3°，同一 front cannon，弹道沿真实炮管方向无补偿。
- 新增 helper：`cannonCarBuild` / `tiltedCannonBuild` / `plainCarBuild`。
- **测试 4/4**：①3 场景 Lab 正式入口创建+120 步无异常、全部 `instanceof PlanckBattleOrchestrator`、Cannon front hardpoint/weapon/六参数、无出生重叠（含炮管）；②Cannon-Hit 120 步 2 发×80 → B hp=840；③Cannon-Recoil 首帧整车 vx<0（recoil 经 Weld 传整车）、B 无伤害 hp=1000、120 步净后座位移 x 减小；④Cannon-Angle partAngle>0.05、一步后弹道方向角≈partAngle（±0.03，含重力小量）、明显偏离水平。scoped tsc exit 0；diff --check exit 0。未 commit/push。

## Q02-M（Cannon Merge Gate）— PASS
- 12 个测试文件 63/63 全过（vmThreads/maxWorkers=1，Node v24.14.0，非并行）：planckWorldProjectileFoundation 7 / contactRouterProjectile 6 / contentCannon 4 / cannonBehavior 6 / projectileRenderSnapshot 3 / rendererProjectile 3 / cannonScenarios 4 / planckBattleOrchestrator 9 / physicsLabEngine 6 / battleContract 7 / foundationCanonical 5 / baselineDrive 3。
- scoped strict tsc（8 个 Q02 生产文件）exit 0；`git diff --check` exit 0。
- **Git ref 状态（重要）**：branch=`feature/foundation-02-canonical`，HEAD=`1c3c8fbe…`（B17B commit），HEAD^=`665a7f3b…`（锚点），index 空——**B17B commit 的 branch ref 已在本会话外被恢复**（B17B-SR 曾 update-ref 静默失败；现 HEAD 正常解析，battleOrchestrator/physicsLab/physicsLabEngine.test 均干净=已提交）。工作区 = B17B 已提交 + Q02 未提交。
- 范围干净：Q02 生产修改 7（battleContract/contactRouter/planckBattleOrchestrator/content/scenarios/planckWorld/renderer）+ 新 cannonBehavior.ts；Q02 新测试 7；`.workbuddy/**`、4 诊断测试、交接 MD 均既有未混入。未修任何代码。

## Git 环境诊断（2026-08-19 18:37-18:40，用户问如何自动化 commit/push）
- **用户已在 IDE 手动提交 Q02**：`d0411fb`「feat: add Planck cannon projectile runtime」，parent=`1c3c8fb`。工作区只剩既有非 Q02 项（.workbuddy/诊断测试/交接 MD）。→ 用户手动提交正是其痛点。
- **环境盘点**：git 2.55.0.windows.3（Git Bash 自带）；remote=HTTPS `github.com/HubinAI/fruits.git`；global `credential.helper=manager`（GCM，沙箱内二进制崩溃 exit 139）；无 `~/.git-credentials`、无 `~/.config/git`；`~/.ssh` 仅 known_hosts，**无密钥**。
- **根因 1（ref 写入）实证**：`git update-ref refs/heads/<顶级>` 沙箱内外均正常落盘；`git update-ref refs/heads/<子目录>/<名>`（嵌套）**一律静默失败**（exit 0 不写文件），即使预先 `mkdir -p` 父目录也一样——git 自身在 `.git/refs/heads/<子>/` 下 temp+rename 原子写被环境静默阻断（非沙箱问题）。**手工 `printf SHA > ref` 直写是唯一可靠路径**（A4R2 已证）。推论：D 盘可能为同步盘（OneDrive/坚果云等）或被杀软/受控文件夹拦截 rename。
- **根因 2（push 凭据）**：HTTPS+GCM 在沙箱不可用；无任何已存凭据/密钥 → push 必须人工（IDE/GCM 弹窗）。一次性解法：A) HTTPS+PAT 入 `credential.helper store`（明文 ~/.git-credentials，推荐 fine-grained 仅限本仓库）；B) SSH 无口令 ed25519 密钥 + remote 改 `git@github.com:HubinAI/fruits.git`。

## Git 环境配置执行（2026-08-19 18:46-18:53，用户选 SSH 方案 + D 盘本地普通盘）
- **根因 3（网络）新发现**：本网络 `api.github.com:443` 通（gh 正常）、`github.com:443` HTTPS **间歇不可达**（curl/git push 均 21s 超时）；但 `ssh.github.com:443` 与 `github.com:22` **当前可达**——中国网络下 github.com 主站被干扰的典型。SSH-443 是绕过路径。
- **ref 根因最终定位（决定性实验）**：同一嵌套目录（`refs/heads/__diag3/`）内，手工 `printf SHA > ref` **成功**；`git update-ref .../y` **exit 0 但失败且把整个父目录连手工文件一起删掉**（=记忆里「清掉 refs/heads 子目录」机制）。即本机 git 2.55.0 嵌套 ref 的 temp+rename 原子写被环境（疑杀软/受控文件夹钩子）静默拦截；顶级 ref 正常；**与沙箱无关**（沙箱外同样失败）。WorkBuddy 侧兜底：commit 后立即校验 `rev-parse HEAD` + 手工 loose-ref 写回（已固化）。建议：升级 Git for Windows 或加白名单；或分支改顶层名（无 `/`）绕开。
- **已配置（一次性）**：①`ssh-keygen -t ed25519 -N ""` → `~/.ssh/id_ed25519(.pub)`（无口令，注释 HubinAI-workbuddy-2026-08-19）；②`gh auth setup-git` → `credential.https://github.com.helper = !gh auth git-credential`（keyring token，repo 权限；非 GCM，沙箱不崩，HTTPS push 免交互）；③`~/.ssh/config`：github.com → ssh.github.com:443（绕开 github.com:443 封锁）；④ssh.github.com 已入 known_hosts。remote 暂保持 HTTPS（gh 凭据链）。
- **待用户一次性动作**：把 `~/.ssh/id_ed25519.pub` 内容贴到 `https://github.com/settings/ssh/new`（或跑 `gh auth refresh -h github.com -s admin:public_key` 后我自动加）。完成后：`git remote set-url origin git@github.com:HubinAI/fruits.git` + 实测 push 即全自动。
- 远端已确认与本地同步：`refs/heads/feature/foundation-02-canonical` = `d0411fb`（用户已手动提交+推送 Q02）。

## GIT-A（SSH + Git 自动化自检）— PASS，全链路自动化打通
- 用户已把 `~/.ssh/id_ed25519.pub`（fruit 命名）加到 GitHub（Read/write）。
- 验证链全部通过：`ssh -T git@github.com` = "Hi HubinAI! You've successfully authenticated"；`ls-remote git@github.com:...` OK；origin 已切 `git@github.com:HubinAI/fruits.git`（走 ~/.ssh/config 的 ssh.github.com:443）。
- **决定性结论（正式固化）**：在**顶层分支** `wb-git-test`（refs/heads/wb-git-test，无 `/`）上 `git commit --allow-empty` → **ref 自动落盘、HEAD 自动前进**（d0cb002e…），无需任何 printf/手工 loose-ref 修复；正式分支 `feature/foundation-02-canonical` ref 保持 d0411fb 未动。
- **即「嵌套 ref 写入 bug」只影响含 `/` 的分支名（feature/xxx）**；顶层分支完全正常。→ **自动化 commit/push 可行，前提是分支名不带 `/`**（或对 feature/ 分支继续用手工 loose-ref 兜底）。
- push `-u origin wb-git-test` 成功（SSH-443），远端 `refs/heads/wb-git-test` = 本地 HEAD d0cb002e ✓。
- 遗留：临时测试分支 `wb-git-test` 已推送远端（可随时删，未删=按要求停止）。后续正式分支若想根治嵌套 bug：改顶层名或升级 git/加白名单（见 GIT 诊断记录）。

## Q02-CAM-R1（Cannon 测试镜头稳定化）— 完成（未 commit/push，等真人看画面）
- **问题**：此前 render() 每帧按 vehicles+projectiles 自动 fit → 开炮/后坐/打空持续呼吸缩放，位移与姿态无法稳定观察。
- **renderer.ts**：删除 render() 每帧 `applyContentFit`；改为公开 `reframe(snap, fit: CameraFit, forwardExtent)`——只在 load/Reset/resize 构图一次并固定；**Projectile 永不参与 camera bounds**；fit='vehicles'(A+B)/'primary'(只 A)/'primary-forward'(A+前方 +X forwardExtent=500px 默认)。`CameraFit` 类型导出。resize() 契约不变（rendererProjectile 测试仍按 resize transform 断言，3/3 过）。
- **scenarios.ts**：`ScenarioDef.camera?: ScenarioCamera`（显示层提示，不改物理/spawn）：Cannon-Hit `{fit:'vehicles'}`、Cannon-Recoil `{fit:'primary'}`（远 B 不缩小画面）、Cannon-Angle `{fit:'primary-forward', forwardExtent:520}`。
- **main.ts**：`currentCamera` 跟踪 + `reframeCamera()`；触发点=场景 select / Reset / Clear / Override 触发 reset / viewport resize / 自定义 Build 加载（后者取默认 vehicles）；`arenaDims()` 用 instanceof 收窄取 `o.arena.config.width/height`（修掉了旧的 `lab.orchestrator?.arena` 类型隐患）。
- **顺手修复 2 个潜在类型问题**（vite 不检查类型此前未暴露）：renderer.ts 缺 `BattleRenderSnapshot` 导入；main.ts `CameraFit` 导入未使用（ScenarioCamera 已含）。
- 验证：rendererProjectile 3/3；scoped tsc（renderer/scenarios/main/测试）exit 0；dev server HTTP 200。未 commit/push（按队列等真人验收画面）。

## Q02-CAM-R2（固定镜头安全区修正）— 完成（未 commit/push，等真人看画面）
- **问题**：CAM-R1 已解决运行中动态缩放，但 Recoil/Angle 连续开炮后 A 进入左侧 UI 区域（构图按全 canvas 且 CONTENT_ZOOM=1.2 放大溢出，A 无身后空间）；Cannon-Hit 后段贴近两侧 UI。
- **renderer.ts**：①新增构图安全区 `SAFE_INSET_X=56` / `SAFE_INSET_Y=28`（左右 UI 阴影区不计入可用画布，内容居中于安全区中心，offset 含内缩量）；②`CONTENT_ZOOM` 1.2→1.05、`CONTENT_MARGIN_WORLD` 60→64（内容完整入安全区，溢出被世界边距吸收，主体不裁切）；③**合并 Cannon 专属 mode**：删 `primary`/`primary-forward`，统一为 `primary-fire`（A 偏左中部 + 身后 recoilExtent 默认 180px + 前方 forwardExtent 默认 520px）——Recoil/Angle 共用一套，不再有第三种 mode；`reframe(snap, fit, opts:{forwardExtent?,recoilExtent?})`。
- **scenarios.ts**：`ScenarioCamera` 增 `recoilExtent?`；Cannon-Recoil 与 Cannon-Angle 均 `{fit:'primary-fire', recoilExtent:180, forwardExtent:520}`；Cannon-Hit 保持 `{fit:'vehicles'}`。
- **main.ts**：`reframeCamera()` 传 `{forwardExtent, recoilExtent}` 两字段。
- 数值推演：cw=1500 时 Recoil scale≈1.53，A 左缘 sx≈393px（距安全区左缘 337px），recoil 后移 ~24-100px 仍远离左缘；Hit 下 A≈203px / B≈1296px，均在画布内远离两侧。
- 验证：scoped tsc exit 0；rendererProjectile 3/3；diff --check exit 0；dev server HTTP 200。未 commit/push（等真人验收画面）。

## Q02-CAM-S（固定镜头验收通过并提交）— 完成
- 真人画面验收通过 → 提交 + push：`fix: stabilize cannon lab camera framing`，HEAD `9d9a072`（parent=d0411fb），恰含 renderer.ts/scenarios.ts/main.ts 3 文件；push `d0411fb..9d9a072`，远端 = 本地 ✓。tracking ref 又被环境吞 → 手工补 `refs/remotes/origin/foundation-02-canonical`（项目惯例）。

## LAB-DEBUG-UX（Debug 一键关闭 + 默认关闭）— 完成（已提交 + push）
- **根因**：`DEFAULT_DEBUG_FLAGS` 有 8 项默认为 true（com/movementHardpoint/functionalHardpoint/groundedWheel/totalMass/inertia/lastImpact/lastDamage）→ main.ts checkbox 用 lab.debugFlags 初始化 → 刷新即恢复勾选，录屏前要逐项手动取消。
- **debugOverlay.ts**：`DEFAULT_DEBUG_FLAGS` 全部 14 项改为 false（Debug 默认全关；各功能绘制逻辑不变；physicsLabEngine 测试用自定义 ALL_DEBUG_FLAGS 覆盖，不受影响）。
- **main.ts**：Debug 面板标题区新增「全部关闭」按钮（红底样式）——遍历 checkboxes Map 一次清空 lab.debugFlags + checkbox UI，立即更新画面；不提供"全部开启"；Override 数值不动。
- **新增测试** `tests/labDebugDefaults.test.ts` 4/4：DEFAULT_DEBUG_FLAGS 14 项全 false / new PhysicsLab().debugFlags 全 false / overrides===DEFAULT_OVERRIDES（Override 不受影响）/ 一键关闭语义无残留。
- 验证：新测试 4/4 + physicsLabEngine 回归 6/6 + scoped tsc exit 0 + diff --check exit 0；dev server HTTP 200。
- Commit：`fix: debug overlay default off + one-click clear` → HEAD `88d9f71`（parent=9d9a072），恰含 debugOverlay.ts/main.ts/labDebugDefaults.test.ts 3 文件；push `9d9a072..88d9f71`，远端 = 本地 ✓；tracking ref 补实。

## Q02-EXP-R1（Cannon 可感知性放大）— 完成（已提交 + push）
- **参数放大（content.ts，录屏体验方向验证，非平衡）**：projectileRadius 6→10、muzzleSpeed 12→8、recoilImpulse 12→30；cooldownMs 1000 / projectileDamage 80 / projectileMass 1 不变。
- **Cannon-Hit（scenarios.ts）**：spawnB 700→750（弹道 ~90-110px，首发 ~13 步 ≈0.22s 可观察飞行）。
- **关键实测发现（物理约束，已固化）**：muzzleSpeed 8 + Planck 重力（一步下落 ~0.28px，n 步下落 ≈0.28·n²）下，**弹道距离有命中上限 ~100px**——spawnB 780+ 首发打空（重力落地）、850 完全 0 伤害；e2e（wedge A）recoil 30 把 A 持续推离，spawnB 640 只剩 2 发命中、580 恢复 4 发全中（step 1/61/121/181）。扫描表：E2E 580→4发/600→3发/620→3发/640→2发；HIT 700→2发(7,70)/720→2发(9,73)/750→2发(13,77)/780→1发/810,850→0发。
- **测试更新**：contentCannon（新数值）、cannonBehavior（radius 10/vel 8/recoil -1.5/spawnB 580/容差 ±12 与 <15）、projectileRenderSnapshot（radius 10/一步 +8px）；cannonScenarios **无需改动**（断言与参数值无关，天然通过）。
- 验证：4 cannon 测试 17/17 + 回归（contactRouterProjectile 6/rendererProjectile 3/physicsLabEngine 6）15/15 + scoped tsc exit 0 + diff --check exit 0。
- Commit：`feat: amplify cannon perceptibility (radius/speed/recoil)` → HEAD `45d951c`（parent=88d9f71），恰含 content.ts/scenarios.ts/3 测试文件；push `88d9f71..45d951c`，远端 = 本地 ✓；tracking ref 补实。

## Q03-HAMMER-R0（只读定位）— 完成
- 结论：Hammer 缺口 = PlanckWorld 缺 2 项 Revolute 能力（angle limit + joint angle 读取）；motor/装配/插入口/Damage 路径/单 collider 摆锤几何均已有；推荐 Q03-F1（planckWorld 1+1）→ F2（content+assembly 2+1）→ C1（hammerBehavior+orchestrator 2+1）→ C2（scenarios 1+1，延后）。

## Q03-F1（Revolute Angle + Limit Foundation）— 完成（已提交 + push）
- **planckWorld.ts** 新增 2 方法：
  - `getRevoluteAngle(joint)`：读原生 `RevoluteJoint.getJointAngle()`（rad，bodyB 相对 bodyA 扣 referenceAngle），rad 直通无换算；非 Revolute/跨 world/失效 handle 明确报错（与 setRevoluteMotor 同语义）。
  - `setRevoluteLimit(joint, {enabled, lowerRad, upperRad})`：映射原生 `setLimits`+`enableLimit`；enabled 必须 boolean、finite 校验、lower<=upper；**先 setLimits 再 enableLimit**（enable 立即应用当前值）；motor 语义不变。
- 原生签名确认：`RevoluteJoint.getJointAngle()` / `enableLimit(flag)` / `setLimits(lower,upper)` 均为 rad。
- **实测限位余量**：motor 驱动撞 limit 停在 upper+~0.027 rad（原生硬约束求解余量，1.5°级），测试容差按 <0.06 rad 设计。
- 测试 `planckWorldRevoluteLimit.test.ts` 5/5：motor 单调增长 / limit 挡停（60→120 步不增长）/ disable 后继续转超 upper / 跨 world+Weld handle 报错（正常 handle 不受误伤）/ lower>upper、NaN、Infinity、enabled 非 boolean 报错。
- 回归 5 文件 25/25（planckRevoluteMotor/planckVehicleRevolute/planckWeldJoint/planckWorldCore/planckWorldProjectileFoundation）；scoped tsc exit 0；diff --check exit 0。
- Commit：`feat: revolute joint angle read + limit (hammer foundation)` → HEAD `879d33d`（parent=45d951c），恰含 2 文件；push `45d951c..879d33d`，远端 = 本地 ✓；tracking ref 补实。下一步 Q03-F2（Hammer 装配）。

## Q03-F2（Hammer Content + Revolute Mount）— 完成（已提交 + push）
- **content.ts**：新增 `hammer`（weapon、behavior:'hammer'、baseDamage:90、mass:40、energy:25、长矩形 collider 60×14 且 offset.x=40 → pivot(body 原点)在矩形一端附近、质心离 pivot 40px 的真实摆锤，单 collider 不扩 compound）；注册进 registry。
- **planckVehicleAssembly.ts**：part 连接改为按 behavior 分支——`behavior==='hammer'` → `createRevoluteJoint`（pivot=功能挂点，与 Weld 同锚点语义），其余（ram/cannon/gadget）保持 `createWeldJoint`；facing/hardpoint/OwnerTag/collision filter 全部不变。
- 测试 `hammerAssembly.test.ts` 4/4：①content 定义（weapon/behavior/baseDamage/质量>20/长矩形/offset.x>20 远端质心）；②Revolute 装配（pivot 与功能挂点重合——用**贴地后真实 chassis 位置+硬点本地坐标**验证，settle 会改变车体位置；getRevoluteAngle 可用；setAngularVelocity 驱动自由旋转 >0.5 rad、pivot 不动 <5px）；③同车无卡死（大角速度扫 120 步无异常、chassis 位移 <20px）；④Cannon/Ram 仍 Weld（getRevoluteAngle 抛错）。
- **坑（记录）**：settlePlanckVehicleToRestPose 贴地后车体位置变化，pivot 验证必须用实时 chassis 位置 + hardpoint 相对坐标，不能用 spawn 绝对坐标（初版断言差 28px 失败）。
- 回归 5 文件 29/29（planckVehicleSettle/planckVehicleRevolute/planckBattleOrchestrator/cannonBehavior/contentCannon）；scoped tsc exit 0；diff --check exit 0。
- Commit：`feat: hammer revolute mount + content` → HEAD `5184340`（parent=879d33d），恰含 3 文件；push `879d33d..5184340`，远端 = 本地 ✓；tracking ref 补实。下一步 Q03-C1（HammerBehavior 状态机）。

## Q03-C1（Hammer Wind-up → Swing → Recover）— 完成（已提交 + push）
- **hammerBehavior.ts（新）**：状态机 `windup → windup-pause → swing → recover` 循环；运动完全来自 `setRevoluteMotor`（windup 向后 0.05 / swing 向前 0.18 / recover 向后 0.04 rad·step⁻¹）+ `setRevoluteLimit`（lower -0.9 / upper 1.2 rad）+ `getRevoluteAngle` 相位判定；windup 到位停 20 步；不追踪（固定弧/固定周期，stepFixed 不读敌方）；禁止 setAngle/teleport/直接扣血；伤害复用 ContactRouter baseDamage（hammer part=weapon 直击）。参数从 behaviorParams 同名 key 覆盖、缺省用 `HAMMER_DEFAULT_PARAMS`。
- **关键物理坑（已固化）**：锤头 40kg 质心离 pivot 0.4m → 重力矩 ≈ 160 N·m，`maxTorqueNm=60` 时 swing 逆重力到不了前限位（角度停在平衡点）、反作用也弱。**默认扭矩 60→400**（首版明显夸张）后：swing 有力挥满弧、反作用可见。另：boxBody chassis 惯量大 → motor 扭矩反作用角速度天然小（实测峰值 ~0.017 rad/s ≈ 1°/s，真实非零），反作用断言阈值 0.01。
- **orchestrator**：`hammers` 数组（仿 cannons）+ constructor 收集 + 同一 onBeforeStep 插入口循环。
- 测试 `hammerBehavior.test.ts` 4/4：①状态机循环（四相位都出现、angle 覆盖 lower/upper ±0.06、多次 swing 起点间隔稳定 ±6 步=固定周期不追踪）；②命中（目标在弧内 spawnB 560 → weapon 伤害 90）；③打空（spawnB 900 弧外 → 0 伤害 hp=1000）；④反作用（chassis ω 峰值 >0.01，motor 真实扭矩反作用）。
- 回归 7 文件 38/38（含 hammerAssembly/planckWorldRevoluteLimit/planckBattleOrchestrator/cannonBehavior/physicsLabEngine/cannonScenarios）；scoped tsc exit 0；diff --check exit 0。（测试文件 tsc 曾因误用 require 报错——已改顶层 import 并清理未用变量。）
- Commit：`feat: hammer windup-swing-recover state machine` → HEAD `a4e4971`（parent=5184340），恰含 3 文件；push `5184340..a4e4971`，远端 = 本地 ✓；tracking ref 补实。

## Q03-C2（Hammer Visual Scenarios）— 完成（已提交 + push）
- **scenarios.ts**：新增 3 个 Planck 场景（全部 engine:'planck'、autoDrive:false、现有固定 Camera）：
  - **Hammer-Hit**：A=hammerCarBuild（boxBody+front hammer），B=plainCarBuild；spawnA 450 / **spawnB 600**（B 左缘 525 = A 车身右缘相切，chassis 无重叠；锤头初始伸入 B 是固定弧挥击武器自然几何——windup 抬起→swing 落下命中）；camera vehicles。
  - **Hammer-Miss**：spawnB 900（B 左缘 825 远在锤头最远 ~595 之外）→ 正常循环但真实打空；camera primary-fire。
  - **Hammer-Reaction**：A=hammerLightBuild（wedgeBody 窄体惯量小→反作用明显），B=1500 远置无接触；camera primary-fire。
- **关键实测（已固化）**：Hammer 出生重叠对带轮 B 会把 B **推滚出挥击弧**（spawnB 620+ 全部打空，B 滚到 680+）；spawnB 560/580/600 命中且**首次伤害在 ~106 步 = swing 阶段**（非出生接触，wc=2 两个 swing 循环）。spawnB 600 最优（B 被 A 车身支撑稳定不动）。
- 测试 `hammerScenarios.test.ts` 4/4：①3 场景创建+step 无异常+Planck+Hammer front/weapon/baseDamage 90+**状态循环正常**（300 步锤头角摆荡覆盖 lower~upper）；②Hit 真实接触掉血 90/hp<1000；③Miss 0 伤害 hp=1000；④Reaction 前 60 步 B 无接触 hp=1000、全程 chassis ω 峰值 >0.02（真实反作用）、B 始终 0 伤害。
- Q03 全部 targeted 4 文件 **17/17**（planckWorldRevoluteLimit 5/hammerAssembly 4/hammerBehavior 4/hammerScenarios 4）；scoped tsc exit 0；diff --check exit 0。
- Commit：`feat: hammer visual scenarios (hit/miss/reaction)` → HEAD `8a111f5`（parent=a4e4971），恰含 2 文件；push `a4e4971..8a111f5`，远端 = 本地 ✓；tracking ref 补实。

## Q03-C1R1（Hammer Revolute Limit 真正接入 Runtime）— 完成（已提交 + push）
- **问题**：Q03-C1 只靠 motor 按 angle 阈值反向，从未调用 setRevoluteLimit——限位不是真实物理边界。
- **hammerBehavior.ts**：加 `limitApplied` 标志，`stepFixed` **首次运行**时对 part.joint 调 `setRevoluteLimit({enabled:true, lowerRad, upperRad})`（limit 只做物理边界，状态机仍用 getRevoluteAngle+setRevoluteMotor 切换相位）。
- **新增测试（hammerBehavior.test.ts +2）**：
  - 高 motor speed 1.0 rad/step + 1000 N·m 驱动 600 步 → angle 始终 ≤ upper+solverTolerance（**实测穿透上限 ~0.103 rad ≈ 5.9°**，speed≥0.4 后饱和、与 speed 无关；speed 0.2 时仅 ~0.05）。对照：无 limit 会转 ~95 圈，现被挡在 1.30 → 物理弧真实生效。solverTolerance=0.15。
  - 状态机全程 500 步 → angle 不越弧（tolerance 0.08 通过）。
- **反作用物理变化（已固化）**：limit 接入后 swing 撞 upper 时力矩被硬约束吸收 → chassis 反作用峰值略降（Hammer-Reaction 实测 0.0156，hammerScenarios 阈值 0.02→0.01，与 hammerBehavior 反作用测试一致）；hammerBehavior 反作用测试 0.01 仍过。
- 回归 8 文件 **44/44**（hammerBehavior 6/hammerScenarios 4/hammerAssembly 4/planckWorldRevoluteLimit 5/cannonBehavior 6/cannonScenarios 4/planckBattleOrchestrator 9/physicsLabEngine 6）；scoped tsc exit 0；diff --check exit 0。
- Commit：`fix: hammer limit as real revolute constraint` → HEAD `2ef3f3d`（parent=8a111f5），恰含 3 文件；push `8a111f5..2ef3f3d`，远端 = 本地 ✓；tracking ref 补实。
















## Q03-C2R1（Hammer Scenario 固定弧回归）— 完成（已提交 + push）
- **只改测试**：`tests/hammerScenarios.test.ts`（scenarios.ts 未动——场景在真实 limit 下依然成立）。
- **增强断言**：①测试 1 三场景全程 300 步 angle 保持在 [lowerRad-0.15, upperRad+0.15]（真实物理 limit 约束，tolerance 取 C1R1 高压力穿透上限 ~0.103 含余量），同时仍到达两端（循环成立）；②测试 2 Hit 记录首次 weapon 伤害 step，断言 ≥30（windup 18+停顿 20 ≈38 步才进首个 Swing；<30 步只可能是初始重叠）→ 证明真实 Swing 接触而非出生接触。
- Miss（0 伤害）/ Reaction（chassis 反作用 >0.01）语义不变。
- Q03 targeted 4 文件 **19/19**（RevoluteLimit 5/assembly 4/behavior 6/scenarios 4）；scoped tsc exit 0；diff --check exit 0。
- Commit：`test: hammer scenario fixed-arc regression` → HEAD `550c965`（parent=2ef3f3d），恰含 1 文件；push `2ef3f3d..550c965`，远端 = 本地 ✓；tracking ref 补实。
## Q04-F1（Prismatic Joint Foundation）— 完成（已提交 + push）
- **planckWorld.ts** 新增 4 方法（仿 Q03-F1 Revolute 模式）+ `prismaticJoints` map：
  - `createPrismaticJoint(bodyA, anchorA, bodyB, anchorB, localAxisA)`：axis bodyA 本地坐标、finite+非零校验、内部规范化（Vec2 单位向量）、opaque handle；Prismatic 约束 1 平移 + 锁旋转（Push Rod 伸缩杆语义）。
  - `getPrismaticTranslation(joint)`：原生 m→px（mToPx）；非 Prismatic/跨 world 明确报错。
  - `setPrismaticMotor({enabled, speedPxPerStep, maxForceN})`：speed 用 pxPerStepToMps（同 setLinearVelocity 语义）、force 原值 N；force>=0/enabled boolean 校验。
  - `setPrismaticLimit({enabled, lowerPx, upperPx})`：px→m（pxToM）、lower<=upper；先 setLimits 再 enableLimit。
- **关键语义实测（已固化）**：Planck `getJointTranslation()` = **沿 localAxisA 的带符号位移**（dot(axis, displacement)）——axis=(-1,0) 且 bodyB 沿 -X 移 53.7 时 translation=+53.8（正值恒=沿 axis 正向，与 axis 世界方向无关）；motor speed 正=沿 axis 方向驱动 bodyB。测试初版预期写反，已修正为「bodyB 世界位置验证 axis 方向 + translation 恒为沿 axis 位移」。
- 测试 `planckWorldPrismatic.test.ts` 6/6：+speed 沿 +axis / -speed 沿 -axis / limit 挡 upper+lower（±1.5px 余量）/ disable 后超 upper / axis ±X 用 bodyB 位置验证 / zero axis+invalid range+wrong joint(Weld)+cross-world 报错（正常 handle 不受误伤）。
- 回归 6 文件 30/30（RevoluteLimit/RevoluteMotor/WeldJoint/VehicleRevolute/WorldCore/ProjectileFoundation）；scoped tsc exit 0；diff --check exit 0。
- Commit：`feat: prismatic joint foundation (push rod)` → HEAD `131d52b`（parent=550c965），恰含 2 文件；push `550c965..131d52b`，远端 = 本地 ✓；tracking ref 补实。下一步 Q04-F2（Push Rod Content + Assembly）。
## Q04-F2（Push Rod Content + Prismatic Mount）— 完成（已提交 + push）
- **content.ts**：新增 `pushRod`（gadget、behavior:'pushRod'、**不设 baseDamage**（Gadget 天然绕过 ContactRouter weapon 路径）、mass:15、energy:20、单长矩形 collider 80×12 且 offset.x=40 → 覆盖 pivot..pivot+80 向车辆前方延伸，不扩 compound）。
- **planckVehicleAssembly.ts**：part 连接三分支——`hammer→Revolute` / **`pushRod→createPrismaticJoint`（axis={x:facing,y:0}，chassis 本地 ±X 跟随姿态，非固定世界 X）** / 其余（ram/cannon/gadget）→Weld。OwnerTag/collision filter/facing 规则不变。
- 测试 `pushRodAssembly.test.ts` 6/6：①content 定义（gadget/pushRod/无 baseDamage/长矩形/形状起点=pivot）；②Prismatic 装配 translation=0 时 pivot 与挂点重合；③facing=+1 沿 +X 伸缩（x 前移、y 不变 ±2px、**锁旋转 angle 不变**、translation>30）；④facing=-1 沿 -X 伸缩（axis 镜像前方）；⑤同车不互卡（3 循环伸/缩 240 步无异常、车体位移<20px）；⑥Hammer→Revolute（getRevoluteAngle 可用/getPrismaticTranslation 报错）、Cannon/Ram→Weld（两种 joint API 都报错）。
- 小调整：y 断言初版 toBeCloseTo(0.005) 过严（车体重力沉降/微动 0.59px），改 ±2px 容差。
- 回归 8 文件 46/46（hammerAssembly/hammerBehavior/hammerScenarios/cannonBehavior/cannonScenarios/planckBattleOrchestrator/planckWorldPrismatic/planckVehicleSettle）；scoped tsc exit 0；diff --check exit 0。
- Commit：`feat: push rod content + prismatic mount` → HEAD `cde39b7`（parent=131d52b），恰含 3 文件；push `131d52b..cde39b7`，远端 = 本地 ✓；tracking ref 补实。下一步 Q04-C1（PushRodBehavior Extend→Hold→Retract）。
## Q04-C1（Push Rod Extend → Hold → Retract）— 完成（已提交 + push）
- **pushRodBehavior.ts（新）**：状态机 `extend → hold → retract` 循环；首次运行 `setPrismaticLimit({enabled, lowerPx:0, upperPx:extendPx})`（真实物理边界）；运动完全来自 `setPrismaticMotor`（extend +2 / retract -2 px·step⁻¹，maxForceN 500）+ `getPrismaticTranslation` 相位判定；hold 停 20 步；**固定行程/固定周期/不读敌方**（stepFixed 无敌方输入），同一套 maxForce/speed 用于所有目标；参数从 behaviorParams 同名 key 覆盖、缺省 `PUSH_ROD_DEFAULT_PARAMS`（extendPx 90 / holdSteps 20 / speed 2 / force 500）。
- **orchestrator**：`pushRods` 数组 + constructor 收集 + 同一 onBeforeStep 插入口循环。
- **关键物理确认（已固化）**：Push Rod 顶 B 是 part(gadget)↔vehicle 接触 → ContactRouter weapon 路径天然跳过（weaponCount=0 ✓）；A/B chassis 接触产生**通用 Impact 自然存在**（B hp 掉 0.23 属预期，不为 Push Rod 特判关闭——测试断言 hp>990 而非 ===1000）。
- 测试 `pushRodBehavior.test.ts` 4/4：①状态机循环（三相位+循环回 extend、translation 不越 [0,extendPx]±1.5 且到远端/回零）；②轻目标（boxBody mass50）被真实碰撞推离 >30px、无 weapon damage；③**轻目标位移 > 重目标**（heavyBox mass150，同一套参数，无按质量补偿）；④推目标时 A 自身反作用（|aShift|>1）。
- 回归 7 文件 43/43（pushRodAssembly/planckWorldPrismatic/hammerBehavior/hammerScenarios/cannonBehavior/planckBattleOrchestrator/physicsLabEngine）；scoped tsc exit 0；diff --check exit 0。
- Commit：`feat: push rod extend-hold-retract behavior` → HEAD `eb53654`（parent=cde39b7），恰含 3 文件；push `cde39b7..eb53654`，远端 = 本地 ✓；tracking ref 补实。下一步 Q04-C2（Push Rod 场景）。
## Q04-C2（Push Rod Visual Scenarios）— 完成（已提交 + push）
- **scenarios.ts**：新增 3 个 Planck 场景（engine:'planck'、autoDrive:false、现有固定 Camera）：
  - **Push-Light**：A=pushRodCarBuild（boxBody+front pushRod），B=plainCarBuild（boxBody 轻车）；spawnA 450/spawnB 700（杆初始 525..605 < B 左缘 625 无重叠；extend 90 → 前端 695 顶推 B）；camera vehicles。
  - **Push-Heavy**：与 Light **完全相同**（A/距离/参数），仅 B=heavyTargetBuild（heavyBox baseMass 150）；camera vehicles。
  - **Push-Reaction**：A=pushRodLightBuild（wedgeBody 轻 chassis），B=heavyTargetBuild（重目标）；camera primary-fire。
- 测试 `pushRodScenarios.test.ts` 4/4（一次通过）：①3 场景创建+step+Planck+Push Rod front/gadget+**Extend/Hold/Retract 循环正常**（translation 到 extendPx-5 且回零 <5）；②Push-Light 轻目标被推离 >30px、weaponCount=0、hp>990（自然 Impact 允许）；③**Light 位移 > Heavy 且差 >15**（同 A/距离/参数，无按质量补偿）；④Push-Reaction 较轻 A 推重目标 → **aShift < -1（A 被真实 joint+collision 反推）**、B 被推、无 weapon damage。
- Q04 全部 targeted 4 文件 **20/20**（planckWorldPrismatic 6/pushRodAssembly 6/pushRodBehavior 4/pushRodScenarios 4）；scoped tsc exit 0；diff --check exit 0。
- Commit：`feat: push rod visual scenarios (light/heavy/reaction)` → HEAD `b012bca`（parent=eb53654），恰含 2 文件；push `eb53654..b012bca`，远端 = 本地 ✓；tracking ref 补实。
## Q05-F1（Lift Roller Content + Revolute Mount）— 完成（已提交 + push）
- **content.ts**：新增 `liftRoller`（gadget、behavior:'liftRoller'、**无 baseDamage**、mass:25、energy:25、单 **circle collider radius:24** 且 offset (0,0) → **pivot=circle center=part 原点=挂点**；不扩 schema）。
- **planckVehicleAssembly.ts**：Revolute 分支合并——`behavior==='hammer' || behavior==='liftRoller'` → createRevoluteJoint；pushRod → Prismatic；其余 → Weld。**Lift Roller 不设 Revolute limit**（连续旋转）。
- 测试 `liftRollerAssembly.test.ts` 4/4（一次通过）：①content 定义（gadget/liftRoller/无 baseDamage/circle/offset 0/radius>15）；②Revolute 装配 pivot 与挂点重合 + **连续旋转验证**（motor 驱动累计角位移 >2π 一整圈以上，无 limit 阻挡）；③同车不互卡（高速 0.3 rad/step 转 150 步无异常、车体位移<20px）；④装配回归（hammer→Revolute、pushRod→Prismatic、cannon/ram→Weld）。
- 回归 9 文件 48/48（hammerAssembly/pushRodAssembly/hammerBehavior/hammerScenarios/pushRodBehavior/pushRodScenarios/planckBattleOrchestrator/planckWorldRevoluteLimit/planckWorldPrismatic）；scoped tsc exit 0；diff --check exit 0。
- Commit：`feat: lift roller content + revolute mount` → HEAD `5f9758d`（parent=b012bca），恰含 3 文件；push `b012bca..5f9758d`，远端 = 本地 ✓；tracking ref 补实。下一步 Q05-C1（LiftRollerBehavior continuous motor + 顶起验证）。
## Q05-C1（Lift Roller Continuous Motor）— 完成（已提交 + push）
- **liftRollerBehavior.ts（新）**：极简 continuous motor——每固定步 `setRevoluteMotor({enabled:true, speedRadPerStep, maxTorqueNm})`（重复设置幂等）；**无状态机、无 limit、不读敌方**；参数从 behaviorParams 同名 key 覆盖、缺省 `LIFT_ROLLER_DEFAULT_PARAMS`（speed 0.25 rad/step、torque 300 N·m，首版夸张、同一套用于所有目标）。
- **orchestrator**：`liftRollers` 数组 + 收集 + 同一 onBeforeStep 插入口。（注意：import 语句首版 Edit 因未读文件失败漏加，补上后才可运行——已有流程教训：多文件 Edit 前先 Read 目标文件。）
- **关键实测（已固化）**：roller（pivot 在 A 前方 75px、radius 24、中心 y≈653）比 B 轮底（700）低 47px，无法在正下方顶起——**从侧面顶 B 轮/车身**实现姿态改变。spawnB 扫描：**bx=560 最优**（B.yShift 14.6px、angShift 0.318 rad、轮 grounded 出现 false、A 反推 -218px、wc=0 hp=1000）；580-620 微弱（grounded 仍 false）；640+ 无效果。
- 测试 `liftRollerBehavior.test.ts` 4/4（一次通过）：①continuous 旋转（300 步 angle 带符号累计 >2π，单向不来回摆）；②接触轻目标真实改变 y/angle/grounded 至少一个 + **grounded 出现 false** + wc=0 + A 反作用 |aShift|>1；③**轻目标（boxBody）响应 > 重目标（heavyBox）**（同一套 speed/torque，behavior.speedRadPerStep 相同验证无按质量补偿）；④**grounded 翻转端到端**：初始全 true → 被顶 false → 落地恢复全 true（真实 wheel↔ground；drivePlanckVehicle motor 门控 `enabled=cmd.enabled && w.grounded` 由既有代码+测试覆盖）。
- 回归 10 文件 52/52（liftRollerAssembly/hammerBehavior/hammerScenarios/pushRodBehavior/pushRodScenarios/cannonBehavior/cannonScenarios/planckBattleOrchestrator/physicsLabEngine/planckWorldRevoluteLimit）；scoped tsc exit 0；diff --check exit 0。
- Commit：`feat: lift roller continuous motor behavior` → HEAD `9d3dd55`（parent=5f9758d），恰含 3 文件；push `5f9758d..9d3dd55`，远端 = 本地 ✓；tracking ref 补实。下一步 Q05-C2（Lift Roller 场景）。
## Q05-V1（Circle Rotation 可感知）— 完成（已提交 + push）
- **问题**：Renderer 的 RenderShape circle 只画纯圆，虽然 snapshot 已有真实 circle.angle，肉眼看不出圆形 Roller 是否在转。
- **renderer.ts**：`drawShape` circle 分支在 fill/stroke 之后追加一条**径向方向线**——center → center + `cos/sin(circle.angle) × radius × 0.8`；方向完全用 RenderCircle.angle（真实物理角度，随旋转变化）；不新增 gameplay 状态、不伪造旋转。所有 circle RenderShape 统一生效（静态 circle angle 恒定 0 → 线固定水平，无害）；Wheel（drawWheel）/ Projectile（drawProjectiles）不受影响。
- 测试 `rendererCircleRotation.test.ts` 3/3：①angle=0（水平 +X）vs angle=π/2（竖直 +Y）方向明显不同（dot < 0.1）；②径向线长度 = radius×0.8×镜头 scale（0.9）；③circle 弧 center/radius 与 angle 无关（几何不变）。
- **坑（记录）**：ctx.lines[0] 是 render() 的 ground 地面线（moveTo/lineTo 记录），车辆径向线是 lines[1]/[2]——初版取 lines[0]/[1] 导致方向/长度断言失败（1440 是地面线长）。
- 回归 4 文件 15/15（rendererCircleRotation/rendererProjectile/projectileRenderSnapshot/physicsLabEngine）；scoped tsc exit 0；diff --check exit 0。
- Commit：`feat: render circle rotation indicator` → HEAD `254e4ac`（parent=9d3dd55），恰含 2 文件；push `9d3dd55..254e4ac`，远端 = 本地 ✓；tracking ref 补实。
## Q05-C2（Lift Roller Visual Scenarios）— 完成（已提交 + push）
- **scenarios.ts**：新增 3 个 Planck 场景（engine:'planck'、autoDrive:false、现有固定 Camera、Debug 默认关、无补偿）：
  - **Lift-Light**：A=liftRollerCarBuild（boxBody+front liftRoller），B=plainCarBuild；spawnB 560（Q05-C1 实测 yShift 14.4/angShift 0.314 最优）；camera vehicles。
  - **Lift-Posture**：同 build，**spawnB 550**（实测扫描 520-560：550 俯仰最明显 angShift 0.334/yShift 15.4）；重点「接触位置→姿态结果」；camera vehicles。
  - **Lift-Grounded**：A=liftRollerHeavyBuild（heavyBox 重型平台，反推小平台稳定），B=plainCarBuild；spawnB 560；camera vehicles。
- **关键实测（已固化）**：Grounded 场景 B 轮 grounded 序列 `11 → 10 → 11`（初始两轮接地 → step1 一个轮被 roller 顶离地 → step9 落地恢复），完美满足 true→false→true；heavy A 反推仅 29.7px（vs boxBody 平台 218px，平台稳定）。
- 测试 `liftRollerScenarios.test.ts` 4/4（一次通过）：①3 场景创建+step+Planck+autoDrive false+Lift Roller front/gadget+**Roller 真实旋转**（300 步累计 >π，Q05-V1 径向线可见）；②Lift-Light B 顶起/姿态明显（angShift>0.05 或 yShift>5）+ wc=0 + hp=1000 + roller 持续转；③Lift-Posture 俯仰明显（angShift>0.1）+ wc=0；④Lift-Grounded **grounded 序列真实翻转**（'11'→含'0'→恢复'11'）+ A 反推 <60（平台稳定）+ wc=0 + hp=1000。
- Q05 全部 targeted 4 文件 **15/15**（liftRollerAssembly 4/liftRollerBehavior 4/liftRollerScenarios 4/rendererCircleRotation 3）；scoped tsc exit 0；diff --check exit 0。
- Commit：`feat: lift roller visual scenarios (light/posture/grounded)` → HEAD `c1e0d6c`（parent=254e4ac），恰含 2 文件；push `254e4ac..c1e0d6c`，远端 = 本地 ✓；tracking ref 补实。
## 会话交接包生成（2026-08-19 22:18）
- 生成 `最强水果_WorkBuddy会话交接_2026-08-19_晚.md`（项目根目录）：含当前 git 状态（分支 foundation-02-canonical / HEAD c1e0d6c / 远端同步）、环境惯例（Node 24/vmThreads/scoped tsc/SSH push/tracking ref 补实/顶层分支规则）、Q02~Q05 全部队列轨迹表、生产文件地图（四类 Behavior + 装配三分支）、测试矩阵、遗留项、下一步候选、可粘贴的交接指令。

## Q04-R1A｜Push Rod 推力粗粒度回收（commit 207604c，已 push）

- **问题**：真人录屏 Push-Light/Heavy 都被快速推到画面两端（爆开），质量差异被掩盖。
- **扫描结论（关键）**：maxForceN 500/350/250/150/100/60/30 七档粗粒度扫描（临时诊断测试，跑完删除）——
  **150~500 区间几乎无效果**（360 步 L 位移 324→314，仅 -3%）：Prismatic motor 是速度控制型，
  setMotorSpeed(2px/step) 所需力远小于 150N，maxForce 未进入瓶颈区；降到 60/30 才明显响应。
  教训：**调 Push Rod 推力时 maxForce 必须下探到「力瓶颈区」量级（本 Build 约 <100N），
  在 150~500 里找参数是无效的**。
- **选定 maxForceN=30**（粗粒度，非微调）：Light 首次推击 100→56px、360 步总位移 324→241px
  （B 停 x≈941，未撞右墙 1525）；L/H 位移比 1.59→1.84（≥1.5 达标）；Reaction 反推 -377→-241
  （仍明显但不再爆开）。全部候选目标满足。
- **改动**：`src/battle/pushRodBehavior.ts` PUSH_ROD_DEFAULT_PARAMS.maxForceN 500→30（含注释说明）；
  `tests/pushRodScenarios.test.ts` 按验收固化新断言（bShift∈(150,400)、bFinalX<1400、aFinalX>75、
  ratio≥1.5、Reaction aShift<-50）。仅此 2 文件，其余参数/场景/质量未动。
- **验证**：pushRodScenarios/Behavior/Assembly + planckWorldPrismatic 20/20 通过（vmThreads）；
  scoped tsc EXIT=0；diff 范围核查干净；HEAD=207604cd465a73eafd5d61a50f8c5dadb7f55a23
  （parent c1e0d6c），远端 ls-remote 已同步，tracking ref 已补。
- 诊断技巧：Behavior 参数快照在构造时 → 扫描需在 loadScenario 后改 part.def.behaviorParams 并
  `new PushRodBehavior(part)` 替换 orchestrator 的 pushRods（private，运行时普通数组，as unknown 访问）。

## Q04-R1B｜Push Rod 真实连接可视化（commit c7b637c，已 push）

- **问题**：画面只显示移动 collider，Push Rod 伸出后车身与推杆间有明显空隙（悬空感）。
- **方案（最小）**：battleContract 的 `RenderFunctionalPart` 增加 optional `connector?: RenderConnector`
  （from/to/width，引擎中立）；仅 planckBattleOrchestrator 的 pushRod part 生成 connector——
  from = chassis front hardpoint 当前世界位置（facing 镜像本地硬点 + chassis 姿态旋转，公式与装配一致），
  to = Prismatic part 原点世界位置，width=6；renderer 新增 drawConnector 画 from→to 窄矩形（复用 drawShape，
  from≈to 时跳过，无假动画/拖尾/发光）。其他部件不提供 connector → Renderer 行为不变，无 Cannon/Hammer/Roller 特判。
- **真实运行验证（关键证据）**：Push-Light 快照 connector 长度**精确等于** Prismatic translation
  （extend 时 t=30.88 → len=30.88；初始/回零 → 0.04/0.00）→ 连接轴完全来自真实世界坐标。
- **测试**：新 tests/rendererPushRodConnector.test.ts（5 用例：无 connector 不变 / translation=0 不画 /
  长度=|to−from|×scale / 越长越长 / facing=-1 镜像 dx<0）；全行为场景回归 36/36；scoped tsc 干净。
- **坑**：① `as const` 的 EMPTY_SHAPE 是 readonly 类型，赋给 RenderShape（mutable）报 TS2322 →
  需显式类型标注；② 测试 CtxStub 的 lineTo 不更新 pen（沿用 circleRotation 简化 stub），线段断言
  dy 方向/起点要按此修正；③ RenderConnector 若仅作类型推断未显式标注会触发 noUnusedLocals。
- 改动 4 文件（contract/orchestrator/renderer + 1 test），HEAD=c7b637c6f73a927a7476af52196937256f63c666
  （parent 207604c），远端已同步，tracking ref 已补。

## Q05-R1｜Lift Roller「驶上」场景重构 —— 停止并回报（几何死路，0 改动）

- **任务**：把 Lift 场景从「侧撞」改为「目标车主动驶上稳定低位 Roller」（A=heavyBox+liftRoller 无轮站桩、
  B=普通双轮驶近、autoDrive、两场景 Light/Heavy），只改 scenarios.ts + liftRollerScenarios.test.ts。
- **实测（临时诊断，跑完删除；spawnB 850/1000/1150/1300/1600 粗粒度扫描）**：B 均正常驶近（121~322 步）→
  接触（bContactX≈673，B 前轮左缘 599 ≈ roller 右缘 598）→ 停住，**pitch≤0.005、yShift≤0.3、连续离地
  仅 1 帧**，A 几乎不动（aXShift≤1.3），roller 持续旋转 113.6rad，0 weapon damage。
- **几何根因（关键）**：无轮 heavyBox A 落地后 roller 圆心 y=ground−27.5（heavyBox 半高）、顶 y=ground−51.5；
  B（boxBody+wheelStd r20）前轮中心 y=ground−20、车身底 y=ground−19.5。B 前轮与 roller 外切
  （圆心距 44=20+24）被侧面顶停，接触法线几乎水平（roller 圆心比 B 轮心低 7.5px）→ 无向上分量；
  B 车身底低于 roller 顶 → 前轮离地 1 帧即被车身压回。roller 顶比 B 轮顶高 11.5px，轮径 20 无法滚上
  51.5px 障碍 → **纯几何死路，spawn 距离无法解决**。
- **结构变体也失败**：B 大轮 r28（A 被推 36px 无 lift）/ r36（A 被推 13px）/ wedgeBody 低鼻
  （pitch 0.017、run 9，仍远低于 run≥12 验收）。
- **结论**：当前 Content（roller r24 装 heavyBox front hardpoint、wheelStd r20、boxBody/plainCar）下
  「驶上」无法形成明显 Lift。按任务指示停止并回报，不继续 spawn 微调。
- **0 生产改动**：scenarios.ts / liftRollerScenarios.test.ts 未动，无 commit/push。
- **后续方向建议（供用户决策，均超本队列约束）**：① Roller Content 队列：roller 更贴地/更大半径 或
  A 用更矮 Body（roller 顶低于 B 车身底才可能「驶上」）；② 接受「侧撞顶起」为当前 Lift 表达，等
  Foundation/Content 变更后再回 Q05-R1。

## Q05-R2D｜Lift Roller 旋转方向诊断（只诊断，0 改动，无 commit/push）

- **固定条件**：A=无轮 heavyBox+liftRoller(front)、B=plainCar 双轮、autoDrive、spawnA 500/spawnB 1000、
  torque=300、radius=24（同一组结构，同 Q05-R1）。
- **结果（±0.25 两档）**：
  - +0.25：approach 222 步 / contact@234 / maxPitch 0.002 rad / maxYShift 0.2 px / maxAirborneRun 1 帧 /
    bXShift 326.9 / aXShift 1.3 / weapon 0 → 几乎无 Lift。
  - -0.25：approach 224 步 / contact@230 / maxPitch 0.041 rad / maxYShift 2.0 px / maxAirborneRun 11 帧 /
    bXShift 326.0 / aXShift 0.4 / weapon 0 → 三项指标全部明显改善，但**未达通过标准**。
- **判定**：通过标准要求 pitch>0.10 或 Y>5px，且连续 grounded=false ≥12 帧。−0.25 为 0.041/2.0/11——
  **两边都失败** → 结论：**旋转方向不是主要根因**（但 −0.25 显著更优，roller 旋转方向是重要影响因素，
  后续队列应保留 −0.25 方向）。按规则进入 Q05-R2B。
- **细节**：−0.25 的 airborne run=11 帧，距 12 帧仅差 1 帧；严格按标准判不通过，不因接近而放水。
- 0 生产改动，临时诊断已删，无 commit/push。HEAD 仍 c7b637c。

## Q05-R2B｜低平台 Roller 几何验证 —— 失败分支，停止（0 改动，无 commit/push）

- **任务**：验证「自然低平台（body height 20 + roller r16/r12，speed=-0.25，torque=300）」能否让 B
  主动驶近 → 前轮爬上 → 抬头/离地 → grounded 丢失 → 落地恢复。通过则条件落地 rollerPlatformBody。
- **阶段1 实测（临时诊断，跑完删除；A=rollerPlatformBody 无轮站桩 baseMass150 / B=plainCar 双轮 / autoDrive / spawnB 1000）**：
  - **第一次设计踩坑（关键）**：front hardpoint y=0 → roller 圆心 = ground−10（body 中心高度），
    r16 下缘埋地 6px → **旋转 roller 压地产生自驱水平力，把 A 平台从 t=0 推走 423px 撞墙**，
    roller 完全不参与（pitch 0、airRun 0）。对比 Q05-R2D heavyBox：roller 下缘 ground−3.5 不埋地 → aX 仅 0.4。
    修正：hardpoint y=−8（roller 圆心 ground−18，r16 下缘 ground−2 / r12 下缘 ground−6，均不埋地）。
  - **修正后实测**：B 正常驶近（~180 步匀速）→ 接触（t≈235）→ **r16：pitch 0.039 / yUp 1.9px /
    airRun 14 帧 / recovered / aX −0.3**；**r12：pitch 0.032 / yUp 1.6 / airRun 11 帧 / aX −0.2**。
    两档 B 前轮均被 roller 顶起 1.6~1.9px 后**顶停**（前轮停 x≈611/607，未驶过 roller），0 weapon damage。
- **判定**：门槛要求 pitch>0.10 **或** Y>5px（r16/r12 均 0.03x/1.x，不满足），且 airRun≥12（r16=14 ✓、r12=11 ✗）。
  **两档均未明显通过**（r16 仅 airRun 单项达标，pitch/Y 远低于门槛，B 是顶停不是驶上）→ 按失败分支停止。
- **几何根因（修正后）**：B 前轮圆心 ground−20，roller 顶 ground−34(r16)/−30(r12)——前轮圆心需抬升
  10~14px 才能骑上 roller 顶（=轮半径 50~70%），实测只抬 1.6~1.9px 即被顶停；roller 反向旋转（−0.25）
  能帮带起 ~2px + 离地 11~14 帧，但**不足以完成「驶上」**。
- **处置**：删除临时诊断测试 + 删除临时 rollerPlatformBody（content.ts 已恢复原状），**0 production change、
  0 commit、0 push**；未做第三档 radius / 新力 / launch / flip / impulse / 隐藏规则。
- **结论回传（按任务格式）**：Q05 Lift Roller 在方向修正 + 两档自然低平台几何后仍无法形成稳定可感知
  Lift，建议停止参数救援并重新判断机制假设。

## Q05-ARCHIVE｜移除失败 Lift Roller Content（commit 7f69b19，已 push）

- **背景**：Lift Roller 三轮体验验证（Q05-R1 场景重构→几何死路；Q05-R2D 方向诊断→非主因；
  Q05-R2B 低平台 r16/r12→仍顶停）均失败，Runtime/renderer 正常但机制假设不成立。
- **改动（8 文件，+5/−719）**：
  - content.ts：删 liftRoller def + registry entry；
  - planckVehicleAssembly.ts：Revolute 装配分支只保留 Hammer（`behavior==='hammer'`），
    删 `|| behavior==='liftRoller'`；
  - planckBattleOrchestrator.ts：删 import、liftRollers 字段、构造收集、onBeforeStep 驱动；
  - 删 src/battle/liftRollerBehavior.ts；
  - scenarios.ts：删 Lift-Light/Posture/Grounded + liftRollerCarBuild/liftRollerHeavyBuild；
  - 删 3 个测试（liftRollerAssembly/Behavior/Scenarios.test.ts）。
- **明确保留**：Revolute API / Motor / grounded 链路 / drive grounded gate / renderer circle
  rotation 径向线（通用能力；rendererCircleRotation.test.ts 保留并通过）。Cannon/Hammer/Push 未动。
- **验证**：14 文件 71/71 通过（Cannon/Hammer/Push 全量 + renderer×3 + orchestrator + physicsLabEngine
  + battleContract）；scoped tsc EXIT=0；git diff --check 干净；全仓库 grep 无 liftRoller 残留。
- **坑**：rm 中文路径下相对/绝对路径均报 No such file（伪影），实际删除成功——用 `ls | grep` 核实
  真实状态（tests 0 残留）确认。git 身份 xiaoyue<xiaoyue@local>。
- HEAD=7f69b19a2584626e3caba0abdccf9f8e5ee5d14e（parent c7b637c = Q04-R1B；R1/R2D/R2B 均 0 改动
  诊断，故 Q05-ARCHIVE 直接接在 Q04-R1B 后），远端 ls-remote 已同步，tracking ref 已补。

## Q06-BUILD-R0｜Build System 真实现状只读定位（0 改动）

- **已确认能力**：
  - BuildSnapshot 类型层全支持（Body/Movement/Functional/hardpoint/energy/quality/空槽，types.ts）。
  - buildValidator.ts **已存在且有测试**（buildValidator.test.ts）：validateSnapshot（未知 Body/
    槽位不存在/重复占用/未知部件/Energy 超载/至少1 Weapon）+ validateFunctionalInstall（单装/
    替换先减后加）+ computeEnergy。**但 0 生产调用点**——main.ts/physicsLab/orchestrator 都不调。
  - Energy：真实计算（仅 functional 消耗；WheelDef 无 energy 字段；Body 是 energyCapacity 容量）。
  - main.ts 已有 Build 编辑 UI：Body 4 种下拉 + 前后轮半径 12/20/26 + extra(none/frontMass/rear)
    + Preset 装载（presets.ts 6 个）。
  - BattleOrchestratorApi.result/phase/timeMs 已有（Matter+Planck 双实现）；arena Convergence
    Active→Warning→Closing→End 已有。
- **关键缺口（按最小 Loop 排序）**：
  - **G1 部件选择**：buildFromEdit 写死 `front→ramHead` + 可选 testMass，**不能选 Cannon/Hammer/Push**。
  - **G2 引擎路由（致命）**：PhysicsLab.loadCustom 硬编码 `{autoDrive:true}` 无 engine → 自定义 Build
    走 **Matter**；而 **Matter battleOrchestrator L121 只是注释，无 cannon/hammer/pushRod Behavior 实现**
    （Behavior 全部只在 Planck 侧）→ 当前 UI 自定义 Build 玩不到 Cannon/Hammer/Push。physicLabEngine
    test L144 明确断言 loadCustom 未传 engine → Matter（改动需保持兼容）。
  - **G3 Validator 未接入**：UI 编辑不跑 validateSnapshot/validateFunctionalInstall，超载不会拒绝。
  - **G4 Energy 无 UI 显示**。
  - **G5 胜负无 UI**：main.ts 无 result 展示 → 无法「输→改→再战」。
  - G6 hardpoint 随 Body 变化 UI 不感知（V1 可最小处理）。
- **最小可玩 Build Loop**：选 Body → 选轮子（已有）→ 选 Cannon/Hammer/Push/空（新增）→ 看 Energy
  （新增）→ 开战（planck）→ 看胜负（新增）→ 输 → 改一个部件 → 再战。
- **推荐拆分**：Q06-F1 Foundation（loadCustom 支持 engine 路由，兼容既有断言）→ Q06-V1 Validator
  接入 UI + Energy 显示 → Q06-U1 Test UI 部件选择 → Q06-B1 Battle Handoff（胜负显示 + 再战流）。

## Q06-F1｜Custom Build Planck Handoff + Reset（commit 5f30840，已 push）

- **背景**：Q06-BUILD-R0 定位的 G2（引擎路由）基础缺口——loadCustom 无 engine 参数、custom battle
  无可复用 Reset 状态。
- **改动（2 文件，+96/−3）**：
  - physicsLab.ts：loadCustom(buildA, buildB, config?)——缺省 config={autoDrive:true} 保持 Matter
    兼容；显式 engine:'planck' → PlanckBattleOrchestrator（Cannon/Hammer/Push Behavior 只在 Planck
    侧，后续 UI 队列经此入口路由）；保存 currentCustom{buildA,buildB,config}，Reset 按同一输入重建；
    Scenario Reset 原语义不变（currentScenario 优先、loadScenario 互斥清 custom）；clear() 清 custom。
  - physicsLabEngine.test.ts：新增 4 用例（7 显式 planck→Planck / 8 custom Reset→新实例且
    build+engine+spawn 相同 / 9 Scenario Reset 原语义 / 10 custom Matter Reset 仍 Matter）。
- **验证**：physicsLabEngine 10/10；buildValidator+cannon/hammer/pushRod scenarios+orchestrator
  回归 38/38；scoped tsc EXIT=0；git diff --check 干净；未改 main.ts/Weapon/Gadget/engine 默认规则。
- HEAD=5f30840256a427fad993f5265eb10b6a4c44e8e1（parent 7f69b19=Q05-ARCHIVE），远端已同步，
  tracking ref 已补。下一队列候选：Q06-V1（Validator 接入 UI + Energy 显示）/ Q06-U1（部件选择 UI）。

## Q06-F2｜Build Editor Pure Model（commit fe3381d，已 push）

- **目的**：把 main.ts 写死的 buildFromEdit 抽成可测试的纯 Draft 模型，供后续 UI 动态生成真实
  Functional Hardpoints。
- **新增**：src/lab/buildEditorModel.ts + tests/buildEditorModel.test.ts（2 文件，+279）。
  - BuildDraft{bodyDefId, rearRadius, frontRadius, functionalSelections(hardpointId→defId|'none')}；
  - editableSlots(body) = 按 BodyDef.functionalHardpoints 返回真实槽位（wedge/tall: front/top/rear；
    box/heavyBox: +frontMass）；
  - migrateDraftBody：Body 切换保留同名选择、丢弃不存在旧槽、新槽默认 none，键集合恒等于目标
    Body 真实槽位（不产生非法遗留槽）；未知 Body → 清空槽位；
  - buildSnapshotFromDraft：movements=rear/front 轮（radius overrides）；functionals 仅非 none 且
    硬点真实存在（未知 Body → 空集 → 全过滤）；不自动塞 ramHead；
  - 只暴露 none/cannon/hammer/pushRod（ramHead/testMass 不暴露）。
- **验证**：buildEditorModel 7/7；buildValidator+physicsLabEngine+cannonScenarios 回归 28/28；
  scoped tsc EXIT=0；git diff --check 干净。未做 UI/Validator/Energy/专属槽。
- **坑**：未知 Body 时 validHardpoints 初版用 null + `!null||has` 导致全保留 → 改为空 Set 后全过滤。
- HEAD=fe3381d3ca56292083c690275c484d0d1697f18f（parent 5f30840=Q06-F1），远端已同步，
  tracking ref 已补。

## Q06-U1｜Build 配置 UI + Validator + Energy（commit 0b3ea67，已 push）

- **问题**：UI functional 写死 ramHead、onchange 自动开战、Validator/Energy 未接入。
- **改动（2 文件，+325/−106）**：
  - main.ts：基于 Q06-F2 BuildDraft 重构 A/B 面板（Body/轮径/真实 Functional 槽位下拉，
    空/Cannon/Hammer/Push Rod 不区分 Weapon/Gadget 槽；Body 切换动态重建槽位）；所有下拉
    只改 Draft 不自动 loadCustom；每侧 Energy used/capacity（复用 computeEnergy）；
    validateSnapshot 实时显示最主要错误；「开始战斗」按钮 A/B 均 valid 才可用，点击
    loadCustom(..., {autoDrive:true, engine:'planck'})；Preset 只装载 Body/轮径不自动开战。
  - 新增 tests/buildFlow.test.ts（7 用例）：Push Rod 与 Cannon/Hammer 共存 / 单独 Push Rod
    被「至少1 Weapon」阻止 / boxBody 4 槽全 cannon Energy 超载（120>100）阻止开战 /
    合法 Build（cannon+pushRod=50）可启动 / Body 切换槽位合法 / 端到端 loadCustom(planck)
    → Planck Runtime + 三 Behavior 接线 step 正常。
- **Energy 数值（关键）**：cannon=30 / hammer=25 / pushRod=20；boxBody/heavyBox capacity=100。
  4 槽全 cannon=120 → 超载案例成立；cannon+hammer+pushRod=75 合法。
- **验证**：targeted 14 文件 81/81；scoped tsc EXIT=0；git diff --check 干净；vite build
  36 modules transform 全过（产物清理被沙箱 delete 保护中止——环境限制非代码问题；dist 未跟踪）。
- **坑**：vite build 在 prepare-out-dir 清理旧 dist 时被 WorkBuddy 沙箱 safe-delete 中止
  （genie-safe-delete trash 操作 aborted）；transform 阶段已全部成功即可证明打包层 OK。
- HEAD=0b3ea67c585fa9240e833e5ebf4098c5f0ca39e2（parent fe3381d=Q06-F2），远端已同步。
  至此「选 Body→选轮→选部件→看 Energy→开战→(输)改→再战」最小 Loop 已通（B1 胜负显示尚未做）。

## Q06-B1｜Result → 修改 → 再战 Loop（commit 454d7c6，已 push）

- **目的**：补齐最小核心循环「配置 → 开战 → 看结果 → 修改配置 → 再战」。
- **改动（2 文件，+197/−9）**：
  - main.ts：toolbar 结果条（战斗结束显示 A胜/B胜/平局 + hpA/hpB，只读
    orchestrator.result，不改 BattleResult 规则）；按钮状态机（未开战「开始战斗」
    valid 才可点 / custom 战斗中「战斗中…」锁定 / 结束后「应用配置再战」点击重新
    validate 再 loadCustom(planck)）；Draft 与 current battle Build 分离（编辑只改
    下一局）；B Draft 独立；loop 每帧 pollBattleResult（result 变化才刷新）；
    scenario/Clear/Reset 同步 activeBattleKind 与按钮。
  - 新增 tests/buildLoop.test.ts（5 用例）：运行时改 Draft 不影响当前战斗（同实例
    同 Build）/ B Draft 独立 / 改部件→再战→新 Build 真进 Runtime（cannon→hammer）/
    未修改原配置再战（位置重置）/ resolveBattleResult 三态。
- **验证**：targeted 16 文件 93/93；scoped tsc EXIT=0；git diff --check 干净。
- **至此 Q06 最小 Build Loop 全链路完成**：F1（planck 路由+Reset）→ F2（Draft 模型）
  → U1（配置 UI+Validator+Energy）→ B1（结果→再战）。真人可：选 Body/轮/部件 →
  看 Energy → 开战 → 看胜负 → 改一个部件 → 再战（B 不变可连续对比两次 A Build）。
- HEAD=454d7c67e5f1efed84c3746b5dd002c731957ae5（parent 0b3ea67=Q06-U1），远端已同步。
