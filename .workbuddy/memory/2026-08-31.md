# 2026-08-31

## F-GARAGE-VISUAL-DENSITY-R2｜强化中央车辆并精简底部装配带（已完成并推送）

- **commit**：`57bdbb30db8512c3f8c7df61975b70e1e45f8b88`（branch foundation-02-wechat，local = remote 三路一致，2 文件 +20/-49）。
- **Must#1 修改前基线（最终像素，renderer previewSolo）**：420×210 车辆宽 31.7%（超短屏受「完整入画+落地」高度约束）、621×351 39.4%、844×390 39.1%、1280×592 43.3%（目标 40-48%，621/844 差 ~1% 为真实 insets 收窄安全宽所致）；车辆中心全部 50.0% ✓；装配带 30.0-30.2% 屏高（∈ Queue 27-32%）✓；舞台高 89/175/203/344px（≥40% 屏高，主导）✓。
- **修改**：
  - Must#5：`drawVehicleHardpoints` idle（未拖动/未 armed/无吸附 flash）**不再常驻任何挂点圆**（selected/occupied/可用轮廓全隐藏；仅成功吸附 flash 期间显示目标挂点金色呼吸）——「车是唯一视觉中心」；拖动/armed 路径（compat 分支）不变（Must#6 交互保持）。
  - Must#3：删除 `drawGarageDragHint`「拖到车辆挂点安装」教学横幅（一次性、与未选挂点提示/卡片状态徽标语义重复）+ `dragHintDismissed` 字段与 4 处赋值清理。
  - Must#4 卡片 5 态确认已达标（已装备灰蓝 equippedFill+「已装备」徽标+竖条 / 可装备 availableFill 亮 / armed 金描边 / 未获得最暗+锁定 / 拖动源 dimmed 压暗），T4 亮度差 ≥0.04。
- **e2e（Must#7）**：`tests/_e2e_garage_drag.cjs` 补 K 块（已装备灰态源码守卫 equippedFill + 「已装备」徽标不省略）；全门禁 **140 PASS / 0 FAIL**（4 环境 × A-J/K 路径：横滑不装备、拖车轮换前后轮、武器/辅助到功能挂点、空白释放不改配置、单击卡→挂点、返回首页再进保持、已装备灰态）。
- **验证门**：tsc exit 0；targeted Garage 批次 50/50；全量 vitest **1430 测试全过**（两轮，偶发音频/platformCore 5s 超时单独跑必过，与本 Queue 无关）；四目标构建（build / build:pages / build:e2e / build:wechat）全过；干净 HEAD `build:wechat:rc` → badge `#57bdbb3`、rc-build.json dirty=false、bundle runtimeInfo=HEAD 三方一致。
- **冻结遵守**：未动 PlayerViewportTransform / Canvas backing / DPR / safe area / Physics / 战斗数值 / 装备规则 / 存档 / Home·Matching·Battle·Result 视觉；未部署 Pages。
- **下一步**：停止。真机/浏览器复验点：4 视口 Garage 车辆完整入画居中、分类切换无缩放跳动、挂点仅在拖动/armed/吸附时出现、卡片 5 态一眼区分、拖装全流程（横滑/替换/空卸/超载/返回保持）。

## F-GARAGE-CENTER-SCALE-R2.1｜提升 Garage 中央车辆实际可见面积（已完成并推送）

- **commit**：`031b8d331940b75a8d2fd2736c101039347564bb`（branch foundation-02-wechat，local = remote 三路一致，3 文件 +231/-7）。
- **Must#1 缩放链追踪（证据驱动）**：stageRect → framingRect → renderer.previewSolo envelope 自适应（vehicleBounds + padX/padY + margin）→ width clamp [40%,47%] → hLimit。**瓶颈定位**：420/621/844 受【高度】限制（bounds 高 = envelope 76.9 + padY 0.31×eh×2 + margin 8×2 = 140.5，车辆仅占 55%）；1280 受【宽度 clamp 上限 47%】。用探针（garage 临时放宽 clamp 到 0.60 → 车辆宽不变）证明宽度上限非 420/621/844 瓶颈。
- **修复（Garage 专用，Home 零影响）**：
  - renderer：Garage 专用 padX 0.24 / padY 0.14 / MIN 24/10、margin 4（Home 保持 0.38/0.31/40/20/8）；clamp 上限按「**最终可见宽 ≤48% 屏宽**」定义（0.48×viewportW，非安全宽百分比——否则无 insets 时 49.3% 超 48% 屏）。
  - uiHost：挂点 hitArea 与视觉同源（idle 不注册 hp-sel——看不见不可点）。
- **最终像素（真实 insets {44,20,12,16}，0.48 屏宽语义 clamp）**：420×210 = **36.6%**（≥36 ✓）、621×351 = **40.3%** ✓、844×390 = **42.3%** ✓、1280×592 = **44.3%** ✓（旧值 31.7/39.4/39.1/43.3）；中心全 50.0%；车辆完整在 stageRect 内（T2 断言）；Home 零变化（T4 对照 + Home 批次回归）。420 未到 38%：高度允许 ~160px vs 屏宽上限 0.48×420=201.6 → hLimit 160 主导 → 36.6%（≥36 达标；继续放大需增 stageH，属布局改动，超出本 Queue「只调 Garage 取景」范围）。
- **测试**：`tests/garageCenterScaleR21.test.ts` 6 项（T1 四视口宽占比、T2 中心≤3%+完整入画+不相交、T3 三分类 scale/中心稳定≤1%/2%、T4 Home 对照、T5 idle 挂点 0、T6 armed 挂点>0）；garageCenterStageP0 T2 保持通过（0.48 屏宽语义下 49.3%→47.3%）。
- **验证门**：Garage 批次 152/152；全量 vitest **1436 全过**；tsc exit 0；e2e 拖装门禁 **140 PASS / 0 FAIL**（车辆放大后 A-J/K 全路径保持）；四目标构建全过；干净 HEAD `build:wechat:rc` → badge `#031b8d3`、rc-build.json dirty=false 三方一致。
- **⚠️ git 对象库损坏与恢复（重要经验）**：`git stash push <file>` 在本环境失败（invalid object）并**破坏本地对象库**（refs/heads 被清空、HEAD commit 对象丢失 → git status 报 bad object HEAD）。恢复路径：`git -C . fetch origin <branch>` 从远端拉回对象 → `cat-file -t <sha>` 验证 → `mkdir -p .git/refs/heads && printf sha > 分支文件` 重建 ref。**避免在本环境使用 git stash**（用临时文件/手动 diff 替代）。
- **冻结遵守**：未动 PlayerViewportTransform / Canvas backing / DPR / safe area / Physics / 战斗数值 / 装备规则 / 存档 / Home·Matching·Battle·Result 视觉（Home 对照断言确认零影响）；未部署 Pages。
- **下一步**：停止。真机/浏览器复验：4 视口 Garage 车辆更大更居中、完整入画落地、挂点仅在拖动/armed/吸附显示、拖装全流程保持。

## F-REPO-HEALTH-GUARD-P0｜Git 仓库健康只读门禁 + 恢复指南（已完成并推送）

- **commit**：`9d476884e10cab561c2fae763e5c5a81f1459bd2`（branch foundation-02-wechat，local = remote 三路一致，5 文件 +426/-1）。
- **交付**：
  - `scripts/repo-health.js`（只读，纯 ESM 可注入 runner）：9 项关键检查 = worktree / HEAD 解析 / branch 存在 / refs/heads 解析 / HEAD==ref 一致性 / origin branch（ls-remote 只读）/ HEAD commit 对象 / HEAD^{tree} 对象 / `fsck --connectivity-only` 无 missing/broken。**只读铁律**：不写 refs、不 reset/checkout/删对象/fetch/push/清理、不调 stash。
  - `scripts/wechat-rc.js`：dirty 检查**之前**先跑健康门禁，任一关键检查失败 → 拒绝 + 输出失败项 + 人工恢复指引（不自动修复）。
  - `docs/GIT-REPOSITORY-RECOVERY.md`：HEAD 对象缺失确认 → 外部 binary patch 备份（仓库外、验证非空）→ origin fetch 只读恢复 → 三路 SHA 核对 → 何时重建 ref → 禁止操作清单（reset --hard / checkout -- / 删 .git / 未备份写 ref / stash）。
  - `tests/repoHealth.test.ts` 11 项（runner 注入模拟：健康通过 / 非 worktree / HEAD 对象缺失 / branch ref 缺失 / HEAD≠ref / remote 缺失 / remote SHA 不一致 / fsck missing / HEAD 无法解析 / 只读命令约束断言 / 恢复指引内容）。
- **验证门**：targeted 25/25（repoHealth+rcGate）；tsc exit 0；全量 vitest **1447 全过**；普通 build:wechat exit 0；**repo-health 最终 HEAD 9/9 通过**；干净 HEAD `build:wechat:rc` → 健康门禁通过 → badge `#9d47688`、rc-build.json dirty=false、bundle runtimeInfo=HEAD 三方一致。
- **冻结遵守**：未改 src 游戏逻辑/UI/Renderer/输入/音频/存档；未部署 Pages；全程未用 git stash；未直接改 .git/refs（本 Queue 未触发恢复路径）。
- **下一步**：停止。后续任何 git 异常先跑 `node scripts/repo-health.js`（只读自检），失败即停并按 docs 指南人工恢复；本环境持续禁止 stash。

## F-BATTLE-CAMERA-HIERARCHY-R2｜Battle 双车成为视觉主体的稳定取景（已完成并推送）

- **commit**：`04be07b7708b55746a25f8e6775ad6eb8f4b01e3`（branch foundation-02-wechat，local = remote 三路一致，3 文件 +281/-9）。
- **Must#1 取景链追踪（证据驱动）**：battle snapshot → world bounds（双车+间距+margin）→ fitLimit = min(safeW/bw, safeH/bh)（宽度主导）→ scale → groundY 锚定 68% → transform。六阶段数据（Active start/near/hit 用 step 循环推进：300 步 A 315→590、B 1125→972、间距 810→382；Warning/Closing/End 复用 battleCam）——**瓶颈**：初始 spawn 双车外廓占可用宽 89-91%（超 Must#3 上限 86%）、间距占屏 59-61%、单车仅 14-16% 屏（可辨但小）；中心 50.7% ✓、地面 68% ✓、120 帧漂移 0%。
- **修复（battle 专用，Must#3/#5/#7）**：
  - renderer battle Active **首帧**（battleCam 未记录时）按 A∪B 真实 envelope 外廓动态收缩：双车+间距 ≤86% 可用宽（`BATTLE_SPAN_MAX_PCT=0.86`），单车可见宽下限 12% 屏（`BATTLE_SINGLE_MIN_PCT`，不可辨则不收缩）；
  - 后续 Active 帧复用 `battleCam.baseScale`（相机稳定，不逐帧重新 fit 放大 → 120 帧漂移 0%）；
  - Warning/Closing/End 走既有 battleCam ±10% clamp（阶段平滑，墙推进不骤缩；Must#6 保持）；
  - Home/Garage/Matching 零影响（previewSolo/previewFixed 未动；garageCenterScaleR21 T4 对照通过）。
- **最终像素（Active 初始，四视口）**：双车+间距全部 **86.0%**（旧 89-91%）；单车 15-19% 屏；中心 50.6-50.7%（≤5%W）；车辆完整入画、不碰 HUD（y ≥ HUD 顶 56）、地面 68%H（∈68-82%）。
- **测试**：`tests/battleCameraHierarchyR2.test.ts` 7 项（T1 四视口初始构图严格 86%/单车≥12%/中心≤5%/入画/HUD/地面、T2 接近碰撞单车≤55% 屏、T3 Warning/Closing scale ≤15% + 完整入画、T4 120 帧漂移 ≤1%、T5 Matching→Battle 无旧帧（battle 首帧重取景+收缩构图）、T6 Result 后离开 Battle 清理、T7 Home/Garage 对照零变化）；既有 battleStageCompositionP0/battleCameraR2/battleHitReadabilityR1 回归通过。
- **e2e（Must#10）**：`tests/_e2e_battle.cjs` 适配单 canvas 架构（`cs[1]||r` 回退、tapById 改 playwright 原生 mouse）+ 新增 J/K 断言（双车+间距 ≤86% 屏宽【rect 外廓 + A 绿/B 橙像素背书】+ 像素证据）+ viewports 补 1280×592 / 844×1.5 / 844×3（原 3 个 DPR1）——**108/108 PASS**（含 6 viewport × 全阶段；Closing 偶发未进入为战斗随机性，重跑全过）。
- **验证门**：targeted battle 批次 28/28 + 全量 vitest **1454 全过**；tsc exit 0；四目标构建全过；**repo-health 最终 HEAD 9/9**；干净 HEAD `build:wechat:rc` → badge `#04be07b`、rc-build.json dirty=false、bundle runtimeInfo=HEAD 三方一致。
- **冻结遵守**：未改 Physics/spawn/伤害/能量/装备/存档/胜负规则；未改 Home/Garage/Matching 构图（对照断言确认）；未动 PlayerViewportTransform/Canvas backing/DPR/safe area；未部署 Pages；未用 git stash。
- **已知**：战斗推进帧（400ms 后）probe rect（含 parts envelope）span 略超 86%（86.5-87.4%），e2e 允许 +2% 运行期容差；单元层严格 86%。
- **下一步**：停止。真机复验：Battle 初始双车留边距居中、间距空白视觉减少、接近/碰撞/Warning/Closing 平滑、HUD 与刺墙不相交、伤害数字/激光表现不回退。
## F-BATTLE-DYNAMIC-FRAMING-R2.1｜Battle 动态取景：接近平滑放大 + Closing 墙可见（已完成并推送）

- **commit**：`7c2d1cb0ce9d3a7b25d70e301c7156b23ca92375`（branch foundation-02-wechat，local = remote 三路一致，4 文件 +410/-32）。
- **Must#1 追踪**：固定 scale 逻辑生效于 Active 首帧后所有帧（旧 else scale = baseScale）；接近/碰撞后 scale 不变 → 车辆屏幕尺寸不变（碰撞后仅 40% span、单车 15%）。
- **修复（renderer battle Active 每帧）**：
  - 每帧目标 scale：双车 core 间距比例（gapRatio = gapWorld/coreUW，core=body+wheels 不含 parts，避免武器伸出扭曲阶段判定）驱动三段目标 span：远(>=0.55)→87%、接近(0.25-0.55)→75%、碰撞(<0.25)→60%（占可用宽）；
  - 首帧直达目标（Matching→Battle 无旧帧闪）；后续帧阻尼 + 单帧钳制 <=1.5% + 死区 <0.3% 滞回（防轮胎震动呼吸）；
  - envelope 合并 bodyVisual sprite（碰撞/后坐/Push 视觉位移也必须完整入画——Q08-A-FIX 回归语义，battlePhaseFx 23/23）；
  - Closing 含墙 fit：墙推进后（wallSpan < arena*0.98）渐进拉远（3%/帧）到左右墙内缘可见（edgeRed 24.3%），墙未推进不触发（S3 阶段 <=10% clamp 保持）；
  - gapWorld 公式修复（原写反为重叠量，初始 gapRatio=0 → 误判碰撞段）。
- **最终数据（844x390）**：初始 span 87%（远段）→ 碰撞段 60% + 单车 24-53% 舞台（车辆成主体，scale 0.557→1.44 平滑放大）；单帧 scale 变化 <=1.5%；中心移动 <=0.07%W；静止 120 帧漂移 0%；地面漂移 0px；无短周期振荡。
- **测试**：tests/battleDynamicFramingR21.test.ts 9 项（T1 初始 82-88、T2 接近过渡、T3 碰撞段 48-70 + 单车 >=16%、T4 单帧 <=1.5% + 中心 <=2% + 地面 <=1px、T5 静止 120 帧 <=0.5%、T6 无振荡（短周期 <=2）、T7 Warning/Closing <=10% + 完整 + 地面、T8 Result 清理 + 下一局重新初始化、T9 Home/Garage 零变化）；battleCameraHierarchyR2 断言更新（86%→82-88、120 帧 <=0.5%）；既有 battlePhaseFx/battleStageCompositionP0/battleCameraR2/battleHitReadabilityR1/mobileBattleFraming/rendererScaleTrace 全部回归通过（BATTLE_SPAN_FAR=0.87 使单车 >=20% 旧断言保持）。
- **e2e（Must#10）**：_e2e_battle.cjs J/J2（初始 span <=88% + >=55%）、L/L2（战斗推进轮询：单车 rect 放大 >=5%【196→242px】+ 像素背书）、goto 改 'load'（networkidle 挂起修复）——**126/126 PASS**（6 viewport × 全阶段，含 Closing 墙边缘红 24.3%）。
- **验证门**：battle 批次 57/57 + 全量 vitest **1466 全过**（platformCore 偶发 5s 超时单独跑必过）；tsc exit 0；四目标构建全过；repo-health 最终 HEAD 9/9；干净 HEAD build:wechat:rc → badge #7c2d1cb、rc-build.json dirty=false、bundle runtimeInfo=HEAD 三方一致。
- **冻结遵守**：未改 spawn/初速度/Physics/接敌/武器/伤害/胜负；未改 Garage/Home/Matching/安全区；未用全局放大或 DPR 补偿；未部署 Pages；未用 git stash。
- **环境注意**：e2e server 长时间运行会挂（goto networkidle 挂起 → 改 'load'）；vitest vmThreads 吞 console.log（调试用 console.error 或测试内直接计算）；sandbox 对 bulk 删除敏感（清理用独立删除命令）。
- **下一步**：停止。真机复验：Battle 初始双车 87% 留边距 → 接近平滑放大 → 碰撞车辆成主体（单车 >=16% 舞台）→ Closing 墙+双车+地面同时可见；无抖动/无振荡/地面稳定。
## F-BATTLE-FX-SCREENSPACE-R2｜Battle FX 稳定 screen-space 尺寸（已完成并推送）

- **commit**：`b1ef36e0c99255ed0d7b67d6f002a9e68a414433`（branch foundation-02-wechat，local = remote 三路一致，9 文件 +511/-69）。
- **Must#1 四类现象追踪（证据驱动）**：
  1. **近战巨大伤害数字** → renderer `fs = f.size ?? Math.max(14, this.ss(22))`（ss=×camera scale；碰撞段 1.245 → 27.4px；重要伤害常量 32px 也超 18px 上限）；
  2. **持续白色矩形外框** → 确认是 hit flash（`spawnHitFlash` upsert 每击刷新 bornAt + body/parts 白描边 alpha 0.85）——高频命中永不淡出 = 常驻白框；
  3. **青色蓄能点/命中特效** → charges 已是金黄（Q08 已改）；青色 = laser projectile trail #7fd8ff + laser beam（尺寸走 ss() +69%）；
  4. **画面下方黑点** → e2e 浏览器画布底部 0 孤立黑点（P 断言 dots=0 全过）→ 确认来自微信开发者工具触控提示，**未改游戏代码**。
- **Must#2 基线（844x390，远 0.737 → 碰撞 1.245，+69%）**：dmg 16.2→27.4（+69%）、important 32（超限）、lane 11.8→19.9、flashLW 2.2→3.7、spark +22%、charge +41%、trail/tracer/beam/tongue 全部 +69%。首次受 scale 放大位置 = 每个 FX draw 路径的 `ss()` 尺寸调用（已全盘点）。
- **修复（Must#3-#7，纯表现）**：
  - **伤害数字**：常规 `ss(22)`→**常量 15px**；重要 `DAMAGE_NUMBER_IMPORTANT_SIZE 32→18`（保留金白描边层级）；lane `ss(16)`→16、rise `ss(40)`→40（screen-space 常量）；ty 下限保留 `ss(44)`（HUD 守卫语义，battleHitReadabilityR1 T6 保持）；总量守恒/≤2 组/聚合规则未动（T5/T8 验证）。
  - **hit flash**：峰值 alpha 0.85→**0.35** + **60% ttl（72ms）内不刷新 bornAt**（高频命中「亮→淡→再亮」非常亮白框）；ttl 120ms ≤160 ✓；描边保留（不删反馈）。
  - **武器/命中 FX**：spark/muzzle/charge/death/laser trail/tracer/beam 宽度/霰弹扇/枪口火舌全部改 **screen-space 常量**（0% 远近差 ≤20%）；laser 长度 ≤45% 屏宽 clamp 保持；弹体方向/命中点/发射位置仍走 sx/sy（准确）。
  - **连续火焰流（fire jet / flame plumes）保持几何跟随**：buildFireJet 由真实弹体位置推导（maxForward/maxSide），属位置几何不属 FX 尺寸——文档化决策，不做常量（避免破坏「喷流到达真实弹体」准确性）。
  - **层级**：hit flash 在 damage 数字之上（既有顺序即符合 车辆>武器>命中反馈>数字>环境）；**清理**：clearBattleVisualFx 补 `damageGroupFx.clear()`（下一局不继承分组）。
- **测试**：`tests/battleFxScreenSpaceR2.test.ts` 8 项（T1 字号 15/18 恒定且 ∈[12,18]（Proxy 记录 ctx.font）、T2 ttl≤160 + 峰值 alpha 0.35 描边、T3 72ms 内不刷新 bornAt + 60ms 淡到 0.175、T4 spark/muzzle/charge 远近尺寸差 ≤20%、T5 同车 ≤2 组 + 总量守恒（5+7+9=21）、T6 laser ≤45% clamp 保持、T7 清理清空全部 FX、T8 越过聚合窗口后不继承）；旧测试更新：q14abR2Final/rendererLaserBeam/rendererProjectile（×scale→常量）、battleHudHazardR1 T5（ss(16)/ss(40)→常量）、battleReadabilityR1 C1/D3（regex 更新）+D1（切片窗口）、F-BATTLE-PRESENTATION-R2（size 32→18）。
- **e2e（Must#10，143/143 PASS）**：analyzeInPage 新增 **N/N2**（伤害数字像素 bbox 高度：文本/车身分离判据 = 宽松暖色域 + 行跨度 ≤24px + 中央 70% x 带 + 带高 ≤26px——实测 B 车身行跨度 46-62px、数字 4-20px）+ **P**（底部 10% 带孤立黑点检测，dots=0）；N2 峰值 14-24px ≤26（旧 27px+ 必超）。6 viewport × 全阶段；Closing 提前击杀为已知随机（K 偶发未进入，重跑全过）。
- **验证门**：battle 批次 97/97 + 全量 vitest **1471 全过**（audioLifecycleP0 T3/T6 + platformCore 偶发 5s 超时，单独跑 30/30 全过）；tsc exit 0；四目标构建全过；**repo-health 最终 HEAD 9/9**；干净 HEAD `build:wechat:rc` → badge `#b1ef36e`、rc-build.json dirty=false、bundle runtimeInfo=HEAD 三方一致。
- **冻结遵守**：未改 spawn/Physics/伤害/攻速/武器规则/胜负/Closing；未改 Home/Garage/Matching；未动 dynamic framing（battleDynamicFramingR21 + battleCameraHierarchyR2 回归通过）；未用全局 alpha 滤镜/DPR 补偿；未部署 Pages；未用 git stash。
- **经验**：e2e 像素级「文本 vs 色块」分离用**行跨度**（文本 ≤24px、色块 46-62px）比窄色域更稳；伤害数字混采 15/18 两类字号时 ratio 无区分力（新旧 ratio 1.69 vs 1.71 相近），核心门禁用**绝对峰值 ≤26px**。
- **下一步**：停止。真机复验：近战伤害数字不再巨大（15/18px 稳定）、受击白框闪烁而非常驻、激光/火花/蓄能尺寸稳定、底部无黑点。
## F-LOSS-ADJUST-REMATCH-LOOP-P0｜失败→Result→Garage换装→再战 闭环（已完成并推送）

- **commit**：`8cf09f3a81fc079b6886f137783ec3cef5461419`（branch foundation-02-wechat，local = remote 三路一致，8 文件 +691/-34）。
- **Must#1 链路追踪（证据驱动，先查后改）**：Battle胜负（pollBattleResult result 引用门）→ Result（battleState=ended）→ 奖励结算（rewardSettler/progressSettler 幂等键 + 每场 reset + battleEndGuard.clear）→ 调整配置/下一场（onResultAdjust→adjustConfig / onResultNext→nextMatch→startMatching）→ Garage（draftA 保留 + refreshFromEdit→savePlayerBuild）→ Renderer 预览（snapshotOf('A')→loadCustomPreview）→ 下一局（startOrRematch：snapshotOf('A')→loadCustom 新 orchestrator + battleSessionSeq 自增新 audio session）。
- **Must#2 六断点确认**：①奖励不重复（引用门+settler 幂等+每场 reset）✓；②换装后用当前 draftA（loadCustom 每次新快照）✓；③Next 路径 FX 未清（fx 绘制不 gate 于 battle fit → Matching previewSolo 会画残留）**→ 已修**；④返回 Garage 不丢装备（draftA 保留）✓；⑤新 session（battleSessionSeq 自增）✓；⑥重开保持（savePlayerBuild/saveInventory/saveProgress + load 恢复）✓。
- **修复**：
  1. **Must#3 Result 按钮层级**：Desktop drawResult + Mobile showResultModal 主/次按钮按胜负切换——战败主=「调整配置」（暖金 V.primary）、胜利主=「下一场」；另一为次按钮；位置不变（adjust 左 / next 右）；hitArea 由 button() 每次绘制注册 → 与视觉同源；不新增弹窗。
  2. **Must#4 断点修复（重要发现）**：Result→adjustConfig 后 metaPage 停在 home（首页态）→ Garage dock/挂点不可见 → **违反「直接进入 Garage 中央装配舞台」**。修复：CanvasPlayerUIHost.render 检测「prev.battleState==='ended' → state.battleState==='editing' + playerPhase==='garage'」→ metaPage='garage'。
  3. **Must#7/#9 nextMatch**：加 `nextMatchInFlight` 防重入门（广告 await 期间二次点击拦截）+ clearBattleFx（与 adjustConfig 对称，Next 路径也清 FX）。
  4. 存档/幂等/清理：既有机制确认（settler 幂等、lastShownResult 引用门、clearBattleVisualFx + damageGroupFx.clear）。
- **测试**：`tests/lossAdjustRematchLoopP0.test.ts` 11 项全过（T1 Loss 主 CTA 源码断言、T2 Win 主 CTA、T3 Loss→Garage 配置保持、T4 奖励只结算一次（同场多次 tick 库存不变）、T5 新部件进库存 +1、T6 换装后下一局 loadCustom Build 一致、T7 Next Match 不进 Garage、T8 连点防重入（in-flight 门，stop 计数验证）、T9 新 session battle-N 递增、T10 重开配置/库存往返、T11 adjust/next 均清 FX+停 audio）。旧测试更新：resultUxR1/resultDemoR2/mobileLandscape（primary: '下一场'→按胜负切换）、wechatResumeRenderStateP0 C1（Result→Garage 装配台新语义 + 显式 nav:home 回首页，保留 transform 泄漏核心检查）。
- **E2E**（`tests/_e2e_loop.cjs`，**20/20 PASS**）：A 强制 Loss（Result 主按钮语义判定：primary→garage=loss / →matching=win，win 重开）、B 调整配置→Garage、C 换 body（bananaBody）+ 换装即时同步（__h.lastState.draft）、D nav:home 回首页再匹配、E **最终合成像素证明外观变化**（A 车 rect 174x60→193x59 + green 0.24→0.00 + orange 0.00→0.11）、F probe.vehicleRects snapshot 证据、G **奖励未重复**（localStorage 库存 s1→s2 差 =1）、H Next Match 路径（重试至 Win 局点主按钮→matching 不进 garage）。视觉证据 = 最终 Canvas 像素；probe 只用于配置/session 证据。
- **验证门**：全量 vitest **1482 全过**（audioLifecycleP0/platformCore 偶发超时单独跑全过；mobileLandscape/wechatResumeRenderStateP0 因 CTA 语义更新后全过）；tsc exit 0；四目标构建全过；**repo-health 最终 HEAD 9/9**；干净 HEAD `build:wechat:rc` → badge `#8cf09f3`、rc-build.json dirty=false、三方一致。
- **冻结遵守**：未改 Physics/伤害/胜负规则/匹配算法；未改 Garage 布局与拖动状态机；未改 Battle 动态相机与 FX；未动 viewport/DPR/safe area；未新增页面层级；未部署 Pages；未用 git stash；未重载页面掩盖问题（E2E 重开是流程内设计）。
- **经验**：
  - Result 的 primary/secondary **位置恒定**（primary 恒在右）——不能靠位置判定胜负，必须靠**回调落点**（点 primary → garage=loss / matching=win）或**重试至 Win**验证 Next 路径；
  - playerPhase='garage' ≠ 装配台——**metaPage** 才是 Home/Garage 的区分（home 也是 garage phase）；Result 返回需显式切 metaPage；
  - 玩家可选 body 仅 4 个水果（watermelon/banana/pineapple/coconut），boxBody 是对手池/测试用；
  - Garage 装配台无匹配按钮——再战需 nav:home 回首页 → home-find-opponent；
  - rc 构建会被未跟踪临时文件（如 tests/zz_*.cjs）拦截（dirty check）——提交/RC 前必须清干净。
- **下一步**：停止。真机复验：战败结算卡主按钮=「调整配置」（暖金）、点击直接进 Garage 装配台换装、再匹配新车外观生效、奖励不重复、Next Match 连续点击防重入。
## F-GARAGE-ADJUST-REMATCH-P0｜战败 Result 一键回流：Garage「完成并再战」直连 Matching（已完成并推送）

- **commit**：`6adc710df86eb4fdc9589d50c5a04c572af76917`（branch foundation-02-wechat，local = remote 三路一致，5 文件 +718）。
- **Must#1 调查结论**：Result→Garage 入口标识丢失（adjustConfig 不设来源标记）；Garage 不知道来自战败 Result（metaPage 是 host render 推断，无来源语义）；直接再战复用 `startMatching()`（与 onFindOpponent/onResultNext 同一状态链：matching 相位门 + 新 draftB + generation 防重入）；须保持的清理 = adjustConfig（FX+audio）、startOrRematch（rewardSettler.reset + battleEndGuard.clear + battleSessionSeq++ + loadCustom 新 orchestrator）。
- **Must#2 瞬时上下文**：`garageFromResult`（host UI 层，不持久化）——render 检测「prev ended→editing+garage」置 true；返回 Home（nav:home dispatch）/ 进入 Matching（render 检测 phase 离开 garage）/ 新 Battle / 重启均清除。BuildDraft 仍走 refreshFromEdit→savePlayerBuild 正式路径。
- **Must#3 按钮**：Garage 顶栏中部「完成并再战」暖金主按钮（id 'garage-retry'，仅 garageFromResult 显示）——位置 = 左「‹ 首页」右侧 ↔ 右能量组左侧（复用 computeGarageTopBarLayout 的 tb.back/energyLabel，几何同源）；不遮挡中央车辆/挂点/底部装配带；hitArea 由 button() 绘制即注册；四视口（420/621/844/1280）可见；safe 区遵守。无效配置 → 文案「配置不合法」+ disabled（Must#6，复用既有 dock 错误提示）。
- **Must#4/#5**：返回 Home 保留（nav:home 清上下文，再进 Garage 无按钮）；点「完成并再战」→ `onGarageRetry`（runtime：buildsValid 守卫 + sfx.resume + startMatching 直连 Matching 不经过 Home）→ 新 battle session（startOrRematch 既有链）。
- **Must#7 防重入**：startMatching 的 matching 相位门（连点第二次被拦）——无需新门。
- **Must#8 冻结**：奖励公式/结算/存档语义零改动（rewardSettler 幂等 + 每场 reset；onGarageRetry 不触发结算）。
- **测试**：`tests/garageAdjustRematchP0.test.ts` 12 项全过（T1 result-adjust 上下文置位 + 按钮出现、T2 正常 Garage 无按钮、T3 nav:home 清除、T4 换装后 onGarageRetry 直连 matching、T5 下一局 loadCustom Build 一致（bananaBody）、T6 超载 disabled 无 hitArea、T7 连点只一次、T8 奖励不重复（onGarageRetry 不结算 + 第二局 End +1 只一次）、T9 新 session battle-2 + adjust 清 FX/audio、T10 重启 Build 保持上下文不恢复、T11 420×210 几何 safe 不重叠、T12 正常 Garage 零变化）。
- **E2E**（`tests/_e2e_garage_rematch.cjs`，**59/59 PASS**，4 视口 420dpr1/844dpr1/1280dpr1.5/844dpr3）：Loss→调整配置→Garage（garage-retry 出现）→换装 bananaBody→点 garage-retry→**直接 Matching（无 nav:home 点击，不经过 Home）**→第二局 phase=Active（新 session）+ 像素外观变化（green→orange）+ snapshot Build=bananaBody + reward +1 只一次 + 无红墙残留 + 返回 Home 后 garage-retry 消失。
- **验证门**：全量 vitest **1494 全过**（platformCore 偶发 5s 超时单独跑 23/23 全过）；tsc exit 0；四目标构建全过；repo-health 9/9；干净 HEAD `build:wechat:rc` → badge `#6adc710`、dirty=false、三方一致。
- **经验**：vitest fake timers 重复 `vi.useFakeTimers()` 会重建 clock——onGarageRetry 注册的 matching 定时器将永不触发（phase 卡 matching）→ 测试内只调用一次；Garage 顶栏布局（computeGarageTopBarLayout）是「完成并再战」按钮位置的唯一几何源（与 back/energyLabel 同源，禁止手算）。
- **下一步**：停止。真机复验：战败→调整配置→Garage 顶部出现「完成并再战」暖金按钮→换装→点击直接进匹配→新车外观生效；正常从首页进 Garage 无该按钮；返回首页后再进不显示。
## F-WX-IOS-COLD-BOOT-PREVIEW-P0｜iOS 冷启动车辆缩小——修改前红测完成（4 FAIL 稳定复现，根因锁定）

- **红测载体**：`tests/wechatColdBootPreviewP0.test.ts`（13 tests: 4 FAIL / 9 PASS，`--pool=vmThreads`）。
- **红测矩阵（FAIL）**：
  - R-CB1：Home 稳定帧 35.1% < R2.1 36%（0-insets + 胶囊 fallback right=101.28）。
  - R-CB2：切页 35.1%→48.0%（差 12.9pt）——「切页才恢复」复现（Home/Garage 取景区不同）。
  - **R-CB6：高车存档（pineappleBody）+ 真实 insets（safeArea 44/20/12/16 + 胶囊 fallback）→ Home 20.9% / Garage 21.8%**——用户「缩成极小」直接复现（正常 48% 的一半以下）。
  - **R-CB7B：高车 + 683×314（2048×941÷3）+ DPR3 → Home 28.1% / Garage 28.2%**——真机截图尺寸模型同样跌破。
- **对照组（PASS）**：R-CB6B 矮车（watermelon）+ 真实 insets = 35.1%/48.0% 完全正常 → **高车才是主因**（insets 只是放大因素）；R-CB7 矮车 683×314 = 45.7%/58.3%（short 档偏大，58.3 超 48% 上限，另案）。
- **根因（file:line）**：renderer.ts:2680-2690 previewSolo clamp——`scale = Math.min(hLimit, Math.min(maxS, Math.max(minS, scale)))`，高车 envelope 高（envH≈166 世界 px，含 visual）+ padY（0.14×eh）+ Garage stageRect 高被真实 insets 压缩（231→203）→ safeH≈191 → hLimit≈0.92 锁死 scale → 21.8%。**数学冲突：844×390 布局下高车「完整入画（hLimit）」与「40-48% 宽（maxS/minS）」不可兼得**——hLimit 压过 minS（38% 宽下限失效）。
- **Queue 假设排除（诚实披露）**：时序/readiness/generation 机制不成立——微信 getStorageSync 同步 → loadPlayerBuild 同步 → loadCustomPreview 同步（wechatBattleHost.ts:35-40）→ vehicleBounds 纯几何同步（renderer.ts:590-618）→ **无「vehicleBounds ready 前 reframe」窗口**（DIAG2 证明 init 后即稳定 35.1%，首帧无漂移）。「<10%」路径在代码库不存在：DIAG3 无 framing reframe=34.6%（compact）/50.2%（normal）；DIAG4 resize arena fit 残留=22.5%（非 0.43——VIEW_ZOOM 放大）。
- **切页恢复机制**：dispatch('home-garage')/'nav:*' → reframeCamera → getPreviewFramingRect（lastState 已就绪）→ 正确 framing。矮车 35.1→48；高车 20.9→21.8（几乎不恢复，高车问题持续）。
- **PASS 项**：R-CB3 badge 每帧恰好 1 次（FakeCanvas op.type='rect' 匹配 fillStyle 'rgba(0,0,0,0.55)'）；R-CB4 5 次冷启动恒 35.1%（≤1% 漂移）；R-CB5 Home 首帧 ink=569401>0。
- **待用户拍板（Task #80 修复方向）**：高车「完整入画 vs 40-48% 宽」取舍——a) 允许高车高度溢出取景区（触碰 R2.1 Must#2 完整入画）；b) 压缩底部装配带（strip 30%→更小，触碰 F-GARAGE-CENTER-STAGE-P0 Must#3 30-34%）；c) 高车 padY 收窄 + 接受 36% 下限（部分达标）；d) 其他布局调整。R2.1 测试只用 watermelon 矮车 → 高车从未被覆盖（测试空白）。
