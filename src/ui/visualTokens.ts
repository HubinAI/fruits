/**
 * F-MOBILE-VISUAL-BASE-R1｜统一手机玩家视觉体系（语义视觉源，单一事实来源）。
 *
 * 目标：不等待正式美术资源，先用现有 Canvas 能力建立可演示的统一视觉语言：
 *   深色竞技场背景 + 高饱和水果主体 + 金黄主操作 + 蓝/橙对战阵营。
 *
 * 这是「语义」视觉源——所有玩家界面（CanvasPlayerUIHost）与场景背景（renderer）
 * 共用同一份取值，避免每页像不同原型。禁止只改颜色名称、实际画面不变（见验收）。
 *
 * 语义分组（覆盖 Queue 必改#1）：
 *   - 场景背景 / 地面（深色竞技场）
 *   - 普通面板 / 强调面板
 *   - 主按钮（金黄）/ 次按钮（轻量无重框）
 *   - 主文字 / 说明文字 / 弱文字
 *   - 胜利 / 失败
 *   - 我方蓝 / 敌方橙
 *   - 统一描边强度 / 圆角
 */
export const V = {
  // ---------- 场景背景 / 地面（深色竞技场）----------
  /** 最深顶带（首页背景下沉第一带，保留旧 layering 测试标记色 #0a0d13） */
  arenaBgTop: '#0a0d13',
  /** 中段过渡（由深蓝黑 → 蓝） */
  arenaBgMid: '#0e1626',
  /** 低段（接近地面，更亮蓝） */
  arenaBgLow: '#16243c',
  /** 地平线辉光带 */
  arenaBgHorizon: '#1d3050',
  /** 地面板（偏蓝的深灰，非中性灰） */
  arenaGround: '#26344a',
  /** 地面顶缘线（亮蓝，强调地平线） */
  arenaGroundEdge: '#3f5d82',

  // ---------- 面板（普通 / 强调）----------
  /** 普通面板（偏蓝深底，替代旧的 #242b38 中性灰） */
  panel: '#1b2433',
  /** 强调面板（更亮蓝，用于卡片/选中容器） */
  panelEmph: '#223150',
  /** 实底表面（顶栏 / 卡片底，偏蓝深色） */
  panelSolid: '#141d2c',

  // ---------- 主按钮（金黄主操作）/ 次按钮（轻量无重框）----------
  /** 主操作 = 金黄实底（第一眼最强的操作权重） */
  primary: '#ffb229',
  /** 主按钮上的文字（深底，保证金黄上的可读性） */
  primaryText: '#1c1304',
  /** 主按钮高亮态 */
  primaryBright: '#ffc75a',
  /** 次按钮 = 轻量半透明蓝填充、无重框（与金黄主按钮形成明确主次） */
  secondary: 'rgba(38,54,82,0.85)',
  /** 次按钮文字（明亮，保证可读性） */
  secondaryText: '#cdd9ec',

  // ---------- 文字（主 / 说明 / 弱）----------
  /** 主文字（标题 / 关键值，明亮白蓝） */
  textPrimary: '#eef3fb',
  /** 说明文字（副信息，偏蓝明亮，替代旧灰 #9aa4b5） */
  textSecondary: '#9fb0c8',
  /** 弱文字（禁用 / 极次要，明显弱于说明） */
  textFaint: '#6f8099',

  // ---------- 胜利 / 失败 ----------
  /** 胜利 = 高饱和绿 */
  win: '#37d67a',
  /** 失败 = 高饱和红 */
  lose: '#ff5c6c',

  // ---------- 阵营：我方蓝 / 敌方橙 ----------
  /** 我方阵营 = 高饱和蓝（头像 / 我方 HP / 选中态） */
  ownBlue: '#3d8bff',
  /** 我方蓝高亮描边 */
  ownBlueBright: '#6aa9ff',
  /** 敌方阵营 = 高饱和橙（对手名 / 敌方 HP） */
  enemyOrange: '#ff8a3d',
  /** 敌方橙高亮 */
  enemyOrangeBright: '#ffab66',

  // ---------- 统一描边强度 / 圆角 ----------
  /** 统一描边色（偏蓝，替代旧中性灰 #38414f；用于分组，非重框） */
  border: '#33455f',
  /** 软描边（极低对比，用于轻量次级元素的可辨识，非重框） */
  borderSoft: 'rgba(120,150,190,0.18)',
  /** 大圆角（面板 / 卡片 / Modal） */
  radiusL: 14,
  /** 中圆角（按钮 / 入口 chip） */
  radiusM: 10,
  /** 统一描边宽度 */
  strokeW: 1.5,
} as const;

/** 语义键（供测试断言「不少于这些语义分组已定义」） */
export type VisualTokenKey = keyof typeof V;
