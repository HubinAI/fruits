# 《最强水果》WorkBuddy 交接文档

> 交接时间：2026-08-28 19:16（GMT+8）
> 交接人：WorkBuddy 助手 → 下一个 WorkBuddy 会话
> 本文件目的：让新窗口在**不依赖旧对话历史**的情况下，10 分钟内恢复完整上下文并继续干活。

---

## 1. 交接快照（三路 SHA 硬校验）

| 项 | 值 |
|---|---|
| 仓库路径 | `D:\0818new\最强水果` |
| 分支 | `foundation-02-wechat` |
| **local HEAD** | `f50cb1e70048c2ff8cce1f54c2f8a3ab78fbf12a` |
| **remote HEAD** | `f50cb1e…`（一致 ✓） |
| **线上部署** | `https://hubinai.github.io/fruits/`（assets/index-CY9zdx3H.js = f50cb1e ✓） |
| 工作树 | src/、tests/ 干净；仅 3 个历史 memory 文件未暂存（惯例不处理） |
| 当前 Queue | 无进行中 Queue；**3 个 Queue 已交付部署，均处于「等待真人体验验收」** |

### 最近 3 个已完成 Queue（均已完成 → 部署 → 等待真人验收）

| Queue | Commit | 内容 | 状态 |
|---|---|---|---|
| F-CROSSLAYER-RECT-DPR-P0 | `f52b11a` | 统一 UI 与 Renderer 跨层矩形逻辑坐标（删两处多余 DPR 转换） | ✅ 已部署，等真人验收 |
| F-GARAGE-LIVE-ASSEMBLY-P0 | `348148f` | 车库即时换装 + 真实挂点反馈（RenderHardpoint / hp-sel / 超载守卫 / 吸附反馈） | ✅ 已部署，等真人验收 |
| F-GARAGE-CENTER-STAGE-P0 | `f50cb1e` | 中央战车舞台 + 底部横向装配带（重构 Garage 展示结构） | ✅ 已部署，等真人验收 |

---

## 2. 项目一句话定位

极简策略 + 事前组装 + 自动物理战斗。玩家循环：「我觉得这样能赢 → 看它自己打 → 原来问题在这 → 改一下再试」。
判断机制是否保留的唯一标准：**它是否让玩家做出不同的下一步动作？**

技术栈：TypeScript + Vite + Matter/Planck 物理 + vitest + Playwright（门禁）。目标：Web + 微信小游戏双端，platform-neutral Gameplay Runtime + platform-specific Host 注入式接入，杜绝双端重复实现。

---

## 3. 当前 Garage 状态（真人录屏视角）

F-GARAGE-CENTER-STAGE-P0 重构后：

```
顶部极简栏：[‹ 首页]                    [能量 80/110]

中央舞台：       完整战车（居中，宽约 43% 屏）
        ○真实挂点（白/蓝轮廓=可用，金圈=选中，实心=已占用）
──────────────────────── 展示地面

底部装配带（高≈32% 屏）：
[车身] [移动] [战斗]          ← 第一行三分类
< [部件卡] [部件卡] [部件卡] > ← 第二行横向卡片带（可横滑）
```

关键行为：
- **挂点选择只走战车真实挂点**（hp-sel），文字挂点页签已全部删除
- **点击部件卡一步装备**（无确认/保存），≤100ms 内旧部件消失、新部件出现在真实挂点
- 武器+辅助合并「战斗」分类（卡片带混排 + 小类型标识），不拆顶层
- 返回首页/再进 Garage/进 Battle 配置持久
- 超载/未获得/不兼容：车辆不变，原因显示在装配带内

---

## 4. 架构地图（关键文件 → 职责）

### 渲染与跨层坐标（F-CROSSLAYER-RECT-DPR-P0 战场）
| 文件 | 关键点 |
|---|---|
| `src/render/renderer.ts` | `sx()/sy()` 返回 **logical 坐标**（x×scale+offset）；`ctx.setTransform(dpr)` 绘制域=logical；`getVehicleScreenRects` / `getVehicleHardpointScreenPts` 已删多余 ÷DPR；**域对齐单点 `vk = this.surface ? this.viewDpr : 1`**（Web 无 surface=1 零转换；微信/surface 注入=dpr 对齐）；`isCompactLandscape(viewWidth/vk)`；previewSolo garage 模式宽度 clamp 38~47% |
| `src/battle/battleContract.ts` | `RenderHardpoint{id, kind:'movement'\|'functional', world, occupied}` |
| `src/battle/planckBattleOrchestrator.ts` | `buildVehicleSnapshot` 用 body 位姿 + hardpoint.localPosition（facing 镜像同装配公式）输出挂点世界坐标 |
| `src/main.ts` | Web 玩家模式 battle host 是**对象字面量**（非 wechatBattleHost！）——双端都要接挂点接口 |

### Garage 结构（F-GARAGE-CENTER-STAGE-P0 战场）
| 文件 | 关键点 |
|---|---|
| `src/ui/mobileGarageLayout.ts` | `computeMobileGarageLayout`：stageRect（中央舞台全宽）+ stripRect（底部装配带 h≈32%）；`computeGarageTopBarLayout` garage 模式只 back+energy |
| `src/ui/canvasPlayerUIHost.ts` | `drawGarageMetaPage`（中央舞台 + `drawGarageStrip` 分类tab+卡片带+状态横幅）；`drawPartCard` 横向紧凑卡；`drawVehicleHardpoints` 挂点 overlay；`ensureGarageSlotSelection` 默认选择（惰性：仅无效时触发，防 draw 递归；actions 桩需 `?.()` 双可选）；手势状态机 `gestureDown/Move/Up`（8px 横滑取消点击）；部分可见卡只绘制不注册 hitArea |
| `src/ui/playerUI.ts` | `PlayerUIState`（garageSelected / hardpointScreenPts / overloadDelta / stripScroll 等）；`PlayerUIActions`（onToggleGarageSlot / onPickGarageOption / selectGarageSlot / **reframeCamera** / flashEquip 在 **PlayerUIHost**） |
| `src/game/playerGameRuntime.ts` | `onPickGarageOption` 装备 → 超载预检守卫（computeEnergy>capacity → 回滚 prev 快照不修改车辆 + overloadDelta）→ `refreshFromEdit` → showPreview → loadCustomPreview；`ensureGarageSlotSelection` 在 host |
| `src/render/renderer.ts` | `drawGarageBackdrop`（深蓝车库+灯光）；`setGarageBackdrop`/`setHomeBackdrop`/`setPrebattleBackdrop`/`setBattleBackdrop` 互斥开关 |

### 输入层（F-GARAGE-CENTER-STAGE-P0 新增手势）
| 文件 | 关键点 |
|---|---|
| `src/platform/types.ts` | `PlatformInput.bindGesture(target, {onDown,onMove,onUp(x,y,cancelled)}, toLogical?)`；`PointerGestureHandlers` |
| `src/platform/web/input.ts` | bindGesture：pointerdown/move/up + 兜底 mousedown/touchstart；逻辑坐标转换 |
| `src/platform/wechat/input.ts` | bindGesture：onTouchStart/Move/End/Cancel；`this.wx` 是 getter，bindGesture 内部用 `this.wx()`（已修复）；微信端 toLogical 归一化 |

---

## 5. 验证工具箱（直接可复制）

```bash
# 基线确认
git rev-parse HEAD && git ls-remote origin foundation-02-wechat | cut -f1

# scoped tsc（先查后改后的第一道门）
npx tsc --noEmit -p tsconfig.json

# targeted / Garage 相关 / 全量 vitest —— 【必须带 --pool=vmThreads！】
npx vitest run --pool=vmThreads --testTimeout=30000 tests/xxx.test.ts
npx vitest run --pool=vmThreads --testTimeout=30000

# 四目标构建
npm run build && npm run build:wechat && npm run build:pages && npm run build:e2e

# e2e 门禁 serve（玩家模式，含 __probe / __h）
pkill -f "_serve_pages" 2>/dev/null; sleep 1
(E2E_DIR=e2e node tests/_serve_pages.cjs > /tmp/serve.log 2>&1 &) ; sleep 2
curl -s -o /dev/null -w "8138=%{http_code}\n" --max-time 3 "http://127.0.0.1:8138/?player=1"

# 像素门禁（Garage 中央舞台）
PROBE_BASE="http://127.0.0.1:8138/" ENVS="420x210@1,621x351@1,844x390@1,1920x1008@1.5" node tests/_e2e_garage_center.cjs

# commit + push + 部署
git add <files> && git commit -m "..." && git push origin foundation-02-wechat
gh workflow run pages.yml --ref foundation-02-wechat && gh run list --workflow=pages.yml --limit 1
gh run watch <run_id> --exit-status; gh run view <run_id> --json conclusion,headSha
```

**浏览器内调试口**（e2e 构建 player=1 模式）：
- `globalThis.__probe`：playerPhase / garageCategory / vehicleRects / stripScroll 等
- `globalThis.__h`：`hitAreas`（读 id/x/y/w/h 定位点击坐标）、`handlePointer(x,y)`（logical 坐标直接派发）、`lastState`、`draw()`（强制重绘）
- 截图检查：`document.querySelector('canvas')` → `toDataURL()`（注意 backing 固定 844×390 logical，DPR 走 CSS contain）

---

## 6. 已知坑（必须避让）

1. **vitest 默认池在本机必挂**：`node_modules/vitest/vitest.mjs run` 不带 `--pool=vmThreads` 会全部报 `Cannot read properties of undefined (reading 'config')`。**所有 vitest 命令必须带 `--pool=vmThreads`**。
2. **git ref 写入不可靠**：此环境 `git commit` 后分支 ref 常不落盘；`git update-ref`/`git branch -f` 静默失败。可靠恢复：commit 后**立刻 `git log -1` 验证**；未落盘则 `git cat-file -t <sha>` 确认对象完整后用 `mkdir -p .git/refs/heads/<branch> && printf '%s\n' <full-sha> > .git/refs/heads/<branch>` 手动写，再 push。
3. **git status 把 `planck` 显示成 `plank`（无 c）**：中文路径显示伪影，真实文件名始终是 `planck`。用 `ls <dir>/` 拿真实拼写。
4. **8138 端口残留 serve 是陈旧构建**：改 src 后必须 `pkill -f "_serve_pages"` 再起，否则门禁跑的是旧代码（stale-recording 陷阱）。曾用 PowerShell 按端口杀（`Get-NetTCPConnection -LocalPort 8138`）。
5. **测试桩缺微信 onTouchEnd**：bindGesture 绑定 onTouchStart/Move/End/Cancel；旧测试桩只模拟 start → 报错。wechatInputContract / wechatPlayerSmoke 已补 `onTouchEnd` 与 `touchEnd` getter，**新测试照此补全**。
6. **测试桩 actions 需 `?.()` 双可选**：`ensureGarageSlotSelection` 对缺失方法（桩）会抛——所有可选 action 调用用 `this.actions?.onToggleGarageSlot?.(sel)` 形式。
7. **rm 被 safe-delete shim 拦截**（中文路径 fail-closed）：清理临时文件用 `rm -f` 会静默失败——临时文件未跟踪无害，无需强行清理；commit 不会包含它们。
8. **Web 玩家模式 battle host 是 main.ts 对象字面量**：加渲染相关接口时 wechatBattleHost 和 main.ts **两处都要接**（F-GARAGE-LIVE-ASSEMBLY-P0 教训：忘了 main.ts 导致 hardpointScreenPts=[]）。
9. **跨页 transform stale**：不进 Garage 时 renderer.transform 停留 home 布局 → 进 Garage 后车辆偏。F-GARAGE-CENTER-STAGE-P0 已加 `PlayerUIActions.reframeCamera`（host 在 nav:*/home-garage dispatch 时调用）。后续加页面切换要注意同样问题。
10. **`text()` 有 maxWidth 参数**（第 8 参）；字号 floors 被测试守卫（Mobile 首屏 ≥7/9/10/14 视方法）。

---

## 7. 项目边界（红线，方案漂移立即提醒不默认实现）

- **物理**：2D 侧视（X/Y 平移 + Z 轴旋转），禁 3D Roll/Yaw。数字定能力，Physics 定表达。同 Build 同条件结果大体稳定，禁随机命中/伤害/散布。
- **四类结构**：Body（无主动攻击）/ Movement（V1 只 Wheel）/ Weapon（直接 HP 伤害）/ Gadget（改碰撞/距离/姿态，不带大额 Direct Damage）。
- **Slot/Energy/Mass 分工**：Slot 限空间、Energy 限装配预算（超载直接拒绝）、Mass 制造物理后果（禁 MaxWeight 硬门槛）。
- **Auto Battle**：玩家不操作，AI 不替玩家聪明赢。V1 持续朝唯一敌人驱动，不后退/拉距/自动瞄准。允许挥空/射空/互扰。
- **Arena**：V1 平地 + 左右普通墙；Convergence 四阶段；结果只有 Win/Lose。
- **Quality**：白绿蓝紫橙红粉彩；跨品质核心 Behavior/物理身份稳定，成长走 HP/Energy/Functional Hardpoint。
- **Physics Lab**：必须直接调正式 Battle Runtime，禁测试场专用物理；三层体验验收必须 1x + Debug 关闭。
- **漂移红线**：复杂职业 AI / 自动瞄准 / 自动拉距 / 大量隐藏 Buff / Body 免费攻击 / Weapon-Gadget 独立槽 / 高品质全维度碾压 / 复杂机关成胜负主体 / MaxWeight 硬限制 / 动画保证命中 / Gadget 偷偷大额伤害 / 大量例外规则 / 稳定最优 Build 通吃但只加 Content / Debug 成立但玩家感知不到。

**冻结守约（最近 Queue 的 Forbidden 惯性）**：不修改 Physics/伤害/质量/速度/能量数值/库存结果；不修改首页/Matching/Locked/Battle/Result（除非 Queue 明确要求）；不拆武器辅助顶层分类；不加确认/保存按钮；不加新页面层级；不实现部件拖拽（后续 Queue 前先问）；不修改单 Canvas/DPR/PlayerViewportTransform/跨层矩形换算。

---

## 8. 工作纪律（用户硬要求）

1. **先调查后修改**（先查后改）：报告「问题发生在哪一段」+ file:line 证据，再动手；强证据驱动，禁止臆测改码。
2. **Queue 指令五要素**：禁止 / 必改 / 验收 / base-branch / HEAD SHA。
3. **标准交付动作**：investigate → 最小实现 → targeted tests → scoped tsc → 全量 vitest → web/wechat/pages/e2e 四构建 → **单 commit + push** → 三路 SHA 核对（local = remote = server badge）→ memory log。
4. **三层验收**：技术正确（tests/build）→ 方案落地（正式 Runtime）→ 实际体验成立（1x + Debug 关，玩家能感知/理解/归因/决策）。只有第三层通过才算设计完成；**开发完成 ≠ 体验通过 ≠ 进入正式主线**。
5. **stop-and-wait**：每条 Queue 完成后单 commit + push，**停下等待真人体验回执再开下一条**，禁止顺手扩展范围。
6. **诚实披露**：如实报告环境差距（不得用 Web 构建冒充 WeChat，不得伪造缺失工具）；响应用详细状态表格 + bullet 摘要 + A/B/C/D/E/F 验收矩阵。
7. **回复语言**：简体中文；代码/标识符英文；引用具体 file:line。

---

## 9. 下一步建议（未决事项）

- **首要**：等待真人体验验收 F-GARAGE-CENTER-STAGE-P0（已部署 f50cb1e）。用户会用 iPhone 真机微信预览 + 桌面浏览器确认。
- 待用户回执后：按反馈修体验问题（可能是中央取景/挂点触控/卡片密度等细节）。
- 可能的后续方向（**不要自作主张开**）：Garage 美术润色、微信端 Garage 触控调优、F-WX 系列后续 Queue、战斗内容扩展。均需用户新 Queue 指令。

---

## 10. 用户偏好速查

- 架构主导型产品/UX 负责人；纪律严明；先调查后修改；强证据驱动。
- 期望：详细状态表格、bullet 摘要、诚实的环境差距披露、local=remote=commit 三路 SHA 验证、file:line 证据、A/B/C/D/E/F 验收矩阵。
- 用 Apple ID + iPhone 做微信真机预览；Devtools 默认打开 `dist-wechat/`。
- 任务 Queue 前缀 F-WX-N / F-XXX；每条指令带五要素结构。
