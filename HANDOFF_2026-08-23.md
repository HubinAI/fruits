# 《最强水果》交接文档 — 2026-08-23

> 本文用于把当前聊天窗口（2026-08-22 晚 ~ 2026-08-23 上午，Q15-UI-R2-RECOVER → Q17）的上下文交接给新窗口。
> 新窗口请先读 `D:\0818new\最强水果\.workbuddy\memory\MEMORY.md`（项目长期基线）与本文件，再读 `.workbuddy\memory\2026-08-22.md` / `2026-08-23.md`（当日流水）即可无断点续作。
> 若 MEMORY.md 与本文冲突，以 MEMORY.md 为准（本文是其 2026-08-23 快照）。

---

## 0. 一行恢复指令（新窗口先跑）

```
先 Read D:\0818new\最强水果\.workbuddy\memory\MEMORY.md 和 D:\0818new\最强水果\HANDOFF_2026-08-23.md 和 D:\0818new\最强水果\.workbuddy\memory\2026-08-23.md，
然后 cd /d/0818new/最强水果 跑：git status / git log --oneline -6 / git branch --show-current / git rev-parse HEAD / netstat -ano | grep :5173
确认 HEAD=2c29698、分支=foundation-02-canonical、远端 ls-remote 一致、Dev Server 在 5173 监听（Badge=2c29698），再等我下一条任务。
```

---

## 1. 项目核心定位（一句话）

极简策略 + 事前组装 + 自动物理战斗。玩家循环：「我觉得这样能赢 → 看它自己打 → 原来问题在这 → 改一下再试」。判断机制是否保留的唯一标准：**它是否让玩家做出不同的下一步动作？**

## 2. 指令优先级（3 级）

1. **最新明确的单次 WorkBuddy 执行指令**（最高）
2. **项目稳定边界（MEMORY.md / 本文）**
3. 历史设计草稿 / 旧方案

冲突处理硬规则：最新任务与核心定位冲突时，**不自行默默实现冲突方案**，必须先指出「冲突点 + 体验/架构后果 + 最小建议修正」。

---

## 3. 当前状态（2026-08-23 09:35）

| 项 | 状态 |
|---|---|
| 分支 | `foundation-02-canonical` |
| 本地 HEAD | `2c29698`（`2c296980309a3aa7b7584e2a2617e4cdd48e8f8a`） |
| 远端 `refs/heads/foundation-02-canonical` | `2c29698`（已 `ls-remote` 核验） |
| Dev Server | PID 23468 监听 127.0.0.1:5173（Badge 显示 `2c29698`，VITE v8.2.1，HTTP 200） |
| targeted tests | 50/50（q15PlayerLoop 30 + q15PlayerShellVisibility 5 + q15FlowAutoBattle 6 + q17DriveProbe 9） |
| scoped tsc --noEmit | 0 errors |

**三端 SHA 一致性（硬验收惯例）**：local HEAD = remote foundation-02-canonical = 页面/服务器 Badge，三者为同一 SHA 才算交付完成。

### 本会话（08-22 晚 ~ 08-23 上午）完成队列（按时间序）

- **Q15-UI-R2-RECOVER**（commit `2cc9004`）：修复玩家 Shell 元素永远不可见——真根因是 `playerTop/garageDock/matchingVs/matchBar` 的 CSS 默认 `display:none`，而 `applyPlayerShell` 显示时用 `style.display=''` 回退到 CSS 的 none → 永远隐藏（"只有车+开发工具"）。修复：新增纯函数 `src/ui/playerShell.ts: computePlayerShellVisibility()`（显示分支一律返回明确 `flex/block`，绝不返回 `''`），main.ts 的 `applyPlayerShell` 改用该函数。`matchBar`（className=start-bar，CSS 默认 flex）也显式 `flex`。
- **Q15-FLOW-R1-ATOMIC**（commit `ba53dd5`）：匹配完成直接开战——`goToMatchPreview()` 末尾：`matchBar.style.display='none'`（同一同步任务内改，按钮永不闪现）+ `setTimeout 250ms` 调现有 `startBattleWithReady()`（带 guard：`playerPhase==='matchPreview' && battleState==='editing'`）。Result「下一场」天然复用 startMatching → 自动下一场。
- **Q16**（commit `3caf66a`）：对手池铺量 6→24 套（6 类战斗身份 × 4 套 + 轮径 12/20/26 五类组合）。`opp()` 轮径改必填参数。内容零新增。详见 §4。
- **Q17-MOVEMENT-PROBE**（commit `2c29698`）：站桩 vs 前进 接敌方式验证探针。详见 §4。

### 挂起项

- **Q14-C（HOLD）**：等你重设计后再处理（方案漂移红线：复杂机关成胜负主体 / Body 免费攻击）。
- **Q17 探针的"下一步"决策**：探针证明"不主动前进"确实形成不同接敌过程；是否把"按侧驱动"接缝用于 Q15 玩家流程（让远程/控距类对手真正站桩）需你拍板，见 §11。

---

## 4. 本会话交付明细

### Q15-UI-R2-RECOVER（2cc9004，3 文件）
- `src/ui/playerShell.ts`（新增）：`computePlayerShellVisibility(uiMode, battleState, playerPhase)` 纯函数，5 元素（playerTop/garageDock/matchingVs/matchInfo/matchBar）显示分支返回明确 display（playerTop/garageDock/matchInfo/matchBar='flex'、matchingVs='block'），**禁止 `''`**。
- `src/main.ts`：`applyPlayerShell` 改用该函数赋值。
- `tests/q15PlayerShellVisibility.test.ts`（新增 5 cases）：Garage/Matching/MatchPreview 三态 + 空串护栏 + Scenario/Fighting/Ended 全隐藏。

### Q15-FLOW-R1-ATOMIC（ba53dd5，2 文件，main.ts +10 行）
- `goToMatchPreview()` 末尾：`matchBar.style.display='none'` + 250ms 后 `startBattleWithReady()`（带 guard）。不复制第二套 READY/Battle 逻辑。
- 关键接线事实：`playerPhase='matchPreview'` 全仓唯一赋值点 = goToMatchPreview(1089)；`btnFight.onclick = startBattleWithReady`（函数引用）；Result 下一场 = `nextMatch → startMatching()`；调整配置 = `btnAdjust.onclick = adjustConfig`。
- `tests/q15FlowAutoBattle.test.ts`（新增 6 cases，源码级护栏：main.ts 不可在 node 导入）：matchBar 隐藏、250ms 自动调用、guard、单一实现、下一场复用、调整配置接线。

### Q16（3caf66a，2 文件）
- `src/player/opponentPool.ts`：`opp(body, wheels, selections)` 轮径必填；24 套 = 远程压制/近距离爆发/持续贴身/冲锋接敌/控距干扰/混合型 × 4；轮径组合覆盖 前小后大/前大后小/双小/双标准/双大；西瓜/香蕉各 12；全部 ≤ 容量（西瓜110/香蕉90），无 HOLD。
- `tests/q15PlayerLoop.test.ts`：6→24；+7 cases（Validator 全过、轮径档位全覆盖、五类组合、去重三元组、Body 覆盖≥10、六类身份≥2、mulberry32 确定性 20 连抽无相邻重复）。

### Q17-MOVEMENT-PROBE（2c29698，4 文件）
- 真实链路结论：`buildSnapshotFromDraft` 恒生成双 wheelStd（无"无 Movement"）；`drivePlanckVehicle` 已支持 `enabled:false`；缺口仅是 `BattleConfig.autoDrive` 是**全局**布尔 → 缺按侧控制。
- `src/battle/battleContract.ts`：`BattleConfig` 加可选 `sideDrive?: { a?, b? }` + 纯函数 `resolveDriveEnable()`（未指定侧跟随 autoDrive，既有行为不变）。
- `src/battle/planckBattleOrchestrator.ts`：step 按侧门控（未驱动侧不调 drivePlanckVehicle → wheel motor 关闭，站桩 = 真实物理可被推动/翻转）。
- `src/lab/scenarios.ts`：`q17RangedBuild`（西瓜+机枪）/ `q17MeleeBuild`（西瓜+圆锯）+ Q17-A `{autoDrive:true, sideDrive:{a:false,b:true}}` / Q17-B `{autoDrive:true}`（对照，唯一变量=drive）。
- `tests/q17DriveProbe.test.ts`（9 cases）。
- **探针运行时结论**（系统 Edge 分时截图）：Q17-A t=1000ms A 原位射击/B 已接近 → t=2500/4000 接触 → t=6000 A 被 B 真实推动（非固定炮塔）；Q17-B t=1000ms 双方已在中央接触。两者肉眼明显不同。

---

## 5. 关键边界速查（勿破）

- **物理**：2D 侧视（X/Y 平移 + Z 轴旋转），禁 3D Roll/Yaw。数字定能力，Physics 定能力能否表达。同 Build 同条件结果大体稳定，禁随机命中/伤害/散布。
- **四类结构**：Body（碰撞几何/质量分布，无主动攻击）、Movement（V1 只 Wheel，真实 Ground Contact 驱动）、Weapon（直接 HP 伤害）、Gadget（改碰撞/距离/姿态，不带大额 Direct Damage）。
- **Slot/Energy/Mass 分工**：Slot 限空间、Energy 限装配预算（超载直接拒绝穿戴）、Mass 制造 Runtime 物理后果（禁 MaxWeight 硬门槛）。
- **Auto Battle**：玩家不操作，AI 不替玩家聪明赢。V1 持续朝唯一敌人驱动，不后退/拉距/自动瞄准。攻击方向来自 Body 姿态 + Hardpoint 世界方向。允许挥空/射空/互扰。（Q17 探针新增：**按侧驱动 sideDrive 是唯一已开放的"不前进"接缝**，只能用于场景/实验，正式玩家流程默认仍双方驱动。）
- **Damage/Force 独立**：Damage 不自动映射 Knockback。Physics Contact 统一进 Contact Router，Damage 统一进 Damage Resolver，Renderer 不参与判定。
- **Arena**：V1 平地 + 左右普通墙。Convergence = Active→Warning→Closing→End（10s/3s/5s），刺墙真实 Collider + Hazard。结果只有 Win/Lose，不建 Draw（同帧双死随机兜底）。
- **Quality**：白绿蓝紫橙红粉彩。跨品质核心 Behavior/物理身份稳定，尺寸/Collider/Mass/COM 基本不变，成长走 HP/Energy/Functional Hardpoint。
- **Physics Lab**：必须直接调正式 Battle Runtime，禁测试场专用物理。第三层体验验收必须 1x + Debug 关闭。

## 6. 三层验收

1. 技术正确（tests/build/Runtime）
2. 方案落地（设计进正式 Runtime）
3. 实际体验成立（正常速度 + Debug 关 + 不听解释，玩家能感知/理解/归因/决策）

只有第三层通过才算设计完成。**开发完成 ≠ 体验通过 ≠ 进入正式主线。**

## 7. 方案漂移红线（出现即主动提醒，不默认实现）

复杂职业 AI / 自动瞄准 / 自动拉距 / 大量隐藏 Buff / Body 免费攻击 / Weapon-Gadget 独立槽 / 高品质全维度碾压 / 复杂机关成胜负主体 / MaxWeight 硬限制 / 动画保证命中 / Gadget 偷偷大额伤害 / 大量例外规则 / 稳定最优 Build 通吃但只加 Content / Debug 成立但玩家感知不到。

---

## 8. Canonical Physics Foundation（2026-08-17，feature/foundation-02-canonical）

统一后的物理基线，后续所有 Queue 必须基于此（勿再用旧 gravity 0.01）：
- **gravity.scale = 0.0001**（唯一值）。根因：Matter Verlet 积分 `velocity += (force/mass)*deltaTime²`，FIXED_DT²≈277.8 放大 gravity.scale；0.01 会让 body.velocity.y 单调累积到 ~278。
- **固定步进**：Drive/Behavior 必须在每个 FIXED_DT 的 Engine.update 之前执行（PhysWorld.step 的 onBeforeStep 回调）；战斗时间按 steps*FIXED_DT 推进。
- **setMeta 已传播到 compound sub-part**：碰撞事件 pair.bodyA/bodyB 是 sub-part，不传 meta 则 Contact Router 读不到 Owner。
- **【F-02A 已解决】接触相对速度**：dispatch 用 `collision.parentA/parentB` + `vPoint = vCOM + ω×r`。
- **Matter 速度读取时机铁律**：collisionStart/collisionActive 回调内 velocity 才是真实值；Resolver 在事件后回写。
- **git 身份**：本仓库 `xiaoyue <xiaoyue@local>`（新会话需重设）。push 走 SSH（`git@github.com:HubinAI/fruits.git`）。
- **git ref 写入不可靠（重要）**：commit 后必须立刻 `git log` 验证 ref 落盘；未落盘用 `mkdir -p .git/refs/heads/<branch> && printf '%s\n' <full-sha> > .git/refs/heads/<branch>` 手动写，再 push。

---

## 9. 环境坑（实测，必读）

- **vitest 必带 `--pool=vmThreads --maxWorkers=1`**（vitest 4.1.10 / Node 24 / Windows 双实例故障：默认池报 `reading 'config'` 全失败；带 vmThreads 即正常）。命令：
  `"C:/Users/62520/.workbuddy/binaries/node/versions/22.22.2/node.exe" node_modules/vitest/vitest.mjs run <file> --pool=vmThreads --maxWorkers=1`
- **受管 node 路径**：`C:\Users\62520\.workbuddy\binaries\node\versions\22.22.2\node.exe`（node_modules 内的 tsc/vitest/vite 都用它跑）。
- **Dev Server 重启**：Bash `netstat -ano | grep ":5173"` 拿 PID → PowerShell `Stop-Process -Id <PID> -Force` → Bash `run_in_background` 重启 `node node_modules/vite/bin/vite.js --port 5173 --strictPort`（PATH 前置受管 node）。Badge 在启动瞬间从 git HEAD 重算。
- **双 listener 排查（2026-08-23 Q16 遇到）**：`netstat -ano | grep ":5173.*LISTENING"` 出现两个 PID 时，用 `ps -ef | grep vite.js` 区分「受管 node + 本项目命令」（要留）与「系统 node `C:\Program Files\nodejs` 残留」（要停，可能服务旧代码）。
- **CSS display 铁律（2026-08-22 RECOVER 教训）**：凡 CSS 默认 `display:none` 的元素，JS 显示时**禁止用 `style.display=''`**（回退 none → 永远不可见），必须用明确值（flex/block）。
- **"真人录像显示旧 SHA"判定法**：先 `git ls-remote origin foundation-02-canonical` + 看运行服务器启动日志 HEAD；git 实际一致时，录像旧 = 录像早于 Dev Server 重启，不必重做。
- **工具层坑（中文路径 + plank/planck 伪影）**：git status 把 `planck` 显示成 `plank`（无 c）是伪影，真实文件名始终 `planck`；用 `ls <dir>/` 拿真实拼写。Read 工具对绝对中文路径偶发失败 → Bash 相对 `cat` 兜底。
- **浏览器运行时验收（2026-08-22 起复用）**：playwright 装在受管 Python venv（`C:\Users\62520\.workbuddy\binaries\python\envs\default`），用 `channel="msedge"` 驱动系统 Edge（无需下载 Chromium）。验证脚本放 `_verify_q15recover/`（未跟踪，可删）。

---

## 10. 关键文件地图（近期）

| 文件 | 作用 |
|---|---|
| `src/main.ts` | 玩家流程编排（uiMode/battleState/playerPhase）、`applyPlayerShell`（Shell 可见性单一真相源）、`goToMatchPreview`（250ms 自动开战）、Garage Dock / Matching / MatchPreview |
| `src/ui/playerShell.ts` | **Q15-UI-R2-RECOVER 新增**：`computePlayerShellVisibility()` 纯函数（明确 display，禁空串） |
| `src/render/renderer.ts` | CameraFit 含 previewSolo/previewFixed；`setPreviewVehicleFx`（B 候选淡入缩放，仅 B） |
| `src/player/opponentPool.ts` | **Q16**：24 套对手 + `opp(body, wheels, sel)` + `pickRandomOpponent` / `buildMatchingSequence`（不连续重复） |
| `src/battle/battleContract.ts` | BattleConfig（含 **Q17** `sideDrive`）+ `resolveDriveEnable()` 纯函数 |
| `src/battle/planckBattleOrchestrator.ts` | step() 按侧门控驱动（Q17）；正式 Planck 战斗编排 |
| `src/lab/scenarios.ts` | 全部 DEV 隔离场景（含 **Q17-A / Q17-B** 站桩探针；Q14-A/B 隔离验收等） |
| `src/core/buildValidator.ts` | `validateSnapshot`（≥1 Weapon / Energy≤容量 / hardpoint 合法） |
| `src/core/content.ts` | 正式 bodies（西瓜 cap110/hp1100、香蕉 cap90/hp900）+ 部件 energy/category |
| `src/core/partOptions.ts` | 正式部件枚举（炮/锤/推杆/刺/镭射/冲锤/圆锯/霰弹炮/推进器/机枪/喷火器） |
| `tests/q15PlayerLoop.test.ts` | Q15/Q16（30 cases：对手池 24 套 + 持久化 + 铺量覆盖） |
| `tests/q15PlayerShellVisibility.test.ts` | RECOVER 回归（5 cases） |
| `tests/q15FlowAutoBattle.test.ts` | ATOMIC 源码护栏（6 cases） |
| `tests/q17DriveProbe.test.ts` | Q17 探针（9 cases） |

---

## 11. 续作建议（下一步）

1. **短期验收**：浏览器开 `http://127.0.0.1:5173/`：① 刷新进 Garage（我的战车 + Dock + 寻找对手，无左栏）；② 点「寻找对手」→ 候选 4 次切换 → 锁定「对手已找到」→ 250ms 自动 READY → 自动开战（**全程零二次点击**）；③ 连打多局看对手池多样性（24 套、轮径差异）；④ Result「下一场」自动进入下一场 / 「调整配置」回 Garage。
2. **Q17 探针决策点（等你拍板）**：探针证明「不主动前进」在现有 Physics 下会自然形成不同接敌过程（A 原地射击 / B 主动接近，且 A 受撞仍被真实推动）。**建议方向**：若想让 Q16 的"远程压制/控距"数据分类在玩家流程中真正差异化，最小路径是沿 `sideDrive` 接缝让部分对手（如远程压制类）在正式战斗里 motor off 站桩——但这触碰"Auto Battle V1 持续朝唯一敌人驱动"的既有边界，需你确认是否开启；若否，则保持现状（所有对手都前进贴脸）。不默认实现。
3. **Q14-C（HOLD）**：等你 redesign 方案（避开红线）后开工。
4. **常规**：新 Queue 接续时先确认是否触碰 §5 冻结边界；vitest 带 `--pool=vmThreads --maxWorkers=1`；commit 后 `git log` 验 ref；push 走 SSH；改完重启 Dev Server 并确认三端 SHA 一致。

---

## 12. 验证命令速查

```bash
# 测试（务必带 --pool=vmThreads）
NODE="C:/Users/62520/.workbuddy/binaries/node/versions/22.22.2/node.exe"
$NODE node_modules/vitest/vitest.mjs run tests/q17DriveProbe.test.ts tests/q15PlayerLoop.test.ts tests/q15PlayerShellVisibility.test.ts tests/q15FlowAutoBattle.test.ts --pool=vmThreads --maxWorkers=1
$NODE node_modules/vitest/vitest.mjs run --pool=vmThreads --maxWorkers=1   # 全量

# 类型
$NODE node_modules/typescript/bin/tsc --noEmit -p tsconfig.json

# 状态 / 推送核验
git status
git log --oneline -6
git branch --show-current
git rev-parse HEAD
git ls-remote origin foundation-02-canonical

# Dev Server 重启
netstat -ano | grep ":5173"                     # Bash 拿 PID（注意双 listener）
# Stop-Process -Id <PID> -Force                 # PowerShell 终止
# Bash run_in_background:
#   PATH="/c/Users/62520/.workbuddy/binaries/node/versions/22.22.2:$PATH" nohup node node_modules/vite/bin/vite.js --port 5173 --strictPort > /tmp/vite.log 2>&1 &
```
