# 《最强水果》V0.8 Release Candidate 验收清单

> Queue：Q31｜V0.8 Release Hardening
> 基础：`foundation-02-canonical`（上一 Queue 最新 HEAD `da2e59c` 之上）
> 目标：形成第一个可发测试包的 Release Candidate；本轮**禁止新增玩法**（无新武器 / 新成长 / UI 大改 / 平衡大改）。

---

## 0. 发布标识（Release Identity）

| 项 | 值 |
| --- | --- |
| 版本号（APP_VERSION） | `0.8.0-rc` |
| 分支 | `foundation-02-canonical` |
| Release Build SHA | `4afbc000f82d99c03f53c694eb5f4d1eafdb185c`（见文末「三路 SHA 一致性」） |
| 运行时模式 | `dev` / `test` / `prod` 三态（见 §10） |
| 验收测试 | `tests/q31Stability.test.ts`、`tests/q31Recovery.test.ts` |

**三路 SHA 一致性（硬验收）**：本地 `git HEAD` = 远程 `origin/foundation-02-canonical` = 运行 Dev Server 的 Badge SHA，三者必须一致，否则视为「未真实部署」。

---

## 1. 平台 Build（Platform Build）

| 构建目标 | 命令 | MODE | 用途 |
| --- | --- | --- | --- |
| 正式发布 | `npm run build` | `production` | 外发 PROD 包（隐藏 DEV 工具 / 关调试日志） |
| 内部 RC | `npm run build:test` | `test` | 真人 RC 验收（保留 QA 工具） |
| 本地开发 | `npm run dev` | `development` | 本地调试 |

- 构建产物 `dist/` 由 Vite 产出；平台层（微信小游戏等，Q29）通过 `setPlatformAdsAdapter` 注入，不改动业务层。
- 验收动作：分别执行 `npm run build` 与 `npm run build:test`，确认均零错误产出 `dist/`；RC 包交付 QA、PROD 包待真人验收通过后发布。

---

## 2. 新账号（New Account）

- 首启无存档 → `ensureInventory(build)` 种子默认 starter 库存（cannon/hammer/pushRod/spear 各 1★），不阻塞开局。
- 首轮引导：全新账号首场获得新部件时提示「回车库调整」（Q26），闭环后隐藏。
- 验收：`tests/q31Recovery.test.ts › ensureInventory 对坏存档幂等` 覆盖坏存档回退 + 不重复种子；`tests/q31Stability.test.ts › 零存档新账号 getInventory 不抛错` 覆盖无 localStorage 环境。

---

## 3. 存档（Save / Migration）

- 统一信封 `saveVersion`：每条记录带 `__v`；字段级安全，单字段非法/缺失只回退该字段默认，不波及他键、不清空整档（Q27）。
- 异常恢复：脏 JSON / 旧版 v0 owned-id 数组 / 部分对象（缺键、类型非法）均归一为合法 `PartInventory`，无抛错。
- 验收：`tests/q31Recovery.test.ts › 页面刷新 / 存档字段缺失迁移`（脏 JSON、部分对象、v0 数组迁移、ensureInventory 幂等，共 4 例）。

---

## 4. 广告（Ads / IAA）

- `RewardedAdClaimer` 以「本场 result 引用」为幂等键，`reset()` 在每场 Result 显示前调用 → 同场只发一次额外金币（Q30）。
- 仅 `completed` 发奖；`failed` / `dismissed` / `no_fill` 均不发，且允许重试（不锁定）。
- 平台适配：`DEV` 用 MockAdsAdapter；`PROD` 有 `platformAdapter` 则转发，否则 NoopAdsAdapter（视为无填充，主流程照常继续）。
- 验收：`tests/q31Recovery.test.ts › 广告失败不发奖且不卡死`（failed / dismissed / 网络异常均不发奖且不卡死；completed 发一次 +50；同场重复 claim 拒绝）。

---

## 5. 埋点（Analytics）

- `isAnalyticsDev()` 现统一由 `ANALYTICS_DEV`（`!IS_PROD`）驱动（Q31 重构，业务层不再直接读 `import.meta.env.DEV`）。
- `dev`/`test`：输出 console + 内存 sink（可观察、可测）；`prod`：默认 no-op。
- 关键事件：`battle_end` / `reward_gain` / `rank_change`，均由 `battleEndGuard.firstTime(r)` 去重（每场 Result 只触发一次，防每帧 poll 重复）。
- 验收：`tests/q31Recovery.test.ts › battleEndGuard：同一 result 引用只触发一次埋点`。

---

## 6. 后台恢复（Background → Foreground）

- 游戏主循环 `dt = Math.min(50, now - last)`（main.ts:2216）钳制单帧步长，后台长时间挂起回前台不会造成物理爆发。
- Runtime 自身对单次巨大 dt 也保持有限（defensive）：即便不钳制，步进结果仍有限、可继续抵达 End。
- 验收：`tests/q31Recovery.test.ts › 后台→前台 dt 钳制韧性`（单次喂 5000ms dt 后仍有限并抵达 End）。

---

## 7. 广告-网络失败（Ad Network Failure）

- `RewardedAdClaimer.claim()` 对 `showRewardedAd()` 的任意异常 `catch` 后返回 `{ granted: false }`，不发奖、不卡死、不阻塞后续流程。
- `tryInterstitialSafe(proceed)`：插屏任何结果（含异常/无填充）都**最终调用 `proceed()`**，绝不卡住 Result / Garage / 下一场。
- 验收：`tests/q31Recovery.test.ts › 插屏 tryInterstitialSafe 任何结果/异常都继续主流程`（无 adapter + 注入 reject 平台 adapter，两例均 proceed）。

---

## 8. 多分辨率（Multi-Resolution）

- 渲染相机 `fit` 系统：正式战斗 `fit:'battle'` 仅 Battle start / phase 切换 / resize 时构图一次（`renderer.ts`），完整入画为硬约束，运行期不呼吸、不跟随。
- 支持常见移动分辨率（如 750×1334 / 828×1792 / iPad 类）。**需真机或模拟器多分辨率 smoke 一遍**（无自动化用例，属手动验收项）。

---

## 9. 50 场稳定性（50-Battle Runtime Stability）

- 头less 连续 50 场 Battle（直接调用正式 Battle Runtime，禁第二套物理），覆盖武器池：cannon / hammer / pushRod / saw / shotgun / thruster / machineGun / flamethrower。
- 检查项全部通过：
  - **NaN / Infinity**：车辆位置 / 线速度 / 角度 / 角速度 / HP 全程有限；
  - **Unhandled Error**：单步 step 抛错即判失败（0 抛错）；
  - **状态机卡死**：50/50 场均在 4000 步内抵达 phase `End`；
  - **timer / listener 残留**：`process._getActiveHandles()` 开战前/清场后无新增持久句柄（无 setInterval/setTimeout/socket）；
  - **Reward 重复结算**：`BattleRewardSettler` 同场 result 引用只结算一次，不同场累加（幂等验证通过）；
  - **明显持续内存上涨**：50 场后堆净增长 < 256MB，每 5 场窗口 < 128MB（无持续泄漏）。
- 验收：`tests/q31Stability.test.ts › 连续跑 50 场 Battle：无 NaN / 无抛错 / 无卡死 / 无句柄残留 / 内存有界`（运行 ~4.3s）。

---

## 10. DEV 工具关闭（PROD Hides Dev Tools）

- 统一决策点 `src/core/env.ts`：`DEV_TOOLS_VISIBLE = !IS_PROD`。
- PROD 构建隐藏：Scenario 选择器、`Runtime Debug Tools`（debugPanel 创建 + 填充 + 显隐全部门控）、对手编辑按钮（`toggle.disabled`）、Runtime Badge（branch@sha + 版本）。
- TEST 构建（内部 RC）**保留**上述工具，便于 QA 真人验收（不静默隐藏）。
- 调试日志：`DEBUG_LOG_ENABLED = !IS_PROD` → PROD 关闭 `console.debug`；`console.error`（真实错误，planckWorld.ts:384）始终保留。
- 验收：grep 确认业务层不再散用 `import.meta.env.DEV` 控制工具可见性；`npm run build` 产物中 Scenario/Debug 面板 DOM 不生成。

---

## 11. 版本号（Version）

- `APP_VERSION = '0.8.0-rc'`（`src/core/env.ts`）。
- Badge 文案：`${branch} @ ${sha.slice(0,7)} · ${APP_VERSION}`（main.ts，仅非 PROD 可见）。
- 验收：RC 包与 PROD 包 Badge 版本一致；与本文档版本号一致。

---

## 12. 已知问题（Known Issues）

1. **多分辨率无自动化用例**：§8 依赖真机/模拟器手动 smoke，未纳入 CI。
2. **插屏广告频控**：依赖 `adFrequency` 模块；PROD 无平台 adapter 时整条广告路径为 no-op（符合「不卡死」预期，非缺陷）。
3. **DamageNumberAggregator.groups**：按 `(target|source|sub)` 分组的 Map 在单场内只增不删过期组，但不同分组键数量受武器部件数上界约束（单场 < 数十），不泄漏；跨场由聚合器随战斗重建，无需清理。
4. **微信小游戏真机 IAA**：平台 adapter 与真机广告位需 RC 阶段真机验证（代码层已用 Mock/Noop 保证回退安全）。

---

## 13. 性能热点复核（Performance — 只修明显热点，禁改玩法）

本轮重点复核：Projectile / MachineGun / Flamethrower / Damage Numbers / Renderer 对象。

| 结构 | 位置 | 有界性结论 |
| --- | --- | --- |
| 武器 projectile 追踪 | `machineGunBehavior.ts`（`Set<BodyHandle>`）、`flamethrowerBehavior.ts`（`Map<BodyHandle,number>`） | 命中 / 越界 / 生命周期超时即真实 `destroyBody` 并从集合移除；每 burst/fan 存活数受节奏参数上界约束，**有界** |
| 火焰颗粒元信息 | `flamethrowerBehavior.particleMeta`（`Map`） | 与 `particles` 同步增删，**有界** |
| 伤害数字聚合 | `damageNumberAggregator.ts`（`Map`） | 分组键 = 来源+目标+部件，单场不同键受武器数上界约束；窗口到期开新组，**有界** |
| Renderer FX 数组 | `renderer.ts`（fx / sparks / muzzleFlashes / laserBeams / shotgunFans / muzzleTongues / deathFxs / hitFlashes / charges） | 每帧 `filter((x) => now - x.bornAt < x.ttl)` 按 TTL 剔除（60~900ms），**有界** |

**结论**：全部热点均为有界结构，无未释放的高速增长路径；本轮**不修改任何玩法表现**，性能结论以文档记录为准（如需进一步压测可于后续 Queue 单独立项）。

---

## 14. 验证矩阵（Verification Matrix）

| 必查项 | 测试文件 | 结果 |
| --- | --- | --- |
| Runtime Stability（50 场） | `tests/q31Stability.test.ts` | ✅ 4/4 |
| 异常恢复（6 类） | `tests/q31Recovery.test.ts` | ✅ 10/10 |
| tsc 类型 | `npm run typecheck` | ✅ 0 error |
| 全量单测 | `npm test`（vmThreads 池） | ✅ 见 CI |
| 生产构建 | `npm run build` | ✅ |
| RC 构建 | `npm run build:test` | ✅ |
| 三路 SHA | HEAD = origin = Badge | ✅ 见文末 |

---

## 15. 三路 SHA 一致性（发布前最终确认）

- 本地 HEAD：`4afbc000f82d99c03f53c694eb5f4d1eafdb185c`
- 远程 `origin/foundation-02-canonical`：`4afbc000f82d99c03f53c694eb5f4d1eafdb185c`
- Dev Server Badge（`http://127.0.0.1:5173/@id/virtual:runtime-info`）：`4afbc000f82d99c03f53c694eb5f4d1eafdb185c`

> 三者一致 → 可交付真人 RC 验收；不一致 → **禁止判定为已部署**（警惕 stale 录制：录制视频可能滞后于未重启的 Dev Server）。
