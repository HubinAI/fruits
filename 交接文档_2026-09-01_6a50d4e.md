# 《最强水果》WorkBuddy 交接文档

> 交接时间：2026-09-01 23:55（GMT+8）
> 交接人：WorkBuddy 助手 → 下一个 WorkBuddy 会话
> 本文件目的：让新窗口在**不依赖旧对话历史**的情况下，10 分钟内恢复完整上下文并继续干活。
> 配套：`/chat 新窗口交接对话指令`（见文末第 8 节，可整段复制粘贴给新窗口）。
> 上一份交接：`交接文档_2026-08-29_c3ed90d.md`（微信视口/Canvas 深水区细节，本文件不再重复，需要时直接读它）。

---

## 1. 交接快照（三路 SHA 硬校验）

| 项 | 值 |
|---|---|
| 仓库路径 | `D:\0818new\最强水果` |
| 分支 | `foundation-02-wechat` |
| **local HEAD** | `6a50d4e53a033b5fc38808e16eca44d199ae147a` |
| **remote HEAD** | `6a50d4e…`（2026-09-01 23:53 ls-remote 实测一致 ✓） |
| **线上部署** | `https://hubinai.github.io/fruits/`（= 最近一次 GitHub Pages 构建，需以 server badge / 页面资源 SHA 复核，勿凭记忆） |
| **电脑体验服务** | `http://localhost:8138/`（200 存活，引用 `dist-e2e/assets/index-DBFHz0kF.js` = 6a50d4e 最新构建，**无 stale**） |
| 工作树 | `src/`、`tests/` 干净；未跟踪仅 `.workbuddy/memory/*`、交接文档、`outputs/`、`_verify_q15recover/` —— **项目惯例，不处理、不进 commit** |
| 当前 Queue | **F-CONTENT-PACK-BROWSER-GATE-R1 已交付（6a50d4e），等「电脑录屏验收回执」** |

### 最近 Queue 时间线（自上而下）

| Queue | Commit | 内容 | 状态 |
|---|---|---|---|
| F-CONTENT-PACK-BROWSER-GATE-R1 | `6a50d4e` | 内容包浏览器集成验收 + E2E 门禁同步（本轮，纯测试层） | ✅ 已 push，等电脑录屏回执 |
| F-CONTENT-REWARD-ACQUISITION-R1-fix | `7cd8c0b` | 全量回归门稳定性：physicsLab stubGlobal 泄漏修复 + typed reward 下 garageAdjust/lossAdjust 库存断言去 body 适配 | ✅ 已交付 |
| F-CONTENT-REWARD-ACQUISITION-R1 | `442e274` | 正式奖励获取链：body/movement 单次候选池 + bodyOwnership 解锁 + Result 三态显示 + 幂等结算 | ✅ 已交付 |
| F-CONTENT-PLAYER-MOVEMENT-PACK-R1 | `d4e9e57` | Movement 内容扩充 1→4：标准/小/大/重轮组 + 拥有模型 + 双端未获得锁定 | ✅ 已交付 |
| F-CONTENT-PLAYER-BODY-PACK-R1 | `1227f2b` | 玩家正式车身 4→8：榴莲/梨子/芒果/橙子 + 拥有模型 + 双端未获得锁定 | ✅ 已交付 |
| F-CONTENT-OPPONENT-BUILD-POOL-R1 | `22230fd` | 对手池 36→49：重型/高重心首次进池 + 五类模板层 + 严格验收 | ✅ 已交付 |
| F-WX-IOS-COLD-BOOT-PREVIEW-P0 | `3b477f8` | iOS 冷启动车辆缩成极小：hLimit 软化宽度优先 | ⚠️ 已交付但 KNOWN-ISSUE 仍复现（见 KNOWN-WX-COLD-BOOT-PREVIEW-SCALE-01） |
| F-WX-IOS-RESUME-VIEWPORT-P0 | `95805d0` | iOS 切后台返回跨层错乱修复：唯一视口同步入口 `syncWechatViewport` | ✅ 已交付 |

更早的微信渲染管线基线（c3ed90d / 46d6ea1 / 1fb1153 / 575f1d0）见 `交接文档_2026-08-29_c3ed90d.md`，此处不重复。

---

## 2. 项目一句话定位

极简策略 + 事前组装 + 自动物理战斗。玩家循环：「我觉得这样能赢 → 看它自己打 → 原来问题在这 → 改一下再试」。
判断机制是否保留的唯一标准：**它是否让玩家做出不同的下一步动作？**

技术栈：TypeScript + Vite + Matter/Planck 物理 + vitest + Playwright（门禁）。目标 Web + 微信小游戏双端，platform-neutral Gameplay Runtime（`playerGameRuntime.ts`）双端共享 + platform-specific BattleHosts 注入式接入，**杜绝双端重复实现**。Logical canvas stage 固定 **844×390**。Devtools 默认打开 `dist-wechat/`。

---

## 3. 本轮核心成果（F-CONTENT-PACK-BROWSER-GATE-R1，commit 6a50d4e）

### 3.1 交付内容（纯测试层，`src/` 生产代码零修改；不部署 Pages、不生成 RC、不改数值）

1. **新增 `tests/_e2e_content_pack.cjs`**（77 断言）：A 车身 8 + B Movement 12 + C 正式奖励闭环 13 + D 三视口精简 16。
2. **`tests/_e2e_loop.cjs` / `tests/_e2e_garage_rematch.cjs`**：G 段奖励断言统一为 `(Δinv=1 ∧ Δbodies=0) ∨ (Δinv=0 ∧ Δbodies=1)` 互斥且只一次；**不预置 grantAllNewBodies**；原 session/幂等/再战断言未降低。

### 3.2 验收矩阵

| 门禁 | 结果 | 说明 |
|---|---|---|
| `_e2e_content_pack.cjs` | **77/77** | A/B/C/D 四段全绿 |
| `_e2e_loop.cjs` | **19/19** | 首跑 20/21（H 段随机匹配三连输），重跑首局即 Win → **随机匹配运气 flake，非回归** |
| `_e2e_garage_rematch.cjs` | **58/58** | 420/844/1280 三视口全覆盖 |
| targeted vitest | **88/88** | bodyPack / movementPack / rewardAcquisition / q18ContentBatch |
| `tsc --noEmit` | exit 0 | |
| 全量 vitest | **187 文件 / 1621/1621 全绿** | `--pool=vmForks --maxWorkers=1` |
| `build:e2e` | `index-DBFHz0kF.js` | 580 kB |
| bundle-clean（e2e 模式） | **PASS** | `__h/__probe/__fx` 仅 e2e 构建放行 |

**三视口**：844×390 DPR1（全量 49 断言）+ 420×210 DPR1（16）+ 1280×592 DPR1.5（16）**全部通过**。

### 3.3 内容验证要点

- **A 车身**：durian/pear/mango/orange 四新车身装备 → 战斗入画 → 包围盒/主色两两可区分 → draft 持久。
- **B Movement**：small(12) < std(20) < large(26) 逻辑轮径排序；heavy 与 std 同径但视觉胎纹可区分；取景走廊左端稳定。
- **C 奖励闭环**：body→ownedBodies+1 / movement→PartInventory+1 互斥；幂等（轮询/hide/show/连点 CTA 均不重复）；重载保持；新 session 再结算；Result 中文名/分类/星级正确。

### 3.4 E2E 侧沉淀（重要，未改产品逻辑）

1. **strip-scroll 箭头无 clamp 缺陷**：`canvasPlayerUIHost.ts` L1520-1526 步进 `garageStripScroll += step` 无 clamp（clamp 只在渲染 L2570）→ 点卡瞬间 hitArea 漂移装错卡。E2E 以 verify+重试吸收；**产品修复留后续 Queue**。
2. **轮卡 armed 路径**：move 分类点卡 → 双挂点 armed（`armGarageCard`）→ 须再点 `hp-sel:rear/front` 才装备；body 分类单目标直接装备。E2E `equipRearWheel` 用 verify + onVerifyFail 吸收。
3. **移动端 drive 槽无任何入口（披露）**：chip/entry/hp-sel:drive 均缺；Garage idle 挂点不注册命中（F-GARAGE-VISUAL-DENSITY-R2 设计）→ **站桩/卸轮真实点击不可达，属 F-GARAGE-TOUCH-ASSEMBLY-R2 范畴**。
4. **战斗动态采样噪声**：车辆实时移动 → 单帧 rect/scale/颜色统计漂移大（等越久越不稳，700ms Δx 30px、5s 收敛 98px）→ 采样改为 Active 后 150ms 立即 + belt 多帧平均（3×350ms 覆盖 ≥1.5 圈去轮子角度偏）。
5. **移动端 Garage 交互模型**：三视口全 mobile（isCompactLandscape: h<600 && aspect≥1.5）→ **无 chip:\* 命中区**；只有 `garage-cat:body|move|combat` 分类 tab + 当前选中槽横向卡带；切分类自动选中首槽（body→body / move→rearWheel）。

---

## 4. 架构地图（关键文件 → 职责）

### 内容/奖励/装配链（本轮战场）
| 文件 | 关键点 |
|---|---|
| `src/ui/canvasPlayerUIHost.ts` | L608-615 Result→Adjust 自动切 `metaPage='garage'`；L1520-1526 strip-scroll 无 clamp（缺陷）；L2570-2609 drawGarageStripCards 只注册完全可见卡；L2644-2737 drawVehicleHardpoints（idle 不注册挂点命中，HIT=30 / R=7）；L1049-1053 garageDragTargets rear/front；L1195-1240 armGarageCard；L940-945 scrollStripBy 正确 clamp（与箭头路径不对称） |
| `src/game/playerGameRuntime.ts` | L241-267 Garage 交互守卫；L871-884 adjustConfig；L909-948 finalizeBattleResult **三态 reward**（functional/movement→addPart 入 PartInventory；body→grantBody 入 ownedBodies） |
| `src/core/partInventory.ts` | L304-329 settle 幂等（settledRef）；L259-283 buildRewardCandidates/computeTypedReward |
| `src/main.ts` | L1217-1233 `__probe`（几何快照，不含 result；`__h` = CanvasPlayerUIHost 实例含 lastState） |
| `src/ui/playerUI.ts` | L26-49 BODY_OPTIONS 8 个 / MOVEMENT_OPTIONS；L123 result 字段（含 winner） |
| `src/render/viewportProfile.ts` / `src/ui/layoutProfile.ts` | isCompactLandscape（h<600 && aspect>=1.5） |

### 微信平台层（95805d0 后基线，深度细节见 c3ed90d 交接文档）
| 文件 | 关键点 |
|---|---|
| `src/platform/wechat/viewportSync.ts` | `syncWechatViewport(reason)` **唯一入口**：onShow / onWindowResize / show-retry 统一；**禁止任何路径独立改 backing 或调 runtime.doResize** |
| `wechat/game.ts` | 入口按 `windowWidth×pixelRatio` 定版 screen+ui 两块 Canvas backing；wx.onWindowResize 同步 |
| `src/platform/wechat/viewport.ts` | `WechatViewport.surface = backing ÷ dpr`（逻辑尺寸） |
| `src/render/renderer.ts` | `viewToLogical`（恒 1）统一域对齐；DPR 仅最终绘制一次 |

### 测试基建（微信侧，c3ed90d 交付）
`tests/wechatCanvasFrameHost.ts`（无 DOM 帧宿主）、`wechatSurfaceEnv.ts`（4 环境枚举）、`wechatSurfaceTransform.test.ts` / `wechatSurfaceGeometry.test.ts`（backing 定版与最终像素门禁）、`wechatCanvasStability.test.ts` / `wechatRendererClear.test.ts` / `singleLoopException.test.ts`（C3 门禁）。

---

## 5. 项目纪律与工具层坑（新会话必读）

### 5.1 运行/验证硬规则
- **vitest 必须带 `--pool`**：默认池报 `Cannot read properties of undefined (reading 'config')` 全失败（vitest 4.1.10 / Node 24 / Windows 环境故障，非代码问题）。**全量回归一律 `--pool=vmForks --maxWorkers=1`**；单文件排查可用 vmThreads。
- **三路 SHA 一致性**：local HEAD / remote（`git ls-remote origin foundation-02-wechat` 实测）/ server badge 交叉核对。Dev Server 未重启时录制内容可能滞后于最新 commit（**stale-recording 陷阱**）。
- **e2e 硬约束**：禁止 fake-viewport / fake-mouse / hitArea / `__h` / `__probe` / getImageData 捷径绕过 e2e；必须真实像素 + 实际鼠标/触摸点击。`__h/__probe/__fx` 仅 `__E2E_INTERNAL_HANDLE__` 构建注入（bundle-clean e2e 模式 allowlist 放行），普通构建零泄漏。禁止 Web 冒充 WeChat。
- **三层验收**：技术正确 → 方案落地（正式 Runtime）→ 实际体验成立（1x + Debug 关 + 玩家能感知/理解/归因/决策）。**开发完成 ≠ 体验通过 ≠ 进入正式主线**。
- **Queue 纪律**：每条 Queue 单 commit + push + 三路 SHA 核对 + stop-and-wait（等真机/录屏回执再开下一条）；禁止顺手扩展范围；investigation 与 fix queue 严格区分，前者禁 commit/push/deploy；开始前声明 frozen baseline invariants。

### 5.2 环境工具层坑（Windows + 中文路径）
- **git status 把 `planck` 显示成 `plank`（无 c）**：中文路径显示伪影，真实文件名始终是 `planck`，用 `ls <dir>/` 拿真实拼写。
- **Git Bash 中文目录路径不稳**：git 命令用裸相对路径（自寻仓库根）几乎都稳；Read 工具对中文绝对路径偶发 "Not found"，Bash `cat` 兜底。
- **git ref 写入不可靠（重要）**：commit 后分支 ref 常不落盘，update-ref/branch -f 也静默失败。**可靠恢复**：`mkdir -p .git/refs/heads/<branch> && printf '%s\n' <full-sha> > .git/refs/heads/<branch>`；commit 后必须立刻 `git log` 验证，未落盘用 fsck/cat-file 确认对象完整后手动写 ref 再 push。
- git 身份：本仓库 `xiaoyue <xiaoyue@local>`（新会话需重设）。push 走 SSH（`git@github.com:HubinAI/fruits.git`，免密可用）。
- 凭据：GCM CredentialHelperSelect dialog 根除唯一路径 = 用户 GUI 勾 `Always use this from now on`；HTTPS 非交互路径 = inline token URL。
- **微信 iOS 真机调试必读**：`.workbuddy/memory/F-WX-IOS-REALDEVICE-DEBUG-PLAYBOOK.md`（坐标域先标注 / DPR 单次应用 / 内部 rect≠最终像素 / Web≠微信≠真机 / 三层状态分开表述 / 先红后绿+模拟器+真机录屏 / 禁补偿掩盖）。

### 5.3 冲突处理
最新任务与本基线核心定位冲突时：**不自行默默实现冲突方案**，先指出「冲突点 + 体验/架构后果 + 最小建议修正」。方案漂移红线（出现即主动提醒）：复杂职业 AI / 自动瞄准 / 自动拉距 / 大量隐藏 Buff / Body 免费攻击 / Weapon-Gadget 独立槽 / 高品质全维度碾压 / 复杂机关成胜负主体 / MaxWeight 硬限制 / 动画保证命中 / Gadget 偷偷大额伤害 / 大量例外规则 / 稳定最优 Build 通吃只加 Content / Debug 成立但玩家感知不到。

---

## 6. 当前待办与下一步（新窗口开工顺序建议）

1. **F-CONTENT-PACK-BROWSER-GATE-R1 电脑录屏回执**（最高优先级，未宣布通过）：
   - 地址 `http://localhost:8138/`（200 存活，引用 `index-DBFHz0kF.js` = 6a50d4e 最新构建，无 stale，可直接录屏）。
   - 重点看：4 新车身（榴莲/梨/芒果/橙子）+ 3 新轮组（小/大/重）的装配（Garage 分类 tab + 卡带滚动）与战斗观感；正式奖励三态显示（Result 中文名/分类/星级）。
   - 收到用户回执前**不要开新 Queue**。
2. **移动端 drive 槽缺口**（已披露）：站桩/卸轮真实点击不可达 → 候选 Queue **F-GARAGE-TOUCH-ASSEMBLY-R2**（等用户排期，勿擅自启动）。
3. **strip-scroll 箭头无 clamp 缺陷**（产品修复候选，本 Queue 未动，等排期）。
4. **微信真机剩余未验**（c3ed90d 基线范围外）：Garage 体验 / UI 正式度 / 胶囊安全区 / 前后台长稳复验。
5. **KNOWN-WX-COLD-BOOT-PREVIEW-SCALE-01**：iOS 冷启动车辆极小仍复现，重启必须以真实冷启动路径为门禁（冻结 viewport/camera/hLimit/冷启动逻辑）。

---

## 7. 构建命令速查

| 命令 | 用途 |
|---|---|
| `npm run build:e2e` | E2E 门禁构建 → `dist-e2e/`（含 `__E2E_INTERNAL_HANDLE__` 句柄） |
| `npm run build:wechat` | 普通微信包（零 badge/debug/probe） |
| `npm run build:wechat:rc` | RC 候选包（SHA badge + debugGrant）→ `dist-wechat/` |
| `npm run build:wechat:rca` | 带 `[WX-VIEWPORT]/[WX-REF]/[WX-RCA]/[WX-SURF]` 诊断日志构建（排查用） |
| vitest 单文件 | `node node_modules/vitest/vitest.mjs run <file> --maxWorkers=1 --pool=vmThreads` |
| vitest 全量 | `node node_modules/vitest/vitest.mjs run --pool=vmForks --maxWorkers=1` |
| bundle-clean | `node scripts/check-wechat-bundle-clean.js <bundle> e2e`（或 rc） |
| repo-health | `node scripts/repo-health.js` |

---

## 8. 新窗口交接对话指令（复制粘贴给新窗口）

```
你是《最强水果》微信小游戏项目的接续会话。先读交接文档恢复上下文，再听用户指令，不要臆测改码。

【第一步：恢复上下文（按顺序执行）】
1. 读 `D:\0818new\最强水果\交接文档_2026-09-01_6a50d4e.md`（全文，本交接）。
2. 读 `D:\0818new\最强水果\.workbuddy\memory\MEMORY.md`（长期基线）与 `.workbuddy\memory\2026-09-01.md`（最近工作日志，含 F-CONTENT-PACK-BROWSER-GATE-R1 完整记录）。
3. 若任务涉及微信/iOS/真机/Canvas/DPR/视口：先读 `.workbuddy\memory\F-WX-IOS-REALDEVICE-DEBUG-PLAYBOOK.md`；微信视口/Canvas 深水区另读 `交接文档_2026-08-29_c3ed90d.md` 第 3/4 节。
4. 用 bash 核对三路 SHA：`git -C "D:/0818new/最强水果" rev-parse HEAD` 与 `git ls-remote origin foundation-02-wechat`，二者必须等于 6a50d4e53a033b5fc38808e16eca44d199ae147a；再确认 src/、tests/ 工作树干净（未跟踪的 .workbuddy/memory/*、交接文档、outputs/、_verify_q15recover/ 是项目惯例，不处理不进 commit）。
5. 向用户复述交接快照（分支/HEAD/当前 Queue/待办），确认后再开工。

【硬纪律（违反即返工）】
- 当前分支 foundation-02-wechat，禁止开新主线；src/ 生产代码零修改是本轮交付的铁证，未经用户指令不得触碰内容/数值/物理。
- vitest 全量必须带 --pool=vmForks --maxWorkers=1；每条 Queue 单 commit + push + 三路 SHA 核对 + stop-and-wait，禁止顺手扩展范围。
- 禁止 fake-viewport/fake-mouse/hitArea/__h/__probe/getImageData 绕过 e2e（句柄仅 e2e 构建注入，bundle-clean 门禁验证）；禁止用 Web 构建冒充 WeChat。
- 先调查后修改；investigation 与 fix queue 区分，investigation 禁 commit/push/deploy。
- 微信底层修复：先红后绿 + 模拟器亲检 + 真机录屏（可见 SHA）；禁止用字号/gap/offset/局部 DPR 补偿掩盖全局坐标错误。
- git 身份需重设：git config user.name xiaoyue / user.email xiaoyue@local（commit 后立刻 git log 验证 ref 落盘，未落盘按 MEMORY.md 手动写 ref）。

【当前最高优先级】
F-CONTENT-PACK-BROWSER-GATE-R1 电脑录屏验收（http://localhost:8138/，引用 index-DBFHz0kF.js = 6a50d4e，无 stale）：重点看 4 新车身/3 新轮组装配与战斗观感、正式奖励三态显示。收到用户录屏回执前，不要开新 Queue。已披露待办：移动端 drive 槽缺口（候选 F-GARAGE-TOUCH-ASSEMBLY-R2）、strip-scroll 箭头无 clamp 缺陷、微信真机剩余未验项（Garage/UI 正式度/胶囊安全区/前后台长稳）、KNOWN-WX-COLD-BOOT-PREVIEW-SCALE-01——均等用户排期，勿擅自启动。
```

---

*本交接文档为项目惯例产物（不纳入 git 提交）。历史交接：`交接文档_2026-08-29_c3ed90d.md`、`交接文档_2026-08-28_f50cb1e.md`、`HANDOFF_2026-08-23.md`、`HANDOFF_2026-08-22.md`。*
